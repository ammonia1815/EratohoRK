;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;		[ここに구상主名を入れる]구상 その他이벤트구상ファイル
;
;	대만족보너스、本日の방침이벤트구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV16, 대만족보너스구상
;========================================================================
;・@EVENTTRAINから呼び出される
;・대만족보너스を得ている場合、죠교開始時に表示される
;・タイミングは죠교開始구상(EV11)の後、죠교자행동前구상(EV12)の前
;-------------------------------------------------
@KOJO_EVENT_KX2_16(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0.선택前/1.선택後)
;-------------------------------------------------

;────────────────────────────────────
;선택前구상
;────────────────────────────────────
;・上機嫌な様子で、금일は何を당했다いかとたずねる
;・구상表示前に地の文(%CALLNAME:TARGET%님 대만족 보너스)
;・구상表示の後に地の文(%CALLNAME:TARGET%は上機嫌な様子で、금일は何を당했다いかとたずねてきた)が続く
;・地の文の後に선택肢が表示される
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;대만족보너스선택前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 0
	;=================================================
	;初回
	;=================================================
	IF 구상記入チェック(0) && FIRSTEVENT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;선택後구상
;────────────────────────────────────
;・죠교대상の返答に対して、一言
;・구상表示に後に地の文(%CALLNAME:TARGET%は%CALLNAME:MASTER%의 返事を聞くと죠교の準備を始めた)が続く
;・上記の地の文を表示했다後に、선택肢毎の地の文が表示される
;・휴식が선택당했다場合はEV12, ご褒美による中止に続く
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;대만족보너스선택後
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 1
	;=================================================
	;언제나처럼
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("통상")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;항문을괴롭혀
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("항문")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;선택する事によって(TFLAG:징계・플래그)が立つ
	;=================================================
	;징계해
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("징계・")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;격렬하게
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("하드")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;응석부리게
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("イチャイチャ")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;망가질때까지
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("이상")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;개목걸이の所持が条件
	;=================================================
	;밖에서해
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("야외")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;호의+호감도が10000以上で可能になる
	;=================================================
	;임신해
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("애만들기")
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;죠교대상の소모に関わらず선택可能
	;=================================================
	;쉬게
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("휴식")
		;죠교대상が소모している
		IF CFLAG:MASTER:소모 >= 3
;OriginalString : 
			PRINTFORMW 
		;実はそれほど疲れていない
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;호의+호감도が10000以上で可能になる
	;=================================================
	;안아줬으면
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("抱いて")
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF

	;반항각인なし、죠교대상からの호감도1000以上で可能になる
	;=================================================
	;봉사하게
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("ご봉사")
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF

	;호의2000、M호의1000から선택可能
	;=================================================
	;かわいがって
	;=================================================
	IF 구상記入チェック(0) && IS_SATIS_BONUS("귀여워해")
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;========================================================================
;EV17, 本日の방침구상
;========================================================================
;・@EVENTTRAINから呼び出される
;・タイミングは죠교開始구상(EV11)の後、죠교자행동前구상(EV12)の前
;・대만족보너스を得ている場合はそちらが우선される(即ち、この箇所が呼び出される事はない)
;・죠교자により本日の방침が決定당했다場合に表示
;・구상表示の後にPRINTLを挟んで地の文。地の文は방침毎に専用のものになる
;・애만들기、휴식はご褒美でしか出ないのでこの箇所では不要
;・Ａ감각が4までは確率（Ａ감각、0で1/3）でTFLAG:금일의방침 == 1（Ａ開発）になり1/5の確率で他の방침によって上書きされる
;・Ａ감각が5以降になると금일의방침 == 1（Ａ開発）になる可能性は無くなる（감각が上昇すればＡ開発の필요はない、という仕様？）
;-------------------------------------------------
@KOJO_EVENT_KX2_17(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(現状、派生なし)
;-------------------------------------------------

;一行空ける(この손の処理は最終的にはシステム側で賄いたいと考える)
;OriginalString : 
PRINTL

;ABL:MASTER:Ａ감각 < 5の条件の時のみ発生する可能性がある
;Ａ감각の上昇に伴い発生確率が落ちてゆき（0=1/3, 1=1/4, 2=1/6. 3=1/8. 4=1/12。さらに1/5で他の방침で上書き）、Ａ감각5以上では完全に発生しなくなる
;━━━━━━━━━━━━━━━━━━━━━━━━
;방침が항문開発
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_POLICY("금일", "항문")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;방침が징계・
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_POLICY("금일", "징계・")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;방침がいつもより激しく
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_POLICY("금일", "하드")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;방침がイチャイチャ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_POLICY("금일", "イチャイチャ")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;방침が이상
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_POLICY("금일", "이상")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;방침が야외
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_POLICY("금일", "야외")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF
