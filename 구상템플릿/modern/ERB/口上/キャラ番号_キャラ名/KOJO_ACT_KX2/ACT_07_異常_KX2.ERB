;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[ここに구상主名を入れる]구상 이상系アクション구상ファイル
;
;	이상系アクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;이상系 ACT70～75
;========================================================================
;・TFLAG:ACT(70=피스트퍽/71=애널피스트/72=양쪽피스트/73=방치플레이/74=공중육변기플레이/75=페니스밴드장착)
;・TFLAG:ACT분류 == 7に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "이상")で参照する事が可能
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では구상側のRESULT(終端のRETURN 1など)によって구상が表示당했다のか否か、
;・また箇所毎の特有の挙動(미약等구상をスキップなど)を制御しています
;・@구상記入チェックの場合代入処理を省略できますが、RESULT値は구상側で決めうちする필요があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------

;────────────────────────────────────
;ACT70, 피스트퍽
;────────────────────────────────────
;・죠교대상の膣に拳を入れる
;・ACT開始時点で손を出している
;・죠교대상が처녀である場合は처녀상실となる(@ACTION_APPLY2_70で処理される)
;・죠교대상が남자である場合は発生しない
;・턴방침が"소프트", "노멀"の場合は発生しない
;・本日の방침が"イチャイチャ"の場合は発生しない
;・성교중/성교봉사중には発生しない
;・안면승마系、자위系は解除される
;-------------------------------------------------
@KOJO_ACT_KX2_70
;-------------------------------------------------
;・ACTの派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT70, 피스트퍽
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文に分岐なし
;-------------------------------------------------
IF 구상記入チェック(0)
	
	;地の文↓
	;%CALLNAME:TARGET%は%CALLNAME:MASTER%의 膣に拳で侵入し始めた…
	
	;初回判定のいずれかが消費당했다場合、以後、その分岐は既読となり表示される事はありません
	;=================================================
	;初回「죠교대상が처녀」
	;=================================================
	IF 구상記入チェック(0) && TALENT:MASTER:처녀 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;初回「죠교대상が처녀ではない」
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT71, 애널피스트
;────────────────────────────────────
;・죠교대상の엉덩이に拳を入れる
;・ACT開始時点で손を出している
;・죠교대상が남자である場合は発生しない
;・턴방침が"소프트", "노멀"の場合は発生しない
;・本日の방침が"イチャイチャ"の場合は発生しない
;・성교중/성교봉사중には発生しない
;・안면승마系、자위系は解除される
;-------------------------------------------------
@KOJO_ACT_KX2_71
;-------------------------------------------------
;・ACTの派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT71, 애널피스트
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文に分岐なし
;-------------------------------------------------
IF 구상記入チェック(0)
	
	;地の文↓
	;%CALLNAME:TARGET%は%CALLNAME:MASTER%의 항문に拳で侵入し始めた…
	
	;=================================================
	;初回
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT72, 양쪽피스트
;────────────────────────────────────
;・죠교대상の膣と엉덩이に拳を入れる
;・ACT開始時点で손を出している
;・죠교대상が처녀である場合は처녀상실となる(@ACTION_APPLY2_72で処理される)
;・죠교대상が남자である場合は発生しない
;・턴방침が"소프트", "노멀", "하드"の場合は発生しない
;・本日の방침が"イチャイチャ"の場合は発生しない
;・성교중/성교봉사중には発生しない
;・안면승마系、자위系は解除される
;-------------------------------------------------
@KOJO_ACT_KX2_72
;-------------------------------------------------
;・ACTの派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT72, 양쪽피스트
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文に分岐なし
;-------------------------------------------------
IF 구상記入チェック(0)
	
	;地の文↓
	;%CALLNAME:TARGET%は%CALLNAME:MASTER%의 膣と항문に拳で侵入し始めた…
	
	;初回判定のいずれかが消費당했다場合、以後、その分岐は既読となり表示される事はありません
	;=================================================
	;初回「죠교대상が처녀」
	;=================================================
	IF 구상記入チェック(0) && TALENT:MASTER:처녀 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF

	;=================================================
	;初回「죠교대상が처녀ではない」
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT73, 방치플레이
;────────────────────────────────────
;・죠교자が죠교대상を방치する
;・턴방침が"소프트", "노멀", "이상"だと発生しない
;・(이상系ACTで턴방침"이상"で弾いているのはミスと思われ)
;・금일의방침が"イチャイチャ"の場合は発生しない
;・밀어넘어지는중には発生しない
;・성교중/성교봉사중には発生しない
;・수치系から発生する場合もある(v1.010現在、수치系から派生했다場合にのみ発生と思われ)
;・안면승마系は解除される
;-------------------------------------------------
@KOJO_ACT_KX2_73
;-------------------------------------------------
;・ACTの派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT73, 방치플레이
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文に分岐なし
;-------------------------------------------------
IF 구상記入チェック(0)
	
	;地の文↓
	;%CALLNAME:TARGET%はただ%CALLNAME:MASTER%を방치してなにもしない…
	
	;=================================================
	;죠교자자위중
	;=================================================
	IF 구상記入チェック(0) && GETBIT(TEQUIP:자위중, 1)
		;죠교대상も자위중
		IF GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
			PRINTFORMW 
		;죠교자のみ자위중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상자위중
	;=================================================
	IF 구상記入チェック(0) && GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;TEQUIP系分岐は눈가리개以外にも바이브、항문바이브、항문비즈等が考えられます
	;필요に応じて拡張してください
	;=================================================
	;눈가리개장착中
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:눈가리개
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT74, 공중육변기플레이
;────────────────────────────────────
;・縛られた죠교대상を外に방치する
;・방치され、発見당했다場合죠교대상が처녀であれば처녀상실。동정の場合は동정상실になる
;・밧줄と개목걸이が揃っていないと発生しない
;・턴방침が"소프트", "노멀", "하드"の場合は発生しない
;・금일의방침が"イチャイチャ"だと発生しない
;・밀어넘어지는중には発生しない
;・성교중/성교봉사중には発生しない
;・REACT분류が"용서를 빌다"であった場合は취소される
;-------------------------------------------------
@KOJO_ACT_KX2_74
;-------------------------------------------------
;・TFLAG:ACT파생(0=無事/1=애원/2=陵辱/3=興奮)
;・죠교대상の反応と確率でREACT以降(ACTION_APPLY2)で派生する
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT73, 공중육변기플레이
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文に分岐なし
;-------------------------------------------------
IF 구상記入チェック(0)
	
	;地の文↓
	;%CALLNAME:TARGET%は外で縛られた%CALLNAME:MASTER%의 お腹にマジックで「↓공중변소」と書いて、
	;その場に置いて一人で帰ろうと했다…
	
	;=================================================
	;죠교대상が처녀
	;=================================================
	IF 구상記入チェック(0) && TALENT:MASTER:처녀
		;初回
		IF FIRSTACT(0)
;OriginalString : 
			PRINTFORMW 
		;初回時に애원により취소당했다場合、もしくは無事だった場合
		;この構造では、처녀を失うまでこの"ELSE"の箇所で処理される
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상が동정
	;=================================================
	IF 구상記入チェック(0) && TALENT:MASTER:동정
		;初回
		IF FIRSTACT(0)
;OriginalString : 
			PRINTFORMW 
		;初回時に애원により취소당했다場合、もしくは無事だった場合
		;この構造では、동정を失うまでこの"ELSE"の箇所で処理される
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;처녀、동정以外の場合の初回
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT75, 페니스밴드장착
;────────────────────────────────────
;・죠교자が双頭페니스밴드を身に着け、죠교대상に見せつける
;・죠교자が처녀である場合처녀상실になる
;・성교중/성교봉사중には発生しない
;・죠교자に페니스がある場合には発生しない
;・既に장착していた場合は解除となる
;・안면승마系は解除される
;-------------------------------------------------
@KOJO_ACT_KX2_75
;-------------------------------------------------
;・TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT75_0, 페니스밴드장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;・페니스밴드装備中は쾌Ｃが쾌Ｖに変換
;・変換率…쾌Ｃを20％にし、残りの値にＶ감각+1/Ｃ감각+1を掛けて２で割って쾌Ｖに加算する
;・地の文はACT파생、及び처녀であるか否かで分岐する
;-------------------------------------------------
IF 구상記入チェック(0) && !TFLAG:ACT파생
	;죠교자の初期設定が처녀でない場合は(基本的に)不要な箇所です
	;FIRSTACTの이벤트번호を揃えて、처녀以外の場合と重複しないようになっていますが
	;この箇所が필요でない場合は削除してしまった方が良いと思います
	;=================================================
	;初回「죠교자が처녀」
	;=================================================
	IF 구상記入チェック(0) && TALENT:TARGET:처녀 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;죠교자が처녀でない場合はここで初回判定をします
	;=================================================
	;初回「죠교자が처녀でない場合」
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT75_1, 페니스밴드解除
;━━━━━━━━━━━━━━━━━━━━━━━━
;・身に着けている페니스밴드を取り外す
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
