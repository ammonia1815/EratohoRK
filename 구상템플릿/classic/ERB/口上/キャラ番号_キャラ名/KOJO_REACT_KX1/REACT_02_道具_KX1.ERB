;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[ここに구상主名を入れる]구상 도구系個別リアクション구상ファイル
;
;	도구系リアクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;도구系 REACT20～26
;========================================================================
;・各REACT内の分け方としてTFLAG:REACT분류(IS_COMGRONAME)やSELECTCOM(IS_NOWCOMNAME)が考えられます
;・ただ、範囲の広い条件で拾ってしまうのは基本的にお勧めしません
;・ACT분류:도구系の場合、基本的に애무系COM(COM10~COM16)、고통系COM(COM30~COM33)が선택肢となりますが
;・状況によってはその他のものが선택肢に含まれる可能性もあります
;・リアクション(REACT)で処理されなかったものは汎用リアクション(COM)で補完されるので
;・無理に全て埋める필요はありません
;・필요と思うものを絞り込み、COMとの連携を考えて効率よく構築すべきです
;-------------------------------------------------
;・各REACT及びACT파생内は、&& IS_NOWCOMNAMEなり、&& IS_COMGRONAME等の条件で決めうちしましょう
;・ここに条件無し分岐があると、リアクションに関わらず、全て拾ってしまって汎用リアクションに流れません
;・죠교대상の反応に関わらず、同じリアクションしかしなくなってしまいます
;・要するにこれは、想定당했다システムの破壊に繋がります
;-------------------------------------------------
;　REACTの地の文はACT_MESSAGE.ERBのTRAIN_MESSAGE2_{TFLAG:ACT}で処理されます
;　REACTによっては専用処理が存在せず、@REACTION_MESSAGEでの汎用処理のみとなっています
;　基本的に地の文はTFLAG:REACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐し、
;　各REACT분류内でTFLAG:REACT부하(自然な행동/無理のない행동/無理を했다행동)で派生
;　ACT_MESSAGE.ERB REACTION_MESSAGE.ERBを参照하면서の作業をお勧めします
;-------------------------------------------------
;・各REACTに於ける分岐は필요に応じて増減して使用して下さい
;-------------------------------------------------

;────────────────────────────────────
;REACT20, 로터
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM…
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_KX1_20
;-------------------------------------------------
;・地の文はREACT분류(受け入れる/적극적으로 한다, 태연하게 한다/용서를 빌다/소극적으로 한다)で分岐する
;・REACT분류:受け入れるはTCVAR:MASTER:가버려か否かで分岐する
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;快感に부끄러워한다
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「状況に부끄러워한다」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;派生1の場合はREACT분류:날뛴다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;嫌がって妨害했다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「싫어한다がACTは妨害せず」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고 모욕한다
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("서투르다고 모욕한다")
	;=================================================
	;快感を感じながら(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「実際に下손な場合」(REACT분류:태연하게 한다)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;派生1の場合はREACT분류:용서를 빌다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「耐久」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!(REACT분류:受け入れる)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;暴れ失敗(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;밧줄구속による暴れ失敗
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
		[SKIPEND]
		
		;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1, 2を書いた場合はREACT파생0,「暴れ成功」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT20,로터

;────────────────────────────────────
;REACT21, 바이브・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
;想定されるCOM…
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;その他反応
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_REACT_KX1_21
;-------------------------------------------------
;・장착時、죠교대상が후타나리の場合にのみ固有の地の文
;・他の条件の場合はREACTION_MESSAGEで処理されるのみ
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;바이브장착
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;MASTERが처녀なら、처녀상실が起こる。この処理は@ACTION_APPLY2_21にて行われる
	;=================================================
	;죠교대상が처녀상실
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:MASTER:처녀상실
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;-------------------------------------------------
		;快感に부끄러워한다
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「状況に부끄러워한다」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1の場合はREACT분류:날뛴다扱い
	;=================================================
	;COM11, 싫어한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;-------------------------------------------------
		;嫌がって妨害했다(REACT분류:날뛴다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「싫어한다がACTは妨害せず」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF

	;派生1の場合はREACT분류:용서를 빌다扱い
	;=================================================
	;COM14, 쾌감을 참는다(소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("쾌감을 참는다")
		;-------------------------------------------------
		;애원(REACT분류:용서를 빌다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「耐久」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포による悲鳴
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;暴れ失敗(REACT분류:소극적으로 한다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※内容を書き分けるならコメントアウトを解除して使用して下さい
			;밧줄구속による暴れ失敗
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
;OriginalString : 
				PRINTFORMW
			ENDIF
			RETURN LOCAL:2
			[SKIPEND]
			
			;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1, 2を書いた場合はREACT파생0,「暴れ成功」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("도구를 제거한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;派生0, 解除を参考に필요な分岐をコピペして使用して下さい
;방치して汎用に丸投げというのも、まぁアリだとは思います
;━━━━━━━━━━━━━━━━━━━━━━━━
;바이브解除
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT21, 바이브

;────────────────────────────────────
;REACT22, 항문바이브・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다/무서워한다
;その他反応
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_REACT_KX1_22
;-------------------------------------------------
;・장착時、죠교대상が남자の場合にのみ地の文변화
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;항문바이브장착
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("용서를 빌다")
		;派生1の場合はREACT분류:なし
		;-------------------------------------------------
		;意見を出すなんていい度가슴だね！(징계・포인트加算)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;-------------------------------------------------
		;快感に부끄러워한다
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「状況に부끄러워한다」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1の場合はREACT분류:날뛴다扱い
	;=================================================
	;COM11, 싫어한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;-------------------------------------------------
		;嫌がって妨害했다(REACT분류:날뛴다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「싫어한다がACTは妨害せず」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF

	;派生1の場合はREACT분류:용서를 빌다扱い
	;=================================================
	;COM14, 쾌감을 참는다(소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("쾌감을 참는다")
		;-------------------------------------------------
		;애원(REACT분류:용서를 빌다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「耐久」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("기분 좋다!")
		;-------------------------------------------------
		;공포による悲鳴
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;暴れ失敗(REACT분류:소극적으로 한다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※内容を書き分けるならコメントアウトを解除して使用して下さい
			;밧줄구속による暴れ失敗
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
;OriginalString : 
				PRINTFORMW
			ENDIF
			RETURN LOCAL:2
			[SKIPEND]
			
			;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1, 2を書いた場合はREACT파생0,「暴れ成功」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM33, 무서워한다(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("무서워한다")
		;-------------------------------------------------
		;징계・포인트加算(REACT분류:なし)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF


	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("도구를 제거한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;COM:도구를 제거한다ではなく、あくまでも죠교자による解除であることに注意
;派生0, 解除を参考に필요な分岐をコピペして使用して下さい
;방치して汎用に丸投げというのも、まぁアリだとは思います
;━━━━━━━━━━━━━━━━━━━━━━━━
;항문바이브解除
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT22, 항문바이브

;────────────────────────────────────
;REACT23, 항문비즈・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=장착/1=解除/2=追加)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다/무서워한다
;その他反応
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_REACT_KX1_23
;-------------------------------------------------
;・地の文は解除時のみREACT분류(受け入れる/적극적으로 한다, 태연하게 한다/용서를 빌다/소극적으로 한다)で分岐する
;・장착、追加時には固有の地の文なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;항문비즈장착
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("용서를 빌다")
		;派生1の場合はREACT분류:なし
		;-------------------------------------------------
		;意見を出すなんていい度가슴だね！(징계・포인트加算)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;-------------------------------------------------
		;快感に부끄러워한다
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「状況に부끄러워한다」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1の場合はREACT분류:날뛴다扱い
	;=================================================
	;COM11, 싫어한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;-------------------------------------------------
		;嫌がって妨害했다(REACT분류:날뛴다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「싫어한다がACTは妨害せず」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1の場合はREACT분류:용서를 빌다扱い
	;=================================================
	;COM14, 쾌감을 참는다(소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("쾌감을 참는다")
		;-------------------------------------------------
		;애원(REACT분류:용서를 빌다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「耐久」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포による悲鳴
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;暴れ失敗(REACT분류:소극적으로 한다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※内容を書き分けるならコメントアウトを解除して使用して下さい
			;밧줄구속による暴れ失敗
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
;OriginalString : 
				PRINTFORMW
			ENDIF
			RETURN LOCAL:2
			[SKIPEND]
			
			;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1, 2を書いた場合はREACT파생0,「暴れ成功」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF

	;=================================================
	;COM33, 무서워한다(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("무서워한다")
		;-------------------------------------------------
		;派生：怯えに対する초조함
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("도구를 제거한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;REACT분류を一部のCOMで決めうち했다形
;条件から漏れたものは汎用リアクション(COM)に
;COM:도구를 제거한다ではなく、あくまでも죠교자による解除であることに注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;항문비즈解除
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;REACT분류, 受け入れる
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_COMGRONAME("受け入れる") && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋다!")
		;以下、消費기력による派生
		;-------------------------------------------------
		;無理を했다행동
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GET_COM_RANK() == "無理を했다행동"
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;無理のない행동
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GET_COM_RANK() == "無理のない행동"
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;負荷1と2を書いた場合は負荷0(自然な행동)として機能します
		;書き分けしない場合は派生内容に関わらず、この箇所の補完の役目を果たします
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;REACT분류, 태연하게 한다
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("태연하게 한다") && IS_NOWCOMNAME("겁없이 웃는다")
		;以下、消費기력による派生
		;-------------------------------------------------
		;無理を했다행동
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GET_COM_RANK() == "無理を했다행동"
			;実は気持ちいい
			IF COMCOR_POSI()
;OriginalString : 
				PRINTFORMW 
			;実は嫌
			ELSE
;OriginalString : 
				PRINTFORMW 
			ENDIF
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;無理のない행동
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GET_COM_RANK() == "無理のない행동"
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;負荷1と2を書いた場合は負荷0(自然な행동)として機能します
		;書き分けしない場合は派生内容に関わらず、この箇所の補完の役目を果たします
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;COM7:용서를 빌다派生0, COM14:쾌감을 참는다派生1を想定
	;=================================================
	;REACT분류, 용서를 빌다
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_COMGRONAME("용서를 빌다") && IS_NOWCOMNAME("용서를 빌다/쾌감을 참는다/무서워한다")
		;以下、消費기력による派生
		;-------------------------------------------------
		;無理を했다행동
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GET_COM_RANK() == "無理を했다행동"
			;実は気持ちいい
			IF COMCOR_POSI()
;OriginalString : 
				PRINTFORMW 
			;実は嫌
			ELSE
;OriginalString : 
				PRINTFORMW 
			ENDIF
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;無理のない행동
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GET_COM_RANK() == "無理のない행동"
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;負荷1と2を書いた場合は負荷0(自然な행동)として機能します
		;書き分けしない場合は派生内容に関わらず、この箇所の補完の役目を果たします
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;REACT분류, 소극적으로 한다
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_COMGRONAME("소극적으로 한다") && IS_NOWCOMNAME("부끄러워한다/쾌감을 참는다")
		;以下、消費기력による派生
		;-------------------------------------------------
		;無理を했다행동
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GET_COM_RANK() == "無理を했다행동"
			;実は気持ちいい
			IF COMCOR_POSI()
;OriginalString : 
				PRINTFORMW 
			;実は嫌
			ELSE
;OriginalString : 
				PRINTFORMW 
			ENDIF
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;無理のない행동
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GET_COM_RANK() == "無理のない행동"
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;負荷1と2を書いた場合は負荷0(自然な행동)として機能します
		;書き分けしない場合は派生内容に関わらず、この箇所の補完の役目を果たします
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;派生0,派生1を参考に필요な分岐を追加して使用して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;항문비즈追加
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT23, 항문비즈

;────────────────────────────────────
;REACT24, Ｃ애무도구・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=클리캡장착/1=클리캡解除/2=오나홀장착/3=오나홀解除)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:날뛴다
;その他反応
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_REACT_KX1_24
;-------------------------------------------------
;・地の文はREACTION_MESSAGEにてCOM毎に汎用処理
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;클리캡장착
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM6, 더 하드하게 해(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("더 하드하게 해")
		;-------------------------------------------------
		;「この恥知らずのメス豚め！」(징계・포인트加算)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;-------------------------------------------------
		;快感に부끄러워한다
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「状況に부끄러워한다」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1の場合はREACT분류:날뛴다扱い
	;=================================================
	;COM11, 싫어한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;-------------------------------------------------
		;嫌がって妨害했다(REACT분류:날뛴다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「싫어한다がACTは妨害せず」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1の場合はREACT분류:용서를 빌다扱い
	;=================================================
	;COM14, 쾌감을 참는다(소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
		;-------------------------------------------------
		;애원(REACT분류:용서를 빌다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「耐久」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;暴れ失敗(REACT분류:소극적으로 한다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			;밧줄구속による暴れ失敗
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
;OriginalString : 
				PRINTFORMW
			ENDIF
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("도구를 제거한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;COM:도구를 제거한다ではなく、あくまでも죠교자による解除であることに注意
;ACT파생0を参考に필요な分岐を追加して使用して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;클리캡解除
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;ACT파생0を参考に필요な分岐を追加して使用して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;오나홀장착
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;COM:도구를 제거한다ではなく、あくまでも죠교자による解除であることに注意
;ACT파생0を参考に필요な分岐を追加して使用して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;오나홀解除
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생 == 3
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT24, Ｃ애무도구

;────────────────────────────────────
;REACT25, 유두캡・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:날뛴다
;その他反応
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_REACT_KX1_25
;-------------------------------------------------
;・地の文はREACTION_MESSAGEにてCOM毎に汎用処理
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;유두캡장착
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM6, 더 하드하게 해(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("더 하드하게 해")
		;-------------------------------------------------
		;「この恥知らずのメス豚め！」(징계・포인트加算)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;-------------------------------------------------
		;快感に부끄러워한다
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「状況に부끄러워한다」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1の場合はREACT분류:날뛴다扱い
	;=================================================
	;COM11, 싫어한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;-------------------------------------------------
		;派生：嫌がってACT妨害(REACT분류:날뛴다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「싫어한다がACTは妨害せず」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1の場合はREACT분류:용서를 빌다扱い
	;=================================================
	;COM14, 쾌감을 참는다(소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
		;-------------------------------------------------
		;派生：애원(REACT분류:용서를 빌다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「耐久」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;暴れ失敗(REACT분류:소극적으로 한다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※内容を書き分けるならコメントアウトを解除して使用して下さい
			;밧줄구속による暴れ失敗
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
;OriginalString : 
				PRINTFORMW
			ENDIF
			RETURN LOCAL:2
			[SKIPEND]
			
			;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1, 2を書いた場合はREACT파생0,「暴れ成功」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("도구를 제거한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;COM:도구를 제거한다ではなく、あくまでも죠교자による解除であることに注意
;ACT파생0を参考に필요な分岐を追加して使用して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;유두캡解除
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT25, 유두캡

;────────────────────────────────────
;REACT26, 페니스밴드・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:용서를 빌다/기분좋게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;その他反応
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_REACT_KX1_26
;-------------------------------------------------
;・地の文はREACTION_MESSAGEにてCOM毎に汎用処理
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;페니스밴드장착
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 0
	;MASTERが처녀なら、처녀상실が起こる。この処理は@ACTION_APPLY2_26にて行われる
	;=================================================
	;죠교대상が처녀상실
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:MASTER:처녀상실
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;-------------------------------------------------
		;派生：快感に부끄러워한다
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「状況に부끄러워한다」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1の場合はREACT분류:날뛴다扱い
	;=================================================
	;COM11, 싫어한다(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;-------------------------------------------------
		;嫌がって妨害했다(REACT분류:날뛴다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「싫어한다がACTは妨害せず」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF

	;派生1の場合はREACT분류:용서를 빌다扱い
	;=================================================
	;COM14, 쾌감을 참는다(소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
		;-------------------------------------------------
		;派生：애원(REACT분류:용서를 빌다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「耐久」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포による悲鳴
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;暴れ失敗(REACT분류:소극적으로 한다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※内容を書き分けるならコメントアウトを解除して使用して下さい
			;밧줄구속による暴れ失敗
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
;OriginalString : 
				PRINTFORMW 
			ENDIF
			RETURN LOCAL:2
			[SKIPEND]
			
			;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1, 2を書いた場合はREACT파생0,「暴れ成功」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("도구를 제거한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;COM:도구를 제거한다ではなく、あくまでも죠교자による解除であることに注意
;ACT파생0を参考に필요な分岐を追加して使用して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;페니스밴드解除
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT26, 페니스밴드
