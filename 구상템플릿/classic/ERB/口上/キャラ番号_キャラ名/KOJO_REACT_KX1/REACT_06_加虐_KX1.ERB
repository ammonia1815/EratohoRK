;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[ここに구상主名を入れる]구상 가학系個別リアクション구상ファイル
;
;	가학系リアクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;가학系 REACT60～68
;========================================================================
;・各REACT内の分け方としてTFLAG:REACT분류(IS_COMGRONAME)やSELECTCOM(IS_NOWCOMNAME)が考えられます
;・ただ、範囲の広い条件で拾ってしまうのは基本的にお勧めしません
;・ACT분류:가학系の場合、고통系COM(COM30~COM32)を中心に、회화系、애무系の一部が선택肢となりますが
;・状況によってはその他のものが선택肢に含まれる可能性もあります
;・リアクション(REACT)で処理されなかったものは汎用リアクション(COM)で補完されるので
;・無理に全て埋める필요はありません
;・필요と思うものを絞り込み、COMとの連携を考えて効率よく構築すべきです
;-------------------------------------------------
;※지속効果のあるTEQUIPで애무系反応が선택肢となる場合があります
;-------------------------------------------------
;・各REACT内は、&& IS_NOWCOMNAMEなり、&& IS_COMGRONAME等の条件で決めうちしましょう
;・ここに条件無し分岐があると、全て拾ってしまって汎用リアクションに流れません
;・죠교대상の反応に関わらず、同じリアクションしかしなくなってしまいます
;・要するにこれは、想定당했다システムの破壊に繋がります
;-------------------------------------------------
;　가학系の場合、REACT毎の個別の地の文は存在せず、REACTION_MESSAGEでCOM毎に汎用処理されるのみです
;　REACTION_MESSAGE.ERBを参照하면서の作業をお勧めします
;-------------------------------------------------
;各REACTでは想定される反応を網羅してあります。필요に応じて分岐を増減して使用して下さい
;COMによってはREACT파생でREACT분류が변화する事に注意
;各COMの派生は基本的にREACT파생1を条件決めうちの小分岐にし、REACT파생0はその他扱いで条件なしとしています
;손間を掛けたくない場合はその他のみを書いて、派生に関わらず当該箇所を補完する事ができます
;またIF LOCAL:1 && TFLAG:REACT파생 < 1とする事で派生했다場合は汎用リアクションへ流すなどという事も可能です
;派生は将来増える可能性もあるので != 1などとするよりは < 1と했다方が無難であると考えます
;-------------------------------------------------

;────────────────────────────────────
;REACT60, 스펭킹・個別リアクション
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:더 하드하게 해/용서를 빌다
;애무系反応
;	COM:겁없이 웃는다
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_REACT_KX1_60
;-------------------------------------------------
;・固有の地の文なし。죠교대상の反応に応じてREACTION_MESSAGEで汎用処理
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해(REACT분류:受け入れる)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;「この恥知らずの豚め！」(징계・포인트加算)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다(REACT분류:용서를 빌다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("용서를 빌다")
	;派生1の場合はREACT분류:なし
	;=================================================
	;意見を出すなんていい度가슴だね！(징계・포인트加算)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 고통을 참는다(REACT분류:受け入れる)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다(REACT분류:受け入れる)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("비명지른다")
	;=================================================
	;공포による悲鳴
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
;REACT분류が날뛴다であった場合は성교は취소になる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;暴れ失敗(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;밧줄구속による暴れ失敗
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
		[SKIPEND]
		
		;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1, 2を書いた場合はREACT파생0,「暴れ成功」(REACT분류:날뛴다)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT60, 스펭킹

;────────────────────────────────────
;REACT61, 채찍・個別リアクション
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:겁없이 웃는다
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_REACT_KX1_61
;-------------------------------------------------
;・固有の地の文なし。죠교대상の反応に応じてREACTION_MESSAGEで汎用処理
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다(REACT분류:용서를 빌다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("용서를 빌다")
	;派生1の場合はREACT분류:なし
	;=================================================
	;意見を出すなんていい度가슴だね！(징계・포인트加算)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 고통을 참는다(REACT분류:受け入れる)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다(REACT분류:受け入れる)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("비명지른다")
	;=================================================
	;공포による悲鳴
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
;REACT분류が날뛴다であった場合は성교は취소になる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;暴れ失敗(REACT분류:소극적으로 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;밧줄구속による暴れ失敗
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
		[SKIPEND]
		
		;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1, 2を書いた場合はREACT파생0,「暴れ成功」(REACT분류:날뛴다)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT61, 채찍・個別リアクション

;────────────────────────────────────
;REACT62, 바늘・個別リアクション
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:겁없이 웃는다
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_REACT_KX1_62
;-------------------------------------------------
;・固有の地の文なし。죠교대상の反応に応じてREACTION_MESSAGEで汎用処理
;・페니스、もしくは클리토리스、及び乳首に바늘を突き刺す
;・刺당했다箇所からは出血し、血液汚れが追加される
;-------------------------------------------------
;　汚れの参照には@GET_STAINの使用をお勧めします！
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상に페니스あり
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && PENIS(MASTER)
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("용서를 빌다")
		;派生1の場合はREACT분류:なし
		;-------------------------------------------------
		;意見を出すなんていい度가슴だね！(징계・포인트加算)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포による悲鳴
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
	;REACT분류が날뛴다であった場合は성교は취소になる
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;暴れ失敗(REACT분류:소극적으로 한다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※内容を書き分けるならコメントアウトを解除して使用して下さい
			;밧줄구속による暴れ失敗
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
;OriginalString : 
				PRINTFORMW
			ENDIF
			RETURN LOCAL:2
			[SKIPEND]
			
			;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1, 2を書いた場合はREACT파생0,「暴れ成功」(REACT분류:날뛴다)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0
ENDIF

;클리토리스に바늘を突き刺す
;죠교대상に페니스ありの箇所を参考に필요な分岐を追加して使用して下さい
;条件なしの記述はやめましょう
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상に페니스なし
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !PENIS(MASTER)
;条件にヒットしなければ汎用リアクションへ
RETURN 0
ENDIF
;REACT62, 바늘

;────────────────────────────────────
;REACT63, 밧줄・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=구속/1=解除)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:침묵한다/빈정거린다/더 하드하게 해/용서를 빌다
;애무系反応
;	COM:겁없이 웃는다
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다/무서워한다
;-------------------------------------------------
@KOJO_REACT_KX1_63
;-------------------------------------------------
;・固有の地の文なし。죠교대상の反応に応じてREACTION_MESSAGEで汎用処理
;-------------------------------------------------

;REACT분류が용서를 빌다である場合は必ず취소になる
;취소処理はTEQUIP参照なので、他のACTでもREACT분류が용서를 빌다なら発生し得る
;現状、当該REACT以外でこの挙動を確実に拾える方法はない
;━━━━━━━━━━━━━━━━━━━━━━━━
;애원を聞き入れ구속を解いた
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("용서를 빌다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;구속時
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("용서를 빌다")
		;派生1の場合はREACT분류:なし
		;-------------------------------------------------
		;意見を出すなんていい度가슴だね！(징계・포인트加算)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포による悲鳴
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1, 밧줄구속による暴れ失敗はACT:밧줄の時には成立しない
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("날뛴다")
		;制止による暴れ失敗という扱い
		;TEQUIP:밧줄への代入はすでに行われていたりしますが、描写的にはREACT終了時点で縛りが成立するとか
		;そんな感じに捉えて貰うのが良いかも知れません
		;-------------------------------------------------
		;暴れ失敗(REACT분류:소극적으로 한다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;暴れ成功(TEQUIP:밧줄は解除される)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && !TFLAG:REACT파생
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生しなかった場合はREACT분류:용서를 빌다扱い(취소処理に該当する)なので考慮しない
	;=================================================
	;COM33, 무서워한다(징계・포인트加算)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0
ENDIF

;派生0,구속時を参考に필요な分岐を追加して使用して下さい
;条件なしの記述はやめましょう
;━━━━━━━━━━━━━━━━━━━━━━━━
;解除時
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
;・COM:도구를 제거한다ではなく、あくまでも죠교자による解除である事に注意
;-------------------------------------------------
IF LOCAL && TFLAG:ACT파생 == 1
;条件にヒットしなければ汎用リアクションへ
RETURN 0
ENDIF
;REACT63, 밧줄

;────────────────────────────────────
;REACT64, 눈가리개・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:침묵한다/빈정거린다/더 하드하게 해/용서를 빌다
;애무系反応
;	COM:겁없이 웃는다
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다/무서워한다
;その他反応
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_REACT_KX1_64
;-------------------------------------------------
;・固有の地の文なし。죠교대상の反応に応じてREACTION_MESSAGEで汎用処理
;-------------------------------------------------

;REACT분류が용서를 빌다である場合は必ず취소になる
;취소処理はTEQUIP参照なので、他のACTでもREACT분류が용서를 빌다なら発生し得る
;現状、当該REACT以外でこの挙動を確実に拾える方法はない
;━━━━━━━━━━━━━━━━━━━━━━━━
;애원を聞き入れ구속を解いた
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("용서를 빌다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 눈가리개장착時
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("용서를 빌다")
		;派生1の場合はREACT분류:なし
		;-------------------------------------------------
		;意見を出すなんていい度가슴だね！(징계・포인트加算)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포による悲鳴
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;暴れ失敗(REACT분류:소극적으로 한다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※内容を書き分けるならコメントアウトを解除して使用して下さい
			;밧줄구속による暴れ失敗
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
;OriginalString : 
				PRINTFORMW
			ENDIF
			RETURN LOCAL:2
			[SKIPEND]
			
			;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1, 2を書いた場合はREACT파생0,「暴れ成功」(REACT분류:날뛴다)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生しなかった場合はREACT분류:용서를 빌다扱い(취소処理に該当する)なので考慮しない
	;=================================================
	;COM33, 무서워한다(징계・포인트加算)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("날뛴다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0
ENDIF

;派生0,장착時を参考に필요な分岐を追加して使用して下さい
;条件なしの記述はやめましょう
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 解除時
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
;条件にヒットしなければ汎用リアクションへ
RETURN 0
ENDIF
;REACT64, 눈가리개

;────────────────────────────────────
;REACT65, 볼개그・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:침묵한다
;애무系反応
;	COM:겁없이 웃는다
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다/무서워한다
;その他反応
;	COM:도구를 제거한다
;※침묵한다以外の회화系及び言葉を伴う系統の反応は不可能になる
;-------------------------------------------------
@KOJO_REACT_KX1_65
;-------------------------------------------------
;・固有の地の文なし。죠교대상の反応に応じてREACTION_MESSAGEで汎用処理
;-------------------------------------------------

;REACT분류が용서를 빌다である場合は必ず취소になる
;취소処理はTEQUIP参照なので、他のACTでもREACT분류が용서를 빌다なら発生し得る
;現状、当該REACT以外でこの挙動を確実に拾える方法はない
;━━━━━━━━━━━━━━━━━━━━━━━━
;애원を聞き入れ구속を解いた
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
;※볼개그の場合COM:무서워한다の場合にのみ、この箇所に該当すると思われる
;-------------------------------------------------
IF LOCAL && IS_COMGRONAME("용서를 빌다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 볼개그장착時
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포による悲鳴
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;暴れ失敗(REACT분류:소극적으로 한다)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※内容を書き分けるならコメントアウトを解除して使用して下さい
			;밧줄구속による暴れ失敗
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
;OriginalString : 
				PRINTFORMW
			ENDIF
			RETURN LOCAL:2
			[SKIPEND]
			
			;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1, 2を書いた場合はREACT파생0,「暴れ成功」(REACT분류:날뛴다)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生しなかった場合はREACT분류:용서를 빌다扱い(취소処理に該当する)なので考慮しない
	;=================================================
	;COM33, 무서워한다(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("도구를 제거한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0
ENDIF

;派生0,장착時を参考に필요な分岐を追加して使用して下さい
;条件なしの記述はやめましょう
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 解除時
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
;条件にヒットしなければ汎用リアクションへ
RETURN 0
ENDIF
;REACT65, 볼개그

;────────────────────────────────────
;REACT66, 매도・個別リアクション
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:침묵한다/마음 약하게 응답한다/무뚝뚝하게 응답한다/상냥하게 응답한다/강하게 응답한다/빈정거린다/기분좋게 해
;애무系反応
;	COM:겁없이 웃는다
;고통系反応
;	COM:비명지른다/무서워한다
;その他反応
;	COM:자위를 시작한다
;-------------------------------------------------
@KOJO_REACT_KX1_66
;-------------------------------------------------
;・固有の地の文なし。죠교대상の反応に応じてREACTION_MESSAGEで汎用処理
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다(REACT분류:なし)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;매도の場合はREACT파생 == 1にはならない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다(REACT분류:소극적으로 한다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("마음 약하게 응답한다")
	;=================================================
	;REACT파생1, 애원する(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「気弱く応答」(REACT분류:소극적으로 한다)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다(REACT분류:なし)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 愛想良く応答する(REACT분류:적극적으로 한다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("상냥하게 응답한다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다(REACT분류:なし)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("강하게 응답한다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다(REACT분류:태연하게 한다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해(REACT분류:なし)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;「この恥知らずの豚め！」(징계・포인트加算)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:소극적으로 한다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다(REACT분류:受け入れる)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("비명지른다")
	;=================================================
	;공포による悲鳴
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다(REACT분류:용서를 빌다)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;派生：怯えに対する초조함
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다(REACT분류:受け入れる)
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("자위를 시작한다")
	;=================================================
	;勝손にオナニーするな(징계・포인트加算)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「勝손にオナニーしてるけどいいか」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;REACT67, 삼각목마・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:침묵한다/빈정거린다/더 하드하게 해/용서를 빌다
;애무系反応
;	COM:겁없이 웃는다
;고통系反応
;	COM:고통을 참는다/비명지른다
;-------------------------------------------------
@KOJO_REACT_KX1_67
;-------------------------------------------------
;・固有の地の文なし。죠교대상の反応に応じてREACTION_MESSAGEで汎用処理
;-------------------------------------------------

;REACT분류が용서를 빌다である場合は必ず취소になる
;취소処理はTEQUIP参照なので、他のACTでもREACT분류が용서를 빌다なら発生し得る
;現状、当該REACT以外でこの挙動を確実に拾える方法はない
;━━━━━━━━━━━━━━━━━━━━━━━━
;애원を聞き入れ木馬から降ろ했다
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("용서를 빌다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 開始時
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM0, 침묵한다(REACT분류:なし)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM5, 빈정거린다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM6, 더 하드하게 해(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("더 하드하게 해")
		;-------------------------------------------------
		;「この恥知らずの豚め！」(징계・포인트加算)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;派生0の場合はREACT분류:용서를 빌다扱い(취소処理に該当する)なので考慮しない
	;=================================================
	;COM7, 용서를 빌다・派生「意見を出すなんていい度가슴だね！」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("용서를 빌다") && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포による悲鳴
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0
ENDIF

;派生0,開始時を参考に필요な分岐を追加して使用して下さい
;条件なしの記述はやめましょう
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 終了時
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
;条件にヒットしなければ汎用リアクションへ
RETURN 0
ENDIF
;REACT67, 삼각목마

;────────────────────────────────────
;REACT68, 관장세트・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생（=0注入/=1排出/=2追加）
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:겁없이 웃는다
;고통系反応
;	COM:고통을 참는다/비명지른다/무서워한다
;-------------------------------------------------
@KOJO_REACT_KX1_68
;-------------------------------------------------
;・固有の地の文なし。죠교대상の反応に応じてREACTION_MESSAGEで汎用処理
;-------------------------------------------------
;　とりあえず注入、追加はまとめてしまいま했다が
;　손間を厭わないのであれば、TFLAG:ACT파생で分けても良いとは思います
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;관장液注入＆追加
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && GROUPMATCH(TFLAG:ACT파생, 0, 2)
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("용서를 빌다")
		;派生1の場合はREACT분류:なし
		;-------------------------------------------------
		;意見を出すなんていい度가슴だね！(징계・포인트加算)
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:受け入れる)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포による悲鳴
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM33, 무서워한다(REACT분류:용서를 빌다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("무서워한다")
		;-------------------------------------------------
		;派生：怯えに対する초조함
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("도구를 제거한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0
ENDIF

;관장液注入＆追加を参考に필요な分岐を追加して使用して下さい
;条件無しの記述はやめましょう
;━━━━━━━━━━━━━━━━━━━━━━━━
;관장液排出
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
;・あくまでも죠교자による解除である事に注意
;-------------------------------------------------
IF LOCAL && TFLAG:ACT파생 == 1
ENDIF
;REACT68, 관장세트
