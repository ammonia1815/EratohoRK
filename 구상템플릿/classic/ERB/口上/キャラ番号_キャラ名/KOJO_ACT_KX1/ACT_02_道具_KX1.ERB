;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[ここに구상主名を入れる]구상 도구系アクション구상ファイル
;
;	도구系アクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;도구系 ACT20～26
;========================================================================
;・TFLAG:ACT(20=로터/21=바이브/22=항문바이브/23=항문비즈/24=Ｃ애무도구/25=유두캡/26=페니스밴드)
;・TFLAG:ACT분류 == 2に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "도구")で拾う事が可能
;・죠교자が죠교대상に도구を使う。この구상の時点で죠교자はすでに손を出している
;・미약・로션を使用するケースがある
;・도구類の장착、解除等はTFLAG:ACT파생で定義される
;-------------------------------------------------

;────────────────────────────────────
;ACT20, 로터
;────────────────────────────────────
;・Ｃ中心に애무する
;・애무系から発生する場合もある
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"하드", "이상"だと発生しない
;・TEQUIP(클리캡/오나홀/페니스밴드/삼각목마)장착中には発生しない
;・성교중/성교봉사중には発生しない
;・죠교대상に페니스がある場合は発生しない
;-------------------------------------------------
@KOJO_ACT_KX1_20
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT20, 로터
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;TCVAR:MASTER:위치전후とTEQUIP:안면승마/안면승마항문で地の文が변화します
	;필요に応じて分岐を増減して下さい
	;=================================================
	;죠교대상がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:MASTER:가버려
		;애태움도によって分岐
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움中
			CASE IS > 1
;OriginalString : 
				PRINTFORMW 
			;애태움無し・애태움終了
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;하반신服装が要押し退け
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PANTSCHECK(1, 1)
		;服越しに
		IF PANTSCHECK(1)
;OriginalString : 
			PRINTFORMW 
		;その他		LOWERJACKET(1) == 1で裾から손をいれ、LOWERUNDER(1) == 1で속옷をずらす状況。両方成立してる可能性もある
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;위치전후を切り分けるなら、TCVAR:MASTER:위치전후 == 2で後ろからになります
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;前から애무
		IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 
			PRINTFORMW 
		;後ろから애무
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT21, 바이브
;────────────────────────────────────
;・죠교대상が남자の場合は発生しない
;・TFLAG:징계・플래그がたっていると発生しない
;・턴방침が"이상"だと発生しない
;・TEQUIP(삼각목마/페니스밴드)장착中だと発生しない
;・성교중には発生しない
;・죠교대상が처녀でも発生する可能性がある
;-------------------------------------------------
@KOJO_ACT_KX1_21
;-------------------------------------------------
;・TFLAG:ACT파생(0=바이브장착/1=解除)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT21_0, 바이브장착
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;죠교대상が처녀の場合、처녀상실が起こる。REACTで参照する場合はTALENT:MASTER:처녀ではなく、TCVAR:MASTER:처녀상실で
	;=================================================
	;처녀
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:처녀
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교대상が후타나리
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:후타나리
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT21_1, 바이브解除
;*************************************************
;・既に装備している場合、바이브解除になる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 바이브解除
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT22, 항문바이브
;────────────────────────────────────
;・턴방침が"소프트", "이상"だと発生しない
;・TEQUIP(항문비즈/관장그릇＋플러그/삼각목마/페니스밴드)장착中は発生しない
;・항문섹스中(TEQUIP:성교중 == 6, もしくはA_SEX(TARGET))には発生しない
;-------------------------------------------------
@KOJO_ACT_KX1_22
;-------------------------------------------------
;TFLAG:ACT파생(0=항문바이브장착/1=解除)
;-------------------------------------------------

;죠교대상が남자の場合、전립선자극扱いとなり발기도が上昇
;地の文は派生しないので、ABL:MASTER:Ａ감각なりTEQUIPなりで分岐をふやしてもいいと思います
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT22_0, 항문바이브장착
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;바이브장착中(二穴責め)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:바이브
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF

	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT22_1, 항문바이브解除
;*************************************************
;既に装備している場合、항문바이브解除になる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT22_1, 항문바이브解除
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT23, 항문비즈
;────────────────────────────────────
;・항문비즈장착/解除/追加
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"이상"だと発生しない
;・TEQUIP(항문바이브/관장그릇＋플러그/삼각목마/페니스밴드)장착中には発生しない
;・Ａ성교중(TEQUIP:성교중 == 6, もしくはA_SEX(TARGET))時には発生しない
;-------------------------------------------------
@KOJO_ACT_KX1_23
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=解除/2=追加)
;-------------------------------------------------

;*************************************************
;ACT23_0, 항문비즈장착
;*************************************************
;・장착時に1～3個の間でランダムで決まる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 항문비즈장착
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT23_1, 항문비즈解除
;*************************************************
;・玉が5つ入っている場合、항문비즈解除になる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 항문비즈解除
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT23_2, 항문비즈追加
;*************************************************
;・비즈の追加は1～3の間でランダムで決まる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2, 항문비즈追加
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;비즈の個数による派生
		SELECTCASE TEQUIP:항문비즈
			;四つ、若しくは五つ目。仕様上、五個以上になる事はない
			CASE IS >= 4
;OriginalString : 
				PRINTFORMW 
			;三つ目
			CASE 3
;OriginalString : 
				PRINTFORMW 
			;その他
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT24, Ｃ애무도구
;────────────────────────────────────
;・클리캡/오나홀の장착、解除
;・オナホを所持かつ죠교대상に페니스がある場合は「오나홀」に派生する
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"하드", "이상"の場合は発生しない
;・TEQUIP(삼각목마/페니스밴드)장착中には発生しない
;・오나홀장착は발기도が1000以下(TCVAR:MASTER:발기도 < 1000)の場合に弾かれ、発生しない
;・성교봉사중には発生しない
;-------------------------------------------------
@KOJO_ACT_KX1_24
;-------------------------------------------------
;・TFLAG:ACT파생(0=클리캡장착/1=클리캡解除/2=오나홀장착/3=오나홀解除)
;-------------------------------------------------

;*************************************************
;ACT24_0, 클리캡장착
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 클리캡장착
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT24_1, 클리캡解除
;*************************************************
;・;既に装備している場合、클리캡解除になる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 클리캡解除
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT24_2, 오나홀장착
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2, 오나홀장착
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(1)
		;죠교대상が동정
		IF TALENT:MASTER:동정
;OriginalString : 
			PRINTFORMW
		;その他
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT24_3, 오나홀解除
;*************************************************
;・;既に装備している場合、오나홀解除になる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생3, 오나홀解除
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 3
	;=================================================
	;정액汚れあり
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_STAIN("페니스", "정액", MASTER)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT25, 유두캡
;────────────────────────────────────
;・유두캡장착/解除
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"하드", "이상"だと発生しない
;・유두캡と言っても、地の文ではクリップ로터という扱い
;-------------------------------------------------
@KOJO_ACT_KX1_25
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------

;*************************************************
;ACT25_0, 유두캡장착
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 유두캡장착
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT25_1, 유두캡解除
;*************************************************
;・既に装備している場合、유두캡解除になる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 유두캡解除
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT26, 페니스밴드
;────────────────────────────────────
;・페니스밴드장착/解除
;・죠교자が죠교대상に双頭페니스밴드장착させようとする場面
;・죠교대상が처녀なら、처녀상실が起こる
;・TFLAG:징계・플래그が立っていると発生しない
;・TEQUIP:(항문비즈/항문바이브/관장그릇＋플러그/삼각목마)장착中には発生しない
;・성교중/성교봉사중には発生しない
;・죠교대상に페니스があると発生しない
;・TEQUIP(바이브/클리캡)は両立しないものとして解除される
;-------------------------------------------------
@KOJO_ACT_KX1_26
;-------------------------------------------------
;・TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------

;*************************************************
;ACT26_0, 페니스밴드장착
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 페니스밴드を장착시킨다
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		;죠교대상が처녀
		IF TALENT:MASTER:처녀
;OriginalString : 
			PRINTFORMW 
		;その他
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT26_1, 페니스밴드解除
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 페니스밴드を解除시킨다
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
