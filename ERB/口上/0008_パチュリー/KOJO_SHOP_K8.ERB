;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	파츄리구상 숍이벤트구상파일
;
;	숍으로부터 불려 가는구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;//잠정판@/L(2015/01/15)

;========================================================================
;숍이벤트구상
;========================================================================

;────────────────────────────────────
;EV101, 의상변경
;────────────────────────────────────
;·숍으로부터의상변경을 실시했을 경우에 표시
;-------------------------------------------------
@KOJO_EVENT_K8_101(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=
;  1의 위…（0, 이제(벌써) 입고 있다, 1, 갈아입은, 2, 부끄러워서 입을 수 없는, 3, 싫어해 입지 않는, 9, 가지고 있지 않다）
;  10의 위…（0, 통상, 10, 반라, 20, 전라, 30, 속옷없음）
;  백의 위… 코스프레를 위해서(때문에) 보류
;  천의 위…（0, 죠교자, 1000, 죠교대상）
;  예（죠교대상가 부끄러워해전라로 할 수 없었다=1022）
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;갈아입었는지 갈아입는 것을 그만두었다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(ARG % 10, 0, 1)
	SELECTCASE ARG / 10
		;죠교자가
		;통상되었다
		CASE 0
;OriginalString : 「この服が一番落ち着くわ」
			PRINTFORMW 「이 옷이 제일 침착해요」
		;반라되었다
		CASE 1
;OriginalString : 「속옷姿を歓ぶ者も居るらしい」
			PRINTFORMW 「속옷 모습을 환자도 있는 것 같다」
		;전라되었다
		CASE 2
;OriginalString : 「마력は肌で直接感じてこそよ」
			PRINTFORMW 「마력은 피부로 직접 느껴야만」
		;죠교대상가
		;통상되었다
		CASE 100
;OriginalString : 「どうせ脱がすのだけれども」
			PRINTFORMW 「어차피 탈의 것이지만」
		;반라되었다
		CASE 101
;OriginalString : 「ここでは반라で不자유はしないの」
			PRINTFORMW 「여기에서는 반나체로 부자유는 하지 않는거야」
		;전라되었다
		CASE 102
;OriginalString : 「노예に服は不要よ」
			PRINTFORMW 「노예에 옷은 불필요해요」
		CASEELSE
			RETURN 0
	ENDSELECT
	RETURN 1
ENDIF

;────────────────────────────────────
;EV102, 죠교자변경
;────────────────────────────────────
;·죠교자를 교대시켰다때에 표시
;-------------------------------------------------
@KOJO_EVENT_K8_102(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 구상주가 다른 캐릭터에게 맡긴, 1, 구상주가죠교자되었다)
;구죠교자는 「%CALLNAME:SHOP_OLDTARGET%」, 신죠교자는 「%CALLNAME:SHOP_NEWTARGET%」로 참조할 수 있다
;구상주는 TARGET로 참조
;-------------------------------------------------
;·반항각인는죠교자 마다 보존되므로, MARK:MASTER:반항각인등과 해 버리면 0밖에 되지 않기 때문에 주의
;·이것은굴복각인도 같습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;다른죠교자에 교대
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 0
	;죠교대상의반항각인Lv5 이상
	IF MARK:반항각인 >= 5
;OriginalString : 
		PRINTFORMW 
	;죠교대상의반항각인Lv4 이하
	ELSE
;OriginalString : 
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님이죠교자되었다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 1
	;죠교대상의반항각인Lv5 이상
	IF MARK:반항각인 >= 5
;OriginalString : 
		PRINTFORMW 
	;죠교대상의반항각인Lv4 이하
	ELSE
;OriginalString : 
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;EV110, 마력사용
;────────────────────────────────────
;·마력를 소비해 어떠한 특전을 구입했다때에 표시
;-------------------------------------------------
@KOJO_EVENT_K8_110(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 마력사용전, 1, 죠교기교, 2, 지식를 얻기 전, 3, 배합지식취득, 4, 매혹취득, 5, 금단의지식취득, 6, 도구능숙취득,
;	7, 기술을 얻기 전, 8, 혀기술취득, 9, 손가락기술취득, 10, 긴박능숙, 11, 새드소거/새드취득, 12, 더러움무시취득, 38, 착의플레이선호, 39, 허리기술)
;이벤트구상: 아이템
;인수 내역…
;ARG=(13, 아이템 연성전, 14, 로터연성, 15, 바이브연성, 16, 항문비즈연성, 17, 페니스밴드연성, 18, 항문바이브연성
; 19, 클리캡연성, 20, 유두캡연성, 21, 오나홀연성)
;이벤트구상:고문도구 연성
;인수 내역…
;ARG=(22, 고문도구 연성전, 23, 눈가리개연성, 24, 채찍연성, 25, 밧줄연성, 26, 볼개그연성, 27,   바늘연성, 28, 관장세트연성
; 29, 삼각목마연성, 30, 거울연성, 31, 개목걸이연성, 32, 도구를 커스터마이즈했다)
;그 외이벤트
;인수 내역…
;ARG=(33, 후타나리화/되돌리는, 34, 거유화/되돌리는, 35, 빈유화/되돌리는, 36, 정력의 한계 가격인상승)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님마력사용전
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 0
;OriginalString : 파츄리は소악마を呼び出했다…
	PRINTFORMDL 파츄리는 소악마를 호출했다…
;OriginalString : 「죠교の効率化はお任せください！」
	PRINTFORMDL 「조교의 효율화는 맡겨라 주세요!」
;OriginalString : 「その対価として、마력をほんのちょっぴり頂きますがね」
	PRINTFORMDL 「그 대가로서 마력을 그저 조금 받습니다만」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님의죠교기교가 업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 1
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님지식를 얻기 전
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 2
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님소질을취득
;━━━━━━━━━━━━━━━━━━━━━━━━
;·필요에 응해 개별의 조건에 분리해 주세요
;------------------------------------------------
IF 구상기입체크(0) && (INRANGE(ARG, 3, 12) || GROUPMATCH(ARG, 38, 39))
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님 아이템 연성전
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 13
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님 아이템 연성
;━━━━━━━━━━━━━━━━━━━━━━━━
;·필요에 응해 개별의 조건에 분리해 주세요
;------------------------------------------------
IF 구상기입체크(0) && INRANGE(ARG, 14, 21)
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님고문도구 연성전
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 22
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님고문도구 연성
;━━━━━━━━━━━━━━━━━━━━━━━━
;·필요에 응해 개별의 조건에 분리해 주세요
;------------------------------------------------
IF 구상기입체크(0) && INRANGE(ARG, 23, 31)
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님도구를 커스터마이즈
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 32
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님후타나리화 or후타나리소거
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 33
	;=================================================
	;후타나리화
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:후타나리
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;후타나리소거
	;=================================================
	IF 구상기입체크(0) && !TALENT:TARGET:후타나리
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님거유화 or통상에 되돌린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 34
	;=================================================
	;거유화
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:거유
;OriginalString : 
		PRINTFORMDW 
		RETURN 1
	ENDIF
	
	;=================================================
	;바탕으로 되돌린다
	;=================================================
	IF 구상기입체크(0) && !TALENT:TARGET:거유
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;파츄리님빈유화 or통상에 되돌린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 35
	;=================================================
	;빈유화
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:빈유
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;바탕으로 되돌린다
	;=================================================
	IF 구상기입체크(0) && !TALENT:TARGET:빈유
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;EV111, 주인님로서 새로운죠교자를 고용했다
;────────────────────────────────────
@KOJO_EVENT_K8_111(ARG)
;-------------------------------------------------
;·새로운죠교자가 TARGET가 되어 있기 (위해)때문에, 구상주는 TARGET는 아니기 때문에 주의
;·구상주를 참조했다 있고 경우는 FLAG:주인님로
;-------------------------------------------------
;반환값（1, 표시, 2, 표시해 가입했다캐릭터의 가입구상를 스킵）
;-------------------------------------------------
IF 구상기입체크(100)
	;//NO:TARGET로 고용한 상손를 참조하는 것이 가능합니다
	;CSTR:(FLAG:주인님):호칭목록 = 전의 이름_부르게 하고 싶은 이름/%CSTR:(FLAG:주인님):호칭목록%
	;임무 전가
;OriginalString : 「何か楽して죠교を進める方法は無いものかしら」
	PRINTFORMW 「무엇인가 락 해 조교를 진행시키는 방법은 없는 것 까」
;OriginalString : 
	PRINTFORML
;OriginalString : ………
	PRINTFORMDW ………
;OriginalString : ……
	PRINTFORMDW ……
;OriginalString : …
	PRINTFORMDW …
;OriginalString : 
	PRINTFORML 
	RETURN 1
ENDIF

;────────────────────────────────────
;EV112, 파츄리님이죠교자로서 가입했다
;────────────────────────────────────
@KOJO_EVENT_K8_112(ARG)
;-------------------------------------------------
;·주인님의 이름은 「FLAG:주인님」로 참조할 수 있다
;·주인님가 고용구상로 반환값 2를 돌려주고 있으면 표시되지 않는다
;·구상주는 TARGET
;-------------------------------------------------
IF 구상기입체크(100)
;OriginalString : 「마력を分けてくれるなんて随分と気前がいいのね」
	PRINTFORMW 「마력을 나누어 주다니 상당히 마음이 후하구나」
;OriginalString : 「悪いようにはしないから安心していいわ」
	PRINTFORMW 「나쁘게는 하지 않기 때문에 안심해도 좋아요」
	RETURN 1
ENDIF
