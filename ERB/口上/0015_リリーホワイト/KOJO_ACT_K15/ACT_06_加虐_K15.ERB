;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[릴리 화이트]구상 가학계 액션구상파일
;
;	가학계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;가학계 ACT60～68
;========================================================================
;·TFLAG:ACT(60=스펭킹/61=채찍/62=바늘/63=밧줄/64=눈가리개/65=볼개그/66=매도/67=삼각목마/68=관장세트)
;·TFLAG:ACT분류 == 6에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "가학")로 참조하는 것이 가능
;·TEQUIP계는 기본, ACT 개시시점으로써장착 된다
;·ACT에 따라서는죠교대상의 반응하기 나름으로취소 되는 것이 있다
;·밧줄/눈가리개/볼개그/삼각목마는 TEQUIP로관리 되고 해당 ACT/REACT 이외에도 TEQUIP:밧줄등으로 참조할 수 있다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT60, 스펭킹
;────────────────────────────────────
;·죠교자가죠교대상의 엉덩이를 평손로 두드린다
;·ACT 개시시점으로써 벌써 두드리고 있다
;·성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중인 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K15_60
;구상독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT60, 스펭킹
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;·@TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
;OriginalString : 「ほら、リリーにお尻向けてくださいね？　いきますよ…えいっ！」
		PRINTFORMW 「자, 릴리에게 엉덩이 향하여 주세요? 갈게요… 에잇!」
;OriginalString : 「한심한格好…リリーにお尻ぺんぺんされて恥ずかしくないんですか？」
		PRINTFORMW 「한심한 모습… 릴리에게 엉덩이 팡팡 되어 부끄럽지 않나요?」
;OriginalString :  
		PRINTL  
;OriginalString : 部屋中に打音と%CALLNAME:MASTER%의 悲鳴が響き、尻たぶに小さな손形が刻まれる…
		PRINTFORMDW 방안에 타음과%CALLNAME:MASTER%의 비명이 울려, 엉덩이에 작은 손자국이 새겨진다…
;OriginalString : 
		PRINTL 
;OriginalString : 「%CNAME(MASTER)%はリリーの노예さんなんですから、ちゃんとリリーの言う事聞かなきゃダメですよ…ねっ！」
		PRINTFORMW 「%CNAME(MASTER)%는 릴리의 노예이니까, 분명하게 릴리가 말하는 일 듣지 않으면 안대… 요!」
;OriginalString : 「んっ！　ちゃんと『ごめんなさい』できるまでっ！　おわりませんからねっ！」
		PRINTFORMW 「응! 제대로 「미안해요」할 수 있을때까지! 끝내지 않으니까욧!」
		RETURN 1
	ENDIF
	
	;금일의방침가징계·의 경우는 여기에 밖에 떨어지지 않게 되므로
	;구상기입 체크(20)등으로 해서 확률을 짜는 것을 추천
	;=================================================
	;징계·발동중
	;=================================================
	IF 구상기입체크(0) && TFLAG:징계·플래그
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;TEQUIP:죠교대상Ｃ사용 == 2（입에 의한애무）는 고려하지 않는다
	;=================================================
	;Ｃ애무계속중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:죠교대상Ｃ사용 == 1
		;수음계속중
		IF PENIS(MASTER)
;OriginalString : 「ねぇ、おちんちん扱かれながらお尻%RAND_SPLIT("叩かれる/ペンペンされる")%ってどんな気分なんですか？」
			PRINTFORMW 「응, 자지 잡아당겨지면서 엉덩이%RAND_SPLIT("얻어맞/팡팡 되")%는 것, 어떤 기분인가요?」
;OriginalString : 「%RAND_SPLIT("/ヘンタイさんじゃあるまいし、")%まさか興奮なんてしませんよね？」
			PRINTFORMW 「%RAND_SPLIT("/변태씨는 아닐 거고,")%설마 흥분은 하지 않았죠?」
;OriginalString :  
			PRINTL  
;OriginalString : 「ほらぁっ♪　%RAND_SPLIT("ビクンっビクンっておちんちん跳ねちゃってますね/なんでおちんちん硬くなってるんですか")%っ
			PRINTFORM 「봐요오♪ %RAND_SPLIT("흠칫흠칫 자지 뛰어 버리고 있네요/어째서 자지 딱딱해지고 있나요?")%
			CALL HEARTCB
;OriginalString : 」
			PRINTFORMW 」
;OriginalString : 「%RAND_SPLIT("イケナイ子ですねっ、/ヘンタイさんっ、")%『ごめんなさい』って言いながらっ！　情けなくぴゅっぴゅしましょうねっ！」
			PRINTFORMW 「%RAND_SPLIT("글러먹은 아이군요,/변태씨,")%「미안해요」라고 말하면서!   한심하게 퓻퓻 하세요!」
		;애무계속중
		ELSE
;OriginalString : 「여자って興奮が限界を超えると、고통が快感に変わるんですよね」
			PRINTFORMW 「여자 아이는 흥분이 한계를 넘으면, 고통이 쾌감으로 바뀌어요」
;OriginalString : 「まぁ、そうじゃないと出産なんてできませんし」
			PRINTFORMW 「뭐어, 그렇지 않으면 출산은 할 수 없고」
;OriginalString :  
			PRINTL  
;OriginalString : 「それで、お豆さんを弄られながらお尻ぺんぺんされて……」
			PRINTFORMW 「그래서, 콩씨를 만지작거려지고 엉덩이 팡팡 되어……」
;OriginalString : 「もう気持ちよくなっちゃいま했다か？　ヘンタイのマゾ豚さんっ♪」
			PRINTFORMW 「이제 기분 좋아져 버렸나요? 변태의 매저키스트 돼지씨♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 「%RAND_SPLIT("お尻真っ赤にしちゃって、お猿さんみたいになってますよ♪/もーっ！　まだ反省できませんか！？")%」
		PRINTFORMW 「%RAND_SPLIT("엉덩이 새빨갛게 해 버려, 원숭이씨같이 되어 있어요♪/정말! 아직도 반성 못하겠어요!?")%」
;OriginalString : 「リリーも%RAND_SPLIT("おてて痛くなってきたん/叩き疲れてきちゃったん")%ですけど…」
		PRINTFORMW 「릴리도%RAND_SPLIT("손 아파진/두드려 지쳐 버린")%거지만…」
;OriginalString :  
		PRINTL  
;OriginalString : 「%CNAME(MASTER)%もしかして…叩かれて悦んでません？」
		PRINTFORMW 「%CNAME(MASTER)%혹시… 얻어맞아 기뻤나요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음%RAND_SPLIT("/")%
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE ABL:MASTER:마조끼
			;마조끼 6 이상
			CASE IS > 6 && PERCENT(70)
;OriginalString : 「ねぇ%RAND_SPLIT("マゾ豚さん/ヘンタイさん")%っ♪　痛かったら大きな声で『%RAND_SPLIT("ぶぅぶぅ/ぶひぃ/気持ちいです")%』って鳴くんですよ
				PRINTFORM 「응%RAND_SPLIT("마조돼지씨/변태씨")%♪　아프면 큰 소리로 「%RAND_SPLIT("후라후라/히비이/아파요")%」하고 울었어요
				CALL HEARTCB
;OriginalString : 」
				PRINTFORMW 」
			;마조끼이상
			CASE IS > 4
;OriginalString : 「%RAND_SPLIT("マゾ豚さん/ヘンタイさん")%はホントに징계・が大好きなんですねぇ
				PRINTFORM 「%RAND_SPLIT("마조돼지씨/변태씨")%(은)는 정말로 징계를 아주 좋아하네요오
				CALL HEARTCB
;OriginalString : 」
				PRINTFORMW 」
		ENDSELECT
		SELECTCASE RANDCOM(2, 0)
			CASE 0
;OriginalString : 「さてと、%RAND_SPLIT("言ってもわからない悪い子/聞き分けのないヘンタイさん")%はお尻ペンペンしちゃいますからねっ」
				PRINTFORMW 「그러면, %RAND_SPLIT("말해도 모르는 나쁜 아이/구별이 없는 변태씨")%(은)는 엉덩이 팡팡 할테니까요」
;OriginalString : 「ほら、リリーにお尻向けてくださいねっ…えいっ！」
				PRINTFORMW 「자, 릴리에 엉덩이 향하여 주세요… 에잇!」
;OriginalString :  
				PRINTL  
;OriginalString : リリーは何度も%CALLNAME:MASTER%의 尻肉を叩き、%RAND_SPLIT("真っ赤な紅葉をいくつも刻みつけてゆく/真っ白な肌を紅く染めてゆく")%…
				PRINTFORMDW 릴리는 몇번이나%CALLNAME:MASTER%의 엉덩이 고기를 다져,%RAND_SPLIT("새빨간 단풍을 얼마든지 새겨 붙여 간다/새하얀 피부를 붉게 물들여 간다")%…
;OriginalString :  
				PRINTL  
;OriginalString : 「ちゃんと%RAND_SPLIT("『ごめんなさい』/反省")%できるまでっ！　おわりませんからねっ！」
				PRINTFORMW 「분명하게 %RAND_SPLIT("『미안해요』/반성")%할 수 있다!   끝나지 않기 때문에!」
			CASE 1
;OriginalString : 「%CNAME(MASTER)%がリリーのモノだって印、付けてあげなきゃねっ♪」
				PRINTFORMW 「%CNAME(MASTER)%하지만 릴리의 물건도 표, 붙여 주지 않으면♪」
;OriginalString :  
				PRINTL  
;OriginalString : リリーが손を振り下ろす度に焼ける様な고통が走る…
				PRINTFORMDW 릴리가 손을 흔들어 내릴 때에 타는 것 같은 아픔이 달린다…
;OriginalString : 真っ白な尻肉に小さな손形がいくつも刻まれ、紅くぷっくりと腫れ上がってゆく…
				PRINTFORMDW 새하얀 고육에 작은 어음이 얼마든지 새겨져 다홍색절의 부엌과 부어 올라 간다…
;OriginalString :  
				PRINTL  
;OriginalString : 「いつまで痛いの참다できるのかな…%RAND_SPLIT("ふふ/くす")%っ♪」
				PRINTFORMW 「언제까지 아픈 것 참을 수 있는 것일까…%RAND_SPLIT("후후/쿠쿡")%♪」
;OriginalString : 「ちゃんと%RAND_SPLIT("『ごめんなさい』/反省")%してくださいねっ！」
				PRINTFORMW 「분명하게%RAND_SPLIT("『미안해요』/반성")%해 주세요!」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT61, 채찍
;────────────────────────────────────
;·죠교자가채찍를 크게 치켜들고 마음껏 찍어내린다
;·ACT 개시시점으로써 벌써 두드리고 있다
;·턴방침가"소프트"라면 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K15_61
;구상독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT61, 채찍
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;눈가리개장착
	;=================================================
	IF 구상기입체크(0) && TEQUIP:눈가리개
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;구속중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:밧줄
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT62, 바늘
;────────────────────────────────────
;·유두와성기에바늘를 찌른다
;·ACT 개시시점으로써 벌써 찌르고 있다
;·죠교대상에페니스가 있는 경우는페니스에, 없는 경우는클리토리스에 찌른다
;·가슴와성기(페니스/바기나)로부터 출혈해, 혈액 더러움이 추가된다
;·턴방침가"소프트", "노멀"라면 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K15_62
;구상독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT62, 바늘
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은페니스의 유무로 분기. 분기내에 파생 없음
;-------------------------------------------------
;IF 구상기입 체크(0) && PENIS(MASTER) 등으로 나누는손도 있습니다. 필요에 응해 분기를 증감해 주세요
;비교적 쓰기 어려운 개소이며, 온전히 하는 것은손간인 것로\@ PENIS(MASTER) ? 페니스 # 클리토리스 \@와 같이 3항을 사용하면 락 할 수 있거나 합니다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;눈가리개장착중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:눈가리개
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT63, 밧줄
;────────────────────────────────────
;·죠교대상를밧줄로 묶으려고 한다
;·지문은"묶으려고했다…"되고 있지만 처리적으로는 ACT 개시시점으로써 속박되고 있다
;·TEQUIP:밧줄로 묶기의 강도를 참조할 수 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:밧줄자체는 REACT분류가"용서를 빌다"의 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을 확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·(REACT63 이외에도취소처리가 되는 경우가 있기 때문에(위해))
;·성교중/성교봉사중에는 발생하지 않는다
;·구속시에자위나안면승마, 죠교대상의 페니스사용중는 해제된다
;-------------------------------------------------
@KOJO_ACT_K15_63
;구상독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_0, 밧줄장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·반항각인, 죠교자죠교레벨, 기교, 긴박능숙소질에 응해구속SOURCE 증가
;·구속SOURCE에 응해 강도 상승
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;묶기 강도는통상라면 1이나 2 정도라고 생각됩니다
	;그 이상의 묶기 강도는반항각인나죠교자의긴박능숙소질+일정 이상의기교가 요구될까하고
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		SELECTCASE TEQUIP:밧줄
			;묶기 강도 3, 혹은 그 이상
			CASE IS >= 3
;OriginalString : 
				PRINTFORMW 
			;묶기 강도 3
			CASE 2
;OriginalString : 
				PRINTFORMW 
			;그 외
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_1, 밧줄해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;밧줄해제에 파생했다 시점에서, "TEQUIP:밧줄 = 0"이 되므로 해제시에 묶어 강도는 참조 할 수 없다
;장착와 강도로 같은 TEQUIP를 재사용하고 있기 때문에(위해), 이와 같이 되어 버린다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT64, 눈가리개
;────────────────────────────────────
;·죠교자가눈가리개를 대게 하려고 한다
;·지문에서는"대게 하려고했다…"되고 있지만 처리상은 ACT 개시시점으로써장착 되고 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:눈가리개는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을 확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·(REACT64 이외에도취소처리가 되는 경우가 있기 때문에(위해))
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중였을 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K15_64
;구상독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·TFLAG:ACT파성(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_0, 눈가리개장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상에페니스가 있는 경우, 마조끼에 응해발기도상승
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_1, 눈가리개해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT65, 볼개그
;────────────────────────────────────
;·죠교자가볼개그를 끼우게 하려고 한다
;·지문에서는"끼우게 하려고했다…"되고 있지만 처리상은 ACT 개시시점으로써장착 되고 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:볼개그는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을※확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·※REACT64 이외에도취소처리가 되는 경우가 있기 때문에(위해)
;·턴방침가"소프트", "노멀"의 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중였을 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K15_65
;구상독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·TFLAG:ACT파성(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_0, 볼개그장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상에페니스가 있는 경우, 마조끼에 응해발기도상승
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_1, 볼개그해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT66, 매도
;────────────────────────────────────
;·도발하도록(듯이)죠교대상의 치태를 소리에 낸다
;·회화계로부터 발생하는 경우도 있다
;·턴방침가"소프트"라면 발생하지 않는다
;·회화계로부터 파생하는 경우도 있다
;-------------------------------------------------
@KOJO_ACT_K15_66
;구상독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT66, 매도
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교중/성교봉사중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:성교중, TEQUIP:성교봉사중) > 0
		;성교중
		IF TEQUIP:성교중
;OriginalString : 
			PRINTFORMW
		;성교봉사중
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;자위중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(GETBIT(TEQUIP:자위중, 0), GETBIT(TEQUIP:자위중, 1)) > 0
		;죠교대상가자위중
		IF GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
			PRINTFORMW 
		;죠교자가자위중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;Ｃ애무계속중
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2)
		;수음·애무계속중
		IF TEQUIP:죠교대상Ｃ사용 == 1
;OriginalString : 
			PRINTFORMW 
		;펠라·쿤닐링구스 계속중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT67, 삼각목마
;────────────────────────────────────
;·죠교대상를 목마에 실으려고 한다
;·지문은"실으려고했다…"되고 있지만 처리상은 ACT 개시시점으로써 실려지고 있다
;· 실은쾌Ｃ는 아니고쾌Ｖ가 들어간다(나있는 목마!?)
;·MASTER가처녀라면, 처녀상실가 일어난다(@ACTION_APPLY2로 처리. REACT분류가용서를 빌다의 경우는 회피된다)
;·죠교대상이남자의 경우는 발생하지 않는다
;·턴방침가"소프트", "노멀"때에는 발생하지 않는다
;·TEQUIP(바이브/항문바이브/항문비즈/관장그릇＋플러그/페니스밴드)장착시에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·야외플레이시에는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·이상계로부터 발생하는 경우도 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:삼각목마는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을※확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·※REACT67 이외에도취소처리가 되는 경우가 있기 때문에(위해)
;-------------------------------------------------
@KOJO_ACT_K15_67
;구상독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_0, 삼각목마장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생로 분기. 파생내에서는 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 목마에 실으려고했다…
	
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_1, 삼각목마해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;TEQUIP취소 시에는 여기서 주울 수 있는 것이 아닌 것에 주의
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 목마로부터 내려 주었다…
	
	;처음라고는 말해도, 과거에 TEQUIP취소나 해제되지 않는 채죠교종료로 있던 가능성도 있다
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT68, 관장세트
;────────────────────────────────────
;·관장액의 주입/추가/배출
;·최초의 주입시(ACT파생=0)는 양 1～3. 추가시(ACT파생=2)는 양 1～4. 각각 랜덤으로 처리된다
;·;이미 장비 하고 있는 경우, 관장액추가가 된다
;·관장액이 5병 들어가 있는 경우, 관장플러그 해제가 된다
;·관장액의 분량은 TEQUIP:관장그릇＋플러그로 참조할 수 있다
;·TEQUIP(항문바이브/항문비즈/페니스밴드/삼각목마)장착시에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·턴방침가"소프트"라면 발생하지 않는다
;·도구계로부터 파생하는 경우도 있다
;-------------------------------------------------
@KOJO_ACT_K15_68
;구상독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제/2=관장액추가)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_0, 관장그릇＋플러그장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기. 파생내에서는 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하며, ")% %CALLNAME:MASTER%의 국혈에
	;관장액을 흘려 넣어, 마개를했다…
	
	;ACT 개시시점으로써 액은 주입되고 있어 TEQUIP:관장그릇＋플러그로 액량을 참조하는 것이 가능
	
	;=================================================
	;처음 「주입」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;사양이 바뀔 가능성을 고려해 ==는 아니고>=로 해둔다
	;=================================================
	;액량 3
	;=================================================
	IF 구상기입체크(0) && TEQUIP:관장그릇＋플러그 >= 3
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;액량 2
	;=================================================
	IF 구상기입체크(0) && TEQUIP:관장그릇＋플러그 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;액량의 분기를 모두 묻었을 경우는 TEQUIP:관장그릇＋플러그 == 1에 상당
	;개별적으로 나누지 않고 SELECTCASE TEQUIP:관장그릇＋플러그로 정리해 버리는 일도 가능
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_1, 관장그릇＋플러그해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;여기에 파생하는 시점에서 액량은 5가 되어 있다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하면서, ")% %CALLNAME:MASTER%로부터
	;항문마개를 뽑아, 장내의 오물을 항문으로부터 따돌려 산등시켰다…
	
	;=================================================
	;처음 「배출」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_2, 관장액추가
;━━━━━━━━━━━━━━━━━━━━━━━━
;이미 주입 끝난 경우에 이 개소에 파생
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하면서, ")% %CALLNAME:MASTER%의 항문에
	;한층 더관장액을 주입하려고했다…
	
	[SKIPSTART]
	액량으로 나누어 쓴다면 comment out를 해제
	하층의 PRINTFORMW를 삭제해 주세요
	;관장액분량 파생
	SELECTCASE TEQUIP:관장그릇＋플러그
		;액량 5
		CASE 5
;OriginalString : 
			PRINTFORMW 
		;액량 4
		CASE 4
;OriginalString : 
			PRINTFORMW 
		;액량 3
		CASE 3
;OriginalString : 
			PRINTFORMW 
		;그 외
		CASEELSE
;OriginalString : 
			PRINTFORMW 
	ENDSELECT
	[SKIPEND]
	;나누어 쓰지 않는 것이면 여기서
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF


