;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[릴리 화이트]구상 애무系個別リアクション구상ファイル
;
;	애무系リアクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;애무系 REACT10～19 & REACT27
;========================================================================
;・各REACT内の分け方としてTFLAG:REACT분류(IS_COMGRONAME)やSELECTCOM(IS_NOWCOMNAME)が考えられます
;・ただ、範囲の広い条件で拾ってしまうのは基本的にお勧めしません
;・ACT분류:애무系の場合、基本的に애무系COM(COM10~COM16)が선택肢となりますが
;・状況によってはその회화系、고통系、その他のものが선택肢に含まれる可能性もあります
;・リアクション(REACT)で処理されなかったものは汎用リアクション(COM)で補完されるので
;・無理に全て埋める필요はありません
;・필요と思うものを絞り込み、COMとの連携を考えて効率よく構築すべきです
;-------------------------------------------------
;・各REACT及びACT파생内は、&& IS_NOWCOMNAMEなり、&& IS_COMGRONAME等の条件で決めうちしましょう
;・ここに条件無し分岐があると、リアクションに関わらず、全て拾ってしまって汎用リアクションに流れません
;・要するに、想定당했다システムの破壊に繋がります
;-------------------------------------------------
;　基本的に地の文はTFLAG:REACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐し、
;　各REACT분류内でTFLAG:REACT부하(自然な행동/無理のない행동/無理を했다행동)で派生
;　ACT_MESSAGE.ERBを参照하면서の作業をお勧めします
;-------------------------------------------------
;　取り敢えずACT파생毎に枠を切り分けてありますが、一つに纏めた上で汎用的な記述で손間を減らすという손もあります
;　正直、やり方はいくらでもあるので、以下に示す方法は一つの例と思ってください
;-------------------------------------------------

;────────────────────────────────────
;REACT10, 손으로 애무・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=애무/1=수음/2=페니스밴드수음)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_K15_10
;-------------------------------------------------
;・ACTTION_APPLY2の処理なし
;・地の文はACT파생毎にREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 애무
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0においては可能性のあるCOMを一通り網羅しておきます
;필요に応じて分岐を増減して下さい
;-------------------------------------------------
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;COM6, 더 하드하게 해(REACT분류:受け入れる)
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("더 하드하게 해")
		;-------------------------------------------------
		;「この恥知らずの豚め！」(징계・포인트加算)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 구상記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("부끄러워한다")
		;快感に부끄러워한다
		;IF TFLAG:REACT파생 == 1
		;	PRINTFORMW 
		;状況に부끄러워한다
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		IF RAND:2 == 0
			PRINTFORMW 「시러시러 해도 안되어요♪」
			PRINTFORMW 「 좀 더 솔직해지지 않으면」
			PRINTFORMW 「이렇게 가랑이 미끈미끈 해져……느끼는거 들켜버린거예요？」
		ELSE
			PRINTFORMW 「기분 좋은것 참아 버립니다인가？」
			PRINTFORMW 「……？　어째서 그런 일 하겠지요」
			PRINTFORM 「어차피 곧 릴리에게 가버리는데 말이야
			CALL HEARTCB
			PRINTFORMW 」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("싫어한다")
		;嫌がって妨害했다			REACT분류"날뛴다"
		;IF TFLAG:REACT파생 == 1
		;	PRINTFORMW 
		;嫌がった					REACT분류"소극적으로 한다"
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		IF RAND:2 == 0
			PRINTFORMW 「무우-！　날뛰면 안되어요……」
			PRINTFORMW 「말하는 것 듣지 않는 아이에게는 벌칙 있으니까요」
		ELSE
			PRINTFORMW 「정말-！　모처럼 릴리가 기분 좋게 했는데……」
			PRINTFORMW 「아픔에서도 정액은 짤 수 있지만,%CNAME(MASTER)%도 아픈 것은 싫겠지요？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고 모욕한다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("서투르다고 모욕한다")
		;強がって					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;未熟なテクニックに対して	REACT분류"태연하게 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 참는다")
		;派生:애원					REACT분류"용서를 빌다"
		;IF TFLAG:REACT파생 == 1
		;	PRINTFORMW 
		;派生:耐久					REACT분류"소극적으로 한다"
		;ELSE
		;	PRINTFORMW
		;ENDIF
		IF RAND:2 == 0
			PRINTFORMW 「시러시러 해도 안되어요♪」
			PRINTFORMW 「 좀 더 솔직해지지 않으면」
			PRINTFORMW 「이렇게 가랑이 미끈미끈 해져……느끼는거 들켜버린거예요？」
		ELSE
			PRINTFORMW 「기분 있고가 참아 버립니다인가？」
			PRINTFORMW 「……？　어째서 그런 일 하겠지요」
			PRINTFORM 「어차피 곧 릴리에게 가버리는데 말이야
			CALL HEARTCB
			PRINTFORMW 」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 받아들인다")
		;애무
		IF TFLAG:ACT파생 == 0
			;ここのIF 0にどのような意図が？@/L
			;IF 0
			;ELSE
				;IF RAND:2 == 0
				;ELSE
					PRINTFORMW 「엉덩이의 살이 실룩실룩 떨리고 있어♪」
					PRINTFORMW 「좀더 좀더 %CNAME(MASTER)%의 약점 꾸짖어 준다♪」
					PRINTFORM 「이봐요♪　졸라 주세요
					CALL HEARTCB
					PRINTFORMW 」
				;ENDIF
			;ENDIF
		;수음
		ELSE
			;IF 0
			;ELSE
				IF RAND:5 == 0
					PRINTFORMW 「아핫♪　시코시코 기분 좋아？」
					PRINTFORMW 「라니, 묻지 않아도 알아요♪」
					PRINTFORMW 「그치만, 이렇게 행복하게 허리 털고 있는걸♪」
				ELSEIF RAND:4 == 0
					PRINTFORMW 「릴리의 손으로 기분 좋아져♪　후후♪」
					PRINTFORMW 「조르기 능숙한 변태씨에게는 상을 주지 않으면♪」
					PRINTFORM 「시코시코 시코시코♪　」
				ELSEIF RAND:3 == 0
					PRINTFORM 「하아, 응
					CALL HEARTCB
					PRINTFORM 　정액이 올라와서 움찔움찔거려요오...
					CALL HEARTCB
					PRINTFORMW 」
					PRINTL  
					PRINTFORMDW 흥분해서 혀가 제대로 돌지 않게 된 릴리가 녹은 얼굴로 사정을 졸라 온다
					PRINTL  
					PRINTFORM 「븃 븃 해줘
					CALL HEARTCB
					PRINTFORM 　릴리에게 따뜨탄 부카케해져여엇
					CALL HEARTCB
					PRINTFORMW 」
				ELSEIF RAND:2 == 0
					PRINTFORMW 「후후♪　열중해 버려서 귀여워♪」
					PRINTFORM 「좋아요, 그 상태로 질척질척한 "정액"을 릴리에 바쳐 주세요♪
					CALL HEARTCB
					PRINTFORMW 」
				ELSE
					PRINTFORMW 「솔직하게 조를 수 있었군요♪　대단해 대단해♪」
					PRINTFORMW 「봉사로 %CNAME(MASTER)%의 약점 전부 꾸짖어버린다♪」
					PRINTFORM 「자위로 얻을 수 없는 최고의 쾌락을 가르쳐 줄게요
					CALL HEARTCB
					PRINTFORMW 」
				ENDIF
			;ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;暴れ失敗(REACT분류:소극적으로 한다)
		;-------------------------------------------------
		IF 구상記入チェック(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※内容を書き分けるならコメントアウトを解除して使用して下さい
			;밧줄구속による暴れ失敗
			IF TFLAG:REACT파생 == 1
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
				PRINTFORMW
			ENDIF
			RETURN 1
			[SKIPEND]
			
			;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1, 2を書いた場合はREACT파생0,「暴れ成功」(REACT분류:날뛴다)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 구상記入チェック(100)
			IF RAND:2 == 0
				PRINTFORMW 「정-말！　날뛰면 안되어요……」
				PRINTFORMW 「말하는 것 듣지 않는 아이에게는 벌칙 있으니까요」
			ELSE
				PRINTFORMW 「정말-！　모처럼 릴리가 기분 좋게 해준걸……」
				PRINTFORMW 「아픔에서도 ”정액”은 짤 수 있지만,%CNAME(MASTER)%도 아픈 것은 싫겠지요？」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 수음
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1は派生0と同じ種類のものを違う形で提示してみます
;まぁこんなやりかたもあるという事で
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("부끄러워한다")
		;快感に부끄러워한다
		;IF TFLAG:REACT파생 == 1
		;	PRINTFORMW 
		;状況に부끄러워한다
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		;수음
		IF RAND:2 == 0
			PRINTFORMW 「저기, 부끄러워하지 않아도 좋아요」
			PRINTFORMW 「상스러운 소리를 내도……」
			PRINTFORMW 「야무지지 못하게 폭발시켜도……」
			PRINTFORMW 「아무리 치태를 부려도……」
			PRINTFORMW 「……그것도 %CNAME(MASTER)%의 매력의 일부이니까」
		ELSE
			PRINTFORMW 「여자 아이에게 야한짓 당하는것 익숙해져 있지 않네요」
			PRINTFORMW 「괜찮아요, 릴리와 함께 천천히 익숙해져 가요♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고 모욕한다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("서투르다고 모욕한다")
		;強がって					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;未熟なテクニックに対して	REACT분류"태연하게 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 참는다")
		;派生:애원					REACT분류"용서를 빌다"
		;IF TFLAG:REACT파생 == 1
		;	PRINTFORMW 
		;派生:耐久					REACT분류"소극적으로 한다"
		;ELSE
		;	PRINTFORMW
		;ENDIF
		IF RAND:2 == 0
			PRINTFORMW 「저기, 부끄러워하지 않아도 좋아요」
			PRINTFORMW 「상스러운 소리를 내도……」
			PRINTFORMW 「야무지지 못하게 폭발시켜도……」
			PRINTFORMW 「아무리 치태를 부려도……」
			PRINTFORMW 「……그것도 %CNAME(MASTER)%의 매력의 일부이니까」
		ELSE
			PRINTFORMW 「여자 아이에게 야한짓 당하는것 익숙해져 있지 않네요」
			PRINTFORMW 「괜찮아요, 릴리와 함께 천천히 익숙해져 가요♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("날뛴다")
		;暴れ失敗(REACT분류:소극적으로 한다)
		;IF GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;	PRINTFORMW 
		;暴れ成功
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		IF RAND:2 == 0
			PRINTFORMW 「날뛰면 분명하게 시코시코 할 수 없어요―！」
			PRINTFORMW 「자지는 이렇게 기분 좋아 하는데 무엇이 불만인가요」
		ELSE
			PRINTFORMW 「싫어한다는 것은 여기가 약점인가요？　후후♪」
			PRINTFORMW 「릴리가 새디즘에 눈을 떠 버리면 %CNAME(MASTER)%의 탓이니까요？」
			PRINTFORM 「그 때는 「이런 식으로」책임져 줄테니까
			CALL HEARTCB
			PRINTFORMW 」
			PRINTL  
			PRINTFORMDW 릴리가 하반신에 걸쳐, 흰 허벅지로 꼬옥 허리를 구속한다
			PRINTFORMDW %CALLNAME:MASTER%(은)는 필사적으로 몸을 비꼼, 근심 해 기분인 교성을 흘리면서,
			PRINTFORMDW 남근의 첨단에”정”이 올라 채워 가는 감각에 전신을 진동시켰다……
		ENDIF
		RETURN 1
	ENDIF
	
	;손間を厭わないのであれば、IFなり三項なりを使って内部で書き分けるのもあり
	;=================================================
	;REACT분류4, 受け入れる＆COM, 쾌감을 받아들인다/기분 좋다!
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("受け入れる") && IS_NOWCOMNAME("더 하드하게 해/쾌감을 받아들인다/기분 좋다!")
		;消費기력による派生
		SELECTCASE GET_COM_RANK()
			;自然な행동
			CASE "自然な행동"
				PRINTFORMW 
			;無理のない행동
			CASE "無理のない행동"
				PRINTFORMW 
			;無理を했다행동
			CASE "無理を했다행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류2, 태연하게 한다＆COM, 겁없이 웃는다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("태연하게 한다") && IS_NOWCOMNAME("겁없이 웃는다")
		;消費기력による派生
		SELECTCASE GET_COM_RANK()
			;自然な행동
			CASE "自然な행동"
				PRINTFORMW 
			;無理のない행동
			CASE "無理のない행동"
				PRINTFORMW 
			;無理を했다행동
			CASE "無理を했다행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;COMが妨害/애원に派生했다らCOMへ流す
	;&& TFLAG:REACT파생 < 1自体はなくてもREACT분류の決めうちで同様の結果を得られますが、仕様の可視化ということで
	;=================================================
	;REACT분류2, 소극적으로 한다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("소극적으로 한다") && IS_NOWCOMNAME("싫어한다/쾌감을 참는다") && TFLAG:REACT파생 < 1
		;消費기력による派生
		SELECTCASE GET_COM_RANK()
			;自然な행동
			CASE "自然な행동"
				PRINTFORMW 
			;無理のない행동
			CASE "無理のない행동"
				PRINTFORMW 
			;無理を했다행동
			CASE "無理を했다행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2, 페니스밴드수음
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0/1を参考に拡張するなり、統合するなり
;そんな訳でここは最小限にしておきます
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
	;=================================================
	;COM12, 서투르다고 모욕한다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("서투르다고 모욕한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT10, 손으로 애무

;────────────────────────────────────
;REACT11, 가슴애무・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=가슴애무/1=乳首吸い/2=揉み合い/3=乳首責め)
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
;・地の文はACT파생2, 揉み合いとその他で分岐
;・ACTION_APPLY2に於いて、ACT파생2の場合は受容、거부の判定処理あり
;・ACT파생2, 揉み合いにおいてREACT분류(소극적으로 한다/태연하게 한다/적극적으로 한다/受け入れる)以外は"거부"扱い
;-------------------------------------------------
;　派生毎にまともにやると손間が尋常ではないので、ACT파생分岐の上層で汎用処理했다り
;　各ACT파생内のCOM分岐等は一部のものに絞ってしまうのが良いと思います
;　またACT파생で区切らずに、大分岐でまとめてしまう손もありますが、손の込んだ事をやるには不向きであると考えます
;-------------------------------------------------
@KOJO_REACT_K15_11
;-------------------------------------------------
;決まったやりかたなどというものはないので、以下に示すものはあくまで一つの例ということで
;分岐の増減、COMに流すのか否かの判断は作者の判断に委ねます
;-------------------------------------------------

;*************************************************
;COM単体の分岐 ACT파생の上層で処理
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;「この恥知らずの豚め！」(징계・포인트加算)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2は挙動が異なるので条件で弾く
;-------------------------------------------------
IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다") && TFLAG:ACT파생 !=2
	;=================================================
	;快感に対して부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1を書いた場合はREACT파생0, 状況に対して부끄러워한다扱い
	;派生1を방치했다場合はここで補完し、&& TFLAG:REACT파생 < 1などとしてCOMに流してしまうのもあり
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("싫어한다")
	;ACT파생2の場合はACT파생2分岐内のIS_COMGRONAME("거부")で処理
	;=================================================
	;嫌がって妨害했다(REACT분류6, 날뛴다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1 && TFLAG:ACT파생 !=2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1を書いた場合はREACT파생0, 嫌がった(REACT분류1, 소극적으로 한다)扱い
	;派生1を방치했다場合はここで補完し、&& TFLAG:REACT파생 < 1などとしてCOMに流してしまうのもあり
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고 모욕한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("서투르다고 모욕한다")
	;=================================================
	;強がって(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1を書いた場合はREACT파생0, 未熟なテクニックに対して(REACT분류:태연하게 한다)扱い
	;派生1を방치했다場合はここで補完し、&& TFLAG:REACT파생 < 1などとしてCOMに流してしまうのもあり
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF

	;=================================================
	;COM15, 쾌감을 받아들인다/기분좋게 해/기분 좋다!
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 받아들인다/기분좋게 해/기분 좋다!")
		IF FAVOR(3000, 3000) && RAND:3
			IF RAND:2 == 0
				PRINTFORM 「완전히 젖가슴이 성감대가 되어 버렸네요
				CALL HEARTCB
				PRINTFORMW 」
				PRINTFORMW 「싫어하는 가슴……정말 좋아해요♪」
				PRINTFORMW 「좀더 좀더 어지러워져요♪」
			ELSE
				PRINTFORMW 「젖가슴 만지작거려져 주저앉았어？ 귀여워♪」
				PRINTFORMW 「그러며언, 가슴으로 갈때까지 문질문질 해줄게요♪」
				PRINTFORM 「한참 잔뜩 , 무뉴무뉴～～～～
				CALL HEARTCB
;OriginalString : 」
				PRINTW 」
			ENDIF
		ELSEIF FAVOR(0, 2000)
			IF RAND:2 == 0
				PRINTFORMW 「젖가슴 만지작거려져 완전히 헤벌레 얼굴이 되어 버렸네요♪」
				PRINTFORMW 「좀더 좀더 쾌감을 쏟아 줄게요……」
				PRINTFORMW 「빵빵하게 부풀어, 모유가 나와버릴 정도로 말이야♪」
			ELSE
				PRINTFORMW 「기분 좋은거네요？　후후♪」
				PRINTFORM 「싫어서, 야한 가슴
				CALL HEARTCB
;OriginalString : 」
				PRINTW 」
				PRINTFORMW 「좀 더 가득 개발해, 충분히 정력을 들이마셔 줄게요♪」
			ENDIF
		ELSE
			IF RAND:2 == 0
				PRINTFORMW 「겨우 솔직해져 주었어요♪」
				PRINTFORMW 「귀여운 가슴, 많이 괴롭혀 줄게요♪」
			ELSE
				PRINTFORM 「쾌감을 받아들이면 후는 줄곧 저속해질 뿐인데……
				CALL HEARTCB
				PRINTFORMW 」
				PRINTFORMW 「릴리가%CNAME(MASTER)%의 젖가슴, 야하게 길러 주네요♪」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;*************************************************
;以下、ACT파생毎の分岐
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 가슴애무
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && !TFLAG:ACT파생
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
		;派生:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;派生:耐久					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 乳首吸い
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자に素質(유치/혀기술)がない場合は発生する確立は低い
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
		;派生:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;派生:耐久					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2, 揉み合い
;━━━━━━━━━━━━━━━━━━━━━━━━
;봉사系COMが선택肢となる場合あり
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
	;=================================================
	;要求に応じなかった
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("거부")
		;恥ずかしがって
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
			PRINTFORMW 
		;嫌がって
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;-----------------------------------------------------------------
	;以下、COMを並列化
	;손間を厭わないのであれば、IFなり三項なりを使って内部で書き分けるのもあり
	;-----------------------------------------------------------------
	
	;=================================================
	;REACT분류4, 受け入れる＆COM, 쾌감을 받아들인다/기분 좋다!
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("受け入れる") && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋다!")
		;消費기력による派生
		SELECTCASE GET_COM_RANK()
			;自然な행동
			CASE "自然な행동"
				PRINTFORMW 
			;無理のない행동
			CASE "無理のない행동"
				PRINTFORMW 
			;無理を했다행동
			CASE "無理を했다행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	;=================================================
	;REACT분류2, 태연하게 한다＆COM, 겁없이 웃는다/태연하게 한다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("태연하게 한다") && IS_NOWCOMNAME("겁없이 웃는다/태연하게 한다")
		;消費기력による派生
		SELECTCASE GET_COM_RANK()
			;自然な행동
			CASE "自然な행동"
				PRINTFORMW 
			;無理のない행동
			CASE "無理のない행동"
				PRINTFORMW 
			;無理を했다행동
			CASE "無理を했다행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류2, 적극적으로 한다＆COM, 적극적으로 한다/애정을 담아 한다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("적극적으로 한다") && IS_NOWCOMNAME("적극적으로 한다/애정을 담아 한다")
		;消費기력による派生
		SELECTCASE GET_COM_RANK()
			;自然な행동
			CASE "自然な행동"
				PRINTFORMW 
			;無理のない행동
			CASE "無理のない행동"
				PRINTFORMW 
			;無理を했다행동
			CASE "無理を했다행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;COM嫌がって妨害/애원に派生했다らCOMへ流す
	;&& TFLAG:REACT파생 < 1自体はなくてもREACT분류の決めうちで同様の結果を得られますが、仕様の可視化ということで
	;=================================================
	;REACT분류2, 소극적으로 한다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("소극적으로 한다") && IS_NOWCOMNAME("싫어한다/쾌감을 참는다") && TFLAG:REACT파생 < 1
		;消費기력による派生
		SELECTCASE GET_COM_RANK()
			;自然な행동
			CASE "自然な행동"
				PRINTFORMW 
			;無理のない행동
			CASE "無理のない행동"
				PRINTFORMW 
			;無理を했다행동
			CASE "無理を했다행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생3, 乳首責め
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자に素質(유치/혀기술)がない場合は発生する確立が高い
;죠교대상이남자の場合はここにしか落ちない
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 3
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다 & 부끄러워한다
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 참는다/부끄러워한다")
		;派生:애원					REACT분류"용서를 빌다"
		;IF TFLAG:REACT파생 == 1
		;	PRINTFORMW 
		;派生:耐久					REACT분류"소극적으로 한다"
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		IF FAVOR(3000, 3000) && RAND:3
			IF RAND:2 == 0
				PRINTFORMW 「유두로 느끼는 것이 부끄러운거야？」
				PRINTFORMW 「언제나 상스러운 소리 내 허덕여에 말이야♪」
				PRINTFORM 「키득……%CNAME(MASTER)%라고 정말로 사랑스럽기 때문에
				CALL HEARTCB
;OriginalString : 」
				PRINTW 」
			ELSE
				PRINTFORMW 「유두의 쾌감은 이상하네요」
				PRINTFORMW 「처음은 참을 수 있어도, 자꾸자꾸 쾌감에 항네 없어져 가……」
				PRINTFORM 「응, 이제 한계지요？　부끄러워하지 말고 유두로 느끼면
				CALL HEARTCB
;OriginalString : 」
				PRINTW 」
			ENDIF
		ELSEIF FAVOR(0, 2000)
			IF RAND:2 == 0
				PRINTFORMW 「어？　허리가 뛰어 버리고 있습니다？」
				PRINTFORMW 「가슴 밖에 만지작거리지 않았는데 이상하네요……후후♪」
				PRINTFORMW 「이제 들키고 들키고이니까 숨기지 않아도 괜찮아요♪」
			ELSE
				PRINTFORMW 「릴리,%CNAME(MASTER)%하지만 젖가슴 만지작거려지는 것 좋아하는 알고 있어요 」
				PRINTFORMW 「언제나 상스러운 소리를 높여, 매우 행복하게가는이라고……♪」
				PRINTFORM 「인내 멈추면(자), 진정한 기분 좋음……맛 맞추어 주는데
				CALL HEARTCB
;OriginalString : 」
				PRINTW 」
				
			ENDIF
		ELSE
			IF RAND:2 == 0
				PRINTFORMW 「유두로 느끼는 것이 부끄럽습니까？」
				PRINTFORM 「귀여운 아이……♪ 좀 더 흐트러진 모습, 보고 싶어져 버렸습니다
				CALL HEARTCB
				PRINTFORMW 」
			ELSE
				PRINTFORMW 「숨겨도 릴리에는 쓸데없어요♪」
				PRINTFORMW 「가슴 손댈 때에,%CNAME(MASTER)%의 신체가 피쿤은 뛰는 것 알고 있습니다 것♪」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT11, 가슴애무

;────────────────────────────────────
;REACT12, 입으로 애무・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=クンニ/1=펠라)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K15_12
;-------------------------------------------------
;・地の文はACT파생毎にREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;・派生毎に区切ってありますが、例えばACT파생1のみ書いてあとは汎用に投げるという事もできます
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, クンニ
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0においては参考として可能性のあるCOMを網羅しておきます
;필요に応じて分岐を増減、ないし構造を改変して使用して下さい
;-------------------------------------------------
IF 구상記入チェック(0) && !TFLAG:ACT파생
	;=================================================
	;COM6, 더 하드하게 해(REACT분류:受け入れる)
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("더 하드하게 해")
		;-------------------------------------------------
		;「この恥知らずの豚め！」(징계・포인트加算)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 구상記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다")
		;快感に부끄러워한다
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;状況に부끄러워한다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("싫어한다")
		;嫌がって妨害했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;嫌がった					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고 모욕한다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("서투르다고 모욕한다")
		;強がって					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;未熟なテクニックに対して	REACT분류"태연하게 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
		;派生:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;派生:耐久					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 펠라
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1は派生0と同じ種類のものを違う形で提示してみます
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다")
		;快感に부끄러워한다
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;状況に부끄러워한다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고 모욕한다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("서투르다고 모욕한다")
		;強がって					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;未熟なテクニックに対して	REACT분류"태연하게 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;손間を厭わないのであれば、IFなり三項なりを使って内部で書き分けるのもあり
	;=================================================
	;REACT분류4, 受け入れる＆COM, 더 하드하게 해/쾌감을 받아들인다/기분 좋다!
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("受け入れる") && IS_NOWCOMNAME("더 하드하게 해/쾌감을 받아들인다/기분 좋다!")
		;消費기력による派生
		SELECTCASE GET_COM_RANK()
			;自然な행동
			CASE "自然な행동"
				PRINTFORMW 
			;無理のない행동
			CASE "無理のない행동"
				PRINTFORMW 
			;無理を했다행동
			CASE "無理を했다행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류2, 태연하게 한다＆COM, 겁없이 웃는다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("태연하게 한다") && IS_NOWCOMNAME("겁없이 웃는다")
		;消費기력による派生
		SELECTCASE GET_COM_RANK()
			;自然な행동
			CASE "自然な행동"
				PRINTFORMW 
			;無理のない행동
			CASE "無理のない행동"
				PRINTFORMW 
			;無理を했다행동
			CASE "無理を했다행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;COMが妨害/애원に派生했다らCOMへ流す
	;&& TFLAG:REACT파생 < 1自体はなくてもREACT분류の決めうちで同様の結果を得られますが、仕様の可視化ということで
	;=================================================
	;REACT분류2, 소극적으로 한다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("소극적으로 한다") && IS_NOWCOMNAME("싫어한다/쾌감을 참는다") && TFLAG:REACT파생 < 1
		;消費기력による派生
		SELECTCASE GET_COM_RANK()
			;自然な행동
			CASE "自然な행동"
				PRINTFORMW 
			;無理のない행동
			CASE "無理のない행동"
				PRINTFORMW 
			;無理を했다행동
			CASE "無理を했다행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT12, 입으로 애무

;────────────────────────────────────
;REACT13, 항문애무・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생（0=항문애무/1=전립선자극/2=항문바이브出し入れ）
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K15_13
;-------------------------------------------------
;・地の文はACT파생で分岐。ACT파생1, 2分岐内の派生なし
;・ACT파생0, 항문애무のみREACT분류(受け入れる/적극적으로 한다, 태연하게 한다/용서를 빌다/소극적으로 한다)で派生処理される
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 항문애무
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0においては可能性のあるCOMを網羅しておきます
;필요に応じて分岐を増減、ないし構造を改変して使用して下さい
;-------------------------------------------------
IF 구상記入チェック(0) && !TFLAG:ACT파생
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
		;派生1の場合はREACT분류:なし
		;-------------------------------------------------
		;意見を出すなんていい度가슴だね！(징계・포인트加算)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 구상記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다")
		;快感に부끄러워한다
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;状況に부끄러워한다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("싫어한다")
		;嫌がって妨害했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;嫌がった					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고 모욕한다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("서투르다고 모욕한다")
		;強がって					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;未熟なテクニックに対して	REACT분류"태연하게 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
		;派生:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;派生:耐久					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 전립선자극
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상이남자の場合のみに派生。地の文の派生なし
;ACT파생0を参考に拡張するなり、統合するなり
;そんな訳でここは最小限にしておきます
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	
	;REACTIONに関わらず以下の地の文が表示される
	;直腸から前立腺をこねくり回される감각に%CALLNAME:MASTER%의 남성기は膨張し、
	;渦を巻くような強烈な사정感がこみ上げてくる…
	
	;=================================================
	;COM14, 쾌감을 참는다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
		;派生:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;派生:耐久					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다/COM16, 기분 좋다!
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋다!")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2, 항문바이브出し入れ
;━━━━━━━━━━━━━━━━━━━━━━━━
;地の文はTALENT:MASTER:남자か否かで分岐
;ACT파생0を参考に拡張するなり、統合するなり
;そんな訳でここは最小限にしておきます
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
	;=================================================
	;죠교대상이남자
	;=================================================
	;直腸から前立腺をこねくり回される감각に%CALLNAME:MASTER%의 남성기は膨張し、
	;渦を巻くような強烈な사정感がこみ上げてくる…
	;-------------------------------------------------
	IF 구상記入チェック(0) && TALENT:MASTER:남자
		;-------------------------------------------------
		;COM14, 쾌감을 참는다
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
			;派生:애원					REACT분류"용서를 빌다"
			IF TFLAG:REACT파생 == 1
				PRINTFORMW 
			;派生:耐久					REACT분류"소극적으로 한다"
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;COM15, 쾌감을 받아들인다/COM16, 기분 좋다!
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋다!")
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	;%CALLNAME:MASTER%は便意のような異様な快感に悶えた…
	;-------------------------------------------------
	IF 구상記入チェック(0)
		;-------------------------------------------------
		;COM14, 쾌감을 참는다
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
			;派生:애원					REACT분류"용서를 빌다"
			IF TFLAG:REACT파생 == 1
				PRINTFORMW 
			;派生:耐久					REACT분류"소극적으로 한다"
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;COM15, 쾌감을 받아들인다/COM16, 기분 좋다!
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋다!")
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT13, 항문애무

;────────────────────────────────────
;REACT14, 항문펠라・個別リアクション
;────────────────────────────────────
;・ACT파생なし。ACTION_APPLY2の処理なし
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K15_14
;-------------------------------------------------
;・地の文はREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;・ACT파생はないのでCOM分岐で完全に区切ります
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다")
	;快感に부끄러워한다
	IF TFLAG:REACT파생 == 1
		PRINTFORMW 
	;状況に부끄러워한다
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("싫어한다")
	;嫌がって妨害했다			REACT분류"날뛴다"
	IF TFLAG:REACT파생 == 1
		PRINTFORMW 
	;嫌がった					REACT분류"소극적으로 한다"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고 모욕한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("서투르다고 모욕한다")
	;強がって					REACT분류"소극적으로 한다"
	IF TFLAG:REACT파생 == 1
		PRINTFORMW 
	;未熟なテクニックに対して	REACT분류"태연하게 한다"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류"태연하게 한다")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;派生:애원					REACT분류"용서를 빌다"
	IF TFLAG:REACT파생 == 1
		PRINTFORMW 
	;派生:耐久					REACT분류"소극적으로 한다"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류"受け入れる")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!(REACT분류"受け入れる")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
	PRINTFORMW 
	RETURN 1
ENDIF
;REACT14, 항문펠라

;────────────────────────────────────
;REACT15, 키스・個別リアクション
;────────────────────────────────────
;・ACT파생なし。ACTION_APPLY2の処理なし
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;その他反応
;	COM:자위를 시작한다
;-------------------------------------------------
@KOJO_REACT_K15_15
;-------------------------------------------------
;・地の文はREACT분류(적극적으로 한다/受け入れる/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;・TEQUIP:성교중は体位毎、TEQUIP:성교봉사중はTCVAR:MASTER:애태움도で派生する
;・可能性のあるものを一通り置いておきます。필요に応じて分岐を増減して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;「この恥知らずの豚め！」(징계・포인트加算)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;快感に부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「今の状況に부끄러워한다」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1の場合はREACT분류:날뛴다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;嫌がって妨害했다(REACT분류:날뛴다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「싫어한다がACTは妨害せず」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고 모욕한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
	;=================================================
	;快感を感じながら(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「実際に下손な場合」(REACT분류:태연하게 한다)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT분류:용서를 빌다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「耐久」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
	PRINTFORMW 
	RETURN 1
ENDIF
;REACT15, 키스

;────────────────────────────────────
;REACT16, 성기마찰한다・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생（=0,貝あわせ/=1,스마타/=2,페니스밴드스마타）
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_K15_16
;-------------------------------------------------
;・地の文はACT파생毎にREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다, 소극적으로 한다)で分岐
;・필요に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 貝あわせ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && !TFLAG:ACT파생
	;=================================================
	;쾌감을 참는다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 스마타
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	;=================================================
	;쾌감을 참는다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2, 페니스밴드스마타
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
	;=================================================
	;쾌감을 참는다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT16, 성기마찰한다

;────────────────────────────────────
;REACT17, 파이즈리한다・個別リアクション
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K15_17
;-------------------------------------------------
;・地の文はCOM:허리를 흔든다、REACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;・애무系COMを網羅。필요に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;「この恥知らずの豚め！」(징계・포인트加算)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;快感に부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「今の状況に부끄러워한다」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;嫌がって妨害했다(REACT분류:날뛴다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「싫어한다がACTは妨害せず」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고 모욕한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
	;=================================================
	;快感を感じながら(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「実際に下손な場合」(REACT분류:태연하게 한다)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움時には선택不可
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("허리를 흔든다")
	;=================================================
	;가버려상태で애태움終了/애태움無し
	;=================================================
	IF 구상記入チェック(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 < 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 고통을 참는다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("고통을 참는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("비명지른다")
	;=================================================
	;공포による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
;REACT분류が날뛴다であった場合は성교は취소になる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;暴れ失敗(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상記入チェック(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;밧줄구속による暴れ失敗
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT파생0,「暴れ成功」(REACT분류:날뛴다)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT17, 파이즈리한다

;────────────────────────────────────
;REACT18, 풋잡하기・個別リアクション
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_K15_18
;-------------------------------------------------
;・地の文はREACT분류(受け入れる/적극적으로 한다, 태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;・필요に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;快感に부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「今の状況に부끄러워한다」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1の場合はREACT분류:날뛴다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;嫌がって妨害했다(REACT분류:날뛴다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「싫어한다がACTは妨害せず」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고 모욕한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
	;=================================================
	;快感を感じながら(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「実際に下손な場合」(REACT분류:태연하게 한다)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT분류:용서를 빌다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「耐久」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("비명지른다")
	;=================================================
	;공포による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT분류:소극적으로 한다扱い
;REACT분류が날뛴다であった場合は성교は취소になる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;暴れ失敗(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상記入チェック(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;밧줄구속による暴れ失敗
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;손間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT파생0,「暴れ成功」(REACT분류:날뛴다)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;ただCOM32날뛴다の場合、REACT파생によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT18, 풋잡하기

;────────────────────────────────────
;REACT19, 손가락삽입한다
;────────────────────────────────────
;・TFLAG:ACT파생(0=손가락삽입한다/1=Ｇスポット刺激/2=바이브出し入れ)
;-------------------------------------------------
;想定されるCOM
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_K15_19
;-------------------------------------------------
;地の文はACT파생0のみREACT분류で分岐
;その他はTALENT:MASTER:후타나리であるか否かで派生するのみ
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 손가락삽입한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다, 소극적으로 한다)で分岐
;・애무系COMを網羅。필요に応じて分岐を増減して使用して下さい
;-------------------------------------------------
IF 구상記入チェック(0) && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다")
		;-------------------------------------------------
		;快感に부끄러워한다
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「今の状況に부끄러워한다」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 구상記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;派生1の場合はREACT분류:날뛴다扱い
	;=================================================
	;COM11, 싫어한다(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("싫어한다")
		;-------------------------------------------------
		;嫌がって妨害했다(REACT분류:날뛴다)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「싫어한다がACTは妨害せず」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 구상記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM12, 서투르다고 모욕한다
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
		;-------------------------------------------------
		;快感を感じながら(REACT분류:소극적으로 한다)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「実際に下손な場合」(REACT분류:태연하게 한다)扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 구상記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("겁없이 웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1の場合はREACT분류:용서를 빌다扱い
	;=================================================
	;COM14, 쾌감을 참는다(소극적으로 한다)
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
		;-------------------------------------------------
		;애원(REACT분류:용서를 빌다)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT파생0,「耐久」扱い
		;書かなかった場合はREACT파생に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 구상記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋다!(REACT분류:受け入れる)
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, Ｇスポット刺激
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文は죠교대상が후타나리か否かで分岐
;・REACT분류による分岐はなく、内容は汎用的なもの
;-------------------------------------------------
;とりあえず最小限の構成にしておきます。필요に応じて分岐を追加して下さい
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	;질내から前立腺をこねくり回される감각に%CALLNAME:MASTER%의 남성기は膨張し、
	;渦を巻くような強烈な사정感がこみ上げてくる…
	;=================================================
	;죠교대상が후타나리
	;=================================================
	IF 구상記入チェック(0) && TALENT:MASTER:후타나리
		;-------------------------------------------------
		;쾌감을 받아들인다
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;쾌감을 참는다
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
	
	;%CALLNAME:MASTER%はＧスポットを指でえぐられる감각に
	;髪を振り乱して悶えた…
	;=================================================
	;죠교대상に페니스없음
	;=================================================
	IF 구상記入チェック(0) && !PENIS(MASTER)
		;-------------------------------------------------
		;쾌감을 받아들인다
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;쾌감을 참는다
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2, 바이브出し入れ
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文は죠교대상が후타나리か否かで分岐
;・REACT분류による分岐はなく、内容は汎用的なもの
;-------------------------------------------------
;とりあえず最小限の構成にしておきます。필요に応じて分岐を追加して下さい
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
	;질내から前立腺をこねくり回される감각に%CALLNAME:MASTER%의 남성기は膨張し、
	;渦を巻くような強烈な사정感がこみ上げてくる…
	;=================================================
	;죠교대상が후타나리
	;=================================================
	IF 구상記入チェック(0) && TALENT:MASTER:후타나리
		;-------------------------------------------------
		;쾌감을 받아들인다
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;쾌감을 참는다
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
	
	;%CALLNAME:MASTER%はＧスポットを바이브にえぐられる감각に
	;髪を振り乱して悶えた…
	;=================================================
	;죠교대상に페니스없음
	;=================================================
	IF 구상記入チェック(0) && !PENIS(MASTER)
		;-------------------------------------------------
		;쾌감을 받아들인다
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;쾌감을 참는다
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;────────────────────────────────────
;REACT27, 밀어 넘어트린다・個別リアクション
;────────────────────────────────────
;TFLAG:ACT파생（=0,밀어 넘어트린다/=1,밀어넘어짐終了）
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:마음 약하게 응답한다/무뚝뚝하게 응답한다/상냥하게 응답한다/강하게 응답한다/용서를 빌다/기분좋게 해
;고통系反応
;	COM:무서워한다
;-------------------------------------------------
@KOJO_REACT_K15_27
;-------------------------------------------------
;地の文はREACT분류(受け入れる/적극적으로 한다, 태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;ACT파생毎の分岐はなしかつ、内容的にはACT파생0のもの
;ACT파생1の場合は整合性が崩れると思われる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 밀어 넘어트린다
;━━━━━━━━━━━━━━━━━━━━━━━━
;この箇所は個別のCOM毎に記述するには適さないと思われるので
;REACT분류で分けておきます
;-------------------------------------------------
IF 구상記入チェック(0) && !TFLAG:ACT파생
	;=================================================
	;REACT분류受け入れる
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("受け入れる")
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		PRINTFORMW
		RETURN 1
	ENDIF

	;=================================================
	;REACT분류용서를 빌다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("용서를 빌다")
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류소극적으로 한다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("소극적으로 한다")
		PRINTFORMW
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 밀어넘어짐終了
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1,밀어넘어짐と処理が共通なのでちょっとアレな感じの箇所
;何もせずにRETURN 1だけ返すのもアリな気もします
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
ENDIF


