;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[릴리 화이트]구상 회화계 개별 리액션구상파일
;
;	회화계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어선택했다COM가 자연스러운행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운행동/1=무리가 없는행동/2=무리를했다행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 지금 기분을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K15_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일했다 있고)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다로, 리액션이빈정거린다 이외의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;자연스러운행동
	;=================================================
	;REACT부하 0, 질문에 분명히 답하는 것이 할 수 없었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 말씨 나쁘게 질문에 답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 나가려 한 말을 삼켜 대답을 탁했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「무우-?   릴리에게는…… 말하기 어려운 일인가요?」
			PRINTFORMW 「우우……무리하게, 라고는 말하지 않지만」
			PRINTFORMW 「역시 조금, 기분이 쳐져 버려요」
		ELSE
			IF RAND:3 == 0
				PRINTFORMW 「어쩐지 불투명하네요……」
				PRINTFORMW 「릴리는 작지만,%CNAME(MASTER)%의 기분, 분명하게 받아들여져요?」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「무슨 일인가요?   부끄러운 일, 인가요?」
				PRINTFORMW 「릴리도 무리하게는 묻지 않지만……무우―……」
				PRINTFORMW 「%CNAME(MASTER)%(은)는 꽤 부끄러워 하네요」
			ELSE
				PRINTFORMW 「응, 들리지 않아요」
				PRINTFORMW 「 좀 더%CNAME(MASTER)%의 솔직한 기분을 들려주세요」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;자연스러운행동
	;=================================================
	;REACT부하 0, 어려운 말을 퍼부었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 용기를 내 강하게 맞았다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 눈을 일등 해입에서는 강하게 거절하는마이
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「앙♪　정말, 깜짝 놀라 버렸어요」
			PRINTFORMW 「무우―……이건 릴리도 질 수 없겠네요♪」
		ELSE
			PRINTFORMW 「꺅!   까, 깜짝 놀라 버렸어요」
			PRINTFORMW 「이것이 「조교의 보람이 있다」라는 건가요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;자연스러운행동
	;=================================================
	;REACT부하 0, 여유의 미소로 어깨를 움츠리고 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 조금 생각하고 나서 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 간신히 야유 섞임에 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「정말, 괴롭히지 말아 주세요……」
			PRINTFORMW 「…… 혹시, 릴리 조롱하고 있나요?」
		ELSE
			PRINTFORMW 「후후♪  릴리 알아 버렸어요!」
			PRINTFORMW 「……%CNAME(MASTER)%(은)는 좀 더 벌 칙 을 주었으면 하는거네요♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;-------------------------------------------------
	;소분기
	;-------------------------------------------------
	;처음
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(200)
		;죠교대상로부터의호의 500 이상
		IF FAVOR(500)
			PRINTFORMW 「아우……릴리는 어떻게 받아 들이면 좋을까요」
			PRINTFORMW 「그렇지만,%CNAME(MASTER)%의 기분을 들을 수 있어, 릴리는 기뻐요♪」
		;죠교대상로부터의호의 500 미만
		ELSE
			PRINTFORMW 「후후♪  릴리 알아 버렸어요!」
			PRINTFORMW 「……%CNAME(MASTER)%(은)는 좀 더 벌 칙 을 주었으면 하는거네요♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;죠교대상로부터의호의 1000 이상
	;처음
	;=================================================
	IF 구상기입체크(100) && FAVOR(1000)
		;역강간로질내사정의 후
		IF TFLAG:전회질싸 == 2
			PRINTFORMW 「앗, 후아♪　기뻐요♪」
			PRINTFORMW 「%CNAME(MASTER)%의 발기 자지도 , 릴리에 범해져 완전히 좋은 아이가 되었어요♪」
		;섹스로질내사정의 후
		ELSEIF TFLAG:전회질싸 == 1
			PRINTFORMW 「앗, 후아, 응♪　그렇지만 , 아직 멀었어요♪」
			PRINTFORMW 「%CNAME(MASTER)%의 말로 , 릴리 좀 더 흥분해 버렸으니까♪」
		;통상
		ELSE
			PRINTFORMW 「응……그렇게 분명히 말해지면, 릴리도 부끄러워요」
			PRINTFORMW 「그렇지만, 매우 기뻐요…… 정말이에요?   후후♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;죠교대상로부터의호의 500 이상
	;처음
	;=================================================
	IF 구상기입체크(100) && FAVOR(500)
		;역강간로질내사정의 후
		IF TFLAG:전회질싸 == 2
			PRINTFORMW 「아하아♪　릴리의 보지, 기분 좋은가요♪」
			PRINTFORMW 「솔직한 자지는, 좀 더 좋은 아이 좋은 아이 해 주고 싶어요♪」
		;섹스로질내사정의 후
		ELSEIF TFLAG:전회질싸 == 1
			PRINTFORMW 「응,♪　솔직한 아이는 릴리 정말 좋아해요♪」
			PRINTFORMW 「 좀 더 빙빙 가치가치 해♪　주인님에게 봉사해 주세요♪」
		;통상
		ELSE
			PRINTFORMW 「너무 정면에서 말해지면 쑥스럽네요……후후♪」
			PRINTFORMW 「%CNAME(MASTER)%의 기분, 릴리는 매우 기뻐요♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;자연스러운행동
	;=================================================
	;REACT부하 0, 함께 있어지는 것이 기쁘면솔직에입에했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 분명히 지금의 기분을 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 기분을 해치지 않게, 손질해 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			IF RAND:3 == 0
				PRINTFORMW 「……기뻐요」
				PRINTFORMW 「%CNAME(MASTER)%의 말로, 릴리는……」
				PRINTFORMW 「으응, 릴리는%CNAME(MASTER)%(와)과 함께, 둘이서 행복하게 되고 싶어요」
				PRINTFORMW 「%CNAME(MASTER)%의 기분……말로, 신체로 연결되어, 좀 더 깊게 알고 싶은데」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「우우……어쩐지 쑥스럽네요」
				PRINTFORMW 「……릴리만, 너무해요……응」
				PRINTL  
				PRINTFORMDW 릴리는 부드러운 가슴으로 %CALLNAME:MASTER%의 얼굴을 감싸는 것처럼 상냥하게 껴안았다……
				PRINTL  
				PRINTFORMW 「%CNAME(MASTER)%……좋아해요……너무 좋아……」
				PRINTFORMW 「……후후, 귀까지 빨개……%CNAME(MASTER)% 귀여워어, 정말좋아♪」
			ELSE
				PRINTFORMW 「%CNAME(MASTER)%가 기뻐해 주면, 릴리도 기뻐요」
				PRINTFORMW 「좀더 좀더 %CNAME(MASTER)%의 일 들려주세요」
				PRINTFORMW 「%CNAME(MASTER)%(이)가 하고 싶은 것, 릴리에게 했으면 하는 일……」
				PRINTFORMW 「가득 가득, 릴리에게 들려줘……」
			ENDIF
		ELSE
			PRINTFORMW 「%CNAME(MASTER)% 솔직해져 주어 기뻐요」
			PRINTFORMW 「릴리도……%CNAME(MASTER)%가 싫어하는 것은 하고 싶지 않고」
			PRINTFORMW 「%CNAME(MASTER)%(와)과 함께……기분 좋아지고 싶으니까♪」
			PRINTFORMW 「%CNAME(MASTER)%의 기분, 좀 더 릴리에게 들려주세요♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 4
	;자연스러운행동
	;=================================================
	;REACT부하 0, 그렇게 나쁘다고 생각하지는 않다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「그저 나쁘지는 않다」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은적극			시선을 피해, 나쁘지는 않아라고 중얼거렸다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				힘껏의 허풍으로, 그저 나쁘지 않다고 대답했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일했다 있고
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 5
	;자연스러운행동
	;=================================================
	;REACT부하 0, 좀 더 기분 좋아지고 싶다고 했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 기분 좋은 일했다 있고 기분이라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 아첨하도록(듯이) 감히 추잡한 행위를 졸랐다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------
[SKIPSTART]
통상의 경우 리액션의 지문은@TRAIN_MESSAGE2와@REACTION_MESSAGE로 처리됩니다만
이하의 부분은@TRAIN_MESSAGE2의 조건으로부터 빠지기 때문에(위해), @REACTION_MESSAGE에서만 처리됩니다
COM 분기에 있어서도 당연히 TFLAG:REACT파생는 기능하고, @REACTION_MESSAGE내에서도 REACT부하 마다 나누어 쓸 수 있습니다
SELECTCASE GET_COM_RANK()
	;자연스러운행동
	CASE "자연스러운행동"
		PRINTFORMW 
	;무리가 없는행동
	CASE "무리가 없는행동"
		PRINTFORMW 
	;무리를했다행동
	CASE "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 싫어
		ELSE
			PRINTFORMW 
		ENDIF
ENDSELECT
상기와 같은 분기를 마련하는 일에 의해, 본체측의변화에 대응하는 일은 가능합니다만
텐프레에 있어서 디폴트로 마련하려면 손간이 너무 걸린다고 생각하는 까닭에, 기본적으로 REACT내의 COM 분기에는 테두리를 마련하지 않는방침입니다
범용적인 내용으로 해도 좋아, 또 부분적으로 범용 리액션(COM)에 흘려도 좋다고 생각합니다
뭐 REACT부하 이외에도 확장하는 방법은 얼마든지 있거나 하므로, 그 근처의 판단은 각구상작자에게 맡기고 싶다고 생각합니다
3 BM/4 MB에서도 쓰기 때문에 어쨌든 생각되는 요소를 전부 넣어 줘! 적인 요망이 만약 있으면, 그 때는 또 생각하고 싶다와
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;지금 기분을 묻다에서의 COM침묵한다는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어징계·포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	IF FAVOR(0, 2000)
		PRINTFORMW 「무우, 침묵은 좋지 않아요?」
		PRINTFORMW 「신경이 쓰여요…… 응, 어떻게 하면 가르쳐 주실건가요?」
		PRINTFORMW 「좀더 좀더,%CNAME(MASTER)%에 대해 알고 싶어져 버렸어요♪」
	ELSE
		PRINTFORMW 「릴리의 독심술 앞에서는, 입다물어도 쓸데없어요」
		PRINTFORMW 「무우─, 흠흠…… 엣?   좀 더 격렬하게 괴롭혀, 인가요?   변태씨군요♪」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계·포인트가산 파생:「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		IF FAVOR(0, 2000)
			PRINTFORMW 「어떻게 하죠, 릴리도 곤란해져요……」
			PRINTFORMW 「그런 반응되면……흥분해버리잖아요♪」
			PRINTFORMW 「%CNAME(MASTER)%도, 알고 말하고 있는 거겠죠……♪」
		ELSE
			PRINTFORMW 「무우─, 어떻게 할까요. 이제 허락했으면 하나요?」
			PRINTFORMW 「%CNAME(MASTER)%의 기분은 알았어요」
			PRINTFORMW 「……그렇지만, 릴리는 주인님이니까♪　구별이 없는 아이에게는 벌주지 않으면 안 되지요♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「앙♪　시러시러라니 아이같은 조르기 해버려서♪」
			PRINTFORMW 「%CNAME(MASTER)% 귀여워……릴리는요, 좀 더 귀여운 모습 보고 싶어져 버렸어요♪」
		ELSE
			PRINTFORMW 「허락했으면 좋겠나요……후후, 어떻게 하지요」
			PRINTFORMW 「그치만, 그런 반응되면……릴리 흥분해버리잖아요♪」
			PRINTFORMW 「응, 시러시러라니……좀 더 릴리에게 조르기 보여 주세요♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「그, 그렇게 무서워하지 않아도 괜찮지 않나요」
			PRINTFORMW 「봐요, 릴리예요?   우─…… 두렵지 않아요」
		ELSE
			PRINTFORMW 「정마알……릴리는 두렵지 않아요」
			PRINTFORMW 「혹시, 조교 너무 격렬했어요?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT0, 지금 기분을 묻다

;────────────────────────────────────
;REACT1, 자신에게의 마음을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K15_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다!/2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;자연스러운행동
	;=================================================
	;REACT부하 0, 아무래도 싫다고는 말할 수 없었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 본심을 숨기도록(듯이) 눈을 숙여, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			IF RAND:2 == 0
				PRINTFORMW 「일지도 몰라……인가요」
				PRINTFORMW 「후후,%CNAME(MASTER)%(은)는 릴리를 기대시키는 것이 능숙하네요」
				PRINTFORMW 「저기 %CNAME(MASTER)%……좀 더 릴리를 봐주세요」
			ELSE
				PRINTFORMW 「그래, 싫지 않아……인가요」
				PRINTFORMW 「매우 기뻐요……그렇지만, 릴리는 욕심쟁이라서……」
				PRINTFORMW 「좀더 좀더,%CNAME(MASTER)%의 마음을 갖고 싶어져 버렸어요」
			ENDIF
		ELSE
			IF RAND:2 == 0
				PRINTFORMW 「싫지 않아…… 후후, 릴리는 기대해도 될까요?」
				PRINTFORMW 「……저기 %CNAME(MASTER)%」
				PRINTFORMW 「신체도 마음도……릴리만을 봐 주세요……」
			ELSE
				PRINTFORMW 「정말, 기분전환이라면 「좋아」라고 말하지 않으면 안되어요?   후후♪」
				PRINTFORMW 「기뻐요…… 사실이에요?」
				PRINTFORMW 「릴리…… 기대해도 되겠죠?」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;자연스러운행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「후-응…… 좋아요?   별─로-오」
			PRINTFORMW 「…… 릴리는 화내지 않았어요?」
			PRINTFORMW 「조금, 기대한거 뿐이니까」
		ELSE
			PRINTFORMW 「무우……뭐 당연하네요」
			PRINTFORMW 「감금한 장본인을 좋아하게 될 수 있다니……안되겠죠」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;자연스러운행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「야유를 말할 수 있다니 아직도 건강한 증거군요♪」
			PRINTFORMW 「…… 저기?   요정에게 조교되어 기뻐하는 변·태·씨♪」
			PRINTFORMW 「앗!   그렇지 않으면, 혹시 벌칙을 조른건가요?」
		ELSE
			PRINTFORMW 「빈정거려도, 릴리는 따로 화내거나 하지 않아요?」
			PRINTFORMW 「다만……고집이 있는 %CNAME(MASTER)%이, 좀 더 온순하게 될 수 있도록」
			PRINTFORM 「조-금만, 격렬하게 해버릴지도…
			CALL HEARTCB
;OriginalString : 」
			PRINTW 」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 적은지, REACT분류가태연하게 한다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;자연스러운행동
	;=================================================
	;REACT부하 0, 「좀 더 좋아하게 되고 싶기 때문에, 심한 것은 하지 말아줘」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「이제(벌써) 심한 일 하지 않으면, 좋아하게 될 수 있을지도 모른다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 기분을 묻도록(듯이) 시선을 향한 후, 「심한 일만 하는 사람은 싫다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			IF RAND:2 == 0
				PRINTFORMW 「아우…… 릴리, 너무 혹시 해 버렸나요?」
				PRINTFORMW 「%CNAME(MASTER)%에게 좋아한다는 말을 들을 수 있도록, 다음부터는 상냥하게 할게요」
			ELSE
				PRINTFORMW 「아우……릴리도 상냥하게 하고 싶지만, 조절이 어려워요……」
				PRINTFORMW 「그치만 %CNAME(MASTER)%도 로리타에 드Ｍ이고……」
				PRINTFORMW 「좀 더 상냥하게……릴리도 노력할게요」
			ENDIF
			ELSE
			IF RAND:2 == 0
				PRINTFORMW 「아우……릴리도 상냥하게 하고 싶지만, 조절이 어려워요……」
				PRINTFORMW 「그치만 %CNAME(MASTER)%도 로리타에 드Ｍ이고……」
				PRINTFORMW 「좀 더 상냥하게……릴리도 노력할게요」
			ELSE
				PRINTFORMW 「아우……미안해요.　꽤 조절이 어려워요」
				PRINTFORMW 「상냥하게 하면, 좀 더 릴리를 좋아하게 되나요?」
				PRINTFORMW 「무무무…… 의지가 나왔어요!   릴리, 노력할게요♪」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 1000이상
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 4
	;부하 2는 연주한 (분)편이 뭔가 형편이 좋은 것 같아 우선
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(4) && TFLAG:REACT부하 < 2
		PRINTFORMW 「좋아해 라니……우우우……」
		PRINTL  
		PRINTFORMDW %CALLNAME:MASTER%(이)가 자신의 정직한 속마음을 전하자,
		PRINTFORMDW 릴리는 빨간 얼굴 해, 멈춰버린 채로 움직이지 못하게 되었다.
		PRINTL  
		;한 번 릴리의입로부터 좋아한다고 전하고 있다
		IF FAVOR(3000, 3000)
			PRINTFORMW 「……앗, 저……엣또……」
			PRINTFORMW 「몹시, 기뻐요……릴리는, 매우, 매우 기뻐요……」
			PRINTFORMW 「……기뻐서, 울어버릴 정도로……릴리는, 매우 행복해요」
			PRINTFORMW 「정말 좋아하는 %CNAME(MASTER)%에게……, 좋아한다는 말을 들을 수 있어서」
			PRINTL  
			PRINTFORMW 「릴리도……에헤헤♪　정-말 좋아해요♪」
			PRINTFORMW 「너무 좋아,%CNAME(MASTER)%……」
			PRINTL  
			PRINTFORMDW 꽃피는 듯한 웃는 얼굴에 시간이 멈춘다
			PRINTFORMDW 릴리의 촉촉한 눈 속에 자신의 모습이 흔들리고 있었다.
			PRINTFORMDW 그녀가 눈을 꼭 감으면, 긴 속눈썹 사이에서
			PRINTFORMDW 눈물이 뺨에 늘어져 담홍색색의 입술을 적셨다.
			PRINTFORMDW 그 눈물 자국을 더듬듯, 들이마실 듯이 흰 백합 꽃잎에 입을 맞추었다.
			PRINTL  
			PRINTFORMDW 「응……」
			PRINTFORMDW 「……후후♪　반드시 오늘이, 릴리에게 있어서의 호기겠지요♪」
		;릴리는, 아직 생각을 전하지 않았다
		ELSEIF FAVOR(0, 2000)
			PRINTFORMW 「……앗, 저……엣또……」
			PRINTFORMW 「몹시, 기뻐요……릴리는, 매우, 매우 기뻐요……」
			PRINTFORMW 「……기뻐서, 울어버릴 정도로……릴리는, 매우 행복해요」
			PRINTFORMW 「정말 좋아하는 %CNAME(MASTER)%에게……, 좋아한다는 말을 들을 수 있어서」
			PRINTFORMW 「릴릴리도……에헤헤♪　정-말 좋아해요♪」
			PRINTL  
			PRINTFORMDW 아직 어림이 남은 웃음에, 수치가 담홍색과 같이 꽃혀
			PRINTFORMDW 릴리를 더욱 더 요염하게 빛내고 있다.
			PRINTL  
			PRINTFORMW 「릴리의 전부,%CNAME(MASTER)%에게 줄게요……헷♪」
			PRINTFORMW 「그러니까 릴리에게도,%CNAME(MASTER)%(을)를 전부 주세요♪」
			PRINTFORM 「%CNAME(MASTER)%의 마음으로 정력으로……릴리를 %CNAME(MASTER)%색에 물들여 주세요
			CALL HEARTCB
;OriginalString : 」
			PRINTW 」
		ELSE
			PRINTFORMW 「엣또……기뻐요……♪」
			PRINTFORMW 「%CNAME(MASTER)%(가)이 릴리를……에헤헤♪」
			PRINTFORMW 「릴리도 %CNAME(MASTER)% 정말 좋아해요♪」
			PRINTL  
			PRINTFORM 「기분 좋아보이고 쭈욱해서 토정하고 있을 때나, 최고로 귀여워서
			CALL HEARTCB
;OriginalString : 」
			PRINTW 」
			PRINTFORM 「자아자아♪　정-말 조아하는 릴리가, 가득 기분 좋게 시켜 줄게요―
			CALL HEARTCB
;OriginalString : 」
			PRINTW 」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;역강간로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
		PRINTFORMW 「에헤헤♪　감사해요♪」
		PRINTFORMW 「그렇지만, 여자 아이에게 범해지면서 그런 일 말해도 설득력 없어요?」
		PRINTFORMW 「믿어줬으면 좋겠다면♪　응♪　좀더 좀더 릴리를 만족시켜 주세요♪」
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;섹스로질내사정의 후
	;=================================================
	IF 구상기입체크(0) && TFLAG:전회질싸 == 1
		PRINTFORMW 
		PRINTFORMW 
		RETURN 1
	ENDIF

	;자연스러운행동
	;=================================================
	;REACT부하 0, 붙임성 좋게 웃어 정말 좋아한다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 분명히 좋아라고 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 억지웃음을 띄워, 좋아해라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(3000, 3000)
			IF RAND:3 == 0
				PRINTFORMW 「릴리가 정말 좋아 인가요―……후후♪」
				PRINTL  
				PRINTFORMD 릴리는 %CALLNAME:MASTER%의 근처에 걸터앉아,
				IF RAND:2 == 0
					PRINTFORMDW 몸을 의지해 왔다.
				ELSE
					PRINTFORMDW %CALLNAME:MASTER%의 몸을 살그머니 껴안았다.
				ENDIF
;OriginalString :  
				PRINTK  
				PRINTFORMW 「그러며언 릴리가 응석부리거나 해도, 괜찮겠죠♪」
				PRINTL  
				PRINTFORMDW 눈썹이 맞닿을 듯한 거리에서, 리리와 시선을 섞는다.
				PRINTFORMDW 한숨으로 입술을 간질일때마다 두근두근 고동이 심하게 울린다...…
				PRINTFORMDW 접하지도 않았는데, 두드러지게 저릴 것 같은 쾌감이 하반신을 지배한다...…

				PRINTL  
				PRINTFORMW 「저항의 방법, 잊어 버렸어?   으응, 받아들여져 버린거네요♪」
				PRINTFORM 「 이제 완전히, 몸도 마음도……릴리의 애인……이구나
				CALL HEARTCB
;OriginalString : 」
				PRINTW 」
			ELSEIF RAND:2 == 0
				PRINTFORM 「기뻐요
				CALL HEARTCB
;OriginalString : 　リリーもね、%CNAME(MASTER)%が大好きですよ♪」
				PRINTW  릴리도,%CNAME(MASTER)%(을)를 아주 좋아해요♪」
				PRINTFORMW 「정말 좋아하니까,%CNAME(MASTER)%의 전부를 갖고 싶어져 버려요……」
				PRINTFORMW 「릴리의 저기, 매우 기분 좋았었지요?」
				PRINTFORMW 「중요한 곳에서 깊게 연결되어, 벌벌 떨어……릴리도 몹시 기분 좋았어요」
				PRINTFORMW 「…… 그래서, 저…… 음란한 아이는, 싫게 되지 말아 주세요?」
				PRINTL  
				PRINTFORMDW 릴리는 수치에 뺨을 붉혀, 머뭇머뭇이라고 말하기 어려운 듯이  시선을 자유롭게 행동하게 한다……
				PRINTL  
				PRINTFORMW 「……좋아하니까……」
				PRINTFORMW 「서로 생각한다면..…더 기분좋게, 될 수 있을까라고 생각해서...…」」
				PRINTFORMW 「휙휙 녹아서 서로 섞여서 둘이 하나가 될정도.. 좀 더%CNAME(MASTER)%(와)과……」
			ELSE
				PRINTFORMW 「……기뻐어..」
				PRINTFORMW 「그렇지만…… 저, 정말로 릴리로 좋나요? 　%CNAME(MASTER)%에게 가득 심한 일 했는데?」
				PRINTL  
				PRINTFORMW 「……고마워요……에헤헷♪」
				PRINTFORM 「%CNAME(MASTER)%의 몸도 마음도……좀더 좀더, 릴리에게 열중하게 해 줄게요
				CALL HEARTCB
;OriginalString : 」
				PRINTW 」
				
			ENDIF
		ELSEIF FAVOR(0, 2000)
			IF RAND:3 == 0
				PRINTFORMW 「앗……」
				PRINTL  
				PRINTFORMW 「에헤헤……놀라 버렸어요」
				PRINTFORMW 「릴리,%CNAME(MASTER)%에게 심한 일……가득 해 버렸으니까」
				PRINTFORMW 「릴리도,%CNAME(MASTER)%가 좋아……」
				PRINTFORMW 「 그렇지만……같은 정도로,%CNAME(MASTER)%(이)가 흐트러지는 모습도 좋아해……」
				PRINTFORMW 「좋아하기 때문에, 소중히 하고 싶은데……기분이 멈출 수 없어서……」
				PRINTL  
				PRINTFORMW 「저기…… 계속, 하자?」
				PRINTFORMW 「릴리는요……좀더 좀더,%CNAME(MASTER)%(와)과 연결되고 싶은거야……」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「아, 안되어요……」
				PRINTFORMW 「릴리는,%CNAME(MASTER)%에게 심한 일 가득 해서……」
				PRINTFORMW 「%CNAME(MASTER)%가 기분 좋은 것은, 그러한 쪽이 정액을 많이 받을 수 있기 때문으로……」
				PRINTFORMW 「그러니까……그……그런 릴리라도……」
				PRINTFORMW 「%CNAME(MASTER)%의 「좋아」라는 마음에……릴리가 응해도 괜찮을까요……」
				PRINTFORM 「……그렇지만, 좋아하게 되어 버렸다면……후후♪　어쩔 수 없지요
				CALL HEARTCB
;OriginalString : 」
				PRINTW 」
			ELSE
				PRINTFORMW 「사랑은 맹목이라고 하지만……」
				PRINTFORMW 「릴리,%CNAME(MASTER)%(을)를 가득 괴롭혀……지금도 그래요」
				PRINTFORMW 「%CNAME(MASTER)%를 가두어, 야한 조교를 하는…… 나쁜 요정이랍니다?」
				PRINTFORMW 「…… 쾌락에 져 버렸다…… 라든지?」
				PRINTFORMW 「……후후, 아하하♪」
				PRINTFORMW 「미안해요, 조금 심술부려 버렸어요♪」
				PRINTFORM 「……기뻐요, 릴리도 정말 좋아해요
				CALL HEARTCB
;OriginalString : 」
				PRINTW 」
			ENDIF
		ELSE
			PRINTFORMW 「 「좋아」인가요….에헤헷♪」
			PRINTFORMW 「릴리에게도, 마침내 봄이 와 버린건가요?」
			PRINTFORMW 「……주인님과 노예의 신분 차이의 사랑……」
			PRINTFORMW 「어슴푸레한 방에서, 서로의 정을 서로 교환할 뿐의……청순과는 먼, 난감한 관계인데……」
			PRINTFORMW 「……만약,%CNAME(MASTER)%(와)과 지금과는 다르게 만났다면……」
			PRINTFORMW 「…………」
			PRINTFORMW 「……라니♪　고마워요, 포상, 드릴게요♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 5
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(5)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;소분기
	;-------------------------------------------------
	;역강간로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
		;주인님임신중!!
		IF TALENT:TARGET:임신 == 1
			PRINTFORM 「읏아앙♪　보테배 섹스보다 과격한 일을…후후
			CALL HEARTCB
			PRINTFORMW 」
			PRINTFORM 「좋아요
			CALL HEARTCB
			PRINTFORMW 　%CNAME(MASTER)%의 끝없는 성욕도, 릴리가 전부 받아 들여 줄게요♪」
		;임신 하고 있지 않아!
		ELSE
			PRINTFORMW 「으앙♪  더, 더욱인가요?」
			PRINTFORM 「……이 이상 퓻퓻-되면, 릴리 엄마가 되어 버려요
			CALL HEARTCB
;OriginalString : 」
			PRINTW 」
			PRINTFORMW 「 그렇지만,%CNAME(MASTER)%의 끝없는 성욕도, 릴리가 전부 받아 들여 줄게요♪」
			PRINTFORMW 「%CNAME(MASTER)%가 그 기분이라면…… 임신까지, 아기방에 사정해 보실래요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;섹스로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
		PRINTFORMW 「조, 좀 더 대단한 일…… 인가요?」
		PRINTFORMW 「우─…… 릴리에게 어떤 변태 플레이를 시킬 생각인가요!」
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;죠교대상에의 호감도 1000 이상
	;=================================================
	IF 구상기입체크(60) && FAVOR(0, 1000)
		PRINTFORM 「 이제,%CNAME(MASTER)%는 릴리에게 지지 않을 정도로  욕심쟁이네요
		CALL HEARTCB
;OriginalString : 」
		PRINTW 」
		PRINTFORMW 「……%CNAME(MASTER)%(은)는 이제 상냥한 는 만족할 수 없어……」
		PRINTFORMW 「……머리가 저릴 정도로 심하게 하지 않으면 만족할 수 없는거야……후후♪」
		PRINTFORM 「그러며언, 릴리와 좀 더 대단한 일……해버려요
		CALL HEARTCB
;OriginalString : 」
		PRINTW 」
		RETURN 1
	ENDIF

	;자연스러운행동
	;=================================================
	;REACT부하 0, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」라고 대답했다
	;=================================================
	IF 구상기입체크(60) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 「저기…… 굉장한 일이라니, 릴리에게 어떤 변태 플레이를 시킬 생각인가요?」
		PRINTFORMW 「분명하게 말해 주지 않으면, 릴리 몰라요─?   후후♪」
		PRINTFORM 「자아♪　부·탁·해
		CALL HEARTCB
;OriginalString : 　してみてください♪」
		PRINTW  해 보세요♪」
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「좀 더 좋아하게 되도록(듯이), 좀 더 귀여워했으면 좋겠다」라고 대답했다
	;=================================================
	IF 구상기입체크(60) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 「앙♪  착정 되는 것 버릇이 되어 버렸나요?」
		PRINTFORM 「그치만 릴리가 %CNAME(MASTER)%(을)를 귀여워한다 라는건……「그러한 일」이지요
		CALL HEARTCB
		PRINTFORMW 」
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 「좋아하게 될 수 있을 정도로, 굉장한 일 해 주면 된다」라고 대답했다
	;=================================================
	IF 구상기입체크(60) && GET_COM_RANK() == "무리를했다행동"
		IF RAND:2 == 0
			PRINTFORMW 「그렇게 「대단한 일」하면, 릴리를 좋아하게 되기 전에 망가져버릴지도……키득♪」
			PRINTFORM 「그렇지만 %CNAME(MASTER)%의 부탁이라면 어쩔 수 없지요
			CALL HEARTCB
			PRINTFORMW 」
		ELSE
			PRINTFORMW 「대단한 일이라니, 릴리에게 어떤 변태 플레이를 시킬 생각이지요……」
			PRINTFORMW 「응─…… 표본으로 하고 싶으니까 릴리에게 「대단한 일」해 줄래요?」
			PRINTFORM 「후후♪　정말, 농담이에요
			CALL HEARTCB
			PRINTFORMW 」
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「앙♪  착정 되는 것 버릇이 되어 버렸나요?」
		PRINTFORMW 「모처럼 릴리가 상냥하게 해도 만족할 수 없다니……글러먹은 아이네요♪」
		PRINTFORMW 　
		PRINTFORMW 「응…… 대단한 일이라니, 릴리에게 어떤 변태 플레이를 시킬 생각인가요?」
		PRINTFORMW 「분명하게 말해 주지 않으면, 릴리 몰라요─?   후후♪」
		PRINTFORM 「자아♪　부·탁·해·요
		CALL HEARTCB
;OriginalString : 　してみてください♪」
		PRINTW  해 보세요♪」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자신에게의 마음을 묻다의 경우는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어징계·포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	IF FAVOR(0, 2000)
		PRINTFORMW 「…… 흠흠, 「릴리를 아주 좋아해」 「그러니까 좀 더 격렬하게 해줘」?」
		PRINTFORMW 「헤에―,%CNAME(MASTER)%는 역시 변태씨네요♪」
		PRINTFORMW 「…… 어라?   부끄러워 하는 %CNAME(MASTER)%의 기분을 대변 해 준거예요? …… 후후♪」
		PRINTFORMW 「이번엔 %CNAME(MASTER)%(으)로부터 직접 들을 수 있도록, 릴리 힘낼게요♪」
	ELSE
		PRINTFORMW 「여보세요-?   입다물고 있으면 몰라요─?」
		PRINTFORMW 「혹시…… 매우 추잡한 망상으로, 릴리를 터무니없게 범하기라도 하는건가요?」
		PRINTFORMW 「후후……야하네요♪　%CNAME(MASTER)%는 변태♪」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계·포인트가산 파생:「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1 ;의도가 알지 않기 때문에 헤맨 것입니다만, 우선 ON로 해 궖했다@/L
		IF FAVOR(0, 2000)
			PRINTFORMW 「허락했으면 좋겠나요?」
			PRINTFORMW 「시러시러 해도 안되어요♪」
			PRINTFORMW 「자, 릴리를 어떻게 생각하고 있는지 말해 보세요♪」
		ELSE
			PRINTFORMW 「그렇게 말하고 싶지 않아요?」
			PRINTFORMW 「……몸도 마음도 릴리의 것이 되면, 지금 보다 더 행복하게 될 수 있는데……」
			PRINTFORMW 「인간은, 어려운 생물이네요……」
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「용서해줘,라니 마치 릴리가 괴롭히는 것 같지 않아요?」
			PRINTFORMW 「「그러지 않아도 괴롭히잖아」라니?」
			PRINTFORMW 「그런 말투 하면 , 정말로 괴롭혀요♪」
		ELSE
			PRINTFORMW 「거, 거기까지 말하고 싶지 않나요?」
			PRINTFORMW 「우우……조금 쇼크네요……」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「그렇게 무서워하지 마?」
			PRINTFORMW 「그래, 즐거운 일 생각해 봐요!」
			PRINTFORMW 「%CNAME(MASTER)%(이)가 좋아하는 일이라든지, 기분 좋은 일……」
			PRINTFORMW 「릴리에게 상냥하게 비난받는 상상...…해보죠?」
		ELSE
			PRINTFORMW 「……명백하게 무서워하게 되면……조금 쇼크예요……」
			PRINTFORMW 「지금의 릴리…… 그렇게 무서워요?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT1, 자신에게의 마음을 묻다

;────────────────────────────────────
;REACT2, 성에 관한 이야기를 하다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K15_2
;-------------------------------------------------
;  파생할 가능성이 있는 COM는 우선 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계·포인트가산 파생:「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;징계·포인트가산 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;TEQUIP에 의해 쾌SOURCE가 들어가 있는 경우에 파생
	;=================================================
	;REACT파생 1, 쾌락에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 상황에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("자위를 시작한다")
	;=================================================
	;REACT파생 1, 「승손에 자위 하지마」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「승손에 자위 하고 있지만 좋은가」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT2, 성에 관한 이야기를 함

;────────────────────────────────────
;REACT3, 상냥하게 위로한다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K15_3
;-------------------------------------------------
;  파생할 가능성이 있는 COM에는 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
;  우선회화계 COM를 모두 망라되어 있습니다만
;  10 접수대의애무계 COM, 50 접수대 이후의 그 외 COM등도 상황에 따라서는선택 가능하게 됩니다
;  필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
	IF FAVOR(3000, 3000)
		PRINTFORMW 「입다물어 버려서 어떡하려구?   부끄러운 걸까?」
		PRINTFORMW 「그런 일 신경쓰지 않아도 되는데……키득♪」
	ELSEIF FAVOR(0, 2000)
		PRINTFORMW 「무우―, 조금 쇼크네요……」
		PRINTFORMW 「좀 더 솔직해져…… 응석부려도 괜찮아요?」
	ELSE
		PRINTFORMW 「무엇인가 말해 주세요―, 외롭지 않나요오……」
		PRINTFORMW 「응, 좀 더 솔직해집시다?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성 있게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("상냥하게 응답한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("강하게 응답한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT3, 상냥하게 위로한다

;────────────────────────────────────
;REACT4, 엄하게 위협한다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_REACT_K15_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(3)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT4, 엄하게 위협한다

;────────────────────────────────────
;REACT5, 의상 변경을 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가"소극적으로 한다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가"적극적으로 한다", "태연하게 한다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_REACT_K15_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;=================================================
	;한 장 벗었다(소극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("소극적으로 한다/받아들인다")
		;-------------------------------------------------
		;마지막 한 장을 벗어전라되었다
		;-------------------------------------------------
		IF 구상기입체크(0) && (TEQUIP:MASTER:악세서리 + TEQUIP:MASTER:신발안 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
			;REACT분류받아들인다			전라가 되어, 기대와 흥분으로 피부가 달아오르는 것을 느꼈다
			IF IS_COMGRONAME("받아들인다")
				PRINTFORMW 
			;REACT분류소극적으로 한다			어쩔 수 없이 마지막 한 장을 벗어, 전라되었다
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;완만한 움직임으로 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;어쩔 수 없이 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("소극적으로 한다")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;상층의 분기를 모두 묻었을 경우는 불필요한 개소
		;반대로 묻지 않는 경우는 이 개소가 보완이 책임을 다합니다
		;-------------------------------------------------
		;기타·조건없음
		;-------------------------------------------------
		IF 구상기입체크(100)
			IF FAVOR(0, 2000)
				IF RAND:2 == 0
					PRINTFORMW 「부끄러운 것 같은 %CNAME(MASTER)%의 표정……두근두근해요♪」
					PRINTFORM 「……%CNAME(MASTER)%의 야한 신체, 릴리에게 좀 더 잘 보여 주세요
					CALL HEARTCB
					PRINTFORMW 」
				ELSE
					PRINTFORMW 「릴리에게 알몸을 보여지는 것이 부끄럽나요?」
					PRINTFORMW 「…… 달라요?」
					PRINTFORMW 「 「보여지는 것」이 아니고 「보여지고 느껴버리는 일」이 부끄러운거야」
					PRINTFORM 「노출광의 재능까지 있다니 정말로 헨타이씨이군요
					CALL HEARTCB
					PRINTFORMW 」
				ENDIF
			ELSE
				IF RAND:2 == 0
					PRINTFORMW 「그렇게 초조하게 해지면, 릴리도 흥분해요♪」
					PRINTFORMW 「자, 계속해 주세요……부끄러운 곳, 릴리가 전부 봐 줄테니까」
				ELSE
					PRINTFORMW 「얼굴 빨개졌네요……후후♪」
					PRINTFORMW 「 좀 더 부끄러운 것 같은 얼굴, 릴리에 보였으면 좋은데」
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;2매 벗었다(적극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;-------------------------------------------------
		;전라되었다
		;-------------------------------------------------
		IF 구상기입체크(0) && (TEQUIP:MASTER:악세서리 + TEQUIP:MASTER:신발안 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;적극적으로 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;손때 좋게 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("태연하게 한다")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;상층의 분기를 모두 묻었을 경우는 불필요한 개소
		;반대로 묻지 않는 경우는 이 개소가 보완이 책임을 다합니다
		;-------------------------------------------------
		;기타·조건없음
		;-------------------------------------------------
		IF 구상기입체크(100)
			IF FAVOR(0, 2000)
				IF RAND:2 == 0
					PRINTFORMW 「또 추워졌나요?   후후♪」
					PRINTFORMW 「와요♪　릴리가 따뜻하게 해 줄게요♪」
				ELSE
					PRINTFORMW 「힘차게 벗어 버려서, 완전히 릴리가 말하는대로네요♪」
					PRINTFORMW 「좀 더 수줍음이 있던 쪽이 흥분되는데요……후후♪」
				ENDIF
			ELSE
				PRINTFORMW 「후후♪　그렇게 초조하게 벗지 않아도 괜찮아요」
				PRINTFORMW 「 좀 더 천천히, 두 명의 시간을 즐깁시다♪」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;벗지 않았다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("거부")
		;부끄러워서 벗겨지지 않았다
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
			IF FAVOR(0, 2000)
				PRINTFORMW 「입은 채로 엣찌하고 싶어요?」
				PRINTFORMW 「그렇지 않으면, 릴리의 손으로 벗겨 줬으면 좋겠어…… 라든지?」
			ELSE
				PRINTFORMW 「릴리가 도와주지 않으면 갈아입는것도 혼자서 할 수 없나요?」
				PRINTFORMW 「그 쪽이, 상당히 부끄럽다고 생각하는데요……」
			ENDIF
		;싫어해 벗지 않았다
		ELSE
			IF FAVOR(0, 2000)
				PRINTFORMW 「릴리에게 무리하게 탈의되고 싶은 걸까요?   후후♪」
				PRINTFORMW 「좋아요♪  그렇지만, 분명하게 저항해, 부끄러워해 주세요?」
				PRINTFORM 「그 쪽이……흥분될테니까
				CALL HEARTCB
				PRINTFORMW 」
			ELSE
				IF RAND:2 == 0
					PRINTFORMW 「얼굴 새빨갛게 해 버려서, 귀여워♪」
					PRINTFORMW 「%CNAME(MASTER)%(이)가 부끄러워하는 얼굴, 좀 더 보고 싶어져 버렸어요」
				ELSE
				PRINTFORMW 「무―, 부끄러워서 벗겨지기 싫은건가요……」
					PRINTFORMW 「그렇지만, 이대로 하면 주름이 생겨 버려요?」
				ENDIF
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("소극적으로 한다/받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;2매 입었다(적극적 반응)
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;지문에서는 "싫어해"라고 하는 취급
	;=================================================
	;입지 않았다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("거부")
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT5, 의상 변경을 요구한다

;────────────────────────────────────
;REACT6, 합의를 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_REACT_K15_6
;-------------------------------------------------
;손간을 싫어하지 않는 것이면, TALENT:MASTER:남자등으로 내부에서 나누는 것도 있을지도 모릅니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("소극적으로 한다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 각오를 결정한 것처럼, 작은 소리로 승낙했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 0, 작은 소리로 승낙했다	
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은적극			부끄러움에 스러질 것 같은 소리로 승낙했다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				분함에 얼굴을 비뚤어지게 하면서도 승낙했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("태연하게 한다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 별로 싫지 않다고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 마음대로 하면 좋다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은적극			했다 좋은들 좋다고 말했다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				승손로 하면 좋다고 대답했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 처음을 줄 수 있어 기쁘다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 뜻을 정해 바쳐도 괜찮다고 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 이제 지킬 수 없다면 적어도 상냥하게 여겨지도록(듯이)와 동의했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게 해 주는 것이라면 처음을 주어도 괜찮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 처음을 바치기 때문에기분좋게 해 갖고 싶다고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 기분좋게 해 주는 것이라면 처음을 주어도 괜찮다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 처음을 바치라고 한다면, 괴로움을 잊을 수 있을 정도로기분좋게 해라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의 하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서"거부"에 세트 된다)
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("거부")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(4)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT6, 합의를 요구한다

;────────────────────────────────────
;REACT7, 징계한다고 위협한다·개별 리액션
;────────────────────────────────────
;·선언해도징계·이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_REACT_K15_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM"마음 약하게 응답한다"는 일정 조건으로 REACT분류"용서를 빌다"에 파생한다
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다") 
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(7)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT7, 징계한다고 위협한다

;────────────────────────────────────
;REACT8, 고함치다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K15_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT8, 고함치다

;────────────────────────────────────
;REACT9, 기분나쁘게 웃다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K15_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT9, 기분나쁘게 웃다


