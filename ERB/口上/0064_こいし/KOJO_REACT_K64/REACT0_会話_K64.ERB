;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	코이시구상 회화系個別リアクション구상ファイル
;
;	회화系リアクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화系 REACT0～9
;========================================================================
;・1231版よりTFLAG:REACT부하が導入され、선택했다COMが自然な행동かどうかが参照できるように
;・0127版以降のリアクション後の地の文は、基本的にREACT부하で分岐するようになっています
;・TFLAG:REACT부하(0=自然な행동/1=無理のない행동/2=無理を했다행동)
;-------------------------------------------------
;　회화系はあまり汎用処理には向かないので、汎用リアクション(COM)に流さず個別リアクション(REACT)
;　で完結させてしまうのが望ましいと考えます
;-------------------------------------------------
;・というわけらしいので、「이루는대로」の処理もこちらに随時作成中…で、いいのかな？
;・あと、私は前後の文脈がよくわからなくなるので、随時私のために地の文をメモしておく
;・구속/성교취소時の回避処理を仮処置済み。휴식系と公衆便器にも필요だけど、そちらは後程…
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 지금 기분을 묻다・個別リアクション
;────────────────────────────────────
;・ACT파생への代入は죠교대상のリアクションに応じて@ACTION_APPLY2_0にて行われる
;-------------------------------------------------
@KOJO_REACT_K64_0
;-------------------------------------------------
;・TFLAG:ACT파생(0=歯切れ悪く答えた/1=勇気を出して強く当たった/2=皮肉/3=はっきりと今の気持ちを告げた
;・4=まあまあ悪くはない/5=気持ち良いこと했다い)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT0_0, 歯切れ悪く質問に答えた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が소극적으로 한다で、リアクションが빈정거린다以外の場合
;------------------------------------------------
;・とりあえず구속/성교취소の場合を弾く
;------------------------------------------------
IF 구상記入チェック() && TFLAG:ACT파생 == 0 && !IS_COMGRONAME("용서를 빌다/날뛴다")
	;自然な행동
	;=================================================
	;REACT부하0, 質問にはっきり答えることができなかった
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 「？ 잘 몰라요…」
		PRINTFORMW 「그렇지 않으면, 잘 모르는 기분인가 들어들？」
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 歯切れ悪く質問に答えた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 出掛かった言葉を飲み込み答えを濁했다
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT0_1, 勇気を出して強く当たった
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが강하게 응답한다の場合
;REACT분류は"거부"にセットされる
;-------------------------------------------------
IF 구상記入チェック() && TFLAG:ACT파생 == 1
	;自然な행동
	;=================================================
	;REACT부하0, 厳しい言葉を浴びせた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 「…어째서 그런 일을 말해？」
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 勇気を出して強く当たった
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;横槍が入って面倒되었다ので방치
		PRINTFORMW 「그러한 것, 지저는 별로 말하지 않는 것이 좋아」
		
		PRINTFORMW 「은…%K64_CNAME()%(이)라면 알고 있지？」
		
		PRINTFORMW 「…여기서의 사는 방법도, 계속해서 가르쳐 주어요」
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 目を逸らし입では強気に振る마이った
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "無理を했다행동"
		PRINTFORMW 「거역할 수 없다고 알고 있어도, 거역하지 않고서는  견딜 수 없는 것인가？」
		PRINTFORMW 「이번은 나의 눈을 봐 말했으면 좋은데♪」
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT0_2, 皮肉「今の気分は、と聞かれても」
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが빈정거린다の場合
;REACT분류は"거부"にセットされる
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
	;自然な행동
	;=================================================
	;REACT부하0, 余裕の笑みで肩をすくめ「今の気分は、と聞かれても」と答えた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 「어머나,%K64_CNAME()%도 마음이 텅 비어？」
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 少し考えてから「今の気分は、と聞かれても」と答えた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		PRINTFORMW 「%K64_CNAME()%라고 생각한 척해, 실은 아무것도 생각하지 않지요？」
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, かろうじて皮肉交じりに答えた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		PRINTFORMW 「그 대사는 어디서 기억해 왔어？」
		PRINTFORMW 「그다지 어울리지 않았어요. 킥킥」
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT0_3, はっきりと今の気持ちを告げた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が적극적으로 한다の場合
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 3
	;自然な행동
	;=================================================
	;REACT부하0, 一緒に居られることが嬉しいと솔직に입に했다
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, はっきりと今の気持ちを告げた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 機嫌を損ねないよう、取り繕って答えた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT0_4, まあまあ悪くはない
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が태연하게 한다の場合
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 4
	;自然な행동
	;=================================================
	;REACT부하0, そう悪く思ってはいないと答えた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 「まあまあ悪くはない」と言った
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		IF COMCOR_POSI()	;実は積極	視線を外し、悪くはないよと呟いた
			PRINTFORMW 
		ELSE				;実は嫌		精一杯の強がりで、まあまあ悪くないと答えた
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT0_5, 気持ち良いこと했다い
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が受け入れるの場合
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 5
	;=================================================
	;COM52, 이루는대로
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("이루는대로")
		;-------------------------------------------------
		;REACT파생8, おなにーに夢中で聞いてない(자위중)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 8
			;%CALLNAME:MASTER%は자위に没頭してまともに返事ができなかった…
			;%CALLNAME:TARGET%は不満そうだ…
			PRINTFORMW 「아, 이것 보이지 않은 패턴이군요」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;REACT파생9, 気持ちよくって生返事(快感がある)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 9
			;%CALLNAME:MASTER%は快感に身悶え하면서頷くことしかできなかった…
			PRINTFORMW 「？ 말할 수 없는거야？ 기분 좋은거야？」
			PRINTFORMW 「기분 좋아서 말할 수 없는거야？ 말할 수 없을 정도 기분이 좋은거야？」
			PRINTFORMW 「이제 어떻게 되어도 괜찮은거야？ 정말로？ 후후, 재미있다♪」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;REACT파생11, 自分でもわからないまま恥ずかしいことを입走る(上記以外)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 11
			;%CALLNAME:MASTER%はどこか茫然と했다まま受け答え했다…
			;自分でもよく解らないままに恥ずかしいことを입走っている気がする…
			PRINTFORMW 「후후. 즐겁다♪」
			RETURN 1
		ENDIF
	ENDIF
	
	;自然な행동
	;=================================================
	;REACT부하0, もっと気持ち良くなりたいと言った
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 気持ち良いこと했다い気分だと答えた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 媚びるように敢えて淫らな行為をねだった
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;以下、上記のCOM、REACT분류に属さないCOM毎の分岐
;-------------------------------------------------
[SKIPSTART]
통상の場合リアクションの地の文は@TRAIN_MESSAGE2と@REACTION_MESSAGEで処理されますが
以下の部分は@TRAIN_MESSAGE2の条件から漏れる為、@REACTION_MESSAGEでのみ処理されます
COM分岐に於いても当然TFLAG:REACT파생は機能しますし、@REACTION_MESSAGE内でもREACT부하毎に書き分けられています
SELECTCASE GET_COM_RANK()
	CASE "自然な행동"
		PRINTFORMW 
	CASE "無理のない행동"
		PRINTFORMW 
	CASE "無理を했다행동"
		IF COMCOR_POSI()	;実はまんざらでもない
			PRINTFORMW 
		ELSE				;実は嫌
			PRINTFORMW 
		ENDIF
ENDSELECT
上記のような分岐を設ける事によって、本体側の변화に対応する事は可能ですが
テンプレに於いてデフォルトで設けるには손間がかかり過ぎると考える故に、基本的にREACT内のCOM分岐には枠を設けない방침です
汎用的な内容にしてもよし、また部分的に汎用リアクション(COM)に流しても良いと思います
まあREACT부하以外にも拡張する方法はいくらでもあったりするので、その辺りの判断は各구상作者に委ねたいと思います
3MB/4MBでも書くから兎に角考えられる要素を全部入れてくれ！的な要望がもしあれば、その時はまた考えたいなと
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;지금 기분을 묻다でのCOM침묵한다はTFLAG:REACT파생 == 1(質問に答えない)扱いとなり징계・포인트が加算される
;-------------------------------------------------
IF 구상記入チェック() && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「…무시한다？」
	PRINTFORMW 「-응…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック() && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계・포인트加算 派生:「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック() && TFLAG:REACT파생 == 1
		PRINTFORMW 「그 반환은 잘 모르지만 허락하지 않아요」
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック()
		PRINTFORMW 「네？ 무엇을 허락하면 좋은거야？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하2未満(自然な행동/無理のない행동)
	;=================================================
	IF 구상記入チェック() && TFLAG:REACT부하 < 2
		PRINTFORMW 「즉 무섭구나. 잘 알았어요」
		PRINTFORMW 「후후…」
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT0, 지금 기분을 묻다

;────────────────────────────────────
;REACT1, 자신에게의 마음을 묻다・個別リアクション
;────────────────────────────────────
;・ACT파생への代入は죠교대상のリアクションに応じて@ACTION_APPLY2_1にて行われる
;-------------------------------------------------
@KOJO_REACT_K64_1
;-------------------------------------------------
;・TFLAG:ACT파생(0=嫌いじゃない…かも知れない…/1=そんなことあるわけない！/2=困るねぇ…好きじゃないといったら怒る？
;・3=もう酷いことしなければ、もっと好きになれるかもしれない/4=はっきりと好きと告げた?5=もっとすごいことしてくれば、もっと好きになれるかも)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「嫌いじゃない…かも知れない…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が소극적으로 한다の場合
;-------------------------------------------------
;・とりあえず구속/성교취소の場合を弾く
;------------------------------------------------
IF 구상記入チェック() && TFLAG:ACT파생 == 0 && !IS_COMGRONAME("용서를 빌다/날뛴다")
	;自然な행동
	;=================================================
	;REACT부하0, どうしても嫌いとは言えなかった
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 「우선 미움받지 않은 것 같고 최상이지만…」
		PRINTFORMW 「원래 나는 미움받는 것이 없는거야. 언니와 달리」
		PRINTFORMW 「그 만큼, 사랑받는 일도 없지만요. 「싫은 싫은 것도 좋아하는 안」은 놈？」
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 「嫌いじゃない…かも知れない…」と答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "無理のない행동"
		PRINTFORMW 「 「일지도 모르다」래？ 이상해…」
		PRINTFORMW 「혹시 스스로도 모르는거야？」
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 本心を隠すように目を伏せ、「嫌いじゃない…かも知れない…」と答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "無理を했다행동"
		PRINTFORMW 「―, 싫지 않았으면 뭐든지 말해…？」
		PRINTFORMDW 진하고는 미묘에 따돌려졌다%CALLNAME:MASTER%의 대답이 불만인가 입을 비쭉 내미고 있다
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「そんなことあるわけない！」 FIRSTREACT(1)
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが강하게 응답한다の場合
;REACT분류は"거부"にセットされる
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	;自然な행동
	;=================================================
	;REACT부하0, 「好きになれるわけがない！」と強く答えた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 「そんなことあるわけない！」と言った
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 感情を否定するように首を振り「嫌いだ！」と答えた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 皮肉「困るねぇ…好きじゃないといったら怒る？」 FIRSTREACT(2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが빈정거린다の場合
;-------------------------------------------------
IF 구상記入チェック() && TFLAG:ACT파생 == 2
	;自然な행동
	;=================================================
	;REACT부하0, 笑って、「まさか、好かれてると思ってたの？」と答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 「으응, 전혀♪」
		PRINTFORMDW 진하고는 웃으면서 대답했다
		PRINTFORMW 「아니-안심했어요. 이것으로 염려 없고 징계나무 할 수 있는 것」
		PRINTFORMW 「태도가 나빠서, 질문에 질문으로 돌려주는 바보같은%K64_CNAME()%에 ·해··나무♪」
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 少し考えてから、「困るねぇ…好きじゃないといったら怒る？」と答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "無理のない행동"
		SELECTCASE RANDREACT(4, 0)
			CASE 1
				PRINTFORMW 「아니, 별로 화내지 않는데？」
				PRINTFORMW 「%K64_CNAME()%에 사랑받는 것 같은 일은, 무엇하나 한 기억은 없는 것」
			CASE 2
				PRINTFORMW 「으응, 별로 좋아하게 되고 주지 않아 좋아」
				PRINTFORMW 「 그렇지만%K64_CNAME()%(은)는 애완동물이니까」
				PRINTFORMW 「남편님에게 분명하게 봉사할 수 있게 될 때까지 엄격하게 예의범절을 가르쳐요」
			CASE 3
				PRINTFORMW 「네에에―」
				CALL HTMLPRINTW(@"「%RAND_SPLIT("비·밀/비밀/비·밀/비밀")%%K64_SYMBOL("BHeartW")%」")
			CASEELSE
				PRINTFORMW 「분노는 하지 않아요」
				PRINTFORMW 「다만, 예의 범절이 엄격해질 뿐♪」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 言葉を飲み込んで자세を正し、躊躇うように「好かれたいって思ってるの？」と答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "無理を했다행동"
		PRINTFORMW 「그런 구상은…묻는 것 옛날에 버렸어요」
		PRINTFORMW 「은 그런 일은 아무래도 좋은거야. 우선%K64_CNAME()%의 생각은 대개 알았어요」
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「もう酷いことしなければ、もっと好きになれるかもしれない」 FIRSTREACT(3)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が적극적으로 한다で、죠교대상호의が少ないか、REACT분류が태연하게 한다
;-------------------------------------------------
IF 구상記入チェック() && TFLAG:ACT파생 == 3
	;自然な행동
	;=================================================
	;REACT부하0, 「もっと好きになりたいから、酷いことはしないで」と答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 「어？ 어쩐지 다르지 않다？」
		PRINTFORMW 「%K64_CNAME()%(은)는 심한 일 되어, 「심한 일」이 좀 더 좋아하게 되는거죠？」
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 「もう酷いことしなければ、好きになれるかもしれない」と答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "無理のない행동"
		SELECTCASE RANDREACT(2, 1)
			CASE 0
				PRINTFORMW 「어머나, 그래？」
				PRINTFORMW 「 나, 틀림없이%K64_CNAME()%라고 학대받는 것을 좋아해라고 생각하고 있었어요」
				PRINTFORMW 「이래…%K64_CNAME()%(은)는 정말, 몹시 좋은 소리로 웁니다 것♪」
			CASEELSE
				PRINTFORMW 「그렇구나…」
				PRINTFORMW 「심한 일 할지 어떨지는,%K64_CNAME()%나름이야？」
				PRINTFORMW 「가장, 어떤의가 심한 일인가, 나에게는 잘 모르는데♪」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 機嫌を伺うように視線を向けた後、「酷いことばかりする人は嫌い」と答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "無理を했다행동"
		SELECTCASE RANDREACT(4, 2)	;割と話が噛み合わないのは仕様
			CASE 0
				PRINTFORMW 「-응…그렇구나」
				PRINTFORMW 「 그렇지만, 미움받고 있는 실감이 전혀 없지만…그것 기분탓이 아니다？」
			CASE 1
				PRINTFORMW 「이라고 하는 것은…나는 심한 일 한 기억이 없기 때문에 괜찮구나♪」
				PRINTFORMW 「…어？ 나는 무엇을 (들)물은 거지？」
			CASE 2
				PRINTFORMW 「%K64_CNAME()%의 좋아하는 타입은 (듣)묻지 않아요」
				PRINTFORMW 「으로, 나는 어떻게야？」
			CASEELSE
				PRINTFORMW 「…심한 일만 하는 사람은 누구일까？」
				PRINTFORMW 「응, 누가%K64_CNAME()%에 심한 일 하는지, 가르쳐 줘♪」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, はっきり好きだと告げた FIRSTREACT(4)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が적극적으로 한다で、죠교대상호의가1000이상
;-------------------------------------------------
IF 구상記入チェック() && TFLAG:ACT파생 == 4
	;負荷2は弾いた方が何かと都合がよさそうなのでとりあえず
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック() && TFLAG:REACT부하 < 2 && FIRSTREACT(4)
		;これは重要なターニングポイントになる予定なんだけれど今のところ意味なし
		;ここから코이시ちゃん本気だす予定。本気出さなくても割と酷いけどさらに過酷に
		PRINTFORMW 「………거짓말이야」
		PRINTFORMW 「이래, 나는 누구로부터도 사랑받지 않고 미움받고도 하지 않는다」
		PRINTFORMW 「그러니까 거짓말. 전부거짓말」
		PRINTFORMW 「…거짓말쟁이」
		RETURN 1
	ENDIF
	
	;=================================================
	;역강간で질내사정의後(負荷2は弾く)
	;=================================================
	IF 구상記入チェック() && TFLAG:전회질싸 == 2 && TFLAG:REACT부하 < 2
		IF AFFECTION("상사상애")
			CALL HTMLPRINTW(@"「고마워요%K64_SYMBOL("BHeartW")%」")
			CALL HTMLPRINTW(@"「그렇지만 말야,%K64_CNAME()%는 애완동물이니까 나와 결혼 할 수 없어%K64_SYMBOL("BHeartW")%」")
			PRINTFORMW 「후후, 자리응이군요-응♪」
		ELSE
			PRINTFORMW 「…이런 상황으로 (들)물으면, 어떤 상대에게도 그렇게 대답하겠지요%K64_CNAME()%(은)는」
			PRINTFORMW 「%K64_CNAME()%하지만 정말로 좋아하는 것은 무엇？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;섹스で질내사정의後(負荷2は弾く)
	;=================================================
	IF 구상記入チェック() && TFLAG:전회질싸 == 1 && TFLAG:REACT부하 < 2
		IF AFFECTION("상사상애")
			CALL HTMLPRINTW(@"「섹스라고 좋아하는 사람끼리가 하는 것인걸, 당연해요%K64_SYMBOL("BHeartW")%」")
			CALL HTMLPRINTW(@"「그렇지만 말야,%K64_CNAME()%는 애완동물이니까 나와 결혼 할 수 없어%K64_SYMBOL("BHeartW")%」")
			PRINTFORMW 「후후, 자리응이군요-응♪」
		ELSE
			PRINTFORMW 「…이런 상황으로 (들)물으면, 어떤 상대에게도 그렇게 대답하겠지요%K64_CNAME()%(은)는」
			PRINTFORMW 「%K64_CNAME()%하지만 정말로 좋아하는 것은 무엇？」
		ENDIF
		RETURN 1
	ENDIF
	
	;自然な행동
	;=================================================
	;REACT부하0, 愛想よく笑って大好きだと答えた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, はっきりと好きと告げた
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 作り笑いを浮かべて、好きだよと答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "無理を했다행동"
		PRINTFORMW 「또, 신경을 써…」
		PRINTFORMW 「뭐, 펫으로서는 그것이 정답인지도 모르네요」
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック()
		PRINTFORMW 「…이런 상황으로 (들)물으면, 어떤 상대에게도 그렇게 대답하겠지요%K64_CNAME()%(은)는」
		PRINTFORMW 「%K64_CNAME()%하지만 정말로 좋아하는 것은 무엇？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「もっとすごいことしてくれれば、もっと好きになれるかも」 FIRSTACT(5)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が受け入れるの場合
;-------------------------------------------------
IF 구상記入チェック() && TFLAG:ACT파생 == 5
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック() && FIRSTREACT(5)
		PRINTFORMW 「후~…%K64_CNAME()%하지만 무슨 말을 하고 있는지, 전혀 몰라요」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM52, 이루는대로
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("이루는대로")
		;-------------------------------------------------
		;REACT파생8, おなにーに夢中で聞いてない(자위중)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 8
			;%CALLNAME:MASTER%は자위に没頭してまともに返事ができなかった…
			;%CALLNAME:TARGET%は不満そうだ…
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;REACT파생9, 気持ちよくって生返事(快感がある)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 9
			;%CALLNAME:MASTER%は快感に身悶え하면서頷くことしかできなかった…
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;REACT파생11, 自分でもわからないまま恥ずかしいことを입走る(上記以外)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 11
			;%CALLNAME:MASTER%はどこか茫然と했다まま受け答え했다…
			;自分でもよく解らないままに恥ずかしいことを입走っている気がする…
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;역강간で질내사정의後(負荷2は弾く)
	;=================================================
	IF 구상記入チェック() && TFLAG:전회질싸 == 2 && TFLAG:REACT부하 < 2
		PRINTFORMW 「-응, 강간되어 짜 놓쳤을 뿐은 어딘지 부족하다고？」
		PRINTFORMW 「욕심장이인 애완동물」
		PRINTDATA
;OriginalString : 「じゃあ今度は%K64_CNAME()%が入れられる側になってみる？」
			DATAFORM 「자이번은%K64_CNAME()%하지만 넣을 수 있는 측에 되어 본다？」
;OriginalString : 「試しに、もっと大勢に輪姦されてみる？」
			DATAFORM 「시험삼아, 좀 더 여럿에게 윤간되어 본다？」
;OriginalString : 
		ENDDATA
		RETURN 1
	ENDIF
	
	;=================================================
	;섹스で질내사정의後(負荷2は弾く)
	;=================================================
	IF 구상記入チェック() && TFLAG:전회질싸 == 1 && TFLAG:REACT부하 < 2
		PRINTFORMW 「-응…이만큼 보지 가짜-나와, 아직 부족하다？」
		PRINTFORMW 「음란한 애완동물」
		PRINTDATA
;OriginalString : 「じゃあ今度はお尻で中出し섹스やってみよっか？」
			DATAFORM 「자이번은 엉덩이로 질내사정 섹스 해 봐라 인가？」
;OriginalString : 「それなら、次は私の발でも突っ込んであげよっか？」
			DATAFORM 「그렇다면, 다음은 나의 다리에서도 돌진해 주어라 인가？」
;OriginalString : 「試しに、もっと大勢に輪姦されてみる？」
			DATAFORM 「시험삼아, 좀 더 여럿에게 윤간되어 본다？」
;OriginalString : 
		ENDDATA
		RETURN 1
	ENDIF
	
	;自然な행동
	;=================================================
	;REACT부하0, 「もっとすごいことしてくれば、もっと好きになれるかも」と答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "自然な행동"
		PRINTFORMW 「 좀 더 스고이 일…응」
		PRINTFORMW 「스고이, 는 무슨 일일까…」
		PRINTFORMDW 진하고는 골똘히 생각해 버린 것 같다
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 「もっと好きになるように、もっと可愛がって欲しい」と答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "無理のない행동"
		PRINTFORMW 「네―, 분명하게 귀여워해 주고 있잖아」
		CALL HTMLPRINTW(@"「%K64_CNAME()%는 욕심장이씨%K64_SYMBOL("BHeartW")%」")
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 「好きになれるくらい、すごいことしてくれればいい」と答えた
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "無理を했다행동"
		PRINTFORMW 「-응…」
		PRINTFORMW 「%K64_CNAME()%라고 그러한 것이 타입인 끓어라？」
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;以下、上記のCOM、REACT분류に属さないCOM毎の分岐
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자신에게의 마음을 묻다の場合はTFLAG:REACT파생 == 1(質問に答えない)扱いとなり징계・포인트が加算される
;-------------------------------------------------
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계・포인트加算 派生:「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック()
		PRINTFORMW 「어머, 무엇인가 생각해 내 버렸다？」
		PRINTFORMW 「무서운 여자에게 트라우마에서도 심어졌어？」
		CALL FADEOUT(8, "「킥킥…」")
		RETURN 1
	ENDIF
ENDIF
;REACT1, 자신에게의 마음을 묻다

;────────────────────────────────────
;REACT2, 성에 관한 이야기를 하다・個別リアクション
;────────────────────────────────────
;・地の文は@REACTION_MESSAGEで汎用処理される分のみ
;-------------------------------------------------
@KOJO_REACT_K64_2
;-------------------------------------------------
;　派生する可能性のあるCOMはとりあえずREACT파생分岐を設けていますが
;　&& TFLAG:REACT파생 < 1などとして汎用リアクションで派生を拾う손もあります
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめてと言った
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계・포인트加算 派生:「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;징계・포인트加算 「この恥知らずの豚め！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다")
	;TEQUIPによって快SOURCEが入っている場合に派生
	;=================================================
	;REACT파생1, 쾌락に부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생0, 状況に부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("자위를 시작한다")
	;=================================================
	;REACT파생1, 「勝손にオナニーするな」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생0, 「勝손にオナニーしてるけどいいか」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;=================================================
;COM52, 이루는대로
;=================================================
IF 구상記入チェック(0) && IS_NOWCOMNAME("이루는대로")
	;-------------------------------------------------
	;REACT파생8, おなにーに夢中で聞いてない(자위중)
	;-------------------------------------------------
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 8
		;%CALLNAME:MASTER%は자위に没頭してまともに返事ができなかった…
		;%CALLNAME:TARGET%は不満そうだ…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;REACT파생9, 気持ちよくって生返事(快感がある)
	;-------------------------------------------------
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 9
		;%CALLNAME:MASTER%は快感に身悶え하면서頷くことしかできなかった…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;REACT파생11, 自分でもわからないまま恥ずかしいことを입走る(上記以外)
	;-------------------------------------------------
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 11
		;%CALLNAME:MASTER%はどこか茫然と했다まま受け答え했다…
		;自分でもよく解らないままに恥ずかしいことを입走っている気がする…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT2, 성에 관한 이야기를 하다

;────────────────────────────────────
;REACT3, 상냥하게 위로한다・個別リアクション
;────────────────────────────────────
;・地の文は@REACTION_MESSAGEで汎用処理される分のみ
;-------------------------------------------------
@KOJO_REACT_K64_3
;-------------------------------------------------
;　派生する可能性のあるCOMにはREACT파생分岐を設けていますが
;　&& TFLAG:REACT파생 < 1などとして汎用リアクションで派生を拾う손もあります
;-------------------------------------------------
;　とりあえず회화系COMを全て網羅してありますが
;　10番台の애무系COM、50番台以降のその他COM等も状況によっては선택可能になります
;　필요に応じて分岐を増減して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめて
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 愛想良く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("상냥하게 응답한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("강하게 응답한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생1, 「この恥知らずの豚め！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생0, 「ふーん」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생1, 「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;REACT파생1, 「この恥知らずの豚め！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;=================================================
;COM52, 이루는대로
;=================================================
IF 구상記入チェック(0) && IS_NOWCOMNAME("이루는대로")
	;-------------------------------------------------
	;REACT파생8, おなにーに夢中で聞いてない(자위중)
	;-------------------------------------------------
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 8
		;%CALLNAME:MASTER%は자위に没頭してまともに返事ができなかった…
		;%CALLNAME:TARGET%は不満そうだ…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;REACT파생9, 気持ちよくって生返事(快感がある)
	;-------------------------------------------------
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 9
		;%CALLNAME:MASTER%は快感に身悶え하면서頷くことしかできなかった…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;REACT파생11, 自分でもわからないまま恥ずかしいことを입走る(上記以外)
	;-------------------------------------------------
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 11
		;%CALLNAME:MASTER%はどこか茫然と했다まま受け答え했다…
		;自分でもよく解らないままに恥ずかしいことを입走っている気がする…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT3, 상냥하게 위로한다

;────────────────────────────────────
;REACT4, 엄하게 위협한다・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理無し。SOURCE_MESSAGEでの汎用処理のみ
;-------------------------------------------------
@KOJO_REACT_K64_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다 FIRSTREACT(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다 FIRSTREACT(1)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめて
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다 FIRSTREACT(2)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다 FIRSTREACT(3)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTREACT(3)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다 FIRSTREACT(4)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문プラグ/삼각목마)장착中等の場合は고통のSOURCEが入るので
	;可能性としては考えられますが、この箇所で손間を掛ける필요性としては微妙なところ
	;=================================================
	;REACT파생0, 고통による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 공포による悲鳴
	;=================================================
	IF 구상記入チェック() && TFLAG:REACT파생 == 1
		PRINTFORMW 「우대신, 후후…」
		PRINTFORMW 「아하하 하하하는 하하하는 하하하는 하하하」
		PRINTFORMDW 지나친 공포에 비명을 올린다%CALLNAME:MASTER%에, 진하고는 치 있고 소리를 높였다…
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다 FIRSTREACT(5)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック()
		PRINTFORMW 「후후…무섭다？ 무섭지요, 우대신 후후후」
		PRINTFORMW 「그 무서워한 표정…더욱 더 학째 싶어져버린다♪」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM52, 이루는대로 FIRSTACT(6)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("이루는대로")
	;=================================================
	;REACT파생5, 悔しそうに睨む
	;=================================================
	;・죠교대상が、굴복각인Lv5以下かつ[반항적][굳셈][프라이드높음][건방짐]のいずれかを持っている場合
	;-------------------------------------------------
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 5
		;%CALLNAME:MASTER%は悔しそうに唇を噛み、%CALLNAME%を見上げた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생6, 怯えたように見つめる (上記以外)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 6
		;%CALLNAME:MASTER%は怯えたように%CALLNAME%を見つめた…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT4, 엄하게 위협한다

;────────────────────────────────────
;REACT5, 의상 변경을 요구한다・個別リアクション
;────────────────────────────────────
;・죠교대상のリアクションによって一枚脱ぐ、二枚脱ぐ、脱がないに分かれる
;・REACT분류が"소극적으로 한다", "受け入れる"の場合は一枚脱ぐ
;・REACT분류が"적극적으로 한다", "태연하게 한다"の場合は二枚脱ぐ
;・その他の場合は脱がない(PALAM치정/반항の数値によって、恥ずかしくて脱がない、嫌がって脱がないに分かれる)
;-------------------------------------------------
@KOJO_REACT_K64_5
;-------------------------------------------------
;・TFLAG:ACT파생(0=服を脱ぐように命令/1=服を着るように命令)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 服を脱ぐように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 0
	;=================================================
	;一枚脱いだ(消極的反応)
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("소극적으로 한다/受け入れる")
		;-------------------------------------------------
		;最後の一枚を脱いで전라되었다
		;-------------------------------------------------
		IF 구상記入チェック(0) && (TEQUIP:MASTER:악세서리 + TEQUIP:MASTER:신발안 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
			IF IS_NOWCOMNAME("이루는대로")		;COM이루는대로			ただ%CALLNAME:TARGET%に言われるまま従った…
				PRINTFORMW 
			ELSEIF IS_COMGRONAME("受け入れる")	;REACT분류受け入れる	전라になり、期待と興奮で肌が火照るのを感じた
				PRINTFORMW 
			ELSE								;REACT분류소극적으로 한다	仕方なく最後の一枚を脱ぎ、전라되었다
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;COM52, 이루는대로
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_NOWCOMNAME("이루는대로")
			;%CALLNAME:MASTER%はただ%CALLNAME%に言われるまま従った…
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;緩やかな動きで服を一枚脱ぎ捨てた
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_COMGRONAME("受け入れる")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;仕方なく服を一枚脱ぎ捨てた
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_COMGRONAME("소극적으로 한다")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;上層の分岐を全て埋めた場合は不要な箇所
		;逆に埋めない場合はこの箇所が補完の役目を果たします
		;-------------------------------------------------
		;기타・조건없음
		;-------------------------------------------------
		IF 구상記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;二枚脱いだ(積極的反応)
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;-------------------------------------------------
		;전라되었다
		;-------------------------------------------------
		IF 구상記入チェック(0) && (TEQUIP:MASTER:악세서리 + TEQUIP:MASTER:신발안 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;적극적으로脱ぎ捨てた
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_COMGRONAME("적극적으로 한다")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;손際よく脱ぎ捨てた
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_COMGRONAME("태연하게 한다")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;上層の分岐を全て埋めた場合は不要な箇所
		;逆に埋めない場合はこの箇所が補完の役目を果たします
		;-------------------------------------------------
		;기타・조건없음
		;-------------------------------------------------
		IF 구상記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;脱がなかった
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("거부")
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항	;恥ずかしくて脱げなかった
			PRINTFORMW 
		ELSE										;嫌がって脱がなかった
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 服を着るように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	;=================================================
	;COM52, 이루는대로
	;=================================================
	IF 구상記入チェック(0) && IS_NOWCOMNAME("이루는대로")
		;%CALLNAME:MASTER%はただ%CALLNAME%に言われるまま従った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;一枚着た(消極的反応)
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("소극적으로 한다/受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;二枚着た(積極的反応)
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;地の文では"嫌がって"という扱い
	;=================================================
	;着なかった
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("거부")
		PRINTFORMW 「예？！ 옷을 입는 것, 싫어？！」
		PRINTFORMW 「응―, 그것도 있는 의미 펫인것 같다…의 것인지인？」
		PRINTFORMW 「단순한 노출광일지도 모르지만♪」
		RETURN 1
	ENDIF
ENDIF
;REACT5, 의상 변경을 요구한다

;────────────────────────────────────
;REACT6, 합의를 요구한다・個別リアクション
;────────────────────────────────────
;・죠교대상のリアクションに応じてREACT분류毎に派生する
;-------------------------------------------------
@KOJO_REACT_K64_6
;-------------------------------------------------
;손間を厭わないのであれば、TALENT:MASTER:남자等で内部で分けるのもアリかもしれません
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 小さい声で了承する FIRSTREACT(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が소극적으로 한다の場合
;-------------------------------------------------
IF 구상記入チェック(0) && IS_COMGRONAME("소극적으로 한다")
	;=================================================
	;REACT부하0, 覚悟を決めたように、小さい声で了承했다(自然な행동)
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は覚悟を決めたように、小さい声で了承했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 小さい声で了承했다(無理のない행동)
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は小さい声で了承했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하2, (無理を했다행동)
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		IF COMCOR_POSI()	;実は積極	恥ずかしさに消え入りそうな声で了承했다
			;%CALLNAME:MASTER%は恥ずかしさに消え入りそうな声で了承했다…
			PRINTFORMW 
		ELSE				;実は嫌		悔しさに顔を歪めながらも了承했다
			;%CALLNAME:MASTER%は悔しさに顔を歪めながらも了承했다…
			PRINTFORMW 「아하하」
			PRINTFORMW 「이런 속담원 자리 (들)물을 것도 없고, 갑자기 힘으로 이겨버리면 좋다고 생각하고 있었는데…」
			PRINTFORMW 「일부러 (들)물은 보람이 있었어요.%K64_CNAME()%의 그 얼굴, 이제 최고♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 好きにすればいいと答えた FIRSTREACT(1)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が태연하게 한다の場合
;-------------------------------------------------
IF 구상記入チェック() && IS_COMGRONAME("태연하게 한다")
	;=================================================
	;REACT부하0, 別に嫌じゃないと言った(自然な행동)
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に別に嫌じゃないと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 好きにすればいいと答えた(無理のない행동)
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に好きにすればいいと答えた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하2, (無理を했다행동)
	;=================================================
	IF 구상記入チェック() && GET_COM_RANK() == "無理を했다행동"
		IF COMCOR_POSI()	;実は積極	했다いならいいと言った
			;%CALLNAME:MASTER%は%CALLNAME:TARGET%が했다いならいいと言った…
			PRINTFORMW 「어머나, 시 싶은 것은%K64_CNAME()%의 (분)편이 아닌거야？ 킥킥」
		ELSE				;実は嫌		勝손にすればいいと答えた
			;%CALLNAME:MASTER%は%CALLNAME:TARGET%に勝손にすればいいと答えた…
			PRINTFORMW 「어？ 좋은거야？ 조금 의외…」
			PRINTFORMW 「하지만, 다음에 후회하는 일이 되어도 몰라요…후후」
			PRINTFORML            ﾄﾗｳﾏ
			PRINTFORMW 「멋진 추억을 만들어 주기 때문에♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 意を決して捧げてもいいと告げる FIRSTREACT(2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が적극적으로 한다の場合
;-------------------------------------------------
IF 구상記入チェック(0) && IS_COMGRONAME("적극적으로 한다")
	;=================================================
	;REACT부하0, 初めてをあげられて嬉しいと答えた(自然な행동)
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に初めてをあげられて嬉しいと答えた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 意を決して捧げてもいいと告げた(無理のない행동)
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は意を決して%CALLNAME:TARGET%に自分の初めてを捧げてもいいと告げた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하2, もう守れないならばせめて優しくされるようにと同意했다(無理を했다행동)
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;%CALLNAME:MASTER%はもう守れないならばせめて優しくされるようにと、
		;%CALLNAME:TARGET%に初めてを捧げることに愛想よく同意했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게 해くれるんだったら初めてをあげてもいい FIRSTREACT(3)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が受け入れるの場合
;-------------------------------------------------
IF 구상記入チェック() && IS_COMGRONAME("受け入れる")
	;=================================================
	;COM52, 이루는대로
	;=================================================
	;・一応枠を作ってみた。レアかもしれないけれど、この流れに落ちる可能性はあるよね？
	;・COMで汎用処理するのはちょっと勿体無い部分のような気が했다のよ…
	;・だからといってCOMでACT見に行くのはおかしいと思ったのでREACTで処理…でいいのかな？
	;・この場合@TRAIN_MESSAGE2は表示されず、代わりに@REACTION_MESSAGEが表示される
	;・状況により内容はバラバラだが、いずれもREACT분류が"受け入れる"扱いであり、합의を得られる
	;-------------------------------------------------
	;・볼개그噛ませた上で저항불능にさせてから無理矢理합의を得ることも出来る…気がする
	;・敢えて一度거부させてから、作為的にこの流れに落とし込んで、そのまま奪ってしまうと楽しい？
	;・しかし、저항불능の相손から합의を得るか。합의、ね… とりあえず私は面白い方を選ぶわ。卑怯とは言うまいね？
	;・あ、코이시ちゃんって基本的に합의求めないんだったわ。どうしようかしら。強制합의か、합의無視か…
	;-------------------------------------------------
	IF 구상記入チェック() && IS_NOWCOMNAME("이루는대로")
		;-------------------------------------------------
		;REACT파생8, おなにーに夢中で聞いてない(자위중)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 8
			;%CALLNAME:MASTER%は자위に没頭してまともに返事ができなかった…
			;%CALLNAME:TARGET%は不満そうだ…
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;REACT파생9, 気持ちよくって生返事(快感がある)
		;-------------------------------------------------
		IF 구상記入チェック(0) && TFLAG:REACT파생 == 9
			;%CALLNAME:MASTER%は快感に身悶え하면서頷くことしかできなかった…
			PRINTFORMW 「후후, 역시. 그렇다고 생각하고 있었어♪」
			PRINTFORMW 「%K64_CNAME()%(은)는 기분 좋으면 뭐든지 좋지요？」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;REACT파생11, 自分でもわからないまま恥ずかしいことを입走る(上記以外)
		;-------------------------------------------------
		IF 구상記入チェック() && TFLAG:REACT파생 == 11
			;%CALLNAME:MASTER%はどこか茫然と했다まま受け答え했다…
			;自分でもよく解らないままに恥ずかしいことを입走っている気がする…
			PRINTFORMW 「후후. 응, 정말로 그런 일 해 버려서도 괜찮아？」
			PRINTFORMW 「이제 캔슬은 받아들이지 않으니까♪」
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;REACT부하0, 初めてを捧げるから기분좋게 해欲しいと言った(自然な행동)
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に初めてを捧げるから、
		;その分もっと기분좋게 해欲しいと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 기분좋게 해くれるんだったら初めてをあげてもいいと答えた(無理のない행동)
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は少し悩んでから、기분좋게 해くれるんだったら
		;%CALLNAME:TARGET%に初めてをあげてもいいと答えた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하2, 初めてを捧げろと言うなら、辛さを忘れられるくらい기분좋게 해と言った
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に、初めてを捧げろと言うなら
		;辛さを忘れられるくらい기분좋게 해欲しいと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의しない FIRSTREACT(4)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류がその他の場合(その他のREACT분류の場合、@ACTION_APPLY2_6にて"거부"にセットされる)
;-------------------------------------------------
IF 구상記入チェック() && IS_COMGRONAME("거부")
	;%CALLNAME:MASTER%의 합의をもらえなかった%CALLNAME:TARGET%は少し失望했다ようだ…
	
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック() && FIRSTREACT(4)
		PRINTFORMW 「-응, 그것은 좋았다」
		PRINTFORMW 「그렇지 않으면 빼앗는 즐거움이６할인인걸♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック()
		PRINTFORMW 「그렇게 소중해？ 키득,%K64_CNAME()%라고 재미있다♪」
		RETURN 1
	ENDIF
ENDIF
;REACT6, 합의를 요구한다

;────────────────────────────────────
;REACT7, 징계한다고 위협한다・個別リアクション
;────────────────────────────────────
;・宣言しても징계・が発生するとは限らない
;-------------------------------------------------
@KOJO_REACT_K64_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM"마음 약하게 응답한다"は一定条件でREACT분류"용서를 빌다"に派生する
;-------------------------------------------------
IF 구상記入チェック(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめてと言った
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생1, 「この恥知らずの豚め！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생0, 「ふーん」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다") 
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTREACT(7)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	;TEQUIP:(관장＋항문プラグ/삼각목마)장착中等の場合は고통のSOURCEが入るので
	;可能性としては考えられますが、この箇所で손間を掛ける필요性としては微妙なところ
	;=================================================
	;REACT파생0, 고통による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 공포による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM52, 이루는대로
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("이루는대로")
	;=================================================
	;REACT파생5, 悔しそうに睨む
	;=================================================
	;・죠교대상が、굴복각인Lv5以下かつ[반항적][굳셈][프라이드높음][건방짐]のいずれかを持っている場合
	;-------------------------------------------------
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 5
		;%CALLNAME:MASTER%は悔しそうに唇を噛み、%CALLNAME%を見上げた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생6, 怯えたように見つめる (上記以外)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 6
		;%CALLNAME:MASTER%は怯えたように%CALLNAME%を見つめた…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT7, 징계한다고 위협한다

;────────────────────────────────────
;REACT8, 고함치다・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理なし
;-------------------------------------------------
@KOJO_REACT_K64_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめて
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생1, 「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문プラグ/삼각목마)장착中等の場合は고통のSOURCEが入るので
	;可能性としては考えられますが、この箇所で손間を掛ける필요性としては微妙なところ
	;=================================================
	;REACT파생0, 고통による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 공포による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM52, 이루는대로
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("이루는대로")
	;=================================================
	;REACT파생5, 悔しそうに睨む
	;=================================================
	;・죠교대상が、굴복각인Lv5以下かつ[반항적][굳셈][프라이드높음][건방짐]のいずれかを持っている場合
	;-------------------------------------------------
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 5
		;%CALLNAME:MASTER%は悔しそうに唇を噛み、%CALLNAME%を見上げた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생6, 怯えたように見つめる (上記以外)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 6
		;%CALLNAME:MASTER%は怯えたように%CALLNAME%を見つめた…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT8, 고함치다

;────────────────────────────────────
;REACT9, 기분나쁘게 웃다・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理なし
;-------------------------------------------------
@KOJO_REACT_K64_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめて
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생1, 「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문プラグ/삼각목마)장착中等の場合は고통のSOURCEが入るので
	;可能性としては考えられますが、この箇所で손間を掛ける필요性としては微妙なところ
	;=================================================
	;REACT파생0, 고통による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 공포による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM52, 이루는대로
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("이루는대로")
	;=================================================
	;REACT파생5, 悔しそうに睨む
	;=================================================
	;・죠교대상が、굴복각인Lv5以下かつ[반항적][굳셈][프라이드높음][건방짐]のいずれかを持っている場合
	;-------------------------------------------------
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 5
		;%CALLNAME:MASTER%は悔しそうに唇を噛み、%CALLNAME%を見上げた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생6, 怯えたように見つめる (上記以外)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 6
		;%CALLNAME:MASTER%は怯えたように%CALLNAME%を見つめた…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT9, 기분나쁘게 웃다
