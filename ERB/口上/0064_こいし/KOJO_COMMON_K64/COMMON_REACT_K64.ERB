;-------------------------------------------------
;코이시구상 통괄 범용 리액션구상파일
;-------------------------------------------------
;개별의 리액션 및 범용 리액션이 표시되지 않는 경우에 불려 가는구상를 정리한 파일（가짜）
;어디까지나 개인용인 것으로 코멘트는 최소한
;다른 캐릭터에게 적용하는 경우는@KOJO_REACT_KXX를 임의의번호로 변경
;현재는 장래용이라는 느낌. 통상 사용하는 일은 우선 없다고 생각된다
;-------------------------------------------------
;·예에 의해 예와 같이, 엘리스씨로부터 빌리고 왔다
;-------------------------------------------------
;*************************************************
;통괄 범용 리액션구상
;*************************************************
[SKIPSTART]
;=================================================
;@SELECTCOM와 TFLAG:80으로 분기 만드는 것이 적절?
;=================================================
;·개별의 리액션＆범용 리액션이 표시되지 않았던 때에 표시
;-------------------------------------------------
@KOJO_REACT_K64
;-------------------------------------------------

;-------------------------------------------------
;애무, 봉사, 성교봉사, 성교등 @TFLAG:80으로 분기
;-------------------------------------------------
IF 구상기입 체크(0)
	;=================================================
	;회화
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT분류 == 0
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;애무
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT분류 == 1
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;도구
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT분류 == 2
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;성교
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT분류 == 3
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;수치
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT분류 == 4
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;봉사
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT분류 == 5
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;가학
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT분류 == 6
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;이상
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT분류 == 7
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;사역
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT분류 == 8
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;휴식
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT분류 == 9
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT분류 == 10
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF
[SKIPEND]
