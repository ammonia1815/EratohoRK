;-------------------------------------------------
;코이시구상 汎用アクション구상ファイル
;-------------------------------------------------
;個別の죠교자アクションが表示されない場合に
;呼び出される구상をまとめたファイル（仮）
;あくまで個人用なのでコメントは最小限
;他のキャラに適用する場合は@KOJO_ACT_KXXを任意の번호に変更
;現在は将来用って感じ
;-------------------------------------------------
;・例によって例の如く엘리스さんからお借りしてきた
;-------------------------------------------------
;*************************************************
;汎用アクション구상
;*************************************************
[SKIPSTART]
;=================================================
;@TFLAG:80の各項目で分岐
;=================================================
;・個別の죠교자アクションが表示されなかった際に表示
;・ACTはUSEのスキップがあるから念のため戻り値に注意
;・戻り値 (0.非表示/1.表示/2.表示して미약等구상をスキップ)
;-------------------------------------------------
@KOJO_ACT_K64
;-------------------------------------------------

;-------------------------------------------------
;애무、봉사、성교봉사、성교等@TFLAG:80で分岐
;-------------------------------------------------
IF 구상記入チェック(0)
	;=================================================
	;회화
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "회화")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상記入チェック(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;애무
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "애무")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상記入チェック(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;도구
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "도구")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상記入チェック(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;성교
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "성교")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상記入チェック(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;수치
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "수치")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상記入チェック(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;봉사
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "봉사")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상記入チェック(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;가학
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "가학")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상記入チェック(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;이상
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "이상")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상記入チェック(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;사역
	;=================================================
	IF 구상記入チェック(0) && GET_ACTM(TFLAG:ACT) == 8
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상記入チェック(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;ご휴식
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "휴식")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상記入チェック(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "성봉")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상記入チェック(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF
[SKIPEND]
