;-------------------------------------------------
;코이시구상 범용 액션구상파일
;-------------------------------------------------
;개별의죠교자액션이 표시되지 않는 경우에
;불려 가는구상를 정리한 파일（가짜）
;어디까지나 개인용인 것으로 코멘트는 최소한
;다른 캐릭터에게 적용하는 경우는@KOJO_ACT_KXX를 임의의번호로 변경
;현재는 장래용이라는 느낌
;-------------------------------------------------
;·예에 의해 예와 같이엘리스씨로부터 빌리고 왔다
;-------------------------------------------------
;*************************************************
;범용 액션구상
;*************************************************
[SKIPSTART]
;=================================================
;@TFLAG:80의 각 항목으로 분기
;=================================================
;·개별의죠교자액션이 표시되지 않았던 때에 표시
;·ACT는 USE의 스킵이 있기 때문에 만약을 위해 반환값에 주의
;·반환값(0. 비표시/1. 표시/2. 표시해미약등구상를 스킵)
;-------------------------------------------------
@KOJO_ACT_K64
;-------------------------------------------------

;-------------------------------------------------
;애무, 봉사, 성교봉사, 성교등 @TFLAG:80으로 분기
;-------------------------------------------------
IF 구상기입 체크(0)
	;=================================================
	;회화
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "회화")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;애무
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "애무")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;도구
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "도구")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;성교
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "성교")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;수치
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "수치")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;봉사
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "봉사")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;가학
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "가학")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;이상
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "이상")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;사역
	;=================================================
	IF 구상기입 체크(0) && GET_ACTM(TFLAG:ACT) == 8
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;휴식
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "휴식")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "성봉")
		;-------------------------------------------------
		;상사상애
		;-------------------------------------------------
		IF 구상기입 체크(0) && AFFECTION("상사상애")
;OriginalString : 
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;그 외
		;-------------------------
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF
[SKIPEND]
