;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	코이시구상 추가 액션구상파일
;
;	죠교자가죠교지시 이외에 실시하는행동의 파일
;
;Version		Reason
;0. 01			엘리스구상 추가 액션구상파일 Version1. 67을 베이스로 재구축
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;*************************************************
;죠교액션·커맨드구상
;*************************************************

;=================================================
;탈의액션구상
;=================================================
;·죠교자액션의 앞에강제탈의나자주탈의가 발생했다때에 표시
;·이구상의 시점에서는 아직 TEQUIP는 갱신되어 있지 않다
;·첫회 분기는 「처음 보았다/보였다」란 할 수 없다.
;  숍 메뉴로 벗게 했던 적이 있을지도 모르고, 죠교의 흐름으로 완전하게 벗게 하지 않고서노출만 하고 있는 경우도 있다.
;  어쩌면조수였던 때에 본 적이 있을지도 모른다. 그러한 의미에서는 간이적인 것이다.
;  엄밀한 의미에서의 「초견」플래그를 준비할지 어떨지는 요망하는 대로라고 하는 일로.
;-------------------------------------------------
;memo
;·기재가 사라지고 있었기 때문에 사양 변경당했다의라고 생각하고 있으면, 아무래도 코드적으로는조수가[주인님]의 경우의 특수 처리가 남아 있는 분위기
;·실제로 움직여 확인은 하고 있지 않지만, 변함없으면 이하와 같이 움직이가 될 것
;·죠교자가 벗게 하는 경우→조수가[주인님]라면죠교자의탈의서포트구상를 부른다. 그렇지 않으면죠교자의탈의구상(여기)를 부른다
;·조수가 벗게 하는 경우→조수가[주인님]라면조수의탈의구상(여기)를 부른다. 그렇지 않으면조수의탈의서포트구상를 부른다
;·이 특수 처리때는, 일시적으로 TARGET와 ASSI가 바뀌므로, 이구상의구상주는 항상 TARGET인 것이 보증된다
;·즉이 구상은 죠교자의 강제/자주탈의시 뿐만이 아니라, 조수의탈의서포트에서도 불리는 일이 있다. 좋다?
;·그리고조수구상는 기본적으로 오른쪽 대고로 표시되므로, 이구상도 오른쪽 공격이 되는 경우가 있다. 여기가 포인트
;· 오른쪽 대고 위, 대사나 지문의 즉시가 발생한다 따위 해, 레이아웃적으로 무너지는 부분이 나오지 않는가 주의 할 것. 테스트 중점인
;·그렇지 않아도 즉시가 발생하는 것 같은 장문은… 적당한 개행이… 그… 어때?
;·귀찮으면 입장 한정으로 회피해 버리는 것도손일지도?
;=================================================
@KOJO_DATUI_K64
;=================================================
;-------------------------------------------------
;관련 변수…
;강제탈의:TFLAG:102  자주탈의:TFLAG:103
;=(-1, 강제탈의에 저항, 1, 겉옷, 2, 하반신겉옷, 3, 웃도리, 4, 상반신전부, 5, 하반신전부,
;  6, 악세서리─, 7, 악세서리─이외 모두, 8, 전라까지, 9, 속옷만)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의에 저항당했다 FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:강제탈의 < 0
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크() && FIRSTDATUI(0)
;OriginalString : 「あれ、%K64_CNAME()%どうして邪魔するの？」
		PRINTFORMW 「저것,%K64_CNAME()%어째서 방해 해?」
		IF TALENT:MASTER:남자
;OriginalString : 「見られて恥ずかしいような粗末なモノでもぶらさがってるの？」
			PRINTFORMW 「보여져 부끄러운 것 같은 허술한 것이라도 매달려 있어?」
;OriginalString : 「…%K64_CNAME()%はペットなんだから、あまりわがままを言ってはダメよ」
			PRINTFORMW 「…%K64_CNAME()%는 애완동물인 것이니까, 너무 이기적임을 말해서는 안 돼요」
		ELSE	;후타나리/여자
			IF ASSISTALENT(GETNUM(TALENT, "남자")) >= 0	;조수에남자가 있다. 이 사양은 현재 사라지고 있다
;OriginalString : 「あ、%K64_CNAME(ASSISTALENT(GETNUM(TALENT, "남자")))%が見てるから恥ずかしいの？」
				PRINTFORMW 「아,%K64_CNAME(ASSISTALENT(GETNUM(TALENT, "남자")))%하지만 보고 있기 때문에 부끄러운거야?」
;OriginalString : 「…そっか…まずはそこから躾けないとダメなのね」
				PRINTFORMW 「…그런가…우선은 거기로부터 예의범절을 가르치지 않으면 안되는거네」
			ELSE
;OriginalString : 「別に여자同士なんだから、부끄러워한다ことないのに…」
				PRINTFORMW 「별로여자같은 종류인 것이니까,부끄러워한다일 없는데…」
;OriginalString : 「ましてや、%K64_CNAME()%はペットなんだからさぁ…」
				PRINTFORMW 「하물며,%K64_CNAME()%는 애완동물인 것이니까…」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;자신은 벗었다
	;=================================================
	IF 구상기입체크() && TFLAG:자주탈의
;OriginalString : 「ちょっと、私だけ脱いで%K64_CNAME()%は脱がないってどういうことなの？」
		PRINTFORMW 「좀, 나만 벗어%K64_CNAME()%는 벗지 않는다는건 어떻게 말하는 일이야?」
;OriginalString : 「それってズルくない？」
		PRINTFORMW 「그 거 간사하지 않다?」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE RANDDATUI(2, 0)
		CASE 0
;OriginalString : 「うーん、服を脱がすだけでそんなに抵抗するなんて…」
			PRINTFORMW 「응, 옷을 벗기는 것만으로 그렇게 저항하다니…」
;OriginalString : 「これじゃ先が思いやられるなぁ…」
			PRINTFORMW 「이래서야 앞일이 걱정되는구나…」
		CASEELSE
;OriginalString : 「またそうやって邪魔する～！」
			PRINTFORMW 「또 그렇게 방해 한다～!」
;OriginalString : 「주인님に肌を晒すのがそんなに嫌？」
			PRINTFORMW 「주인님에 피부를 쬐는 것이 그렇게 싫어?」
;OriginalString : 「………ま、それはそれで楽しめそうだけどね♪」
			PRINTFORMW 「………뭐, 그건 그걸로 즐길 수 있을 것 같지만 말야♪」
	ENDSELECT
	RETURN 1
ENDIF

;·주로섹스계행동의 예비 동작으로 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;두 사람 모두 뭔가 벗은 FIRSTDATUI(1)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 > 0 && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가[정욕]또한전라되었다
	;=================================================
	IF 구상기입체크(0) && T_COND("정욕") && ZENRACHECK(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가죠교대상를익애
	;=================================================
	IF 구상기입체크(0) && AFFECTION("익애")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·주로신발안. 그 밖에장갑나소매 등
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·악세서리─마셔 FIRSTDATUI(2)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 == 6 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(2)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전라되었다（벗기 전은신발안 이외 알몸이라든지 그러한 느낌）
	;=================================================
	IF 구상기입체크(0) && ZENRACHECK(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번탈의로성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·만p FIRSTDATUI(3)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(3)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써가슴가 보이고 있다
	;=================================================
	IF 구상기입체크(0) && B_DATUICHECK(0) == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;응 이렇게 p
	;=================================================
	IF 구상기입체크(0) && PENIS(TARGET)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번탈의로 젖가슴이 보이도록(듯이)되었다경우에 표시
;·브라 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·말하는 p FIRSTDATUI(4)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(4)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써성기가 보이고 있다
	;=================================================
	IF 구상기입체크(0) && P_DATUICHECK(0) == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번탈의로팬티（캐릭터에 의해드로와즈·트렁크）가 보이도록(듯이)되었다경우에 표시
;·하반신속옷가 없는 캐릭터는 쓰는필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·팬티 보이고 FIRSTDATUI(5)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && P_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(5)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번탈의로 브라（캐릭터에 의해스포츠브라·사라시）가 보이도록(듯이)되었다경우에 표시
;·상반신속옷가 없는 캐릭터는 쓰는필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·브라 보이고 FIRSTDATUI(6)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(6)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·「전라까지 벗었다」만이 아니고, 「지금 벗었으므로전라되었다」라고 하는 의미
;·「겉옷 밖에 남지 않은상태로겉옷를 벗었다」등도 포함된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·전라에 FIRSTDATUI(7)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && ZENRACHECK(0) && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(7)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·주로신발안. 그 밖에장갑나소매 등
;·탈이했다중에 접어들지 않는다! 되기 십상인 것으로 이것만은 부위별 텐프레를 준비
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·악세서리─마셔 FIRSTDATUI(8)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 == 6
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(8)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전라되었다
	;=================================================
	IF 구상기입체크(0) && ZENRACHECK(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번탈의로남성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·고추 보여라 FIRSTDATUI(9)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번탈의로 여자성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·그대로응 보여라 FIRSTDATUI(10)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
	IF PENIS(TARGET)	;죠교자에페니스 있어
;OriginalString : 
		PRINTFORMW 
	ELSE				;죠교자에페니스없음
;OriginalString : 
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;·이번탈의로 젖가슴이 보이도록(듯이)되었다경우에 표시
;·브라 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;·죠교대상이남자라고 표시하지 않기 때문에, 남자한정구상에서는 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·젖가슴 보여라 FIRSTDATUI(11)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번탈의로하반신속옷가 보이도록(듯이)되었다경우에 표시
;·죠교대상가하반신속옷가 없는 캐릭터 한정의구상이면 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·팬티 보여라 FIRSTDATUI(12)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 2
	IF TALENT:MASTER:남자
;OriginalString : 
		PRINTFORMW 
	ELSEIF TALENT:MASTER:후타나리
;OriginalString : 
		PRINTFORMW 
	ELSE
;OriginalString : 
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;·이번탈의로 브라상반신속옷가 보이도록(듯이)되었다경우에 표시
;·죠교대상이남자라고 표시하지 않기 때문에, 남자한정구상에서는 쓰는필요가 없다
;·죠교대상가상반신속옷가 없는 캐릭터 한정의구상이면 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·브라 보여라 FIRSTDATUI(13)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 2 && !TALENT:MASTER:남자
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(13)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·「전라까지 벗겼다」만이 아니고, 「지금 벗겼으므로전라되었다」라고 하는 의미
;·「겉옷 밖에 남지 않은상태로겉옷를 벗겼다」등도 포함된다
;·「젖가슴 보여라」～「팬티 보여라」를 썼을 경우는 기입하는필요는 없다（상층의 분기에 먹혀져 여기까지 떨어져 오지 않기 때문에）
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·전라에 FIRSTDATUI(14)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && ZENRACHECK(1)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(14)
		IF TALENT:MASTER:남자
;OriginalString : 
			PRINTFORMW 
		ELSEIF TALENT:MASTER:후타나리
;OriginalString : 
			PRINTFORMW 
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF TALENT:MASTER:남자
;OriginalString : 
		PRINTFORMW 
	ELSEIF TALENT:MASTER:후타나리
;OriginalString : 
		PRINTFORMW 
	ELSE
;OriginalString : 
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;·자위·봉사계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·뭔가 벗은 FIRSTDATUI(15)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(15)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;·애무계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·뭔가 벗긴 FIRSTDATUI(16)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(16)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF


;=================================================
;추가 액션구상
;=================================================
;·죠교자가추가행동를 취했을 때에 범용적으로 표시
;·쓰는 경우, 액션구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------

;-------------------------------------------------
;EXTRA1, Ｃ애무
;-------------------------------------------------
;·추가로애무 또는수음를 실시한다
;·죠교대상에페니스가 없는 경우는애무, 어느 경우는수음
;-------------------------------------------------
@KOJO_ACT_EXTRA_K64_1
;-------------------------------------------------
;EXTRA1_0, 애무 한다
;-------------------------------------------------
;·죠교대상에페니스가 없는 경우
;-------------------------------------------------
IF 구상기입체크(0) && !PENIS(MASTER)
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;-------------------------------------------------
;EXTRA1_1, 수음 한다
;-------------------------------------------------
;·죠교대상에페니스가 있는 경우
;-------------------------------------------------
IF 구상기입체크(0) && PENIS(MASTER)
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;-------------------------------------------------
;EXTRA2, Ｖ애무
;-------------------------------------------------
;·추가로 바지애무를 실시한다
;·죠교대상이남자의 경우 발생하지 않는다
;·죠교대상가처녀의 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K64_2
IF 구상기입체크(0)
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;-------------------------------------------------
;EXTRA3, Ａ애무
;-------------------------------------------------
;·추가로항문애무를 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K64_3
IF 구상기입체크(0)
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;-------------------------------------------------
;EXTRA4, Ｂ애무
;-------------------------------------------------
;·추가로 유두 고문을 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K64_4
IF 구상기입체크(0)
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF


;=================================================
;미약등사용구상
;=================================================
;·죠교자가미약나로션를 사용했다때에 범용적으로 표시
;·「액션구상」로 기입 체크를=2로했다경우는 표시되지 않는다
;·쓰는 경우, 액션구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
@KOJO_USE_K64
;-------------------------------------------------
;로션/미약동시 사용
;-------------------------------------------------
;·주로성교계 커맨드로부터 발생한다
;-------------------------------------------------
IF 구상기입체크(0) && (TFLAG:미약등사용구상 & 3) == 3
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;-------------------------------------------------
;로션사용
;-------------------------------------------------
;·완구·성교계 커맨드등으로부터 발생한다
;-------------------------------------------------
IF 구상기입체크() && (TFLAG:미약등사용구상 & 1)
;OriginalString : 「そうだ、これも使ってあげる」
	PRINTFORMW 「그렇다, 이것도 사용해 준다」
;OriginalString : 「はい、ぬるぬる～♪」
	PRINTFORMW 「네, 미끈미끈～♪」
	RETURN 1
ENDIF

;-------------------------------------------------
;미약사용
;-------------------------------------------------
;·애무계 커맨드등으로부터 발생한다
;·키스로 발생했다경우는입옮기기가 되기 (위해)때문에, 그 케이스는키스의 액션구상내에서 처리하는 편이 좋다
;-------------------------------------------------
IF 구상기입체크(0) && (TFLAG:미약등사용구상 & 2)
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF
