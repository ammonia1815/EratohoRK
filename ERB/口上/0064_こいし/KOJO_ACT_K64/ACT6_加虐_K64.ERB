;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	코이시구상 가학계 액션구상파일
;
;	가학계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;가학계 ACT60～68
;========================================================================
;·TFLAG:ACT(60=스펭킹/61=채찍/62=바늘/63=밧줄/64=눈가리개/65=볼개그/66=매도/67=삼각목마/68=관장세트)
;·TFLAG:ACT분류 == 6에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "가학")로 참조하는 것이 가능
;·TEQUIP계는 기본, ACT 개시시점으로써장착 된다
;·ACT에 따라서는죠교대상의 반응하기 나름으로취소 되는 것이 있다
;·밧줄/눈가리개/볼개그/삼각목마는 TEQUIP로관리 되고 해당 ACT/REACT 이외에도 TEQUIP:밧줄등으로 참조할 수 있다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT60, 스펭킹
;────────────────────────────────────
;·죠교자가죠교대상의 엉덩이를 평손로 두드린다
;·ACT 개시시점으로써 벌써 두드리고 있다
;·성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중인 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K64_60
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT60, 스펭킹
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;·@TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	[SKIPSTART]
	각 분기내에 ABL:MASTER:마조끼나 ABL:MASTER:고통중독등으로 파생을 마련하는 것도 좋다고 생각합니다
	디폴트로 준비하면 무리하게 묻어에 걸리는 예가 비교적 좋게 보여지므로, 텐프레로서는 굳이 마련하지 않습니다
	필요에 응해 확장해 주세요
	
	SELECTCASE ABL:MASTER:마조끼
		CASE IS >= 6	;마조끼 6 이상
;OriginalString : 
			PRINTFORMW 
		CASE IS >= 4	;마조끼 4 이상
;OriginalString : 
			PRINTFORMW 
		CASEELSE		;그 외(마조끼 3 이하)
;OriginalString : 
			PRINTFORMW 
	ENDSELECT
	
	라든지, 이런 느낌으로. 확률도 관련되어 나눈다면…
	IF ELSEIF로 ABL:MASTER:마조끼 > 6 && ! RAND:2같은 방식이라도 좋을까
	단지 파생시킨다 뿐이라면 IF ELSEIF보다 SELECTCASE의 사용이 바람직합니다
	[SKIPEND]
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
;OriginalString : 「言う事聞かない子は、お尻ぺんぺんよ」
		PRINTFORMW 「말하는 일 (듣)묻지 않는 아이는, 엉덩이응이야」
		RETURN 1
	ENDIF
	
	;금일의방침가징계·의 경우는 여기에 밖에 떨어지지 않게 되므로
	;구상기입 체크(20)등으로 해서 확률을 짜는 것을 추천
	;=================================================
	;징계·발동중
	;=================================================
	IF 구상기입체크(33) && TFLAG:징계·플래그
;OriginalString : %CALLNAME:MASTER%의 尻を叩く코이시はとても楽しそうだ…
		PRINTFORMDW %CALLNAME:MASTER%(이)가 격려 하는 진하고는 매우 즐거운 듯 하다…
		IF ALI(0)	;우선 나누어 보는 테스트
;OriginalString : 「ごめんね、私も本当はこんなこと했다くないの」
			PRINTFORMW 「미안해요, 나도 사실은 이런 금년 싶지 않은거야」
;OriginalString : 「でも、痛くしないとおしおきにならないから…」
			PRINTFORMW 「 그렇지만, 아프게 하지 않으면 사형이 되지 않으니까…」
		ELSE
;OriginalString : 「悪い子にはお尻ぺんぺんの刑よ！」
			PRINTFORMW 「나쁜 아이에게는 엉덩이응의 형이야!」
;OriginalString : 「びしっ！ ばしっ！ う후후…」
			PRINTFORMW 「쉿! 쉿! 후후…」
;OriginalString : 何故か코이시の입から発せられる擬音と、尻を叩くタイミングや強さがまるで合っていない
			PRINTFORMDW 왜일까 진하고의 입으로부터 발 다투어지는 의음과 격려 하는 타이밍이나 강함이 전혀 맞지 않았다
;OriginalString : 期せずして若干不意打ちめいた衝撃が、%CALLNAME:MASTER%의 고통と공포を増幅する…
			PRINTFORMDW 우연히 약간 기습인 듯한 충격이, %CALLNAME:MASTER%의 고통과 공포를 증폭한다…
;OriginalString : 「びしっ！ ばしっ！ ぱぁん！ 쿠쿡…」
			PRINTFORMW 「쉿! 쉿!! 킥킥…」
		ENDIF
		RETURN 1
	ENDIF
	
	;TEQUIP:죠교대상Ｃ사용 == 2（입에 의한애무）는 고려하지 않는다
	;=================================================
	;Ｃ애무계속중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:TARGET:죠교대상Ｃ사용 == 1
		IF PENIS(MASTER)	;수음계속중
;OriginalString : 
			PRINTFORMW 
		ELSE				;애무계속중
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크() && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 「후후…%K64_CNAME()%のお尻、キレイな色になってきたよ？」
		PRINTFORMW 「후후…%K64_CNAME()%의 엉덩이, 깨끗한 색이 되어 왔어?」
;OriginalString : 「このまま叩き続けたら、どんな色になるのかなぁ…」
		PRINTFORMW 「이대로 계속 이대로 두드리면(자) , 어떤 색이 되는지…」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
;OriginalString : 「くすっ…どう？ 痛い？」
		PRINTFORMW 「키득… 어때? 아프다?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT61, 채찍
;────────────────────────────────────
;·죠교자가채찍를 크게 치켜들고 마음껏 찍어내린다
;·ACT 개시시점으로써 벌써 두드리고 있다
;·턴방침가"소프트"라면 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K64_61
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT61, 채찍
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
;OriginalString : 突然、空を裂くような音と共に、%CALLNAME:MASTER%의 身体に激痛が走った
		PRINTFORMDW 돌연, 하늘을 찢는 것 같은 소리와 함께, %CALLNAME:MASTER%의 신체에 격통이 달렸다
;OriginalString : 慌てて状況を確認すると、どうやら코이시に채찍으로 맞았다ようだ
		PRINTFORMDW 당황해 상황을 확인하면, 아무래도 진하고에 채찍으로 맞은 것 같다
;OriginalString : 「あははっ、채찍は楽だし面白いし손も痛くならない♪」
		PRINTFORMW 「아하하 , 채찍은 편하고 재미있고 손도 아프게 안 된다♪」
;OriginalString : 「まだ終わりじゃないよ？ ほらっ！ う후후…」
		PRINTFORMW 「아직 마지막이 아니야? 이봐요! 후후…」
		RETURN 1
	ENDIF
	
	;=================================================
	;눈가리개장착
	;=================================================
	IF 구상기입체크(0) && TEQUIP:TARGET:눈가리개
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;구속중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:TARGET:밧줄
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
;OriginalString : 「さぁ、びしびし躾けるよ♪」
		PRINTFORMW 「, 가차없이 예의범절을 가르쳐♪」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT62, 바늘
;────────────────────────────────────
;·유두와성기에바늘를 찌른다
;·ACT 개시시점으로써 벌써 찌르고 있다
;·죠교대상에페니스가 있는 경우는페니스에, 없는 경우는클리토리스에 찌른다
;·가슴와성기(페니스/바기나)로부터 출혈해, 혈액 더러움이 추가된다
;·턴방침가"소프트", "노멀"라면 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K64_62
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT62, 바늘
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은페니스의 유무로 분기. 분기내에 파생 없음
;-------------------------------------------------
;IF 구상기입 체크(0) && PENIS(MASTER) 등으로 나누는손도 있습니다. 필요에 응해 분기를 증감해 주세요
;비교적 쓰기 어려운 개소이며, 온전히 하는 것은손간인 것로\@ PENIS(MASTER) ? 페니스 # 클리토리스 \@와 같이 3항을 사용하면 락 할 수 있거나 합니다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;눈가리개장착중
	;=================================================
	IF 구상기입체크() && TEQUIP:TARGET:눈가리개
;OriginalString : 「あ、動かないほうがいいよ」
		PRINTFORMW 「아, 움직이지 않는 편이 좋아」
;OriginalString : 「暴れられたら、狙いが外れて酷いことになっちゃうわ。쿠쿡…」
		PRINTFORMW 「날뛸 수 있으면, 목적이 빗나가 심하게 되어버리는 원. 킥킥…」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT63, 밧줄
;────────────────────────────────────
;·죠교대상를밧줄로 묶으려고 한다
;·지문은"묶으려고했다…"되고 있지만 처리적으로는 ACT 개시시점으로써 속박되고 있다
;·TEQUIP:밧줄로 묶기의 강도를 참조할 수 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:밧줄자체는 REACT분류가"용서를 빌다"의 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을 확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·(REACT63 이외에도취소처리가 되는 경우가 있기 때문에(위해))
;·성교중/성교봉사중에는 발생하지 않는다
;·구속시에자위나안면승마, 죠교대상의 페니스사용중는 해제된다
;-------------------------------------------------
@KOJO_ACT_K64_63
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_0, 밧줄장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·반항각인, 죠교자죠교레벨, 기교, 긴박능숙소질에 응해구속SOURCE 증가
;·구속SOURCE에 응해 강도 상승
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;묶기 강도는통상라면 1이나 2 정도라고 생각됩니다
	;그 이상의 묶기 강도는반항각인나죠교자의긴박능숙소질+일정 이상의기교가 요구될까하고
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		SELECTCASE TEQUIP:TARGET:밧줄
			CASE IS >= 3	;묶기 강도 3, 혹은 그 이상
;OriginalString : 
				PRINTFORMW 
			CASE 2			;묶기 강도 2
;OriginalString : 
				PRINTFORMW 
			CASEELSE		;그 외
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_1, 밧줄해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;밧줄해제에 파생했다 시점에서, "TEQUIP:밧줄 = 0"이 되므로 해제시에 묶어 강도는 참조 할 수 없다
;장착와 강도로 같은 TEQUIP를 재사용하고 있기 때문에(위해), 이와 같이 되어 버린다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT64, 눈가리개
;────────────────────────────────────
;·죠교자가눈가리개를 대게 하려고 한다
;·지문에서는"대게 하려고했다…"되고 있지만 처리상은 ACT 개시시점으로써장착 되고 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:눈가리개는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을 확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·(REACT64 이외에도취소처리가 되는 경우가 있기 때문에(위해))
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중였을 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K64_64
;-------------------------------------------------
;·TFLAG:ACT파성(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_0, 눈가리개장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상에페니스가 있는 경우, 마조끼에 응해발기도상승
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_1, 눈가리개해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT65, 볼개그
;────────────────────────────────────
;·죠교자가볼개그를 끼우게 하려고 한다
;·지문에서는"끼우게 하려고했다…"되고 있지만 처리상은 ACT 개시시점으로써장착 되고 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:볼개그는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을※확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·※REACT64 이외에도취소처리가 되는 경우가 있기 때문에(위해)
;·턴방침가"소프트", "노멀"의 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중였을 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K64_65
;-------------------------------------------------
;·TFLAG:ACT파성(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_0, 볼개그장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상에페니스가 있는 경우, 마조끼에 응해발기도상승
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		SELECTCASE TENSION(TARGET, 7)	;죠교자의초조함
			CASE "높다"
;OriginalString : 「ウルサイのよ。ちょっと黙りなさい」
				PRINTFORMW 「우르사이의 것. 조금 입다무세요」
			CASEELSE
;OriginalString : 「ごめん、ちょっと黙ってて」
				PRINTFORMW 「미안, 조금 입다물고 있어」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_1, 볼개그해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT66, 매도
;────────────────────────────────────
;·도발하도록(듯이)죠교대상의 치태를 소리에 낸다
;·회화계로부터 발생하는 경우도 있다
;·턴방침가"소프트"라면 발생하지 않는다
;·회화계로부터 파생하는 경우도 있다
;-------------------------------------------------
@KOJO_ACT_K64_66
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT66, 매도
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
IF 구상기입체크(0)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교중/성교봉사중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:TARGET:성교중, TEQUIP:TARGET:성교봉사중) > 0
		IF TEQUIP:TARGET:성교중	;성교중
;OriginalString : 
			PRINTFORMW
		ELSE					;성교봉사중
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;자위중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(GETBIT(TEQUIP:TARGET:자위중, 0), GETBIT(TEQUIP:TARGET:자위중, 1)) > 0
		IF GETBIT(TEQUIP:TARGET:자위중, 0)	;죠교대상가자위중
;OriginalString : 
			PRINTFORMW 
		ELSE								;죠교자가자위중
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;Ｃ애무계속중
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:TARGET:죠교대상Ｃ사용, 1, 2)
		IF TEQUIP:TARGET:죠교대상Ｃ사용 == 1	;수음·애무계속중
;OriginalString : 
			PRINTFORMW 
		ELSE									;펠라·쿤닐링구스 계속중
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;특수한 조건(확률 있어)
	;=================================================
	IF 구상기입체크(0)
		IF 0	;추가하기 쉽게 하기 위해서
		ELSEIF !RAND:3 && NOWEX:MASTER:방뇨			;전회ACT로실금
			IF TALENT:MASTER:작은체형
;OriginalString : 「あ、もしかして%K64_CNAME()%には、まだオムツが필요だった？」
				PRINTFORMW 「아, 혹시%K64_CNAME()%에는, 아직 옴트가 필요했다?」
;OriginalString : 「ごめんね。私、ぜんぜん気がつかなかった」
				PRINTFORMW 「미안해요. 나, 전혀 깨닫지 못했다」
			ELSE
;OriginalString : 「いい年してお漏らしするんだ？」
				PRINTFORMW 「좋은 해 해 흘린다?」
;OriginalString : 「恥ずかしくないの？」
				PRINTFORMW 「부끄럽지 않은거야?」
			ENDIF
			RETURN 1
		ELSEIF !RAND:2 && NOWEX:MASTER:사정 == 4	;전회ACT로사정(조루)
			IF TALENT:MASTER:작은체형
;OriginalString : 「ちょっとお子様には刺激が強すぎたかな？」
				PRINTFORMW 「조금 아드님에게는 자극이 너무 강했는지?」
;OriginalString : 「쿠쿡」
				PRINTFORMW 「킥킥」
			ELSE
				CALL HTMLPRINTW("「인내력이 없는 자지의 일을,<b>조루</b>라고 말해」")
;OriginalString : 「…%K64_CNAME()%の事を言ってるのよ？」
				PRINTFORMW 「…%K64_CNAME()%의 일을 말해?」
			ENDIF
			RETURN 1
		ENDIF
		;여기서 새면 조건 없이 흘린다
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT67, 삼각목마
;────────────────────────────────────
;·죠교대상를 목마에 실으려고 한다
;·지문은"실으려고했다…"되고 있지만 처리상은 ACT 개시시점으로써 실려지고 있다
;· 실은쾌Ｃ는 아니고쾌Ｖ가 들어간다(나있는 목마!?)
;·MASTER가처녀라면, 처녀상실가 일어난다(@ACTION_APPLY2로 처리. REACT분류가용서를 빌다의 경우는 회피된다)
;·죠교대상이남자의 경우는 발생하지 않는다
;·턴방침가"소프트", "노멀"때에는 발생하지 않는다
;·TEQUIP(바이브/항문바이브/항문비즈/관장그릇＋플러그/페니스밴드)장착시에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·야외플레이시에는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·이상계로부터 발생하는 경우도 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:삼각목마는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을※확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·※REACT67 이외에도취소처리가 되는 경우가 있기 때문에(위해)
;-------------------------------------------------
@KOJO_ACT_K64_67
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_0, 삼각목마장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생로 분기. 파생내에서는 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 목마에 실으려고했다…
	
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 「후후후…次は『お馬さんごっこ』で遊びましょう♪」
		PRINTFORMW 「후후후…다음은 「마 산호개」로 놉시다♪」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_1, 삼각목마해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;TEQUIP취소 시에는 여기서 주울 수 있는 것이 아닌 것에 주의
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 목마로부터 내려 주었다…
	
	;처음라고는 말해도, 과거에 TEQUIP취소나 해제되지 않는 채죠교종료로 있던 가능성도 있다
	;엄밀하게 하고 싶으면 연구필요인가…
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT68, 관장세트
;────────────────────────────────────
;·관장액의 주입/추가/배출
;·최초의 주입시(ACT파생=0)는 양 1～3. 추가시(ACT파생=2)는 양 1～4. 각각 랜덤으로 처리된다
;·이미 장비 하고 있는 경우, 관장액추가가 된다
;·관장액이 5병 들어가 있는 경우, 관장플러그 해제가 된다
;·관장액의 분량은 TEQUIP:관장그릇＋플러그로 참조할 수 있다
;·TEQUIP(항문바이브/항문비즈/페니스밴드/삼각목마)장착시에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·턴방침가"소프트"라면 발생하지 않는다
;·도구계로부터 파생하는 경우도 있다
;-------------------------------------------------
@KOJO_ACT_K64_68
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제/2=관장액추가)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_0, 관장그릇＋플러그장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기. 파생내에서는 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하며, ")% %CALLNAME:MASTER%의 국혈에
	;관장액을 흘려 넣어, 마개를했다…
	
	;ACT 개시시점으로써 액은 주입되고 있어 TEQUIP:관장그릇＋플러그로 액량을 참조하는 것이 가능
	
	;=================================================
	;처음 「주입」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;사양이 바뀔 가능성을 고려해 ==는 아니고>=로 해둔다
	;=================================================
	;액량 3
	;=================================================
	IF 구상기입체크(0) && TEQUIP:TARGET:관장그릇＋플러그 >= 3
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;액량 2
	;=================================================
	IF 구상기입체크(0) && TEQUIP:TARGET:관장그릇＋플러그 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;액량의 분기를 모두 묻었을 경우는 TEQUIP:관장그릇＋플러그 == 1에 상당
	;개별적으로 나누지 않고 SELECTCASE TEQUIP:관장그릇＋플러그로 정리해 버리는 일도 가능
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_1, 관장그릇＋플러그해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;여기에 파생하는 시점에서 액량은 5가 되어 있다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하면서, ")% %CALLNAME:MASTER%로부터
	;항문마개를 뽑아, 장내의 오물을 항문으로부터 따돌려 산등시켰다…
	
	;=================================================
	;처음 「배출」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_2, 관장액추가
;━━━━━━━━━━━━━━━━━━━━━━━━
;이미 주입 끝난 경우에 이 개소에 파생
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하면서, ")% %CALLNAME:MASTER%의 항문에
	;한층 더관장액을 주입하려고했다…
	
	[SKIPSTART]
	액량으로 나누어 쓴다면 comment out를 해제
	하층의 PRINTFORMW를 삭제해 주세요
	SELECTCASE TEQUIP:TARGET:관장그릇＋플러그	;관장액분량 파생
		CASE 5		;액량 5
;OriginalString : 
			PRINTFORMW 
		CASE 4		;액량 4
;OriginalString : 
			PRINTFORMW 
		CASE 3		;액량 3
;OriginalString : 
			PRINTFORMW 
		CASEELSE	;그 외
;OriginalString : 
			PRINTFORMW 
	ENDSELECT
	[SKIPEND]
	;나누어 쓰지 않는 것이면 여기서
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF
