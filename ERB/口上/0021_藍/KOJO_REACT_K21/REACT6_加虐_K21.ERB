;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	야쿠모란구상 가학계 개별 리액션구상파일
;
;	가학계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;가학계 REACT60～68
;========================================================================
;· 각 REACT내의 나누는 방법으로서 TFLAG:REACT분류(IS_COMGRONAME)나 SELECTCOM(IS_NOWCOMNAME)가 생각됩니다
;·다만, 범위의 넓은 조건으로 주워 버리는 것은 기본적으로 추천하지 않습니다
;·ACT분류:가학계의 경우, 고통계 COM(COM30~COM32)를 중심으로, 회화계, 애무계의 일부가선택지가 됩니다만
;·상황에 따라서는 그 외의 것이선택지에 포함될 가능성도 있습니다
;·리액션(REACT)으로 처리되지 않았던 것은 범용 리액션(COM)으로 보완되므로
;·무리하게 모두 묻는필요는 없습니다
;·필요라고 생각하는 것을 좁혀, COM와의 제휴를 생각해 효율 좋게 구축해야 합니다
;-------------------------------------------------
;※지속효과가 있는 TEQUIP로애무계 반응이선택지가 되는 경우가 있습니다
;-------------------------------------------------
;· 각 REACT내는,&& IS_NOWCOMNAME든지,&& IS_COMGRONAME등의 조건으로 결정하고 쳐 합시다
;·여기에 조건 없음 분기가 있으면, 모두 주워 버려 범용 리액션에 흐르지 않습니다
;·죠교대상의 반응에 관련되지 않고, 같은 리액션 밖에 하지 않게 되어 버립니다
;·요컨데 이것은, 상정당했다시스템의 파괴에 연결됩니다
;-------------------------------------------------
;  가학계의 경우, REACT 마다의 개별의 지문은 존재하지 않고, REACTION_MESSAGE로 COM마다 범용 처리될 뿐입니다
;  REACTION_MESSAGE.ERB를 참조하면서의 작업을 추천합니다
;-------------------------------------------------
;각 REACT에서는 상정되는 반응을 망라되어 있습니다. 필요에 응해 분기를 증감해 사용해 주세요
;COM에 따라서는 REACT파생로 REACT분류가변화 하는 일에 주의
;각 COM의 파생은 기본적으로 REACT파생 1를 조건 결정 우리 소분기로 해, REACT파생 0는 그 외 취급으로 조건 없다고 보고 있습니다
;손간을 걸고 싶지 않은 경우는 그 외만을 써, 파생에 관련되지 않고 해당 개소를 보완할 수가 있습니다
;또 IF 구상기입 체크(0) && TFLAG:REACT파생 < 1으로 하는 일로 파생했다경우는 범용 리액션에 흘린다 따위라고 하는 일도 가능합니다
;파생은 장래 증가할 가능성도 있으므로 != 1등으로 하는 것보다는< 1으로했다 (분)편이 무난하다라고 생각합니다
;-------------------------------------------------

;────────────────────────────────────
;REACT60, 스펭킹·개별 리액션
;────────────────────────────────────
;·ACT파생없음
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:더 하드하게 해/용서를 빌다
;애무계 반응
;	COM:겁없이 웃는다
;고통계 반응
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K21_60
;-------------------------------------------------
;·고유의 지문 없음. 죠교대상의 반응에 응해 REACTION_MESSAGE로 범용 처리
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;「이 철면피의 돼지째!」(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다(REACT분류:용서를 빌다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;파생 1의 경우는 REACT분류:없음
	;=================================================
	;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 고통을 참는다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「고통에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1. 파생 2의 경우는 REACT분류:소극적으로 한다취급
;REACT분류가날뛴다였을 경우는성교는취소가 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;날뛰고 실패(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
		;밧줄구속에 의한 날뛰고 실패
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;제지에 의한 날뛰고 실패
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;손간을 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT60, 스펭킹

;────────────────────────────────────
;REACT61, 채찍·개별 리액션
;────────────────────────────────────
;·ACT파생없음
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를 빌다
;애무계 반응
;	COM:겁없이 웃는다
;고통계 반응
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K21_61
;-------------------------------------------------
;·고유의 지문 없음. 죠교대상의 반응에 응해 REACTION_MESSAGE로 범용 처리
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다(REACT분류:용서를 빌다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;파생 1의 경우는 REACT분류:없음
	;=================================================
	;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 고통을 참는다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「고통에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1. 파생 2의 경우는 REACT분류:소극적으로 한다취급
;REACT분류가날뛴다였을 경우는성교는취소가 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;날뛰고 실패(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
		;밧줄구속에 의한 날뛰고 실패
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;제지에 의한 날뛰고 실패
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;손간을 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT61, 채찍·개별 리액션

;────────────────────────────────────
;REACT62, 바늘·개별 리액션
;────────────────────────────────────
;·ACT파생없음
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를 빌다
;애무계 반응
;	COM:겁없이 웃는다
;고통계 반응
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K21_62
;-------------------------------------------------
;·고유의 지문 없음. 죠교대상의 반응에 응해 REACTION_MESSAGE로 범용 처리
;·페니스, 혹은클리토리스, 및 유두에바늘를 찌른다
;·회당했다개소로부터는 출혈해, 혈액 더러움이 추가된다
;-------------------------------------------------
;  더러움의 참조에는@GET_STAIN의 사용을 추천합니다!
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상에페니스 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
;페니스에바늘를 찌른다. ACT파생에서는 참조 할 수 없다
;-------------------------------------------------
IF 구상기입체크(0) && PENIS(MASTER)
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
		;파생 1의 경우는 REACT분류:없음
		;-------------------------------------------------
		;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포에 의한 비명
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「고통에 의한 비명」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;파생 1. 파생 2의 경우는 REACT분류:소극적으로 한다취급
	;REACT분류가날뛴다였을 경우는성교는취소가 된다
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;날뛰고 실패(REACT분류:소극적으로 한다)
		;-------------------------------------------------
		IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
			;밧줄구속에 의한 날뛰고 실패
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;제지에 의한 날뛰고 실패
			ELSE
;OriginalString : 
				PRINTFORMW
			ENDIF
			RETURN 1
			[SKIPEND]
			
			;손간을 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF

;죠교대상에페니스 있는 개소를 참고에필요인 분기를 추가해 사용해 주세요
;조건 없음의 기술은 그만둡시다
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상에페니스없음
;━━━━━━━━━━━━━━━━━━━━━━━━
;클리토리스에바늘를 찌른다. ACT파생에서는 참조 할 수 없다
;-------------------------------------------------
IF 구상기입체크(0) && !PENIS(MASTER)
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF
;REACT62, 바늘

;────────────────────────────────────
;REACT63, 밧줄·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=구속/1=해제)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:침묵한다/빈정거린다/더 하드하게 해/용서를 빌다
;애무계 반응
;	COM:겁없이 웃는다
;고통계 반응
;	COM:고통을 참는다/비명지른다/날뛴다/무서워한다
;-------------------------------------------------
@KOJO_REACT_K21_63
;-------------------------------------------------
;·고유의 지문 없음. 죠교대상의 반응에 응해 REACTION_MESSAGE로 범용 처리
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;애원를 들어주고구속를 풀었다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가용서를 빌다인 경우는 반드시취소가 된다
;취소처리는 TEQUIP 참조인 것으로, 다른 ACT에서도 REACT분류가용서를 빌다라면 발생 할 수 있다
;현상, 해당 REACT 이외로 이 거동을 확실히 주울 수 있는 방법은 없다
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("용서를 빌다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 구속시
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
		;파생 1의 경우는 REACT분류:없음
		;-------------------------------------------------
		;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포에 의한 비명
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「고통에 의한 비명」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;파생 1, 밧줄구속에 의한 날뛰고 실패는 ACT:밧줄때에는 성립하지 않는다
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
		;제지에 의한 날뛰고 실패라고 하는 취급
		;TEQUIP:밧줄에의 대입은 벌써 행해져 있거나 합니다만, 묘사적으로는 REACT 종료시점으로써 묶기가 성립한다든가
		;그런 느낌에 파악해 받는 것이 좋을지도 모릅니다
		;-------------------------------------------------
		;날뛰고 실패(REACT분류:소극적으로 한다)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 2
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;날뛰고 성공(TEQUIP:밧줄는 해제된다)
		;-------------------------------------------------
		IF 구상기입체크(0) && !TFLAG:REACT파생
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;파생하지 않았던 경우는 REACT분류:용서를 빌다취급(취소처리에 해당한다)인 것으로 고려하지 않는다
	;=================================================
	;COM33, 무서워한다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF

;파생 0, 구속시를 참고에필요인 분기를 추가해 사용해 주세요
;조건 없음의 기술은 그만둡시다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 해제시
;━━━━━━━━━━━━━━━━━━━━━━━━
;·COM:도구를 제거한다는 아니고, 어디까지나죠교자에 의한 해제인 일에 주의
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF
;REACT63, 밧줄

;────────────────────────────────────
;REACT64, 눈가리개·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:침묵한다/빈정거린다/더 하드하게 해/용서를 빌다
;애무계 반응
;	COM:겁없이 웃는다
;고통계 반응
;	COM:고통을 참는다/비명지른다/날뛴다/무서워한다
;그 외 반응
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_REACT_K21_64
;-------------------------------------------------
;·고유의 지문 없음. 죠교대상의 반응에 응해 REACTION_MESSAGE로 범용 처리
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;애원를 들어주고구속를 풀었다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가용서를 빌다인 경우는 반드시취소가 된다
;취소처리는 TEQUIP 참조인 것으로, 다른 ACT에서도 REACT분류가용서를 빌다라면 발생 할 수 있다
;현상, 해당 REACT 이외로 이 거동을 확실히 주울 수 있는 방법은 없다
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("용서를 빌다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 눈가리개장착시
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포에 의한 비명
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「고통에 의한 비명」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;파생 1. 파생 2의 경우는 REACT분류:소극적으로 한다취급
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;날뛰고 실패(REACT분류:소극적으로 한다)
		;-------------------------------------------------
		IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
			;밧줄구속에 의한 날뛰고 실패
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;제지에 의한 날뛰고 실패
			ELSE
;OriginalString : 
				PRINTFORMW
			ENDIF
			RETURN 1
			[SKIPEND]
			
			;손간을 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;파생하지 않았던 경우는 REACT분류:용서를 빌다취급(취소처리에 해당한다)인 것으로 고려하지 않는다
	;=================================================
	;COM33, 무서워한다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF

;파생 0, 장착시를 참고에필요인 분기를 추가해 사용해 주세요
;조건 없음의 기술은 그만둡시다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 해제시
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF
;REACT64, 눈가리개

;────────────────────────────────────
;REACT65, 볼개그·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:침묵한다
;애무계 반응
;	COM:겁없이 웃는다
;고통계 반응
;	COM:고통을 참는다/비명지른다/날뛴다/무서워한다
;그 외 반응
;	COM:도구를 제거한다
;※침묵한다 이외의회화계 및 말을 수반하는 계통의 반응은 불가능이 된다
;-------------------------------------------------
@KOJO_REACT_K21_65
;-------------------------------------------------
;·고유의 지문 없음. 죠교대상의 반응에 응해 REACTION_MESSAGE로 범용 처리
;-------------------------------------------------

;※볼개그의 경우 COM:무서워한다의 경우에게만, 이 개소에 해당한다고 생각된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;애원를 들어주고구속를 풀었다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가용서를 빌다인 경우는 반드시취소가 된다
;취소처리는 TEQUIP 참조인 것으로, 다른 ACT에서도 REACT분류가용서를 빌다라면 발생 할 수 있다
;현상, 해당 REACT 이외로 이 거동을 확실히 주울 수 있는 방법은 없다
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("용서를 빌다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 볼개그장착시
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포에 의한 비명
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「고통에 의한 비명」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;파생 1. 파생 2의 경우는 REACT분류:소극적으로 한다취급
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;날뛰고 실패(REACT분류:소극적으로 한다)
		;-------------------------------------------------
		IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
			;밧줄구속에 의한 날뛰고 실패
			IF TFLAG:REACT파생 == 1
;OriginalString : 
				PRINTFORMW 
			;제지에 의한 날뛰고 실패
			ELSE
;OriginalString : 
				PRINTFORMW
			ENDIF
			RETURN 1
			[SKIPEND]
			
			;손간을 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;파생하지 않았던 경우는 REACT분류:용서를 빌다취급(취소처리에 해당한다)인 것으로 고려하지 않는다
	;=================================================
	;COM33, 무서워한다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF

;파생 0, 장착시를 참고에필요인 분기를 추가해 사용해 주세요
;조건 없음의 기술은 그만둡시다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 해제시
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF
;REACT65, 볼개그

;────────────────────────────────────
;REACT66, 매도·개별 리액션
;────────────────────────────────────
;·ACT파생없음
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:침묵한다/마음 약하게 응답한다/무뚝뚝하게 응답한다/상냥하게 응답한다/강하게 응답한다/빈정거린다/기분좋게 해
;애무계 반응
;	COM:겁없이 웃는다
;고통계 반응
;	COM:비명지른다/무서워한다
;그 외 반응
;	COM:자위를 시작한다
;-------------------------------------------------
@KOJO_REACT_K21_66
;-------------------------------------------------
;·고유의 지문 없음. 죠교대상의 반응에 응해 REACTION_MESSAGE로 범용 처리
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다(REACT분류:없음)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「何か言ったらどうだ？」
	PRINTFORMW 「뭔가 말하면 어때?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;=================================================
	;REACT파생 1, 애원 한다(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「심약구응답」(REACT분류:소극적으로 한다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「ふふっ、可愛いなぁ、お前は」
		PRINTFORMW 「후후, 사랑스럽다, 너는」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다(REACT분류:없음)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
;OriginalString : 「つまらない反応だな」
	PRINTFORMW 「시시한 반응이다」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성 있게 응답한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("상냥하게 응답한다")
;OriginalString : 「こやつめハハハ」
	PRINTFORMW 「이자식째하 하 하」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다(REACT분류:없음)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("강하게 응답한다")
;OriginalString : 「ははは、なかなかどうしていい度가슴じゃないか」
	PRINTFORMW 「하하하, 꽤 어째서 좋은 담력이 아닌가」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 「ほう……」
	PRINTFORMW 「편……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;「이 철면피의 돼지째!」(징계·포인트가산)
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
;OriginalString : 「……」
		PRINTFORMW 「……」
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「えっ」
		PRINTFORMW 「어」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 「クックックッ……黒マテリア」
	PRINTFORMW 「쿠쿡쿡……흑마떼차 리어」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("비명지른다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「고통에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「こ、こっちが驚くじゃないか！」
		PRINTFORMW 「개, 여기가 놀라잖아!」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다(REACT분류:용서를 빌다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「いや……怯えなくても……」
		PRINTFORMW 「아니……무서워하지 않아도……」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("자위를 시작한다")
	;=================================================
	;승손에 자위 하지마(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「승손에 자위 하고 있지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「おい、誰がオナニーしろと言った？」
		PRINTFORMW 「두어 누가 자위하라고 했다?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;REACT67, 삼각목마·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:침묵한다/빈정거린다/더 하드하게 해/용서를 빌다
;애무계 반응
;	COM:겁없이 웃는다
;고통계 반응
;	COM:고통을 참는다/비명지른다
;-------------------------------------------------
@KOJO_REACT_K21_67
;-------------------------------------------------
;·고유의 지문 없음. 죠교대상의 반응에 응해 REACTION_MESSAGE로 범용 처리
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;애원를 들어주고 목마로부터 강했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가용서를 빌다인 경우는 반드시취소가 된다
;취소처리는 TEQUIP 참조인 것으로, 다른 ACT에서도 REACT분류가용서를 빌다라면 발생 할 수 있다
;현상, 해당 REACT 이외로 이 거동을 확실히 주울 수 있는 방법은 없다
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("용서를 빌다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 개시시
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;COM0, 침묵한다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM5, 빈정거린다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM6, 더 하드하게 해(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("더 하드하게 해")
		;-------------------------------------------------
		;「이 철면피의 돼지째!」(징계·포인트가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;파생 0의 경우는 REACT분류:용서를 빌다취급(취소처리에 해당한다)인 것으로 고려하지 않는다
	;=================================================
	;COM7, 용서를 빌다·파생 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다") && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포에 의한 비명
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「고통에 의한 비명」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF

;파생 0, 개시시를 참고에필요인 분기를 추가해 사용해 주세요
;조건 없음의 기술은 그만둡시다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 종료시
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF
;REACT67, 삼각목마

;────────────────────────────────────
;REACT68, 관장세트·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생（=0 주입/=1 배출/=2 추가）
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를 빌다
;애무계 반응
;	COM:겁없이 웃는다
;고통계 반응
;	COM:고통을 참는다/비명지른다/무서워한다
;-------------------------------------------------
@KOJO_REACT_K21_68
;-------------------------------------------------
;·고유의 지문 없음. 죠교대상의 반응에 응해 REACTION_MESSAGE로 범용 처리
;-------------------------------------------------
;  우선 주입, 추가는 정리해 섬지금했다가
;  손간을 싫어하지 않는 것이면, TFLAG:ACT파생로 나누어도 좋다고는 생각합니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;관장액주입＆추가
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && GROUPMATCH(TFLAG:ACT파생, 0, 2)
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
		;파생 1의 경우는 REACT분류:없음
		;-------------------------------------------------
		;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM30, 고통을 참는다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM31, 비명지른다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
		;-------------------------------------------------
		;공포에 의한 비명
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「고통에 의한 비명」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM33, 무서워한다(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
		;-------------------------------------------------
		;파생:두려움에 대한초조함
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM51, 도구를 제거한다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("도구를 제거한다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF

;관장액주입＆추가를 참고에필요인 분기를 추가해 사용해 주세요
;조건 없음의 기술은 그만둡시다
;━━━━━━━━━━━━━━━━━━━━━━━━
;관장액배출
;━━━━━━━━━━━━━━━━━━━━━━━━
;·어디까지나죠교자에 의한 해제인 일에 주의
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF
;REACT68, 관장세트
