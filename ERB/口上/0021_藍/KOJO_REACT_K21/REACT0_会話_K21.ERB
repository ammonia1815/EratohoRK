;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	야쿠모란구상 회화系個別リアクション구상ファイル
;
;	회화系リアクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화系 REACT0～9
;========================================================================
;・1231版よりTFLAG:REACT부하が導入され、선택했다COMが自然な행동かどうかが参照できるように
;・0127版以降のリアクション後の地の文は、基本的にREACT부하で分岐するようになっています
;・TFLAG:REACT부하(0=自然な행동/1=無理のない행동/2=無理を했다행동)
;-------------------------------------------------
;　회화系はあまり汎用処理には向かないので、汎用リアクション(COM)に流さず個別リアクション(REACT)
;　で完結させてしまうのが望ましいと考えます
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 지금 기분을 묻다・個別リアクション
;────────────────────────────────────
;・ACT파생への代入は죠교대상のリアクションに応じて@ACTION_APPLY2_0にて行われる
;-------------------------------------------------
@KOJO_REACT_K21_0
;-------------------------------------------------
;・TFLAG:ACT파생(0=歯切れ悪く答えた/1=勇気を出して強く当たった/2=皮肉/3=はっきりと今の気持ちを告げた
;・4=まあまあ悪くはない/5=気持ち良いこと했다い)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생,0 歯切れ悪く質問に答えた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が소극적으로 한다で、リアクションが빈정거린다以外の場合
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 0
	;自然な행동
	;=================================================
	;REACT부하0, 質問にはっきり答えることができなかった
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 歯切れ悪く質問に答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 出掛かった言葉を飲み込み答えを濁했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「ん、聞こえないな」
		PRINTFORMW 「응, 들리지 않는데」
		;地の文扱いならPRINTD系で@/L(2015/01/14)
;OriginalString : すると란は당신の입元に耳を傾けた
		PRINTFORMDW 그러자(면) 쪽은 당신의 입가에 귀를 기울였다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생,1 勇気を出して強く当たった
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが강하게 응답한다の場合
;REACT분류は"거부"にセットされる
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
	;自然な행동
	;=================================================
	;REACT부하0, 厳しい言葉を浴びせた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 勇気を出して強く当たった
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 目を逸らし입では強気に振る마이った
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「おぉ、強気だな」
		PRINTFORMW 「, 강하다」
;OriginalString : 「その強気もいつまで続くかな？　ははっ」 
		PRINTFORMW 「그 강경세도 언제까지 계속될까？ (은)는 하」 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생,2 皮肉「今の気分は、と聞かれても」
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが빈정거린다の場合
;REACT분류は"거부"にセットされる
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 2
	;自然な행동
	;=================================================
	;REACT부하0, 余裕の笑みで肩をすくめ「今の気分は、と聞かれても」と答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 少し考えてから「今の気分は、と聞かれても」と答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, かろうじて皮肉交じりに答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「ほう、なかなか達者な입をしてるな」
		PRINTFORMW 「편, 꽤 능숙한 입을 하고 있는」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생,3 はっきりと今の気持ちを告げた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が적극적으로 한다の場合
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 3
	;自然な행동
	;=================================================
	;REACT부하0, 一緒に居られることが嬉しいと솔직に입に했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, はっきりと今の気持ちを告げた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 機嫌を損ねないよう、取り繕って答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「ふふっ、そうか」
		PRINTFORMW 「후후, 그런가」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생,4 まあまあ悪くはない
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が태연하게 한다の場合
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 4
	;自然な행동
	;=================================================
	;REACT부하0, そう悪く思ってはいないと答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 「まあまあ悪くはない」と言った
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
		;実は적극			視線を外し、悪くはないよと呟いた
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;実は嫌				精一杯の強がりで、まあまあ悪くないと答えた
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생,5 気持ち良いこと했다い
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が受け入れるの場合
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 5
	;自然な행동
	;=================================================
	;REACT부하0, もっと気持ち良くなりたいと言った
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 気持ち良いこと했다い気分だと答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 媚びるように敢えて淫らな行為をねだった
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;以下、上記のCOM、REACT분류に属さないCOM毎の分岐
;-------------------------------------------------
[SKIPSTART]
통상の場合リアクションの地の文は@TRAIN_MESSAGE2と@REACTION_MESSAGEで処理されますが
以下の部分は@TRAIN_MESSAGE2の条件から漏れる為、@REACTION_MESSAGEでのみ処理されます
COM分岐に於いても当然TFLAG:REACT파생は機能しますし、@REACTION_MESSAGE内でもREACT부하毎に書き分けられています
SELECTCASE TFLAG:REACT부하
	;自然な행동
	CASE 0
;OriginalString : 
		PRINTFORMW 
	;無理のない행동
	CASE 1
;OriginalString : 
		PRINTFORMW 
	;無理を했다행동
	CASE 2
		;実はまんざらでもない
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;実は嫌
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
ENDSELECT
上記のような分岐を設ける事によって、本体側の변화に対応する事は可能ですが
テンプレに於いてデフォルトで設けるには손間がかかり過ぎると考える故に、基本的にREACT内のCOM分岐には枠を設けない방침です
汎用的な内容にしてもよし、また部分的に汎用リアクション(COM)に流しても良いと思います
まあREACT부하以外にも拡張する方法はいくらでもあったりするので、その辺りの判断は各구상作者に委ねたいと思います
3BM/4MBでも書くから兎に角考えられる要素を全部入れてくれ！的な要望がもしあれば、その時はまた考えたいなと
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;지금 기분을 묻다でのCOM침묵한다はTFLAG:REACT파생 == 1(質問に答えない)扱いとなり징계・포인트が加算される
;-------------------------------------------------
IF 구상記入チェック(100) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「何か言ったらどうだ？」
	PRINTFORMW 「무엇인가 말하면 어때？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계・포인트加算 派生:「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
;OriginalString : 「そう言われてもなぁ…」
		PRINTFORMW 「그런 말을 들어도…」
;OriginalString : 「まぁ、せいぜい自分の可愛い顔を恨んでおくことだな」
		PRINTFORMW 「아무튼, 겨우 자신의 사랑스러운 얼굴을 원망해 두는 것이다」
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「仕方ないな   なんて、言うと思ったか？」
		PRINTFORMW 「어쩔 수 없는데   정말, 말한다고 생각했는지？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「お、おい、大丈夫か？」
		PRINTFORMW 「, 두어 괜찮은가？」
		RETURN 1
	ENDIF
ENDIF
;REACT0, 지금 기분을 묻다

;────────────────────────────────────
;REACT1, 자신에게의 마음을 묻다・個別リアクション
;────────────────────────────────────
;・ACT파생への代入は죠교대상のリアクションに応じて@ACTION_APPLY2_1にて行われる
;-------------------------------------------------
@KOJO_REACT_K21_1
;-------------------------------------------------
;・TFLAG:ACT파생(0=嫌いじゃない…かも知れない…/1=そんなことあるわけない！/2=困るねぇ…好きじゃないといったら怒る？
;・3=もう酷いことしなければ、もっと好きになれるかもしれない/4=はっきりと好きと告げた?5=もっとすごいことしてくれば、もっと好きになれるかも)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「嫌いじゃない…かも知れない…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が소극적으로 한다の場合
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 0
	;自然な행동
	;=================================================
	;REACT부하0, どうしても嫌いとは言えなかった
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 「嫌いじゃない…かも知れない…」と答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 本心を隠すように目を伏せ、「嫌いじゃない…かも知れない…」と答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「そ、そうか？」
		PRINTFORMW 「, 그런가？」
;OriginalString : 「…んー」
		PRINTFORMW 「…응―」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「そんなことあるわけない！」
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが강하게 응답한다の場合
;REACT분류は"거부"にセットされる
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
	;自然な행동
	;=================================================
	;REACT부하0, 「好きになれるわけがない！」と強く答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 「そんなことあるわけない！」と言った
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 感情を否定するように首を振り「嫌いだ！」と答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「そうか…」
		PRINTFORMW 「그런가…」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 皮肉「困るねぇ…好きじゃないといったら怒る？」
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが빈정거린다の場合
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 2
	;自然な행동
	;=================================================
	;REACT부하0, 「好きになれるわけがない！」と強く答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 「そんなことあるわけない！」と言った
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 感情を否定するように首を振り「嫌いだ！」と答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「相変わらず達者な입をしているな」
		PRINTFORMW 「변함 없이 능숙한 입을 하고 있데」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「もう酷いことしなければ、もっと好きになれるかもしれない」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が적극적으로 한다で、죠교대상호의が少ないか、REACT분류が태연하게 한다
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 3
	;自然な행동
	;=================================================
	;REACT부하0, 「もっと好きになりたいから、酷いことはしないで」と答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 「もう酷いことしなければ、好きになれるかもしれない」と答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 機嫌を伺うように視線を向けた後、「酷いことばかりする人は嫌い」と答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「そうか…んー」
		PRINTFORMW 「그런가…응―」
;OriginalString : 「죠교と人間関係の両立とは難しいものだな」
		PRINTFORMW 「조교와 인간 관계의 양립과는 어려운 것이다」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, はっきり好きだと告げた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が적극적으로 한다で、죠교대상호의가1000이상
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 4
	;負荷2は弾いた方が何かと都合がよさそうなのでとりあえず
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTREACT(4) && TFLAG:REACT부하 < 2
;OriginalString : 「えっ、こ、こんな죠교してるのにか？」
		PRINTFORMW 「어, 개, 이런 조교해 인가？」
;OriginalString : 「…ははっ、そうか。じゃあ私はもっと頑張らないといけないな」
		PRINTFORMW 「…(은)는 하, 그런가. 자 나는 좀 더 노력하지 않으면 안 되는데」
;OriginalString : 「よしっ、張り切っていくぞっ」
		PRINTFORMW 「좋아 , 의욕에 넘쳐 간다」
		RETURN 1
	ENDIF
	
	;自然な행동
	;=================================================
	;REACT부하0, 愛想よく笑って大好きだと答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, はっきりと好きと告げた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 作り笑いを浮かべて、好きだよと答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「そうか…照れるな」
		PRINTFORMW 「그런가…수줍지 말아라」
;OriginalString : 「可愛いな、お前」
		PRINTFORMW 「사랑스럽다, 너」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「もっとすごいことしてくれれば、もっと好きになれるかも」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が受け入れるの場合
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 5
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTREACT(5)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;自然な행동
	;=================================================
	;REACT부하0, 愛想よく笑って大好きだと答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, はっきりと好きと告げた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 作り笑いを浮かべて、好きだよと答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「ほう…言ったな」
		PRINTFORMW 「편…말했군」
;OriginalString : 「後悔するなよ」
		PRINTFORMW 「후회하지 마」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;以下、上記のCOM、REACT분류に属さないCOM毎の分岐
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자신에게의 마음을 묻다の場合はTFLAG:REACT파생 == 1(質問に答えない)扱いとなり징계・포인트が加算される
;-------------------------------------------------
IF 구상記入チェック(100) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「…ふんっ、私が焦らされるとはな」
	PRINTFORMW 「…응, 내가 초조하게 해진다고는」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계・포인트加算 派生:「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「なんだ、무서워한다こともないじゃないか」
		PRINTFORMW 「뭐야, 무서워할 것도 없지 않은가」
		RETURN 1
	ENDIF
ENDIF
;REACT1, 자신에게의 마음을 묻다

;────────────────────────────────────
;REACT2, 성에 관한 이야기를 하다・個別リアクション
;────────────────────────────────────
;・地の文は@REACTION_MESSAGEで汎用処理される分のみ
;-------------------------------------------------
@KOJO_REACT_K21_2
;-------------------------------------------------
;　派生する可能性のあるCOMはとりあえずREACT파생分岐を設けていますが
;　&& TFLAG:REACT파생 < 1などとして汎用リアクションで派生を拾う손もあります
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめてと言った
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계・포인트加算 派生:「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;징계・포인트加算 「この恥知らずの豚め！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("부끄러워한다")
	;TEQUIPによって快SOURCEが入っている場合に派生
	;=================================================
	;REACT파생1, 쾌락に부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생0, 状況に부끄러워한다
	;=================================================
	IF 구상記入チェック(50) && TFLAG:REACT파생 == 0
;OriginalString : 「初々しいやつだな！」
		PRINTFORMW 「신선한 녀석이다！」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("자위를 시작한다")
	;=================================================
	;REACT파생1, 「勝손にオナニーするな」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생0, 「勝손にオナニーしてるけどいいか」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT2, 성에 관한 이야기를 하다

;────────────────────────────────────
;REACT3, 상냥하게 위로한다・個別リアクション
;────────────────────────────────────
;・地の文は@REACTION_MESSAGEで汎用処理される分のみ
;-------------------------------------------------
@KOJO_REACT_K21_3
;-------------------------------------------------
;　派生する可能性のあるCOMにはREACT파생分岐を設けていますが
;　&& TFLAG:REACT파생 < 1などとして汎用リアクションで派生を拾う손もあります
;-------------------------------------------------
;　とりあえず회화系COMを全て網羅してありますが
;　10番台の애무系COM、50番台以降のその他COM等も状況によっては선택可能になります
;　필요に応じて分岐を増減して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめて
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 愛想良く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("상냥하게 응답한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("강하게 응답한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생1, 「この恥知らずの豚め！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생0, 「ふーん」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생1, 「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;REACT파생1, 「この恥知らずの豚め！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT3, 상냥하게 위로한다

;────────────────────────────────────
;REACT4, 엄하게 위협한다・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理無し。SOURCE_MESSAGEでの汎用処理のみ
;-------------------------------------------------
@KOJO_REACT_K21_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめて
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTREACT(3)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문プラグ/삼각목마)장착中等の場合は고통のSOURCEが入るので
	;可能性としては考えられますが、この箇所で손間を掛ける필요性としては微妙なところ
	;=================================================
	;REACT파생0, 고통による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 공포による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT4, 엄하게 위협한다

;────────────────────────────────────
;REACT5, 의상 변경을 요구한다・個別リアクション
;────────────────────────────────────
;・죠교대상のリアクションによって一枚脱ぐ、二枚脱ぐ、脱がないに分かれる
;・REACT분류が"소극적으로 한다", "受け入れる"の場合は一枚脱ぐ
;・REACT분류が"적극적으로 한다", "태연하게 한다"の場合は二枚脱ぐ
;・その他の場合は脱がない(PALAM치정/반항の数値によって、恥ずかしくて脱がない、嫌がって脱がないに分かれる)
;-------------------------------------------------
@KOJO_REACT_K21_5
;-------------------------------------------------
;・TFLAG:ACT파생(0=服を脱ぐように命令/1=服を着るように命令)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 服を脱ぐように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:ACT파생 == 0
	;=================================================
	;一枚脱いだ(消極的反応)
	;=================================================
	IF 구상記入チェック(100) && IS_COMGRONAME("소극적으로 한다/受け入れる")
		;-------------------------------------------------
		;最後の一枚を脱いで전라되었다
		;-------------------------------------------------
		IF 구상記入チェック(100) && (TEQUIP:MASTER:악세서리 + TEQUIP:MASTER:신발안 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
			;REACT분류受け入れる			전라になり、期待と興奮で肌が火照るのを感じた
			IF IS_COMGRONAME("受け入れる")
				IF PENIS(MASTER)
;OriginalString : 「随分とまぁ……盛りが良いな」
					PRINTFORMW 「상당히 아무튼……번화가가 좋은데」
				ELSE
;OriginalString : 「『주인님に鼓動が聞こえちゃうぅ』…なーんて思ってるんじゃないよな」
					PRINTFORMW 「 「남편님에게 고동이 들려버리는」…-응이라고 생각하고 있는  것이 아니야」
				ENDIF
			;REACT분류소극적으로 한다			仕方なく最後の一枚を脱ぎ、전라되었다
			ELSE
;OriginalString : 「あーらら、すっかり丸裸になっちゃって」
				PRINTFORMW 「어머들 , 완전히 발가숭이가 되어 버리며」
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;緩やかな動きで服を一枚脱ぎ捨てた
		;-------------------------------------------------
		IF 구상記入チェック(100) && IS_COMGRONAME("受け入れる")
;OriginalString : 「服一枚脱ぐ仕草もいやらしくなったものだ」
			PRINTFORMW 「옷한 장 벗는 행동도 불쾌해진 것이다」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;仕方なく服を一枚脱ぎ捨てた
		;-------------------------------------------------
		IF 구상記入チェック(100) && IS_COMGRONAME("소극적으로 한다")
;OriginalString : 「そんなに自分の体が大事かね？」
			PRINTFORMW 「그렇게 자신의 몸이 소중한가？」
			RETURN 1
		ENDIF
		
		;上層の分岐を全て埋めた場合は不要な箇所
		;逆に埋めない場合はこの箇所が補完の役目を果たします
		;-------------------------------------------------
		;기타・조건없음
		;-------------------------------------------------
		IF 구상記入チェック(0)
;OriginalString : 「む、一枚だけか…」
			PRINTFORMW 「, 한 장만인가…」
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;二枚脱いだ(적극的反応)
	;=================================================
	IF 구상記入チェック(100) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;-------------------------------------------------
		;전라되었다
		;-------------------------------------------------
		IF 구상記入チェック(0) && (TEQUIP:MASTER:악세서리 + TEQUIP:MASTER:신발안 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;적극적으로脱ぎ捨てた
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_COMGRONAME("적극적으로 한다")
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;손際よく脱ぎ捨てた
		;-------------------------------------------------
		IF 구상記入チェック(0) && IS_COMGRONAME("태연하게 한다")
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;上層の分岐を全て埋めた場合は不要な箇所
		;逆に埋めない場合はこの箇所が補完の役目を果たします
		;-------------------------------------------------
		;기타・조건없음
		;-------------------------------------------------
		IF 구상記入チェック(100)
;OriginalString : 「よしよし、言うことさえ聞けばいいんだ」
			PRINTFORMW 「좋아 좋아, 말하는 것 조차 (들)물으면 괜찮다」
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;脱がなかった
	;=================================================
	IF 구상記入チェック(100) && IS_COMGRONAME("거부")
		;恥ずかしくて脱げなかった
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
;OriginalString : 「恥ずかしいのか？」
			PRINTFORMW 「부끄러운 것인지？」
;OriginalString : 「可愛いなぁ、まったく」
			PRINTFORMW 「사랑스럽다, 완전히」
		;嫌がって脱がなかった
		ELSE
;OriginalString : 「ん？　どうして脱がない？」
			PRINTFORMW 「응？ 어째서 벗지 않는다？」
		ENDIF
		RETURN 1
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 服を着るように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
	;=================================================
	;一枚着た(消極的反応)
	;=================================================
	IF 구상記入チェック(100) && IS_COMGRONAME("소극적으로 한다/受け入れる")
;OriginalString : 「ちゃちゃっと着ちゃってくれ」
		PRINTFORMW 「채팅 입어 버려서 주고」
		RETURN 1
	ENDIF
	
	;=================================================
	;二枚着た(적극的反応)
	;=================================================
	IF 구상記入チェック(100) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
;OriginalString : 「自分で脱げと言っておいてなんだがな」
		PRINTFORMW 「스스로 벗겨지고라고 말해 두어지만」
		RETURN 1
	ENDIF
	
	;地の文では"嫌がって"という扱い
	;=================================================
	;着なかった
	;=================================================
	IF 구상記入チェック(100) && IS_COMGRONAME("거부")
;OriginalString : 「寒くないのか？」
		PRINTFORMW 「춥지 않은 것인지？」
		RETURN 1
	ENDIF
ENDIF
;REACT5, 의상 변경을 요구한다

;────────────────────────────────────
;REACT6, 합의를 요구한다・個別リアクション
;────────────────────────────────────
;・죠교대상のリアクションに応じてREACT분류毎に派生する
;-------------------------------------------------
@KOJO_REACT_K21_6
;-------------------------------------------------
;손間を厭わないのであれば、TALENT:MASTER:남자等で内部で分けるのもアリかもしれません
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 小さい声で了承する
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が소극적으로 한다の場合
;-------------------------------------------------
IF 구상記入チェック(0) && IS_COMGRONAME("소극적으로 한다")
	;自然な행동
	;=================================================
	;REACT부하0, 覚悟を決めたように、小さい声で了承했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하0, 小さい声で了承했다	
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
		;実は적극			恥ずかしさに消え入りそうな声で了承했다
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;実は嫌				悔しさに顔を歪めながらも了承했다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 好きにすればいいと答えた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が태연하게 한다の場合
;-------------------------------------------------
IF 구상記入チェック(0) && IS_COMGRONAME("태연하게 한다")
	;自然な행동
	;=================================================
	;REACT부하0, 別に嫌じゃないと言った
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 好きにすればいいと答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
		;実は적극			했다いならいいと言った
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;実は嫌				勝손にすればいいと答えた
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 意を決して捧げてもいいと告げる
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が적극적으로 한다の場合
;-------------------------------------------------
IF 구상記入チェック(0) && IS_COMGRONAME("적극적으로 한다")
	;自然な행동
	;=================================================
	;REACT부하0, 初めてをあげられて嬉しいと答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 意を決して捧げてもいいと告げた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, もう守れないならばせめて優しくされるようにと同意했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게 해くれるんだったら初めてをあげてもいい
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류が受け入れるの場合
;-------------------------------------------------
IF 구상記入チェック(0) && IS_COMGRONAME("受け入れる")
	;自然な행동
	;=================================================
	;REACT부하0, 初めてを捧げるから기분좋게 해欲しいと言った
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない행동
	;=================================================
	;REACT부하1, 기분좋게 해くれるんだったら初めてをあげてもいいと答えた
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理を했다행동
	;=================================================
	;REACT부하2, 初めてを捧げろと言うなら、辛さを忘れられるくらい기분좋게 해と言った
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT부하 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류がその他の場合(その他のREACT분류の場合、@ACTION_APPLY2_6にて"거부"にセットされる)
;-------------------------------------------------
IF 구상記入チェック(0) && IS_COMGRONAME("거부")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTREACT(4)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT6, 합의를 요구한다

;────────────────────────────────────
;REACT7, 징계한다고 위협한다・個別リアクション
;────────────────────────────────────
;・宣言しても징계・が発生するとは限らない
;-------------------------------------------------
@KOJO_REACT_K21_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「なんだ、臆했다のか？」
	PRINTFORMW 「뭐야, 겁먹었는가？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM"마음 약하게 응답한다"は一定条件でREACT분류"용서를 빌다"に派生する
;-------------------------------------------------
IF 구상記入チェック(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめてと言った
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
;OriginalString : 「ふん、その程度の訴えが私に届くと思うなよ」
		PRINTFORMW 「응, 그 정도의 호소가 나에게 닿는다고 생각하지 마」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 0
;OriginalString : 「なんだ、つまらない反応だな」
		PRINTFORMW 「뭐야, 시시한 반응이다」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 「ほう……この状況で、か」
	PRINTFORMW 「편……이 상황으로, 인가」
;OriginalString : 「随分とご立派な입だな！」
	PRINTFORMW 「상당히 훌륭한 입이다！」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생1, 「この恥知らずの豚め！」
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
;OriginalString : 「このマゾ犬が！」
		PRINTFORMW 「이 매저키스트개가！」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생0, 「ふーん」
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 0
;OriginalString : 「そ、そうか、なら激しくいくぞ！」
		PRINTFORMW 「, 그런가, 라면 격렬하게 가겠어！」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("용서를 빌다") 
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTREACT(7)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「今更言ったって、許してもらえるわけがないだろう？」
		PRINTFORMW 「이제 와서 말해도, 허락받을 수 있는 것이 없을 것이다？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	;TEQUIP:(관장＋항문プラグ/삼각목마)장착中等の場合は고통のSOURCEが入るので
	;可能性としては考えられますが、この箇所で손間を掛ける필요性としては微妙なところ
	;=================================================
	;REACT파생0, 고통による悲鳴
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 0
;OriginalString : 「もっと声を上げてみろ！」
		PRINTFORMW 「 좀 더 소리를 높여 봐라！」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 공포による悲鳴
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
;OriginalString : 「いい鳴き声だ！ 可愛いぞ！」
		PRINTFORMW 「좋은 울음 소리다！ 사랑스러워！」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;무서워한다
;記入チェック
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「ふふふっ、可愛い。逆効果だぞ」
		PRINTFORMW 「후후후, 사랑스럽다. 역효과다」
		RETURN 1
	ENDIF
ENDIF
;REACT7, 징계한다고 위협한다

;────────────────────────────────────
;REACT8, 고함치다・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理なし
;-------------------------------------------------
@KOJO_REACT_K21_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめて
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생1, 「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문プラグ/삼각목마)장착中等の場合は고통のSOURCEが入るので
	;可能性としては考えられますが、この箇所で손間を掛ける필요性としては微妙なところ
	;=================================================
	;REACT파생0, 고통による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 공포による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT8, 고함치다

;────────────────────────────────────
;REACT9, 기분나쁘게 웃다・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理なし
;-------------------------------------------------
@KOJO_REACT_K21_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"扱い
	;=================================================
	;REACT파생1, もうこんなことやめて
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"扱い
	;=================================================
	;REACT파생0, 気弱く返事했다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생1, 「意見を出すなんていい度가슴だね！」
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문プラグ/삼각목마)장착中等の場合は고통のSOURCEが入るので
	;可能性としては考えられますが、この箇所で손間を掛ける필요性としては微妙なところ
	;=================================================
	;REACT파생0, 고통による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 공포による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생1分岐を埋めた場合は、事実上TFLAG:REACT파생 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT파생に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT9, 기분나쁘게 웃다
