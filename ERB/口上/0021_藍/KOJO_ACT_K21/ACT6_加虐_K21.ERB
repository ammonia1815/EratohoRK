;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	야쿠모란구상 가학계 액션구상파일
;
;	가학계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;가학계 ACT60～68
;========================================================================
;·TFLAG:ACT(60=스펭킹/61=채찍/62=바늘/63=밧줄/64=눈가리개/65=볼개그/66=매도/67=삼각목마/68=관장세트)
;·TFLAG:ACT분류 == 6에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "가학")로 참조하는 것이 가능
;·TEQUIP계는 기본, ACT 개시시점으로써장착 된다
;·ACT에 따라서는죠교대상의 반응하기 나름으로취소 되는 것이 있다
;·밧줄/눈가리개/볼개그/삼각목마는 TEQUIP로관리 되고 해당 ACT/REACT 이외에도 TEQUIP:밧줄등으로 참조할 수 있다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT60, 스펭킹
;────────────────────────────────────
;·죠교자가죠교대상의 엉덩이를 평손로 두드린다
;·ACT 개시시점으로써 벌써 두드리고 있다
;·성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중인 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K21_60
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT60, 스펭킹
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;·@TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	[SKIPSTART]
	각 분기내에 ABL:MASTER:마조끼나 ABL:MASTER:고통중독등으로 파생을 마련하는 것도 좋다고 생각합니다
	디폴트로 준비하면 무리하게 묻어에 걸리는 예가 비교적 좋게 보여지므로, 텐프레로서는 굳이 마련하지 않습니다
	필요에 응해 확장해 주세요
	
	SELECTCASE ABL:MASTER:마조끼
		;마조끼 6 이상
		CASE IS > 6
;OriginalString : 
			PRINTFORMW 
		;마조끼이상
		CASE IS > 4
;OriginalString : 
			PRINTFORMW 
		;그 외
		CASEELSE
;OriginalString : 
			PRINTFORMW 
	ENDSELECT
	
	라든지, 이런 느낌으로. 확률도 관련되어 나눈다면…
	IF ELSEIF로 ABL:MASTER:마조끼 > 6 && ! RAND:2같은 방식이라도 좋을까
	단지 파생시킨다 뿐이라면 IF ELSEIF보다 SELECTCASE의 사용이 바람직합니다
	[SKIPEND]
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;금일의방침가징계·의 경우는 여기에 밖에 떨어지지 않게 되므로
	;구상기입 체크(20)등으로 해서 확률을 짜는 것을 추천
	;=================================================
	;징계·발동중
	;=================================================
	IF 구상기입체크(0) && TFLAG:징계·플래그
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;TEQUIP:죠교대상Ｃ사용 == 2（입에 의한애무）는 고려하지 않는다
	;=================================================
	;Ｃ애무계속중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:죠교대상Ｃ사용 == 1
		;수음계속중
		IF PENIS(MASTER)
;OriginalString : 
			PRINTFORMW 
		;애무계속중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(50)
		SELECTCASE ABL:MASTER:마조끼
			;마조끼 5 이상
			CASE IS >= 5
;OriginalString : 「そんな笑顔で…変態め」
				PRINTFORMW 「그런 웃는 얼굴로…변태째」
;OriginalString : 「ふふっ、可愛い声が聞けることを楽しみにしてるよ」
				PRINTFORMW 「후후, 사랑스러운 소리를 들을 수 있는 것을 기대하고 있어」
			;마조끼 3 이상
			CASE IS >= 3
;OriginalString : 「隠さなくても分かるぞ…顔に出てるからな」
				PRINTFORMW 「숨기지 않아도 알겠어…얼굴에 나오고 있으니」
;OriginalString : 「そんなに叩かれるのが好きならば、いくらでも叩いてやろう！」
				PRINTFORMW 「그렇게 얻어맞는 것을 좋아하면, 얼마든지 두드려 주자!」
			CASEELSE
;OriginalString : 「ふふっ、可愛い声上げて…」
				PRINTFORMW 「후후, 사랑스러운 소리 올려…」
;OriginalString : 「もっと苛めたくなってしまうじゃないか」
				PRINTFORMW 「 좀 더 괴롭히고 싶어져 버리잖아」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT61, 채찍
;────────────────────────────────────
;·죠교자가채찍를 크게 치켜들고 마음껏 찍어내린다
;·ACT 개시시점으로써 벌써 두드리고 있다
;·턴방침가"소프트"라면 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K21_61
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT61, 채찍
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;눈가리개장착
	;=================================================
	IF 구상기입체크(0) && TEQUIP:눈가리개
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;구속중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:밧줄
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(50)
		SELECTCASE ABL:MASTER:마조끼
			;마조끼 5 이상
			CASE IS >= 5
;OriginalString : 「とうとう채찍の虜になってしまったか…」
				PRINTFORMW 「드디어 채찍의 포로가 되어 버렸는지…」
;OriginalString : 「まぁいい、たくさん痕を残してやろう」
				PRINTFORMW 「아무튼 좋은, 많이 자국을 남겨 주자」
			;마조끼 3 이상
			CASE IS >= 3
;OriginalString : 「いくらでも痛くしてやろう」
				PRINTFORMW 「얼마든지 아프게 해 주자」
;OriginalString : 「そんなに期待して…ふふっ」
				PRINTFORMW 「그렇게 기대해…후후」
			CASEELSE
;OriginalString : 「さて、これをどうするか…分かるよな？」
				PRINTFORMW 「그런데, 이것을 어떻게 할까… 알아?」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT62, 바늘
;────────────────────────────────────
;·유두와성기에바늘를 찌른다
;·ACT 개시시점으로써 벌써 찌르고 있다
;·죠교대상에페니스가 있는 경우는페니스에, 없는 경우는클리토리스에 찌른다
;·가슴와성기(페니스/바기나)로부터 출혈해, 혈액 더러움이 추가된다
;·턴방침가"소프트", "노멀"라면 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K21_62
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT62, 바늘
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은페니스의 유무로 분기. 분기내에 파생 없음
;-------------------------------------------------
;IF 구상기입 체크(0) && PENIS(MASTER) 등으로 나누는손도 있습니다. 필요에 응해 분기를 증감해 주세요
;비교적 쓰기 어려운 개소이며, 온전히 하는 것은손간인 것로\@ PENIS(MASTER) ? 페니스 # 클리토리스 \@와 같이 3항을 사용하면 락 할 수 있거나 합니다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;눈가리개장착중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:눈가리개
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT63, 밧줄
;────────────────────────────────────
;·죠교대상를밧줄로 묶으려고 한다
;·지문은"묶으려고했다…"되고 있지만 처리적으로는 ACT 개시시점으로써 속박되고 있다
;·TEQUIP:밧줄로 묶기의 강도를 참조할 수 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:밧줄자체는 REACT분류가"용서를 빌다"의 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을 확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·(REACT63 이외에도취소처리가 되는 경우가 있기 때문에(위해))
;·성교중/성교봉사중에는 발생하지 않는다
;·구속시에자위나안면승마, 죠교대상의 페니스사용중는 해제된다
;-------------------------------------------------
@KOJO_ACT_K21_63
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_0, 밧줄장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·반항각인, 죠교자죠교레벨, 기교, 긴박능숙소질에 응해구속SOURCE 증가
;·구속SOURCE에 응해 강도 상승
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;묶기 강도는통상라면 1이나 2 정도라고 생각됩니다
	;그 이상의 묶기 강도는반항각인나죠교자의긴박능숙소질+일정 이상의기교가 요구될까하고
	;=================================================
	;그 외·조건 없음
	;=================================================
	;장착, 해제시 밖에 발생하지 않는 커맨드에 있어서 확률은 짜지 않는 것이 좋습니다@/L(2015/01/14)
	IF 구상기입체크(20)
;OriginalString : 「この밧줄をどうするかって？」
		PRINTFORMW 「이 줄을 어떻게 할까는?」
;OriginalString : 「決まっているだろう？　ふふっ」
		PRINTFORMW 「정해져 있을 것이다? 후후」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_1, 밧줄해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;밧줄해제에 파생했다 시점에서, "TEQUIP:밧줄 = 0"이 되므로 해제시에 묶어 강도는 참조 할 수 없다
;장착와 강도로 같은 TEQUIP를 재사용하고 있기 때문에(위해), 이와 같이 되어 버린다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(20)
;OriginalString : 「ふふふっ、また縛ってやるから安心しろ」
		PRINTFORMW 「후후후, 또 묶어 주기 때문에 안심해라」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT64, 눈가리개
;────────────────────────────────────
;·죠교자가눈가리개를 대게 하려고 한다
;·지문에서는"대게 하려고했다…"되고 있지만 처리상은 ACT 개시시점으로써장착 되고 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:눈가리개는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을 확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·(REACT64 이외에도취소처리가 되는 경우가 있기 때문에(위해))
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중였을 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K21_64
;-------------------------------------------------
;·TFLAG:ACT파성(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_0, 눈가리개장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상에페니스가 있는 경우, 마조끼에 응해발기도상승
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(20)
;OriginalString : 「何も見えないというシチュエーションはどうだ？」
		PRINTFORMW 「아무것도 안보인다고 하는 시추에이션은 어때?」
;OriginalString : 「私も당했다ことがあるが、なかなか興奮するだろう？」
		PRINTFORMW 「나도 되었던 적이 있지만, 꽤 흥분할 것이다?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_1, 눈가리개해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(20)
;OriginalString : 「そろそろ見えたほうがいいこともあってだな」
		PRINTFORMW 「이제 보이는 편이 좋은 일도 있어다」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT65, 볼개그
;────────────────────────────────────
;·죠교자가볼개그를 끼우게 하려고 한다
;·지문에서는"끼우게 하려고했다…"되고 있지만 처리상은 ACT 개시시점으로써장착 되고 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:볼개그는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을※확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·※REACT64 이외에도취소처리가 되는 경우가 있기 때문에(위해)
;·턴방침가"소프트", "노멀"의 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중였을 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K21_65
;-------------------------------------------------
;·TFLAG:ACT파성(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_0, 볼개그장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상에페니스가 있는 경우, 마조끼에 응해발기도상승
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(20)
;OriginalString : 「悶えてみせろ」
		PRINTFORMW 「번민이라고 보여라」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_1, 볼개그해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(20)
;OriginalString : 「喋られない気分はどうだった？」
		PRINTFORMW 「말해지지 않은 기분은 어땠어요?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT66, 매도
;────────────────────────────────────
;·도발하도록(듯이)죠교대상의 치태를 소리에 낸다
;·회화계로부터 발생하는 경우도 있다
;·턴방침가"소프트"라면 발생하지 않는다
;·회화계로부터 파생하는 경우도 있다
;-------------------------------------------------
@KOJO_ACT_K21_66
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT66, 매도
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교중/성교봉사중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:성교중, TEQUIP:성교봉사중) > 0
		;성교중
		IF TEQUIP:성교중
;OriginalString : 
			PRINTFORMW
		;성교봉사중
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;자위중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(GETBIT(TEQUIP:자위중, 0), GETBIT(TEQUIP:자위중, 1)) > 0
		;죠교대상가자위중
		IF GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
			PRINTFORMW 
		;죠교자가자위중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;Ｃ애무계속중
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2)
		;수음·애무계속중
		IF TEQUIP:죠교대상Ｃ사용 == 1
;OriginalString : 
			PRINTFORMW 
		;펠라·쿤닐링구스 계속중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「ばーか！ まぬけ！ 甲斐性なし！」
		PRINTFORMW 「-인가! 빠짐! 기개 없는 사람!」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT67, 삼각목마
;────────────────────────────────────
;·죠교대상를 목마에 실으려고 한다
;·지문은"실으려고했다…"되고 있지만 처리상은 ACT 개시시점으로써 실려지고 있다
;· 실은쾌Ｃ는 아니고쾌Ｖ가 들어간다(나있는 목마!?)
;·MASTER가처녀라면, 처녀상실가 일어난다(@ACTION_APPLY2로 처리. REACT분류가용서를 빌다의 경우는 회피된다)
;·죠교대상이남자의 경우는 발생하지 않는다
;·턴방침가"소프트", "노멀"때에는 발생하지 않는다
;·TEQUIP(바이브/항문바이브/항문비즈/관장그릇＋플러그/페니스밴드)장착시에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·야외플레이시에는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·이상계로부터 발생하는 경우도 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:삼각목마는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을※확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·※REACT67 이외에도취소처리가 되는 경우가 있기 때문에(위해)
;-------------------------------------------------
@KOJO_ACT_K21_67
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_0, 삼각목마장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생로 분기. 파생내에서는 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 목마에 실으려고했다…
	
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_1, 삼각목마해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;TEQUIP취소 시에는 여기서 주울 수 있는 것이 아닌 것에 주의
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 목마로부터 내려 주었다…
	
	;처음라고는 말해도, 과거에 TEQUIP취소나 해제되지 않는 채죠교종료로 있던 가능성도 있다
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT68, 관장세트
;────────────────────────────────────
;·관장액의 주입/추가/배출
;·최초의 주입시(ACT파생=0)는 양 1～3. 추가시(ACT파생=2)는 양 1～4. 각각 랜덤으로 처리된다
;·;이미 장비 하고 있는 경우, 관장액추가가 된다
;·관장액이 5병 들어가 있는 경우, 관장플러그 해제가 된다
;·관장액의 분량은 TEQUIP:관장그릇＋플러그로 참조할 수 있다
;·TEQUIP(항문바이브/항문비즈/페니스밴드/삼각목마)장착시에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·턴방침가"소프트"라면 발생하지 않는다
;·도구계로부터 파생하는 경우도 있다
;-------------------------------------------------
@KOJO_ACT_K21_68
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제/2=관장액추가)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_0, 관장그릇＋플러그장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기. 파생내에서는 분기 없음
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	
	;지문↓（제어하고 있기 (위해)때문에 아래와 같은 문장이 표시된다）
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하며, ")% %CALLNAME:MASTER%의 국혈에
	;관장액을 흘려 넣어, 마개를했다…
	
	;ACT 개시시점으로써 액은 주입되고 있어 TEQUIP:관장그릇＋플러그로 액량을 참조하는 것이 가능
	
	SELECTCASE GET_ABL(MASTER, "Ａ감각")
		CASE IS > 3
;OriginalString : %CALLNAME:TARGET%は%CALLNAME:MASTER%의 緩んだ尻穴に一気に관장液を流し込むと、栓をねじ込んだ……
			PRINTFORMDL %CALLNAME:TARGET%(은)는 %CALLNAME:MASTER%(이)가 느슨해진 고혈에 단번에 관장액을 흘려 넣으면(자), 마개를 나사 붐비었다……
		CASE IS > 1
;OriginalString : %CALLNAME:TARGET%は%CALLNAME:MASTER%의 開発당했다菊穴にプラグをねじ込むと、ぐぐっと관장液を流し込み、栓を했다…
			PRINTFORMDL %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 개발된 국혈에 플러그를 나사 붐비면(자), 훨씬 관장액을 흘려 넣어, 마개를 했다…
		CASEELSE
;OriginalString : %CALLNAME:TARGET%は%CALLNAME:MASTER%의 菊穴にプラグを押し込むと、チューブから관장液を流しこんできた……
			PRINTFORMDL %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 국혈에 플러그를 밀어넣으면(자), 튜브로부터 관장액을 흘려 넣어 왔다……
	ENDSELECT
	
	
	;=================================================
	;처음 「주입」
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
;OriginalString : 「うん、そろそろ、こっちも試してみようかな……」
		PRINTFORMW 「응, 이제, 여기도 시험해 볼까……」
		RETURN 1
	ENDIF
	
	;사양이 바뀔 가능성을 고려해 ==는 아니고>=로 해둔다
	;=================================================
	;액량 3
	;=================================================
	IF 구상기입체크(100) && TEQUIP:관장그릇＋플러그 >= 3
		IF PENIS(MASTER)
;OriginalString : 「むむ？ ちんちんも膨らんでないか？」
			PRINTFORMW 「? 응도 부풀어 오르지 않은가?」
;OriginalString : 「こっちにも溜まっちゃったのか……안쓰러운な」
			PRINTFORMW 「여기에도 모여 버렸어인가……야무지지 못한데」
		ELSE
;OriginalString : 「漏らしちゃだめだぞー」
			PRINTFORMW 「흘리면 안되구나―」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;액량 2
	;=================================================
	IF 구상기입체크(100) && TEQUIP:관장그릇＋플러그 == 2
;OriginalString : 「遠慮しないで全部飲めって」
		PRINTFORMW 「사양하지 않고 전부 마셔라고」
		RETURN 1
	ENDIF
	
	;액량의 분기를 모두 묻었을 경우는 TEQUIP:관장그릇＋플러그 == 1에 상당
	;개별적으로 나누지 않고 SELECTCASE TEQUIP:관장그릇＋플러그로 정리해 버리는 일도 가능
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「ほら、お腹綺麗綺麗しようなー」
		PRINTFORMW 「이봐요, 배기려기려 하자―」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_1, 관장그릇＋플러그해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;여기에 파생하는 시점에서 액량은 5가 되어 있다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	
	;지문↓（제어되고 있기 (위해)때문에 아래와 같은 문장을 표시）
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하면서, ")% %CALLNAME:MASTER%로부터
	;항문마개를 뽑아, 장내의 오물을 항문으로부터 따돌려 산등시켰다…
	
	SELECTCASE TEQUIP:관장그릇＋플러그
		CASE IS > 3
;OriginalString : 大量の水がブシュっと音を立てて吹き出してきた……
			PRINTFORMDL 대량의 물이 브슈와 소리를 내 불기 시작해 왔다……
;OriginalString : 流れだす水は段々と茶色く濁った色に変わり、最後には下品な音を立ててどろどろに溶けきった軟便が流れ落ちた……
			PRINTFORMDL 흐르기 시작하는 물은 점점갈색구 탁해진 색으로 바뀌어, 마지막에는 천한 소리를 내 질척질척에 녹고 자른 연변이 흐르고 떨어졌다……
		CASE 3
;OriginalString : 水が音を立てて吹き出してきた……
			PRINTFORMDL 물이 소리를 내 불기 시작해 왔다……
;OriginalString : ひとしきり水を吹くと半開きの%CALLNAME:MASTER%의 肛門から糞便がもりもりと溢れ、地面に落ちた……
			PRINTFORMDL 한 바탕수를 불면(자) 반쯤 열려 있는 상태의 %CALLNAME:MASTER%의 항문으로부터 분편이 와작와작 흘러넘쳐 지면에 떨어졌다……
		CASE 2
;OriginalString : ぶぷっ、ぷぴぴっ、と音を立てて%CALLNAME:MASTER%의 汚物が水とともに噴出했다……
			PRINTFORMDL , , 라고 소리를 내%CALLNAME:MASTER%의 오물이 물과 함께 분출했다……
		CASE 1
;OriginalString : 糞便が勢い良く飛び出했다……
			PRINTFORMDL 분편이 기세 좋게 뛰쳐나왔다……
	ENDSELECT

	;=================================================
	;처음 「배출」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_2, 관장액추가
;━━━━━━━━━━━━━━━━━━━━━━━━
;이미 주입 끝난 경우에 이 개소에 파생
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;지문↓（제어되고 있기 (위해)때문에 아래와 같은 문장을 표시）
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하면서, ")% %CALLNAME:MASTER%의 항문에
	;한층 더관장액을 주입하려고했다…
	;관장액분량 파생
	SELECTCASE TEQUIP:관장그릇＋플러그
		;액량 5
		CASE IS >= 5
;OriginalString : %CALLNAME:MASTER%は大きく重くなった腹を抑え、限界を超えた注水に悶絶している……
			PRINTFORMDW %CALLNAME:MASTER%(은)는 크고 무거워진 배를 억제해 한계를 넘은 주수에 기절 하고 있다……
		;액량 4
		CASE 4
;OriginalString : 液圧に耐え切れず、断続的に水を噴く%CALLNAME:MASTER%의 プラグに、更なる관장液が注入당했다……
			PRINTFORMDW 액압에 다 참아 수 있지 않고, 단속적으로 물을 분출하는 %CALLNAME:MASTER%의 플러그에, 한층 더 관장액이 주입되었다……
		;액량 3
		CASE 3
;OriginalString : 既にな당했다관장の肛虐で하복부を膨らませた%CALLNAME:MASTER%의 プラグに、強い力で液体が流れこんできた……
			PRINTFORMDW 이미 된 관장의 항학으로 하복부를 부풀렸다%CALLNAME:MASTER%의 플러그에, 강한 힘으로 액체가 흘러들어 왔다……
		;액량 2
		CASE 2
;OriginalString : 液圧で苦しむ%CALLNAME:MASTER%의 プラグに次の관장液がセットされ、一気に流し込まれた……
			PRINTFORMDW 액압으로 괴로워하는 %CALLNAME:MASTER%의 플러그에 다음의 관장액이 세트 되어 단번에 흘려 넣어졌다……
		;액량 1
		CASE IS <= 1
;OriginalString : %CALLNAME:TARGET%は%CALLNAME:MASTER%의 항문にさらに관장液を注入しようと했다… 
			PRINTFORMDW %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 어널에 한층 더 관장액을 주입하려고 했다… 
	ENDSELECT 
	IF 구상기입체크(100)
		SELECTCASE TEQUIP:관장그릇＋플러그
			CASE IS > 3
;OriginalString : 「くっ、これだけ入れるとシリンダーにかなり力がいるな……ふんっ……」
				PRINTFORML 「구, 이만큼 들어갈 수 있으면 실린더에 꽤 힘이 있데……응……」
			CASE 3
;OriginalString : 「ふふっ、いい顔してるな。ご褒美におかわりをあげようか……」
				PRINTFORML 「후후, 좋은 얼굴 하고 있는. 포상에 한 그릇 더를 줄까……」
			CASE 2
;OriginalString : 「狭い管を水が流れる時の音がいいんだ……」
				PRINTFORML 「좁은 관을 물이 흐를 때의 소리가 좋다……」
			CASE 1
;OriginalString : 「さあ、もう一杯いかが？」
				PRINTFORML 「자, 한잔 더 어떻게?」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF
