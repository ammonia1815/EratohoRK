;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	첸구상 이상계 액션구상파일
;
;	이상계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;이상계 ACT70～75
;========================================================================
;·TFLAG:ACT(70=피스트퍽/71=애널피스트/72=양쪽피스트/73=방치플레이/74=공중육변기플레이/75=페니스밴드장착)
;·TFLAG:ACT분류 == 7에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "이상")로 참조하는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT70, 피스트퍽
;────────────────────────────────────
;·죠교대상의 질에 주먹을 넣는다
;·ACT 개시시점으로써손를 내고 있다
;·죠교대상가처녀인 경우는처녀상실가 된다(@ACTION_APPLY2_70으로 처리된다)
;·죠교대상이남자인 경우는 발생하지 않는다
;·턴방침가"소프트", "노멀"의 경우는 발생하지 않는다
;·오늘의방침가"노닥노닥"의 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K13_70
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT70, 피스트퍽
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(0)
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 질에 주먹으로 침입하기 시작했다…
	
	;처음판정중 한쪽이 소비당했다경우, 이후, 그 만큼기는 기독이 되어 표시되는 일은 없습니다
	;=================================================
	;처음 「죠교대상가처녀」
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:처녀 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 「죠교대상가처녀는 아니다」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT71, 애널피스트
;────────────────────────────────────
;·죠교대상의엉덩이에 주먹을 넣는다
;·ACT 개시시점으로써손를 내고 있다
;·죠교대상이남자인 경우는 발생하지 않는다
;·턴방침가"소프트", "노멀"의 경우는 발생하지 않는다
;·오늘의방침가"노닥노닥"의 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K13_71
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT71, 애널피스트
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(0)
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 항문에 주먹으로 침입하기 시작했다…
	
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT72, 양쪽피스트
;────────────────────────────────────
;·죠교대상의 질과엉덩이에 주먹을 넣는다
;·ACT 개시시점으로써손를 내고 있다
;·죠교대상가처녀인 경우는처녀상실가 된다(@ACTION_APPLY2_72로 처리된다)
;·죠교대상이남자인 경우는 발생하지 않는다
;·턴방침가"소프트", "노멀", "하드"의 경우는 발생하지 않는다
;·오늘의방침가"노닥노닥"의 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K13_72
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT72, 양쪽피스트
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(0)
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 질과항문에 주먹으로 침입하기 시작했다…
	
	;처음판정중 한쪽이 소비당했다경우, 이후, 그 만큼기는 기독이 되어 표시되는 일은 없습니다
	;=================================================
	;처음 「죠교대상가처녀」
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:처녀 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF

	;=================================================
	;처음 「죠교대상가처녀는 아니다」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT73, 방치플레이
;────────────────────────────────────
;·죠교자가죠교대상를방치 한다
;·턴방침가"소프트", "노멀", "이상"라면 발생하지 않는다
;·(이상계 ACT로턴방침"이상"로 연주하고 있는 것은 미스라고 생각되고)
;·금일의방침가"노닥노닥"의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·수치계로부터 발생하는 경우도 있다(v1. 010 현재, 수치계로부터 파생했다경우에게만 발생이라고 생각되고)
;·안면승마계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K13_73
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT73, 방치플레이
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(0)
	
	;지문↓
	;%CALLNAME:TARGET%는 다만 %CALLNAME:MASTER%를방치 해 아무것도 하지 않는다…
	
	;=================================================
	;죠교자자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;죠교대상도자위중
		IF GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
			PRINTFORMW 
		;죠교자만자위중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;TEQUIP계 분기는눈가리개 이외에도바이브, 항문바이브, 항문비즈등이 생각됩니다
	;필요에 응해 확장해 주세요
	;=================================================
	;눈가리개장착중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:눈가리개
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT74, 공중육변기플레이
;────────────────────────────────────
;·속박된죠교대상를 밖에방치 한다
;·방치 되어 발견당했다경우죠교대상가처녀이면처녀상실. 동정의 경우는동정상실가 된다
;·밧줄와개목걸이가 갖추어지지 않았다고 발생하지 않는다
;·턴방침가"소프트", "노멀", "하드"의 경우는 발생하지 않는다
;·금일의방침가"노닥노닥"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·REACT분류가"용서를 빌다"였던 경우는취소 된다
;-------------------------------------------------
@KOJO_ACT_K13_74
;-------------------------------------------------
;·TFLAG:ACT파생(0=무사/1=애원/2=능욕/3=흥분)
;·죠교대상의 반응과 확률로 REACT 이후(ACTION_APPLY2)에 파생한다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT73, 공중육변기플레이
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(0)
	
	;지문↓
	;%CALLNAME:TARGET%는 밖에서 속박된 %CALLNAME:MASTER%의 배에 매직으로 「↓공중변소」라고 써,
	;그 자리에 두어 혼자서 돌아가려고했다…
	
	;=================================================
	;죠교대상가처녀
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:처녀
		;처음
		IF FIRSTACT(0)
;OriginalString : 
			PRINTFORMW 
		;처음시에애원에 의해취소당했다경우, 혹은 무사했던 경우
		;이 구조에서는, 처녀를 잃을 때까지 이"ELSE"의 개소에서 처리된다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가동정
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:동정
		;처음
		IF FIRSTACT(0)
;OriginalString : 
			PRINTFORMW 
		;처음시에애원에 의해취소당했다경우, 혹은 무사했던 경우
		;이 구조에서는, 동정를 잃을 때까지 이"ELSE"의 개소에서 처리된다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;처녀, 동정 이외의 경우의 첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT75, 페니스밴드장착
;────────────────────────────────────
;·죠교자가 쌍두페니스밴드를 몸에 대어, 죠교대상에 과시한다
;·죠교자가처녀인 경우처녀상실가 된다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교자에페니스가 있는 경우에는 발생하지 않는다
;·이미장착 하고 있었을 경우는 해제가 된다
;·안면승마계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K13_75
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT75_0, 페니스밴드장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·페니스밴드장비중은쾌Ｃ가쾌Ｖ에 변환
;·변환율… 쾌Ｃ를 20％로 해, 나머지의 값에Ｖ감각+1/Ｃ감각+1을 걸어 2로 나누어쾌Ｖ에 가산한다
;·지문은 ACT파생, 및처녀인지 아닌지로 분기 한다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;죠교자의 초기설정이처녀가 아닌 경우는(기본적으로) 불필요한 개소입니다
	;FIRSTACT의이벤트번호를 가지런히 해, 처녀 이외의 경우와 중복 하지 않게 되어 있습니다만
	;이 개소가필요가 아닌 경우는 삭제해 버리는 것이 좋다고 생각합니다
	;=================================================
	;처음 「죠교자가처녀」
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:처녀 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;죠교자가처녀가 아닌 경우는 여기서 첫회 판정을 합니다
	;=================================================
	;처음 「죠교자가처녀가 아닌 경우」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT75_1, 페니스밴드해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·몸에 대고 있는페니스밴드를 떼어낸다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
