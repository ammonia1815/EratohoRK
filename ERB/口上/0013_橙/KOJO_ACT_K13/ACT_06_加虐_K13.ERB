;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	첸구상 가학계 액션구상파일
;
;	가학계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;가학계 ACT60～68
;========================================================================
;·TFLAG:ACT(60=스펭킹/61=채찍/62=바늘/63=밧줄/64=눈가리개/65=볼개그/66=매도/67=삼각목마/68=관장세트)
;·TFLAG:ACT분류 == 6에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "가학")로 참조하는 것이 가능
;·TEQUIP계는 기본, ACT 개시시점으로써장착 된다
;·ACT에 따라서는죠교대상의 반응하기 나름으로취소 되는 것이 있다
;·밧줄/눈가리개/볼개그/삼각목마는 TEQUIP로관리 되고 해당 ACT/REACT 이외에도 TEQUIP:밧줄등으로 참조할 수 있다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT60, 스펭킹
;────────────────────────────────────
;·죠교자가죠교대상의 엉덩이를 평손로 두드린다
;·ACT 개시시점으로써 벌써 두드리고 있다
;·성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중인 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K13_60
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT60, 스펭킹
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;·@TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	[SKIPSTART]
	각 분기내에 ABL:MASTER:마조끼나 ABL:MASTER:고통중독등으로 파생을 마련하는 것도 좋다고 생각합니다
	디폴트로 준비하면 무리하게 묻어에 걸리는 예가 비교적 좋게 보여지므로, 텐프레로서는 굳이 마련하지 않습니다
	필요에 응해 확장해 주세요
	
	SELECTCASE ABL:MASTER:마조끼
		;마조끼 6 이상
		CASE IS > 6
;OriginalString : 
			PRINTFORMW 
		;마조끼이상
		CASE IS > 4
;OriginalString : 
			PRINTFORMW 
		;그 외
		CASEELSE
;OriginalString : 
			PRINTFORMW 
	ENDSELECT
	
	라든지, 이런 느낌으로. 확률도 관련되어 나눈다면…
	IF ELSEIF로 ABL:MASTER:마조끼 > 6 && ! RAND:2같은 방식이라도 좋을까
	단지 파생시킨다 뿐이라면 IF ELSEIF보다 SELECTCASE의 사용이 바람직합니다
	[SKIPEND]
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
;OriginalString : 「あんまりヌルい躾け方で調子に乗られても困るし、今からしっかり躾けてあげる」
		PRINTFORMW 「너무 눌 있고 예의범절을 가르치는 방법으로 본궤도에 올라져도 곤란하고, 지금부터 확실히 예의범절을 가르쳐 준다」
;OriginalString : 「――そぉれっ♪」
		PRINTFORMW 「――♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크() && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 「はいはい、暴れないのっ」
		PRINTFORML 「네네, 날뛰지 않는 것」
;OriginalString : 「それじゃあ、もう一回いくよ？」
		PRINTFORMW 「그러면 아, 다시 한번 가?」
		RETURN 1
	ENDIF
	
	;금일의방침가징계·의 경우는 여기에 밖에 떨어지지 않게 되므로
	;구상기입 체크(20)등으로 해서 확률을 짜는 것을 추천
	;=================================================
	;징계·발동중
	;=================================================
	IF 구상기입체크(50) && TFLAG:징계·플래그
;OriginalString : 「私が怒ってるの、わかってるよねえ？」
		PRINTFORML 「내가 화내, 알고 있지?」
;OriginalString : 「후후……大人しくしてないと、『うっかり』爪で尻肉を抉っちゃうかもよ？」
		PRINTFORMW 「후후…… 얌전하게 하지 않으면 「무심코」손톱으로 고육을 떠버릴까도?」
		RETURN 1
	ENDIF
	
	;TEQUIP:죠교대상Ｃ사용 == 2（입에 의한애무）는 고려하지 않는다
	;=================================================
	;Ｃ애무계속중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:죠교대상Ｃ사용 == 1
		;수음계속중
		IF PENIS(MASTER)
;OriginalString : 
			PRINTFORMW 
		;애무계속중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
;OriginalString : 「これでも怪我しない程度に손加減してあげてるんだから、大人しくしてなさいっ」
		PRINTFORMW 「이것이라도 상처나지 않는 정도로 손대중 해 주고 있기 때문에, 얌전하게 해 주세요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT61, 채찍
;────────────────────────────────────
;·죠교자가채찍를 크게 치켜들고 마음껏 찍어내린다
;·ACT 개시시점으로써 벌써 두드리고 있다
;·턴방침가"소프트"라면 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K13_61
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT61, 채찍
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;눈가리개장착
	;=================================================
	IF 구상기입체크(0) && TEQUIP:눈가리개
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;구속중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:밧줄
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
;OriginalString : 「コレって便利よね。壊さずに고통を与えられるんだもの」
		PRINTFORML 「코레는 편리해요. 부수지 않고 아픔이 주어진다 것」
;OriginalString : 「ねえ、%FUNC_K13_CALLNAME()%もそう思うでしょ？」
		PRINTFORMW 「응,%FUNC_K13_CALLNAME()%도 그렇게 생각하겠죠?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT62, 바늘
;────────────────────────────────────
;·유두와성기에바늘를 찌른다
;·ACT 개시시점으로써 벌써 찌르고 있다
;·죠교대상에페니스가 있는 경우는페니스에, 없는 경우는클리토리스에 찌른다
;·가슴와성기(페니스/바기나)로부터 출혈해, 혈액 더러움이 추가된다
;·턴방침가"소프트", "노멀"라면 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K13_62
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT62, 바늘
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은페니스의 유무로 분기. 분기내에 파생 없음
;-------------------------------------------------
;IF 구상기입 체크(0) && PENIS(MASTER) 등으로 나누는손도 있습니다. 필요에 응해 분기를 증감해 주세요
;비교적 쓰기 어려운 개소이며, 온전히 하는 것은손간인 것로\@ PENIS(MASTER) ? 페니스 # 클리토리스 \@와 같이 3항을 사용하면 락 할 수 있거나 합니다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;눈가리개장착중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:눈가리개
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT63, 밧줄
;────────────────────────────────────
;·죠교대상를밧줄로 묶으려고 한다
;·지문은"묶으려고했다…"되고 있지만 처리적으로는 ACT 개시시점으로써 속박되고 있다
;·TEQUIP:밧줄로 묶기의 강도를 참조할 수 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:밧줄자체는 REACT분류가"용서를 빌다"의 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을 확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·(REACT63 이외에도취소처리가 되는 경우가 있기 때문에(위해))
;·성교중/성교봉사중에는 발생하지 않는다
;·구속시에자위나안면승마, 죠교대상의 페니스사용중는 해제된다
;-------------------------------------------------
@KOJO_ACT_K13_63
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_0, 밧줄장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·반항각인, 죠교자죠교레벨, 기교, 긴박능숙소질에 응해구속SOURCE 증가
;·구속SOURCE에 응해 강도 상승
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;묶기 강도는통상라면 1이나 2 정도라고 생각됩니다
	;그 이상의 묶기 강도는반항각인나죠교자의긴박능숙소질+일정 이상의기교가 요구될까하고
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		SELECTCASE TEQUIP:밧줄
			;묶기 강도 3, 혹은 그 이상
			CASE IS >= 3
;OriginalString : 
				PRINTFORMW 
			;묶기 강도 3
			CASE 2
;OriginalString : 
				PRINTFORMW 
			;그 외
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_1, 밧줄해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;밧줄해제에 파생했다 시점에서, "TEQUIP:밧줄 = 0"이 되므로 해제시에 묶어 강도는 참조 할 수 없다
;장착와 강도로 같은 TEQUIP를 재사용하고 있기 때문에(위해), 이와 같이 되어 버린다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT64, 눈가리개
;────────────────────────────────────
;·죠교자가눈가리개를 대게 하려고 한다
;·지문에서는"대게 하려고했다…"되고 있지만 처리상은 ACT 개시시점으로써장착 되고 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:눈가리개는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을 확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·(REACT64 이외에도취소처리가 되는 경우가 있기 때문에(위해))
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중였을 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K13_64
;-------------------------------------------------
;·TFLAG:ACT파성(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_0, 눈가리개장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상에페니스가 있는 경우, 마조끼에 응해발기도상승
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_1, 눈가리개해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT65, 볼개그
;────────────────────────────────────
;·죠교자가볼개그를 끼우게 하려고 한다
;·지문에서는"끼우게 하려고했다…"되고 있지만 처리상은 ACT 개시시점으로써장착 되고 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:볼개그는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을※확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·※REACT64 이외에도취소처리가 되는 경우가 있기 때문에(위해)
;·턴방침가"소프트", "노멀"의 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계는 해제된다
;·죠교자가자위중였을 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K13_65
;-------------------------------------------------
;·TFLAG:ACT파성(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_0, 볼개그장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상에페니스가 있는 경우, 마조끼에 응해발기도상승
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독든지 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 응해 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_1, 볼개그해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT66, 매도
;────────────────────────────────────
;·도발하도록(듯이)죠교대상의 치태를 소리에 낸다
;·회화계로부터 발생하는 경우도 있다
;·턴방침가"소프트"라면 발생하지 않는다
;·회화계로부터 파생하는 경우도 있다
;-------------------------------------------------
@KOJO_ACT_K13_66
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT66, 매도
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교중/성교봉사중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:성교중, TEQUIP:성교봉사중) > 0
		;성교중
		IF TEQUIP:성교중
;OriginalString : 
			PRINTFORMW
		;성교봉사중
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;자위중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(GETBIT(TEQUIP:자위중, 0), GETBIT(TEQUIP:자위중, 1)) > 0
		;죠교대상가자위중
		IF GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
			PRINTFORMW 
		;죠교자가자위중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;Ｃ애무계속중
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2)
		;수음·애무계속중
		IF TEQUIP:죠교대상Ｃ사용 == 1
;OriginalString : 
			PRINTFORMW 
		;펠라·쿤닐링구스 계속중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT67, 삼각목마
;────────────────────────────────────
;·죠교대상를 목마에 실으려고 한다
;·지문은"실으려고했다…"되고 있지만 처리상은 ACT 개시시점으로써 실려지고 있다
;· 실은쾌Ｃ는 아니고쾌Ｖ가 들어간다(나있는 목마!?)
;·MASTER가처녀라면, 처녀상실가 일어난다(@ACTION_APPLY2로 처리. REACT분류가용서를 빌다의 경우는 회피된다)
;·죠교대상이남자의 경우는 발생하지 않는다
;·턴방침가"소프트", "노멀"때에는 발생하지 않는다
;·TEQUIP(바이브/항문바이브/항문비즈/관장그릇＋플러그/페니스밴드)장착시에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·야외플레이시에는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·이상계로부터 발생하는 경우도 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미장착 끝나, 한편 TFLAG:징계·플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:삼각목마는 REACT분류가"용서를 빌다"였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을※확실히 참조하는손단없음. 본체측의 개선이 요구된다
;·※REACT67 이외에도취소처리가 되는 경우가 있기 때문에(위해)
;-------------------------------------------------
@KOJO_ACT_K13_67
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_0, 삼각목마장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생로 분기. 파생내에서는 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 목마에 실으려고했다…
	
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_1, 삼각목마해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;TEQUIP취소 시에는 여기서 주울 수 있는 것이 아닌 것에 주의
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 목마로부터 내려 주었다…
	
	;처음라고는 말해도, 과거에 TEQUIP취소나 해제되지 않는 채죠교종료로 있던 가능성도 있다
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT68, 관장세트
;────────────────────────────────────
;·관장액의 주입/추가/배출
;·최초의 주입시(ACT파생=0)는 양 1～3. 추가시(ACT파생=2)는 양 1～4. 각각 랜덤으로 처리된다
;·;이미 장비 하고 있는 경우, 관장액추가가 된다
;·관장액이 5병 들어가 있는 경우, 관장플러그 해제가 된다
;·관장액의 분량은 TEQUIP:관장그릇＋플러그로 참조할 수 있다
;·TEQUIP(항문바이브/항문비즈/페니스밴드/삼각목마)장착시에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·턴방침가"소프트"라면 발생하지 않는다
;·도구계로부터 파생하는 경우도 있다
;-------------------------------------------------
@KOJO_ACT_K13_68
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제/2=관장액추가)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_0, 관장그릇＋플러그장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기. 파생내에서는 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하며, ")% %CALLNAME:MASTER%의 국혈에
	;관장액을 흘려 넣어, 마개를했다…
	
	;ACT 개시시점으로써 액은 주입되고 있어 TEQUIP:관장그릇＋플러그로 액량을 참조하는 것이 가능
	
	;=================================================
	;처음 「주입」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;사양이 바뀔 가능성을 고려해 ==는 아니고>=로 해둔다
	;=================================================
	;액량 3
	;=================================================
	IF 구상기입체크(0) && TEQUIP:관장그릇＋플러그 >= 3
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;액량 2
	;=================================================
	IF 구상기입체크(0) && TEQUIP:관장그릇＋플러그 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;액량의 분기를 모두 묻었을 경우는 TEQUIP:관장그릇＋플러그 == 1에 상당
	;개별적으로 나누지 않고 SELECTCASE TEQUIP:관장그릇＋플러그로 정리해 버리는 일도 가능
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_1, 관장그릇＋플러그해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;여기에 파생하는 시점에서 액량은 5가 되어 있다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하면서, ")% %CALLNAME:MASTER%로부터
	;항문마개를 뽑아, 장내의 오물을 항문으로부터 따돌려 산등시켰다…
	
	;=================================================
	;처음 「배출」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_2, 관장액추가
;━━━━━━━━━━━━━━━━━━━━━━━━
;이미 주입 끝난 경우에 이 개소에 파생
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("하면서, ")% %CALLNAME:MASTER%의 항문에
	;한층 더관장액을 주입하려고했다…
	
	[SKIPSTART]
	액량으로 나누어 쓴다면 comment out를 해제
	하층의 PRINTFORMW를 삭제해 주세요
	;관장액분량 파생
	SELECTCASE TEQUIP:관장그릇＋플러그
		;액량 5
		CASE 5
;OriginalString : 
			PRINTFORMW 
		;액량 4
		CASE 4
;OriginalString : 
			PRINTFORMW 
		;액량 3
		CASE 3
;OriginalString : 
			PRINTFORMW 
		;그 외
		CASEELSE
;OriginalString : 
			PRINTFORMW 
	ENDSELECT
	[SKIPEND]
	;나누어 쓰지 않는 것이면 여기서
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF
