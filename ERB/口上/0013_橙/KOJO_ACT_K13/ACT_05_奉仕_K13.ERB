;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	첸구상 봉사계 액션구상파일
;
;	봉사계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;봉사계 ACT50～57
;========================================================================
;·TFLAG:ACT(50=손으로 애무를 강요/51=입으로 애무를 강요/52=파이즈리/53=스마타/55=발핥기강제/56=이라마치오/57=가슴애무강제)
;·TFLAG:ACT분류 == 5에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "봉사")로 줍는 것이 가능
;·죠교대상에, 죠교자의 신체를애무 하도록(듯이) 명령한다
;·기본적으로는 명령인 것으로, 이 단계에서는 따를지 어떨지 확정하고 있지 않다(ACT56, 이라마치오는거부 할 수 없다)
;-------------------------------------------------
;·ACT54, 발코키는 12/04/08판으로 숙청
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT50, 손으로 애무를 강요
;────────────────────────────────────
;·애무강제/수음강제/페니스밴드수음강제
;·죠교대상(MASTER)에, 손에 의한애무를 강요 한다
;·죠교자에페니스가 있으면 반드시수음강제가 된다(페니스가 있으면통상의애무강제는 일어나지 않는다)
;·죠교자가페니스밴드장착중은페니스밴드수음강제가 된다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP(밧줄/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교대상가 따르는지 아닌지는 이 시점에서는 미정
;·REACT분류가거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_ACT_K13_50
;-------------------------------------------------
;·TFLAG:ACT파생(0=애무강제/1=수음강제/2=페니스밴드수음강제)
;-------------------------------------------------
;·지문은 ACT파생 마다 분기. 각 ACT파생내에서는 분기 없음
;·ACT파생 0, 애무때만쾌Ｂ/쾌ＡSOURCE 증가
;·그 외의 경우는쾌Ｃ한정
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50, 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
;이 구조로 첫회 분기에 떨어지면 파생에 관련되지 않고 한 번 뿐인 표시
;파생마다 첫회 분기를 마련하고 싶으면 각 ACT파생내에서 처리하는 일을 추천합니다
;FIRSTACT(0)/FIRSTACT(1)/FIRSTACT(2)와 같이 나누어 주면 파생마다 첫회를 참조할 수 있습니다
;-------------------------------------------------
IF 구상기입체크(0) && FIRSTACT(0)
	;=================================================
	;ACT파생 1, 수음강제
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT파생 2, 페니스밴드수음강제
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;상층의 분기를 묻었을 경우는 TFLAG:ACT파생 == 0으로 동의가 된다
	;여기에서만 끝마친다면, 범용적으로 사용할 수 있도록(듯이) 유의해야 할 개소
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_0, 애무강제 「자신의성기를손으로 애무 하라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_1, 수음강제 「자신의남성기를손로 잡아당기라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_2, 페니스밴드수음강제 「페니스밴드를손로 잡아 움직이라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;페니스밴드에의애무는쾌ＣSOURCE가 감소해, 쾌ＶSOURCE가 증가한다
;장착 하고 있는 시점에서대상가처녀일 가능성은 없다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT51, 입으로 애무를 강요
;────────────────────────────────────
;·쿤닐링구스/펠라티오/안면승마/안면승마항문/
;·죠교대상에, 입에 의한애무를 강요 한다
;·죠교자가후타나리의 경우, 죠교대상가누움라면 1/2으로안면승마, 그렇지 않으면펠라강제가 된다
;·죠교자에페니스가 없고, 죠교대상가누움라면안면승마가 된다
;·안면승마가 선택되고 있었을 때, 안면승마항문에변화 하는 일이 있다
;·모든 조건으로부터 빠졌을 경우는통상의 쿤닐링구스가 된다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마)장착중은 발생하지 않는다
;·죠교자가페니스밴드를장착 하고 있는 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·REACT분류가거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_ACT_K13_51
;-------------------------------------------------
;·TFLAG:ACT파생(0=쿤닐링구스/1=펠라/2=안면승마/3=안면승마항문)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51, 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
;우선 FIRSTACT(0)로 최상층에 첫회를 배치 섬했다가, 각 ACT파생내에서 처리하는 것도 좋을 것입니다
;FIRSTACT의이벤트번호를 입지 않도록 하면, 파생마다 첫회 판정을 할 수 있습니다
;-------------------------------------------------
IF 구상기입체크(0) && FIRSTACT(0)
	;=================================================
	;ACT파생가펠라강제
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT파생가안면승마
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT파생가안면승마항문
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 분기를 모두 묻었을 경우는 쿤닐링구스 강제 취급이 된다
	;파생마다 나누어 쓰지 않는 것이면, 이 개소를 범용적으로 사용할 수 있는 내용에했다상에서 불필요한 분기를 삭제하면 좋을 것입니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가Ｃ를 꾸짖어지고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상의Ｃ를 꾸짖으면서안면승마가 되는 시추에이션
;안면승마가 되는 경우는죠교자의 페니스, 페니스밴드등을 고려하는필요없음
;필요에 응해
;IF TEQUIP:페니스밴드
;ELSEIF PENIS(MASTER)
;ELSE
;등의 분기를 마련해 사용해 주세요
;TEQUIP:죠교대상Ｃ사용 == 1은손으로 애무중
;TEQUIP:죠교대상Ｃ사용 == 2는입으로 애무중이 됩니다
;일단입와손 각각의 분기설했다가
;죠교자의 소질에 따라서는입으로 애무가 대부분 나오지 않았다거나 하므로, 그 근처를 지켜본 다음 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2) > 0
	;=================================================
	;질내사정 후 3 턴 이내		질입로부터는 조금 전 충분히 짜낸정액가 걸죽 넘쳐 나오고 있다…
	;=================================================
	IF 구상기입체크(0) && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
		;입으로 애무중
		IF TEQUIP:죠교대상Ｃ사용 == 2
;OriginalString : 
			PRINTFORMW 
		;손으로 애무중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마항문파생			눈앞에 실룩거리는 국문이 드러내지고 있다……
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		;입으로 애무중
		IF TEQUIP:죠교대상Ｃ사용 == 2
;OriginalString : 
			PRINTFORMW 
		;손으로 애무중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;통상의안면승마를 상정@VAGINA_DESCRIPTION는 묘사계 함수
	;=================================================
	;기타·조건없음			무방비인%VAGINA_DESCRIPTION(TARGET)%가 드러내지고 있다……
	;=================================================
	IF 구상기입체크(0)
		;입으로 애무중
		IF TEQUIP:죠교대상Ｃ사용 == 2
;OriginalString : 
			PRINTFORMW 
		;손으로 애무중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_0, 쿤닐링구스 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_1, 펠라티오
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_2, 안면승마
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;질내사정 후 3 턴 이내
	;=================================================
	IF 구상기입체크(0) && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_3, 안면승마항문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 3
	;쾌Ｃ이외는 TCVAR:가버려에서는 주울 수 없기 때문에 PALAM로
	;=================================================
	;죠교자의쾌Ａ가 일정 이상 「가버려」
	;=================================================
	IF 구상기입체크(0) && PALAM:쾌Ａ >= 8500
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT52, 파이즈리
;────────────────────────────────────
;·죠교대상에파이즈리에서의봉사를 강요 한다
;·죠교대상이남자의 경우는 발생하지 않는다
;·죠교자에페니스가 없는 경우는 발생하지 않는다
;·턴방침가"이상"의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP(밧줄/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·REACT분류가거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_ACT_K13_52
;-------------------------------------------------
;·ACT파생없음. 지문에 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT52, 파이즈리
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;죠교대상의Ｂ절정가 가깝다고 하는 시추에이션
	;=================================================
	;죠교대상의쾌Ｂ가 일정 이상
	;=================================================
	IF 구상기입체크(0) && PALAM:MASTER:쾌Ｂ >= 8500
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가거유
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:거유
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가빈유
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:빈유
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT53, 스마타
;────────────────────────────────────
;·죠교대상에페니스밴드, 혹은페니스를 가랑이에 끼워 스치고라고 명령한다
;·죠교자에 봉이 없는(페니스/페니스밴드) 경우는 발생하지 않는다
;·죠교대상에페니스가 있는 경우는 발생하지 않는다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP:삼각목마장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교대상에발가 없으면 발생하지 않는다(현재 상태로서는토지코가죠교대상인 경우만이라고 생각된다)
;·REACT분류가거부/날뛴다/도망치는인 경우는 거절 취급
;-------------------------------------------------
@KOJO_ACT_K13_53
;-------------------------------------------------
;·TFLAG:ACT파생(0=스마타/1=페니스밴드스마타)
;-------------------------------------------------
;·안면승마계, 자위계는 해제된다
;·지문은 ACT파생로 분기. 파생내에서는 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;처음
;━━━━━━━━━━━━━━━━━━━━━━━━
;우선 최상층에 배치 섬했다가, ACT파생내에서 처리해도 된다고 생각합니다
;FIRSTACT의 인수를 가지런히 하는지, FIRSTACT(0), FIRSTACT(1)로서 개별적으로 첫회 판정을 주울까는 쓰기손나름
;-------------------------------------------------
IF 구상기입체크(0) && FIRSTACT(0)
	;=================================================
	;페니스밴드스마타강제
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;TFLAG:ACT파생 == 1의 분기를 썼을 경우는 ACT파생 == 0(스마타강제) 취급이 된다
	;상층의 분기를방치 해 여기에만 한다면, 범용적으로 사용할 수 있도록(듯이) 유의해야 할 개소
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_0, 스마타강제
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자에페니스가 있는 경우
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TCVAR:TARGET:가버려, TCVAR:MASTER:가버려) > 0
		;죠교자(TARGET)가가버려
		IF TCVAR:TARGET:가버려
;OriginalString : 
			PRINTFORMW 
		;죠교대상(MASTER)가가버려
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		;윤활 충분히
		IF PALAM:MASTER:윤활 >= 500
;OriginalString : 
			PRINTFORMW 
		;윤활불발
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_1, 페니스밴드스마타강제
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자가페니스밴드장착중의 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TCVAR:TARGET:가버려, TCVAR:MASTER:가버려) > 0
		;죠교자(TARGET)가가버려
		IF TCVAR:TARGET:가버려
;OriginalString : 
			PRINTFORMW 
		;죠교대상(MASTER)가가버려
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		;윤활 충분히
		IF PALAM:MASTER:윤활 >= 500
;OriginalString : 
			PRINTFORMW 
		;윤활불발
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT55, 발핥기강제
;────────────────────────────────────
;·죠교자가발를 핥아라고 명령해 온다
;·이상계로부터 발생하는 경우 있어
;·턴방침가"소프트"라면 발생하지 않는다
;·오늘의방침가"노닥노닥"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교자에발가 없는 경우는 발생하지 않는다
;·REACT분류가거부/날뛴다/도망치는 것 경우는 거절 취급
;-------------------------------------------------
@KOJO_ACT_K13_55
;-------------------------------------------------
;·지문에 분기 없음@TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT55, 발핥기강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
;OriginalString : 「さっきまで駆け回ってたから、ちょっと발が汚れちゃってるんだよねー」
		PRINTFORMW 「조금 전까지 이리저리 다니고 있었기 때문에, 조금 다리가 더러워져 버린다 지요―」
;OriginalString : 「せっかくだから、舐めて綺麗にしてもらおうかな？」
		PRINTFORML 「모처럼이니까, 빨아 깨끗이 받을까?」
;OriginalString : 「ほらほら、주인님の발だよ？　丁寧にお願いね♪」
		PRINTFORMW 「이봐요 이봐요, 남편님의 다리야?   정중하게 부탁이군요♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;전회COM가 허가를 청했다
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("용서를 빌다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;금일의방침가징계·의 경우는징계·플래그가 립 하며 가 되므로 확률 표시, 혹은 ONCEACT를 사용하는 것을 추천
	;=================================================
	;징계·모드
	;=================================================
	IF 구상기입체크(50) && TFLAG:징계·플래그 ;&& ONCEACT(0)
;OriginalString : 「ほら、舐めなさい」
		PRINTFORML 「이봐요, 빠세요」
;OriginalString : 「今は機嫌が悪いから……嫌だって言ったら、何するかわからないよ？」
		PRINTFORMW 「지금은 기분이 나쁘니까…… 싫다고 말하면, 어떻게 할지 몰라?」
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가누움
	;=================================================
	IF 구상기입체크(0) && TFLAG:누움
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가신발안를 신고 있다
	;(디폴트로신발안 신지 않은 캐릭터는 표시되지 않는다)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:신발안
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「ほら、주인님の발だよ？」
		PRINTFORML 「이봐요, 남편님의 다리야?」
;OriginalString : 「きちんと汚れ를 핥아とって、綺麗にしてね」
		PRINTFORMW 「제대로 더러움을 빨고 취해, 깨끗이 해」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT56, 이라마치오
;────────────────────────────────────
;·명령하는 것은 아니고, 억지로페니스를입에 돌진한다
;·죠교대상의 반응은 미정이지만, 거절하는 일은 할 수 없다
;·가학계로부터 발생하는 경우 있어
;·턴방침가"노멀", "소프트"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교자에페니스가 없는 경우는 발생하지 않는다(페니스밴드에 의한이라마치오는 없다)
;-------------------------------------------------
@KOJO_ACT_K13_56
;-------------------------------------------------
;·자위중이면 해제된다
;·지문에 분기 없음. 지문은↓
;·%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 두를 잡아, 난폭하게 %CALLNAME:MASTER%의 목까지 찔러 넣었다…
;-------------------------------------------------
;·죠교자의발기도합 있고는 TCVAR:발기로 참조할 수 있다
;·TCVAR:발기(0= 아직 부드럽다/1=서서히 경도를 증하면서 있다/2=발기했다/2이상=뒤로 젖혔다)적인 느낌으로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT56, 이라마치오
;━━━━━━━━━━━━━━━━━━━━━━━━
;필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가누움
	;=================================================
	IF 구상기입체크(0) && TFLAG:누움
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT57, 가슴애무강제
;────────────────────────────────────
;·죠교대상에, 죠교자의가슴를애무 하도록(듯이) 강제한다
;·죠교자가남자의 경우는 발생하지 않는다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP:밧줄장착중은입로가슴애무강제에 파생한다. 다만볼개그를장착중의 경우는 튕겨진다
;·볼개그가 없으면,1/2의 확률로입으로 애무가 된다
;·REACT분류가거부/날뛴다/도망치는 것 경우는 거절 취급
;·PALAM가치정선행이나반항선행인가로"부끄러워서봉사 할 수 없었다/싫어해 요구에 응하지 않았다"에 파생한다
;-------------------------------------------------
@KOJO_ACT_K13_57
;-------------------------------------------------
;·TFLAG:ACT파생(0=손로가슴애무강제/2=입로가슴애무강제)
;-------------------------------------------------
;·지문은 ACT파생로 분기. 파생내에서는 분기 없음
;·@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;·안면승마는 해제된다
;·REACT에 대해, 누움가 아니고, ACT파생가입으로 애무가 아닌 경우,1/2그리고 뒤로부터애무에 파생한다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;처음
;━━━━━━━━━━━━━━━━━━━━━━━━
;우선 최상층에 배치 섬했다가, ACT파생내에서 처리해도 된다고 생각합니다
;FIRSTACT의 인수를 가지런히 하는지, FIRSTACT(0), FIRSTACT(1)로서 개별적으로 첫회 판정을 주울까는 쓰기손나름
;-------------------------------------------------
IF 구상기입체크(0) && FIRSTACT(0)
	;=================================================
	;ACT파생 2, 입로가슴애무를 강요
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;TFLAG:ACT파생 == 2 분기를 묻었을 경우는! TFLAG:ACT파생분기로서 기능
	;여기에서만 끝마친다면, 어디라도 사용할 수 있는 내용을
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_0, 손로가슴애무를 강요
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;이전에는 체위마다야는은 규제되고 있었지만, 현재는 모든 체위로 발생한다
	;경우에 따라서는 엉뚱한 체제가 되지만, 이것은 현재 상태로서는 사양이라고 하는 일로
	;=================================================
	;죠교자삽입중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상삽입중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && PALAM:쾌Ｂ > 8500
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_2, 입로가슴애무를 강요
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;이전에는 체위마다야는은 규제되고 있었지만, 현재는 모든 체위로 발생한다
	;경우에 따라서는 엉뚱한 체제가 되지만, 이것은 현재 상태로서는 사양이라고 하는 일로
	;=================================================
	;죠교자삽입중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상삽입중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && PALAM:쾌Ｂ > 8500
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
