;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	첸구상 회화계 개별 리액션구상파일
;
;	회화계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어선택했다COM가 자연스러운행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운행동/1=무리가 없는행동/2=무리를했다행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 지금 기분을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K13_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일했다 있고)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다로, 리액션이빈정거린다 이외의 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 0
	;자연스러운행동
	;=================================================
	;REACT부하 0, 질문에 분명히 답하는 것이 할 수 없었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 말씨 나쁘게 질문에 답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 나가려 한 말을 삼켜 대답을 탁했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;자연스러운행동
	;=================================================
	;REACT부하 0, 어려운 말을 퍼부었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 용기를 내 강하게 맞았다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 눈을 일등 해입에서는 강하게 거절하는마이
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;자연스러운행동
	;=================================================
	;REACT부하 0, 여유의 미소로 어깨를 움츠리고 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 조금 생각하고 나서 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 간신히 야유 섞임에 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 3
	;자연스러운행동
	;=================================================
	;REACT부하 0, 함께 있어지는 것이 기쁘면솔직에입에했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 분명히 지금의 기분을 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 기분을 해치지 않게, 손질해 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 4
	;자연스러운행동
	;=================================================
	;REACT부하 0, 그렇게 나쁘다고 생각하지는 않다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「그저 나쁘지는 않다」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은적극			시선을 피해, 나쁘지는 않아라고 중얼거렸다
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;실은 혐				힘껏의 허풍으로, 그저 나쁘지 않다고 대답했다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일했다 있고
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 5
	;자연스러운행동
	;=================================================
	;REACT부하 0, 좀 더 기분 좋아지고 싶다고 했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 기분 좋은 일했다 있고 기분이라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 아첨하도록(듯이) 감히 추잡한 행위를 졸랐다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------
[SKIPSTART]
통상의 경우 리액션의 지문은@TRAIN_MESSAGE2와@REACTION_MESSAGE로 처리됩니다만
이하의 부분은@TRAIN_MESSAGE2의 조건으로부터 빠지기 때문에(위해), @REACTION_MESSAGE에서만 처리됩니다
COM 분기에 있어서도 당연히 TFLAG:REACT파생는 기능하고, @REACTION_MESSAGE내에서도 REACT부하 마다 나누어 쓸 수 있습니다
SELECTCASE GET_COM_RANK()
	;자연스러운행동
	CASE "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
	;무리가 없는행동
	CASE "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
	;무리를했다행동
	CASE "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
ENDSELECT
상기와 같은 분기를 마련하는 일에 의해, 본체측의변화에 대응하는 일은 가능합니다만
텐프레에 있어서 디폴트로 마련하려면 손간이 너무 걸린다고 생각하는 까닭에, 기본적으로 REACT내의 COM 분기에는 테두리를 마련하지 않는방침입니다
범용적인 내용으로 해도 좋아, 또 부분적으로 범용 리액션(COM)에 흘려도 좋다고 생각합니다
뭐 REACT부하 이외에도 확장하는 방법은 얼마든지 있거나 하므로, 그 근처의 판단은 각구상작자에게 맡기고 싶다고 생각합니다
3 BM/4 MB에서도 쓰기 때문에 어쨌든 생각되는 요소를 전부 넣어 줘! 적인 요망이 만약 있으면, 그 때는 또 생각하고 싶다와
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;지금 기분을 묻다에서의 COM침묵한다는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어징계·포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계·포인트가산 파생:「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT0, 지금 기분을 묻다

;────────────────────────────────────
;REACT1, 자신에게의 마음을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K13_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다!/2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 0
	;자연스러운행동
	;=================================================
	;REACT부하 0, 아무래도 싫다고는 말할 수 없었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 본심을 숨기도록(듯이) 눈을 숙여, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;자연스러운행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;자연스러운행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 적은지, REACT분류가태연하게 한다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 3
	;자연스러운행동
	;=================================================
	;REACT부하 0, 「좀 더 좋아하게 되고 싶기 때문에, 심한 것은 하지 말아줘」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「이제(벌써) 심한 일 하지 않으면, 좋아하게 될 수 있을지도 모른다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 기분을 묻도록(듯이) 시선을 향한 후, 「심한 일만 하는 사람은 싫다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 1000이상
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 4
	;부하 2는 연주한 (분)편이 뭔가 형편이 좋은 것 같아 우선
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(4) && TFLAG:REACT부하 < 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크() && TEQUIP:성교봉사중 && TFLAG:REACT부하 < 2
		IF FUNC_K13_ISLOVER()
;OriginalString : 「はぁん……❤　もっと言って……好きって、言ってぇ❤」
			PRINTFORML 「후~응……❤　좀 더 말해……좋아한다는 것은, 말해❤」
;OriginalString : %CALLNAME:MASTER%が%CALLNAME%への호의を告げるたびに、%CALLNAME%の膣がキュウッと締まり、
			PRINTFORML %CALLNAME:MASTER%(은)는 %CALLNAME%에의 호의를 고할 때마다, %CALLNAME%의 질이 큐욱과 꼭 죄여,
;OriginalString : %CALLNAME:MASTER%의 肉棒に甘くとろけるような刺激を与えてくる……
			PRINTFORMW %CALLNAME:MASTER%의 페니스에 달게 녹는 것 같은 자극을 주어 온다……
;OriginalString : 「私も好き❤　いっぱい愛してあげるから……いっぱい愛してぇ❤」
			PRINTFORMW 「 나도 좋아해❤　가득 사랑해 주기 때문에……가득 사랑해❤」
		ELSE
;OriginalString : 「あんっ……そんな風に言われたら、お股がキュンキュンしちゃう❤」
			PRINTFORML 「팥고물……그런 식으로 말해지면(자), 가랑이가 쿨쿨 해버린다❤」
;OriginalString : %CALLNAME:MASTER%의 言葉に反応して%CALLNAME%の膣が締まり、페니스를刺激してくる……
			PRINTFORML %CALLNAME:MASTER%의 말에 반응해 %CALLNAME%의 질이 꼭 죄여, 페니스를 자극해 온다……
;OriginalString : 「ほぉら、ちゃんと腰を振って？　休んじゃダメだからね……♪」
			PRINTFORMW 「등, 분명하게 허리를 털어?   쉬면 안되니까……♪」
		ENDIF
		RETURN 1
	ENDIF

	;자연스러운행동
	;=================================================
	;REACT부하 0, 붙임성 좋게 웃어 정말 좋아한다고 대답했다
	;=================================================
	IF 구상기입체크() && GET_COM_RANK() == "자연스러운행동"
		IF FUNC_K13_ISLOVER()
			IF RAND:2
;OriginalString : 「そうよね、当然よねぇ、う후후후……❤」
				PRINTFORML 「그래요, 당연해요 , 우대신 후후……❤」
;OriginalString : %CALLNAME:MASTER%의 言葉を聞いた%CALLNAME%は、すっかり顔を緩ませてニタニタと笑っている……
				PRINTFORML %CALLNAME:MASTER%의 말을 (들)물었다%CALLNAME%(은)는, 완전히 얼굴을 느슨해지게 해 니타니타와 웃고 있다……
;OriginalString : ………………
				PRINTFORMW ………………
;OriginalString : 「ちょっと、何見てるのよ？　……わ、笑うんじゃないわよ！　건방짐ねっ！！」
				PRINTFORMW 「조금, 무엇 봐?   …… 원, 웃지 않아요!   건방지구나!!」
			ELSE
;OriginalString : 「うふふ、私も大好きよ……恋人さん❤」
				PRINTFORML 「후후, 나도 정말 좋아해요……애인씨❤」
;OriginalString : 「……もっと、何度でも好きって言ってくれていいのよ？」
				PRINTFORMW 「…… 좀 더, 몇 번이라도 좋아한다고 말해 주어도 좋아?」
			ENDIF
		ELSE
;OriginalString : 「うふふ♪　ここまで懐いてくれると、私も嬉しくなっちゃうわね♪」
			PRINTFORML 「후후♪　여기까지 따라 주면, 나도 기뻐져버리는 원이군요♪」
;OriginalString : 「ほら、頭を撫でてあげる♪　こっちに来なさい？」
			PRINTFORML 「이봐요, 머리를 어루만져 준다♪  여기에 오세요?」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 頭を、小さな손でワシワシと乱暴に撫で回했다
			PRINTFORMW %CALLNAME%(은)는 %CALLNAME:MASTER%의 머리를, 작은 손으로 와시와시와 난폭하게 어루만지고 돌렸다
		ENDIF
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 분명히 좋아라고 고했다
	;=================================================
	IF 구상기입체크() && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 「ふふ、嬉しい♪」
		PRINTFORML 「후후, 기쁘다♪」
;OriginalString : 「%FUNC_K13_CALLNAME()%が私も好きでいてくれるなら、私も%FUNC_K13_CALLNAME()%に優しくしてあげる……♪」
		PRINTFORMW 「%FUNC_K13_CALLNAME()%하지만 나도 좋아하고 있어 준다면, 나도%FUNC_K13_CALLNAME()%에 상냥하게 해 준다……♪」
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 억지웃음을 띄워, 좋아해라고 대답했다
	;=================================================
	IF 구상기입체크() && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 「ふぅん？　別に無理しなくていいのに」
		PRINTFORML 「응?   별로 무리하지 않아 좋은데」
;OriginalString : 「……まあ、주인님の機嫌が悪くならないように頑張った所は、ちょっと評価してあげる」
		PRINTFORMW 「……뭐, 남편님의 기분이 나쁘게 안 되게 노력한 곳은, 조금 평가 해 준다」
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 5
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(5)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;자연스러운행동
	;=================================================
	;REACT부하 0, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「좀 더 좋아하게 되도록(듯이), 좀 더 귀여워했으면 좋겠다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 「좋아하게 될 수 있을 정도로, 굉장한 일 해 주면 된다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자신에게의 마음을 묻다의 경우는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어징계·포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계·포인트가산 파생:「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT1, 자신에게의 마음을 묻다

;────────────────────────────────────
;REACT2, 성에 관한 이야기를 하다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K13_2
;-------------------------------------------------
;  파생할 가능성이 있는 COM는 우선 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계·포인트가산 파생:「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;징계·포인트가산 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;TEQUIP에 의해 쾌SOURCE가 들어가 있는 경우에 파생
	;=================================================
	;REACT파생 1, 쾌락에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 상황에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("자위를 시작한다")
	;=================================================
	;REACT파생 1, 「승손에 자위 하지마」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「승손에 자위 하고 있지만 좋은가」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT2, 성에 관한 이야기를 함

;────────────────────────────────────
;REACT3, 상냥하게 위로한다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K13_3
;-------------------------------------------------
;  파생할 가능성이 있는 COM에는 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
;  우선회화계 COM를 모두 망라되어 있습니다만
;  10 접수대의애무계 COM, 50 접수대 이후의 그 외 COM등도 상황에 따라서는선택 가능하게 됩니다
;  필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성 있게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("상냥하게 응답한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("강하게 응답한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT3, 상냥하게 위로한다

;────────────────────────────────────
;REACT4, 엄하게 위협한다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_REACT_K13_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(3)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT4, 엄하게 위협한다

;────────────────────────────────────
;REACT5, 의상 변경을 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가"소극적으로 한다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가"적극적으로 한다", "태연하게 한다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_REACT_K13_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:ACT파생 == 0
	;-------------------------------------------------
	;전라되었다(악세서리·신발안를 제외함)
	;-------------------------------------------------
	IF 구상기입체크() && (TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
		;REACT분류 받아들인다/적극적으로 한다/태연하게 한다
		IF IS_COMGRONAME("받아들인다/적극적으로 한다")
			IF TALENT:MASTER:남자
;OriginalString : 「ふふ、美味しそうな身体してるわね♪」
				PRINTFORML 「후후, 맛있을 것 같은 신체 해♪」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 남성기を見て舌なめずりを했다……
				PRINTFORML %CALLNAME%(은)는 %CALLNAME:MASTER%의 남성기를 봐 혀인 째를 했다……
;OriginalString : 「ねえ……ひょっとして期待してる？」
				PRINTFORMW 「응…… 혹시 기대하고 있다?」
			ELSE
;OriginalString : 「ふふ、可愛い身体してるわね♪」
				PRINTFORML 「후후, 사랑스러운 신체 해♪」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 体를 핥아回すように観察했다
				PRINTFORML %CALLNAME%(은)는 %CALLNAME:MASTER%의 몸을 뚫어지듯 관찰했다
;OriginalString : 「さてと……どうやって食べてあげようかしら？」
				PRINTFORMW 「그런데…… 어떻게 먹어 줄까?」
			ENDIF
			RETURN 1
		ELSEIF IS_COMGRONAME("태연하게 한다")
			IF TALENT:MASTER:남자
;OriginalString : 「む～……もうちょっと何か反応しなさいよ、つまらないわねぇ……」
				PRINTFORMW 「～…… 좀 더 뭔가 반응해 주세요, 시시하네요……」
;OriginalString : 「まあ、それはそれとして、身体の方は美味しそうね♪」
				PRINTFORML 「뭐, 그것은 그것으로서 신체는 맛있을 것 같구나♪」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 남성기を見て舌なめずりを했다……
				PRINTFORML %CALLNAME%(은)는 %CALLNAME:MASTER%의 남성기를 봐 혀인 째를 했다……
			ELSE
;OriginalString : 「む～……もうちょっと何か反応しなさいよ、つまらないわねぇ……」
				PRINTFORMW 「～…… 좀 더 뭔가 반응해 주세요, 시시하네요……」
;OriginalString : 「まあ、それはそれとして、身体の方は可愛いくて素敵ね♪」
				PRINTFORML 「뭐, 그것은 그것으로서 신체는 사랑스러운 구라고 멋지구나♪」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 体를 핥아回すように観察했다
				PRINTFORML %CALLNAME%(은)는 %CALLNAME:MASTER%의 몸을 뚫어지듯 관찰했다
			ENDIF
			RETURN 1
		;REACT분류소극적으로 한다			어쩔 수 없이 마지막 한 장을 벗어, 전라되었다
		ELSE
			IF TALENT:MASTER:남자
;OriginalString : 「ふふ、美味しそうな身体してるわね♪」
				PRINTFORML 「후후, 맛있을 것 같은 신체 해♪」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 남성기を見て舌なめずりを했다……
				PRINTFORML %CALLNAME%(은)는 %CALLNAME:MASTER%의 남성기를 봐 혀인 째를 했다……
;OriginalString : 「怖がらなくていいわよ？　気持よくしてあげる……たぶんね？」
				PRINTFORMW 「무서워하지 않아 좋아요?   기분 좋게 해 준다…… 아마?」
			ELSE
;OriginalString : 「ふふ、可愛い身体してるわね♪」
				PRINTFORML 「후후, 사랑스러운 신체 해♪」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 体를 핥아回すように観察했다
				PRINTFORML %CALLNAME%(은)는 %CALLNAME:MASTER%의 몸을 뚫어지듯 관찰했다
;OriginalString : 「怖がらなくていいわよ？　……美味しく食べちゃうだけだから♪」
				PRINTFORMW 「무서워하지 않아 좋아요?   …… 맛있게 먹어버린다만이니까♪」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF

	;=================================================
	;한 장 벗었다(소극적 반응)
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("소극적으로 한다/받아들인다")
		
		;-------------------------------------------------
		;완만한 움직임으로 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;어쩔 수 없이 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("소극적으로 한다")
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;상층의 분기를 모두 묻었을 경우는 불필요한 개소
		;반대로 묻지 않는 경우는 이 개소가 보완이 책임을 다합니다
		;-------------------------------------------------
		;기타·조건없음
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;2매 벗었다(적극적 반응)
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;-------------------------------------------------
		;전라되었다
		;-------------------------------------------------
		IF 구상기입체크(0) && (TEQUIP:MASTER:악세서리 + TEQUIP:MASTER:신발안 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;적극적으로 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다")
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;손때 좋게 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("태연하게 한다")
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;상층의 분기를 모두 묻었을 경우는 불필요한 개소
		;반대로 묻지 않는 경우는 이 개소가 보완이 책임을 다합니다
		;-------------------------------------------------
		;기타·조건없음
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;벗지 않았다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("거부")
		;부끄러워서 벗겨지지 않았다
		;IF PALAM:MASTER:치정 > PALAM:MASTER:반항
		;싫어해 벗지 않았다
		;ELSE
		;ENDIF

		;탈의거부첫회
		IF !VAR_K13:0
			VAR_K13:0 = 1
;OriginalString : 「何よ、주인님の命令が聞けないっていうの？」
			PRINTFORML 「뭐야, 남편님의 명령을 들을 수 없다고 말해?」
;OriginalString : 「仕方ないわね、それなら……」
			PRINTFORMW 「어쩔 수 없네요, 그렇다면……」
;OriginalString : 
			PRINTL 
;OriginalString : 「その服、無理矢理にでも引きちぎってやるわよっ！」
			PRINTFORMW 「그 옷, 무리하게라도 당겨 뜯어 준다!」
;OriginalString : %CALLNAME%はそう言うなり、%CALLNAME:MASTER%に飛びかかってきた
			PRINTFORML %CALLNAME%(은)는 그렇게 말하든지, %CALLNAME:MASTER%에 달려들어 왔다
;OriginalString : 一瞬のうちに、%CALLNAME:MASTER%의 体が침대の上に押さえつけられる
			PRINTFORMW 일순간에, %CALLNAME:MASTER%의 몸을 침대 위에 억누를 수 있다
;OriginalString : %CALLNAME%の小さな左손が%CALLNAME:MASTER%의 首に押し当てられ、見た目からは想像できない強い力で締めあげてくる
			PRINTFORML %CALLNAME%(이)가 작은 왼손이%CALLNAME:MASTER%의 목에 꽉 누를 수 있어 외형으로부터는 상상할 수 없는 강한 힘으로 잡아 온다
;OriginalString : %CALLNAME:MASTER%はあまりの苦しさに발をバタつかせて날뛴다が、%CALLNAME%の손はビクともしない
			PRINTFORMW %CALLNAME:MASTER%(은)는 너무나 괴로워서 다리를 바타 붙게 해 날뛰지만, %CALLNAME%의 손은 꿈쩍도 하지않다
;OriginalString : 「ほら、暴れないの！　すぐ終わるから、じっとしてなさいっ！」
			PRINTFORMW 「이봐요, 날뛰지 않는거야!   곧 끝나기 때문에, 가만히 해 주세요!」
;OriginalString : %CALLNAME%は空いている右손を%CALLNAME:MASTER%의 服の襟元に掛け、爪を立ててグッと力を込める……
			PRINTFORMW %CALLNAME%(은)는 비어 있는 오른손을 %CALLNAME:MASTER%의 옷의 옷깃에 걸쳐 손톱을 세워 훨씬 힘을 집중한다……
;OriginalString : 
			PRINTL 
;OriginalString : 「うーん……」
			PRINTFORMW 「응……」
;OriginalString : 今にも服を破ろうかというところで、ピタリと%CALLNAME%の動きが止まる……%CALLNAME:MASTER%의 首を締めあげ하며 
			PRINTFORMW 당장 옷을 찢을까하고 하는 (곳)중에, 꼭과%CALLNAME%의 움직임이 멈춘다……%CALLNAME:MASTER%의 목을 조인 채로
;OriginalString : 「よく考えてみたら、裸のまま過ごさせて風邪でも引かれたら困るのよね……」
			PRINTFORML 「잘 생각해 보면(자), 벌거벗은 채로 보내게 해 감기에서도 끌리면(자) 곤란해……」
;OriginalString : 「代わりの服なんて用意できないし……」
			PRINTFORMW 「대신의 옷은 준비할 수 없고……」
;OriginalString : 
			PRINTL 
;OriginalString : 「……ああ、もう！　面倒ねっ！」
			PRINTFORML 「…… 아, 이제!   귀찮구나!」
;OriginalString : %CALLNAME%はようやく%CALLNAME:MASTER%의 首から손を離すと、%CALLNAME:MASTER%의 위から降りて침대の上で立ち上がった
			PRINTFORMW %CALLNAME%(은)는 간신히%CALLNAME:MASTER%의 목으로부터 손을 떼어 놓으면(자), %CALLNAME:MASTER%의 위에서 내려 침대 위에서 일어섰다
;OriginalString : 「ほら！　服を破くのは勘弁してあげるから、さっさと体を起こしなさいっ！」
			PRINTFORML 「이봐요!   옷을 찢는 것은 용서 해 주기 때문에, 빨리 몸을 일으키세요!」
;OriginalString : あからさまに苛立った声で急かしてくる%CALLNAME%
			PRINTFORMW 명백하게 초조한 소리로 갑작스러운가 해 온다%CALLNAME%
;OriginalString : だが、首を締められ続けた%CALLNAME:MASTER%は激しく咳き込んでしまい、酸欠で意識も몽롱としていて動くことができない……
			PRINTFORMW 하지만, 목을 조일 수 있는 계속했다%CALLNAME:MASTER%(은)는 격렬하게 기침해 붐비어 버려, 산소 결핍으로 의식도 몽롱해지고 있어 움직일 수가 없다……
;OriginalString : 「起きろって言ってるでしょっ！　また주인님の命令を無視する気なの？」
			PRINTFORMW 「일어나라고 하고 있지!   또 남편님의 명령을 무시할 생각이야?」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 腰を横からゲシゲシと蹴りつけてきた……
			PRINTFORMW %CALLNAME%(은)는 %CALLNAME:MASTER%의 허리를 옆으로부터 게시게시와 차대어 왔다……
		ELSE
			IF RAND:2
;OriginalString : 「ふぅん、주인님の命令が聞けないのね？」
				PRINTFORML 「응, 남편님의 명령을 들을 수 없구나?」
;OriginalString : 「だったら、私が責任を持って、命令が聞けるようになるまで躾けてあげる」
				PRINTFORMW 「이라면, 내가 책임을 가져, 명령을 (들)물을 수 있게 될 때까지 예의범절을 가르쳐 준다」
			ELSE
;OriginalString : 「……あんまりイライラさせないでよね」
				PRINTFORML 「……너무 이라이라 시키지 않고군요」
;OriginalString : 「私だって無闇に분노たくはないのよ？　出来の悪いペットちゃん」
				PRINTFORMW 「나도 함부로 화내고 싶지는 않아?   할 수 있고의 나쁜 애완동물」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("소극적으로 한다/받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;2매 입었다(적극적 반응)
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;지문에서는 "싫어해"라고 하는 취급
	;=================================================
	;입지 않았다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("거부")
;OriginalString : 「ちょっと！　服を着るくらいいいじゃないの！？」
		PRINTFORML 「조금!   옷을 입을 정도로 좋지 않은거야!?」
;OriginalString : 「ホント、聞き分けのないペットね……どうやって躾けてやろうかしら？」
		PRINTFORMW 「진짜, 구별하고가 없는 애완동물…… 어떻게 예의범절을 가르쳐 줄까?」
		RETURN 1
	ENDIF
ENDIF
;REACT5, 의상 변경을 요구한다

;────────────────────────────────────
;REACT6, 합의를 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_REACT_K13_6
;-------------------------------------------------
;손간을 싫어하지 않는 것이면, TALENT:MASTER:남자등으로 내부에서 나누는 것도 있을지도 모릅니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("소극적으로 한다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 각오를 결정한 것처럼, 작은 소리로 승낙했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 0, 작은 소리로 승낙했다	
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은적극			부끄러움에 스러질 것 같은 소리로 승낙했다
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;실은 혐				분함에 얼굴을 비뚤어지게 하면서도 승낙했다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("태연하게 한다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 별로 싫지 않다고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 마음대로 하면 좋다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은적극			했다 좋은들 좋다고 말했다
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;실은 혐				승손로 하면 좋다고 대답했다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 처음을 줄 수 있어 기쁘다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 뜻을 정해 바쳐도 괜찮다고 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 이제 지킬 수 없다면 적어도 상냥하게 여겨지도록(듯이)와 동의했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게 해 주는 것이라면 처음을 주어도 괜찮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 처음을 바치기 때문에기분좋게 해 갖고 싶다고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 기분좋게 해 주는 것이라면 처음을 주어도 괜찮다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 처음을 바치라고 한다면, 괴로움을 잊을 수 있을 정도로기분좋게 해라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의 하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서"거부"에 세트 된다)
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("거부")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(4)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT6, 합의를 요구한다

;────────────────────────────────────
;REACT7, 징계한다고 위협한다·개별 리액션
;────────────────────────────────────
;·선언해도징계·이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_REACT_K13_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM"마음 약하게 응답한다"는 일정 조건으로 REACT분류"용서를 빌다"에 파생한다
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다") 
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(7)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「ああ、もう！　散々손間かけさせておいて、今更怯えてるんじゃないわよ！」
		PRINTFORML 「아, 이제!   산들수고 걸치게 해 둬, 이제 와서 무서워하고 있는  것이 아니에요!」
;OriginalString : 「私がわざわざ面倒な思いをしてまで、きちんと躾けてやるって言ってるの！　少しは感謝しなさいっ！！」
		PRINTFORMW 「내가 일부러 귀찮은 생각을 해서까지, 제대로 예의범절을 가르쳐 준다 라고 해!   조금은 감사해 주세요!!」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「あら、何を무서워한다ことがあるの？」
		PRINTFORML 「어머나, 무엇을 무서워하는 일이 있어?」
;OriginalString : 「こうなることがわかってて반항してたんでしょ？　違う？」
		PRINTFORMW 「이러한 것을 알고 있어 반항했기 때문에 실마리?   다르다?」
		RETURN 1
	ENDIF
ENDIF
;REACT7, 징계한다고 위협한다

;────────────────────────────────────
;REACT8, 고함치다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K13_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
	IF FUNC_K13_ISLOVER()
;OriginalString : 「……だ、黙ってないで何か言いなさいよ！」
		PRINTFORML 「……(이)다, 입다물지 말고 뭔가 말하세요!」
;OriginalString : 「叱られる%FUNC_K13_CALLNAME()%が悪いのよ？　わかってるんでしょうね！？」
		PRINTFORMW 「꾸중듣는다%FUNC_K13_CALLNAME()%하지만 나빠?   알고 있는 군요!?」
	ELSE
;OriginalString : 「……なに？　せっかく叱ってあげたのに、『ごめんなさい』もないの？」
		PRINTFORML 「…… 뭐?   모처럼 꾸짖어 주었는데, 「미안해요」도 없는거야?」
;OriginalString : 「いい度가슴じゃない……どうしてやろうかしら？」
		PRINTFORMW 「좋은 담력이 아니다…… 어째서 할까?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「……何か言ったかしら？　ひょっとして、反論があるの？」
		PRINTFORML 「…… 뭔가 말했는지 해들?   혹시, 반론이 있어?」
;OriginalString : 「反論があるなら聞くわよ？　……途中でイラついたら腕の一本や二本、引き千切っちゃうかもしれないけど♪」
		PRINTFORMW 「반론이 있다면 (들)물어요?   …… 도중에 이라 붙으면 팔의 한 개나 2 개, 당겨 잘게 썰어버릴까도 모르는데♪」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
;OriginalString : 「……何か言ったかしら？　声が小さくて聞こえないわよ？」
		PRINTFORML 「…… 뭔가 말했는지 해들?   소리가 작아서 들리지 않아요?」
;OriginalString : 「何よりもまず、『ごめんなさい』でしょ。小さい頃に習わなかったの？」
		PRINTFORMW 「무엇보다도 우선, 「미안해요」겠지. 어렸을 적에 배우지 않았어?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「……あのねえ……許すも何もないでしょ？」
		PRINTFORML 「…… 저기요 네…… 허락하는 것도 아무것도 없지요?」
;OriginalString : 「わざわざ出来損ないの%FUNC_K13_CALLNAME()%のために叱ってあげたのよ？　주인님に感謝しなさい！」
		PRINTFORMW 「일부러 할 수 있어 손해 없는거야%FUNC_K13_CALLNAME()%를 위해서(때문에) 꾸짖어 주었어요?   남편님에게 감사해 주세요!」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「……本当に反省してるのかしら？」
		PRINTFORML 「…… 정말로 반성하고 있는 거나?」
;OriginalString : 「まあいいわ。ダメならまた叱ってやればいいだけの話だしね♪」
		PRINTFORMW 「뭐 좋아요. 안되면 또 꾸짖어 하면 되는 것뿐의 이야기이고♪」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
;OriginalString : 「煩いわね！　ちょっとは黙っていられないの！？」
		PRINTFORML 「번거롭네요!   조금은 입다물고 있을 수 없는거야!?」
;OriginalString : 「せっかく叱ってあげたっていうのに、本当に恩知らずなペットね！！」
		PRINTFORMW 「모처럼 꾸짖어 주었다고 하는데, 정말로 배은망덕한 애완동물!!」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「煩いわね！　ちょっとは黙っていられないの！？」
		PRINTFORML 「번거롭네요!   조금은 입다물고 있을 수 없는거야!?」
;OriginalString : 「せっかく叱ってあげたっていうのに、本当に恩知らずなペットね！！」
		PRINTFORMW 「모처럼 꾸짖어 주었다고 하는데, 정말로 배은망덕한 애완동물!!」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		IF RAND:2
;OriginalString : 「何を怯えてるのよ！？　それじゃあまるで私が悪いみたいじゃない」
			PRINTFORML 「무엇을 무서워해!?   그러면 남는 것으로 내가 나쁜 것 같지 않다」
;OriginalString : 「悪いのはお前の方でしょ？　私が出来損ないのお前を正しく躾けてやってるの！　違う！？」
			PRINTFORMW 「나쁜 것은 너의 (분)편이겠지?   내가 생겨 손해 없는 것 너를 올바르게 예의범절을 가르쳐 주어!   다르다!?」
		ELSE
;OriginalString : 「ガタガタ震えてれば、許してもらえるとでも思ってるの？」
			PRINTFORML 「덜컹덜컹 떨리고 있으면, 허락받을 수 있다고 생각해?」
;OriginalString : 「かえってイラッとするのよ、そういう態度は！！」
			PRINTFORMW 「오히려 이락으로 해, 그러한 태도는!!」
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		IF FUNC_K13_ISLOVER()
			IF RAND:2
;OriginalString : 「……ふふ、ごめんなさいね」
				PRINTFORML 「……후후, 미안해요」
;OriginalString : 「たまには引き締めておかないといけないと思って……。傷ついちゃったかしら？」
				PRINTFORMW 「가끔 씩은 긴축시켜 두지 않으면 안 된다고 생각해……. 다쳐 버렸다 까?」
			ELSE
;OriginalString : 「……おっと、ついつい怒鳴っちゃった」
				PRINTFORML 「……어, 그만 고함쳐 버렸다」
;OriginalString : 「ちょっと苛立ってたのよ。……もう落ち着いたから、大丈夫♪」
				PRINTFORMW 「조금 초조했었던 것.……벌써 침착했기 때문에, 괜찮아♪」
			ENDIF
		ELSE
			IF RAND:2
;OriginalString : 「……ふふ、わかればいいのよ」
				PRINTFORML 「……후후, 알면 좋아」
;OriginalString : 「私も鬼じゃないからね。ちゃんと反省するなら、今回は許してあげる♪」
				PRINTFORMW 「 나도 귀신이 아니니까. 분명하게 반성한다면, 이번은 허락해 준다♪」
			ELSE
;OriginalString : 「……ふふ、悪かったわね？　いきなり怒鳴ったりして」
				PRINTFORML 「…… 후후, 나빴어요?   갑자기 고함치거나 하며」
;OriginalString : 「別に私も%FUNC_K13_CALLNAME()%を怖がらせるために言ってるわけじゃないのよ？　……ね？」
				PRINTFORMW 「별로 나도%FUNC_K13_CALLNAME()%를 무서워하게 하기 위해서(때문에) 말하고 있는  것이 아니어?   …… 그런데?」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT8, 고함치다

;────────────────────────────────────
;REACT9, 기분나쁘게 웃다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K13_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「……私の顔を見ただけでそんなに怖がるなんて、失礼だと思わない？」
		PRINTFORML 「…… 나의 얼굴을 본 것만으로 그렇게 무서워하다니 실례라고 생각하지 않는다?」
;OriginalString : 「正直、傷ついちゃうわね……これは징계・が필요かしら？」
		PRINTFORMW 「정직, 다쳐버리는 원이군요…… 이것은 징계나무가 필요할까?」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「何をそんなに怖がってるの？」
		PRINTFORML 「무엇을 그렇게 무서워해?」
;OriginalString : 「まあ、震えたいなら好きに震えててもいいけど……私のやることは変わらないわよ？」
		PRINTFORMW 「뭐, 떨리고 싶으면 좋아하게 떨려도 좋은데…… 내가 하는 것은 변함없어요?」
		RETURN 1
	ENDIF
ENDIF
;REACT9, 기분나쁘게 웃다
