;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	첸구상 성교봉사계 개별 리액션구상파일
;
;	성교봉사계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;성교봉사계
;========================================================================
;· 각 REACT내의 나누는 방법으로서 TFLAG:REACT분류(IS_COMGRONAME)나 SELECTCOM(IS_NOWCOMNAME)가 생각됩니다
;·다만, 범위의 넓은 조건으로 주워 버리는 것은 기본적으로 추천하지 않습니다
;·ACT분류:성교봉사계의 경우, 봉사계 COM(COM40~COM44)를 중심의선택지가 됩니다만
;·상황에 따라서는 그 외의 것이선택지에 포함될 가능성도 있습니다
;·리액션(REACT)으로 처리되지 않았던 것은 범용 리액션(COM)으로 보완되므로
;·무리하게 모두 묻는필요는 없습니다
;·필요라고 생각하는 것을 지켜봐, 좁혀, COM와의 제휴를 생각해 효율 좋게 구축해야 합니다
;-------------------------------------------------
;· 각 REACT내는,&& IS_NOWCOMNAME든지,&& IS_COMGRONAME등의 조건으로 결정하고 쳐 합시다
;·여기에 조건 없음 분기가 있으면, 모두 주워 버려 범용 리액션에 흐르지 않습니다
;·죠교대상의 반응에 관련되지 않고, 같은 리액션 밖에 하지 않게 되어 버립니다
;·요컨데 이것은, 상정당했다시스템의 파괴에 연결됩니다
;-------------------------------------------------
;  REACT의 지문은 ACT_MESSAGE.ERB의 TRAIN_MESSAGE2_{TFLAG:ACT}로 처리됩니다
;  지문은 수용의 경우는 TFLAG:REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)
;  거절했다경우는 REACT분류(날뛴다/도망친다)와 REACT분류:거부로 나누어집니다
;  각 REACT분류내에서 TFLAG:REACT부하(자연스러운행동/무리가 없는행동/무리를했다행동)로 파생
;  REACT분류가(받아들인다/적극적으로 한다/태연하게 한다)의 경우는, TCVAR:MASTER:가버려, TCVAR:MASTER:애태움도등으로 세세하게 묘사됩니다
;  ACT_MESSAGE.ERB REACTION_MESSAGE.ERB를 참조하면서의 작업을 추천합니다
;-------------------------------------------------
;각 REACT에서는 상정되는 반응을 망라되어 있습니다. 필요에 응해 분기를 증감해 사용해 주세요
;COM에 따라서는 REACT파생로 REACT분류가변화 하는 일에 주의
;각 COM의 파생은 기본적으로 REACT파생 1를 조건 결정 우리 소분기로 해, REACT파생 0는 그 외 취급으로 조건 없다고 보고 있습니다
;손간을 걸고 싶지 않은 경우는 그 외만을 써, 파생에 관련되지 않고 해당 개소를 보완할 수가 있습니다
;또 IF 구상기입 체크(0) && TFLAG:REACT파생 < 1으로 하는 일로 파생했다경우는 범용 리액션에 흘린다 따위라고 하는 일도 가능합니다
;파생은 장래 증가할 가능성도 있으므로 != 1등으로 하는 것보다는< 1으로했다 (분)편이 무난하다라고 생각합니다
;-------------------------------------------------
;·COM20. 허리를 흔든다에 대해…
;·COM20, 허리를 흔든다는 ACT에 의해 REACT분류가변화
;·역강간의 경우는 REACT분류:받아들인다. 그 외성교봉사계 T에서는 REACT분류:적극적으로 한다취급이 된다
;·이변화는 함수@SEX_INITIATIVE에서 행해진다
;-------------------------------------------------



;-------------------------------------------------
;성봉공통
;이하의 커맨드로 개별의 REACT가 없으면 호출한다
;  정상위시킨다, 후배위시킨다, 대면좌위시킨다, 배면좌위시킨다
;덧붙여역강간의 경우는 REACT의 거동이 다르기 (위해)때문에, 이 부분은 불려 가지 않는다
;★개별 REACT를 쓸 때는 조건 판정이라고 불러 방편의 차례로 배려하는 것★
;-------------------------------------------------
@KOJO_REACT_K13_성봉공통
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·REACT분류가날뛴다/거부의 경우는취소취급
;·ACT가취소가 되었을 경우는 TEQUIP:성교봉사중도 리셋트
;·그 외의 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)의 경우는 수용 취급이 된다
;·죠교대상가동정라면동정상실, 죠교자가처녀라면처녀상실가 일어난다
;·TEQUIP:성교봉사중가 서 있을 때에 REACT분류가날뛴다라면 거절 취급이 된다(즉, 성교봉사계 ACT 이외에도 거절 처리가 되는 경우가 있다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우(REACT분류가날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
;개별 REACT로우선처리

;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실
;━━━━━━━━━━━━━━━━━━━━━━━━
;개별 REACT로우선처리

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
		IF !VAR_K13:30
;OriginalString : 「あん、ふぁあんっ❤　イキそう？　でも、まだ出しちゃダメっ！」
			PRINTFORML 「팥고물, 팥고물?   이키 그렇게?   그렇지만, 아직 내면 안 됨!」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 페니스の根本に尻尾をキツく巻きつけ、사정できないようにしてきた
			PRINTFORML %CALLNAME%(은)는 %CALLNAME:MASTER%의 페니스의 근본에 꼬리를 귤나무구 감아 사정할 수 없게 해 왔다
;OriginalString : 「ちゃんと봉사できたら解いてあげる♪　私のオマンコ、全力でズコバコ突きまくりなさいっ❤」
			PRINTFORMW 「분명하게 봉사할 수 있으면 풀어 준다♪　나의 보지, 전력으로 즈코바코 마구 찌르세요❤」
			;꼬리플래그
			VAR_K13:30 = 1
		ELSE
;OriginalString : 「あっ❤　はあぁんっ❤　イイわよ、その調子ぃ❤　もっと腰振って、기분좋게 해ぇっ！」
			PRINTFORML 「아?   는?   이이원이야, 그 상태?   좀 더 허리 털어, 기분 좋게 해!」
;OriginalString : 「만족させてくれたらぁ、思いっきり気持ちよーく私の中でお漏らしさせてあげるっ❤」
			PRINTFORMW 「만족시켜 주면, 마음껏 기분 잘 나중에서 흘려 시켜 준다❤」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
		IF !VAR_K13:30
;OriginalString : 「あはっ、気持ち良さそうね❤　……でも、사정はまだダーメ♪」
			PRINTFORML 「아는 , 기분 좋음 그렇구나❤　……그렇지만, 사정은 아직 부인♪」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 페니스の根本に尻尾をキツく巻きつけ、사정できないようにしてきた
			PRINTFORML %CALLNAME%(은)는 %CALLNAME:MASTER%의 페니스의 근본에 꼬리를 귤나무구 감아 사정할 수 없게 해 왔다
;OriginalString : 「ふふっ♪　簡単には出させてあげないわ♪　もっともっと、濃ゆい精をいっぱい溜めなさい❤」
			PRINTFORMW 「후후♪　간단하게는 내게 해 주지 않아요♪　좀더 좀더, 농 해 정령을 가득 모으세요❤」
			;꼬리플래그
			VAR_K13:30 = 1
		ELSE
;OriginalString : 「あんっ❤　あぅんっ❤　ねえ、사정했다い？　中出し했다い？」
			PRINTFORML 「팥고물?   아응?   저기, 사정하고 싶다?   질내사정하고 싶다?」
;OriginalString : 「でもまだダメ♪　もっと참다してぇ、あんっ、いっぱいキンタマに정액溜めるのよ❤」
			PRINTFORMW 「 그렇지만 아직 안 됨♪　좀 더 참아 , 팥고물, 가득 불알에 정액 모아❤」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1은 REACT분류:날뛴다취급인 것으로 고려하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 「はぁんっ❤　必死で腰振っちゃって、そんなに気持ちいいの？」
	PRINTFORML 「후~응?   필사적으로 허리 털어 버려서, 그렇게 기분이 좋은거야?」
;OriginalString : 「せっかく私のオマンコを使わせてあげてるんだからぁ……」
	PRINTFORML 「모처럼 나의 보지를 사용하게 해 주고 있기 때문에……」
;OriginalString : 「いっぱい腰を振って、濃ゆい精を주인님に捧げるのよ❤」
	PRINTFORMW 「가득 허리를 털어, 농 해 정령을 남편님에게 바쳐❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 「あはっ♪　そんなに気持ち良さそうな声上げちゃって、可愛い子ね❤」
	PRINTFORML 「아는♪　그렇게 기분 좋음 그런 소리 올려 버려서, 사랑스러운 아이❤」
;OriginalString : 「いいのよ？　そのまま獣みたいに주인님を犯しまくって、孕ませるくらいビュルビュル種付け사정しても❤」
	PRINTFORMW 「좋아?   그대로짐승같이 남편님을 마구 범해, 잉다툴 정도로 뷰르뷰르 교배 사정해도?」
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가변화 하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 「あんっ❤　はあんっ❤　いいわよ、その調子で、もっと激しく突いてぇっ❤」
	PRINTFORMW 「팥고물❤　(은)는 팥고물❤　좋아요, 그 상태로, 좀 더 격렬하게 찔러❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 「あっ！　ひゃぁんっ！？　イイよぉっ、感じちゃうっ❤」
	PRINTFORMW 「아!   !?   이이야, 느껴버리는?」
;OriginalString : 「そのまま、もっと激しくシてぇっ❤　」
	PRINTFORMW 「그대로, 좀 더 격렬하게 시라고❤　」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("애정을 담아 한다")
	IF FUNC_K13_ISLOVER()
		IF RAND:2
;OriginalString : 「ふぁあっ！？　あっ❤　イイっ❤　すっごくイイよぉ❤❤」
			PRINTFORML 「!?   아?   이이?   대단히 이이야??」
;OriginalString : 「今は恋人でいいからぁ、ああんっ❤　もっと、愛してぇ❤　もっと、感じさせてぇっ❤❤」
			PRINTFORMW 「지금은 애인으로 좋으니까, 아 응❤　좀 더, 사랑해❤　좀 더, 느끼게 해❤❤」
		ELSE
;OriginalString : 「あっ、ひぃんっ、んぁああっ❤❤　そんな風に당했다らぁっ、あっ！　感じちゃうっ❤」
			PRINTFORML 「아, 히응, 응아??  그런 식으로 되면, 아!   느껴버리는?」
;OriginalString : 「あはぁ❤　子宮が、熱いぃ……っ❤　本気で孕んじゃいそぉっ❤❤」
			PRINTFORMW 「아는❤　자궁이, 뜨거운……❤　진심으로 잉응은 해변❤❤」
		ENDIF
	ELSE
		IF RAND:2
;OriginalString : 「ふぁあっ！？　あっ❤　イイっ❤　すっごくイイよぉっ❤❤」
			PRINTFORML 「!?   아?   이이?   대단히 이이야??」
;OriginalString : 「こんなの知ったらぁ❤　ああんっ❤　本気で離れられなくなるぅっ❤　ふあああっ❤❤」
			PRINTFORMW 「이런 건 알면(자)❤　아 응❤　진심으로 떨어질 수 없게 되는❤　아 아❤❤」
		ELSE
;OriginalString : 「あっ、ひぃんっ、んぁああっ❤❤　そんな風に당했다らぁっ、あっ！　私ダメになっちゃうぅっ❤」
			PRINTFORML 「아, 히응, 응아??  그런 식으로 되면, 아!   나 안되게 되어버리는?」
;OriginalString : 「ひぃんっ❤　子宮が、熱いよぉ……っ❤　もっと、犯してぇ……っ❤❤」
			PRINTFORMW 「히응❤　자궁이, 뜨거워……❤　좀 더, 범해……❤❤」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;성봉공통

;────────────────────────────────────
;REACT95, 정상위시킨다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교계 반응
;	COM:허리를 흔든다
;고통계 반응
;	COM:날뛴다
;봉사계 반응
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K13_95
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·REACT분류가날뛴다/거부의 경우는취소취급
;·ACT가취소가 되었을 경우는 TEQUIP:성교봉사중도 리셋트
;·그 외의 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)의 경우는 수용 취급이 된다
;·죠교대상가동정라면동정상실, 죠교자가처녀라면처녀상실가 일어난다
;·TEQUIP:성교봉사중가 서 있을 때에 REACT분류가날뛴다라면 거절 취급이 된다(즉, 성교봉사계 ACT 이외에도 거절 처리가 되는 경우가 있다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우(REACT분류가날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2로 TEQUIP는 해제되고 있으므로
	;현재의@V_SEX의 사양(TEQUIP:성교봉사중를 참조하고 있다)라고 죽어 분기가 된다고 생각됩니다
	;ACT파생로 묶는 일로 상사형의 결과를 얻을 수 있을 것 같습니다만, 함수가손 넣고 될 가능성을 고려해 우선 이대로라고 하는 일로
	;=================================================
	;이미삽입 하고 있었을 경우
	;=================================================
	;IF 구상기입 체크(0) && V_SEX(MASTER)
	;	PRINTFORMW 
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		CALL FUNC_K13_CANCEL_SEX()
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실
;━━━━━━━━━━━━━━━━━━━━━━━━
IF TCVAR:처녀상실
	;처녀 때의 공통 처리
	CALL FUNC_K13_LOST_VERGIN_TARGET()
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1은 REACT분류:날뛴다취급인 것으로 고려하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가변화 하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을 담아 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;개별 REACT가 없으면성봉공통으로 난다
CALL KOJO_REACT_K13_성봉공통
;REACT95, 정상위시킨다

;────────────────────────────────────
;REACT96, 후배위시킨다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교계 반응
;	COM:허리를 흔든다
;고통계 반응
;	COM:날뛴다
;봉사계 반응
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K13_96
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·REACT분류가날뛴다/거부의 경우는취소취급
;·ACT가취소가 되었을 경우는 TEQUIP:성교봉사중도 리셋트
;·그 외의 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)의 경우는 수용 취급이 된다
;·죠교대상가동정라면동정상실, 죠교자가처녀라면처녀상실가 일어난다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우(REACT분류가날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2로 TEQUIP는 해제되고 있으므로
	;현재의@V_SEX의 사양(TEQUIP:성교봉사중를 참조하고 있다)라고 죽어 분기가 된다고 생각됩니다
	;ACT파생로 묶는 일로 상사형의 결과를 얻을 수 있을 것 같습니다만, 함수가손 넣고 될 가능성을 고려해 우선 이대로라고 하는 일로
	;=================================================
	;이미삽입 하고 있었을 경우
	;=================================================
	;IF 구상기입 체크(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;그 외·ACT 개시시의 거절
	;=================================================
	IF 구상기입체크()
		CALL FUNC_K13_CANCEL_SEX()
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실
;━━━━━━━━━━━━━━━━━━━━━━━━
IF TCVAR:처녀상실
	;처녀 때의 공통 처리
	CALL FUNC_K13_LOST_VERGIN_TARGET()
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1은 REACT분류:날뛴다취급인 것으로 고려하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가변화 하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을 담아 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;개별 REACT가 없으면성봉공통으로 난다
CALL KOJO_REACT_K13_성봉공통
;REACT96, 후배위시킨다

;────────────────────────────────────
;REACT97, 대면좌위시킨다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교계 반응
;	COM:허리를 흔든다
;고통계 반응
;	COM:날뛴다
;봉사계 반응
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K13_97
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·REACT분류가날뛴다/거부의 경우는취소취급
;·ACT가취소가 되었을 경우는 TEQUIP:성교봉사중도 리셋트
;·그 외의 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)의 경우는 수용 취급이 된다
;·죠교대상가동정라면동정상실, 죠교자가처녀라면처녀상실가 일어난다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우(REACT분류가날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2로 TEQUIP는 해제되고 있으므로
	;현재의@V_SEX의 사양(TEQUIP:성교봉사중를 참조하고 있다)라고 죽어 분기가 된다고 생각됩니다
	;ACT파생로 묶는 일로 상사형의 결과를 얻을 수 있을 것 같습니다만, 함수가손 넣고 될 가능성을 고려해 우선 이대로라고 하는 일로
	;=================================================
	;이미삽입 하고 있었을 경우
	;=================================================
	;IF 구상기입 체크(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		CALL FUNC_K13_CANCEL_SEX()
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실
;━━━━━━━━━━━━━━━━━━━━━━━━
IF TCVAR:처녀상실
	;처녀 때의 공통 처리
	CALL FUNC_K13_LOST_VERGIN_TARGET()
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1은 REACT분류:날뛴다취급인 것으로 고려하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가변화 하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 「にゃあんっ❤　ソコ突かれるの、好きぃっ！　気持ちイイっ❤」
	PRINTFORML 「팥고물?   소코 찔리는 것, 좋아!   기분 이이?」
;OriginalString : 「その調子で、もっと突いてっ、犯しまくってぇ……っ❤」
	PRINTFORMW 「그 상태로, 좀 더 찔러 , 마구 범해……❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("애정을 담아 한다")
;OriginalString : 「にゃぁあんっ❤　気持ちイイところぉ、ちんぽで押されてるっ❤　はぁああっ❤」
	PRINTFORML 「팥고물❤　기분 이이곳 , 로 밀리고 있는❤　후~ 아❤」
;OriginalString : 「こんな気持ち良く당했다ら、赤ちゃんの部屋が疼いちゃう……本当に孕んじゃいそぉっ❤❤」
	PRINTFORMW 「이런 기분 좋게 되면(자), 아기의 방이 쑤셔버린다……정말로 잉응은 해변❤❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;개별 REACT가 없으면성봉공통으로 난다
CALL KOJO_REACT_K13_성봉공통
;REACT97, 대면좌위시킨다

;────────────────────────────────────
;REACT98, 배면좌위시킨다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교계 반응
;	COM:허리를 흔든다
;고통계 반응
;	COM:날뛴다
;봉사계 반응
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K13_98
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·REACT분류가날뛴다/거부의 경우는취소취급
;·ACT가취소가 되었을 경우는 TEQUIP:성교봉사중도 리셋트
;·그 외의 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)의 경우는 수용 취급이 된다
;·죠교대상가동정라면동정상실, 죠교자가처녀라면처녀상실가 일어난다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우(REACT분류가날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2로 TEQUIP는 해제되고 있으므로
	;현재의@V_SEX의 사양(TEQUIP:성교봉사중를 참조하고 있다)라고 죽어 분기가 된다고 생각됩니다
	;ACT파생로 묶는 일로 상사형의 결과를 얻을 수 있을 것 같습니다만, 함수가손 넣고 될 가능성을 고려해 우선 이대로라고 하는 일로
	;=================================================
	;이미삽입 하고 있었을 경우
	;=================================================
	;IF 구상기입 체크(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		CALL FUNC_K13_CANCEL_SEX()
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실
;━━━━━━━━━━━━━━━━━━━━━━━━
IF TCVAR:처녀상실
	;처녀 때의 공통 처리
	CALL FUNC_K13_LOST_VERGIN_TARGET()
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가변화 하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을 담아 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;개별 REACT가 없으면성봉공통으로 난다
CALL KOJO_REACT_K13_성봉공통
;REACT98, 배면좌위시킨다

;────────────────────────────────────
;REACT99, 항문섹스시킨다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ａ성교계로부터/3=Ｖ성교봉사계로부터/4=Ｖ성교계로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교계 반응
;	COM:허리를 흔든다
;고통계 반응
;	COM:날뛴다
;봉사계 반응
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K13_99
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·REACT분류가날뛴다/거부의 경우는취소취급
;·ACT가취소가 되었을 경우는 TEQUIP:성교봉사중도 리셋트
;·그 외의 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)의 경우는 수용 취급이 된다
;·죠교대상가동정에서도동정상실는 되지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우(REACT분류가날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2로 TEQUIP는 해제되고 있으므로
	;현재의@V_SEX의 사양(TEQUIP:성교봉사중를 참조하고 있다)라고 죽어 분기가 된다고 생각됩니다
	;ACT파생로 묶는 일로 상사형의 결과를 얻을 수 있을 것 같습니다만, 함수가손 넣고 될 가능성을 고려해 우선 이대로라고 하는 일로
	;=================================================
	;이미삽입 하고 있었을 경우
	;=================================================
	;IF 구상기입 체크(0) && A_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1은 REACT분류:날뛴다취급인 것으로 고려하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가변화 하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을 담아 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT99, 항문섹스시킨다

;────────────────────────────────────
;REACT103, 역강간·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를 빌다
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통계 반응
;	COM:비명지른다/날뛴다/무서워한다
;성교계 반응
;	COM:허리를 흔든다
;봉사계 반응
;	COM:소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K13_103
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·다른성교봉사계와 달리, 죠교대상는 거절 할 수 없다
;·COM20, 허리를 흔든다는 REACT분류:받아들이는 것에 세트 된다
;·죠교대상가동정라면동정상실, 죠교자가처녀라면처녀상실가 일어난다
;·COM:싫어한다/날뛴다는 REACT분류:날뛴다(즉, 날뛰고가 통과하는 상황)는 되지 않는다
;-------------------------------------------------
;·역강간는 거동 및 COM의경향가 조금 다릅니다
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실
;━━━━━━━━━━━━━━━━━━━━━━━━
IF TCVAR:처녀상실
	;처녀 때의 공통 처리
	CALL FUNC_K13_LOST_VERGIN_TARGET()
	RETURN 1
ENDIF

;역강간는 거절 할 수 없기 때문에, 거절계의 분기는 설치하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
		IF !VAR_K13:30
;OriginalString : 「ああんっ❤　そんなに腰突き上げちゃって、もう出했다くてたまらないのね？」
			PRINTFORML 「아 응?   그렇게 허리 밀어 올려 버려서, 이제 내고 싶어서 견딜 수 없구나?」
;OriginalString : 「でもダメよ、まだ참다しなさいっ！」
			PRINTFORML 「그렇지만 안되어요, 아직 참아 주세요!」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 페니스の根本に尻尾をキツく巻きつけ、사정できないようにしてきた
			PRINTFORML %CALLNAME%(은)는 %CALLNAME:MASTER%의 페니스의 근본에 꼬리를 귤나무구 감아 사정할 수 없게 해 왔다
;OriginalString : 「もっと정액いっぱい溜めるまで、사정はおあずけ……❤　頑張ってキンタマ働かせるのよっ♪」
			PRINTFORMW 「 좀 더 정액 가득 모을 때까지, 사정은 맡기고……❤　노력해 불알 일하게 한다♪」
			;꼬리플래그
			VAR_K13:30 = 1
		ELSE
;OriginalString : 「ひゃんっ❤　はぁんっ❤　うんっ、イイわよ、その調子でもっと突き上げてぇっ！」
			PRINTFORML 「응?   후~응?   응, 이이원이야, 그 상태에서 좀 더 밀어 올려!」
;OriginalString : 「만족させてくれたら出させてあげるからぁっ、사정참다했다ガチガチちんぽで、もっとズコズコ突いてぇっ❤❤」
			PRINTFORMW 「만족시켜 주면 내게 해 주기 때문, 사정 참은 가치가치로, 좀 더 즈코즈코 찔러❤❤」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
		IF !VAR_K13:30
;OriginalString : 「あはっ、そんなに可愛い顔して、イキたい？　イキたいわよねぇ？」
			PRINTFORML 「아는, 그렇게 사랑스러운 얼굴 해, 이키 싶다?   이키 싶어요?」
;OriginalString : 「でもダーメ♪　まだ사정させてあげないっ♪」
			PRINTFORML 「 그렇지만 부인♪　아직 사정시켜 주지 않는다♪」
;OriginalString : %CALLNAME%は%CALLNAME:MASTER%의 페니스の根本に尻尾をキツく巻きつけ、사정できないようにしてきた
			PRINTFORML %CALLNAME%(은)는 %CALLNAME:MASTER%의 페니스의 근본에 꼬리를 귤나무구 감아 사정할 수 없게 해 왔다
;OriginalString : 「もっともっと、可愛い表情にしてあげる❤　사정했다くて泣いちゃうくらい、애태움てあげるからね❤」
			PRINTFORMW 「좀더 좀더, 사랑스러운 표정으로 해 준다❤　사정하고 싶어서 울어버릴 정도, 초조하게 해 주니까요❤」
			;꼬리플래그
			VAR_K13:30 = 1
		ELSE
;OriginalString : 「あんっ❤　はあんっ❤　イキたい？　정액いっぱい出했다い？」
			PRINTFORML 「팥고물?   는 팥고물?   이키 싶다?   정액 가득 내고 싶다?」
;OriginalString : 「でも私、사정참다させられて切なそうな%FUNC_K13_CALLNAME()%の表情見ると、すっごく興奮しちゃうのよね♪」
			PRINTFORML 「 그렇지만 나, 사정 인내 당해 안타까운 것 같은%FUNC_K13_CALLNAME()%의 표정 보면(자), 대단히 흥분해버린다 것이군요♪」
;OriginalString : 「だから……주인님が%FUNC_K13_CALLNAME()%のちんぽでオナニーするところ、ああんっ❤　黙って見てなさい❤」
			PRINTFORMW 「그러니까……남편님이%FUNC_K13_CALLNAME()%후 로 자위하는 곳(중), 아 응❤　입다물고 봐 주세요❤」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다(REACT분류:용서를 빌다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를 빌다")
	;파생 1의 경우는 REACT분류:없음
	;=================================================
	;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「何よ？　주인님がわざわざオマンコしてあげてるっていうのに、不満なの？」
		PRINTFORML 「뭐야?   남편님이 일부러 보지 해 주고 있다고 말하는데, 불만이야?」
;OriginalString : 「……건방짐なこと言うじゃない。そのちんぽ、今すぐズタズタに引き裂いてやろうかしら？」
		PRINTFORMW 「…… 건방진 일 말하잖아. 후, 금방 즈타즈타에 찢어 줄까?」
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「せっかく주인님がオマンコ貸してあげてるのに、何を馬鹿なこと言ってるのよ？」
		PRINTFORML 「모처럼 남편님이 보지 빌려 주어, 무엇을 바보같은 일 말해?」
		IF TALENT:MASTER:남자
;OriginalString : 「雄なら雄らしく、주인님に中出しすることだけ考えてパコパコ腰振ってなさい」
			PRINTFORMW 「수컷이라면 수컷인것 같고, 남편님에게 질내사정하는 것만 생각해 파코 파코허리 털어 주세요」
		ELSE
;OriginalString : 「せっかくそんなモノが付いてるんだから、솔직に사정することだけ考えてパコパコ腰振ってなさい」
			PRINTFORMW 「모처럼 그런 물건이 붙고 있기 때문에, 솔직하게 사정하는 것만 생각해 파코 파코허리 털어 주세요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「んぁっ❤　はぁんっ❤　こら、顔を隠すんじゃないの！」
		PRINTFORML 「응?   후~응?   이거 참, 얼굴을 숨기지 않아!」
;OriginalString : 「ほら、주인님の下で情けなくよがってる姿、ちゃんと주인님に見せなさいっ！」
		PRINTFORMW 「이봐요, 남편님아래에서 한심하고가 비치는 모습, 분명하게 남편님에게 보이게 하세요!」
		RETURN 1
	ENDIF
ENDIF

;역강간의 경우, 파생 1, REACT분류:날뛴다가 되는 일은 없기 때문에 고려하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 「あはっ❤　すっごくイイ表情してるわよ？」
	PRINTFORML 「아는?   대단히 이이 표정 하고 있는 원이야?」
;OriginalString : 「イイ顔見せてくれたご褒美にぃ……ああんっ❤　いっぱい腰振って、ちんぽ虐めてあげるっ❤」
	PRINTFORMW 「이이 얼굴 좀 보여준 포상에……아 응❤　가득 허리 털어, 학째라고 준다❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 「ああんっ❤　そんなに気持ち良いの？　ふふっ♪」」
	PRINTFORML 「아 응?   그렇게 기분 좋은거야?   후후♪」」
;OriginalString : 「それじゃあもっと격렬하게あげるっ❤　んぁっ、あっ❤　どう？　イイでしょ？」
	PRINTFORMW 「그러면 아 좀 더 격렬하게 해 준다?   응, 아?   어때?   이이겠지?」
	RETURN 1
ENDIF

;역강간의 경우는 REACT분류:받아들이는이 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 「あっ❤　ひゃあんっ❤　イイよぉ、もっと突き上げてぇっ！！」
	PRINTFORML 「아?   팥고물?   이이야, 좀 더 밀어 올려!!」
;OriginalString : 「私も、ほらぁっ❤　ナカをきゅって絞めてあげるからぁ、気持ちよーくお漏らししちゃいなさいっ❤」
	PRINTFORMW 「 나도, 이봐요❤　나카는 매어 주기 때문, 기분 잘 흘려버릴 없음 있고❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 고통을 참는다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("고통을 참는다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「고통에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;역강간의 경우 날뛰고가 통과하는 일은 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 2, 제지에 의한 날뛰고 실패 취급. 파생 1은 범용 리액션에
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다") && TFLAG:REACT파생 == 2
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다(REACT분류:용서를 빌다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT103, 역강간


;━━━━━━━━━━━━━━━━━━━━━━━━
;성봉시에거부했다경우의 공통 처리
;━━━━━━━━━━━━━━━━━━━━━━━━
@FUNC_K13_CANCEL_SEX
IF TALENT:처녀
;OriginalString : 「何よ！　주인님がヤラせてやるって言ってるのに、断るつもり！？」
	PRINTFORML 「뭐야!   남편님이 야라키라고 한다 라고 해, 거절할 생각!?」
;OriginalString : 「一体何のために、股間にちんぽをぶら下げてるつもりよっ！！」
	PRINTFORML 「도대체 무엇을 위해서, 고간를 매달고 있는 생각이야!!」
;OriginalString : 「せっかく私が……！」
	PRINTFORMW 「모처럼 내가……!」
	TCVAR:상태변화 = 8
	TCVAR:상태변화지속 = 6
	BASE:이성 = MAX(0, BASE:이성 - 800)
	BASE:초조함 = MIN(MAXBASE:초조함, BASE:초조함 + 800)
ELSE
;OriginalString : 「何よ！　주인님がヤラせてやるって言ってるのに、断るつもり！？」
	PRINTFORML 「뭐야!   남편님이 야라키라고 한다 라고 해, 거절할 생각!?」
;OriginalString : 「一体何のために、股間にちんぽをぶら下げてるつもりよっ！！　使えないペットね！」
	PRINTFORMW 「도대체 무엇을 위해서, 고간를 매달고 있는 생각이야!!   사용할 수 없는 애완동물!」
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성봉로죠교자가처녀상실했다때의 공통 처리
;━━━━━━━━━━━━━━━━━━━━━━━━
@FUNC_K13_LOST_VERGIN_TARGET
;OriginalString : 「…………っ！」
PRINTFORMW 「…………!」
;OriginalString : %CALLNAME:MASTER%의 페니스를 咥え込んだ%CALLNAME%の膣からは、처녀の証である純潔の血が流れ落ちた……
PRINTFORMW %CALLNAME:MASTER%의 페니스를 질네 붐비었다%CALLNAME%의 질로부터는, 처녀의 증거인 순결의 피가 흐르고 떨어졌다……
;OriginalString : 
PRINTL 
;OriginalString : 「んっ、く……はぁ……」
PRINTFORMW 「응, 구……후~……」
;OriginalString : 「……ちょっと……何をボサッとしてるのよ」
PRINTFORMW 「……조금……무엇을 보삭궴 해」
;OriginalString : 「주인님の命令よ。さっさと腰を振って……そのちんぽで私のナカを突きなさい」
PRINTFORMW 「남편님의 명령이야. 빨리 허리를 털어……후 로 나의 나카를 찌르세요」
;OriginalString : %CALLNAME%は平然と振마이おうとしているようだが、破瓜の고통を隠せずにいる
PRINTFORMW %CALLNAME%(은)는 태연하게 행동하려 하고 있는 것 같지만, 파과의 아픔을 숨기지 못하고 있다
VAR_K13:9 = 1
