;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코구상 가학系アクション구상ファイル
;
;	가학系アクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;가학系 ACT60～68
;========================================================================
;・TFLAG:ACT(60=스펭킹/61=채찍/62=바늘/63=밧줄/64=눈가리개/65=볼개그/66=매도/67=삼각목마/68=관장세트)
;・TFLAG:ACT분류 == 6に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "가학")で参照する事が可能
;・TEQUIP系は基本、ACT開始時点で장착される
;・ACTによっては죠교대상の反応次第で취소される事がある
;・밧줄/눈가리개/볼개그/삼각목마はTEQUIPで관리され当該ACT/REACT以外でもTEQUIP:밧줄等で参照できる
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では구상側のRESULT(終端のRETURN 1など)によって구상が表示당했다のか否か、
;・また箇所毎の特有の挙動(미약等구상をスキップなど)を制御しています
;・@구상記入チェックの場合代入処理を省略できますが、RESULT値は구상側で決めうちする필요があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------

;────────────────────────────────────
;ACT60, 스펭킹
;────────────────────────────────────
;・죠교자が죠교대상の尻を平손で叩く
;・ACT開始時点ですでに叩いている
;・성교봉사중には発生しない
;・안면승마系は解除される
;・죠교자が자위중である場合は解除される
;-------------------------------------------------
@KOJO_MALE_ACT_K44_60
;-------------------------------------------------
;・ACTの派生なし
;-------------------------------------------------
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「네？ 아픈 것은 조금은？」
;OriginalString : 「大丈夫よ、それが良くなるから%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「괜찮아요, 그것이 좋아지기 때문에%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;죠교대상のマゾッ気3以上かつ50%で真
	;=================================================
	IF 구상記入チェック(50) && ABL:MASTER:마조끼 >= 3 ;//마조끼 >= 3の場合でも1/2の確率で下層に流す
		SELECTCASE ABL:MASTER:마조끼
			;마조끼5以上
			CASE IS >= 5
				PRINTFORMW 「마침내 기쁨응으로 구멍 모습이면서 산등성이다 시작하게 되었군요…」
				PRINTFORMW 「부끄럽지 않은 것일까！ 이 돼지가！♪」
			;마조끼3以上
			CASEELSE
				PRINTFORMW 「자기, 점점 얻어맞는 것 버릇이 되어 오지 않았다…？」
				PRINTFORMW 「숨겨도 쓸데없으니까！ 들키고 들키고야！♪」
		ENDSELECT
		RETURN 1
	ENDIF

	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		;고통중독による派生
		SELECTCASE ABL:MASTER:고통중독
			;고통중독5以上
			CASE IS > 5
				PRINTFORMW 「그렇게 괴로운 눈으로 나를 보다니…」
				PRINTFORMW 「이 변태！ 드 변태！！ 변태 어른！！！」
			;고통중독3以上
			CASE IS > 3
				PRINTFORMW 「최근 이이소리로 울잖아…♪」
				PRINTFORMW 「구멍 얻어맞고 느껴 버린다의！ 이 철면피！♪」
			;고통중독1以上
			CASE IS > 1
				PRINTFORMW 「점점 아픔안에 다른 감각이 보여 왔지 않는다？」
				PRINTFORMW 「기분이 좋은 것 하！ 지금부터！」
			CASEELSE
				PRINTFORMW 「이봐요 구멍 내세요」
				PRINTFORMW 「조금(뿐)만 예의 범절을 해 준다…♪」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT61, 채찍
;────────────────────────────────────
;・죠교자が채찍を大きく振り上げ思いっきり振り下ろす
;・ACT開始時点ですでに叩いている
;・턴방침が"소프트"だと発生しない
;・성교중/성교봉사중には発生しない
;-------------------------------------------------
@KOJO_MALE_ACT_K44_61
;-------------------------------------------------
;・ACT파생なし
;-------------------------------------------------
IF 구상記入チェック(100) && !TFLAG:ACT파생
	PRINTFORMW 「\@ FIRSTACT(0) ? 그래서 도대체 무엇 # 또 이것으로 \@하는지는？ 정해지고 있잖아…」
	PRINTFORMW 「채찍의 용도는！ 이외！ \@ FIRSTACT(1) ? 있다 # 생각난다 \@수월하지 않다！！」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT62, 바늘
;────────────────────────────────────
;・乳首と성기に바늘を突き刺す
;・ACT開始時点ですでに刺している
;・죠교대상に페니스がある場合は페니스に、無い場合は클리토리스に突き刺す
;・가슴と성기(페니스/바기나)から出血し、血液汚れが追加される
;・턴방침が"소프트", "노멀"だと発生しない
;・성교중/성교봉사중には発生しない
;・안면승마系、자위系は解除される
;-------------------------------------------------
@KOJO_MALE_ACT_K44_62
;-------------------------------------------------
;・ACTの派生なし
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;ACT63, 밧줄
;────────────────────────────────────
;・죠교대상を밧줄で縛ろうとする
;・地の文は"縛ろうと했다…"となっているが処理的にはACT開始時点で縛られている
;・TEQUIP:밧줄 で縛りの強度を参照できる
;・既に装備していた場合は解除になる
;・既に장착済み、かつTFLAG:징계・플래그が立っている場合は発生しない(即ち、解除されない)
;・TEQUIP:밧줄自体はREACT분류が"용서를 빌다"の場合に解除される
;・0616版時点の仕様でこの挙動を確実に参照する손段なし。本体側の改善が求められる
;・(REACT63以外でも취소処理になる場合がある為)
;・성교중/성교봉사중には発生しない
;・구속時に자위や안면승마、죠교대상의 페니스사용중は解除される
;-------------------------------------------------
@KOJO_MALE_ACT_K44_63
;-------------------------------------------------
;・TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;ACT64, 눈가리개
;────────────────────────────────────
;・죠교자が눈가리개を着けさせようとする
;・地の文では"着けさせようと했다…"となっているが処理上はACT開始時点で장착されている
;・既に装備していた場合は解除になる
;・既に장착済み、かつTFLAG:징계・플래그が立っている場合は発生しない(即ち、解除されない)
;・TEQUIP:눈가리개はREACT분류が"용서를 빌다"であった場合に解除される
;・0616版時点の仕様でこの挙動を確実に参照する손段なし。本体側の改善が求められる
;・(REACT64以外でも취소処理になる場合がある為)
;・성교중/성교봉사중には発生しない
;・안면승마系は解除される
;・죠교자が자위중であった場合は解除される
;-------------------------------------------------
@KOJO_MALE_ACT_K44_64
;-------------------------------------------------
;・TFLAG:ACT派性(0=장착/1=解除)
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;ACT65, 볼개그
;────────────────────────────────────
;・죠교자が볼개그を嵌めさせようとする
;・地の文では"嵌めさせようと했다…"となっているが処理上はACT開始時点で장착されている
;・既に装備していた場合は解除になる
;・既に장착済み、かつTFLAG:징계・플래그が立っている場合は発生しない(即ち、解除されない)
;・TEQUIP:볼개그はREACT분류が"용서를 빌다"であった場合に解除される
;・0616版時点の仕様でこの挙動を※確実に参照する손段なし。本体側の改善が求められる
;・※REACT64以外でも취소処理になる場合がある為
;・턴방침が"소프트", "노멀"の場合は発生しない
;・성교중/성교봉사중には発生しない
;・안면승마系は解除される
;・죠교자が자위중であった場合は解除される
;-------------------------------------------------
@KOJO_MALE_ACT_K44_65
;-------------------------------------------------
;・TFLAG:ACT派性(0=장착/1=解除)
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;ACT66, 매도
;────────────────────────────────────
;・挑発するように죠교대상の痴態を声に出す
;・회화系から発生する場合もある
;・턴방침が"소프트"だと発生しない
;・회화系から派生する場合もある
;-------------------------------------------------
@KOJO_MALE_ACT_K44_66
;-------------------------------------------------
;・ACT파생なし
;-------------------------------------------------
IF 구상記入チェック(100) && !TFLAG:ACT파생
	PRINTFORMW 「대체로 나같은 미발들 바디에 마구 흥분해 부끄럽지 않은 것？」
	PRINTFORMW 「개의 로리타！ 로리타！ 쓰레기！ 로리타！！」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT67, 삼각목마
;────────────────────────────────────
;・죠교대상を木馬に乗せようとする
;・地の文は"乗せようと했다…"となっているが処理上はACT開始時点で乗せられている
;・実は쾌Ｃではなく쾌Ｖが入る(生えてる木馬！？)
;・MASTERが처녀なら、처녀상실が起こる(@ACTION_APPLY2で処理。REACT분류が용서를 빌다の場合は回避される)
;・죠교대상が남자の場合は発生しない
;・턴방침が"소프트", "노멀"時には発生しない
;・TEQUIP(바이브/항문바이브/항문비즈/관장그릇＋플러그/페니스밴드)장착時には発生しない
;・성교중/성교봉사중には発生しない
;・야외플레이時には発生しない
;・밀어넘어지는중には発生しない
;・이상系から発生する場合もある
;・既に装備していた場合は解除になる
;・既に장착済み、かつTFLAG:징계・플래그が立っている場合は発生しない(即ち、解除されない)
;・TEQUIP:삼각목마はREACT분류が"용서를 빌다"であった場合に解除される
;・0616版時点の仕様でこの挙動を※確実に参照する손段なし。本体側の改善が求められる
;・※REACT67以外でも취소処理になる場合がある為
;-------------------------------------------------
@KOJO_MALE_ACT_K44_67
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;ACT68, 관장세트
;────────────────────────────────────
;・관장液の注入/追加/排出
;・最初の注入時(ACT파생=0)は量1～3。追加時(ACT파생=2)は量1～4。それぞれランダムで処理される
;・;既に装備している場合、관장液追加になる
;・관장液が5本入っている場合、관장プラグ解除になる
;・관장液の分量はTEQUIP:관장그릇＋플러그で参照できる
;・TEQUIP(항문바이브/항문비즈/페니스밴드/삼각목마)장착時には発生しない
;・성교중/성교봉사중には発生しない
;・턴방침が"소프트"だと発生しない
;・도구系から派生する場合もある
;-------------------------------------------------
@KOJO_MALE_ACT_K44_68
;-------------------------------------------------
;・TFLAG:ACT파생(0=장착/1=解除/2=관장液追加)
;-------------------------------------------------
RETURN 0


[SKIPSTART]
R#私家版に存在していたコマンド
;-------------------------------------------------
;ACT69, 首絞め FIRSTTIME(169)
;-------------------------------------------------
@KOJO_MALE_ACT_K44_69
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	PRINTFORMW 「네？어째서 목 매고 있는지는？」
	PRINTFORMW 「어째서이겠지-군요♪」
	RETURN LOCAL
ENDIF
[SKIPEND]
