;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코구상 이상계 액션구상파일
;
;	이상계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;이상계 ACT70～75
;========================================================================
;·TFLAG:ACT(70=피스트퍽/71=애널피스트/72=양쪽피스트/73=방치플레이/74=공중육변기플레이/75=페니스밴드장착)
;·TFLAG:ACT분류 == 7에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "이상")로 참조하는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT70, 피스트퍽
;────────────────────────────────────
;·죠교대상의 질에 주먹을 넣는다
;·ACT 개시시점으로써손를 내고 있다
;·죠교대상가처녀인 경우는처녀상실가 된다(@ACTION_APPLY2_70으로 처리된다)
;·죠교대상이남자인 경우는 발생하지 않는다
;·턴방침가"소프트", "노멀"의 경우는 발생하지 않는다
;·오늘의방침가"노닥노닥"의 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계, 자위계는 해제된다
;-------------------------------------------------
;@KOJO_MALE_ACT_K44_70
;-------------------------------------------------
;남자한정구상에서는 불요
;RETURN 0

;────────────────────────────────────
;ACT71, 애널피스트
;────────────────────────────────────
;·죠교대상의엉덩이에 주먹을 넣는다
;·ACT 개시시점으로써손를 내고 있다
;·죠교대상이남자인 경우는 발생하지 않는다
;·턴방침가"소프트", "노멀"의 경우는 발생하지 않는다
;·오늘의방침가"노닥노닥"의 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_71
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
;OriginalString : 「オラァ！！」
	PRINTFORMW 「오라!!」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT72, 양쪽피스트
;────────────────────────────────────
;·죠교대상의 질과엉덩이에 주먹을 넣는다
;·ACT 개시시점으로써손를 내고 있다
;·죠교대상가처녀인 경우는처녀상실가 된다(@ACTION_APPLY2_72로 처리된다)
;·죠교대상이남자인 경우는 발생하지 않는다
;·턴방침가"소프트", "노멀", "하드"의 경우는 발생하지 않는다
;·오늘의방침가"노닥노닥"의 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마계, 자위계는 해제된다
;-------------------------------------------------
;@KOJO_MALE_ACT_K44_72
;-------------------------------------------------
;남자한정구상에서는 불요
;RETURN 0

;────────────────────────────────────
;ACT73, 방치플레이
;────────────────────────────────────
;·죠교자가죠교대상를방치 한다
;·턴방침가"소프트", "노멀", "이상"라면 발생하지 않는다
;·(이상계 ACT로턴방침"이상"로 연주하고 있는 것은 미스라고 생각되고)
;·금일의방침가"노닥노닥"의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·수치계로부터 발생하는 경우도 있다(v1. 010 현재, 수치계로부터 파생했다경우에게만 발생이라고 생각되고)
;·안면승마계는 해제된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_73
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
;OriginalString : 「じーっ」
	PRINTFORMW 「글자-」
;OriginalString : 「にへらにへら♪」
	PRINTFORMW 「에에들에게에들♪」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT74, 공중육변기플레이
;────────────────────────────────────
;·속박된죠교대상를 밖에방치 한다
;·방치 되어 발견당했다경우죠교대상가처녀이면처녀상실. 동정의 경우는동정상실가 된다
;·밧줄와개목걸이가 갖추어지지 않았다고 발생하지 않는다
;·턴방침가"소프트", "노멀", "하드"의 경우는 발생하지 않는다
;·금일의방침가"노닥노닥"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·REACT분류가"용서를 빌다"였던 경우는취소 된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_74
;-------------------------------------------------
;·TFLAG:ACT파생(0=무사/1=애원/2=능욕/3=흥분)
;·죠교대상의 반응과 확률로 REACT 이후(ACTION_APPLY2)에 파생한다
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;ACT75, 페니스밴드장착
;────────────────────────────────────
;·죠교자가 쌍두페니스밴드를 몸에 대어, 죠교대상에 과시한다
;·죠교자가처녀인 경우처녀상실가 된다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교자에페니스가 있는 경우에는 발생하지 않는다
;·이미장착 하고 있었을 경우는 해제가 된다
;·안면승마계는 해제된다
;-------------------------------------------------
;@KOJO_MALE_ACT_K44_75
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;남자한정구상에서는 불요
;RETURN 0

[SKIPSTART]
R#사가판에 존재하고 있던 커맨드
현재는 죽어구상인 것으로 comment out@/L
;-------------------------------------------------
;ACT75, 깨물기 FIRSTTIME(175)
;-------------------------------------------------
@KOJO_MALE_ACT_K44_75
;-------------------------------------------------
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
;OriginalString : 「먹어 버리고 싶을 정도 사랑스럽다고는 말하지만」
	PRINTFORMW 「먹어버릴 싶을 정도  사랑스럽다고는 말하는데」
;OriginalString : 「정말 먹으면 어떨까…」
	PRINTFORMW 「정말로 먹으면 어떤가 해들…」
	RETURN LOCAL
ENDIF

;-------------------------------------------------
;ACT76, 짓밟아 FIRSTTIME(176)
;-------------------------------------------------
@KOJO_MALE_ACT_K44_76
;-------------------------------------------------
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
;OriginalString : 「이런 식으로짓밟아라고…」
	PRINTFORMW 「개응인 바람으로 짓밟을 수 있어…」
;OriginalString : 「포상이라면인가 말해 버릴까! 당신도!」
	PRINTFORMW 「포상이라면인가 말해버리는의 것인지 해들! 당신도!」
	RETURN LOCAL
ENDIF
[SKIPEND]
