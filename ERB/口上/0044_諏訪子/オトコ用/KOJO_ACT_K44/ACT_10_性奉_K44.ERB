;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코구상 성교봉사계 액션구상파일
;
;	성교봉사계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;성교봉사계 ACT95～103
;========================================================================
;·TFLAG:ACT(95=정상위시킨다/96=후배위시킨다/97=대면좌위시킨다/98=배면좌위시킨다/99=항문섹스시킨다/103=역강간)
;·TFLAG:ACT분류 == 10에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "성봉")로 참조하는 것이 가능
;·원칙으로서죠교대상→죠교자의삽입가 된다
;·체위의 제휴에 의해 TFLAG:ACT파생로 파생한다
;· 각 ACT 에 있어서 주도권은@SEX_INITIATIVE에서 정의된다
;-------------------------------------------------
;※성교중, 성교봉사중로부터의 파생에 대해…
;  일부의 ACT파생는 ACT_VARIATION로 정의되면서도, ACT_ABLE로 튕겨지고 있는 현상입니다
;  것에 따라서는 현재의 사양 자체가 이상한 생각도 듭니다만, 이 건에 대한 본체 측에서의 회답을 얻어지고 있지 않습니다
;  머지않아손가 들어갈 가능성도 있으므로, 주석을 적은 다음 테두리는 마련해 둡니다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT95, 정상위시킨다
;────────────────────────────────────
;·죠교자가죠교대상에정상위에서의삽입를 명한다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상가누움, 한편성교봉사중가 아닌 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·배면좌위에서의성교봉사중로부터는 파생하지 않는다
;·Ａ성교봉사중로부터는 파생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;·REACT분류가"날뛴다", "거부"였던 경우는취소 된다
;·죠교대상가동정라면동정상실가 일어난다
;·죠교자가처녀라면처녀상실가 일어난다
;·이것들의 처리는@ACTION_APPLY2_95에서 행해진다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_95
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;※v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT95, 정상위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기
;·각파생내에서애태움도, 페니스밴드, 가버려, 죠교자의속옷의 유무등으로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;죠교대상가동정
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:동정 && FIRSTTIME(195)
		CALL HTMLPRINTW(@"「붓 내려 시켜 주세요는 부탁한다면 넣게 해 주지 않는 것도 아니야%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「…거짓말 거짓말%HTML_HEART()% 그러면 장소 안다? 코코에 분명하게 들어갈 수 있는거야%HTML_HEARTB()% 」")
		CALL HTMLPRINTW(@"「읏%HTML_HEARTB()% 이제 펄떡펄떡이 아니다…%HTML_HEART()% 넣은 순간사정했다들 용납하지 않아요?」")

		;//현재의 사양에서는 리액션에 따라서는 거절 취급이 되어, 동정상실가 되지 않기 때문에 comment out
		;CALL HTMLPRINTW(@" 「츄푸… 응%HTML_HEARTB()%  들어갔다아…%HTML_HEARTB()%」")
		;CALL HTMLPRINTW(@" 「무엇 번민이라는거야, 아직 끝 쪽 뿐이 아닌%HTML_HEARTB()%  후~응%HTML_HEARTB()%… 이것으로 전부%HTML_HEARTB()%」")
		;CALL HTMLPRINTW(@" 「분명하게 친숙해 지게 해… 응, 딱딱한…%HTML_HEARTB()%  , 빨리 움직여 %HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(100) && TFLAG:안빼고
		CALL HTMLPRINTW(@"「하아읏%HTML_HEARTB()% 그래! 그 상태%HTML_HEART()%」")
		CALL HTMLPRINTW(@"「딱딱한 것 가득 와앗…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;//원래의 IF TFLAG:ACT파생(상황 묘사) 개소는 조건마다 절단 분리@/L
	;=================================================
	;죠교대상가가버려(ACT파생 1 혹은 2의 경우)
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려 && INRANGE(TFLAG:ACT파생, 1, 2)
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				IF RAND:2
					CALL HTMLPRINTW(@"「야읏…%HTML_HEARTB()% 나카로 굉장한 부풀어 오르고 있다아%HTML_HEARTB()%」")
					CALL HTMLPRINTW(@"「좀 더, 간바는참다하세요…%HTML_HEARTB()%」")
				ELSE
					PRINTFORMW 「이 기초…? 이제 사정의…?」
					CALL HTMLPRINTW(@"「안돼에…아직도 지금부터인 것이니까%HTML_HEARTB()%」")
				ENDIF
			;애태움없음
			CASEELSE
				CALL HTMLPRINTW(@"「앗%HTML_HEARTB()% 핫%HTML_HEARTB()% 좋아…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「좀더 좀더…%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF

	;=================================================
	;연속(전회ACT가정상위시킨다)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		CALL HTMLPRINTW(@"「와아…%HTML_HEARTB()% 자지 좀 더…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「가득 가득 와아…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		CALL HTMLPRINTW(@"「읏%HTML_HEART()% 역시 여기%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「아하아…%HTML_HEARTB()% 좋다…%HTML_HEART()% 좀 더 가득 해에%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;Ａ성교로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 3
	;ENDIF

	;=================================================
	;Ｖ성교계로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
	;ENDIF

	;=================================================
	;Ａ성교봉사로부터
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 5
		CALL HTMLPRINTW(@"「엉덩이도 좋지만…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「역시 여기%HTML_HEART()% 가득 즈코즈코 해에…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「응…이제…」
		PRINTFORMW 「자지… 삽입싶고 없다?」
		CALL HTMLPRINTW(@"「좋아 이봐요…와…%HTML_HEARTB()%」")
		PRINTFORMDW 스와자는 음순을 열거나 닫거나 하면서 유혹하고 있다…
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT96, 후배위시킨다
;────────────────────────────────────
;·죠교자가죠교대상에후배위에서의삽입를 명한다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상가누움, 한편성교봉사중가 아닌 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·대면좌위에서의성교봉사중로부터는 파생하지 않는다
;·Ａ성교봉사중로부터는 파생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;·REACT분류가"날뛴다", "거부"였던 경우는취소 된다
;·죠교대상가동정라면동정상실가 일어난다
;·죠교자가처녀라면처녀상실가 일어난다
;·이것들의 처리는@ACTION_APPLY2_96에서 행해진다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_96
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;※v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT96, 후배위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기
;·각파생내에서애태움도, 페니스밴드, 가버려, 죠교자의속옷의 유무등으로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;죠교대상가동정
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:동정 && FIRSTTIME(195)
		CALL HTMLPRINTW(@"「붓 내려 시켜 주세요는 부탁한다면 넣게 해 주지 않는 것도 아니야%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「…거짓말 거짓말%HTML_HEART()% 그러면 장소 안다? 코코에 분명하게 들어갈 수 있는거야%HTML_HEARTB()% 」")
		CALL HTMLPRINTW(@"「읏%HTML_HEARTB()% 이제 펄떡펄떡이 아니다…%HTML_HEART()% 넣은 순간사정했다들 용납하지 않아요?」")

		;//현재의 사양에서는 리액션에 따라서는 거절 취급이 되어, 동정상실가 되지 않기 때문에 comment out
		;CALL HTMLPRINTW(@" 「츄푸… 응%HTML_HEARTB()%  들어갔다아…%HTML_HEARTB()%」")
		;CALL HTMLPRINTW(@" 「무엇 번민이라는거야, 아직 끝 쪽 뿐이 아닌%HTML_HEARTB()%  후~응%HTML_HEARTB()%… 이것으로 전부%HTML_HEARTB()%」")
		;CALL HTMLPRINTW(@" 「분명하게 친숙해 지게 해… 응, 딱딱한…%HTML_HEARTB()%  , 빨리 움직여 %HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(100) && TFLAG:안빼고
		CALL HTMLPRINTW(@"「응아앗%HTML_HEARTB()% 굉장하다아…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「좀 더 자지 찔러어…%HTML_HEARTB()% 앗%HTML_HEARTB()% 앗%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;죠교대상가가버려(ACT파생 1 혹은 2의 경우)
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려 && INRANGE(TFLAG:ACT파생, 1, 2)
		;참다플래그가 서 있는 경우
		IF TCVAR:MASTER:참다
			CALL HTMLPRINTW(@"「좋아…%HTML_HEARTB()% 가득 자제 해에…%HTML_HEART()%」")
			CALL HTMLPRINTW(@"「가득 찔러 가득사정해엣…%HTML_HEARTB()%」")
			RETURN 1
		ENDIF

		;애태움중의 경우
		IF TCVAR:MASTER:애태움도 > 1
			CALL HTMLPRINTW(@"「자지 떨리고 있닷%HTML_HEARTB()% 사정의?」")
			PRINTFORMW 「진한 정액 가득 사정해버린다의?」
		;애태움없음
		ELSE
			CALL HTMLPRINTW(@"「아하아읏%HTML_HEARTB()% 깊고 좋다…%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「좀 더 좀더 좀더%HTML_HEARTB()%」")
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;연속(전회ACT가후배위시킨다)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		CALL HTMLPRINTW(@"「후앗%HTML_HEARTB()% 읏%HTML_HEARTB()% 좋앗%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「교미하고 싶은 좀 더 허리 흔들어어%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		CALL HTMLPRINTW(@"「이번은…여기%HTML_HEART()% 뒤로부터%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「앗%HTML_HEARTB()% 가득 즈코즈코 해에%HTML_HEART()%」")
		RETURN 1
	ENDIF

	;=================================================
	;Ａ성교로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 3
	;ENDIF

	;=================================================
	;Ｖ성교계로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
	;ENDIF

	;=================================================
	;Ａ성교봉사로부터
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 5
		PRINTFORMW 「엉덩이도 좋지만…」
		CALL HTMLPRINTW(@"「이번은 여기로%HTML_HEARTB()% 가득 찔러%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		CALL HTMLPRINTW(@"「이봐요빨리%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「뒤로부터 와아…%HTML_HEART()% 교미하고 싶은으로 합시다…%HTML_HEARTB()%」")
		PRINTFORMDW 스와자는 귀여운 엉덩이를 후리 후리 시키면서 유혹해 왔다…
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT97, 대면좌위시킨다
;────────────────────────────────────
;·죠교자가죠교대상에대면좌위에서의삽입를 명한다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상가누움, 한편성교봉사중가 아닌 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·배면좌위에서의성교봉사중로부터는 파생하지 않는다
;·Ａ성교봉사중로부터는 파생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;·REACT분류가"날뛴다", "거부"였던 경우는취소 된다
;·죠교대상가동정라면동정상실가 일어난다
;·죠교자가처녀라면처녀상실가 일어난다
;·이것들의 처리는@ACTION_APPLY2_97에서 행해진다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_97
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;※v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT97, 대면좌위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기
;·각파생내에서애태움도, 페니스밴드, 가버려, 죠교자의속옷의 유무등으로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;죠교대상가동정
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:동정 && FIRSTTIME(195)
		CALL HTMLPRINTW(@"「붓 내려 시켜 주세요는 부탁한다면 넣게 해 주지 않는 것도 아니야%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「…거짓말 거짓말%HTML_HEART()% 그러면 장소 안다? 코코에 분명하게 들어갈 수 있는거야%HTML_HEARTB()% 」")
		CALL HTMLPRINTW(@"「읏%HTML_HEARTB()% 이제 펄떡펄떡이 아니다…%HTML_HEART()% 넣은 순간사정했다들 용납하지 않아요?」")

		;//현재의 사양에서는 리액션에 따라서는 거절 취급이 되어, 동정상실가 되지 않기 때문에 comment out
		;CALL HTMLPRINTW(@" 「츄푸… 응%HTML_HEARTB()%  들어갔다아…%HTML_HEARTB()%」")
		;CALL HTMLPRINTW(@" 「무엇 번민이라는거야, 아직 끝 쪽 뿐이 아닌%HTML_HEARTB()%  후~응%HTML_HEARTB()%… 이것으로 전부%HTML_HEARTB()%」")
		;CALL HTMLPRINTW(@" 「분명하게 친숙해 지게 해… 응, 딱딱한…%HTML_HEARTB()%  , 빨리 움직여 %HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(100) && TFLAG:안빼고
		CALL HTMLPRINTW(@"「응츗%HTML_HEARTB()% 핫%HTML_HEARTB()% 핫%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「좀 더 움직여…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;죠교대상가가버려(ACT파생 1 혹은 2의 경우)
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려 && INRANGE(TFLAG:ACT파생, 1, 2)
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				CALL HTMLPRINTW(@"「이제(벌써)…%HTML_HEARTB()% 좀 더 노력해요…%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「모처럼 기분이 좋기 때문에%HTML_HEARTB()%」")
			;애태움종료
			CASE 1
				CALL HTMLPRINTW(@"「가버려(이)가 되어 버렸다아? 좋아%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「사정했다실 나무에 가…%HTML_HEART()% 사정용수철%HTML_HEARTB()%」")
			;애태움없음
			CASEELSE
				CALL HTMLPRINTW(@"「이렇게 해 얼싸안고 있는 것은…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「굉장히 좋지요…읏%HTML_HEARTB()% 히웃%HTML_HEART()%」")
		ENDSELECT
		RETURN 1
	ENDIF

	;=================================================
	;연속(대면좌위시킨다로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		CALL HTMLPRINTW(@"「응웃…%HTML_HEARTB()% 좀 더 동글동글 햇%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「가득 휘저어…%HTML_HEART()%」")
		RETURN 1
	ENDIF

	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		CALL HTMLPRINTW(@"「포옹해 이대로삽입해에…%HTML_HEART()%」")
		CALL HTMLPRINTW(@"「읏%HTML_HEARTB()% 얼굴 가깝다…%HTML_HEARTB()%츄」")
		RETURN 1
	ENDIF

	;=================================================
	;Ａ성교로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 3
	;ENDIF

	;=================================================
	;Ｖ성교계로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
	;ENDIF

	;=================================================
	;Ａ성교봉사로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 5
	;ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(2, 10)
			CASE 0
				CALL HTMLPRINTW(@"「저기…이대로 안아…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「읏…그래, 그대로 들어갈 수 있어…응앗%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「움직이기 힘들다고…? 거기는 노력해%HTML_HEARTB()%」")
			CASE 1
				;대면좌위항문로부터 개수 후 회수
				CALL HTMLPRINTW(@"「그런데, 포옹해에…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「응아앗%HTML_HEARTB()% 들어 온닷%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT98, 배면좌위시킨다
;────────────────────────────────────
;·죠교자가죠교대상에배면좌위에서의삽입를 명한다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상가누움, 한편성교봉사중가 아닌 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·정상위, 대면좌위에서의성교봉사중로부터는 파생하지 않는다
;·Ａ성교봉사중로부터는 파생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;·REACT분류가"날뛴다", "거부"였던 경우는취소 된다
;·죠교대상가동정라면동정상실가 일어난다
;·죠교자가처녀라면처녀상실가 일어난다
;·이것들의 처리는@ACTION_APPLY2_98에서 행해진다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_98
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;※v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT98, 배면좌위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기
;·각파생내에서애태움도, 페니스밴드, 가버려, 죠교자의속옷의 유무등으로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;죠교대상가동정
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:동정 && FIRSTTIME(195)
		CALL HTMLPRINTW(@"「붓 내려 시켜 주세요는 부탁한다면 넣게 해 주지 않는 것도 아니야%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「…거짓말 거짓말%HTML_HEART()% 그러면 장소 안다? 코코에 분명하게 들어갈 수 있는거야%HTML_HEARTB()% 」")
		CALL HTMLPRINTW(@"「읏%HTML_HEARTB()% 이제 펄떡펄떡이 아니다…%HTML_HEART()% 넣은 순간사정했다들 용납하지 않아요?」")

		;//현재의 사양에서는 리액션에 따라서는 거절 취급이 되어, 동정상실가 되지 않기 때문에 comment out
		;CALL HTMLPRINTW(@" 「츄푸… 응%HTML_HEARTB()%  들어갔다아…%HTML_HEARTB()%」")
		;CALL HTMLPRINTW(@" 「무엇 번민이라는거야, 아직 끝 쪽 뿐이 아닌%HTML_HEARTB()%  후~응%HTML_HEARTB()%… 이것으로 전부%HTML_HEARTB()%」")
		;CALL HTMLPRINTW(@" 「분명하게 친숙해 지게 해… 응, 딱딱한…%HTML_HEARTB()%  , 빨리 움직여 %HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(100) && TFLAG:안빼고
		CALL HTMLPRINTW(@"「야읏…%HTML_HEARTB()% 하읏%HTML_HEARTB()% 좀 더…%HTML_HEART()%」")
		CALL HTMLPRINTW(@"「조금 전부터 몸 너무 더듬어요 이 호색가%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;죠교대상가가버려(ACT파생 1 혹은 2의 경우)
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려 && INRANGE(TFLAG:ACT파생, 1, 2)
		;참다플래그있는 곳개애태움중이 아닌 경우
		IF TCVAR:MASTER:참다 && !TCVAR:MASTER:애태움도
			CALL HTMLPRINTW(@"「%HTMLFONT("개", , 0xFFFFFF)%%HTML_HEARTB()% 아앙, 빠져 버렸다아%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「굉장하다사정했다있고의 자제 하고 있네요…좀 더만 보류군요%HTML_HEARTB()% %HTMLFONT("", , 0xFFFFFF)%%HTML_HEARTB()%」")
			RETURN 1
		ENDIF

		;애태움도에 의한 파생을 본다
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				CALL HTMLPRINTW(@"「굉장한 움찔움찔 하고 있다아…%HTML_HEARTB()% 사정했다있고의?」")
				CALL HTMLPRINTW(@"「도-조금 힘내라 없을까?」")
			;애태움없음
			CASEELSE
				CALL HTMLPRINTW(@"「읏%HTML_HEARTB()% 좀 더 밀어올려%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「몸이 뜬다…%HTML_HEARTB()% 햐웃%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF

	;=================================================
	;연속(전회ACT가배면좌위시킨다)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		CALL HTMLPRINTW(@"「아앗%HTML_HEART()% 좋앗%HTML_HEARTB()% 그대로」")
		CALL HTMLPRINTW(@"「허리 가지고 휘저어엇%HTML_HEARTB()% 응하앗%HTML_HEART()%」")
		RETURN 1
	ENDIF

	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		CALL HTMLPRINTW(@"「뒤로부터 이렇게 해 넣어…%HTML_HEART()%」")
		CALL HTMLPRINTW(@"「그래…그대로 아래로부터 밀어올려어%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;Ａ성교로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 3
	;ENDIF

	;=================================================
	;Ｖ성교계로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
	;ENDIF

	;=================================================
	;Ａ성교봉사로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 5
	;ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(2, 10)
			CASE 0
				CALL HTMLPRINTW(@"「뒤로부터 안아…%HTML_HEARTB()% 후훗, 가볍지요%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「그대로 자지 넣어…후앗%HTML_HEARTB()% 그래…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「그대로 가득 밀어올려엇…%HTML_HEARTB()%」")
			CASE 1
				;배면좌위항문로부터 개수 후 회수
				CALL HTMLPRINTW(@"「묻는다♪후후, 뒤로부터 안아?」")
				CALL HTMLPRINTW(@"「이 몸의 자세 들어가 버린다…햐아아읏%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT99, 항문섹스시킨다
;────────────────────────────────────
;·죠교자가죠교대상에엉덩이에의삽입를 명한다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상가누움, 한편성교봉사중가 아닌 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·턴방침가"소프트"로Ａ성교봉사중가 아닌 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·Ｖ성교봉사중로부터는 파생하지 않는다
;·죠교자의Ａ감각가 2 이하의 경우는 발생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;·REACT분류가"날뛴다", "거부"였던 경우는취소 된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_99
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ａ성교계로부터/3=Ｖ성교봉사계로부터/4=Ｖ성교계로부터)
;※v1. 010 현재, ACT파생 2로부터 4는 ACT_ABLE로 규제되고 있으므로 기능하고 있지 않다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT99, 항문섹스시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생 1(연속), ACT파생 2 TO 5(Ａ성교/Ｖ성교봉사/Ｖ성교), 그 외로 분기
;·각파생내에서페니스밴드의 유무로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(100) && TFLAG:안빼고
		;//숙청당했다커맨드로부터 사용할 수 있는 것을 회수
		SELECTCASE RANDACT(4, 0)
			CASE 0
				CALL HTMLPRINTW(@"「힛%HTML_HEARTB()% 응앗%HTML_HEARTB()% 좋은 것…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「여기도 좋지요…%HTML_HEARTB()% 응읏%HTML_HEARTB()%」")
			CASE 1
				CALL HTMLPRINTW(@"「훗%HTML_HEARTB()% 하앗%HTML_HEARTB()% 응읏%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「뜨겁고 좋다…%HTML_HEARTB()% 후앗%HTML_HEARTB()%」")
			CASE 2
				CALL HTMLPRINTW(@"「이봐요 분명하게 즈코즈코 해? 엉덩이 휘저어어…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「%HTML_HEARTB()% 후아읏%HTML_HEARTB()% 그 상태%HTML_HEARTB()%」")
			CASE 3
				CALL HTMLPRINTW(@"「응읏%HTML_HEARTB()% 너의 몸…있었는지…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「이봐요%HTML_HEARTB()% 좀 더 엉덩이의 구멍 맛 깨어엇%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF

	;=================================================
	;연속(전회ACT가항문섹스시킨다)
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
	;ENDIF

	;=================================================
	;Ａ성교계로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 2
	;ENDIF

	;=================================================
	;Ｖ성교봉사계로부터
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 3
		CALL HTMLPRINTW(@"「엉덩이도 갖고 싶어져 버렸다…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「이봐요…%HTML_HEARTB()% 빨리 와아…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;Ｖ성교계로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
	;ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(2, 10)
			CASE 0
				CALL HTMLPRINTW(@"「이봐요 들어갈 수 있어어…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「그쪽이 아니고 엉덩이의 (분)편%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「준비 되어 있기 때문에…저기, 빨리…%HTML_HEARTB()%」")
			CASE 1
				CALL HTMLPRINTW(@"「이봐요 이봐요, 넣게 해 준다…%HTML_HEARTB()%엉덩이(분)편에 말야%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「는-나-구%HTML_HEARTB()% 그래그래, 그대로…자, 찔러어%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF


;//////////////////////////////////////////////////////////////////////////////
;//ACT100~102는 R#사가판에 실장되고 있었지만, 본바리안트에 있어서는 존재하고 있지 않다
;//스와코구상로 죽어구상가 되어 있던 것은, 다른 개소에 전용, 회수가 끝난 상태@/L
;//////////////////////////////////////////////////////////////////////////////


;────────────────────────────────────
;ACT103, 역강간
;────────────────────────────────────
;·죠교자가기승위로 억지로성교섭에 이른다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·Ａ성교봉사중로부터는 파생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(TARGET)"
;-------------------------------------------------
;·다른성교봉사계와 달리, 죠교대상는 저항 할 수 없다
;·죠교대상가동정라면동정상실가 일어난다
;·죠교자가처녀라면처녀상실가 일어난다
;·이것들의 처리는@ACTION_APPLY2_103에서 행해진다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_103
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;·v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;ACT103, 역강간
;━━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기
;·각파생내에서애태움도, 페니스밴드, 가버려, 죠교자의속옷의 유무등으로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;죠교대상가동정
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:동정 && FIRSTTIME(195)
		CALL HTMLPRINTW(@"「붓 내려 시켜 주세요는 부탁한다면 넣게 해 주지 않는 것도 아니야%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「…거짓말 거짓말%HTML_HEART()% 그러면 장소 안다? 코코에 분명하게 들어갈 수 있는거야%HTML_HEARTB()% 」")
		CALL HTMLPRINTW(@"「읏%HTML_HEARTB()% 이제 펄떡펄떡이 아니다…%HTML_HEART()% 넣은 순간사정했다들 용납하지 않아요?」")

		;//현재의 사양에서는 리액션에 따라서는 거절 취급이 되어, 동정상실가 되지 않기 때문에 comment out
		;CALL HTMLPRINTW(@" 「츄푸… 응%HTML_HEARTB()%  들어갔다아…%HTML_HEARTB()%」")
		;CALL HTMLPRINTW(@" 「무엇 번민이라는거야, 아직 끝 쪽 뿐이 아닌%HTML_HEARTB()%  후~응%HTML_HEARTB()%… 이것으로 전부%HTML_HEARTB()%」")
		;CALL HTMLPRINTW(@" 「분명하게 친숙해 지게 해… 응, 딱딱한…%HTML_HEARTB()%  , 빨리 움직여 %HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(100) && TFLAG:안빼고
		CALL HTMLPRINTW(@"「이봐요 이봐요 이봐요%HTML_HEARTB()% 좀 더 허리 움직인닷%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「가득 나카로 비비어엇%HTML_HEARTB()% 아읏%HTML_HEARTB()% 야읏…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;죠교대상가가버려(ACT파생 1 혹은 2의 경우)
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려 && INRANGE(TFLAG:ACT파생, 1, 2)
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				SELECTCASE RANDACT(2, 0)
					CASE 0
						CALL HTMLPRINTW(@"「아하앗%HTML_HEARTB()% 좋아%HTML_HEARTB()% 너의 자지 좋다%HTML_HEARTB()%」")
						CALL HTMLPRINTW(@"「아직도사정해서는 안됏%HTML_HEARTB()% 좀 더 노력해엣%HTML_HEARTB()%」")
					CASE 1
						PRINTFORMW 「후~…? 이제 이제 가버릴 것 같다고?」
						CALL HTMLPRINTW(@"「무슨 말하고 있는거야! 여기는 좀 더… 갖고 싶기 때문에%HTML_HEARTB()%」")
				ENDSELECT
			;애태움없음
			CASEELSE
				PRINTFORMW 「어떻게? 이것 어떻게인 것?」
				PRINTFORMW 「이런 식으로 움직여져 기분은 어떻게?」
		ENDSELECT
		RETURN 1
	ENDIF

	;//불필요라고 생각되는 HEART계는 독단과 편견으로 삭제@/L
	;=================================================
	;연속(전회ACT가역강간)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		PRINTFORMW 「-? 이것혐의-?」
		PRINTFORMW 「자지 벌벌 시키지 말고 움직이세요! 자기도!」
		RETURN 1
	ENDIF

	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		CALL HTMLPRINTW(@"「이제(벌써) 속상하다!  %HTMLFONT("화악", , 0xFFFFFF)%」")
		PRINTFORMW 「이제 속상하다! 가박」
		CALL HTMLPRINTW(@"「이봐요%HTML_HEARTB()% 내 쪽으로부터 범해 주기 때문에!」")
		RETURN 1
	ENDIF

	;=================================================
	;Ａ성교로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 3
	;ENDIF

	;=================================================
	;Ｖ성교계로부터
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
	;ENDIF

	;=================================================
	;Ａ성교봉사로부터
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 5
		PRINTFORMW 「엉덩이도 좋았지만…여기도 갖고 싶어져 버렸다가 아니다」
		CALL HTMLPRINTW(@"「얌전하게 해…응앗%HTML_HEARTB()% 들어갔다아…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		CALL HTMLPRINTW(@"「이봐요 봐아…%HTML_HEARTB()% 나의 여기 이제(벌써) 이렇게 되어 버리고 있다…」")
		PRINTFORMW 「자제 할 수 없게 되어 버렸다……이제 나부터 삽입아니야들 」
;OriginalString : (@"「もうキミのもこんなガッチガチにさせちゃってぇ…%HTML_HEARTB()%　期待しちゃってるんでしょ…？」")
		CALL HTMLPRINTL(@"「이제(벌써) 너의 것도 이런 딱딱하게 시켜 버려어…%HTML_HEARTB()% 기대해 버리고 있는거죠…?」")
		CALL HTMLPRINTW(@"「나에게 범해져 버려♪ 츄쿳%HTML_HEARTB()% 아하앗%HTML_HEARTB()%」")
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
존재하지 않는 커맨드
;-------------------------------------------------
;ACT104, 역강간항문 FIRSTTIME(304)
;-------------------------------------------------
@KOJO_MALE_ACT_K44_104
;-------------------------------------------------
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;-------------------------------------------------
	;처녀·동정
	;기입 체크
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && TALENT:MASTER:동정
		PRINTFORMW 「이봐요, 동정 졸업하고 싶지요…? %UNICODE(0 x2661)%」
		PRINTFORMW 「이이야…%UNICODE(0 x2661)%나부터 삽여(어) 주기 때문에 거기에 자…%UNICODE(0 x2661)%」
		PRINTFORMW 「조금 눈 감고 있어%UNICODE(0 x2661)%좋으니까%UNICODE(0 x2661)%」
		PRINTFORMW 「… 아는, 아하하 하하하♪」」
		PRINTFORMW 「자리-응이군요%UNICODE(0 x2661)%엉덩이의 (분)편이었습니다! %UNICODE(0 x2661)%」
		PRINTFORMW 「모처럼이니까 콧치로 짜 준다♪」
		ENDIF
		RETURN LOCAL
	ENDIF

	;-------------------------------------------------
	;안빼고에 속행
	;기입 체크
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && TFLAG:안빼고
		PRINTFORMW 「-? 여자 아이에게 엉덩이의 구멍에서 범해지고 있는 기분%UNICODE(0 x2661)%」
		PRINTFORMW 「엉덩이입니다 어휘 움직이고 있어 알아%UNICODE(0 x2661)%… 히%UNICODE(0 x2661)%」
		RETURN LOCAL
	ENDIF

	;-------------------------------------------------
	;상황 묘사
	;기입 체크
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && TFLAG:ACT파생
		SELECTCASE TFLAG:ACT파생
			;연속
			CASE 1
				PRINTFORMW 「히%UNICODE(0 x2661)%%UNICODE(0 x2661)%견딜 수 없다%UNICODE(0 x2661)%」
				PRINTFORMW 「엉덩이의 구멍 견딜 수 없어%UNICODE(0 x2661)%」
			;체위 변경(다른Ｖ성교봉사계로부터)
			CASE 2
				PRINTFORMW 「이제 성실하게 해」
				PRINTFORMW 「벌로서 콧치로 범해 준다…%UNICODE(0 x2661)%」
			;Ａ성교봉사계로부터
			CASE 5
				PRINTFORMW 「응─, 이이응이지만」
				PRINTFORMW 「이것이라면 나부터 움직이는 것이 좋을지도%UNICODE(0 x2661)%」
		ENDSELECT
		SELECTCASE TCVAR:MASTER:가버려
			;죠교대상가가버려인 것을참다 하고 있다
			CASE IS > 5
				PRINTFORMW 「좋은 얼굴%UNICODE(0 x2661)%좀 더… 좀 더 보여%UNICODE(0 x2661)%」
				PRINTFORMW 「가득 자제 해 좀 더 보여…%UNICODE(0 x2661)%」
			;죠교대상가가버려
			CASE IS > 0
				PRINTFORMW 「이구? 이우노? %UNICODE(0 x2661)%」
				PRINTFORMW 「엉덩이안에 정액 사정해버린다의? %UNICODE(0 x2661)%」
			;통상
			CASEELSE
				PRINTFORMW 「아는%UNICODE(0 x2661)%이봐요, 어떻게인 것%UNICODE(0 x2661)%」
				PRINTFORMW 「일방적으로 동 쉬어 버린다가 아닌 것%UNICODE(0 x2661)%」
		ENDSELECT
		RETURN LOCAL
	ENDIF

;일반
	PRINTFORMW 「도-자제 할 수 없는거야…%UNICODE(0 x2661)%그러니까…」
	PRINTFORMW 「범하게 해? %UNICODE(0 x2661)%콧치의 구멍에서%UNICODE(0 x2661)%… 후~%UNICODE(0 x2661)%」
	RETURN LOCAL
ENDIF
[SKIPEND]
