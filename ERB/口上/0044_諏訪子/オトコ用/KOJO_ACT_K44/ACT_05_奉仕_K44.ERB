;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코구상 봉사계 액션구상파일
;
;	봉사계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;봉사계 ACT50～57
;========================================================================
;·TFLAG:ACT(50=손으로 애무를 강요/51=입으로 애무를 강요/52=파이즈리/53=스마타/55=발핥기강제/56=이라마치오/57=가슴애무강제)
;·TFLAG:ACT분류 == 5에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "봉사")로 줍는 것이 가능
;·죠교대상에, 죠교자의 신체를애무 하도록(듯이) 명령한다
;·기본적으로는 명령인 것으로, 이 단계에서는 따를지 어떨지 확정하고 있지 않다(ACT56, 이라마치오는거부 할 수 없다)
;-------------------------------------------------
;·ACT54, 발코키는 12/04/08판으로 숙청
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT50, 손으로 애무를 강요
;────────────────────────────────────
;·애무강제/수음강제/페니스밴드수음강제
;·죠교대상(MASTER)에, 손에 의한애무를 강요 한다
;·죠교자에페니스가 있으면 반드시수음강제가 된다(페니스가 있으면통상의애무강제는 일어나지 않는다)
;·죠교자가페니스밴드장착중은페니스밴드수음강제가 된다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP(밧줄/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교대상가 따르는지 아닌지는 이 시점에서는 미정
;·REACT분류가거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_50
;-------------------------------------------------
;·TFLAG:ACT파생(0=애무강제/1=수음강제/2=페니스밴드수음강제)
;-------------------------------------------------
;·지문은 ACT파생 마다 분기. 각 ACT파생내에서는 분기 없음
;·ACT파생 0, 애무때만쾌Ｂ/쾌ＡSOURCE 증가
;·그 외의 경우는쾌Ｃ한정
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_0, 애무강제 「자신의성기를손으로 애무 하라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:TARGET:가버려
;OriginalString : 「んはぁ…や…もう가버려…っ%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「응하아…야…이제(벌써)가버려…읏%HTML_HEARTB()%」")
;OriginalString : 「もっといっぱいさわってぇ…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「좀 더 가득함 깨어어…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(3, 10)
			CASE 0
				PRINTFORMW 「나의 몸… 손대어 준다?」
;OriginalString : 「好きなようにしていいからね%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「좋을대로 해도 좋으니까%HTML_HEARTB()%」")
			CASE 1
				PRINTFORMW 「요철 없기 때문에 재미없을지도 모르는데…」
;OriginalString : 「いっぱいさわって…気持ちよくしてね%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「가득함 깨어…기분 좋게 해%HTML_HEARTB()%」")
			CASE 2
;OriginalString : 「ほら、好きにさわって%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「이봐요, 좋아하게 손대어%HTML_HEARTB()%」")
				PRINTFORMW 「기분 좋게 할 수 있을까─?」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_1, 수음강제 「자신의남성기를손로 잡아당기라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_2, 페니스밴드수음강제 「페니스밴드를손로 잡아 움직이라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF
[SKIPEND]

;────────────────────────────────────
;ACT51, 입으로 애무를 강요
;────────────────────────────────────
;·쿤닐링구스/펠라티오/안면승마/안면승마항문/
;·죠교대상에, 입에 의한애무를 강요 한다
;·죠교자가후타나리의 경우, 죠교대상가누움라면 1/2으로안면승마, 그렇지 않으면펠라강제가 된다
;·죠교자에페니스가 없고, 죠교대상가누움라면안면승마가 된다
;·안면승마가 선택되고 있었을 때, 안면승마항문에변화 하는 일이 있다
;·모든 조건으로부터 빠졌을 경우는통상의 쿤닐링구스가 된다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마)장착중은 발생하지 않는다
;·죠교자가페니스밴드를장착 하고 있는 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·REACT분류가거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_51
;-------------------------------------------------
;·TFLAG:ACT파생(0=쿤닐링구스/1=펠라/2=안면승마/3=안면승마항문)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_0, 쿤닐링구스 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	SELECTCASE RANDACT(3, 0)
		CASE 0
			PRINTFORMW 「나의 여기… 입으로 하고 준다?」
;OriginalString : 「ほら、早くぅ%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「이봐요, 빨리%HTML_HEARTB()%」")
		CASE 1
			PRINTFORMW 「무릎 꿇어 날름날름 해 주지 않는다?」
;OriginalString : 「できるでしょ？　やりたいんでしょホントは%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「할 수 있겠죠? 하고 싶지요 사실은%HTML_HEARTB()%」")
		CASE 2
;OriginalString : 「おまんこ疼いてしょうがないの…%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「보지 쑤셔 어쩔 수 없는거야…%HTML_HEARTB()%」")
;OriginalString : 「ね…舐めて%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「저기…빨아%HTML_HEARTB()%」")
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_1, 펠라티오
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_2, 안면승마
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	PRINTFORMW 「등, 마음껏 날름날름 해도 좋아」
;OriginalString : 「んぅ…%HTML_HEARTB()%　鼻が当たってる…%HTML_HEARTB()%」")
	CALL HTMLPRINTW(@"「응우…%HTML_HEARTB()% 코가 맞고 있다…%HTML_HEARTB()%」")
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_3, 안면승마항문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	PRINTFORMW 「엉덩이의 구멍도 빨기를 원하는거야」
;OriginalString : 「嫌なんて言わせないよ？　ほらほらぁ%HTML_HEARTB()%」")
	CALL HTMLPRINTW(@"「혐 같은거 말하게 하지 않아? 이봐요 이봐요%HTML_HEARTB()%」")
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT52, 파이즈리
;────────────────────────────────────
;·죠교대상에파이즈리에서의봉사를 강요 한다
;·죠교대상이남자의 경우는 발생하지 않는다
;·죠교자에페니스가 없는 경우는 발생하지 않는다
;·턴방침가"이상"의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP(밧줄/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·REACT분류가거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
;@KOJO_MALE_ACT_K44_52
;-------------------------------------------------
;남자한정구상에서는 불요
;RETURN 0

;────────────────────────────────────
;ACT53, 스마타
;────────────────────────────────────
;·죠교대상에페니스밴드, 혹은페니스를 가랑이에 끼워 스치고라고 명령한다
;·죠교자에 봉이 없는(페니스/페니스밴드) 경우는 발생하지 않는다
;·죠교대상에페니스가 있는 경우는 발생하지 않는다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP:삼각목마장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교대상에발가 없으면 발생하지 않는다(현재 상태로서는토지코가죠교대상인 경우만이라고 생각된다)
;·REACT분류가거부/날뛴다/도망치는인 경우는 거절 취급
;-------------------------------------------------
;@KOJO_MALE_ACT_K44_53
;-------------------------------------------------
;·TFLAG:ACT파생(0=스마타/1=페니스밴드스마타)
;-------------------------------------------------
;남자한정구상에서는 불요
;RETURN 0

;────────────────────────────────────
;ACT55, 발핥기강제
;────────────────────────────────────
;·죠교자가발를 핥아라고 명령해 온다
;·이상계로부터 발생하는 경우 있어
;·턴방침가"소프트"라면 발생하지 않는다
;·오늘의방침가"노닥노닥"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교자에발가 없는 경우는 발생하지 않는다
;·REACT분류가거부/날뛴다/도망치는 것 경우는 거절 취급
;-------------------------------------------------
@KOJO_MALE_ACT_K44_55
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT55, 발핥기강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	PRINTFORMW 「네.…이봐요 뭐 하고 있어」
	;오리지날 =>「빨리발 빠세요♪」설명 수상한 감이 있으므로 『발』은 삭제
	;시추에이션적으로 『발』을 핥아의 것이 전제가 되어 있으므로
	PRINTFORMW 「빨리 빠세요♪」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT56, 이라마치오
;────────────────────────────────────
;·명령하는 것은 아니고, 억지로페니스를입에 돌진한다
;·죠교대상의 반응은 미정이지만, 거절하는 일은 할 수 없다
;·가학계로부터 발생하는 경우 있어
;·턴방침가"노멀", "소프트"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교자에페니스가 없는 경우는 발생하지 않는다(페니스밴드에 의한이라마치오는 없다)
;-------------------------------------------------
@KOJO_MALE_ACT_K44_56
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;ACT57, 가슴애무강제
;────────────────────────────────────
;·죠교대상에, 죠교자의가슴를애무 하도록(듯이) 강제한다
;·죠교자가남자의 경우는 발생하지 않는다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP:밧줄장착중은입로가슴애무강제에 파생한다. 다만볼개그를장착중의 경우는 튕겨진다
;·볼개그가 없으면,1/2의 확률로입으로 애무가 된다
;·REACT분류가거부/날뛴다/도망치는 것 경우는 거절 취급
;·PALAM가치정선행이나반항선행인가로"부끄러워서봉사 할 수 없었다/싫어해 요구에 응하지 않았다"에 파생한다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_57
;-------------------------------------------------
;·TFLAG:ACT파생(0=손로가슴애무강제/2=입로가슴애무강제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_0, 손로가슴애무를 강요
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생

	;=================================================
	;죠교대상삽입중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
;OriginalString : 「やっ…%HTML_HEARTB()%　はっ%HTML_HEART()%　おっぱいぃ…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「얏…%HTML_HEARTB()% 핫%HTML_HEART()% 젖가슴…%HTML_HEARTB()%」")
;OriginalString : 「おっぱいも弄ってよぉ…っ%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「젖가슴도 만지작거려…읏%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && PALAM:쾌Ｂ > 8500
;OriginalString : 「やん…%HTML_HEARTB()%　んぁ…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「…%HTML_HEARTB()% 응아…%HTML_HEARTB()%」")
;OriginalString : 「乳首ぃ…コリコリして가게해줘ぇ…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「유두 있고…오돌오돌 해가게해줘에…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「조금 전부터 가슴뿐 봐…」
		PRINTFORMW 「그렇게 신경이 쓰인다면 손대어 준다?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_2, 입로가슴애무를 강요
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;=================================================
	;죠교대상삽입중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
;OriginalString : 「っ…%HTML_HEARTB()%　おっぱい吸ってっ%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「읏…%HTML_HEARTB()% 젖가슴 들이마셔%HTML_HEARTB()%」")
;OriginalString : 「ちゅーちゅー하면서ズコズコしてぇっ%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「츄우츄우하면서즈코즈코 해엣%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && PALAM:쾌Ｂ > 8500
;OriginalString : 「はぁっ…%HTML_HEARTB()%　はぁあ%HTML_HEARTB()%　もうだめぇ%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「하앗…%HTML_HEARTB()% 하아아%HTML_HEARTB()% 이제 안돼에%HTML_HEARTB()%」")
;OriginalString : 「おっぱい吸って…%HTML_HEARTB()%　ちゅうちゅうして가게해줘よぉっ%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「젖가슴 들이마셔…%HTML_HEARTB()% 츄우츄우 해가게해줘%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「이런\@ TALENT:TARGET:빈유 ? 작다 # \@젖가슴에서도 들이마시고 싶은거야?」
;OriginalString : 「しょーがないわねぇ…ほらっ%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「어쩔 수 없네요…이봐요%HTML_HEARTB()%」")
		RETURN 1
	ENDIF
ENDIF
