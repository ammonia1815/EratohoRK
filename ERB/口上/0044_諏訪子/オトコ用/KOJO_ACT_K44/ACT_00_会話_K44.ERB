;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코구상 회화계 액션구상파일
;
;	회화계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금 기분을 묻다/1=자신에게의 마음을 묻다/2=성에 관한 이야기를 하다/3=상냥하게 위로한다/4=엄하게 위협한다/5=의상 변경을 요구한다/6=합의를 요구한다/7=징계한다고 위협한다/8=고함치다/9=기분나쁘게 웃다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분나쁘게 웃다는이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;────────────────────────────────────
;ACT0, 지금 기분을 묻다
;────────────────────────────────────
;·지금의 기분에 대해 들어 온다
;·죠교대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침가"이상"라면 발생하지 않는다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_0
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 지금 기분을 묻다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「그런데」
		PRINTFORMW 「지금부터 시작하는 것이지만, 지금의 기분이라도 가르쳐 받을까?」
		RETURN 1
	ENDIF

	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(100) && ONCEACT(0)
		PRINTFORMW 「-라고, 시작하는 것에 해당되어 지금의 기분은 어때?」
		PRINTFORMW 「정직하게 말해 보고? 응?」
		RETURN 1
	ENDIF

	;=================================================
	;성교봉사계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
;OriginalString : 「何の躊躇もなくこんなに사정しちゃってぇ…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「아무 주저도 없고 이렇게사정해 버려어…%HTML_HEARTB()%」")
;OriginalString : 「気分はどうなの？このスケベ%HTML_HEART()%」")
		CALL HTMLPRINTW(@"「기분은 어때? 이 호색가%HTML_HEART()%」")
		RETURN 1
	ENDIF

	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		;가버려인 것을참다 하고 있다
		IF TCVAR:MASTER:참다
;OriginalString : 「健気にガマンしちゃって…%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「기특하게 자제 해 버려…%HTML_HEARTB()%」")
			PRINTFORMW 「지금 어떤 기분? 내고 싶은거야?」
		ELSE
;OriginalString : 「キンタマ上がってもう사정ちゃいそうじゃないの%HTML_HEART()%」")
			CALL HTMLPRINTW(@"「불알 올라 이제(벌써)사정있을 것 같지 않아%HTML_HEART()%」")
			PRINTFORMW 「응 지금 어떤 기분?」
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
;OriginalString : 「こんなにカタくチンポ발기させちゃって…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「이렇게 딱딱하고 자지발기시켜 버려…%HTML_HEARTB()%」")
		PRINTFORMW 「응 어떤 기분이야?」
		RETURN LOCAL
	ENDIF

	;=================================================
	;쇠약해졌다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 < TCVAR:MASTER:전발기도
		PRINTFORMW 「어 거짓말」
		PRINTFORMW 「뭔가 비위에 거슬렸는지?」
		RETURN 1
	ENDIF

	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그래, 자…」
		PRINTFORMW 「지금의 기분은 어떻게야?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT1, 자신에게의 마음을 묻다
;────────────────────────────────────
;·자신의 일을 어떻게 생각할까 얘기한다
;·죠교대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침가"이상"라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_1
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 자신에게의 마음을 묻다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「자 우선 시작으로…」
		PRINTFORMW 「나 어떻게 생각한다? 정직하게 말해?」
		RETURN 1
	ENDIF

	;=================================================
	;그죠교의 최초의 액션
	;=================================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1
		PRINTFORMW 「그렇구나 우선은…」
		PRINTFORMW 「나 어떻게 생각할까 가르쳐 받을까♪」
		RETURN 1
	ENDIF

	;=================================================
	;성교봉사계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
;OriginalString : 「もーこんなにぃ…%HTML_HEART()%」")
		CALL HTMLPRINTW(@"「도-이렇게…%HTML_HEART()%」")
		PRINTFORMW 「나의 것은 어땠어요?」
		RETURN 1
	ENDIF

	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		;가버려인 것을참다 하고 있다
		IF TCVAR:MASTER:참다
;OriginalString : 「ガマンしちゃってるねぇ…%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「자제 해 버리고 있네요…%HTML_HEARTB()%」")
			PRINTFORMW 「나로 흥분해 버린다의?」
		ELSE
			PRINTFORMW 「아는, 이제 이 기초?」
			PRINTFORMW 「나로 흥분해 버렸다?」
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
;OriginalString : 「こーんなチンポおっ勃てちゃってぇ%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「이런 자지세워 버려어%HTML_HEARTB()%」")
		PRINTFORMW 「작은 몸에서도 흥분해 버렸어?」
		RETURN 1
	ENDIF

	;=================================================
	;쇠약해졌다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 < TCVAR:MASTER:전발기도
		PRINTFORMW 「쇠약해졌다…(이)래…?」
		PRINTFORMW 「역시 사나에같은 젖가슴이 없으면 안돼?」
		RETURN 1
	ENDIF

	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「사나에나 카나자같은 젖가슴은 없는데」
		PRINTFORMW 「나 어떻게 생각할까?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT2, 성에 관한 이야기를 함
;────────────────────────────────────
;·죠교자가 성에 대한지식를 말하기 시작한다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·죠교자에성지식가 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_2
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT2, 성에 관한 이야기를 함
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTFORMW 「이이 일 가르쳐 주어라 인가♪」
	PRINTFORMW 「조금 귀 빌려 주어 보고」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT3, 상냥하게 위로한다
;────────────────────────────────────
;·죠교자가 상냥한 말로 위로해 온다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_3
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 상냥하게 위로한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;그죠교의 최초의 액션
	;=================================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1
		PRINTFORMW 「가끔 씩은 상냥하게 접해 줄까」
		PRINTFORMW 「이 얇은 가슴으로 응석부려도 괜찮아?」
		RETURN 1
	ENDIF

	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「좋아 좋아」
		PRINTFORMW 「조금 응석부리게 해 줄게♪」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT4, 엄하게 위협한다
;────────────────────────────────────
;·어려운 말로 위협한다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_4
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은변화 할 가능성이 있다
;기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 엄하게 위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTFORMW 「그런 상태는 사나에는 단념해 주어?」
	PRINTFORMW 「유희적인 일이라면 타처에서 해 받을 수 있다?」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT5, 의상 변경을 요구한다
;────────────────────────────────────
;·죠교자가의상의 변경을 요구해 온다
;·죠교대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:밀어넘어지는중에는 발생하지 않는다
;·TEQUIP:밧줄가 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_5
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 옷을 벗어와 명령했다…"되고 있어 이것이변화 하는 일은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;그죠교의 최초의 액션
	;=================================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1
		PRINTFORMW 「갑자기이지만 벗어버릴 없음 있고♪」
		PRINTFORMW 「-키 다음에 벗는다 해」
		RETURN 1
	ENDIF

	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
;OriginalString : 「もーこんなに발기させちゃってるじゃないのよ…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「도-이렇게발기시켜 버리고 있지 않아…%HTML_HEARTB()%」")
		PRINTFORMW 「불필요한 것 벗어 버리자」
		RETURN 1
	ENDIF

	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「옷은 입어도 쟈마겠지?」
		PRINTFORMW 「빨리 벗어버릴 없음 있어라♪」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT5_1, 옷을 입도록(듯이) 명령
;────────────────────────────────────
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	PRINTFORMW 「조금 으스스 춥네요…」
	PRINTFORMW 「입어도 상관없어?」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT6, 합의를 요구한다
;────────────────────────────────────
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀or동정(양쪽 모두의 경우도 있다)
;·이미합의를 얻고 있는 경우는 발생하지 않는다
;·죠교대상가처녀에서도동정도 아닌 경우는 발생하지 않는다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_6
;-------------------------------------------------
;함수@TARGET_DESCRIPTION로 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은변화 한다
;후는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 합의를 요구한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;죠교대상이남자의 경우
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:남자
		PRINTFORMW 「네, 진짜 동정이었어요!?」
		PRINTFORMW 「아니별로 상관없지만, 그렇다면 일단 (들)물어 두어」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT7, 징계한다고 위협한다
;────────────────────────────────────
;·지금부터징계·한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:징계·플래그는 서 있다
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·TEQUIP:밀어넘어지는중시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_7
;-------------------------------------------------
;여기에서는 지문의변화는 없다. 분기의 증감은 기호로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 징계한다고 위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;전COM가 「무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;=================================================
	;IF 구상기입 체크(0) && IS_PREVCOMNAME("무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다")
	;ENDIF

	;=================================================
	;전COM가 「기분좋게 해」（이 철면피! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("기분좋게 해")
		PRINTFORMW 「눈앞의 에로 있고 일에 놀아나 간단하게 목적 잃는 야와인 남자아…」
		PRINTFORMW 「조금…현실 보여 주지 않으면…」
		RETURN 1
	ENDIF

	;=================================================
	;전COM가 「더 하드하게 해」（이 돼지째! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("더 하드하게 해")
		PRINTFORMW 「말하면 기쁨라고 생각해 굳이 말하지 않았지만 말해」
		PRINTFORMW 「이 돼지 자식이!!」
		RETURN 1
	ENDIF

	;=================================================
	;전COM가 「용서를 빌다」（허락해 받을 수 없어했다）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("용서를 빌다")
		PRINTFORMW 「세상 사과해 허락받을 수 있는 일뿐이면 구멍 있는이야」
		PRINTFORMW 「태도로 가리켜 받을까…」
		RETURN 1
	ENDIF

	;=================================================
	;전COM가 「자위를 시작한다」（인의 이야기를 들어라! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("자위를 시작한다")
		PRINTFORMW 「말하는 것 (듣)묻지 않고 마음대로 시고 오기 시작하는 원숭이아…」
		PRINTFORMW 「조금 예의 범절이 필요할지도…」
		RETURN 1
	ENDIF

	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「테메이는 나를 화나게 했다」
		PRINTFORMW 「조금 바보해 아픈 눈에도 있어 받을까. 무슨 일도 경험이지요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT8, 고함치다
;────────────────────────────────────
;·큰 소리로고함치다
;·턴방침가"소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:징계·플래그에 관련되지 않고, 발생할 가능성이 있다（징계·안에 한층 더고함치다가능성등 ）
;-------------------------------------------------
@KOJO_MALE_ACT_K44_8
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 큰 소리로 고함쳤다…"만으로 파생은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 고함치다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTFORMW 「이 드 축생이!!」
	PRINTFORMW 「그런 것으로 사나에와 어울리다고 생각해!?」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT9, 기분나쁘게 웃다
;────────────────────────────────────
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·금일의방침가"노닥노닥"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_9
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은변화 한다
;기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 기분나쁘게 웃다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTFORMW 「후, 우후후후…어쩐지 옛날을 생각해 내 와 버렸다…」
	PRINTFORMW 「그 무렵은 여러가지 했던가…우에에에 헤헤…♪」
	RETURN 1
ENDIF


[SKIPSTART]
R#사가판에 실장되고 있었지만, 현행판에서는 존재하지 않는 커맨드인 것으로 comment out
상손 있는 상태에 회수할 수 있을 것 같으면 그 쪽으로 이했다 있고 의향@/L
;-------------------------------------------------
;ACT110, 사랑을 고하는 FIRSTTIME(310)
;-------------------------------------------------
@KOJO_MALE_ACT_K32_110
;-------------------------------------------------
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;-------------------------------------------------
	;소분기
	;-------------------------------------------------
	;상사상애
	;기입 체크
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && FAVOR(3000, 3000)
		PRINTFORMW 「이런 의견 우노도이지만 냉큼 말해 놓아요」
		PRINTFORMW 「사랑하고 있는 원당신… 농담이라도 아무것도 아니야」
;OriginalString : 「사나에에래 도했다구 없을 정도로…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@" 「사나에에게라도 도했다구 없을 정도로… 저기%HTML_HEARTB()%」")
		PRINTFORMW 「진짜는 당신이라도 눈치챘기 때문에 실마리? … 나의 이 기분을」
		RETURN LOCAL
	ENDIF

;일반
	PRINTFORMW 「응… 당신은 사나에에 넣고 있는 (뜻)이유이지만…」
	PRINTFORMW 「나는 어떻게 생각해…?」
	PRINTFORMW 「아니, 스승이라든지가 아니고 한사람의 여자로서…」
	PRINTFORMW 「원, 나는… 상당히 당신…, 빈틈이야…?」
	PRINTFORMW 「아니틈투성이의 빈틈이 아니에요 공기 읽을 수 있는 이 스카폰탄」
	RETURN LOCAL
ENDIF
[SKIPEND]
