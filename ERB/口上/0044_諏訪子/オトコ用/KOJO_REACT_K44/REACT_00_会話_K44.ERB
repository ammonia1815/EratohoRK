;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코구상 회화계 개별 리액션구상파일
;
;	회화계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어선택했다COM가 자연스러운행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운행동/1=무리가 없는행동/2=무리를했다행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 지금 기분을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일했다 있고)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다로, 리액션이빈정거린다 이외의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	PRINTFORMW 「무엇으로 입다물까…」
	PRINTFORMW 「부끄러운거야? 이제 대답하세요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	PRINTFORMW 「꽤 좋은 담력 하고 있잖아」
	PRINTFORMW 「지금부터가 기다려지구나…♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	PRINTFORMW 「여유라고 응의 것도 지금 가운데일지도?」
	PRINTFORMW 「아무튼 위협하고 있는  것이 아닌데」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;=================================================
	;REACT부하 0, 함께 있어지는 것이 기쁘면솔직에입에했다
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 「うんうん%HTML_HEARTB()%そうなんだね」")
		CALL HTMLPRINTW(@"「우응우응%HTML_HEARTB()%그렇네」")
		PRINTFORMW 「좀처럼 나를 싣는 것이 능숙하지 않다♪」
		RETURN 1
	ENDIF

	;=================================================
	;REACT부하 1, 분명히 지금의 기분을 고했다
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는행동"
		PRINTFORMW 「어머나, 생각했던 것보다도 좋은 대답이 아니다」
		PRINTFORMW 「좋은 아이로 하고 있다고(면) 좀 더 이이코트 있을지도♪」
		RETURN 1
	ENDIF

	;=================================================
	;REACT부하 2, 기분을 해치지 않게, 손질해 대답했다
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		PRINTFORMW 「응, 그렇게」
		PRINTFORMW 「아무튼 분명하게 대답해 주었고 좋아로 할까」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
;IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일했다 있고
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
;IF 구상기입 체크(0) && TFLAG:ACT파생 == 5
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;지금 기분을 묻다에서의 COM침묵한다는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어징계·포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「무엇으로 입다물어」
	PRINTFORMW 「부끄러워하지 않고 말해도 좋은데」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	PRINTFORMW 「캔 벤 해 주세요?」
	PRINTFORMW 「아무튼 무리하게라고는 말하지 않지만…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다") && !TFLAG:REACT파생
	PRINTFORMW 「아니 그렇게 위축되지 않아도」
	PRINTFORMW 「한심해-사내 아이」
	RETURN 1
ENDIF

;────────────────────────────────────
;REACT1, 자신에게의 마음을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다!/2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	PRINTFORMW 「싫지 않다는야」
	PRINTFORMW 「 좀 더 기분은 분명하게 말해♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	PRINTFORMW 「에-그래-응」
	PRINTFORMW 「뭐야별로 슬프게 풀지 않아요 분」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	PRINTFORMW 「익살맞은 짓을 해  것이 아니어요」
	PRINTFORMW 「싶고 아가씨 고코로의 모르는 놈」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 적은지, REACT분류가태연하게 한다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	PRINTFORMW 「너가 지금 심하다고 생각하고 있는 것이 있고 개에든지 쾌감으로 바뀔지도 몰라?」
	PRINTFORMW 「그건 그렇고, 아무튼 생각해 두어」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 1000이상
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 4
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(4)
		PRINTFORMW 「!? … 라는 자기 사나에를 좋아하는 것이겠죠가」
		PRINTFORMW 「너무 조롱하지 말아…」
		RETURN 1
	ENDIF

	;=================================================
	;부하 0, 부하 1 붙임성 좋게 웃어 정말 좋아한다고 대답했다
	;=================================================
	IF 구상기입체크(100) && GROUPMATCH(GET_COM_RANK(), "자연스러운행동", "무리가 없는행동")
		IF RAND:2
			PRINTFORMW 「,…아, 가와…」
			PRINTFORMW 「뭐니요니요 하고 있어이 바보…」
		ELSE
			PRINTFORMW 「그런 드 직구에 좋아와 산다―…」
			PRINTFORMW 「뭐, 아무튼 기쁜 케드야…」
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;REACT부하 2, 억지웃음을 띄워, 좋아해라고 대답했다
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;RESULT를 돌려주어 범용 리액션에 흘리지 않고 완결시킨다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 5
	PRINTFORMW 「좀 더 대단한 일이래?」
	PRINTFORMW 「너도 챌린저구나…」
	RETURN 1
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자신에게의 마음을 묻다의 경우는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어징계·포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「거기서 입다물어지면(자) 다칩니다만―」
	PRINTFORMW 「다칩니다만―」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	PRINTFORMW 「아니 거기서 참회 되어도」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다")
	PRINTFORMW 「프르풀 해 버려서 어떻게 하지 고양이의 새끼양은」
	RETURN 1
ENDIF

;────────────────────────────────────
;REACT2, 성에 관한 이야기를 하다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_MALE_REACT_K44_2
;-------------------------------------------------
;  파생할 가능성이 있는 COM는 우선 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「당겼군 진한 개!」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
	PRINTFORMW 「무엇 그 비묘궶아크션」
	PRINTFORMW 「거기는 분명하게 트넣으세요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	PRINTFORMW 「에에에, 이렇게 되고 싶지 않았으면 뭐든지 말하는 일 (들)물으세요♪」
	PRINTFORMW 「이라니, 전부 거짓말이야」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("기분좋게 해")
	PRINTFORMW 「설마 노는 온다고는…」
	PRINTFORMW 「반야케크소가 아니다?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("부끄러워한다")
	PRINTFORMW 「리액션 외지 한녀들주위의 것이죠…」
	PRINTFORMW 「자지 붙고 있어 까자지」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다")
	PRINTFORMW 「가치로 진심으로 하지 않아도…」
	PRINTFORMW 「전부 픽션이니까 안심 없음 있고」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("자위를 시작한다")
	PRINTFORMW 「이 이야기로 자지 훑어 낼 수 있는 (자식)놈은 아직도 전혀 없었어요」
	PRINTFORMW 「일재야 당신…나쁜 의미로」
	RETURN 1
ENDIF

;────────────────────────────────────
;REACT3, 상냥하게 위로한다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_MALE_REACT_K44_3
;-------------------------------------------------
;  파생할 가능성이 있는 COM에는 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
;  우선회화계 COM를 모두 망라되어 있습니다만
;  10 접수대의애무계 COM, 50 접수대 이후의 그 외 COM등도 상황에 따라서는선택 가능하게 됩니다
;  필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「왜 입다문다」
	PRINTFORMW 「 이제, 뒤는 없다니까 이 이♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
	PRINTFORMW 「그러고 보니 뒤가 없는가 억측해?」
	PRINTFORMW 「솔직하게 응석부려도 좋은데」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
	PRINTFORMW 「응 이제, 사랑스럽지 않다」
	PRINTFORMW 「 나의 기분이 좋은 동안에 응석부리고 풀면 좋은데」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성 있게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("상냥하게 응답한다")
;OriginalString : 「そうそう、いい子だねぇ%HTML_HEARTB()%」")
	CALL HTMLPRINTW(@"「그래그래, 좋은 아이구나%HTML_HEARTB()%」")
	PRINTFORMW 「이거 참, 드사크사 요행에 날름날름 하지 않는다」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("강하게 응답한다")
	PRINTFORMW 「에 」
	PRINTFORMW 「튼데레 만큼 없고 솔직해지세요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 「능숙한 일 말한 생각?」
	PRINTFORMW 「완전히…솔직하지 않기 때문에」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("더 하드하게 해")
	PRINTFORMW 「잠시동안의 상냥함을 헛되게 하는 란…」
	PRINTFORMW 「아무튼 솟치를 좋아하면 별로 좋지만」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	PRINTFORMW 「땅에 엎드려 조아림되어도 나도 곤란하다」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("기분좋게 해")
	PRINTFORMW 「싶고 에로 있고 것 밖에두에 없는 것일까 이 원숭이는」
;OriginalString : 「そんじゃ、めいっぱいしてあげる%HTML_HEARTB()%」")
	CALL HTMLPRINTW(@"「그러면, 힘껏 해 준다%HTML_HEARTB()%」")
	RETURN 1
ENDIF

;────────────────────────────────────
;REACT4, 엄하게 위협한다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_MALE_REACT_K44_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「입다물어  것이 아니어요」
	PRINTFORMW 「분명하게 해 주세요양멘」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
	PRINTFORMW 「이다-인가-들 -그런 무기력은 안된다니까」
	PRINTFORMW 「싶게 자지는 확실히 해 」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 「응─20점」
	PRINTFORMW 「좀 더 능숙한 표현이 있지 않다」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	PRINTFORMW 「여기서 노력해 식등 있고 붙으세요」
	PRINTFORMW 「언제래 당신을 측정해? 나는」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("비명지른다")
;OriginalString : 「んん♪　いい…%HTML_HEARTB()%」")
	CALL HTMLPRINTW(@"「응응♪ 좋다…%HTML_HEARTB()%」")
	PRINTFORMW 「…(이)가 아니었다, 분명하게 반골 정신 보이세요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다")
	PRINTFORMW 「거기서 무서워하지 않는다! 분명하게 직면한다!」
	PRINTFORMW 「후~, 이래서야 너의 엄마같지 않다…」
	RETURN 1
ENDIF

;────────────────────────────────────
;REACT5, 의상 변경을 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가"소극적으로 한다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가"적극적으로 한다", "태연하게 한다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_MALE_REACT_K44_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;=================================================
	;한 장 벗었다(소극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("소극적으로 한다/받아들인다")
		PRINTFORMW 「또 불완전인…」
		PRINTFORMW 「남자라면 박이라고 벗으세요 박과」
		RETURN 1
	ENDIF

	;=================================================
	;2매 벗었다(적극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		PRINTFORMW 「좋다♪」
;OriginalString : 「적극的な子は好きよ%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「적극적인 아이는 좋아해요%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;벗지 않았다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("거부")
		;부끄러워서 벗겨지지 않았다
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
			PRINTFORMW 「부끄러워해  것이 아니어요 이제 와서」
			PRINTFORMW 「카크고 결정하세요 카크고」
		;싫어해 벗지 않았다
		ELSE
			PRINTFORMW 「팬츠안에 브따돌려지고 싶은 것?」
			PRINTFORMW 「그런의가 좋으면 멈추지 않지만 바꾸고의 팬츠 가져와?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("소극적으로 한다/받아들인다")
		PRINTFORMW 「무엇으로 불완전에 입어」
		RETURN 1
	ENDIF

	;=================================================
	;2매 입었다(적극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		PRINTFORMW 「남자는 의류 적어 좋아요」
		RETURN 1
	ENDIF

	;=================================================
	;입지 않았다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("거부")
		PRINTFORMW 「뿌리로부터의 라족이었구나 」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;REACT6, 합의를 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_6
;-------------------------------------------------
;손간을 싫어하지 않는 것이면, TALENT:MASTER:남자등으로 내부에서 나누는 것도 있을지도 모릅니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("소극적으로 한다")
	PRINTFORMW 「무엇 그 아가씨인 리액션은」
	PRINTFORMW 「무리야리 빼앗아 주고 싶은 충동에 사로잡히잖아」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1 합의 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;현재의 사양에서는 REACT분류:태연하게 한다/적극적으로 한다/받아들이는 것으로 파생하지만
;쓰여진 시점에서는 파생은 존재하고 있지 않았다. 그렇다고 하는 것으로 정리해 버립니다@/L
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("태연하게 한다/적극적으로 한다/받아들인다")
	PRINTFORMW 「처음이 사나에가 아니어도 괜찮다?」
;OriginalString : 「それじゃいっぱい気持ちよくしてあげるよ%HTML_HEARTB()%」")
	CALL HTMLPRINTW(@"「그러면 가득 기분 좋게 해 준다%HTML_HEARTB()%」")
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의 하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서"거부"에 세트 된다)
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("거부")
	PRINTFORMW 「아무튼 그근처는 양보할 수 없는 것도 있을지도 몰라」
	PRINTFORMW 「…이 사치자째」
	RETURN 1
ENDIF

;────────────────────────────────────
;REACT7, 징계한다고 위협한다·개별 리액션
;────────────────────────────────────
;·선언해도징계·이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「무엇 포칸으로 해」
	PRINTFORMW 「피투성이의 역사가 다시 막을 올려요?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM"마음 약하게 응답한다"는 일정 조건으로 REACT분류"용서를 빌다"에 파생한다
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
	PRINTFORMW 「작은 소리로 보소보소 말해  것이 아니어요」
	PRINTFORMW 「이렇게 된 나를 제지당한 놈은 역사상 별로 없다」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 「이 기에 미쳐 아직 그런 태도로 있을 수 있다」
	PRINTFORMW 「자기의 존엄이 핀치일지도 몰라?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("더 하드하게 해")
	PRINTFORMW 「물건 폰의 돼지 자식이나 테메이는!!」
	PRINTFORMW 「…와 더 이상 옛 나를 자극하지 않고 주세요…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	PRINTFORMW 「간단하게 사 되는 라인을 조금 넘어버렸어이죠」
	PRINTFORMW 「이것을 기에 학습하면 좋아요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	PRINTFORMW 「아하하는 하! … 이이야! 좀 더 외쳐…!! ♪♪」
	PRINTFORMW 「와와 안 되는, 옛 김이 소생해 와버린다」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다")
	PRINTFORMW 「후후후…이이원 다른  표정…두근두근 와버린다♪」
	PRINTFORMW 「좀 더 나를 외경심 존경하는게  좋아요!! … 와 안 되는, 지금 것은 잊으며」
	RETURN 1
ENDIF

;────────────────────────────────────
;REACT8, 고함치다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_MALE_REACT_K44_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「그러니까 거기서 입다물지 않는거야」
	PRINTFORMW 「반론해 주세요반론」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
	PRINTFORMW 「그런 무기력은 안된다 개-의」
	PRINTFORMW 「 좀 더 용기라든지 내세요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	PRINTFORMW 「거기서 사과하지 않아요!」
	PRINTFORMW 「자신의 불 보람이 없음에 대해서 사과하세요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("비명지른다")
	PRINTFORMW 「소엉뚱한 소리 내 버려서…」
	PRINTFORMW 「앞일이 걱정되어요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다")
	PRINTFORMW 「거기! 벌벌 하지 않는다!」
	PRINTFORMW 「벌벌 시켜도 좋은 것은 자지만! … 라고 최악인가 나는」
	RETURN 1
ENDIF

;────────────────────────────────────
;REACT9, 기분나쁘게 웃다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_MALE_REACT_K44_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「입다물고 당겨  것이 아니어요」
	PRINTFORMW 「완전하게 아브 없는 사람을 보는 눈이 아니다」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
	PRINTFORMW 「아레를 하면 너는 견딜 수 있는 것일까♪」
	PRINTFORMW 「아니 실신할 가능성이 높네요…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	PRINTFORMW 「무엇? 아직 아무것도 말하지 않았어?」
	PRINTFORMW 「흥미 있다? 응 흥미 있는 것?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("비명지른다")
	PRINTFORMW 「그렇게 자주 그런 느낌으로 좋은 소리가 매일 저녁…」
	PRINTFORMW 「아무튼 지금은 기분은 없기 때문에 안심해」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다")
	PRINTFORMW 「소변은 끝마쳤는지 해들? 기원은? 방의 구석에서 덜컹덜컹 떨려 명걸 있고를 할 준비는ＯＫ!?」
	PRINTFORMW 「…조금 안색 블루 레이같이 되어있는 원이야. 농담이야 농담」
	RETURN 1
ENDIF


[SKIPSTART]
R#사가판에 실장되고 있었지만, 현행판에서는 존재하지 않는 커맨드인 것으로 comment out
상손 있는 상태에 회수할 수 있을 것 같으면 그 쪽으로 이했다 있고 의향@/L
;-------------------------------------------------
;REACT110, 사랑을 고하는·개별 리액션 FIRSTTIME(410)
;-------------------------------------------------
@KOJO_MALE_REACT_K32_110
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;-------------------------------------------------
	;소분기
	;-------------------------------------------------
	;상사상애
	;기입 체크
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && FAVOR(3000, 3000)
		;사랑을 받아들인다
		IF IS_COMGRONAME("태연하게 한다/적극적으로 한다/받아들인다")
;OriginalString : 「응, 고마워요%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@" 「응, 고마워요%HTML_HEARTB()%」")
			PRINTFORMW 「나 사랑을 받아들여 준다면 지금부터가 큰 일이어요?」
;OriginalString : 「매일 마를 여유도 없을 정도, 가득 시켜 주기 때문에%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@" 「매일 마를 틈도 없을 정도, 가득 시켜 주기 때문에%HTML_HEARTB()%」")
		;당황한다
		ELSEIF IS_COMGRONAME("소극적으로 한다")
			PRINTFORMW 「아직 헤매고 있어 이 우유부단」
;OriginalString : 「차라리 사나에의 일도 잊을 수 있을 정도로 짜내 줄까…%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@" 「차라리 사나에의 일도 잊을 수 있을 정도로 짜내 줄까…%HTML_HEARTB()%」")
		;거부
		ELSE
			PRINTFORMW 「거기서 거부인가」
;OriginalString : 「도～%HTML_HEARTB()%솔직가 아니기 때문에 %HTML_HEARTB()%(제손로 타마를 잡으면서)」")
			CALL HTMLPRINTW(@" 「도～%HTML_HEARTB()%솔직(이)가 아니기 때문에%HTML_HEARTB()%(제손그리고 타마를 잡으면서)」")
		ENDIF
		RETURN LOCAL
	ENDIF

	;일반
	;사랑을 받아들인다
	IF IS_COMGRONAME("태연하게 한다/적극적으로 한다/받아들인다")
		PRINTFORMW 「, 사실…!?」
		PRINTFORMW 「농담이라든지 그 자리의 기세등으로 말하고 있다면 하 넘어뜨려요」
;OriginalString : 「그렇지만 기쁜… 고마워요, %HTML_HEARTB()%」")
		CALL HTMLPRINTW(@" 「그렇지만 기쁘다… 고마워요, 츄%HTML_HEARTB()%」")
	;당황한다
	ELSEIF IS_COMGRONAME("소극적으로 한다")
		PRINTFORMW 「키득… 아무튼 갑자기 이런 일 말해져도 당황해버리는군요」
		PRINTFORMW 「헤매고 있을 뿐이라면 진심에 시켜버리는 원이야?」
	;거부
	ELSE
		PRINTFORMW 「그렇게… 아무튼 한결같은 것은 좋은 일이군요」
		PRINTFORMDW 스와자는 눈에 보여 쓸쓸히 하고 있다
	ENDIF
	RETURN LOCAL
ENDIF
[SKIPEND]
