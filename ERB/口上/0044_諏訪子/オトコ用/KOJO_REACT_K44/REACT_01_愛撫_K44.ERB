;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코구상 애무系個別リアクション구상ファイル
;
;	애무系リアクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;애무系 REACT10～19 & REACT27
;========================================================================
;・各REACT内の分け方としてTFLAG:REACT분류(IS_COMGRONAME)やSELECTCOM(IS_NOWCOMNAME)が考えられます
;・ただ、範囲の広い条件で拾ってしまうのは基本的にお勧めしません
;・ACT분류:애무系の場合、基本的に애무系COM(COM10~COM16)が선택肢となりますが
;・状況によってはその회화系、고통系、その他のものが선택肢に含まれる可能性もあります
;・リアクション(REACT)で処理されなかったものは汎用リアクション(COM)で補完されるので
;・無理に全て埋める필요はありません
;・필요と思うものを絞り込み、COMとの連携を考えて効率よく構築すべきです
;-------------------------------------------------
;・各REACT及びACT파생内は、&& IS_NOWCOMNAMEなり、&& IS_COMGRONAME等の条件で決めうちしましょう
;・ここに条件無し分岐があると、リアクションに関わらず、全て拾ってしまって汎用リアクションに流れません
;・要するに、想定당했다システムの破壊に繋がります
;-------------------------------------------------
;　基本的に地の文はTFLAG:REACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐し、
;　各REACT분류内でTFLAG:REACT부하(自然な행동/無理のない행동/無理を했다행동)で派生
;　ACT_MESSAGE.ERBを参照하면서の作業をお勧めします
;-------------------------------------------------
;　取り敢えずACT파생毎に枠を切り分けてありますが、一つに纏めた上で汎用的な記述で손間を減らすという손もあります
;　正直、やり方はいくらでもあるので、以下に示す方法は一つの例と思ってください
;-------------------------------------------------

;────────────────────────────────────
;REACT10, 손으로 애무・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=애무/1=수음/2=페니스밴드수음)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_10
;-------------------------------------------------
;・ACTTION_APPLY2の処理なし
;・地の文はACT파생毎にREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 수음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(70) && TFLAG:ACT파생 == 1
	IF IS_COMGRONAME("소극적으로 한다/태연하게 한다/적극적으로 한다/受け入れる/용서를 빌다/")
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				PRINTFORMW 「기분이 좋다？이봐요」
;OriginalString : 「ほらほらほらぁ%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「이봐요 이봐요 이봐요%HTML_HEARTB()%」")
			CASE 1
;OriginalString : 「참다汁出しすぎじゃない…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「참다국물 너무 내지 않아…%HTML_HEARTB()%」")
			CASE 2
				PRINTFORMW 「자신으로부터 허리 움직여 버린다 원이야？」
;OriginalString : 「しょうがない奴%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「어쩔 수 없는 녀석%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0

[SKIPSTART]
남자限定구상なのでコメントアウト
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2, 페니스밴드수음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
ENDIF
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 애무
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && !TFLAG:ACT파생
ENDIF
[SKIPEND]

;────────────────────────────────────
;REACT11, 가슴애무・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=가슴애무/1=乳首吸い/2=揉み合い/3=乳首責め)
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
;・地の文はACT파생2, 揉み合いとその他で分岐
;・ACTION_APPLY2に於いて、ACT파생2の場合は受容、거부の判定処理あり
;・ACT파생2, 揉み合いにおいてREACT분류(소극적으로 한다/태연하게 한다/적극적으로 한다/受け入れる)以外は"거부"扱い
;-------------------------------------------------
;　派生毎にまともにやると손間が尋常ではないので、ACT파생分岐の上層で汎用処理했다り
;　各ACT파생内のCOM分岐等は一部のものに絞ってしまうのが良いと思います
;　またACT파생で区切らずに、大分岐でまとめてしまう손もありますが、손の込んだ事をやるには不向きであると考えます
;-------------------------------------------------
@KOJO_MALE_REACT_K44_11
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 가슴애무
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && !TFLAG:ACT파생
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 乳首吸い
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자に素質(유치/혀기술)がない場合は発生する確立は低い
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2, 揉み合い
;━━━━━━━━━━━━━━━━━━━━━━━━
;봉사系COMが선택肢となる場合あり
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생3, 乳首責め
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자に素質(유치/혀기술)がない場合は発生する確立が高い
;죠교대상이남자の場合はここにしか落ちない
;-------------------------------------------------
IF 구상記入チェック(70) && TFLAG:ACT파생 == 3
	IF IS_COMGRONAME("소극적으로 한다/태연하게 한다/적극적으로 한다/受け入れる/용서를 빌다")
		SELECTCASE RANDREACT(3, 30)
			CASE 0
				PRINTFORMW 「낯간지럽다고？」
;OriginalString : 「すぐ気持ちよくなるわよ%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「곧 기분 좋아져요%HTML_HEARTB()%」")
			CASE 1
;OriginalString : 「いい顔するじゃない…%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「좋은 얼굴 하지 않아…%HTML_HEART()%」")
;OriginalString : 「今여자みたいよキミ%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「지금여자같아요 너%HTML_HEARTB()%」")
			CASE 2
				PRINTFORMW 「유두 코리코리로 해 버려서…」
				PRINTFORMW 「점점 느껴 왔는지？」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0

;────────────────────────────────────
;REACT12, 입으로 애무・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=クンニ/1=펠라)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_12
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 펠라
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(80) && TFLAG:ACT파생 == 1
	IF IS_COMGRONAME("소극적으로 한다/태연하게 한다/적극적으로 한다/受け入れる/용서를 빌다")
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				PRINTFORMW 「끝(쪽)이 이이의？ 그렇지 않으면 뒤스지？」
;OriginalString : 「レロレロレロ…くすっ、どっちも反応するじゃない%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「레로레로레로…키득, 어느 쪽도 반응하지 않아%HTML_HEARTB()%」")
			CASE 1
;OriginalString : 「じゅぷっじゅぷっ…ぷはっ%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「…푸핫%HTML_HEART()%」")
				PRINTFORMW 「입 중(안)에서 움켜 뜨고 있어 들키고 들켜라？」
			CASE 2
;OriginalString : 「さっきから참다汁出しっぱなし%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「조금 전부터참다국물 내고 있을 뿐%HTML_HEARTB()%」")
;OriginalString : 「そんなに飲ませたいのかなぁ…%HTML_HEART()%　쿠쿡」")
				CALL HTMLPRINTW(@"「그렇게 먹이고 싶은 것인지…%HTML_HEART()% 킥킥」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, クンニ
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && !TFLAG:ACT파생
;ENDIF


;────────────────────────────────────
;REACT13, 항문애무・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생（0=항문애무/1=전립선자극/2=항문바이브出し入れ）
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_13
;-------------------------------------------------
;・地の文はACT파생で分岐。ACT파생1, 2分岐内の派生なし
;・ACT파생0, 항문애무のみREACT분류(受け入れる/적극적으로 한다, 태연하게 한다/용서를 빌다/소극적으로 한다)で派生処理される
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT13, 항문애무/전립선자극/항문바이브出し入れ
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0においては可能性のあるCOMを網羅しておきます
;필요に応じて分岐を増減、ないし構造を改変して使用して下さい
;-------------------------------------------------
IF 구상記入チェック(70) && INRANGE(TFLAG:ACT파생, 0, 2)
	IF IS_COMGRONAME("소극적으로 한다/태연하게 한다/적극적으로 한다/受け入れる/용서를 빌다")
		SELECTCASE RANDREACT(3, 0)
			CASE 0
;OriginalString : 「ヒクヒクさせちゃってぇ%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「흠칫흠칫 시켜 버려어%HTML_HEARTB()%」")
				PRINTFORMW 「 좀 더 갖고 싶은거야？ 팔아♪」
			CASE 1
				PRINTFORMW 「 아직 익숙해지지 않는다？」
;OriginalString : 「大丈夫よ、そのうち良くなるから%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「괜찮아요, 그 중 좋아지기 때문에%HTML_HEARTB()%」")
			CASE 2
;OriginalString : 「いいよ…%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「좋아…%HTML_HEART()%」")
;OriginalString : 「もっとその顔見せて%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「좀 더 그 얼굴 좀 보여줘%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 전립선자극
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2, 항문바이브出し入れ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
ENDIF
[SKIPEND]

;────────────────────────────────────
;REACT14, 항문펠라・個別リアクション
;────────────────────────────────────
;・ACT파생なし。ACTION_APPLY2の処理なし
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_14
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT14, 항문펠라
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(70) && !TFLAG:ACT파생
	IF IS_COMGRONAME("소극적으로 한다/태연하게 한다/적극적으로 한다/受け入れる/용서를 빌다")
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				PRINTFORMW 「더럽기 때문에 그만두어라고？」
				PRINTFORMW 「뭐 말하고 있어. 자지도 같은 것이야♪」
			CASE 1
;OriginalString : 「舐められるたんびにヒクヒクしてる…%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「지에 흠칫흠칫 하고 있다…%HTML_HEART()%」")
				PRINTFORMW 「이제 느껴 왔지 않는다？」
			CASE 2
				PRINTFORMW 「무서운거야？ 괜찮아♪」
;OriginalString : 「全部私に任せなさい%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「전부 나에게 맡기세요%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0

;────────────────────────────────────
;REACT15, 키스・個別リアクション
;────────────────────────────────────
;・ACT파생なし。ACTION_APPLY2の処理なし
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;その他反応
;	COM:자위를 시작한다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT15, 키스
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(70) && !TFLAG:ACT파생
	IF IS_COMGRONAME("소극적으로 한다/태연하게 한다/적극적으로 한다/受け入れる/용서를 빌다")
		SELECTCASE RANDREACT(3, 0)
			CASE 0
;OriginalString : 「んちゅっ…れろれろぉ…ぁはぁ%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「응츗…날름날름…아하아%HTML_HEART()%」")
;OriginalString : 「中々上손いじゃない%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「꽤 위손가 아니다%HTML_HEARTB()%」")
			CASE 1
;OriginalString : 「んん…%HTML_HEARTB()%　もっと舌絡めて…%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「응응…%HTML_HEARTB()% 좀 더 혀 관련되어…%HTML_HEART()%」")
;OriginalString : 「ちゅぴ…ちゅぷっ…ちゅっ%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「…츄풋…츗%HTML_HEARTB()%」")
			CASE 2
;OriginalString : 「恥ずかしがらないの%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「부끄러워하지 않는거야%HTML_HEARTB()%」")
;OriginalString : 「はぁむ…くちゅ…%HTML_HEART()%　れろぉ」")
				CALL HTMLPRINTW(@"「…쿠츄…%HTML_HEART()% 날름」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0


;────────────────────────────────────
;REACT16, 성기마찰한다・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생（=0,貝あわせ/=1,스마타/=2,페니스밴드스마타）
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_16
;-------------------------------------------------
;・地の文はACT파생毎にREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다, 소극적으로 한다)で分岐
;・필요に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생1, 스마타
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
	IF IS_COMGRONAME("태연하게 한다/적극적으로 한다/受け入れる") ;//적극的受容である場合に限定
		SELECTCASE RANDREACT(3, 0)
			CASE 0
;OriginalString : 「あはっ%HTML_HEART()%　気持ちいい？」")
				CALL HTMLPRINTW(@"「아핫%HTML_HEART()% 기분이 좋다？」")
;OriginalString : 「すっごいチンポ反り返ってるぅ%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「굉장한 자지 뒤로 젖히고 있다아%HTML_HEARTB()%」")
			CASE 1
;OriginalString : 「ん…はぁっ%HTML_HEARTB()%　ダメッ%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「응…하앗%HTML_HEARTB()% 안됏%HTML_HEART()%」")
;OriginalString : 「腰動かしすぎぃ…%HTML_HEART()%　クリに当たってるよぉ%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「허리 너무 움직이고…%HTML_HEART()% 클리에 해당되고 있는%HTML_HEARTB()%」")
			CASE 2
;OriginalString : 「自分から擦りつけてくるなんて…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「자신으로부터 문질러 오다니…%HTML_HEARTB()%」")
;OriginalString : 「ほんっとにエロなんだからぁ%HTML_HEART()%　はぁ♪　ヌルヌルいい…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「정말 에로한 것이니까%HTML_HEART()% 하아♪ 미끈미끈 좋다…%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생2, 페니스밴드스마타
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 貝あわせ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && !TFLAG:ACT파생
ENDIF
[SKIPEND]

;────────────────────────────────────
;REACT17, 파이즈리한다・個別リアクション
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:더 하드하게 해
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:고통을 참는다/비명지른다/날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_17
;-------------------------------------------------
;・地の文はCOM:허리를 흔든다、REACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;・애무系COMを網羅。필요に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT17, 파이즈리한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:ACT파생
	IF IS_COMGRONAME("태연하게 한다/적극적으로 한다/受け入れる") ;//적극的受容である場合に限定する
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				PRINTFORMW 「……」
;OriginalString : 「ぁん…%HTML_HEARTB()%　ねぇ気持ちいーい？」")
				CALL HTMLPRINTW(@"「앙…%HTML_HEARTB()% 저기 기분이 좋다？」")
			CASE 1
;OriginalString : 「腰動いちゃってるじゃない…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「허리 움직여 버리고 있지 않아…%HTML_HEARTB()%」")
;OriginalString : 「もっともっと、乳首にこすってぇ%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「좀더 좀더, 유두에 비비어어%HTML_HEARTB()%」")
			CASE 2
;OriginalString : 「れろぉー…くちゅくちゅ%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「날름-…질척질척%HTML_HEARTB()%」")
				PRINTFORMW 「침에서 좀 더 누르눌이 되었겠죠？」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0

;────────────────────────────────────
;REACT18, 풋잡하기・個別リアクション
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_18
;-------------------------------------------------
;・地の文はREACT분류(受け入れる/적극적으로 한다, 태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;・필요に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT18, 풋잡하기
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(70) && !TFLAG:ACT파생
	IF IS_COMGRONAME("태연하게 한다/적극적으로 한다/受け入れる") ;//적극的受容である場合に限定する
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				PRINTFORMW 「자꾸자꾸 누르눌 나오고 있잖아…」
;OriginalString : 「こーゆーのが好きだったんだねぇ%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「이런 것이 좋아했던 것이구나%HTML_HEARTB()%」")
			CASE 1
				PRINTFORMW 「등, 양 다리로 끼워 쫄깃쫄깃이라고」
;OriginalString : 「손とどっちが気持ちいいのかしらね%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「손(와)과 어느 쪽이 기분이 좋은 것일까요%HTML_HEARTB()%」")
			CASE 2
;OriginalString : 「タマも踏んじゃおうかな…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「타마도 밟아 버릴까…%HTML_HEARTB()%」")
				PRINTFORMW 「거짓말 거짓말, 두근두근 오겠죠？」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;条件にヒットしなければ汎用リアクションへ
RETURN 0

;────────────────────────────────────
;REACT19, 손가락삽입한다
;────────────────────────────────────
;・TFLAG:ACT파생(0=손가락삽입한다/1=Ｇスポット刺激/2=바이브出し入れ)
;-------------------------------------------------
;想定されるCOM
;애무系反応
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:날뛴다
;-------------------------------------------------
;@KOJO_MALE_REACT_K44_19
;-------------------------------------------------
;地の文はACT파생0のみREACT분류で分岐
;その他はTALENT:MASTER:후타나리であるか否かで派生するのみ
;-------------------------------------------------
;남자限定구상では不要
;RETURN 0

;────────────────────────────────────
;REACT27, 밀어 넘어트린다・個別リアクション
;────────────────────────────────────
;TFLAG:ACT파생（=0,밀어 넘어트린다/=1,밀어넘어짐終了）
;-------------------------------------------------
;想定されるCOM
;회화系反応
;	COM:마음 약하게 응답한다/무뚝뚝하게 응답한다/상냥하게 응답한다/강하게 응답한다/용서를 빌다/기분좋게 해
;고통系反応
;	COM:무서워한다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_27
;-------------------------------------------------
;地の文はREACT분류(受け入れる/적극적으로 한다, 태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;ACT파생毎の分岐はなしかつ、内容的にはACT파생0のもの
;ACT파생1の場合は整合性が崩れると思われる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 밀어 넘어트린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;内容的に条件なしRANDで分けるべきものではないので、個別に切り分ける
	SELECTCASE COMGRONAME(TFLAG:REACT분류)
		CASE "태연하게 한다", "적극적으로 한다", "受け入れる"
			PRINTFORMW 「엉망으로 만드는 든지와」
;OriginalString : 「もう、期待しちゃってるじゃない%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「벌써, 기대해 버리고 있지 않아%HTML_HEARTB()%」")
		CASE "날뛴다", "거부", "逃げる"
			PRINTFORMW 「이거 참, 날뛰지 않는거야」
;OriginalString : 「これから気持ちよくするんだから%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「지금부터 기분 좋게 하기 때문에%HTML_HEARTB()%」")
		CASEELSE
			PRINTFORMW 「하하 말해 버려서…」
;OriginalString : 「キミにはやられるほうがお似合いね%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「너에게는 당하는 편이 어울리구나%HTML_HEARTB()%」")
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생0, 밀어넘어짐終了
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
;ENDIF
