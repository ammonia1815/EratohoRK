;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코구상 その他죠교이벤트구상ファイル
;
;	その他죠교ターン中이벤트をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;EV21, 大만족구상
;────────────────────────────────────
;・죠교자の대만족보너스を취득했다場合に表示
;-------------------------------------------------
@KOJO_MALE_EVENT_K44_21(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;스와코さま大만족
;━━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
;OriginalString : 「ん…%HTML_HEARTB()%」")
	CALL HTMLPRINTW(@"「응…%HTML_HEARTB()%」")
;OriginalString : 「今の良かったわよ…%HTML_HEARTB()%」")
	CALL HTMLPRINTW(@"「지금의 좋았어요…%HTML_HEARTB()%」")
	RETURN 2
ENDIF

;────────────────────────────────────
;EV22, 상태변화구상
;────────────────────────────────────
;・상태이상が付いたときに表示
;・TCVAR:상태변화が죠교자、TCVAR:MASTER:상태변화が죠교대상
;・상태변화が起こると3ターン지속する
;-------------------------------------------------
@KOJO_MALE_EVENT_K44_22(ARG)
;-------------------------------------------------

;EV22_0, 죠교자상태변화
;**********************
;━━━━━━━━━━━━━━━━━━━━━━━━
;스와코さま[피폐]化  소모時：疲労のため消極的행동が우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(50) && IS_CONDITION(TARGET, "피폐")
	;=================================================
	;조교중初回
	;=================================================
	IF ONCEEVENT(0)
		PRINTFORMW 「―, 조금 탄마…」
		PRINTFORMW 「유석에는 사례품 지났는지 해들…」
		RETURN 2
	ENDIF

	;条件なし
	PRINTFORMW 「어쩐지 지쳐 왔어요…」
	PRINTFORMW 「조금은 노 깨세요…」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스와코さま[쇠약]化  強소모時：疲労のため消極的행동が우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_CONDITION(TARGET, "쇠약")
	;=================================================
	;조교중初回
	;=================================================
	IF ONCEEVENT(1)
		PRINTFORMW 「--…아직！」
		PRINTFORMW 「아직도 이것인가…ｵﾛﾛﾛｫ」
		RETURN 2
	ENDIF

	;条件なし
	PRINTFORMW 「 어째서 이렇게 지쳤는지 해들…」
	PRINTFORMW 「어제 카나자와 도철 너무 한 탓…？」
	RETURN 2
ENDIF

;EV22_1, 죠교대상상태변화
;************************
;記入箇所なし

;────────────────────────────────────
;EV23, 죠교대상能力値アップ구상
;────────────────────────────────────
;・조교중に죠교대상の能力値が上昇했다際に表示
;-------------------------------------------------
@KOJO_MALE_EVENT_K44_23(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(今回上昇했다ABL번호/各ARG번호はABLのそれに相当)

;ARG:0 순종/1 욕망/2 기교/3 Ｃ감각/4 Ｖ감각/5 Ａ감각/6 Ｂ감각/7 봉사정신/8 노출증
;    9 레즈끼/10 ＢＬ끼/11 마조끼/12 자위중독/13 정액중독/14 레즈중독/15 고통중독/16 구속중독

;ARG:20 회화/21 애무/22 도구/23 성교/24 수치/25 봉사/26 가학/27 이상
;-------------------------------------------------
IF 구상記入チェック(100)
	;一行空ける
	PRINTL
	;今回上昇했다ABLの派生を見る
	SELECTCASE ARG
		;죠교대상[순종]アップ
		CASE 0
			PRINTFORMW 「순수에…순수에…」
			RETURN 2
		;죠교대상[욕망]アップ
		CASE 1
			PRINTFORMW 「거기, 하하 하지 않는다」
			RETURN 2
		;죠교대상[기교]アップ
		CASE 2
			PRINTFORMW 「조금 능숙해졌지 않은거야…♪」
			RETURN 2
		;죠교대상[Ｃ감각]アップ
		CASE 3
			PRINTFORMW 「너무 민감해도 큰 일이지 않다？」
			RETURN 2
		[SKIPSTART]
		//남자用なのでコメントアウト
		;죠교대상[Ｖ감각]アップ
		CASE 4
			PRINTFORMW 「어디에 보지 붙고 있어」
			RETURN 2
		[SKIPEND]
		;죠교대상[Ａ감각]アップ
		CASE 5
			PRINTFORMW 「눈을 떠라…！」
			RETURN 2
		;죠교대상[Ｂ감각]アップ
		CASE 6
			PRINTFORMW 「좋았어요, 스스로 빨거나 하면 좋아요」
			RETURN 2
		;その他
		CASEELSE
			RETURN 0
	ENDSELECT
ENDIF

;────────────────────────────────────
;EV24, 죠교자能力値アップ구상
;────────────────────────────────────
;・조교중に죠교자の能力値が上昇했다際に表示
;-------------------------------------------------
@KOJO_MALE_EVENT_K44_24(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(今回上昇했다ABL번호)

;ARG:0 순종/1 욕망/2 기교/3 Ｃ감각/4 Ｖ감각/5 Ａ감각/6 Ｂ감각/7 봉사정신/8 노출증
;    9 레즈끼/10 ＢＬ끼/11 마조끼/12 자위중독/13 정액중독/14 레즈중독/15 고통중독/16 구속중독

;ARG:20 회화/21 애무/22 도구/23 성교/24 수치/25 봉사/26 가학/27 이상
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;스와코さま[욕망]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 1
	PRINTL
	PRINTFORMW 「 나의 욕망이라면 맨틀보다 깊어요？」
	RETURN 2
ENDIF

[SKIPSTART]
;────────────────────────────────────
;EV29, 굴복・고통각인による반항각인上限処理
;────────────────────────────────────
;・반항각인Lv上限の処理
;・반항각인は10-(고통각인+굴복각인)/2を超えない
;-------------------------------------------------
@KOJO_MALE_EVENT_K44_29(ARG)
;-------------------------------------------------
;%CALLNAME:TARGET%의 与える쾌락\@ MARK:MASTER:고통각인 ? と고통 # \@が%CALLNAME:MASTER%から반항の기력を奪っていく…
;반항각인は{MARK:반항각인}되었다
;-------------------------------------------------
IF 구상記入チェック(0)
ENDIF

;========================================================================
;각인취득구상
;========================================================================
;・조교중に각인を취득시켰다場合に表示。どのレベルで구상表示するかは加減자유なので個々で調整するのが望ましい
;・記入チェックが=1の場合、同時に複数취득했다ときはこのファイルで一番上のものだけを表示する
;・=2の場合、취득했다각인の구상すべてを上から順に表示する
;・각인취득による素質変動の前に表示するため、素質취득/喪失するかはこの時点でのTALENTの有無で判定できる
;-------------------------------------------------
@KOJO_MALE_MARK_K44(ARG:0, ARG:1, ARG:2, ARG:3, ARG:4)
#DIM 記入チェック
#DIM すべて順に表示
すべて順に表示 = 0	;각인취득구상「すべて順に表示」のための初期化処理
;-------------------------------------------------
;引数内訳…
;ARG:(각인번호)=(今回취득했다レベル)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;[트라우마]취득
記入チェック = 0	;0.非表示/1.同時に취득했다각인をこれ以下表示しない/2.すべて順に表示する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[고통각인]취득
記入チェック = 0	;0.非表示/1.同時に취득했다각인をこれ以下表示しない/2.すべて順に表示する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[반항각인]취득
記入チェック = 0	;（0, 非表示、1, 同時に취득했다각인をこれ以下表示しない、2, すべて順に表示する）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG:3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[굴복각인]취득
記入チェック = 0	;（0, 非表示、1, 同時に취득했다각인をこれ以下表示しない、2, すべて順に表示する）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[쾌락각인]취득
記入チェック = 0	;（0, 非表示、1, 同時に취득했다각인をこれ以下表示しない、2, すべて順に表示する）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:1
ENDIF

;각인취득구상「すべて順に表示」のための終端処理
RETURN すべて順に表示 != 0
[SKIPEND]
