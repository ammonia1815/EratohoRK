;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코구상 죠교종료이벤트구상파일
;
;	죠교종료이벤트구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 죠교종료구상
;========================================================================
;·@EVENTCOMEND로부터 불려 간다
;·구상주가죠교종료하기 직전에 표시
;·ARG:1, ARG:2의 경우는 지문 표시 후에 PRINTL로 공행. 그 뒤로구상를 표시
;·ARG:0의 경우는 고유의 지문 없음. 구상표시전에 PRINTL로 공행이 들어간다
;·RESULT 0이상을반환와구상표시 후에 PRINTL로 공행이 들어간다
;-------------------------------------------------
;관련 변수…
;FLAG:일상제어	죠교종료시 상황(1=통상/2=정력 0/3=일회휴게/4=실신했다)
;-------------------------------------------------
@KOJO_MALE_EVENT_K44_13(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 보통으로 종료, 1, 죠교대상가 피곤한, 2, 죠교대상가실신했다)
;-------------------------------------------------

;────────────────────────────────────
;ARG:1, ARG:2, 피로·실신에 의한죠교종료 FIRSTEVENT(0)～
;────────────────────────────────────
;·죠교대상소모도에 응해 피로/실신로 나누어진다
;·호출 타이밍은 지문의 표시→PRINTL→구상의 표시
;·EVENT201, 간호(일상이벤트)의 개소를 쓰는 경우는, 상손구제휴할 수 있도록(듯이)하면 좋을 것입니다
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 피곤함
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가실신했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 2
	PRINTFORMW 「야, 큰일난 너무 했다…」
	PRINTFORMW 「아휴, 이 정도로 실신하다니 아직도 근성이 부족하네요. 뭐,내가 나쁘지만」
	RETURN 1
ENDIF

;────────────────────────────────────
;ARG:0보통으로 종료 FIRSTEVENT(10)～
;────────────────────────────────────
;·통상의죠교종료시의구상
;·지문 없음. PRINTL에 의한 공행의 뒤로구상표시
;·테두리마다이벤트번호를 털어 있습니다만, 임의로 설정하는 일은 가능합니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;대만족보너스를 얻고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:금회대만족
	;=================================================
	;처음
	;=================================================
	IF FIRSTEVENT(10)
		PRINTFORMW 「꽤 좋았어요,○주어버린다♪」
		PRINTFORMW 「아니 아직 인정한 것이 아니고, 본궤도에 오르지 않는거야」
		RETURN 1
	ENDIF

	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「ん%HTML_HEARTB()%　合格合格%HTML_HEARTB()%　よかったわよ」")
		CALL HTMLPRINTW(@"「응%HTML_HEARTB()% 합격 합격%HTML_HEARTB()% 좋았어요」")
		PRINTFORMW 「다음도 좀 더 만족 당하듯이 노력해」
		RETURN 1
	ENDIF
ENDIF

;*************************************************
;이하, 호감도에 의한 파생
;*************************************************

;━━━━━━━━━━━━━━━━━━━━━━━━
;상사상애（호의 3000 이상·호감도 3000 이상）
;-------------------------------------------------
;·서로 서로 좋아하고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("상사상애")
	;=================================================
	;처음
	;=================================================
	;IF 구상기입 체크(0) && FIRSTEVENT(20)
		;처음구상는 EV2:엔딩에 이전
	;ENDIF

	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「はぁ…%HTML_HEARTB()%　幸せってこういうのを言うのかしらね」")
		CALL HTMLPRINTW(@"「하아…%HTML_HEARTB()% 행복하다는 것은 이런 것을 말할까요」")
		PRINTFORMW 「좀 더 이대로 얼싸안아 싶은거야. 떨어지면 싫어요?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교완료（호의 2000 이상）
;-------------------------------------------------
;·죠교대상가구상 주로 이제(벌써) 헤롱헤롱
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("죠교완료")
	PRINTFORMW 「은 가득 차고 충분한 얼굴 해당신은…」
	PRINTFORMW 「네, 이제 사나에가 아니고 내가 좋다고? , 무엇 말하기 시작해 자기는」
	PRINTFORMDW 의외로만갱에서도 없을 것 같은 스와자였다.
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;익애（호감도 2000 이상）
;-------------------------------------------------
;·죠교자가죠교대상에 빠져 버려, 깊기도 하고 도착했다 사랑을 따르고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("익애")
	;=================================================
	;처음
	;=================================================
	IF FIRSTEVENT(40)
		PRINTFORMW 「최근 자기의 일이 머리로부터 멀어지지 않아요…여러가지 의미로」
		PRINTFORMW 「어떻게 해 버렸어 우두머리군요, 나답지도 않다…」
		PRINTFORMW 「아--, 역시 아무것도 아니다! 여기의 이야기! 신경쓰지 마!!」
		RETURN 1
	ENDIF

	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「당신은 사나에를 좋아해. 별로 아무것도 아닌데」
		PRINTFORMW 「이것도 전부 사나에를 위해 하고 있는 일이야. 별로 아무것도 아닌데」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교가 끝난 상태（호의 1000 이상）
;-------------------------------------------------
;·죠교대상가구상 주로죠교 되는 것을 행복하게 느끼고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("죠교가 끝난 상태")
	PRINTFORMW 「매우 솔직해진 것이군요. 기쁜데」
	PRINTFORMW 「콧노래를 부르며 일하는 모양에 사정하는 놈 처음으로 보았어요…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;불구대천（호의-500）
;------------------------------------------------
;·거듭되는 심한 처사에, 마음속으로부터죠교자를 미워하고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("불구대천")
	PRINTFORMW 「완전히 미움받아 버렸다일까?」
	PRINTFORMW 「계속할지 어떨지는 당신이 결정해도 좋아. 호
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;공포（호의 200 미만·트라우마Lv3 이상）
;-------------------------------------------------
;·마음에 상처를 만들어져주인님의 모습을 보는 것만으로 무서워해 버린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("공포")
	PRINTFORMW 「어머, 어쩐지 묘하게 무서워해 버려서」
	PRINTFORMW 「그렇게%ACTSTR(CFLAG:MASTER:트라우마)%하지만 아레였어요? 안되어요 분명하게 받아들이지 않으면」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;반항（호의 0 이하·반항각인Lv1 이상）
;-------------------------------------------------
;·마음을 열지 않는노예에 초조해지는주인님
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("반항") && !FLAG:END달성
	PRINTFORMW 「이제! 분명하게 하는 일 (듣)묻지 않으면 사나에를 만나게 하지 않아!」
	PRINTFORMW 「어머나 얌전해졌다. 완전히 어떻게 한 것인가…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;눈을 뜨고（호의 500 이상）
;-------------------------------------------------
;·죠교 되는 것도 오히려 좋아하게 되었는지도 모른다…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("눈을 뜸")
	PRINTFORMW 「최근 어쩐지 즐기고 있는이 아닌거야」
	PRINTFORMW 「여유가 태어나는 것은 좋은 일이예요♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;순응（호의 100 이상·호감도 100 이상）
;-------------------------------------------------
;·정중하게죠교를 진행되어, 생각했던 것보다 싫지 않을지도…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("순응")
	PRINTFORMW 「어쩐지 조금씩 익숙해져 온 것 같구나」
	PRINTFORMW 「조금은 능숙해지고 있는 원이야?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;초반（조건 없음）
;-------------------------------------------------
;·여기까지의 조건에 히트 하지 않았던 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF FIRSTEVENT(110)
		PRINTFORMW 「응, 역시 아직도구나. 그런 응은 사나에는 맡길 수 없어」
		PRINTFORMW 「그런 실망 하는 것 없어. 그러니까 내가 남자를 닦아 준다 라고 하고 있어♪」
		PRINTFORMW 「사나에에 적당한 남자가 되어 받기 위해. 응은 다음은 밤에 하기 때문에 천천히 쉬고 있어」
		RETURN 1
	ENDIF

	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「수고 하셨습니다. 일단 돌아가도 괜찮지만 매일 여기에 다니도록(듯이)」
		PRINTFORMW 「자택에는 신파워로 날려 주기 때문에. 열매 꼭지에 텐구라든지에 발견되어도 귀찮고」
		RETURN 1
	ENDIF
ENDIF
