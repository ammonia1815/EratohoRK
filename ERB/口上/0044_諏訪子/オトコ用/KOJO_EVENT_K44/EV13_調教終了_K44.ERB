;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코구상 죠교終了이벤트구상ファイル
;
;	죠교終了이벤트구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 죠교終了구상
;========================================================================
;・@EVENTCOMENDから呼び出される
;・구상主が죠교終了する直前に表示
;・ARG:1, ARG:2の場合は地の文表示後にPRINTLで空行。その後に구상を表示
;・ARG:0の場合は固有の地の文なし。구상表示の前にPRINTLで空行が入る
;・RESULT 0以上을반환と구상表示後にPRINTLで空行が入る
;-------------------------------------------------
;関連変数…
;FLAG:일상제어	죠교終了時状況(1=통상/2=精力0/3=일회휴게/4=실신했다)
;-------------------------------------------------
@KOJO_MALE_EVENT_K44_13(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 普通に終了、1, 죠교대상が疲れている、2, 죠교대상が실신했다)
;-------------------------------------------------

;────────────────────────────────────
;ARG:1, ARG:2, 疲労・실신による죠교終了 FIRSTEVENT(0)～
;────────────────────────────────────
;・죠교대상소모도に応じて疲労/실신に分かれる
;・呼び出しタイミングは地の文の表示→PRINTL→구상の表示
;・EVENT201, 介抱(일상이벤트)の箇所を書く場合は、上손く連携できるようにすると良いでしょう
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が疲れている
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が실신했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 2
	PRINTFORMW 「야, 큰일난 너무 했다…」
	PRINTFORMW 「아휴, 이 정도로 실신하다니 아직도 근성이 부족하네요. 뭐,내가 나쁘지만」
	RETURN 1
ENDIF

;────────────────────────────────────
;ARG:0 普通に終了 FIRSTEVENT(10)～
;────────────────────────────────────
;・통상の죠교終了時の구상
;・地の文なし。PRINTLによる空行の後に구상表示
;・枠毎に이벤트번호を振ってありますが、任意に設定する事は可能です
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;대만족보너스を得ている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:금회대만족
	;=================================================
	;처음
	;=================================================
	IF FIRSTEVENT(10)
		PRINTFORMW 「꽤 좋았어요,○주어버린다♪」
		PRINTFORMW 「아니 아직 인정한 것이 아니고, 본궤도에 오르지 않는거야」
		RETURN 1
	ENDIF

	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「ん%HTML_HEARTB()%　合格合格%HTML_HEARTB()%　よかったわよ」")
		CALL HTMLPRINTW(@"「응%HTML_HEARTB()% 합격 합격%HTML_HEARTB()% 좋았어요」")
		PRINTFORMW 「다음도 좀 더 만족 당하듯이 노력해」
		RETURN 1
	ENDIF
ENDIF

;*************************************************
;以下、호감도による派生
;*************************************************

;━━━━━━━━━━━━━━━━━━━━━━━━
;상사상애（호의3000以上・호감도3000以上）
;-------------------------------------------------
;・互いに好き合っている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("상사상애")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTEVENT(20)
		;처음구상はEV2：エンディングに移設
	;ENDIF

	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
;OriginalString : 「はぁ…%HTML_HEARTB()%　幸せってこういうのを言うのかしらね」")
		CALL HTMLPRINTW(@"「하아…%HTML_HEARTB()% 행복하다는 것은 이런 것을 말할까요」")
		PRINTFORMW 「좀 더 이대로 얼싸안아 싶은거야. 떨어지면 싫어요？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교完了（호의2000以上）
;-------------------------------------------------
;・죠교대상が구상主にもうメロメロ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("죠교完了")
	PRINTFORMW 「은 가득 차고 충분한 얼굴 해당신은…」
	PRINTFORMW 「네, 이제 사나에가 아니고 내가 좋다고？, 무엇 말하기 시작해 자기는」
	PRINTFORMDW 의외로만갱에서도 없을 것 같은 스와자였다.
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;익애（호감도2000以上）
;-------------------------------------------------
;・죠교자の方が죠교대상にはまってしまい、深かったり倒錯했다愛を注いでいる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("익애")
	;=================================================
	;처음
	;=================================================
	IF FIRSTEVENT(40)
		PRINTFORMW 「최근 자기의 일이 머리로부터 멀어지지 않아요…여러가지 의미로」
		PRINTFORMW 「어떻게 해 버렸어 우두머리군요, 나답지도 않다…」
		PRINTFORMW 「아-―, 역시 아무것도 아니다！여기의 이야기！신경쓰지 마！！」
		RETURN 1
	ENDIF

	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「당신은 사나에를 좋아해. 별로 아무것도 아닌데」
		PRINTFORMW 「이것도 전부 사나에를 위해 하고 있는 일이야. 별로 아무것도 아닌데」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교済み（호의1000以上）
;-------------------------------------------------
;・죠교대상が구상主に죠교されるのを幸せに感じている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("죠교済み")
	PRINTFORMW 「매우 솔직해진 것이군요. 기쁜데」
	PRINTFORMW 「콧노래를 부르며 일하는 모양에 사정하는 놈 처음으로 보았어요…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;不倶戴天（호의-500）
;------------------------------------------------
;・度重なる酷い仕打ちに、心の底から죠교자を憎んでいる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("不倶戴天")
	PRINTFORMW 「완전히 미움받아 버렸다일까？」
	PRINTFORMW 「계속할지 어떨지는 당신이 결정해도 좋아. 호
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;공포（호의200未満・트라우마Lv3以上）
;-------------------------------------------------
;・心に傷を作られ、주인님の姿を見るだけで怯えてしまう
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("공포")
	PRINTFORMW 「어머, 어쩐지 묘하게 무서워해 버려서」
	PRINTFORMW 「그렇게%ACTSTR(CFLAG:MASTER:트라우마)%하지만 아레였어요？ 안되어요 분명하게 받아들이지 않으면」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;반항（호의0以下・반항각인Lv1以上）
;-------------------------------------------------
;・心を開かない노예に苛つく주인님
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("반항") && !FLAG:END달성
	PRINTFORMW 「 이제！분명하게 하는 일 (듣)묻지 않으면 사나에를 만나게 한 없어！」
	PRINTFORMW 「어머나 얌전해졌다. 완전히 어떻게 한 것인가…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;目覚め（호의500以上）
;-------------------------------------------------
;・죠교されるのもむしろ好きになってきたかも知れない…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("目覚め")
	PRINTFORMW 「최근 어쩐지 즐기고 있는이 아닌거야」
	PRINTFORMW 「여유가 태어나는 것은 좋은 일이예요♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;順応（호의100以上・호감도100以上）
;-------------------------------------------------
;・丁寧に죠교を進められて、思ったより嫌じゃないかも…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("順応")
	PRINTFORMW 「어쩐지 조금씩 익숙해져 온 것 같구나」
	PRINTFORMW 「조금은 능숙해지고 있는 원이야？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;序盤（条件なし）
;-------------------------------------------------
;・ここまでの条件にヒットしなかった場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;처음
	;=================================================
	IF FIRSTEVENT(110)
		PRINTFORMW 「응, 역시 아직도구나. 그런 응은 사나에는 맡길 수 없어」
		PRINTFORMW 「그런 실망 하는 것 없어. 그러니까 내가 남자를 닦아 준다 라고 하고 있어♪」
		PRINTFORMW 「사나에에 적당한 남자가 되어 받기 위해. 응은 다음은 밤에 하기 때문에 천천히 쉬고 있어」
		RETURN 1
	ENDIF

	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「수고 하셨습니다. 일단 돌아가도 괜찮지만 매일 여기에 다니도록(듯이)」
		PRINTFORMW 「자택에는 신파워로 날려 주기 때문에. 열매 꼭지에 텐구라든지에 발견되어도 귀찮고」
		RETURN 1
	ENDIF
ENDIF
