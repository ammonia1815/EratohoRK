;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코구상 죠교자행동前이벤트구상ファイル
;
;	죠교자행동前이벤트구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV12, 죠교자행동前구상
;========================================================================
;・TRAIN_PROCCES.ERB内、@TRAIN_CANCELから呼び出される
;・구상主のパラメータ表示の直後に表示。죠교指示は既に決定されている
;・조교중止宣言の差し替えや、죠교ターン開始前に주인님から一言、のように書くことも可能
;・午前、午後の죠교開始ではなく、죠교ターンの開始前には毎回呼び出される箇所
;・その為もあって、조교중止時と경과시간0以外の箇所はIF 구상記入チェック(25)等として確率表示する事を推奨
;・조교중止時にのみ地の文が存在する
;・文章表示後に本体側のDRAWLINE処理あり
;-------------------------------------------------
;FIRSTEVENT(0)～や、FIRSTEVENT(10)～などとして枠毎に割り当てていますが
;FIRSTEVENT/ONCEEVENT/RANDEVENT/ORDEREVENTは事実上無限に이벤트번호を増やせるので
;필요に応じて任意に割り当てて下さい
;-------------------------------------------------
@KOJO_MALE_EVENT_K44_12(ARG)
;-------------------------------------------------
;引数内訳…
;ARG(0=죠교可/1=구상主の疲労で中止/2=죠교대상の疲労で中止/3=ご褒美による中止)
;-------------------------------------------------

;────────────────────────────────────
;조교중止 ARG:1 ARG:2 ARG:3 FIRSTEVENT(0)～
;────────────────────────────────────
;・疲労、もしくはご褒美による조교중止の場合の구상
;・記入チェック=1の場合は구상表示後本体側でPRINTLの空行が入る
;・記入チェックを=2にすると、地の文を表示しない
;・というかRESULT > 2で判定されるので2以上なら同じ挙動になりますが一応非推奨ということで
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ご褒美による中止
;RESULT値（1, 구상の後に地の文を表示(非推奨)、2, 구상のみ表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 3
	PRINTFORMW 「창의나 쉬고 싶다는 말라고 」
	PRINTFORMW 「천천히 영기를 길러 다음에 대비하는 것♪」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자の疲労による中止
;RESULT値（1, 구상の後に地の文を表示(非推奨)、2, 구상のみ表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 1
	PRINTFORMW 「응-조금 달 있고로부터 이번 패스군요」
	PRINTFORMW 「거기의 찬장에 오레오 들어가고 있으니라고 귀로-에 가져는 좋아」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상の疲労による中止
;RESULT値（1, 구상の後に地の文を表示(非推奨)、2, 구상のみ表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 2
	PRINTFORMW 「무슨 컨디션 위험한 것 같지 않아 괜찮아？」
	PRINTFORMW 「오늘은 그만두는 것이 좋을지도. 일방적 지나는 것도 시시하고」
	RETURN 2
ENDIF

;-------------------------------------------------
;・以下、ARG:0(죠교可)相当
;・TRAIN_PROCCES.ERB内、@BEFORE_ACTから呼び出される
;・この時点でACTはすでに決定しているがACT파생の決定はなされていない
;・NOWEXのリセットはなされていない
;・これらの箇所を抜けた後にACT開始になる
;・分岐の増減は자유にできますが、우선度には注意しましょう
;-------------------------------------------------

;────────────────────────────────────
;죠교開始直後 FIRSTEVENT(10)～
;────────────────────────────────────
;・경과시간が0である場合に表示される
;・その回の죠교開始直後に一度きりで、以後その조교중には表示されない
;・この箇所を抜けた後にACT開始になる
;-------------------------------------------------
;　ここでIS_POLICY("금일", "징계・")等で금일의방침を喋らせる事も可能だったりしますが
;　EVENT17, 本日の방침구상と被る可能性があります
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교開始直後
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:경과시간
	;=================================================
	;처음
	;=================================================
	IF FIRSTEVENT(10)
		PRINTFORMW 「뭐심의 준비가는？남자니까 비식으로 해 주세요」
		PRINTFORMW 「네？유석에 범죄취가 한다 라고？무슨 말하고 있어 여기는 환상시골이야？」
		PRINTFORMW 「곧바로 그런 일 말할 수 있을 여유는 없게 해 주기 때문에♪」
		RETURN 1
	ENDIF

	;=================================================
	;죠교대상에의 호감도1000以上
	;=================================================
	IF AFFECTION("권애")
		PRINTFORMW 「-않고 -해일까―♪」
		PRINTFORMW 「매우 기분이 좋다？ 기분탓 기분탓」
		RETURN 1
	ENDIF

	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「-라고, 오늘은 어째서 할까」
		PRINTFORMW 「어차피라면 서로 즐깁시다♪」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;징계・ FIRSTEVENT(20)～
;────────────────────────────────────
;・TFLAG:징계・플래그が立っている場合に表示される箇所
;・징계・플래그が立っていても、가학系以外のACTになる場合がある
;-------------------------------------------------
;가학系ACT、派生する事により가학系に含まれる可能性のある이라마치오
;의상 변경을 요구한다/기분나쁘게 웃다/항문바이브/후배위/항문섹스/자위/야외플레이/방뇨/발핥기강제
;이상系ACT/성교봉사系ACT
;上記のACT以外はTFLAG:징계・플래그によってACT_ABLEで規制される
;-------------------------------------------------
;-------------------------------------------------
;・一回の죠교で一回のみ表示される
;━━━━━━━━━━━━━━━━━━━━━━━━
;징계・開始
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:징계・플래그 && ONCEEVENT(20)
	PRINTFORMW 「조금 바보해 머리 차게 할까…」
	PRINTFORMDW 스와자의 배후로부터 눈에 보여 도스 검은 아우라가 나와 있는 것을 확인해, 당신은 생각하는 것을 그만두었다.
	RETURN 1
ENDIF

;-------------------------------------------------
;징계・開始以外を処理する箇所
;━━━━━━━━━━━━━━━━━━━━━━━━
;징계・中
;구상記入チェック(確率％, 入力했다数字の確率で구상を表示)
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && TFLAG:징계・플래그
;ENDIF

;────────────────────────────────────
;질내、장내사정直後 FIRSTEVENT(30)～
;────────────────────────────────────
;NOWEX:사정ないしNOWEX:MASTER:사정で사정種の参照も可能だったりしますが
;EVENT20, 절정구상に同種の分岐がると被る可能性もあります
;-------------------------------------------------
;関連変数…
;
;TFLAG:전회질싸(1=Ｖ성교/2=Ｖ성교봉사/3=Ｖ성교(드라이사정)/4=Ｖ성교봉사(드라이사정)/5=Ａ성교/6=Ａ성교봉사/7=Ａ성교(드라이사정)/8=Ａ성교봉사(드라이사정)
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A責사정)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정直後(죠교대상→죠교자)
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && GROUPMATCH(TFLAG:전회질싸, 2, 4)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상記入チェック(40) && TFLAG:전회질싸 == 4
		PRINTFORMW 「 이제 텅 빈？실마리-가 없다…」
		PRINTFORMW 「이봐요, 빨리 그 불알로 빨리 정자 만드세요♪」
		RETURN 1
	ENDIF

	;=================================================
	;드라이사정以外
	;=================================================
	IF 구상記入チェック(40)
;OriginalString : 「んっ%HTML_HEARTB()%　もう、こんなに出して…」")
		CALL HTMLPRINTW(@"「읏%HTML_HEARTB()% 이제(벌써), 이렇게 내…」")
;OriginalString : 「神様孕ませる気かしら…？　業が深いわね、貴方も%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「신님잉다툴 생각일까…？ 업이 깊네요, 당신도%HTML_HEARTB()%」")
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내사정直後(죠교대상→죠교자)
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && GROUPMATCH(TFLAG:전회질싸, 6, 8)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상記入チェック(40) && TFLAG:전회질싸 == 8
;OriginalString : 「おしりでいっぱいビクつかせちゃってこいつは…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「엉덩이로 가득 흠칫거리게 해 버려 이 녀석은…%HTML_HEARTB()%」")
;OriginalString : 「もう出せないなんて한심한事言わないの%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「이제 낼 수 없다니 한심한 것 말하지 않는거야%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;드라이사정以外
	;=================================================
	IF 구상記入チェック(40)
;OriginalString : 「ほらぁ、おしりからいっぱいザーメン漏れてきたよ…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「이봐요, 엉덩이로부터 가득 정액 새어 왔어…%HTML_HEARTB()%」")
;OriginalString : 「遠慮なく出し過ぎよこのばか…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「너무 사양말고 내요 이 바보…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정直後(죠교자→죠교대상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && GROUPMATCH(TFLAG:전회질싸, 1, 3)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내사정直後(죠교자→죠교대상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && GROUPMATCH(TFLAG:전회질싸, 5, 7)
ENDIF

;────────────────────────────────────
;성교중・성교봉사중 FIRSTEVENT(40)～
;────────────────────────────────────
;関連変数…
;TEQUIP:성교중(죠교자が죠교대상に삽입중) (1=정상위/2=후배위/3=기승위/4=대면좌위/5=배면좌위/6=항문섹스)
;TEQUIP:성교봉사중(죠교대상が죠교자に삽입중) 内容は上記のものと同じ
;
;関連関数…
;V_SEX (=TARGET, TEQUIP:성교중 < 6/=MASTER, TEQUIP:성교봉사중 < 6)と同義
;A_SEX (=TARGET, TEQUIP:성교중 == 6/MASTER, TEQUIP:성교봉사중 == 6)と同義
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;성교중(죠교자→죠교대상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TEQUIP:성교중
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교봉사중(죠교대상→죠교자)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TEQUIP:성교봉사중
ENDIF

;────────────────────────────────────
;절정했다直後 FIRSTEVENT(50)～
;────────────────────────────────────
;・죠교자、죠교대상が절정했다場合に表示される구상
;・個別の部位別절정を拾いたいなら、IF 구상記入チェック(0) && NOWEX:Ｃ절정等の分岐を追加してください
;・사정種はNOWEX:사정/NOWEX:MASTER:사정で参照する事が可能です
;-------------------------------------------------
;関連変数…
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A責사정)
;
;関連関数…
;TRUECHECK 真になるものがいくつあるか을반환
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자が절정했다直後
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정) > 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が절정했다直後
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정) > 0
ENDIF
[SKIPEND]

;────────────────────────────────────
;죠교방침変更 FIRSTEVENT(60)
;────────────────────────────────────
;・죠교방침が変わったときに表示する
;・あくまでも방침なので행동を細かく決定するものではない
;-------------------------------------------------
;関連変数…
;TFLAG:턴방침		このターンの죠교방침(1=휴식/2=소프트/3=노멀/4=하드/5=이상)
;TFLAG:전턴방침		전턴の죠교방침(内容は턴방침と同じ)
;
;関連関数…
;IS_POLICY	죠교방침名称と合致すれば真을반환
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교방침変更
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:턴방침 != TFLAG:전턴방침
	;=================================================
	;턴방침1:휴식（確実に체력回復행동になる）
	;=================================================
	IF 구상記入チェック(30) && IS_POLICY("今", "휴식")
		PRINTFORMW 「조금 휴게로 할까」
		PRINTFORMW 「너무 너무 노력해 헤바라고도 곤란하고」
		RETURN 1
	ENDIF

	;=================================================
	;턴방침2:소프트
	;=================================================
	IF 구상記入チェック(30) && IS_POLICY("今", "소프트")
		PRINTFORMW 「조금 헤바비치기 때문에 소프트에 삽시다」
		PRINTFORMW 「당황해도 괜찮은 일 없어？」
		RETURN 1
	ENDIF

	;=================================================
	;턴방침3:노멀
	;=================================================
	IF 구상記入チェック(30) && IS_POLICY("今", "노멀")
		PRINTFORMW 「응―」
		PRINTFORMW 「역시 보통이 제일일지도보통이」
		RETURN 1
	ENDIF

	;=================================================
	;턴방침4:하드
	;=================================================
	IF 구상記入チェック(30) && IS_POLICY("今", "하드")
		PRINTFORMW 「무슨 미 기 없어…」
		PRINTFORMW 「도 조금 하드하게 말해 볼까」
		RETURN 1
	ENDIF

	;=================================================
	;턴방침5:이상
	;=================================================
	;IF 구상記入チェック(0) && IS_POLICY("今", "이상")
	;ENDIF

[SKIPSTART]
	//現在は実装されていない턴방침
	;=================================================
	;ＳＭ
	;=================================================
	IF 구상記入チェック(0) && TFLAG:턴방침 == 6
		PRINTFORMW 「무슨 공연히 자기째 싶어졌어요」
		PRINTFORMW 「우선 엉덩이전용 없음 있고 엉덩이, 이야기는 그리고」
		RETURN 1
	ENDIF
[SKIPEND]
ENDIF

;────────────────────────────────────
;条件なし FIRSTEVENT(70)～
;────────────────────────────────────
;・ここまでの条件にヒットしなかった場合に表示される箇所
;・BASEで小分岐を設けてありますが、任意の条件を追加する事もできます
;------------------------------------------------
;関連変数…
;BASE		(0, 체력/1, 기력/2, 사정/3, 모유/4, 소변/5, 이성/6, 흥미/7, 초조함/8, 만족)
;
;関連関数…
;TENSION	引数で指定했다BASE値を表す(50%以上は高い、25%以上は一般、25%未満は低い)
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;条件なし
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0)
;ENDIF
