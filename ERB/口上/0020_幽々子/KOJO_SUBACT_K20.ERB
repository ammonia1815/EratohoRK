;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	유유코구상 追加アクション구상ファイル
;
;	죠교자が죠교指示以外に行う행동のファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;죠교アクション・コマンド구상
;========================================================================

;オリジナルで記述の無かった箇所はコメントアウトしておきます@/L

;────────────────────────────────────
;탈의アクション구상
;────────────────────────────────────
;・죠교자アクションの前に강제탈의や자주탈의が発生했다ときに表示
;・この구상の時点ではまだTEQUIPは更新されていない
;・初回分岐は「初めて見た/見せた」とは限らない。
;　ショップメニューで脱がせたことがあるかもしれないし、죠교の流れで完全に脱がせずに노출だけしている場合もある。
;　もしかすると조수だったときに見たことがあるかもしれない。そういう意味では簡易的なものである。
;　厳密な意味での「初見」플래그を用意するかどうかは要望次第ということで。
;-------------------------------------------------
@KOJO_DATUI_K20
;-------------------------------------------------
;関連変数…
;강제탈의：TFLAG:102　자주탈의：TFLAG:103
;=(-1, 강제탈의に抵抗、1, 겉옷、2, 하반신겉옷、3, 上衣、4, 상반신全部、5, 하반신全部、
;　6, 악세서리ー、7, 악세서리ー以外全て、8, 전라まで、9, 속옷だけ)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의に抵抗당했다 FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:강제탈의 < 0
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTDATUI(0)
	;ENDIF
	
	;=================================================
	;自分は脱いだ
	;=================================================
	;IF 구상記入チェック(0) && TFLAG:자주탈의
	;ENDIF
	
	;아라이멘도正
	IF ALI(0)
;OriginalString : 「ふ～ん、そういうこと、ふ～ん」
		PRINTFORMW 「～응, 그런 것,～응」
;OriginalString : 「着衣플레이がご所望なのね」	
		PRINTFORMW 「착의 플레이가 소망인 것」	
	;아라이멘도負
	ELSE
;OriginalString : 「…あら。何のつもり」
		PRINTFORMW 「…어머나. 무슨 작정(생각)」
;OriginalString : 「징계・がご所望かしら？」
		PRINTFORMW 「징계나무가 소망일까？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;二人とも何か脱いだ FIRSTDATUI(1)
;------------------------------------------------
;・主に섹스系행동の予備動作で発生する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:강제탈의 > 0 && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTDATUI(1) && ZENRACHECK(0)
	;ENDIF
	
	;=================================================
	;죠교자が[정욕]상태
	;=================================================
	IF 구상記入チェック(100) && T_COND("정욕")
;OriginalString : 「んっ、こんなの着てないで」
		PRINTFORMW 「응, 이런 건 입지 말고」
;OriginalString : 「お互いぬるぬるになろ、ね？」
		PRINTFORMW 「 서로 칠하는 칠하는에, 군요？」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
;OriginalString : 「さーてそっちも脱ぎ脱ぎしましょうね～？」
	PRINTFORMW 「-라고 그쪽도 벗어 해요～？」
;OriginalString : 「ご所望なら引き裂いてあげましょうか？」
	PRINTFORMW 「 소망이라면 찢어 줄까요？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의・악세서리ーのみ FIRSTDATUI(2)
;------------------------------------------------
;・主に신발안。他には장갑や소매など
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:자주탈의 == 6 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;전라되었다（脱ぐ前は신발안以外裸とかそういう感じ）
	;=================================================
	;IF 구상記入チェック(0) && TFLAG:자주탈의 && ZENRACHECK(0)
	;ENDIF
	
	;=================================================
	;죠교대상이남자
	;=================================================
	IF 구상記入チェック(100) && TALENT:MASTER:남자
;OriginalString : 「う～ん、私あんまり신발안って好きじゃないのよね」
		PRINTFORMW 「～응, 나 너무 양말은 좋아하지 않아군요」
;OriginalString : 「そうだ！　これで先っぽを虐めてあげよっか」
		PRINTFORMW 「그렇다！ 이것으로 끝(쪽)을 학째라고 주어라 인가」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의・まんまんうｐ FIRSTDATUI(3)
;------------------------------------------------
;・今回の탈의で성기が見えるよう되었다場合に表示
;・ぱんつ見せを経由せず一気に脱ぐ場合あり
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && TALENT:TARGET:주인님 && FIRSTDATUI(3)
	;ENDIF
	
	;=================================================
	;すでに가슴が見えてる
	;=================================================
	;IF 구상記入チェック(0) && B_DATUICHECK(0) == 1
	;ENDIF
	
	;=================================================
	;ちんこうｐ（죠교자が후타나리）
	;=================================================
	;IF 구상記入チェック(0) && PENIS(TARGET)
	;ENDIF
	
	;-------------------------
	;その他
	;-------------------------
;OriginalString : 「うふふ、どう？入れたい」
	PRINTFORMW 「후후, 어때？넣고 싶다」
;OriginalString : 「どうしてもっていうならぺろぺろさせてあげてもいいわよ？」
	PRINTFORMW 「아무래도라고 한다면 날름날름 시켜 주어도 괜찮아요？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의・おっぱいうｐ FIRSTDATUI(4)
;------------------------------------------------
;・今回の탈의でおっぱいが見えるよう되었다場合に表示
;・ブラ見せを経由せず一気に脱ぐ場合あり
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && TALENT:TARGET:주인님 && !TALENT:거유 && FIRSTDATUI(4)
	;ENDIF
	
	;=================================================
	;すでに성기が見えてる
	;=================================================
	;IF 구상記入チェック(0) && P_DATUICHECK(0) == 1
	;ENDIF
	
	;=================================================
	;조교중初回
	;=================================================
	;IF 구상記入チェック(0) && ONCEDATUI(4)
	;ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	;現行仕様では가슴の大きさは変更可能なので거유の場合を切り分け@/L
;OriginalString : 「どう？　\@ TALENT:거유 ? おっきいでしょ？ # 私の가슴 \@」
	PRINTFORMW 「어때？ \@ TALENT:거유 ? 크지요？ # 나의가슴 \@」
;OriginalString : 「吸いたい？　揉みたい？　挟まれたい？　どれかしら？」
	PRINTFORMW 「들이마시고 싶다？ 비비고 싶다？ 끼고 싶다？ 어떤 것일까？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의・ぱんつ見せ FIRSTDATUI(5)
;------------------------------------------------
;・今回の탈의で팬티（キャラによって드로와즈・트렁크）が見えるよう되었다場合に表示
;・하반신속옷の無いキャラは書く필요なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && TALENT:TARGET:주인님 && FIRSTDATUI(5)
	;ENDIF
	
	;=================================================
	;조교중初回
	;=================================================
	;IF 구상記入チェック(0) && ONCEDATUI(5)
	;ENDIF
	
	;-------------------------
	;その他
	;-------------------------
;OriginalString : 「どう？なかなかイヤらしいでしょ？」
	PRINTFORMW 「어때？꽤 싫은 것 같지요？」
;OriginalString : 「被せてあげましょうか？なんてね」
	PRINTFORMW 「씌워 줄까요？(이)라니」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의・ブラ見せ FIRSTDATUI(6)
;------------------------------------------------
;・今回の탈의でブラ（キャラによって스포츠브라・サラシ）が見えるよう되었다場合に表示
;・상반신속옷の無いキャラは書く필요なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && TALENT:TARGET:주인님 && FIRSTDATUI(6)
	;ENDIF
	
	;現行仕様は가슴のサイズが変更可能なので条件を絞っておきます@/L
	;=================================================
	;죠교자が거유
	;=================================================
	IF 구상記入チェック(100) && TALENT:거유
;OriginalString : 「うふふ、おっきいでしょ？」
		PRINTFORMW 「후후, 귀의겠지？」
;OriginalString : 「こんなサイズの人はそうそういないわよ～」
		PRINTFORMW 「이런 사이즈의 사람은 그래그래 없어요～」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의・전라に FIRSTDATUI(7)・RANDDATUI(8) ～ (11)
;------------------------------------------------
;・「전라まで脱いだ」だけではなく、「今脱いだので전라되었다」という意味
;・「겉옷しか残っていない상태で겉옷を脱いだ」なども含まれる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:자주탈의 && ZENRACHECK(0) && !IS_NOWASSIACTNAME(, "탈의")
;OriginalString : 「さ、エッチなことしましょ？」
	PRINTFORMW 「, 음란한 일 합시다？」
;OriginalString : 「この身体が당신に対する一番の武器よ？」
	PRINTFORMW 「이 신체가 당신에 대한 제일의 무기야？」
	RETURN 1
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의・악세서리ーのみ FIRSTDATUI(8)
;------------------------------------------------
;・主に신발안。他には장갑や소매など
;・脱が했다うちに入らない！となりがちなのでこれだけは部位別テンプレを用意
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:강제탈의 == 6
	;=================================================
	;전라되었다
	;=================================================
	IF 구상記入チェック(0) && ZENRACHECK(1)
	ENDIF
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의・ちんこ見せろ FIRSTDATUI(9)
;------------------------------------------------
;・今回の탈의で남성기が見えるよう되었다場合に表示
;・ぱんつ見せろを経由せず一気に脱がせる場合あり
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
;OriginalString : 「さーて脱ぎ脱ぎしましょうね～」
	PRINTFORMW 「-라고 벗어 해요～」
;OriginalString : 「あら、おっきくなってるわね」
	PRINTFORMW 「어머나, 쑥갓비쳐요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의・まんまん見せろ FIRSTDATUI(10)
;------------------------------------------------
;・今回の탈의で女성기が見えるよう되었다場合に表示
;・ぱんつ見せろを経由せず一気に脱がせる場合あり
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의・おっぱい見せろ FIRSTDATUI(11)
;------------------------------------------------
;・今回の탈의でおっぱいが見えるよう되었다場合に表示
;・ブラ見せろを経由せず一気に脱がせる場合あり
;・죠교대상이남자だと表示しないので、남자限定구상では書く필요が無い
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의・ぱんつ見せろ FIRSTDATUI(12)
;------------------------------------------------
;・今回の탈의で하반신속옷が見えるよう되었다場合に表示
;・죠교대상が하반신속옷の無いキャラ限定の구상であれば書く필요がない
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 2
;ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의・ブラ見せろ FIRSTDATUI(13)
;------------------------------------------------
;・今回の탈의でブラ상반신속옷が見えるよう되었다場合に表示
;・죠교대상이남자だと表示しないので、남자限定구상では書く필요が無い
;・죠교대상が상반신속옷の無いキャラ限定の구상であれば書く필요がない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 2 && !TALENT:MASTER:남자
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && TALENT:TARGET:주인님 && FIRSTDATUI(13)
	ENDIF
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의・전라に FIRSTDATUI(14)
;------------------------------------------------
;・「전라まで剥いだ」だけではなく、「今剥いだので전라되었다」という意味
;・「겉옷しか残っていない상태で겉옷を剥いだ」なども含まれる
;・「おっぱい見せろ」～「ぱんつ見せろ」を書いた場合は記入する필요はない（上層の分岐に食われてここまで落ちてこない為）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:강제탈의 && ZENRACHECK(1)
	;=================================================
	;죠교대상이남자
	;=================================================
	IF 구상記入チェック(100) && TALENT:MASTER:남자
;OriginalString : 「さ、脱いだ脱いだ♪」
		PRINTFORMW 「, 벗은 벗었다♪」
;OriginalString : 「さ、すぐに汁まみれにしてあげるわね」
		PRINTFORMW 「, 곧바로 국물 투성이로 해 주어요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의・何か脱いだ FIRSTDATUI(15)
;------------------------------------------------
;・자위・봉사系の予備動作になりやすい
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의・何か剥いだ FIRSTDATUI(16)
;------------------------------------------------
;・애무系の予備動作になりやすい
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && TFLAG:강제탈의
;ENDIF

;========================================================================
;追加アクション구상
;========================================================================
;・죠교자が추가행동を取ったときに汎用的に表示
;・書く場合、アクション구상の後に続けて違和感のない形で書かれることが望ましい
;-------------------------------------------------
;────────────────────────────────────
;EXTRA1, Ｃ애무
;────────────────────────────────────
;・追加で애무または수음を行う
;・죠교대상に페니스がない場合は애무、ある場合は수음
;-------------------------------------------------
@KOJO_ACT_EXTRA_K20_1
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_0, 애무する
;-------------------------------------------------
;・죠교대상に페니스がない場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && !PENIS(MASTER)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_1, 수음する
;-------------------------------------------------
;・죠교대상に페니스がある場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && PENIS(MASTER)
;ENDIF

;────────────────────────────────────
;EXTRA2, Ｖ애무
;────────────────────────────────────
;・追加でヴァッジ애무を行う
;・죠교대상이남자の場合発生しない
;・죠교대상が처녀の場合は発生しない
;-------------------------------------------------
@KOJO_ACT_EXTRA_K20_2
;-------------------------------------------------
;IF 구상記入チェック(0)
;ENDIF

;────────────────────────────────────
;EXTRA3, Ａ애무
;────────────────────────────────────
;・追加で항문애무を行う
;-------------------------------------------------
@KOJO_ACT_EXTRA_K20_3
;-------------------------------------------------
;IF 구상記入チェック(0)
;ENDIF

;────────────────────────────────────
;EXTRA4, Ｂ애무
;────────────────────────────────────
;・追加で乳首責めを行う
;-------------------------------------------------
@KOJO_ACT_EXTRA_K20_4
;-------------------------------------------------
;IF 구상記入チェック(0)
;ENDIF

;────────────────────────────────────
;미약등사용구상
;────────────────────────────────────
;・죠교자が미약や로션を使用했다ときに汎用的に表示
;・「アクション구상」で記入チェックを=2と했다場合は表示されない
;・書く場合、アクション구상の後に続けて違和感のない形で書かれることが望ましい
;-------------------------------------------------
@KOJO_USE_K20
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션/미약同時使用
;------------------------------------------------
;・主に성교系コマンドから発生する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && GETBIT(TFLAG:미약등사용구상, 0) && GETBIT(TFLAG:미약등사용구상, 1)
	;아라이멘도正
	IF ALI(0)
;OriginalString : 「さーて로션の시간よ～」
		PRINTFORMW 「-라고 로션의 시간이야～」
;OriginalString : 「あ、미약も使ってあげる…すぐ効いてくるからね～」
		PRINTFORMW 「아, 미약도 사용해 준다…곧 효과가 있어 오니까요～」
	;아라이멘도負
	ELSE
;OriginalString : 「おクスリ欲しそうな顔してるわね。中毒かしら、クスクス」
		PRINTFORMW 「약 갖고 싶은 것 같은 얼굴 해. 중독일까, 킥킥」
;OriginalString : 「ふふ、おまけにぬるぬるにもしてあげる♪]
		PRINTFORMW 「후후, 게다가 미끈미끈에도 해 준다♪]
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션使用
;------------------------------------------------
;・玩具・성교系コマンド等から発生する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && GETBIT(TFLAG:미약등사용구상, 0)
	;아라이멘도正
	IF ALI(0)
;OriginalString : 「ふふ、ぬるぬるにしてあげる♪]
		PRINTFORMW 「후후, 미끈미끈으로 해 준다♪]
;OriginalString : 「今度はおっぱいで塗ってあげようか、うふふ」
		PRINTFORMW 「이번은 젖가슴으로 발라 줄까, 후후」
	;아라이멘도負
	ELSE
;OriginalString : 「さーて로션の시간よ～」
		PRINTFORMW 「-라고 로션의 시간이야～」
;OriginalString : 「あら？　まだ塗ってないのにヌルヌルなところがあるわよ？」
		PRINTFORMW 「어머나？ 아직 바르지 않았는데 누르눌인 곳이 있어요？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;미약使用
;------------------------------------------------
;・애무系コマンド等から発生する
;・키스で発生했다場合は입移しとなるため、そのケースは키스のアクション구상内で処理するほうがよい
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && GETBIT(TFLAG:미약등사용구상, 1)
	;아라이멘도正
	IF ALI(0)
;OriginalString : 「あ、これも使ってあげる…すぐ効いてくるからね～」
		PRINTFORMW 「아, 이것도 사용해 준다…곧 효과가 있어 오니까요～」
	;아라이멘도負
	ELSE
;OriginalString : 「おクスリ欲しそうな顔してるわね。中毒かしら、クスクス」
		PRINTFORMW 「약 갖고 싶은 것 같은 얼굴 해. 중독일까, 킥킥」
	ENDIF
	RETURN 1
ENDIF
