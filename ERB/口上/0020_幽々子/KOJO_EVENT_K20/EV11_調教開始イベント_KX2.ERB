;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	유유코구상 죠교開始이벤트구상ファイル
;
;	죠교開始이벤트구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV11, 죠교開始구상
;========================================================================
;・大抵の場合、구상作者がまず考えるのは"主人"としての구상と考えられるので
;・@KOJO_MS_EVENTとして、TALENT:주인님である場合に限定してしまいます
;・이벤트関連は구상主の立場によって整合性が損なわれる可能性があるのでこのような処置にしま했다
;・구상内の全ての箇所で구상関数による同様の排他処理は可能だったりします
;-------------------------------------------------
;　召喚され、죠교を任される場合の구상を書くのであれば
;　呼び出しを@KOJO_EVENT_KX1_11として排他を行わず、TALENT:주인님とその他で分岐を設ける
;　EVENT1, OP(主人である場合にしか処理されない)でKFLAGへの代入を行い、分岐を設ける
;　若しくは、@KOJO_AS_EVENT_KX1_11として別ファイルに切り分けてしまうと良いでしょう
;-------------------------------------------------
;とりあえず分岐を設けてありますが、EVENT11枠内の分岐の増減は자유にできます
;COMMON_GETTER_CHARA.ERB内、@AFFECTIONにはここに無いものも定義してあったりします
;ただし、条件のゆるい分岐を上層にもってくると下位の分岐が全て死に구상になるので
;우선度には十分に注意してください
;-------------------------------------------------
@KOJO_EVENT_K20_11(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 통상/1, 죠교자の疲労による中止/2, 죠교대상の疲労による中止)
;-------------------------------------------------

;────────────────────────────────────
;죠교初回・１０回目
;────────────────────────────────────
;・죠교初回、及び10回目に表示される구상
;・CFLAG:죠교횟수で횟수は如何様にでも参照できますが、やりすぎると他の条件を食いつぶす可能性あり
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교初回
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && CFLAG:죠교횟수 == 1
	;유유코が주인님
	IF TALENT:TARGET:주인님
		PRINTFORML 「, 즐거운 시간의 시작되어 시작～」
		PRINTFORMW 「무엇을 할까는？ 그것은요. 싫은 것 같고 기분이 좋은 일이야」
		PRINTFORMW 「인간, 고통에는 견딜 수 있어도 쾌락에는 견딜 수  없어♪」
		PRINTFORMW 「 그렇지만 말했겠죠.30날로 자유롭게 해 주기 때문에」
		;ＢＧＭカットイン　ゴーストリード
		CALL BGM_CUTIN(CSTR:곡２)
;OriginalString : 인10, @"「――そのころの%CNAME(MASTER, TARGET)%に逃げる気があったらの話だけどね」")
		CALL FADEIN(10, @"「――그 당시의 %CNAME(MASTER, TARGET)%에 도망치는 마음이 있으면의 이야기이지만 말야」")
		WAIT
	;その他
	ELSE
		PRINTFORMW 「그러면은-응, 도우러 왔어요」
		PRINTFORMW 「물론, 당신의 조교의 도움에 말이야♪」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교１０回目
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && CFLAG:죠교횟수 == 10 && !ARG
	;아라이멘도正		죠교１０回目程度だと恐らく아라이멘도は負だと思われるのでご"主人様"の場合にも分岐するようにしま했다@/L
	IF ALI(0) || TALENT:TARGET:주인님
		PRINTFORMW 「이제 여기에도 익숙해졌을 무렵 까？왜？ 나를 만날 수 있어 떨릴 만큼 기쁜거야？」
		PRINTFORMW 「이라면, 지금까지 이상으로 가득 귀여워해 주어요. 기쁘지요？」
	;아라이멘도負
	ELSE
		PRINTFORMW 「이제 여기에도 익숙해졌을 무렵？왜？내가 무서운 것일까？」
		PRINTFORMDW 당신은 다만 눈물을 흘릴 뿐(만큼)이었다
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;조교중止 ARG:1 ARG:2
;────────────────────────────────────
;CFLAG:소모 > 5もしくはCFLAG:MASTER:소모 > 5を条件として呼び出される箇所
;この条件から漏れた場合は통상の죠교開始구상が呼ばれる
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;疲労で죠교が中止になる場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG > 0
	;=================================================
	;疲れているのは구상主(TARGET)
	;=================================================
	IF 구상記入チェック(0) && ARG == 1
	ENDIF
	
	;=================================================
	;疲れているのは죠교대상(MASTER)
	;=================================================
	IF 구상記入チェック(0) && ARG == 2
	ENDIF
ENDIF
[SKIPEND]
;────────────────────────────────────
;その他・호감도等による分岐
;────────────────────────────────────
;・現在のACTABLEやCOMABLEの仕様的に、進展度を測るには(事実上)호감도を基準にするしかないと思われる
;・의존도は@COMで代入され参照できるのみで、本体側の挙動には何ら영향を与えていない
;・今後の손入れ如何によっては使い道もあるかも？といったところ
;-------------------------------------------------

;-------------------------------------------------
;・互いに好き合っている
;-------------------------------------------------
;元々はEDが実装されていない時代に、仮のED枠として使われる事の多かった箇所
;現状でもこの段階に至るには、それなりの일수と進展が필요になると思われます
;━━━━━━━━━━━━━━━━━━━━━━━━
;상사상애（호의3000以上・호감도3000以上)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("상사상애")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && TALENT:TARGET:주인님 && FIRSTEVENT(0)
		PRINTFORMW 「후~ 있고, ···키･･････어머나, 혹시」
		PRINTFORMW 「나를 좋아하게 되어 버렸다？만약 그러면, 환영해요, 후후」
		RETURN 1
	ENDIF
	
	;처음とその他が全く同じだったので、勝손ながら弄らせてもらいま했다@/L
	;=================================================
	;처음以外
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「후~ 있고, ···키･･････%RAND_SPLIT("해 버렸다/(이었)였다")%까？」
		PRINTFORMW 「어머나, 속여도 안되어요. 나, 그러한 것 알아버린다 것이군요. 후후」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;・죠교대상が구상主にもうメロメロ
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교完了（호의2000以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("죠교完了")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTEVENT(10)
	;ENDIF
	
	;=================================================
	;처음以外
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「당신, 최근 귀염성이 나왔어요」
		PRINTFORMW 「이렇게, 몸도 마음도 노예가 되어 간다…후후」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;・죠교자の方が죠교대상にはまってしまい、深かったり倒錯했다愛を注いでいる
;━━━━━━━━━━━━━━━━━━━━━━━━
;익애（호감도2000以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("익애")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTEVENT(20)
	;ENDIF
	
	;=================================================
	;처음以外
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「나, 싫다？」
		PRINTFORMW 「 그렇지만 나는 당신을 좋아해요, 노예로서」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;・죠교대상が구상主に죠교されるのを幸せに感じている
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교済み（호의1000以上
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("죠교済み")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTEVENT(30)
	;ENDIF
	
	;PENISの有無を参照するように改変@/L
	;=================================================
	;처음以外
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「즐겁고 기분이 좋은 조교의 시간이야」
		PRINTFORMW 「어머나, 지금\@ PENIS(MASTER) ? 자지가 # \@피쿡은 했어요\@ PENIS(MASTER) ? ？ # 저기.\@ 빨리 솔직해져버릴 없음 있고」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;・度重なる酷い仕打ちに、心の底から죠교자を憎んでいる
;━━━━━━━━━━━━━━━━━━━━━━━━
;不倶戴天（호의-500未満）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("不倶戴天")
	PRINTFORMW 「상당히 무서운 얼굴 해. 모처럼의 얼굴이 엉망이야？」
	PRINTFORMW 「오늘은 조금 격렬하게 해 주어요, 후후」
	RETURN 1
ENDIF

[SKIPSTART]
;-------------------------------------------------
;・죠교자に対し嫌悪感を抱いている
;━━━━━━━━━━━━━━━━━━━━━━━━
;嫌悪（호의-100未満）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && AFFECTION("嫌悪")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTEVENT(40)
	ENDIF
		
	;=================================================
	;처음以外
	;=================================================
	IF 구상記入チェック(0)
	ENDIF
ENDIF
[SKIPEND]

;-------------------------------------------------
;・心に傷を作られ、주인님の姿を見るだけで怯えてしまう
;・트라우마の種は「%ACTSTR(CFLAG:MASTER:8)%」で表示できる
;　例）PRINTFORMW 「もう%ACTSTR(CFLAG:MASTER:트라우마)%は嫌ですって？」
;━━━━━━━━━━━━━━━━━━━━━━━━
;공포（호의200未満・트라우마Lv3以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("공포")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTEVENT(50)
	;ENDIF
	
	;=================================================
	;처음以外
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「상당히 무서운 얼굴 해. 모처럼의 얼굴이 엉망이야？」
		PRINTFORMW 「 그렇지만 당신도 나빠, 나는 공포로 재기 불능케 하는 것보다 쾌락으로 녹게 하는 (분)편이 좋아하니까」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;・心を開かない노예に苛つく주인님
;━━━━━━━━━━━━━━━━━━━━━━━━
;반항（호의0以下・반항각인Lv3以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("반항")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTEVENT(60)
	;ENDIF
	
	;=================================================
	;처음以外
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「무엇, 사람의 얼굴 보자 마자 비크는. 그렇게 나의 얼굴은 무서운 것일까？」
		PRINTFORMW 「괴물 보고 싶다？ 대강 반드시 잘못되어 있지 않아요」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;・죠교されるのもむしろ好きになってきたかも知れない…
;━━━━━━━━━━━━━━━━━━━━━━━━
;目覚め（호의500以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("目覚め")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTEVENT(70)
	;ENDIF
	
	;=================================================
	;처음以外
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「당신, 최근 귀염성이 나왔어요」
		PRINTFORMW 「저속해지는 편이 편해져요？」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;・丁寧に죠교を進められて、思ったより嫌じゃないかも…
;━━━━━━━━━━━━━━━━━━━━━━━━
;順応（호의100以上・호감도100以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("順応")
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTEVENT(80)
	;ENDIF
	
	;内容的に主人の場合限定と했다方が相応しいと思われたので改変@/L
	;=================================================
	;처음以外
	;=================================================
	IF 구상記入チェック(100) && TALENT:TARGET:주인님
		PRINTFORMW 「여기에는 익숙해졌다？」
		PRINTFORMW 「맛있는 것이 먹을 수 있어 기분 좋아질 수 있는, 최고가 아니다？」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;・要するに、他の条件にヒットしなかった場合に落ちる箇所
;・仮に他の分岐を全て방치했다場合、この箇所だけ書いておけば何かは表示されるとか、そんな場所です
;━━━━━━━━━━━━━━━━━━━━━━━━
;序盤（条件なし）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTEVENT(90)
	;ENDIF
	
	;=================================================
	;처음以外
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「, 조교의 시간이야」
		PRINTFORMW 「여기에서 냈으면 좋겠다？무리한 이야기군요」
		RETURN 1
	ENDIF
ENDIF
