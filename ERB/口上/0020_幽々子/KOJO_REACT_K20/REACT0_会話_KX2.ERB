;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	유유코구상 회화계 개별 리액션구상파일
;
;	회화계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어선택했다COM가 자연스러운행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운행동/1=무리가 없는행동/2=무리를했다행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 지금 기분을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K20_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일했다 있고)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다로, 리액션이빈정거린다 이외의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「あらあら緊張しちゃってるの？ならほぐしてあげないとね」
		PRINTFORMW 「대강 긴장해 버린다의? 라면 풀어 주지 않으면」
;OriginalString : 유유코は당신の身体をいやらしく撫で始めた
		PRINTFORMDW 유들자는 당신의 신체를 불쾌하게 어루만지기 시작했다
	;아라이멘도부
	ELSE
;OriginalString : 「あらあら怯えちゃってるの？ならほぐしてあげないとね」
		PRINTFORMW 「대강 무서워해 버린다의? 라면 풀어 주지 않으면」
;OriginalString : 유유코は당신の身体をいやらしく撫で始めた
		PRINTFORMDW 유들자는 당신의 신체를 불쾌하게 어루만지기 시작했다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「ふ～ん、まだそんなこと思ってるんだ」
		PRINTFORMW 「～응, 아직 그런 일 생각하고 있어」
;OriginalString : 「本当かどうか身体に聞いてあげるね」
		PRINTFORMW 「사실인가 어떤가 신체에 들어 주는군」
	;아라이멘도부
	ELSE
;OriginalString : 「ふ～ん、まだそんなこと思ってるんだ」
		PRINTFORMW 「～응, 아직 그런 일 생각하고 있어」
;OriginalString : 「ちょっとお灸を据えてあげるね」
		PRINTFORMW 「조금 뜸을 떠 주는군」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「입ではそう言ってるけど」
		PRINTFORMW 「입에서는 그렇게 말하고 있는데」
;OriginalString : 「身体はどうかしらね、楽しみね」
		PRINTFORMW 「신체는 어떻게든 해들, 기다려지구나」
	;아라이멘도부
	ELSE
;OriginalString : 「精一杯の見栄ってことかしら？」
		PRINTFORMW 「힘껏의 허세는 것일까?」
;OriginalString : 「いいわ、そんなの粉々に砕いてあげる」
		PRINTFORMW 「좋아요, 그런 것 가루들에 분쇄해 여(어) 준다」
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일했다 있고
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 5
ENDIF
[SKIPEND]

;────────────────────────────────────
;REACT1, 자신에게의 마음을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K20_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다!/2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「じゃあ好きでもないのね、残念」
		PRINTFORMW 「자를 좋아하지도 않구나, 유감」
;OriginalString : 「금일は私が大好きになるように虐めてあげる」
		PRINTFORMW 「오늘은 나를 아주 좋아하게 되도록(듯이) 학째라고 준다」
	;아라이멘도부
	ELSE
;OriginalString : 「嫌いじゃない？　それってつまらないわね」
		PRINTFORMW 「싫지 않다? 그 거 시시하네요」
;OriginalString : 「もっと苛めたら嫌われちゃうの？　それとも好きになる？」
		PRINTFORMW 「좀 더 괴롭히면 미움받아버린다의? 그렇지 않으면 좋아하게 된다?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「そう？じゃ、好きになるまで虐めてあげる♪」
		PRINTFORMW 「그렇게? 그러면, 좋아하게 될 때까지 학째라고 준다♪」
;OriginalString : 
		PRINTFORMW 
	;아라이멘도부
	ELSE
;OriginalString : 「あっそ、じゃあ嫌いな女に何されても仕方ないわよねぇ」
		PRINTFORMW 「, 자 싫은 여자에게 무엇 되어도 어쩔 수 없어요」
;OriginalString : 
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「怒ったりなんかしないわ。好きって言うまで虐めちゃうだけよ♪」
		PRINTFORMW 「화내거나 무엇을 하지 않아요. 좋아한다고 말할 때까지 학만♪」
;OriginalString : 
		PRINTFORMW 
	;아라이멘도부
	ELSE
;OriginalString : 「どうして？　こんなに楽しいのに怒るなんて」
		PRINTFORMW 「어째서? 이렇게 즐거운데 화내다니」
;OriginalString : 「なら…玉をつぶ했다ら、好きになってくれる？
		PRINTFORMW 「이라면… 구슬을 부수면, 좋아하게 되어 준다?
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 적은지, REACT분류가태연하게 한다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「それは嘘ね。酷いことされて好き되었다んでしょう、変態さん」
		PRINTFORMW 「그것은 거짓말이군요. 심한 일 되어 좋아하게 되었겠지요, 변태씨」
;OriginalString : 
		PRINTFORMW 
	;아라이멘도부
	ELSE
;OriginalString : 「なにそれ、気持ち悪い」
		PRINTFORMW 「뭐그것, 기분 나쁘다」
;OriginalString : 「こんなの好かれたくないから、いっぱい酷いことするわ」
		PRINTFORMW 「이런 건 사랑받고 싶지 않기 때문에, 가득 심한 일 해요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 1000이상
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 4
	;부하 2는 연주한 (분)편이 뭔가 형편이 좋은 것 같아 우선
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(4) && TFLAG:REACT부하 < 2
;OriginalString : 「……あら、好きになっちゃったの？」
		PRINTFORMW 「…… 어머나, 좋아하게 되어 버렸어?」
;OriginalString : 「嘘かホントか確かめてあげる」
		PRINTFORMW 「거짓말인가 진짜인가 확인해 준다」
		RETURN 1
	ENDIF
	
	;=================================================
	;역강간로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
;OriginalString : 「私のことが好きなのか、私の身体が好きなのか。こんなに出しておいて…」
		PRINTFORMW 「나를 좋아하는가, 나의 신체를 좋아하는가. 이렇게 내 두어…」
;OriginalString : 「まぁいいのよ、いっぱい、いっぱい犯してあげるから」
		PRINTFORMW 「아무튼 좋아, 가득, 가득 범해 주기 때문에」
		RETURN 1
	ENDIF
	
	;=================================================
	;섹스로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
;OriginalString : 「あらあら。女の身体ってそういう風に出来ているってことかしら」
		PRINTFORMW 「대강. 여자의 신체는 그러한 식으로 되어있다는 것 까」
;OriginalString : 「それとも유카리が言ってたストックホルム症候群ってやつかしら、うふふ」
		PRINTFORMW 「그렇지 않으면 보라색이 말하고 있었던 스톡홀름 증후군은 녀석일까, 후후」
		RETURN 1
	ENDIF
	
	;오리지날에서는 FAVOR(0, 1000)의 개소입니다만,
	;TFLAG:ACT파생 == 4가 성립하는 시점에서 조건은 클리어 하고 있다고 생각되므로 일반 분기 취급으로 합니다@/L
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「ありがとう、私も당신のことが大好きよ」
		PRINTFORMW 「고마워요, 나도 당신을 아주 좋아해요」
;OriginalString : 「だから금일はたっぷり可愛がってあげる。うれしいでしょ？」
		PRINTFORMW 「그러니까 오늘은 충분히 귀여워해 준다. 기쁘지요?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 5
	;=================================================
	;처음
	;=================================================
	;IF 구상기입 체크(0) && FIRSTREACT(5)
	;ENDIF
	
	;=================================================
	;역강간로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
;OriginalString : 「犯されるのがくせになっちゃったのかしら、うふふ」
		PRINTFORMW 「범해지는 것이 주제에 되어 버렸어 까, 후후」
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;섹스로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
;OriginalString : 「要するに孕みたいのね、子宮でしかものを考えられないのね」
		PRINTFORMW 「요컨데 잉 보고 싶구나, 자궁으로 게다가의를 생각할 수 없구나」
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「すごいこと…少しやりすぎたかなと思ったりも했다けど」
		PRINTFORMW 「대단한 일…너무 조금 했는지 라고 생각하기도 했지만」
;OriginalString : 「次はどんなことしてあげようかな、쿠쿡」
		PRINTFORMW 「다음은 어떤 금년이라고 줄까, 킥킥」
	;아라이멘도부
	ELSE
;OriginalString : 「じゃあ、どれだけすごいことやったら嫌いになってもらえるかしら」
		PRINTFORMW 「자, 얼마나 대단한 일 하면 싫게 되어 줄 수 있는 것일까」
;OriginalString : 「下剤飲ませてお尻に栓でもしてあげようかしら？」
		PRINTFORMW 「설사약 먹여 엉덩이에 마개에서도 해 줄까?」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;REACT2, 성에 관한 이야기를 하다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K20_2
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;REACT3, 상냥하게 위로한다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K20_3
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;REACT4, 엄하게 위협한다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_REACT_K20_4
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;REACT5, 의상 변경을 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가"소극적으로 한다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가"적극적으로 한다", "태연하게 한다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_REACT_K20_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:ACT파생 == 0
	;=================================================
	;한 장 벗었다(소극적 반응)
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("소극적으로 한다/받아들인다")
	ENDIF
	
	;=================================================
	;2매 벗었다(적극적 반응)
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
	ENDIF
	
	;=================================================
	;벗지 않았다
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("거부")
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("소극적으로 한다/받아들인다")
	ENDIF
	
	;=================================================
	;2매 입었다(적극적 반응)
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
	ENDIF
	
	;지문에서는 "싫어해"라고 하는 취급
	;=================================================
	;입지 않았다
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("거부")
	ENDIF
ENDIF
[SKIPEND]

;────────────────────────────────────
;REACT6, 합의를 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_REACT_K20_6
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && IS_COMGRONAME("소극적으로 한다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && IS_COMGRONAME("태연하게 한다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && IS_COMGRONAME("적극적으로 한다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게 해 주는 것이라면 처음을 주어도 괜찮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입 체크(0) && IS_COMGRONAME("받아들인다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의 하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서"거부"에 세트 된다)
;-------------------------------------------------
IF 구상기입 체크(0) && IS_COMGRONAME("거부")
ENDIF
[SKIPEND]

;────────────────────────────────────
;REACT7, 징계한다고 위협한다·개별 리액션
;────────────────────────────────────
;·선언해도징계·이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_REACT_K20_7
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;REACT8, 고함치다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K20_8
;-------------------------------------------------
RETURN 0

;────────────────────────────────────
;REACT9, 기분나쁘게 웃다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K20_9
;-------------------------------------------------
RETURN 0
