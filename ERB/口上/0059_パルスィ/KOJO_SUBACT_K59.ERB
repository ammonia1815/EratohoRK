;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	파르시구상 탈의·추가 액션구상파일
;
;	죠교자가죠교지시 이외에 실시하는행동의 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;탈의액션구상
;────────────────────────────────────
;·죠교자액션의 앞에강제탈의나자주탈의가 발생했다때에 표시
;·이구상의 시점에서는 아직 TEQUIP는 갱신되어 있지 않다
;·첫회 분기는 「처음 보았다/보였다」란 할 수 없다.
;  숍 메뉴로 벗게 했던 적이 있을지도 모르고, 죠교의 흐름으로 완전하게 벗게 하지 않고서노출만 하고 있는 경우도 있다.
;  어쩌면조수였던 때에 본 적이 있을지도 모른다. 그러한 의미에서는 간이적인 것이다.
;  엄밀한 의미에서의 「초견」플래그를 준비할지 어떨지는 요망하는 대로라고 하는 일로.
;-------------------------------------------------
@KOJO_DATUI_K59
;-------------------------------------------------
;관련 변수…
;강제탈의:TFLAG:102  자주탈의:TFLAG:103
;=(-1, 강제탈의에 저항, 1, 겉옷, 2, 하반신겉옷, 3, 웃도리, 4, 상반신전부, 5, 하반신전부,
;  6, 악세서리─, 7, 악세서리─이외 모두, 8, 전라까지, 9, 속옷만)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의에 저항당했다 FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 < 0
;OriginalString : 「…へぇ。何のつもりよ、それは」
	PRINTFORML 「…에. 무슨 작정(생각)이야, 그것은」
;OriginalString : 「その布切れ一枚守るために、私の機嫌を損ねようだなんて」
	PRINTFORMW 「그 헝겊 한 장 지키기 위해서(때문에), 나의 기분을 해치자이라니」
;OriginalString : 「노예は算数もできないのね、哀れだわ」
	PRINTFORML 「노예는 산수도 할 수 없구나, 불쌍하구나」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;두 사람 모두 뭔가 벗은 FIRSTDATUI(10)
;------------------------------------------------
;·주로섹스계행동의 예비 동작으로 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && TFLAG:강제탈의 > 0 && TFLAG:자주탈의 && ! IS_NOWASSIACTNAME(, "탈의")
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·악세서리─마셔 FIRSTDATUI(20)
;------------------------------------------------
;·주로신발안. 그 밖에장갑나소매 등
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 == 6 && !IS_NOWASSIACTNAME(, "탈의")
;OriginalString : 「はい」
	PRINTFORML 「네」
;OriginalString : 파르시はおもむろに신발안を脱ぐと、%CALLNAME:MASTER%의 鼻筋から頭の上に乗せた。
	PRINTFORMDW 파르스는 조용히 양말을 벗으면(자), %CALLNAME:MASTER%의 콧날로부터 머리 위에 실었다.
;OriginalString : 「ふふ、よく似合うわね。香りもいっぱい堪能しなさいよ？」
	PRINTFORML 「후후, 잘 어울려요. 향기도 가득 만끽해 주세요?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·만p FIRSTDATUI(30)
;------------------------------------------------
;·이번탈의로성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(30)
;OriginalString : 「私のアソコ見せてあげましょうか。ほら、ぽってり熱くなってる…」
		PRINTFORMW 「 나의 아소코 보여 줄까요. 이봐요, 윤 뜨거워지고 있다…」
;OriginalString : 「ふふ、何ガン見しちゃってるのよ。本当に浅ましい노예ね？」
		PRINTFORML 「후후, 무엇 암 보고 해 버린다 것. 정말로 야비한 노예군요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;응 이렇게 p
	;=================================================
	IF 구상기입체크(100) && PENIS(TARGET)
;OriginalString : 「熱くなってはちきれそう…もう収まりきらないわ」
		PRINTFORMW 「뜨겁고는 벌조각 그렇게…이제 완전히 해결되지 않아요」
;OriginalString : 「ほら、いいでしょ…これ、どこかに入れたらキモチよさそうよねぇ…♪」
		PRINTFORML 「이봐요, 좋지요…이것, 어디엔가 들어갈 수 있으면 키모치야 유혹하지요…♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써가슴가 보이고 있다
	;=================================================
	IF 구상기입체크(100) && B_DATUICHECK(0) == 1
;OriginalString : 「下も見たいの？　…そういう顔してるわね」
		PRINTFORMW 「아래도 보고 싶은거야? … 그러한 얼굴 해」
;OriginalString : 「欲張りさんには見せてあげましょうか。上손に봉사しなさいよ？」
		PRINTFORML 「욕심쟁이씨에게는 보여 줄까요. 능숙하게 봉사해 주세요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:주인님
;OriginalString : 「ほら、주인님のおまんこよ。舐めたいわよね？」
		PRINTFORMW 「이봐요, 남편님의 보지야. 빨고 싶어요?」
;OriginalString : 「違うなんてことないでしょう。指と舌、どっちでさせようかしら…？」
		PRINTFORML 「다른 것 않을 것입니다. 손가락과 혀, 어느 쪽으로 시킬까…?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·말하는 p FIRSTDATUI(40)
;------------------------------------------------
;·이번탈의로 젖가슴이 보이도록(듯이)되었다경우에 표시
;·브라 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(40)
;OriginalString : 「何、人の가슴じろじろ見てるの？」
		PRINTFORMW 「무엇, 사람의 가슴 빤히 봐?」
;OriginalString : 「いいわよ、ほら…私の가슴、見なさい。幸せでしょう？」
		PRINTFORML 「좋아요, 이봐요… 나의 가슴, 보세요. 행복하겠지요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써성기가 보이고 있다
	;=================================================
	IF 구상기입체크(100) && P_DATUICHECK(0) == 1
;OriginalString : 「下だけじゃ興奮しない？　…本当におっぱい好きなのね」
		PRINTFORMW 「아래만은 흥분하지 않는다? … 정말로 젖가슴을 좋아하는」
;OriginalString : 「ほら、これで上も下も丸見え。特別に、私で욕정してもいいわよ」
		PRINTFORML 「이봐요, 이것으로 위나 아래도 죄다 보임. 특별히, 나로 욕정해도 돼요」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「ほーら…おっぱい見せてあげる」
		PRINTFORMW 「오-들…젖가슴 보여 준다」
;OriginalString : 「후후후…ペットにエサをあげる気分に近いわね」
		PRINTFORML 「후후후…애완동물에게 먹이를 주는 기분에 가깝네요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·팬티 보이고 FIRSTDATUI(50)
;------------------------------------------------
;·이번탈의로팬티（캐릭터에 의해드로와즈·트렁크）가 보이도록(듯이)되었다경우에 표시
;·하반신속옷가 없는 캐릭터는 쓰는필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(50) && FIRSTDATUI(30, 1)
;OriginalString : 「何で下を脱いでるのかって？」
		PRINTFORMW 「무엇으로 아래를 벗고 있는지는?」
;OriginalString : 「触りやすいほうが気持ちいいからよ。さあ、始めましょう？」
		PRINTFORML 「손대기 쉬운 편이 기분이 좋기 때문에. 자, 시작합시다?」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「見て、ほら。このクロッチの染み」
		PRINTFORMW 「봐, 이봐요. 이 크로치가 스며들고」
;OriginalString : 「貴方のこと苛めると、じわじわじくじく滲んでくるの…♪」
		PRINTFORML 「당신괴롭히면(자), 서서히 질척질척 배여 와…♪」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·브라 보이고 FIRSTDATUI(60)
;------------------------------------------------
;·이번탈의로 브라（캐릭터에 의해스포츠브라·사라시）가 보이도록(듯이)되었다경우에 표시
;·상반신속옷가 없는 캐릭터는 쓰는필요없음
;·맨손에 말하는 p의경험가 있다면, 첫회 판정을 날린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(60) && FIRSTDATUI(40, 1)
;OriginalString : 「…ブラ見せたくらいで、なに目を逸らしてるの？」
		PRINTFORMW 「… 브라 보여 주고 싶고들 있고로, 뭐눈을 일등 해?」
;OriginalString : 「私の가슴が見るに耐えないとでも言いたいのかしら、この子は」
		PRINTFORML 「 나의 가슴이 차마 볼 수 없으면에서도 말하고 싶은 것일까, 이 아이는」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		IF RAND:2
;OriginalString : 「금일は黒のブラよ…どうかしら、お気に召さない？」
			PRINTFORMW 「오늘은 흑의 브라야… 어떻게든 해들, 마음에 드시지 않는다?」
;OriginalString : 「これをつけると貴方のこと、苛めたい気分になるのよね」
			PRINTFORML 「이것을 붙이면(자) 당신, 괴롭히고 싶은 기분이 되어」
		ELSE
;OriginalString : 「금일は白のレースブラよ、見なさい？」
			PRINTFORMW 「오늘은 흰색의 레이스브라야, 보세요?」
;OriginalString : 「形、自信あるんだけど…ま、貴方には関係ないわね」
			PRINTFORML 「형태, 자신 있는데…뭐, 당신에게는 관계없네요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·전라에 FIRSTDATUI(70)
;------------------------------------------------
;·「전라까지 벗었다」만이 아니고, 「지금 벗었으므로전라되었다」라고 하는 의미
;·「겉옷 밖에 남지 않은상태로겉옷를 벗었다」등도 포함된다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:자주탈의 && ZENRACHECK(0) && ! IS_NOWASSIACTNAME(, "탈의")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·악세서리─마셔 FIRSTDATUI(80)
;------------------------------------------------
;·주로신발안. 그 밖에장갑나소매 등
;·탈이했다중에 접어들지 않는다! 되기 십상인 것으로 이것만은 부위별 텐프레를 준비
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의 == 6
ENDIF
[SKIPEND]
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·고추 보여라 FIRSTDATUI(90)
;------------------------------------------------
;·이번탈의로남성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
;OriginalString : 「うわっ…なにこの硬いの、跳ねてきたわよ…」
	PRINTFORMW 「위…뭐 이 딱딱한 것, 뛰어 왔어요…」
;OriginalString : 「先から珠が滲んでるわ…なによ、その안쓰러운顔は？」
	PRINTFORML 「앞으로부터 주가 배이고 있어요… 뭐야, 그 야무지지 못한 얼굴은?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·그대로응 보여라 FIRSTDATUI(100)
;------------------------------------------------
;·이번탈의로 여자성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
;OriginalString : 「후후…貴方の大事なところ、丸見えね」
	PRINTFORMW 「후후…당신의 소중한 곳, 훤히 들여다보이구나」
;OriginalString : 「ここを気持ちよくしてあげる方法なら、たくさん知ってるのよ…♪」
	PRINTFORML 「여기를 기분 좋게 해 주는 방법이라면, 많이 알아…♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·젖가슴 보여라 FIRSTDATUI(110)
;------------------------------------------------
;·이번탈의로 젖가슴이 보이도록(듯이)되었다경우에 표시
;·브라 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;·죠교대상이남자라고 표시하지 않기 때문에, 남자한정구상에서는 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && B_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
;OriginalString : 「はい、おっぱい見せなさい」
	PRINTFORMW 「네, 젖가슴 보이세요」
;OriginalString : 「…綺麗ね、妬ましいわ。覚悟しなさいね？」
	PRINTFORML 「… 깨끗하구나, 부러워요. 각오 해 주세요?」
	RETURN 1
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·팬티 보여라 FIRSTDATUI(120)
;------------------------------------------------
;·이번탈의로하반신속옷가 보이도록(듯이)되었다경우에 표시
;·죠교대상가하반신속옷가 없는 캐릭터 한정의구상이면 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·브라 보여라 FIRSTDATUI(130)
;------------------------------------------------
;·이번탈의로 브라상반신속옷가 보이도록(듯이)되었다경우에 표시
;·죠교대상이남자라고 표시하지 않기 때문에, 남자한정구상에서는 쓰는필요가 없다
;·죠교대상가상반신속옷가 없는 캐릭터 한정의구상이면 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 2 && ! TALENT:MASTER:남자
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·전라에 FIRSTDATUI(140)
;------------------------------------------------
;·「전라까지 벗겼다」만이 아니고, 「지금 벗겼으므로전라되었다」라고 하는 의미
;·「겉옷 밖에 남지 않은상태로겉옷를 벗겼다」등도 포함된다
;·「젖가슴 보여라」～「팬티 보여라」를 썼을 경우는 기입하는필요는 없다（상층의 분기에 먹혀져 여기까지 떨어져 오지 않기 때문에）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의 && ZENRACHECK(1)
ENDIF
[SKIPEND]
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·뭔가 벗은 FIRSTDATUI(150)
;------------------------------------------------
;·자위·봉사계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;죠교자가[정욕]상태
	;=================================================
	IF 구상기입체크(100) && IS_CONDITION(TARGET, "정욕")
;OriginalString : 「ああっ、もう…！　んんッ、脱げないし、ああもう！」
		PRINTFORML 「아, 이제…! 응응, 벗겨지지 않고, 아 이제!」
;OriginalString : 파르시は乱暴に…自ら破り捨てるような勢いで服を脱ぎ捨てた。
		PRINTFORMDW 파르스는 난폭하게…스스로 찢어 버리는 것 같은 기세로 옷을 벗어 던졌다.
;OriginalString : 「何見てるの…？　ねぇ…見てるだけで何やってんの、ほら、はやくぅ…」
		PRINTFORML 「무엇 봐…? 응… 보고 있을 뿐으로 뭐 하고 있어, 이봐요, 빠르게…」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;아라이멘도정
		IF ALI(0)
;OriginalString : 「ふうん、見ていいなんて言ってないのにそんなにジロジロと」
			PRINTFORML 「식응, 봐도 좋다니 말하지 않았는데 그렇게 빤히」
;OriginalString : 「…まあいいわ。私の身体、興奮するんでしょ？　노예のくせに…♪」
			PRINTFORMW 「… 뭐 좋아요. 나의 신체, 흥분하는거죠? 노예의 주제에…♪」
		;아라이멘도부
		ELSE
;OriginalString : 「目瞑りなさい、あんたなんかが見ていい身体じゃないのよ」
			PRINTFORML 「눈 감고 리나 차이, 너등이 봐도 좋은 신체는 아닌거예요 」
;OriginalString : 파르시は%CALLNAME:MASTER%にそう命じて服を脱いでいく。
			PRINTFORMDW 파르스는 %CALLNAME:MASTER%에 그렇게 명해 옷을 벗어 간다.
			IF RAND:2
;OriginalString : 「何バカ正直に目瞑ってるのよ。見たくもならないってわけ？　失礼な犬ね」
				PRINTFORML 「무엇 터무니없이 정직하게 눈 감고 비쳐. 보고 싶지도 안 된다고 끓어라? 실례인 개군요」
			ELSE
;OriginalString : 「…誰が見ていいなんて言ったの？　本当、どうしようもない노예…」
				PRINTFORML 「… 누가 봐도 좋다고 말했어? 사실, 어쩔 수 없는 노예…」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·뭔가 벗긴 FIRSTDATUI(160)
;------------------------------------------------
;·애무계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의
;OriginalString : 「はい、脱がすわよー…そうそう、大人しくしてなさいね」
	PRINTFORMW 「네, 탈원이야―…그렇게 자주, 얌전하게 해 주세요」
;OriginalString : 「脱いだ方が気持ちよくなれるのよ。知ってるでしょ…？」
	PRINTFORML 「벗는 것이 기분 좋게 될 수 있어. 알고 있지…?」
	RETURN 1
ENDIF

;========================================================================
;추가 액션구상
;========================================================================
;·죠교자가추가행동를 취했을 때에 범용적으로 표시
;·쓰는 경우, 액션구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
;────────────────────────────────────
;EXTRA1, Ｃ애무
;────────────────────────────────────
;·추가로애무 또는수음를 실시한다
;·죠교대상에페니스가 없는 경우는애무, 어느 경우는수음
;-------------------------------------------------
@KOJO_ACT_EXTRA_K59_1
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_0, 애무 한다
;-------------------------------------------------
;·죠교대상에페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ! PENIS(MASTER)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_1, 수음 한다
;-------------------------------------------------
;·죠교대상에페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && PENIS(MASTER)
ENDIF
[SKIPEND]
;────────────────────────────────────
;EXTRA2, Ｖ애무
;────────────────────────────────────
;·추가로 바지애무를 실시한다
;·죠교대상이남자의 경우 발생하지 않는다
;·죠교대상가처녀의 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K59_2
;-------------------------------------------------
;IF 구상기입 체크(0)
;ENDIF

;────────────────────────────────────
;EXTRA3, Ａ애무
;────────────────────────────────────
;·추가로항문애무를 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K59_3
;-------------------------------------------------
;IF 구상기입 체크(0)
;ENDIF

;────────────────────────────────────
;EXTRA4, Ｂ애무
;────────────────────────────────────
;·추가로 유두 고문을 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K59_4
;-------------------------------------------------
;IF 구상기입 체크(0)
;ENDIF

;────────────────────────────────────
;미약등사용구상
;────────────────────────────────────
;·죠교자가미약나로션를 사용했다때에 범용적으로 표시
;·「액션구상」로 기입 체크를=2로했다경우는 표시되지 않는다
;·쓰는 경우, 액션구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
@KOJO_USE_K59
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;로션/미약동시 사용
;------------------------------------------------
;·주로성교계 커맨드로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && GETBIT(TFLAG:미약등사용구상, 0) && GETBIT(TFLAG:미약등사용구상, 1)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션사용
;------------------------------------------------
;·완구·성교계 커맨드등으로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && GETBIT(TFLAG:미약등사용구상, 0)
ENDIF
[SKIPEND]
;━━━━━━━━━━━━━━━━━━━━━━━━
;미약사용
;------------------------------------------------
;·애무계 커맨드등으로부터 발생한다
;·키스로 발생했다경우는입옮기기가 되기 (위해)때문에, 그 케이스는키스의 액션구상내에서 처리하는 편이 좋다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GETBIT(TFLAG:미약등사용구상, 1)
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「あ、これも使ってあげる…誰とでも仲良くなれるお薬」
		PRINTFORML 「아, 이것도 사용해 준다…누구와라도 사이 좋게 될 수 있는 약」
	;아라이멘도부
	ELSE
;OriginalString : 「おクスリ欲しそうな顔してるわね。いいのよ、遠慮しなくて」
		PRINTFORML 「약 갖고 싶은 것 같은 얼굴 해. 좋아, 사양하지 않아서」
	ENDIF
	RETURN 1
ENDIF
