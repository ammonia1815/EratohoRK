;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	파르시구상 회화계 개별 리액션구상파일
;
;	회화계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어선택했다COM가 자연스러운행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운행동/1=무리가 없는행동/2=무리를했다행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 지금 기분을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K59_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일했다 있고)
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다로, 리액션이빈정거린다 이외의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일했다 있고
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 5
ENDIF
;REACT0, 지금 기분을 묻다
[SKIPEND]

;────────────────────────────────────
;REACT1, 자신에게의 마음을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K59_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다!/2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「じゃあ好きでもないのね」
		PRINTFORML 「자를 좋아하지도 않구나」
;OriginalString : 「さて、どれだけ可愛がってやれば好きになってもらえるだろうか」
		PRINTFORMW 「그런데, 얼마나 귀여워해 주면 좋아하게 되어 줄 수 있을까」
	;아라이멘도부
	ELSE
;OriginalString : 「嫌いじゃない？　それってつまらないわね」
		PRINTFORML 「싫지 않다? 그 거 시시하네요」
;OriginalString : 「もっと苛めたら嫌われちゃうかしら。それとも好きになる？」
		PRINTFORMW 「좀 더 괴롭히면 미움받아버릴까 해들. 그렇지 않으면 좋아하게 된다?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
;OriginalString : 「正直者は長生きできないって寺子屋で教わらなかった？」
	PRINTFORMW 「정직한 사람은 장수 할 수 없다는 서당에서 배우지 않았다?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「怒ったりなんかしないわ。好きって言うまでわからせてあげるだけよ」
		PRINTFORMW 「화내거나 무엇을 하지 않아요. 좋아한다고 말할 때까지 알게 해 줄 뿐(만큼)」
	;아라이멘도부
	ELSE
;OriginalString : 「どうして？　こんなに楽しいのに怒るわけがないわ」
		PRINTFORML 「어째서? 이렇게 즐거운데 화내는 것이 없어요」
;OriginalString : 「好きじゃないなら…爪を一枚ずつ剥がしてあげたら、好きになってくれる？」
		PRINTFORMW 「좋아하지 않으면… 손톱을 한 장씩 벗겨 주면, 좋아하게 되어 준다?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 적은지, REACT분류가태연하게 한다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「それは嘘ね。酷いことされて好き되었다んでしょう、貴方」
		PRINTFORMW 「그것은 거짓말이군요. 심한 일 되어 좋아하게 되었겠지요, 당신」
	;아라이멘도부
	ELSE
;OriginalString : 「なにそれ、気持ち悪い」
		PRINTFORML 「뭐그것, 기분 나쁘다」
;OriginalString : 「こんなウジ虫に好かれたくないから、いっぱい酷いことするわ」
		PRINTFORMW 「이런 구더기에게 사랑받고 싶지 않기 때문에, 가득 심한 일 해요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 1000이상
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 4
	;=================================================
	;성교봉사계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
;OriginalString : 「私のことが好きなのか、私の身体が好きなのか。こんなに出しておいて…」
		PRINTFORML 「나를 좋아하는가, 나의 신체를 좋아하는가. 이렇게 내 두어…」
;OriginalString : 「페니스를埋められれば木のうろでもいいんでしょう、あんた」
		PRINTFORMW 「페니스를 묻을 수 있으면 나무의 속이 빈 것이라도 좋겠지요, 너」
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
;OriginalString : 「哀れね。女の身体ってそういう風に出来ているってこと」
		PRINTFORML 「불쌍하구나. 여자의 신체는 그러한 식으로 되어있다는 것」
;OriginalString : 「種付けされてキモチよくなっちゃったら、相손のこと好きになるしかないのよ」
		PRINTFORMW 「교배되어 키모치 좋아져 버렸다들 , 상대를 좋아하게 될 수 밖에 없어」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「ありがとう、嬉しいわ。好かれるのってすごく好き」
		PRINTFORML 「고마워요, 기뻐요. 사랑받는 것은 몹시 좋아해」
;OriginalString : 「惨めに振ってやるのはもっと好き。自分が당했다ら殺했다くなるほど嫌なことだから」
		PRINTFORMW 「비참하게 털어 주는 것은 좀 더 좋아. 자신이 되면(자) 죽이고 싶어질수록 싫은 일이니까」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 5
	;=================================================
	;성교봉사계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
;OriginalString : 「出했다りないってわけ…？　この、猿」
		PRINTFORMW 「내거나 없다고 끓어라…? 이, 원숭이」
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
;OriginalString : 「要するに孕みたいのね、このメス豚」
		PRINTFORMW 「요컨데 잉 보고 싶구나, 이 메스돼지」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		;아라이멘도정
		IF ALI(0)
;OriginalString : 「すごいこと…少しやりすぎたかなと思ったりも했다けど」
			PRINTFORML 「대단한 일…너무 조금 했는지 라고 생각하기도 했지만」
;OriginalString : 「呆れるわ。あれで喜んでたどころか、飽き발らないなんて」
			PRINTFORMW 「기가 막혀요. 저것으로 기뻐하고 있었던은 커녕, 부족다니」
		;아라이멘도부
		ELSE
;OriginalString : 「じゃあ、どれだけすごいことやったら嫌いになってもらえるかしら」
			PRINTFORML 「자, 얼마나 대단한 일 하면 싫게 되어 줄 수 있는 것일까」
;OriginalString : 「これは燃えるわね。…そうね、試しに燃やしてみる？」
			PRINTFORMW 「이것은 불타요. … 그렇구나, 시험삼아 태워 본다?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT1, 자신에게의 마음을 묻다

;────────────────────────────────────
;REACT2, 성에 관한 이야기를 하다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K59_2
;-------------------------------------------------
;  파생할 가능성이 있는 COM는 우선 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
RETURN 0
;REACT2, 성에 관한 이야기를 함

;────────────────────────────────────
;REACT3, 상냥하게 위로한다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K59_3
;-------------------------------------------------
;  파생할 가능성이 있는 COM에는 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
;  우선회화계 COM를 모두 망라되어 있습니다만
;  10 접수대의애무계 COM, 50 접수대 이후의 그 외 COM등도 상황에 따라서는선택 가능하게 됩니다
;  필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
RETURN 0
;REACT3, 상냥하게 위로한다

;────────────────────────────────────
;REACT4, 엄하게 위협한다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_REACT_K59_4
;-------------------------------------------------
RETURN 0
;REACT4, 엄하게 위협한다

;────────────────────────────────────
;REACT5, 의상 변경을 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가"소극적으로 한다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가"적극적으로 한다", "태연하게 한다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_REACT_K59_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:ACT파생 == 0
	;=================================================
	;한 장 벗었다(소극적 반응)
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("소극적으로 한다/받아들인다")
		;-------------------------------------------------
		;마지막 한 장을 벗어전라되었다
		;-------------------------------------------------
		IF 구상기입 체크(0) && (TEQUIP:MASTER:악세서리 + TEQUIP:MASTER:신발안 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
		ENDIF
		
		;-------------------------------------------------
		;완만한 움직임으로 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입 체크(0) && IS_COMGRONAME("받아들인다")
		ENDIF
		
		;-------------------------------------------------
		;어쩔 수 없이 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입 체크(0) && IS_COMGRONAME("소극적으로 한다")
		ENDIF
	ENDIF
	
	;=================================================
	;2매 벗었다(적극적 반응)
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;-------------------------------------------------
		;전라되었다
		;-------------------------------------------------
		IF 구상기입 체크(0) && (TEQUIP:MASTER:악세서리 + TEQUIP:MASTER:신발안 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
		ENDIF
		
		;-------------------------------------------------
		;적극적으로 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입 체크(0) && IS_COMGRONAME("적극적으로 한다")
		ENDIF
		
		;-------------------------------------------------
		;손때 좋게 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입 체크(0) && IS_COMGRONAME("태연하게 한다")
		ENDIF
	ENDIF
	
	;=================================================
	;벗지 않았다
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("거부")
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("소극적으로 한다/받아들인다")
	ENDIF
	
	;=================================================
	;2매 입었다(적극적 반응)
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
	ENDIF
	
	;지문에서는 "싫어해"라고 하는 취급
	;=================================================
	;입지 않았다
	;=================================================
	IF 구상기입 체크(0) && IS_COMGRONAME("거부")
	ENDIF
ENDIF
;REACT5, 의상 변경을 요구한다
[SKIPEND]
;────────────────────────────────────
;REACT6, 합의를 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_REACT_K59_6
;-------------------------------------------------
;손간을 싫어하지 않는 것이면, TALENT:MASTER:남자등으로 내부에서 나누는 것도 있을지도 모릅니다
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && IS_COMGRONAME("소극적으로 한다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && IS_COMGRONAME("태연하게 한다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && IS_COMGRONAME("적극적으로 한다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게 해 주는 것이라면 처음을 주어도 괜찮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입 체크(0) && IS_COMGRONAME("받아들인다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의 하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서"거부"에 세트 된다)
;-------------------------------------------------
IF 구상기입 체크(0) && IS_COMGRONAME("거부")
ENDIF
;REACT6, 합의를 요구한다
[SKIPEND]

;────────────────────────────────────
;REACT7, 징계한다고 위협한다·개별 리액션
;────────────────────────────────────
;·선언해도징계·이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_REACT_K59_7
;-------------------------------------------------
RETURN 0
;REACT7, 징계한다고 위협한다

;────────────────────────────────────
;REACT8, 고함치다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K59_8
;-------------------------------------------------
RETURN 0
;REACT8, 고함치다

;────────────────────────────────────
;REACT9, 기분나쁘게 웃다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K59_9
;-------------------------------------------------
RETURN 0
;REACT9, 기분나쁘게 웃다
