;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	니토리구상 その他죠교이벤트구상ファイル
;
;	その他죠교ターン中이벤트をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;EV21, 大만족구상
;────────────────────────────────────
;・죠교자の대만족보너스を취득했다場合に表示
;-------------------------------------------------
@KOJO_EVENT_K40_21(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま大만족
;━━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;中にだして！
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("안에 내!")
		;아라이멘도正
		IF ALI(0)
			SELECTCASE RANDEVENT(3, 0)
				CASE 0
;OriginalString : 「凄く、嬉しいよ……っ」
					PRINTFORMW 「굉장히, 기뻐……」
;OriginalString : 「私も、만족させてあげるから……」
					PRINTFORMW 「 나도, 만족시켜 주기 때문에……」
				CASE 1
;OriginalString : 「何か、凄く……」
					PRINTFORMW 「무엇인가, 굉장히……」
;OriginalString : 「ううん、何でも無いよっ！」
					PRINTFORMW 「으응, 뭐든지 없어！」
				CASE 2
;OriginalString : 「%CNAME(MASTER, TARGET)%で、良かったよ……」
					PRINTFORMW 「%CNAME(MASTER, TARGET)%그리고, 좋았어요……」
;OriginalString : 「私、本当にそう思ってるんだからね……」
					PRINTFORMW 「 나, 정말로 그렇게 생각하고 있기 때문에……」
			ENDSELECT
		;아라이멘도負
		ELSE
			SELECTCASE RANDEVENT(3, 11)
				CASE 0
;OriginalString : 「いいなあ、この感じ……」
					PRINTFORMW 「좋다, 이 느낌……」
;OriginalString : 「お裾分け、してあげたいくらいだよ……」
					PRINTFORMW 「이익 분배, 해 주고 싶을 정도 야……」
				CASE 1
;OriginalString : 「何て言うか、あれだねえ……」
					PRINTFORMW 「뭐라고 말하는지, 저것이구나……」
;OriginalString : 「호기심をそそられるよ、%CNAME(MASTER, TARGET)%には……」
					PRINTFORMW 「호기심을 돋워져,%CNAME(MASTER, TARGET)%에는……」
				CASE 2
;OriginalString : 「今、ぞくって来たよ……」
					PRINTFORMW 「지금, 속편은 왔어……」
;OriginalString : 「もっと、言って？」
					PRINTFORMW 「 좀 더, 말해？」
			ENDSELECT
		ENDIF
		RETURN 2
	ENDIF
	
	;=================================================
	;비명지른다
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("비명지른다")
		;아라이멘도正
		IF ALI(0)
			SELECTCASE RANDEVENT(3, 10)
				CASE 0
;OriginalString : 「ちょっと、今……」
					PRINTFORMW 「조금, 지금……」
;OriginalString : 「何か妙な感じだったねえ……」
					PRINTFORMW 「무엇인가 이상한 느낌이었다 응……」
				CASE 1
;OriginalString : 「今の声、凄く……」
					PRINTFORMW 「지금의 소리, 굉장히……」
;OriginalString : 「ううん、何でもないんだけどさあ……」
					PRINTFORMW 「으응, 아무것도 아니지만 자……」
				CASE 2
;OriginalString : 「うあっ……」
					PRINTFORMW 「아……」
;OriginalString : 「びっくり했다……のと、何か……」
					PRINTFORMW 「놀랐다……의와 무엇인가……」
			ENDSELECT
		;아라이멘도負
		ELSE
			SELECTCASE RANDEVENT(3, 11)
				CASE 0
;OriginalString : 「ああ……良いねえ」
					PRINTFORMW 「아……좋구나」
;OriginalString : 「%CNAME(MASTER, TARGET)%の反応……最高だよ」
					PRINTFORMW 「%CNAME(MASTER, TARGET)%의 반응……최고야」
				CASE 1
;OriginalString : 「수치心とか、無いのかなあ」
					PRINTFORMW 「수치심이라든지, 없는 것인지」
;OriginalString : 「私は凄く、嬉しいんだけどさあ」
					PRINTFORMW 「 나는 굉장히, 기쁘지만 자」
				CASE 2
;OriginalString : 「私なんかにそんなことしちゃって……」
					PRINTFORMW 「 나같은 것에(이) 그런 일 해 버려서……」
;OriginalString : 「惨めだとは思わない……？」
					PRINTFORMW 「비참하다고는 생각하지 않는다……？」
			ENDSELECT
		ENDIF
		RETURN 2
	ENDIF
	
	;아라이멘도正
	IF ALI(0)
		SELECTCASE RANDEVENT(3, 20)
			CASE 0
;OriginalString : 「何か당했다いこととか、あるかなあ」
				PRINTFORMW 「무엇인가 되고 싶은 것이라든지, 걷고」
;OriginalString : 「自分で言うのもなんだけど、今凄く機嫌良いんだよねえ」
				PRINTFORMW 「스스로 말하는 것도이지만, 지금 굉장히 기분 좋아」
			CASE 1
;OriginalString : 「じゃあ次は、私が何か봉사してあげようかな？」
				PRINTFORMW 「자 다음은, 내가 무엇인가 봉사 해 줄까？」
;OriginalString : 「どう당했다いか、ちゃんと考えておくこと。いいね？」
				PRINTFORMW 「어떻게 하시고 싶은지, 제대로 생각해 두는 것. 좋다？」
			CASE 2
;OriginalString : 「これは、ご褒美あげないとなあ……」
				PRINTFORMW 「이것은, 포상 주지 않으면……」
;OriginalString : 「何がいい？　多少の無理は聞くよ？」
				PRINTFORMW 「무엇이 좋다？ 다소의 무리는 (들)물어？」
		ENDSELECT
	;아라이멘도負
	ELSE
		SELECTCASE RANDEVENT(3, 21)
			CASE 0
;OriginalString : 「大分、わかってきたみたいだねえ……」
				PRINTFORMW 「상당히, 알게 된 것 같구나……」
;OriginalString : 「私は、嬉しいよ……っ」
				PRINTFORMW 「 나는, 기뻐……」
			CASE 1
;OriginalString : 「今の%CNAME(MASTER, TARGET)%、良かったよ？」
				PRINTFORMW 「지금의%CNAME(MASTER, TARGET)%, 좋았어요？」
;OriginalString : 「惚れちゃいそうな程、可哀想でさあ……」
				PRINTFORMW 「반해버릴 것 같을 수록, 불쌍해 자……」
			CASE 2
;OriginalString : 「いやー、만족しちゃったなあ」
				PRINTFORMW 「아니―, 만족해 버렸다」
;OriginalString : 「次は、どんなのが良い？」
				PRINTFORMW 「다음은, 어떤 것이 좋다？」
		ENDSELECT
	ENDIF
	RETURN 2
ENDIF

;────────────────────────────────────
;EV22, 상태변화구상
;────────────────────────────────────
;・상태이상が付いたときに表示
;・TCVAR:상태변화が죠교자、TCVAR:MASTER:상태변화が죠교대상
;・상태변화が起こると3ターン지속する
;-------------------------------------------------
@KOJO_EVENT_K40_22(ARG)
;-------------------------------------------------
[SKIPSTART]
オリジナルで未記入の箇所はコメントアウトしておきます@/L
;*************************************************
;EV22_0, 죠교자상태변화
;*************************************************
;T_COND TARGETが指定했다상태なら真を返す
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[피폐]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && T_COND("피폐") && !TFLAG:일회휴게
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[쇠약]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && T_COND("쇠약") && !TFLAG:일회휴게
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[무기력]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && T_COND("무기력")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[몽롱]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && T_COND("몽롱")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[정욕]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && T_COND("정욕")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[분노]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && T_COND("분노")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[싫증]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && T_COND("싫증")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[울굴]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && T_COND("울굴")
ENDIF
;TALENT:광기がある場合にのみ1/5の確率で울굴化

;*************************************************
;EV22_1, 죠교대상상태변화
;*************************************************
;M_COND MASTERが指定했다상태なら真を返す
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[피폐]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && M_COND("피폐") && !TFLAG:일회휴게
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[쇠약]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && M_COND("쇠약") && !TFLAG:일회휴게
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[무기력]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && M_COND("무기력")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[몽롱]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && M_COND("몽롱")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[정욕]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && M_COND("정욕")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[분노]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && M_COND("분노")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[싫증]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && M_COND("싫증")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[울굴]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && M_COND("울굴")
ENDIF
[SKIPEND]

;────────────────────────────────────
;EV23, 죠교대상能力値アップ구상
;────────────────────────────────────
;・조교중に죠교대상の能力値が上昇했다際に表示
;-------------------------------------------------
@KOJO_EVENT_K40_23(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(今回上昇했다ABL번호/各ARG번호はABLのそれに相当)
;
;ARG:0 순종/1 욕망/2 기교/3 Ｃ감각/4 Ｖ감각/5 Ａ감각/6 Ｂ감각/7 봉사정신/8 노출증/9 레즈끼/10ＢＬ끼/11마조끼
;12자위중독/13정액중독/14레즈중독/15고통중독/16구속중독
;
;ARG:20 회화/21 애무/22 도구/23 성교/24 수치/25 봉사/26 가학/27 이상
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[순종]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[욕망]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[기교]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[Ｃ감각]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[Ｖ감각]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[Ａ감각]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 5
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[Ｂ감각]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 6
ENDIF
[SKIPEND]

;────────────────────────────────────
;EV24, 죠교자能力値アップ구상
;────────────────────────────────────
;・조교중に죠교자の能力値が上昇했다際に表示
;-------------------------------------------------
@KOJO_EVENT_K40_24(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(今回上昇했다ABL번호)
;
;ARG:0 순종/1 욕망/2 기교/3 Ｃ감각/4 Ｖ감각/5 Ａ감각/6 Ｂ감각/7 봉사정신/8 노출증/9 레즈끼/10ＢＬ끼/11마조끼
;12자위중독/13정액중독/14레즈중독/15고통중독/16구속중독
;
;ARG:20 회화/21 애무/22 도구/23 성교/24 수치/25 봉사/26 가학/27 이상
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[욕망]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[기교]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[Ｃ감각]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[Ｖ감각]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[Ａ감각]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 5
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;니토리さま[Ｂ감각]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 6
ENDIF
[SKIPEND]

;────────────────────────────────────
;EV29, 굴복・고통각인による반항각인上限処理
;────────────────────────────────────
;・반항각인Lv上限の処理
;・반항각인は10-(고통각인+굴복각인)/2を超えない
;-------------------------------------------------
@KOJO_EVENT_K40_29(ARG)
;-------------------------------------------------
;%CALLNAME:TARGET%의 与える쾌락\@ MARK:MASTER:고통각인 ? と고통 # \@が%CNAME(MASTER, TARGET)%から반항の기력を奪っていく…
;반항각인は{MARK:반항각인}되었다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;반항각인上限処理
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0)
;ENDIF

;========================================================================
;각인취득구상
;========================================================================
;・조교중に각인を취득시켰다場合に表示。どのレベルで구상表示するかは加減자유なので個々で調整するのが望ましい
;・記入チェックが=1の場合、同時に複数취득했다ときはこのファイルで一番上のものだけを表示する
;・=2の場合、취득했다각인の구상すべてを上から順に表示する
;・각인취득による素質変動の前に表示するため、素質취득/喪失するかはこの時点でのTALENTの有無で判定できる
;-------------------------------------------------
@KOJO_MARK_K40(ARG:0, ARG:1, ARG:2, ARG:3, ARG:4)
#DIM 記入チェック
#DIM すべて順に表示
すべて順に表示 = 0	;각인취득구상「すべて順に表示」のための初期化処理
;-------------------------------------------------
;引数内訳…
;ARG:(각인번호)=(今回취득했다レベル)
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;[트라우마]취득
記入チェック = 0	;0.非表示/1.同時に취득했다각인をこれ以下表示しない/2.すべて順に表示する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:4
	SETBIT すべて順に表示, 4
	SELECTCASE ARG:4
		;트라우마Lv1
		CASE 1
		;트라우마Lv10…[겁쟁이]がない場合、それを得る
		;              既に[겁쟁이]があり[광기]がない場合、[유치]がある場合は100%、ない場合は1/3の確率で[광기]を得る
		;              [광기]の취득判定に漏れ、かつ[유치]を持たない場合、[유치]を得る(幼児退行する)
		;              現状、[광기]と[유치]はどちらに転ぶか先に判定する術は無い(要望があれば…)。とりあえず頭がおかしくなると解釈してください
		CASE 10
		;表示しない
		CASEELSE
			CLEARBIT すべて順に表示, 4
	ENDSELECT
	SIF GETBIT(すべて順に表示, 4) && 記入チェック == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[고통각인]취득
記入チェック = 0	;0.非表示/1.同時に취득했다각인をこれ以下表示しない/2.すべて順に表示する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:0
	SETBIT すべて順に表示, 0
	SELECTCASE ARG:0
		;고통각인Lv2
		CASE 2
		;고통각인Lv9…[굳셈]がある場合、それを失う
		CASE 9
		;表示しない
		CASEELSE
			CLEARBIT すべて順に表示, 0
	ENDSELECT
	SIF GETBIT(すべて順に表示, 0) && 記入チェック == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[반항각인]취득
記入チェック = 0	;（0, 非表示、1, 同時に취득했다각인をこれ以下表示しない、2, すべて順に表示する）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG:3
	SETBIT すべて順に表示, 3
	SELECTCASE ARG:3
		;반항각인Lv2
		CASE 2
		;表示しない
		CASEELSE
			CLEARBIT すべて順に表示, 3
	ENDSELECT
	SIF GETBIT(すべて順に表示, 3) && 記入チェック == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[굴복각인]취득
記入チェック = 0	;（0, 非表示、1, 同時に취득했다각인をこれ以下表示しない、2, すべて順に表示する）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:2
	SETBIT すべて順に表示, 2
	SELECTCASE ARG:2
		;굴복각인Lv2
		CASE 2
		;굴복각인Lv7…[반항적]がある場合、それを失う
		CASE 7
		;굴복각인Lv10…[프라이드높음]がある場合、それを失う
		CASE 10
		;表示しない
		CASEELSE
			CLEARBIT すべて順に表示, 2
	ENDSELECT
	SIF GETBIT(すべて順に表示, 2) && 記入チェック == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[쾌락각인]취득
記入チェック = 0	;（0, 非表示、1, 同時に취득했다각인をこれ以下表示しない、2, すべて順に表示する）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:1
	SETBIT すべて順に表示, 1
	SELECTCASE ARG:1
		;쾌락각인Lv2
		CASE 2
		;쾌락각인Lv5…[쾌감을부정]がある場合、それを失う
		CASE 5
		;쾌락각인Lv7…[억압]がある場合、それを失う
		CASE 7
		;쾌락각인Lv10…[쾌감에솔직]がない場合、それを得る
		CASE 10
		;表示しない
		CASEELSE
			CLEARBIT すべて順に表示, 1
	ENDSELECT
	SIF GETBIT(すべて順に表示, 1) && 記入チェック == 1
		RETURN 1
ENDIF

;각인취득구상「すべて順に表示」のための終端処理
RETURN すべて順に表示 != 0
[SKIPEND]
