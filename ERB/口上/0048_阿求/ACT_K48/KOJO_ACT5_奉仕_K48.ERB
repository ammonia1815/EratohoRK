;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	아큐구상 봉사系アクション구상ファイル
;
;	봉사系アクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;봉사系
;────────────────────────────────────
;・TFLAG:ACT(50=손으로 애무를 강요/51=입으로 애무를 강요/52=파이즈리/53=스마타/55=발핥기강제/56=이라마치오/57=가슴애무강제)
;・TFLAG:ACT분류 == 5に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "봉사")で拾う事が可能
;・죠교대상に、죠교자の身体を애무するように命令する
;・基本的には命令なので、この段階では従うかどうか確定していない(ACT56,이라마치오は거부できない)
;-------------------------------------------------
;・ACT54,발コキは12/04/08版で粛清
;-------------------------------------------------

;*************************************************
;ACT50, 손으로 애무를 강요
;*************************************************
;・애무強制/수음強制/페니스밴드수음強制
;・죠교대상(MASTER)に、손による애무를 강요する
;・죠교자に페니스があると必ず수음強制になる(페니스があると통상の애무強制は起こらない)
;・죠교자が페니스밴드장착中は페니스밴드수음強制になる
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"하드", "이상"だと発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP(밧줄/삼각목마)장착中には発生しない
;・성교중/성교봉사중には発生しない
;・죠교대상が従うか否かはこの時点では未定
;・REACT분류が거부/날뛴다/逃げるの場合は拒絶扱いとなる
;-------------------------------------------------
;TFLAG:ACT파생(0=애무強制/1=수음強制/2=페니스밴드수음強制)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_50
;=================================================
;・地の文はACT파생毎に分岐。各ACT파생内では分岐なし
;・ACT파생0,애무の時のみ쾌Ｂ/쾌ＡSOURCE増加
;・その他の場合は쾌Ｃ限定
;-------------------------------------------------

;この構造で初回分岐に落ちたら派生に関わらず一度きりの表示
;派生毎に初回分岐を設けたいなら各ACT파생内で処理する事をお勧めします
;FIRSTACT(0)/FIRSTACT(1)/FIRSTACT(2)のように分けてやれば派生毎に初回を参照できます
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50, 初回
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;ACT파생1, 수음強制
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;ACT파생2, 페니스밴드수음強制
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 2
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;上層の分岐を埋めた場合はTFLAG:ACT파생 == 0と同義となる
	;ここだけで済ませるなら、汎用的に使えるよう留意すべき箇所
	;=================================================
	;その他・条件無し
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_0, 애무強制「自分の성기を손으로 애무しろと命令する」
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;죠교자がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;기타・조건없음
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_1, 수음強制「自分の남성기を손で扱けと命令する」
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;죠교자がイキそう
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;기타・조건없음
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;페니스밴드への애무は쾌ＣSOURCEが減少し、쾌ＶSOURCEが増加する
;장착している時点で대상が처녀である可能性はない
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_2, 페니스밴드수음強制「페니스밴드を손で掴んで動かせと命令する」
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;죠교자がイキそう
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;기타・조건없음
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT51, 입으로 애무를 강요
;*************************************************
;・クンニ/펠라チオ/안면승마/안면승마항문/
;・죠교대상に、입による애무를 강요する
;・죠교자が후타나리の場合、죠교대상が누움なら1/2で안면승마、そうでなければ펠라強制になる
;・죠교자に페니스がなく、죠교대상が누움なら안면승마になる
;・안면승마が選ばれていたとき、안면승마항문に변화することがある
;・全ての条件から漏れた場合は통상のクンニとなる
;・TFLAG:징계・플래그が立っていると発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP(볼개그/삼각목마)장착中は発生しない
;・죠교자が페니스밴드を장착している場合は発生しない
;・성교중/성교봉사중には発生しない
;・REACT분류が거부/날뛴다/逃げるの場合は拒絶扱いになる
;-------------------------------------------------
;TFLAG:ACT파생(0=クンニ/1=펠라/2=안면승마/3=안면승마항문)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_51
;=================================================

;とりあえずFIRSTACT(0)で最上層に初回を配置しま했다が、各ACT파생内で処理するのも良いでしょう
;FIRSTACTの이벤트번호を被らないようにすれば、派生毎に初回判定ができます
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51, 初回
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;ACT파생が펠라強制
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;ACT파생が안면승마
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;ACT파생が안면승마항문
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 3
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;上層の分岐を全て埋めた場合はクンニ強制扱いになる
	;派生毎に書き分けないのであれば、この箇所を汎用的に使える内容に했다上で不要な分岐を削除すると良いでしょう
	;=================================================
	;기타・조건없음
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;죠교대상のＣを責めつつ안면승마になるシチュエーション
;안면승마になる場合は죠교자의 페니스、페니스밴드等を考慮する필요なし
;필요に応じて
;IF TEQUIP:페니스밴드
;ELSEIF PENIS(MASTER)
;ELSE
;等の分岐を設けて使用して下さい
;TEQUIP:죠교대상Ｃ사용 == 1は손으로 애무中
;TEQUIP:죠교대상Ｃ사용 == 2は입으로 애무中となります
;一応입と손それぞれの分岐を設けま했다が
;죠교자の素質によっては입으로 애무が殆ど出なかったりするので、そのあたりを見極めた上で分岐を増減して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상がＣを責められている
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TEQUIP:죠교대상Ｃ사용 == 1 || TEQUIP:죠교대상Ｃ사용 == 2
	;=================================================
	;질내사정後3ターン以内		膣입からは先程たっぷりと搾り取った정액がどろりとあふれでている…
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
		;입으로 애무中
		IF TEQUIP:죠교대상Ｃ사용 == 2
;OriginalString : 
			PRINTFORMW 
		;손으로 애무中
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;안면승마항문派生			目の前にひくつく菊門がさらけ出されている……
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 3
		;입으로 애무中
		IF TEQUIP:죠교대상Ｃ사용 == 2
;OriginalString : 
			PRINTFORMW 
		;손으로 애무中
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;통상の안면승마を想定。@VAGINA_DESCRIPTIONは描写系関数
	;=================================================
	;기타・조건없음			無防備な%VAGINA_DESCRIPTION(TARGET)%がさらけ出されている……
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;입으로 애무中
		IF TEQUIP:죠교대상Ｃ사용 == 2
;OriginalString : 
			PRINTFORMW 
		;손으로 애무中
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_0, クンニ強制
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;죠교자がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;연속실행
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
;OriginalString : 「この身体はまだおぼこなので……確りとほぐして下さいね」
		PRINTFORMW 「이 신체는 아직 숫처녀이므로……단단히풀어 주세요」
;OriginalString : 「でないと、困るのは私だけでは有りませんよ……旦那様？」
		PRINTFORMW 「이 아니면 곤란한 것은 나만으로는 없어요……남편분？」
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_1, 펠라チオ
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;죠교자がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;연속실행
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_2, 안면승마
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;죠교자がイキそう
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:가버려
;OriginalString : 「んんっ……早く、早く舐めて下さい……」
		PRINTFORMW 「응응……빨리, 빨리 빨아 주세요……」
;OriginalString : 「당신の顔に擦り付けて……私っ……！」
		PRINTFORMW 「당신의 얼굴에 칠해……나……！」
;OriginalString : 
		PRINTFORMDL
;OriginalString : 아큐は大きく발を開くと、%CALLNAME:MASTER%의 頭を掴みグリグリと클리토리스を鼻の頭に押し付ける……
		PRINTFORMDW 아구는 크게 다리를 열면(자), %CALLNAME:MASTER%의 머리를 잡아 그리그리와 클리토리스를 코의 머리에 꽉 누른다……
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;연속실행
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;질내사정後3ターン以内
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
;OriginalString : 「당신様ばかり愉しんでいて……ずるいです」
			PRINTFORMW 「귀하만 즐기고 있어……간사합니다」
;OriginalString : 「私のそそにも、御慈悲を下さいませんか？」
			PRINTFORMW 「 나의 것솔솔에도, 자비를 주시지 않겠습니까？」
;OriginalString : 
			PRINTFORMDL
;OriginalString : そう言うと아큐は、발を大きく開き누움된 %CALLNAME:MASTER%に見せつける。
			PRINTFORMDW 그렇게 말하면 아구는, 다리를 크게 벌어져 위로 향해 된 %CALLNAME:MASTER%에 과시한다.
;OriginalString : ぷにっと했다お腹から、程よく膨らんだスジまでが丸見えになる……
			PRINTFORMDW 에 로 한 배로부터, 적당하게 부풀어 오른 스지까지가 훤히 들여다보로 된다……
;OriginalString : 아큐はそのまま입の上に腰を下ろすと、もじもじと身を捩った。
			PRINTFORMDW 아구는 그대로구 위에 앉으면(자), 머뭇머뭇몸열.
;OriginalString : 
			PRINTFORMDL
;OriginalString : 「あ、ふ……これは、中々」
			PRINTFORMW 「아,……이것은, 꽤」
;OriginalString : 「背徳的で、ちょっと、楽しいかも……？」
			PRINTFORMW 「배덕적이어, 조금, 즐거울지도……？」
			KFLAG:151 += 1
		ELSE
;OriginalString : 「全くもって鈍臭い。せめてちんぽがクズなら입で楽しませよう、と位気が利かないんですか？」
			PRINTFORMW 「완전히 가지고 몹시 느리다. 책임이라고 가 쓰레기라면 입으로 즐겁게 하자, 라고 위 생각이 잘 미치지 않습니까？」
;OriginalString : 
			PRINTFORMDL
;OriginalString : むぎゅう、と自らの성기を押し付ける아큐。
			PRINTFORMDW 소, 라고 스스로의 성기를 꽉 누르는 아구.
;OriginalString : 体重は思ったよりも軽く、肉付きが薄い事を伺わせる。
			PRINTFORMDW 체중은 생각했던 것보다도 가볍고, 살집이 얇은 일을 방문하게 한다.
;OriginalString : だがそれ以上に、鼻孔にまで入り込んだ甘ったるい匂いが%CALLNAME:MASTER%의 頭の芯を焼いていく……
			PRINTFORMDW 하지만 그 이상으로, 콧구멍에까지 비집고 들어간 달콤한 냄새가 %CALLNAME:MASTER%의 머리의 심지를 구워 간다……
;OriginalString : 
			PRINTFORMDL
;OriginalString : 「さぁ、少しは誠意を見せて下さいな」
			PRINTFORMW 「, 조금은 성의를 보여 주세요」
;OriginalString : 「豚は豚らしく、ペロペロ舐めて봉사するんですよ？」
			PRINTFORMW 「돼지는 돼지인것 같고, 날름날름 빨아 봉사합니다？」
			KFLAG:151 += 1
		ENDIF
	RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_3, 안면승마항문
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 3
	;쾌Ｃ以外はTCVAR:가버려では拾えないのでPALAMで
	;=================================================
	;죠교자の쾌Ａが一定以上「가버려」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:쾌Ａ >= 8500
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;연속실행
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
;OriginalString : 「ふふっ、ちょっと困らせちゃいます」
			PRINTFORMW 「후후, 고있습니다」
;OriginalString : 「こっちは、子供を作るのには使えませんが……」
			PRINTFORMW 「여기는, 아이를 만드는데는 사용할 수 없습니다만……」
;OriginalString : 「당신様を悦ばせるのには、使えるんですからね？」
			PRINTFORMW 「귀하를 기쁨다투는데는, 사용할 수 있기 때문에？」
;OriginalString : 
			PRINTFORMDL
;OriginalString : 아큐は座りながらも、もじもじと腰を動かして尻たぶに顔を埋め시킨다。
			PRINTFORMDW 아구는 앉으면서도, 머뭇머뭇허리를 움직여 엉덩이에 얼굴을 묻게 한다.
;OriginalString : その様子からは、隠しきれぬ흥미が滲み出ている……
			PRINTFORMDW 그 모습으로부터는, 다 숨길 수 있지 않는 흥미가 배어 나오고 있다……
			KFLAG:151 += 1
		ELSE
;OriginalString : %CALLNAME:MASTER%의 입に항문を押し付け、아큐は嗜虐的な笑みを浮かべた。
			PRINTFORMDW %CALLNAME:MASTER%의 입에 어널을 꽉 눌러 아구는 기학적인 미소를 띄웠다.
;OriginalString : 
			PRINTFORMDL
;OriginalString : 「そんなに酷い匂いはしないでしょう？　お肉とかあんまり食べませんしね」
			PRINTFORMW 「그렇게 심한 냄새는 하지 않을 것입니다？ 고기라든지 너무 먹지않고」
;OriginalString : 「さぁ、存分に봉사なさって下さいな、子供のお尻がだーい好きなマゾ豚さん？」
			PRINTFORMW 「, 마음껏 봉사 하셔 주세요, 아이의 엉덩이가다-있고 좋아하는 매저키스트돼지씨？」
;OriginalString : 「違う？　そんな事は無いでしょう。肉棒から迸る悦びが隠しきれていませんよ？」
			PRINTFORMW 「다르다？ 그런 일은 없을 것입니다. 페니스로부터 내뿜는 즐거움이 다 숨길 수 있고 있지 않아요？」
;OriginalString : 
			PRINTFORMDL
;OriginalString : 更に아큐は腰を何度も上下に揺らし、無理矢理봉사を強要してくる……
			PRINTFORMDW 더욱 아구는 허리를 몇번이나 상하에 흔들어, 무리하게 봉사를 강요해 온다……
			KFLAG:151 += 1
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT52, 파이즈리
;*************************************************
;・죠교대상に파이즈리での봉사를 강요する
;・죠교대상이남자の場合は発生しない
;・죠교자に페니스がない場合は発生しない
;・턴방침が"이상"の場合は発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP(밧줄/삼각목마)장착中には発生しない
;・성교중/성교봉사중には発生しない
;・REACT분류が거부/날뛴다/逃げるの場合は拒絶扱いとなる
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_52
;=================================================
;地の文に分岐なし。@TARGET_DESCRIPTIONによる状況描写が入る可能性あり
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT52, 파이즈리
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;처음
	;記入チェック
	LOCAL = 0
	;=================================================
	IF LOCAL && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교자がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;죠교대상のＢ절정が近いというシチュエーション
	;=================================================
	;죠교대상の쾌Ｂが一定以上
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:MASTER:쾌Ｂ >= 8500
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교대상が거유
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:거유
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교대상が빈유
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:빈유
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT53, 스마타
;*************************************************
;・죠교대상に페니스밴드、もしくは페니스를股に挟んで擦れと命令する
;・죠교자に棒がない(페니스/페니스밴드)場合は発生しない
;・죠교대상に페니스がある場合は発生しない
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"이상"の場合は発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP:삼각목마장착中には発生しない
;・성교중/성교봉사중には発生しない
;・죠교대상に발がないと発生しない(現状では屠自古が죠교대상である場合のみと考えられる)
;・REACT분류が거부/날뛴다/逃げるである場合は拒絶扱い
;-------------------------------------------------
;TFLAG:ACT파생(0=스마타/1=페니스밴드스마타)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_53
;=================================================
;・안면승마系、자위系は解除される
;・地の文はACT파생で分岐。派生内では分岐なし。@TARGET_DESCRIPTIONによる状況描写が入る可能性あり
;-------------------------------------------------

;取り敢えず最上層に配置しま했다が、ACT파생内で処理してもいいと思います
;FIRSTACTの引数を揃えるか、FIRSTACT(0), FIRSTACT(1)として個別に初回判定を拾うかは書き손次第
;━━━━━━━━━━━━━━━━━━━━━━━━
;처음
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;페니스밴드스마타強制
	;記入チェック
	LOCAL = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;TFLAG:ACT파생 == 1の分岐を書いた場合はACT파생 == 0(스마타強制)扱いとなる
	;上層の分岐を방치してここだけにするなら、汎用的に使えるよう留意すべき箇所
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;죠교자に페니스がある場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_0, 스마타強制
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;イキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:가버려 || TCVAR:MASTER:가버려
		;죠교자(TARGET)が가버려
		IF TCVAR:TARGET:가버려
;OriginalString : 
			PRINTFORMW 
		;죠교대상(MASTER)が가버려
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;윤활十分
		IF PALAM:MASTER:윤활 >= 500
;OriginalString : 
			PRINTFORMW 
		;윤활不발
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;죠교자が페니스밴드장착中の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_1, 페니스밴드스마타強制
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;イキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:가버려 || TCVAR:MASTER:가버려
		;죠교자(TARGET)が가버려
		IF TCVAR:TARGET:가버려
;OriginalString : 
			PRINTFORMW 
		;죠교대상(MASTER)が가버려
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;윤활十分
		IF PALAM:MASTER:윤활 >= 500
;OriginalString : 
			PRINTFORMW 
		;윤활不발
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT55, 발핥기강제
;*************************************************
;・죠교자が발를 핥아ろと命令してくる
;・이상系から発生する場合あり
;・턴방침が"소프트"だと発生しない
;・本日の방침が"노닥노닥"だと発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP(볼개그/삼각목마)장착中には発生しない
;・성교중/성교봉사중には発生しない
;・죠교자に발がない場合は発生しない
;・REACT분류が거부/날뛴다/逃げるの場合は拒絶扱い
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_55
;=================================================
;・地の文に分岐なし。@TARGET_DESCRIPTIONで状況描写が入る可能性あり
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT55, 발핥기강제
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;처음
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;전회COMが許しを乞った
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("용서를 빌다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;징계・모드
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:징계・플래그
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교대상が누움
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:누움
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교자が신발안を履いている
	;(デフォルトで신발안履いていないキャラは表示されない)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:신발안
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT56, 이라마치오
;*************************************************
;・命令するのではなく、強引に페니스를입に突っ込む
;・죠교대상の反応は未定だが、拒絶する事はできない
;・가학系から発生する場合あり
;・턴방침が"노멀", "소프트"だと発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP(볼개그/삼각목마)장착中には発生しない
;・성교중/성교봉사중には発生しない
;・죠교자に페니스がない場合は発生しない(페니스밴드による이라마치오は無い)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_56
;=================================================
;・자위중であれば解除される
;・地の文に分岐なし。地の文は↓
;・%CALLNAME:TARGET%は%CALLNAME:MASTER%의 頭をつかんで、乱暴に%CALLNAME:MASTER%의 喉まで突き入れた…
;-------------------------------------------------
;・죠교자の발기도合いはTCVAR:발기で参照できる
;・TCVAR:발기(0=まだ柔らかい/1=徐々に硬度を増하면서ある/2=발기했다/2以上=反り返った)的な感じで
;-------------------------------------------------

;필요に応じて分岐を増減して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT56, 이라마치오
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;처음
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교자がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교대상が누움
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:누움
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT57, 가슴애무강제
;*************************************************
;・죠교대상に、죠교자の가슴を애무するよう強制する
;・죠교자が남자の場合は発生しない
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"이상"だと発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP:밧줄장착中は입で가슴애무강제に派生する。ただし볼개그を장착中の場合は弾かれる
;・볼개그がなければ、1/2の確率で입으로 애무になる
;・REACT분류が거부/날뛴다/逃げるの場合は拒絶扱い
;・PALAMが치정先行か반항先行かで"恥ずかしくて봉사できなかった/嫌がって要求に応じなかった"に派生する
;-------------------------------------------------
;TFLAG:ACT파생(0=손で가슴애무강제/2=입で가슴애무강제)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_57
;=================================================
;・地の文はACT파생で分岐。派生内では分岐なし
;・@TARGET_DESCRIPTIONによる状況描写が入る可能性あり
;・안면승마は解除される
;・REACTにおいて、누움でなく、ACT파생が입으로 애무でない場合、1/2で後ろから애무に派生する
;-------------------------------------------------

;取り敢えず最上層に配置しま했다が、ACT파생内で処理してもいいと思います
;FIRSTACTの引数を揃えるか、FIRSTACT(0), FIRSTACT(1)として個別に初回判定を拾うかは書き손次第
;━━━━━━━━━━━━━━━━━━━━━━━━
;처음
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;ACT파생2, 입で가슴애무를 강요
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 2
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;TFLAG:ACT파생 == 2分岐を埋めた場合は!TFLAG:ACT파생分岐として機能
	;ここだけで済ませるのであれば、どちらでも使える内容を
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_0, 손で가슴애무를 강요
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;以前は体位毎によっては規制されていたが、現在では全ての体位で発生する
	;場合によっては無茶な体制になるが、これは現状では仕様ということで
	;=================================================
	;죠교자삽입중
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교중
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교대상삽입중
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교봉사중
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교자がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:쾌Ｂ > 8500
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_2, 입で가슴애무를 강요
;記入チェック（=0, 非表示、1, 表示、2, 表示して미약等구상をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;以前は体位毎によっては規制されていたが、現在では全ての体位で発生する
	;場合によっては無茶な体制になるが、これは現状では仕様ということで
	;=================================================
	;죠교자삽입중
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교중
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교대상삽입중
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교봉사중
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;죠교자がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:쾌Ｂ > 8500
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
