;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	아큐구상 수치계 개별 리액션구상파일
;
;	수치계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;************************************************************************
;수치계 REACT40 ～ REACT45
;************************************************************************
;· 각 REACT내의 나누는 방법으로서 TFLAG:REACT분류(IS_COMGRONAME)나 SELECTCOM(IS_NOWCOMNAME)가 생각됩니다
;·다만, 범위의 넓은 조건으로 주워 버리는 것은 기본적으로 추천하지 않습니다
;·ACT분류:수치계의 경우, 기본적으로회화계 COM, 애무계 COM, 봉사계 COM가선택지가 됩니다만
;·상황에 따라서는 그 외의 것이선택지에 포함될 가능성도 있습니다
;·리액션(REACT)으로 처리되지 않았던 것은 범용 리액션(COM)으로 보완되므로
;·무리하게 모두 묻는필요는 없습니다
;·필요라고 생각하는 것을 좁혀, COM와의 제휴를 생각해 효율 좋게 구축해야 합니다
;-------------------------------------------------
;· 각 REACT내는,&& IS_NOWCOMNAME든지,&& IS_COMGRONAME등의 조건으로 결정하고 쳐 합시다
;·여기에 조건 없음 분기가 있으면, 모두 주워 버려 범용 리액션에 흐르지 않습니다
;·죠교대상의 반응에 관련되지 않고, 같은 리액션 밖에 하지 않게 되어 버립니다
;·요컨데 이것은, 상정당했다시스템의 파괴에 연결됩니다
;-------------------------------------------------
;  REACT의 지문은 ACT_MESSAGE.ERB의 TRAIN_MESSAGE2_{TFLAG:ACT}로 처리됩니다
;  기본적으로 지문은 TFLAG:REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)로 분기 해,
;  각 REACT분류내에서 TFLAG:REACT부하(자연스러운행동/무리가 없는행동/무리를했다행동)로 파생
;  고유의 TRAIN_MESSAGE2가 없는 경우는, REACTION_MESSAGE로 COM마다 범용 처리됩니다
;  ACT_MESSAGE.ERB REACTION_MESSAGE.ERB를 참조하면서의 작업을 추천합니다
;-------------------------------------------------
;각 REACT에서는 상정되는 반응을 망라되어 있습니다. 필요에 응해 분기를 증감해 사용해 주세요
;COM에 따라서는 REACT파생로 REACT분류가변화 하는 일에 주의
;각 COM의 파생은 기본적으로 REACT파생 1를 조건 결정 우리 소분기로 해, REACT파생 0는 그 외 취급으로 조건 없다고 보고 있습니다
;손간을 걸고 싶지 않은 경우는 그 외만을 써, 파생에 관련되지 않고 해당 개소를 보완할 수가 있습니다
;또 IF LOCAL:1 && TFLAG:REACT파생 < 1으로 하는 일로 파생했다경우는 범용 리액션에 흘린다 따위라고 하는 일도 가능합니다
;파생은 장래 증가할 가능성도 있으므로 != 1등으로 하는 것보다는< 1으로했다 (분)편이 무난하다라고 생각합니다
;-------------------------------------------------

;────────────────────────────────────
;REACT40, 자위·개별 리액션
;────────────────────────────────────
;·지문은 REACT분류로 분기
;·REACT분류가소극적으로 한다/태연하게 한다/적극적으로 한다/받아들이는인 경우는 수용 취급
;·그 외의 반응의 경우는 REACT분류는 모두거부에 세트 된다
;-------------------------------------------------
;  0203판 현재, ACT_MESSAGE에 있어서 REACT분류받아들이지만 새고 있으므로
;  지문적으로는 REACT분류:받아들이는에서 만나도거부취급이 된다
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_40
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("거부")
	;=================================================
	;부끄러워서 명령 대로로 할 수 없었다(치정선행)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:MASTER:치정 > PALAM:MASTER:반항
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;치정선행의 분기를 썼을 경우에는 싫어해 요구에 응하지 않았던(반항선행) 취급
	;쓰지 않았던 경우는 PALAM의상태에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다(REACT분류:용서를 빌다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("용서를 빌다")
	;파생 1의 경우는 REACT분류:없음
	;=================================================
	;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
;OriginalString : 「恥ずかしい……ですか？」
		PRINTFORMW 「부끄럽다…… 입니까?」
;OriginalString : 「でもそんなにいやらしく、손を動かして……」
		PRINTFORMW 「 그렇지만 그렇게 불쾌하고, 손을 움직여……」
;OriginalString : 「すりすり……しゅにしゅに……くりくり……ふふ♪」
		PRINTFORMW 「소매치기 소매치기……종류에 종류에……동글동글……후후♪」
;OriginalString : 「ええ、しっかりと見て覚えますよ……？」
		PRINTFORMW 「예, 제대로 봐 기억해요……?」
		RETURN LOCAL:1
	ENDIF
ENDIF

;파생 1의 경우는 REACT분류:날뛴다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
;OriginalString : 「恥ずかしい……ですか？」
		PRINTFORMW 「부끄럽다…… 입니까?」
;OriginalString : 「でもそんなにいやらしく、손を動かして……」
		PRINTFORMW 「 그렇지만 그렇게 불쾌하고, 손을 움직여……」
;OriginalString : 「すりすり……しゅにしゅに……くりくり……ふふ♪」
		PRINTFORMW 「소매치기 소매치기……종류에 종류에……동글동글……후후♪」
;OriginalString : 「ええ、しっかりと見て覚えますよ……？」
		PRINTFORMW 「예, 제대로 봐 기억해요……?」
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 「わぁ……」
	PRINTFORMW 「원……」
;OriginalString : 「ふふ、どう했다んですか？　見られるの、癖になっちゃいま했다？」
	PRINTFORMW 「후후, 어떻게 했습니까? 볼 수 있는 것, 버릇이 되어 버렸습니다?」
;OriginalString : 「こうやって、鼻を近づけると……男の人の匂いが、凄い……」
	PRINTFORMW 「이렇게, 코를 접근하면……남자의 냄새가, 굉장하다……」
;OriginalString : 「そのお姿……しっかりと書き留めて置きませんとね？」
	PRINTFORMW 「그 모습…… 제대로 써두지 않습니다와?」
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 「わぁ……」
	PRINTFORMW 「원……」
;OriginalString : 「ふふ、どう했다んですか？　見られるの、癖になっちゃいま했다？」
	PRINTFORMW 「후후, 어떻게 했습니까? 볼 수 있는 것, 버릇이 되어 버렸습니다?」
;OriginalString : 「こうやって、鼻を近づけると……男の人の匂いが、凄い……」
	PRINTFORMW 「이렇게, 코를 접근하면……남자의 냄새가, 굉장하다……」
;OriginalString : 「そのお姿……しっかりと書き留めて置きませんとね？」 
	PRINTFORMW 「그 모습…… 제대로 써두지 않습니다와?」 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;기입 체크
LOCAL:1 = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
;OriginalString : 「恥ずかしい……ですか？」
		PRINTFORMW 「부끄럽다…… 입니까?」
;OriginalString : 「でもそんなにいやらしく、손を動かして……」
		PRINTFORMW 「 그렇지만 그렇게 불쾌하고, 손을 움직여……」
;OriginalString : 「すりすり……しゅにしゅに……くりくり……ふふ♪」
		PRINTFORMW 「소매치기 소매치기……종류에 종류에……동글동글……후후♪」
;OriginalString : 「ええ、しっかりと見て覚えますよ……？」
		PRINTFORMW 「예, 제대로 봐 기억해요……?」
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;기입 체크
LOCAL:1 = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 「わぁ……」
	PRINTFORMW 「원……」
;OriginalString : 「ふふ、どう했다んですか？　見られるの、癖になっちゃいま했다？」
	PRINTFORMW 「후후, 어떻게 했습니까? 볼 수 있는 것, 버릇이 되어 버렸습니다?」
;OriginalString : 「こうやって、鼻を近づけると……男の人の匂いが、凄い……」
	PRINTFORMW 「이렇게, 코를 접근하면……남자의 냄새가, 굉장하다……」
;OriginalString : 「そのお姿……しっかりと書き留めて置きませんとね？」
	PRINTFORMW 「그 모습…… 제대로 써두지 않습니다와?」
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;기입 체크
LOCAL:1 = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF
;REACT40, 자위

;────────────────────────────────────
;REACT41, 비패개장·개별 리액션
;────────────────────────────────────
;·지문은 REACT분류로 분기
;·ACTION_APLLY2에 고유의 처리 없음
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_41
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다(REACT분류:용서를 빌다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("용서를 빌다")
	;파생 1의 경우는 REACT분류:없음
	;=================================================
	;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;파생 1의 경우는 REACT분류:날뛴다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("겁없이 웃는다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF
;REACT41, 비패개장

;────────────────────────────────────
;REACT42, 자위를과시한다·개별 리액션
;────────────────────────────────────
;·지문은 REACT분류로 분기
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_42
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분 좋게 해(REACT분류:소극적으로 한다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;「이 철면피의 돼지째!」(징계·포인트가산)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다(REACT분류:받아들인다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("자위를 시작한다")
	;=================================================
	;승손에 자위 하지마(징계·포인트가산)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「승손에 자위 하고 있지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT42, 자위를과시한다

;────────────────────────────────────
;REACT43, 수치플레이·개별 리액션
;────────────────────────────────────
;·@TRAIN_MESSAGE2_43이 존재하지 않고, 고유의 리액션의 지문 없음
;·죠교대상의 반응에 응해 REACTTION_MESSAGE에서의 범용 처리만
;-------------------------------------------------
;·TFLAG:ACT파생(0=개시/1=종료)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_43
;=================================================
;·뭐라고 할까, 거동 사태 시원스럽게 하고 있으므로 노력하고 써도 보답받지 못하는 생각은 듭니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;개시시
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;COM0의 REACT파생는 특정의 ACT로 한정되므로, 이 경우 고려하는필요없음
	;=================================================
	;COM0, 침묵한다(REACT분류:없음)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM5, 빈정거린다침묵한다(REACT분류:태연하게 한다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM6, 더 하드하게 해(REACT분류:받아들인다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("더 하드하게 해")
		;-------------------------------------------------
		;「이 철면피의 돼지째!」(징계·포인트가산)
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("용서를 빌다")
		;파생 1의 경우는 REACT분류:없음
		;-------------------------------------------------
		;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	
		;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;기입 체크
		LOCAL:1 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM8, 기분 좋게 해(REACT분류:소극적으로 한다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분좋게 해")
		;-------------------------------------------------
		;「이 철면피의 돼지째!」(징계·포인트가산)
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM33, 무서워한다(REACT분류:용서를 빌다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("무서워한다")
		;-------------------------------------------------
		;징계·포인트가산(REACT분류:없음)
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;기입 체크
		LOCAL:1 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM50, 자위를 시작한다(REACT분류:받아들인다)
	;기입 체크
	LOCAL = 0
	;=================================================
	IF LOCAL && IS_NOWCOMNAME("자위를 시작한다")
		;-------------------------------------------------
		;승손에 자위 하지마(징계·포인트가산)
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「승손에 자위 하고 있지만 좋은가」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;파생 0을 참고에필요인 분기를 추가해 사용해 주세요
;조건 없음의 기술은 멈춥시다
;━━━━━━━━━━━━━━━━━━━━━━━━
;종료시
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
ENDIF
;REACT43, 수치플레이

;────────────────────────────────────
;REACT44, 야외플레이·개별 리액션
;────────────────────────────────────
;·@TRAIN_MESSAGE2_44가 존재하지 않고, 고유의 리액션의 지문 없음
;·죠교대상의 반응에 응해 REACTTION_MESSAGE에서의 범용 처리만
;-------------------------------------------------
;·TFLAG:ACT파생(0=개시/1=종료)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_44
;=================================================
;·뭐라고 할까, 거동 사태 시원스럽게 하고 있으므로 노력하고 써도 보답받지 못하는 생각은 듭니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;개시시
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;COM0의 REACT파생는 특정의 ACT로 한정되므로, 이 경우 고려하는필요없음
	;=================================================
	;COM0, 침묵한다(REACT분류:없음)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM5, 빈정거린다(REACT분류:태연하게 한다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM6, 더 하드하게 해(REACT분류:받아들인다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("더 하드하게 해")
		;-------------------------------------------------
		;「이 철면피의 돼지째!」(징계·포인트가산)
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("용서를 빌다")
		;파생 1의 경우는 REACT분류:없음
		;-------------------------------------------------
		;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	
		;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM8, 기분 좋게 해(REACT분류:소극적으로 한다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분좋게 해")
		;-------------------------------------------------
		;「이 철면피의 돼지째!」(징계·포인트가산)
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM33, 무서워한다(REACT분류:용서를 빌다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("무서워한다")
		;-------------------------------------------------
		;징계·포인트가산(REACT분류:없음)
		;기입 체크
		LOCAL:1 = 0
		;-------------------------------------------------
		IF LOCAL:2 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;COM50, 자위를 시작한다(REACT분류:받아들인다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("자위를 시작한다")
		;-------------------------------------------------
		;승손에 자위 하지마(징계·포인트가산)
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「승손에 자위 하고 있지만 좋은가」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
;OriginalString : 
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;파생 0을 참고에필요인 분기를 추가해 사용해 주세요
;조건 없음의 기술은 멈춥시다
;━━━━━━━━━━━━━━━━━━━━━━━━
;종료시
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
ENDIF
;REACT44, 야외플레이

;────────────────────────────────────
;REACT45, 방뇨·개별 리액션
;────────────────────────────────────
;·지문은 REACT분류로 분기
;·REACT분류가소극적으로 한다/태연하게 한다/적극적으로 한다/받아들이는인 경우는 수용 취급
;·REACT분류가소극적으로 한다의 경우는 「어쩔 수 없이」, 태연하게 한다/적극적으로 한다의 경우는 「부끄러운 듯하지도 않고」라고 하는 취급
;·그 외의 반응의 경우는 REACT분류는 모두거부에 세트 된다
;-------------------------------------------------
;  0203판 현재, ACT_MESSAGE에 있어서 REACT분류:받아들이지만 새고 있으므로
;  지문적으로는 REACT분류:받아들이는에서 만나도거부취급이 된다
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_45
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("거부")
	;=================================================
	;부끄러워서 명령 대로로 할 수 없었다(치정선행)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:MASTER:치정 > PALAM:MASTER:반항
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;치정선행의 분기를 썼을 경우에는 싫어해 요구에 응하지 않았던(반항선행) 취급
	;쓰지 않았던 경우는 PALAM의상태에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다(REACT분류:용서를 빌다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("용서를 빌다")
	;파생 1의 경우는 REACT분류:없음
	;=================================================
	;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분 좋게 해(REACT분류:소극적으로 한다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;「이 철면피의 돼지째!」(징계·포인트가산)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;기입 체크
LOCAL:1 = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;기입 체크
LOCAL:1 = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;기입 체크
LOCAL:1 = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF
;REACT45, 방뇨
