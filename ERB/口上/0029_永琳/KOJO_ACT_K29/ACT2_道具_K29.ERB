;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	에이린구상 도구계 액션구상파일
;
;	도구계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;도구계 ACT20～26
;========================================================================
;·TFLAG:ACT(20=로터/21=바이브/22=항문바이브/23=항문비즈/24=Ｃ애무도구/25=유두캡/26=페니스밴드)
;·TFLAG:ACT분류 == 2에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "도구")로 줍는 것이 가능
;·죠교자가죠교대상에도구를 사용한다. 이구상의 시점에서죠교자는 벌써손를 내고 있다
;·미약·로션를 사용하는 케이스가 있다
;·도구류의장착, 해제등은 TFLAG:ACT파생로 정의된다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT20, 로터
;────────────────────────────────────
;·Ｃ중심으로애무 한다
;·애무계로부터 발생하는 경우도 있다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·TEQUIP(클리캡/오나홀/페니스밴드/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교대상에페니스가 있는 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K29_20
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT20, 로터
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && !TFLAG:ACT파생
	;TCVAR:MASTER:위치전후와 TEQUIP:안면승마/안면승마항문로 지문이변화 합니다
	;필요에 응해 분기를 증감해 주세요
	;=================================================
	;죠교대상가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:가버려
		;애태움도에 의해 분기
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
;OriginalString : 
				PRINTFORMW 
			;애태움없음·애태움종료
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;하반신복장이 요점 밀치고
	;=================================================
	IF 구상기입체크(0) && PANTSCHECK(1, 1)
		;옷 너머로
		IF PANTSCHECK(1)
;OriginalString : 
			PRINTFORMW 
		;그 외		LOWERJACKET(1) == 1으로 옷자락으로부터손를 넣어 LOWERUNDER(1) == 1으로속옷를 비켜 놓는 상황. 양쪽 모두 성립하고 있는 가능성도 있다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;위치전후를 분리한다면, TCVAR:MASTER:위치전후 == 2로 뒤로부터가 됩니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
;OriginalString : 「これで遊んであげる」
		PRINTFORMW 「이것으로 놀아 준다」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT21, 바이브
;────────────────────────────────────
;·죠교대상이남자의 경우는 발생하지 않는다
;·TFLAG:징계·플래그가 끊고 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드)장착중이라면 발생하지 않는다
;·성교중에는 발생하지 않는다
;·죠교대상가처녀에서도 발생할 가능성이 있다
;-------------------------------------------------
@KOJO_ACT_K29_21
;-------------------------------------------------
;·TFLAG:ACT파생(0=바이브장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT21_0, 바이브장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가후타나리의 경우, 전립선자극취급이 되어발기도가 상승한다
;지문은 파생하지 않기 때문에, ABL:MASTER:Ｖ감각든지 TEQUIP든지로 분기를 늘려도 괜찮다고 생각합니다
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	;죠교대상가처녀의 경우, 처녀상실가 일어난다. REACT로 참조하는 경우는 TALENT:MASTER:처녀는 아니고, TCVAR:MASTER:처녀상실로
	;=================================================
	;죠교대상가처녀
	;=================================================
	IF 구상기입체크() && TALENT:MASTER:처녀
;OriginalString : 「당신の처녀は、これに決めたわ」
		PRINTFORMW 「당신의 처녀는, 이것으로 결정했어요」
;OriginalString : 「ふふふ、そんな顔しちゃって……どんな顔をしようとも、これよ」
		PRINTFORMW 「후후후, 그런 얼굴 해 버려서……어떤 얼굴을 해도, 이것이야」
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가후타나리
	;=================================================
	IF 구상기입체크() && TALENT:MASTER:후타나리
;OriginalString : 「당신のモノよりこっちの도구のほうが立派なんじゃない？」
		PRINTFORMW 「당신의 물건보다 여기의 도구 쪽이 훌륭한 것이 아니다?」
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
;OriginalString : 「おちんぽとどっちが気持ちいいかしら」
		PRINTFORMW 「와 어느 쪽이 기분이 좋을까」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 바이브해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 바이브해제가 된다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 1
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
;OriginalString : 「気分はどうだった？」
		PRINTFORMW 「기분은 어땠어요?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT22, 항문바이브
;────────────────────────────────────
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·TEQUIP(항문비즈/관장그릇＋플러그/삼각목마/페니스밴드)장착중은 발생하지 않는다
;·항문섹스중(TEQUIP:성교중 == 6, 혹은 A_SEX())에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K29_22
;-------------------------------------------------
;TFLAG:ACT파생(0=항문바이브장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT22_0, 항문바이브장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상이남자의 경우, 전립선자극취급이 되어발기도가 상승
;지문은 파생하지 않기 때문에, ABL:MASTER:Ａ감각든지 TEQUIP든지로 분기를 늘려도 괜찮다고 생각합니다
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	;=================================================
	;바이브장착중(두 구멍 고문)
	;=================================================
	IF 구상기입체크() && TEQUIP:바이브
;OriginalString : 「あはっ、この子、オモチャに輪姦されちゃってるわ」
		PRINTFORMW 「아는 , 이 아이, 장난감에 윤간되어 버린다 원」
		RETURN 1
	ENDIF

	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
;OriginalString : 「変に力むと裂けちゃうかもしれないわよ？」
		PRINTFORMW 「이상하게 힘주면 찢어져버릴까도 몰라요?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT22_1, 항문바이브해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;이미 장비 하고 있는 경우, 항문바이브해제가 된다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 1
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
;OriginalString : 「당신の항문、ぽっかりと拡がっちゃったわね」
		PRINTFORMW 「당신의 어널, 뻥펼쳐져 버렸다 원이군요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT23, 항문비즈
;────────────────────────────────────
;·항문비즈장착/해제/추가
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·TEQUIP(항문바이브/관장그릇＋플러그/삼각목마/페니스밴드)장착중에는 발생하지 않는다
;·Ａ성교중(TEQUIP:성교중 == 6, 혹은 A_SEX()) 시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K29_23
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=해제/2=추가)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT23_0, 항문비즈장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·장착시에 1～3개의 사이에 랜덤으로 정해진다
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
;OriginalString : 「入っちゃったわねぇ、쿠쿡」
		PRINTFORMW 「들어가 버렸다 원이군요 , 킥킥」
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
		SELECTCASE TEQUIP:항문비즈
			CASE 1
;OriginalString : 「まずは……このくらいね」
				PRINTFORMW 「우선은……이 정도군요」
			CASE 2
;OriginalString : 「ふふ、苦しそうね。このくらいにしてあげる」
				PRINTFORMW 「후후, 괴로운 듯하구나. 이 정도로 해 준다」
			CASE 3
;OriginalString : 「さ、その調子で全部飲み込んでちょうだい」
				PRINTFORMW 「, 그 상태로 전부 삼켜 줘」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT_23_1, 항문비즈해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·구슬이 5개 들어가 있는 경우, 항문비즈해제가 된다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 1
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
;OriginalString : 「はい、よくできま했다」
		PRINTFORMW 「네, 잘 할 수 있었습니다」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT23_2, 항문비즈추가
;━━━━━━━━━━━━━━━━━━━━━━━━
;·비즈의 추가는 1～3의 사이에 랜덤으로 정해진다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 2
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
		;비즈의 개수에 의한 파생
		SELECTCASE TEQUIP:항문비즈
			;4개, 혹은 5번째. 사양상, 5개 이상이 되는 일은 없다
			CASE 5
;OriginalString : 「全部入ったわねぇ、お腹どんな気分？」
				PRINTFORMW 「전부 들어간 원이군요, 배 어떤 기분?」
			CASE 4
;OriginalString : 「もう少し、全部入るまで頑張りなさい」
				PRINTFORMW 「좀 더, 전부 들어갈 때까지 노력하세요」
			;그 외
			CASEELSE
;OriginalString : 「ふふ、はみ出た珠が안쓰러운わね」
				PRINTFORMW 「후후, 초과한 주가 야무지지 못하네요」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT24, Ｃ애무도구
;────────────────────────────────────
;·클리캡/오나홀의장착, 해제
;·오나홀을 소지 또한죠교대상에페니스가 있는 경우는 「오나홀」에 파생한다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"의 경우는 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드)장착중에는 발생하지 않는다
;·오나홀장착는발기도가 1000 이하(TCVAR:MASTER:발기도 < 1000)의 경우에 연주해져 발생하지 않는다
;·성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K29_24
;-------------------------------------------------
;·TFLAG:ACT파생(0=클리캡장착/1=클리캡해제/2=오나홀장착/3=오나홀해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_0, 클리캡장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_1, 클리캡해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 클리캡해제가 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_2, 오나홀장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:ACT파생 == 2
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
		;죠교대상가동정
		IF TALENT:MASTER:동정
;OriginalString : 
			PRINTFORMW 
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
		;죠교대상가동정
		IF TALENT:MASTER:동정
;OriginalString : 「동정には使い慣れた도구かしら、ふふっ」
			PRINTFORMW 「동정에는 익숙해진 도구일까, 후후」
		;그 외
		ELSE
;OriginalString : 「ひんやりして気持ちいいでしょ？」
			PRINTFORMW 「서늘해 기분이 좋지요?」
		ENDIF 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_3, 오나홀해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 오나홀해제가 된다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 3
	;=================================================
	;정액 서로 더러워져
	;=================================================
	IF 구상기입체크() && GET_STAIN("페니스", "정액", MASTER)
;OriginalString : 「あらあら、中から정액が溢れだしちゃってるじゃない」
		PRINTFORMW 「대강, 안으로부터 정액이 흘러넘치기 시작해 버린다가 아니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
;OriginalString : 「少しは만족できたかしら？」
		PRINTFORMW 「조금은 만족할 수 있었는지 해들?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT25, 유두캡
;────────────────────────────────────
;·유두캡장착/해제
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·유두캡라고 말해도, 지문에서는 클립로터라고 하는 취급
;-------------------------------------------------
@KOJO_ACT_K29_25
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT25_0, 유두캡장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT25_1, 유두캡해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 유두캡해제가 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT26, 페니스밴드
;────────────────────────────────────
;·페니스밴드장착/해제
;·죠교자가죠교대상에 쌍두페니스밴드장착 시키려고 하는 장면
;·죠교대상가처녀라면, 처녀상실가 일어난다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·TEQUIP:(항문비즈/항문바이브/관장그릇＋플러그/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교대상에페니스가 있으면 발생하지 않는다
;·TEQUIP(바이브/클리캡)는 양립하지 않는 것으로서 해제된다
;-------------------------------------------------
@KOJO_ACT_K29_26
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT26_0, , 페니스밴드를장착시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		;죠교대상가처녀
		IF TALENT:MASTER:처녀
;OriginalString : 
			PRINTFORMW 
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT26_1, 페니스밴드를 해제시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
