;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	에이린구상 콤비 네이션계 액션구상파일
;
;	콤비 네이션계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;콤비 네이션계 ACT300～312
;========================================================================
;·TFLAG:ACT(300=Ｗ수음/301=Ｗ펠라/302=Ｗ키스/303=Ｗ스마타/304=Ｗ파이즈리/305=Ｗ풋잡
;·306=기승위이중공격/307=대면이중공격/308=노니는모난/309=항문이중공격/310=Ｗ역강간/311=타니와타리/312=이중이라마치오)
;·조수가 존재하는 경우,50%의 확률로 콤비 네이션에 파생한다
;·TFLAG:ACT가 GET_COMBIACTNUM(TFLAG:ACT)로 덧쓰기되어 함수내에서 ACT번호가 정의된다
;·콤비 네이션시의 ASSI는 반드시(ASSI:1)된다
;·ACT_ABLE는 기본적으로 파생 바탕으로 준거
;-------------------------------------------------
;  회화문중등에서조수를 불러 나누고 싶을 때는%CNAME((ASSI:1), TARGET)%등으로 하는 일에 의해
;  캐릭터 CSV로 정의되고 있으면 상손에 의해 불러 나눕니다
;  EVENT111, 주인님로서 새로운죠교자를 고용한 개소에서 CSTR:(FLAG:주인님):호칭목록에 대입하는 일에 의해
;  구상측에서 임의의 통칭을 정의하는 일도 가능
;  조수가 본래의주인님라고 하는 가능성도 있다
;-------------------------------------------------

;────────────────────────────────────
;ACT300, Ｗ수음
;────────────────────────────────────
;·죠교자와조수의 2명이죠교대상에손코키 한다
;·ACT10, 손으로 애무로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_300
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT300, Ｗ수음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:MASTER%는 %CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%의 손가락에성기전체를 빠짐없이애무 되고
	;단번에절정로 이끌렸다…
	;=================================================
	;절정로 이끌리고
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:애태움도 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 반응을 물으면서 잡은 육봉을 상하시키고
	;절정직전에 애태우고 있다…
	;=================================================
	;애태움중
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:애태움도 > 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 함께 %CALLNAME:MASTER%의 남성기를손로 훑어 냈다…
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT301, Ｗ펠라
;────────────────────────────────────
;·죠교자와조수의 2명이죠교대상에펠라티오를 실시한다
;·ACT12, 입으로 애무로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_301
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT301, Ｗ펠라
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 귀두를 사이에 두어 혀를 얽히게 해라
	;사정를 촉했다…
	;=================================================
	;절정로 이끌리고
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:애태움도 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 귀두를 중심으로 졸졸 혀를 기 조생
	;절정직전에 애태우고 있다…
	;=================================================
	;애태움중
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:애태움도 > 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 서로 경쟁하도록(듯이) %CALLNAME:MASTER%의 남성기에 혀를 기게 했다…
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT302, Ｗ키스
;────────────────────────────────────
;·죠교자와조수의 2명이죠교대상에키스를 실시한다
;·ACT15, 키스로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_302
;-------------------------------------------------
;·지문에 분기 없음. 내용은 이하와 같다
;  %CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%에 얼굴을 접근해,
;  %CALLNAME:MASTER%의 혀를 서로 빼앗았다…
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT302, Ｗ키스
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;죠교자가가버려
		IF TCVAR:가버려
;OriginalString : 
			PRINTFORMW 
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;애태움중
		IF TCVAR:MASTER:애태움도 > 1
;OriginalString : 
			PRINTFORMW 
		;애태움없음
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT303, Ｗ스마타
;────────────────────────────────────
;·죠교자와조수의 2명이죠교대상에스마타를 실시한다
;·죠교대상에페니스가 있는 것이 전제
;·ACT16, 성기마찰한다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_303
;-------------------------------------------------
;·지문은죠교대상가누움인가 아닌가로 분기
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT303, Ｗ스마타
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 음순으로 %CALLNAME:MASTER%의 남성기를 끼워 넣어 켜기 시작했다…
	;=================================================
	;죠교대상가누움
	;=================================================
	IF 구상기입체크(0) && TFLAG:누움
		;애태움중
		IF TCVAR:MASTER:애태움도 > 1
;OriginalString : 
			PRINTFORMW 
		;애태움없음
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%상하과 겹쳐져, 밀착했다음순의 사이에
	;남성기를삽입 하도록(듯이) 명했다…
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		;애태움중
		IF TCVAR:MASTER:애태움도 > 1
;OriginalString : 
			PRINTFORMW 
		;애태움없음
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT304, Ｗ파이즈리
;────────────────────────────────────
;·죠교자와조수의 2명이죠교대상에파이즈리를 실시한다
;·죠교대상에페니스가 있는 것이 전제
;·ACT17, 파이즈리한다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_304
;-------------------------------------------------
;·지문은애태움도에 의해 분기. 각 분기내에서죠교자＆조수의가슴의 사이즈에 의한 파생
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT304, Ｗ파이즈리
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;정직, 그 밖에 얼마든지 할 길은 있습니다만, 우선 텐프레로서 제시한다면 이런 느낌일까와
	;=================================================
	;절정로 이끌리고
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:애태움도 == 1
		;%CALLNAME:MASTER%의 페니스는 두 명의거유의 틈새로 구깃구깃으로 되고
		;가는 것은 이미시간의 문제다…
		;-------------------------------------------------
		;죠교자와조수가거유
		;-------------------------------------------------
		IF 구상기입체크(0) && TALENT:거유 && TALENT:(ASSI:1):거유
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 힘껏가슴를 대어 자그마한 골짜기를 만들면
		;%CALLNAME:MASTER%의 페니스를 사이에 둬, 사랑스럽고정액를 졸라대고 있다…
		;-------------------------------------------------
		;죠교자와조수가빈유
		;-------------------------------------------------
		IF 구상기입체크(0) && TALENT:빈유 && TALENT:(ASSI:1):빈유
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%은(는) %CALLNAME:MASTER%의 페니스를가슴로 사이에 두면
		;격렬하고상반신를 흔들어 단번에절정까지 이끌었다…
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		;온전히 하는 것은손간인 것으로 "그 외"만으로 끝마쳐 버리는 것도 있어
		;여기를 묻어 두면 상위의 분기를 비워도 보완됩니다
	ENDIF
	
	;=================================================
	;애태움중
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:애태움도 > 1
		;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%은(는) %CALLNAME:MASTER%의 페니스를가슴로 완전하게 감싸면
		;보들보들 압력을 가하고 %CALLNAME:MASTER%를애태움라고 있다…
		;-------------------------------------------------
		;죠교자와조수가거유
		;-------------------------------------------------
		IF 구상기입체크(0) && TALENT:거유 && TALENT:(ASSI:1):거유
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%은(는) %CALLNAME:MASTER%의 페니스를가슴로 사이에 두어 얼싸안아
		;둘이서 혀를 서로 걸고 있다…
		;-------------------------------------------------
		;죠교자와조수가빈유
		;-------------------------------------------------
		IF 구상기입체크(0) && TALENT:빈유 && TALENT:(ASSI:1):빈유
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%은(는) %CALLNAME:MASTER%의 페니스를가슴로 사이에 두면
		;%CALLNAME:MASTER%의 반응을 물으면서애태움라고 있다…
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		;온전히 하는 것은손간인 것으로 "그 외"만으로 끝마쳐 버리는 것도 있어
		;여기를 묻어 두면 상위의 분기를 비워도 보완됩니다
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 남성기를 4개의 유방으로 끼워 넣었다…
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		;발기
		IF TCVAR:MASTER:발기도 >= 1000
;OriginalString : 
			PRINTFORMW 
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT305, Ｗ풋잡
;────────────────────────────────────
;·죠교자와조수의 2명이죠교대상에풋잡하기
;·죠교대상에페니스가 있는 것이 전제
;·ACT18, 풋잡하기로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_305
;-------------------------------------------------
;·지문에 분기 없음. 내용은 이하와 같다
;  %CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 협력해 %CALLNAME:MASTER%의 남성기를발로 훑어 냈다…
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT305, Ｗ풋잡
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;죠교대상가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:가버려
		;애태움도에 의한 파생
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
;OriginalString : 
				PRINTFORMW 
			;애태움종료
			CASE 1
;OriginalString : 
				PRINTFORMW 
			;애태움없음
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		;발기
		IF TCVAR:MASTER:발기도 >= 1000
;OriginalString : 
			PRINTFORMW 
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT306, 승마 두 구멍 고문
;────────────────────────────────────
;·기승위중의죠교대상에조수가Ａ삽입 한다
;·ACT32, 기승위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_306
;-------------------------------------------------
;ACT파생에의 대입은 TEQUIP를 참조해 행해지고 있다
;예를 들면 ACT파생 1등의 경우, 전회ACT가기승위라고는 할 수 없기 때문에 주의
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT306, 승마 두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 질내로부터안빼고에,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에항문도 범하도록 지시했다…
	;=================================================
	;연속(기승위로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%가삽입했다까지 체위를기승위로 바꾸면,
	;%CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 실룩거리는항문에 자신의남성기를 주었다…
	;=================================================
	;체위 변경(Ｖ성교계로부터)			페니스밴드에서의Ａ성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는삽입했다\@ TEQUIP:죠교자페니스밴드? 모형자지 # 페니스 \@를항문로부터 뽑아 %CALLNAME:MASTER%에기승위로삽입 하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에항문도 범하도록 지시했다…
	;=================================================
	;Ａ성교로부터						페니스밴드에서의Ａ성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 자신에게삽입했다\@ TEQUIP:페니스밴드? 모형자지 # 페니스 \@를 뽑아, %CALLNAME:MASTER%에기승위로삽입 하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에항문도 범하도록 지시했다…
	;=================================================
	;Ｖ성교봉사계로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 자신에게삽입했다\@ TEQUIP:페니스밴드? 모형자지 # 페니스 \@를 뽑아, %CALLNAME:MASTER%에기승위로삽입 하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에항문도 범하도록 지시했다…
	;=================================================
	;Ａ성교봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 5
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%에기승위로삽입시킨다와
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에항문도 범하도록 지시했다…
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT307, 대면두 구멍 고문
;────────────────────────────────────
;·죠교자가죠교대상의Ｖ에삽입했다뭐, 조수가죠교대상의Ａ에삽입 한다
;·ACT32, 대면좌위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_307
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT307, 대면두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 질내에삽입했다
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에항문도 범하도록 지시했다…
	;=================================================
	;연속(대면좌위로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%가삽입했다까지 체위를대면좌위로 바꾸면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에항문도 범하도록 지시했다…
	;=================================================
	;체위 변경(Ｖ성교계로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는삽입했다\@ TEQUIP:죠교자페니스밴드? 모형자지 # 페니스 \@를항문로부터 뽑아 %CALLNAME:MASTER%에대면좌위로삽입 하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에항문도 범하도록 지시했다…
	;=================================================
	;Ａ성교로부터						페니스밴드에서의Ａ성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 자신에게삽입했다\@ TEQUIP:페니스밴드? 모형자지 # 페니스 \@를 뽑아 %CALLNAME:MASTER%에대면좌위로삽입 하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에항문도 범하도록 지시했다…
	;=================================================
	;Ｖ성교봉사계로부터				v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 자신에게삽입했다\@ TEQUIP:페니스밴드? 모형자지 # 페니스 \@를 뽑아 %CALLNAME:MASTER%에대면좌위로삽입 하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에항문도 범하도록 지시했다…
	;=================================================
	;Ａ성교봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 5
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%에대면좌위로삽입 하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에항문도 범하도록 지시했다…
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT308, 노니는모난
;────────────────────────────────────
;·죠교대상의Ｖ에배면좌위로삽입하면서, 조수에 결합부의 상황을 실황시킨다
;·ACT33, 배면좌위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_308
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT308, 노니는모난
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 질내로부터안빼고에 그대로 안으면 와
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;연속(배면좌위로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%%CALLNAME:MASTER%를 뒤로부터삽입했다안아,
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;체위 변경(Ｖ성교계로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를배면좌위로 관철하면 그대로 안아,
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;Ａ성교로부터						페니스밴드에서의Ａ성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를배면좌위로 관철하면 그대로 안아,
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;Ｖ성교봉사계로부터				v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를배면좌위로 관철하면 그대로 안아,
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;Ａ성교봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 5
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를배면좌위로 관철하면 그대로 안아,
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT309, 항문두 구멍 고문
;────────────────────────────────────
;·죠교자가죠교대상의Ａ에삽입했다뭐, 조수가죠교대상의Ｖ에삽입 한다
;·죠교대상이남자의 경우는 발생하지 않는다
;·ACT33, 항문섹스로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_309
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT309, 항문두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음. 내용은 이하와 같다
;  %CALLNAME:TARGET%는 %CALLNAME:MASTER%를의항문를 관철하면 그대로 안아,
;  %CALLNAME:(ASSI:1)%에 전부터도 범하도록 지시했다
;-------------------------------------------------
IF 구상기입체크(0)
	;지문은 분기 하지 않습니다만, ACT파생자체는 ACT33, 항문섹스와 같게 정의되고 있습니다
	;필요에 응해 확장해 주세요
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT310, Ｗ역강간
;────────────────────────────────────
;·죠교자와조수로, 죠교대상의 페니스를 교대로삽입 하고 즐긴다
;·ACT103, 역강간로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_310
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;·v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT310, Ｗ역강간
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;Ｖ성교봉사로질내사정 후, 계속해 실행된다고 하는 시추에이션
	;이 경우의 지문은, ACT파생 1와 같은 것이 된다
	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(0) && TFLAG:안빼고
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:MASTER%의 위로 격렬하게 허리를 흔들고 있던 %CALLNAME:TARGET%는 일단\@ TEQUIP:페니스밴드? 모형자지 # 페니스 \@를해방 하면
	;%CALLNAME:(ASSI:1)%에삽입 시키고 %CALLNAME:MASTER%의 남성기를 교대로 즐기는 것에했다…
	;=================================================
	;연속(역강간로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%과 개인이 하며 밀어 넘어트린다와 %CALLNAME:(ASSI:1)%를 불러들여,
	;%CALLNAME:MASTER%의 남성기를 교대로삽입하면서 즐겼다…
	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를밀어 넘어트린다와 %CALLNAME:(ASSI:1)%를 불러들여,
	;%CALLNAME:MASTER%의 남성기를 교대로삽입하면서 즐겼다…
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT311, 타니와타리
;────────────────────────────────────
;·3Ｐ의 일종
;·죠교자와조수가 상하과 겹쳐져, 죠교대상가 두 명의바기나를 교대로 범한다
;·ACT95, 정상위시킨다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_311
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;·v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT311, 타니와타리
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음. 내용은 이하와 같다
;  %CALLNAME:TARGET%는 %CALLNAME:(ASSI:1)%과 상하서로 과 겹쳐져,
;  %CALLNAME:MASTER%에 두 명의바기나를 교대로 범하도록(듯이) 명령했다…
;-------------------------------------------------
IF 구상기입체크(0)
	;Ｖ성교봉사로질내사정 후, 계속해 실행된다고 하는 시추에이션
	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(0) && TFLAG:안빼고
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;지문은 분기 하지 않습니다만, ACT파생자체는 ACT95, 정상위시킨다와 같게 정의되고 있습니다
	;필요에 응해 확장해 주세요
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT312, 이중이라마치오
;────────────────────────────────────
;·죠교자와조수의 페니스에죠교대상가봉사 하는플레이
;·이 단계에서는 명령만
;·ACT51, 입으로 애무를 강요로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K29_312
;-------------------------------------------------
;·v1. 010 현재 전용의 지문이 없기 때문에, 파생원의 지문이 표시된다. 내용은 이하와 같다
;  %CALLNAME:TARGET%는 %CALLNAME:MASTER%에 자신의남성기를입로 잡아당기라고 명령했다…
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT312, 이중이라마치오
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;죠교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
