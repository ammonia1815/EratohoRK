;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	에이린구상 애무계 액션구상파일
;
;	애무계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;애무계 ACT10～19 & 27
;========================================================================
;·TFLAG:ACT(10=손으로 애무/11=가슴애무/12=입으로 애무/13=항문애무/14=항문펠라/15=키스/16=성기마찰한다/17=파이즈리한다/18=풋잡하기/19=손가락삽입한다/27=밀어 넘어트린다)
;·TFLAG:ACT분류 == 1에 해당
;·ACT18, 발코키는이상계로부터 파생하는 경우 있어
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "애무")로 줍는 것이 가능
;·죠교자가죠교대상를애무 한다. 이구상의 시점에서죠교자는 벌써손를 내고 있다
;·미약를 사용하는 케이스가 있다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT10, 손으로 애무(ACT10_0, 애무 하는/ACT10_1, 수음 하는/ACT10_2, 페니스밴드수음)
;────────────────────────────────────
;·Ｃ중심으로애무 한다
;·죠교대상에페니스가 있는 경우, 및페니스밴드장착시에는 파생 커맨드가 된다
;·죠교대상가자위중의 경우는 해제된다
;·죠교자의손에 더러움이 있는 경우불쾌SOURCE가 변동한다
;·죠교자기교, 애무, 손가락기술소질에 응해발기도상승
;-------------------------------------------------
@KOJO_ACT_K29_10
;-------------------------------------------------
;·TFLAG:ACT파생(0=애무/1=수음/2=페니스밴드수음)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_1, 수음 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·남성기에의애무를 실시한다, 즉손코키
;·다른 파생과 비교해 SOURCE:MASTER:쾌Ｃ는+200%
;·죠교자기교, 애무, 손가락기술소질에 응해발기도상승
;-------------------------------------------------
;필요에 응해, TEQUIP:오나홀나 TCVAR:MASTER:발기도
;PANTSCHECK(1) 등으로 분기를 마련하는 일도 할 수 있습니다.
;온전히 하면손간이 심상치 않아서, 효율 좋게 기술하는 일을 추천합니다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 1
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
;OriginalString : 「あら、何？ 他人の손に触られるのがそんなに不思議？」
		PRINTFORMW 「어머나, 무엇? 타인의 손에 손대어지는 것이 그렇게 불가사의?」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크() && ONCEACT(0)
		;하반신복장이 있는 경우
		IF PANTSCHECK(1)
			IF TCVAR:MASTER:발기도 >= 1000
;OriginalString : 「布の下で窮屈そうね。ずいぶんと안쓰러운じゃない？」
				PRINTFORMW 「옷감아래에서 거북함 그렇구나. 대단히 야무지지 못하지 않다?」
			ELSE
;OriginalString : 「ほらほら、服の上からでも容赦しないわよ」
				PRINTFORMW 「이봐요 이봐요, 옷 위로부터에서도 용서하지 않아요」
			ENDIF
		;그 외
		ELSE
			IF TCVAR:MASTER:발기도 >= 1000
;OriginalString : 「もうこんなに膨らませちゃって……何がそんなに興奮했다の？」
				PRINTFORMW 「이제 이렇게 부풀려 버려서…… 무엇이 그렇게 흥분했어?」
			ELSE
;OriginalString : 「弱々しいわね。さっさと勃たせなさい」
				PRINTFORMW 「허약하네요. 빨리 발 더할 수 있는 없음 있고」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가 갈 것 같다
	;=================================================
	IF 구상기입체크() && TCVAR:MASTER:가버려
		;-------------------------------------------------
		;전부터애무
		;-------------------------------------------------
		;IF 구상기입 체크(0) && TCVAR:MASTER:위치전후 == 1
		IF 구상기입체크()
			;애태움도에 의해 분기
			SELECTCASE TCVAR:MASTER:애태움도
				;애태움중
				CASE IS > 1
					SELECTCASE RANDACT(3, 0)
						CASE 0
;OriginalString : 「イキそうなのは分かるわ。だけど……」
							PRINTFORMW 「이키 그런 것은 알아요. 그렇지만……」
;OriginalString : 「そう簡単に사정させてあげると思う？」
							PRINTFORMW 「그렇게 간단하게 사정시켜 준다고 생각한다?」
						CASE 1
;OriginalString : 「そんな
							PRINTFORM 「그런
							IF TCVAR:MASTER:위치전후 == 2
;OriginalString : 声出しても
								PRINTFORM 소리 내도
							ELSE
;OriginalString : 顔しても
								PRINTFORM 얼굴 해도
							ENDIF
;OriginalString : ダメなものはだーめ」
							PRINTFORMW 안된 것은이다-째」
;OriginalString : 「どうしても下品に사정했다いです、っていうのなら、もっと徹底的に媚びてみなさい」
							PRINTFORMW 「아무래도 천하게 사정하고 싶습니다, 라고 한다면, 좀 더 철저하게 아첨해 보세요」
						CASE 2
;OriginalString : 「その
							PRINTFORM 「그
							IF TCVAR:MASTER:위치전후 == 2
;OriginalString : 必死そうな腰の動き
								PRINTFORM 필사적일 것 같은 허리의 움직임
							ELSE
;OriginalString : 必死な表情
								PRINTFORM 필사적인 표정
							ENDIF
;OriginalString : 、たまらないわ」
							PRINTFORMW , 견딜 수 없어요」
;OriginalString : 「ふふ、私はそういうのが見たいのよ」
							PRINTFORMW 「후후, 나는 그렇게 말하는 것을 보고 싶어」
					ENDSELECT
				;애태움종료
				CASE 1
					SELECTCASE RANDACT(3, 1)
						CASE 0
;OriginalString : 「참다出来たわね、えらいえらい」
							PRINTFORMW 「인내 할 수 있던 원이군요, 대단한 대단하다」
;OriginalString : 「いいわよ、ほら、思うがままに사정しなさい」
							PRINTFORMW 「좋아요, 이봐요, 마음껏 사정해 주세요」
						CASE 1
;OriginalString : 「もう限界？ しょうがないわねぇ」
							PRINTFORMW 「이제 한계? 어쩔 수 없네요」
							IF TEQUIP:오나홀
;OriginalString : 「この筒の中で虚しく사정することね」
								PRINTFORMW 「이 통 중(안)에서 허무하게 사정하는 것」
							ELSE
;OriginalString : 「出しなさい……私の손の中に」
								PRINTFORMW 「내세요……나의 손안에」
							ENDIF
						CASE 2
;OriginalString : 「あらあら……ふふふ」
							PRINTFORMW 「대강……후후후」
;OriginalString : 「もう、堪え性の無いケダモノなんだから」
							PRINTFORMW 「 이제, 인내력이 없는 케다모노니까」
					ENDSELECT
				;애태움없음
				CASEELSE
					SELECTCASE RANDACT(2, 2)
						CASE 0
							IF TEQUIP:오나홀
;OriginalString : 「こんな下劣な物で達しそうだなんて、ずいぶんと低俗だこと」
								PRINTFORMW 「이런 비열한 것으로 달할 것 같다 라고, 대단히 저속하다 일」
;OriginalString : 「その低俗で劣等な子種、この中にたくさんぶちまけちゃいなさいっ」
								PRINTFORMW 「그 저속하고 열등한 자식, 이 안에 많이 털어 놓아버릴 없음 있고」
							ELSE
;OriginalString : 「出すならさっさと出しなさい」
								PRINTFORMW 「낸다면 빨리 내세요」
							ENDIF
						CASE 1
;OriginalString : 에이린は震える당신의 페니스를 作業的に擦り上げている……
							PRINTFORMDW 영림은 떨리는 당신의 페니스를 작업적으로 켜고 있다……
					ENDSELECT
			ENDSELECT
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;뒤로부터애무
		;-------------------------------------------------
		;IF 구상기입 체크(0) && TCVAR:MASTER:위치전후 == 2
		;	;애태움도에 의해 분기
		;	SELECTCASE TCVAR:MASTER:애태움도
		;		;애태움중
		;		CASE IS > 1
		;			PRINTFORMW 
		;		;애태움종료
		;		CASE 1
		;			PRINTFORMW 
		;		;애태움없음
		;		CASEELSE
		;			PRINTFORMW 
		;	ENDSELECT
		;	RETURN 1
		;ENDIF
		
		;위치전후분기를 묻는다면필요없음. 묻지 않으면 여기서 보완시킵시다
		;불필요한 분기는 삭제, 혹은 comment out 하는 것으로 부하는 가벼워집니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;애태움도에 의해 분기
			SELECTCASE TCVAR:MASTER:애태움도
				;애태움중
				CASE IS > 1
;OriginalString : 
					PRINTFORMW 
				;애태움종료
				CASE 1
;OriginalString : 
					PRINTFORMW 
				;애태움없음
				CASEELSE
;OriginalString : 
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마중
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
			;전부터애무
			IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 
				PRINTFORMW 
			;뒤로부터애무
			ELSE
;OriginalString : 
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;전부터애무
			IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 
				PRINTFORMW 
			;뒤로부터애무
			ELSE
;OriginalString : 
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
		;전부터애무
		IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 「당신には손でも勿体無いくらいよ」
			PRINTFORMW 「당신에게는 손에서도 과분할 정도 」
		;뒤로부터애무
		ELSE
;OriginalString : 「これじゃ後ろが無防備ね……쿠쿡」
			PRINTFORMW 「이래서야 뒤가 무방비군요……킥킥」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;ACT10_1, 수음 한다

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_2, 페니스밴드수음
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상가페니스밴드장착시에 발생
;·쾌ＣSOURCE적으로는 TFLAG:ACT파생 == 1인가 아닌가로 밖에 보지 않은 같기 때문에, 사실상수음는 아니고애무와 동등?
;·쾌Ｖ도 보지 않는 것 같은 느낌. 그리고 부위의 더러움도 참조 할 수 없는 것이 아닐까와
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 2
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(1)
		;하반신복장이 있는 경우
		IF PANTSCHECK(1)
;OriginalString : 
			PRINTFORMW 
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마중
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
			;전부터애무
			IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 
				PRINTFORMW 
			;뒤로부터애무
			ELSE
;OriginalString : 
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;전부터애무
			IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 
				PRINTFORMW 
			;뒤로부터애무
			ELSE
;OriginalString : 
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
		;전부터애무
		IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 「本物の癖して、偽物よりも醜いのね、당신のは」
			PRINTFORMW 「진짜의 버릇 해, 가짜보다 추악하구나, 당신의 것은」
		;뒤로부터애무
		ELSE
;OriginalString : 「あらあら、お尻が少し寂しそうじゃない？」
			PRINTFORMW 「대강, 엉덩이가 조금 외로운 것 같지 않다?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;ACT10_2, 페니스밴드수음

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_0, 애무 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·여자성기에의애무를 실시한다
;·SOURCE적으로는쾌Ｂ와쾌Ｃ는 동등
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크() && FIRSTACT(2)
;OriginalString : 「何を驚いてるの？」
		PRINTFORMW 「무엇을 놀라?」
;OriginalString : 「自分で했다こと無い なんて言うんじゃないでしょうね？」
		PRINTFORMW 「스스로 한 것 없다 뭐라고 말하지는 않겠지요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크() && ONCEACT(2)
		;하반신복장이 요점 밀치고
		IF PANTSCHECK(1, 1)
			IF 0
			;------------------------------------------------------------------------
			;LOWERJACKET→옷을 걷어 올리고 취급/PANTSCHECK(1)→옷 넘어 취급.
			;상정에 맞은 (분)편 것을 사용하든지, ELSEIF PANTSCHECK(1)를 상층에 마련하든지
			;
			;하반신복장이 요점 밀치고
			;IF PANTSCHECK(1, 1)
			;	;하반신복장이 방해
			;	IF PANTSCHECK(1)
			;	;그 외（하반신에 착의에서도, 두등했다, 걷고 가 가능의 경우）
			;	ELSE
			;		;하반신겉옷가 요점 밀치고
			;		IF LOWERJACKET(1) == 1
			;		;하반신속옷가 요점 밀치고
			;		ELSEIF LOWERUNDER(1) == 1
			;		ENDIF
			;	ENDIF
			;그 외
			;ELSE
			;ENDIF
			;
			;ACT:10:애무로하반신복장의 분기를 마련한다면 기본적인 사고방식은 이러한 느낌으로
			;그리고, 타인에게도 읽혀지는 일을 의식해, 코멘트는 극력 흘리지 않고 써 두어 받고 싶다@/L
			;
			;------------------------------------------------------------------------
			;하반신겉옷가 요점 밀치고
			ELSEIF LOWERJACKET(1) == 1
				;죠교대상가후타나리
				IF TALENT:MASTER:후타나리
;OriginalString : 「もぞもぞしてても、触るのはそっちじゃないのよ」
					PRINTFORMW 「스멀스멀 해도, 손대는 것은 그쪽은 아닌거예요 」
				;그 외
				ELSE
;OriginalString : 「大事な服がまたぐっちょり汚れちゃうのかしらねぇ」
					PRINTFORMW 「소중한 옷이 넘는 더러워져버리는 의 것인지 해들」
				ENDIF
			;하반신속옷가 요점 밀치고
			ELSEIF LOWERUNDER(1) == 1
				;죠교대상복장에 의한 파생
				SELECTCASE TEQUIP:MASTER:하반신속옷
					;드로와
					CASE 1
;OriginalString : 「드로와즈ってふわふわで、お股も変な感じにならない？」
						PRINTFORMW 「드로워즈는 둥실둥실로, 가랑이도 이상한 기분이 들지 않는다?」
					;팬티 및 팬티스타킹		병렬시킨다경우는 CASE X, Y, Z로 좋다. 캐릭터 라인이라면 CASE "HOGE", "HAGE", "PUGE"
					CASE 2, 4
;OriginalString : 「ぴっちりと속옷に形が浮かんじゃって……面白い画よね」
						PRINTFORMW 「딱속옷에 형태가 떠올라 버려서……재미있는 획이군요」
				ENDSELECT
			;그 외
			;ELSE
			;	RETURN 0
			ENDIF
		;그 외
		ELSE
			;죠교대상가후타나리
			IF TALENT:MASTER:후타나리
;OriginalString : 「ふふ、たまにはこっちも可愛がってあげないとね」
				PRINTFORMW 「후후, 가끔 씩은 여기도 귀여워해 주지 않으면」
			;그 외
			ELSE
;OriginalString : 「力を抜きなさい」
				PRINTFORMW 「힘을 빼세요」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:가버려
		;-------------------------------------------------
		;전부터애무
		;-------------------------------------------------
		IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 1
			;애태움도에 의해 분기
			SELECTCASE TCVAR:MASTER:애태움도
				;애태움중
				CASE IS > 1
;OriginalString : 
					PRINTFORMW 
				;애태움종료
				CASE 1
;OriginalString : 
					PRINTFORMW 
				;애태움없음
				CASEELSE
;OriginalString : 
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;뒤로부터애무
		;-------------------------------------------------
		IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 2
			;애태움도에 의해 분기
			SELECTCASE TCVAR:MASTER:애태움도
				;애태움중
				CASE IS > 1
;OriginalString : 
					PRINTFORMW 
				;애태움종료
				CASE 1
;OriginalString : 
					PRINTFORMW 
				;애태움없음
				CASEELSE
;OriginalString : 
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;위치전후분기를 묻는다면필요없음. 묻지 않으면 여기서 보완시킵시다
		;불필요한 분기는 삭제, 혹은 comment out 하는 것으로 부하는 가벼워집니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;애태움도에 의해 분기
			SELECTCASE TCVAR:MASTER:애태움도
				;애태움중
				CASE IS > 1
;OriginalString : 
					PRINTFORMW 
				;애태움종료
				CASE 1
;OriginalString : 
					PRINTFORMW 
				;애태움없음
				CASEELSE
;OriginalString : 
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마중
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
			;전부터애무
			IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 
				PRINTFORMW 
			;뒤로부터애무
			ELSE
;OriginalString : 
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;전부터애무
			IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 
				PRINTFORMW 
			;뒤로부터애무
			ELSE
;OriginalString : 
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
		;전부터애무
		IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 「目を逸らしちゃダメ」
			PRINTFORMW 「눈을 일등 하면 안 됨」
		;뒤로부터애무
		ELSE
			IF TALENT:MASTER:장신
				IF TALENT:MASTER:각선미
;OriginalString : 「당신ってスタイルは良いけど……
					PRINTFORM 「당신은 스타일은 좋은데……
					IF TALENT:MASTER:빈유
;OriginalString : ずいぶんと가슴は控えめなのね、ふふっ」
						PRINTFORMW 대단히 가슴은 소극적인, 후후」
					ELSE
;OriginalString : まぁ私には劣るわね」
						PRINTFORMW 아무튼 나에게는 뒤떨어져요」
					ENDIF
				ELSE
;OriginalString : 「広い背中……여자らしくない体ね、くふっ」
					PRINTFORMW 「넓은 등……여자 아이답지 않은 몸이군요, 」
				ENDIF
			ELSEIF TALENT:MASTER:작은체형
;OriginalString : 「私の腕の中に収まっちゃうような……発育の悪い体ね」
				PRINTFORMW 「 나의 팔안에 들어가버릴 같은……발육이 나쁜 몸이군요」
			ELSE
;OriginalString : 「見えないところから責められるっていうのは、また違った感じかしら？」
				PRINTFORMW 「안보이는 곳으로부터 꾸짖을 수 있다 라고 하는 것은, 또 다른 느낌 까?」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;ACT10_0, 애무 한다

;────────────────────────────────────
;ACT11, 가슴애무
;────────────────────────────────────
;·가슴애무/유두 들이마셔/젖의 몸싸움/유두 고문
;·특정의 조건으로 「유두 들이마셔」 「젖의 몸싸움」 「유두 고문」에 파생한다
;·죠교대상이남자의 경우는 반드시 「유두 고문」(이)가 되어, 그 밖에 파생하는 일은 없다
;-------------------------------------------------
@KOJO_ACT_K29_11
;-------------------------------------------------
;·TFLAG:ACT파생(0=가슴애무/1=유두 들이마셔/2=몸싸움/3=유두 고문)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_0, 가슴애무
;━━━━━━━━━━━━━━━━━━━━━━━━
;·모든 파생 조건으로부터 빠지면통상의가슴애무가 된다
;·죠교자의욕망+Ｂ감각가 3이상의 경우 1/2의 확률로 몸싸움에 파생
;·그리고 새었을 경우에게만, 통상의가슴애무가 된다
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	;전부터애무
	IF TCVAR:MASTER:위치전후 == 1
		;작은체형이며레이센가 아닌 경우 3분의 1으로 표시
		IF TALENT:MASTER:작은체형 && NO:MASTER != 28 && !RAND:3
;OriginalString : 「ずいぶんと綺麗で柔らかい肌ね……ウドンゲじゃ、こうはいかないわ」
			PRINTFORMW 「대단히 깨끗하고 부드러운 피부군요……우돈게는, 후배일까 있고 원」
			RETURN 1
		ENDIF
		IF TALENT:MASTER:작은체형
			IF TALENT:MASTER:거유
;OriginalString : 「小さい体してずいぶん大きいのね。男の相손をするための体なのかしら？」
				PRINTFORMW 「작은 실행해 대단히 크구나. 남자의 상대를하기 위한 체인가 해들?」
			ELSE
;OriginalString : 「上のほうは自分で弄ったりしないのかしら？」
				PRINTFORMW 「위 쪽은 스스로 만지작거리거나 하지 않는 것일까?」
			ENDIF
		ELSE
;OriginalString : 「こっちは慣れないの？」
			PRINTFORMW 「여기는 익숙해지지 않는거야?」
		ENDIF
	;뒤로부터애무
	ELSE
;OriginalString : 「急に後ろから揉まれたり、そういう体験無いかしら」
		PRINTFORMW 「갑자기 뒤로부터 비비어지거나 그러한 체험 없을까」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_1, 유두 들이마셔
;━━━━━━━━━━━━━━━━━━━━━━━━
;·전회ACT가가슴애무의 경우 유두계가 된다
;·그 때, 죠교자에유치나혀기술가 있으면 3/4, 없으면 1/4으로 유두흡실 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;상반신복장이 요점 밀치고			옷을
	IF BRACHECK(1, 1)
;OriginalString : 
		PRINTFORMW
	;그 외
	ELSE
;OriginalString : 
		PRINTFORMW
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_2, 젖의 몸싸움
;━━━━━━━━━━━━━━━━━━━━━━━━
;·가슴의 몸싸움을 요구한다. 이 시점에서의죠교대상의 반응은 미정
;·유두계의 조건으로부터 빠졌을 경우, 죠교자의욕망+Ｂ감각 3 이상이라면,1/2그리고가슴의 몸싸움이 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;상반신복장이 방해			옷 위로부터
	;=================================================
	IF 구상기입체크(0) && BRACHECK(1)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;상반신겉옷가 요점 밀치고		겉옷를 걸어 인상
	;=================================================
	IF 구상기입체크(0) && UPPERJACKET(1) == 1
		;속옷도 걸었다
		IF UPPERUNDER(1) == 1
;OriginalString : 
			PRINTFORMW 
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;상반신겉옷가 요점 밀치고		속옷의 옷자락으로부터손를 숨기고
	;=================================================
	IF 구상기입체크(0) && UPPERUNDER(1) == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_3, 유두 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
;·전회ACT가가슴애무의 경우 유두계가 된다
;·그 때, 죠교자에유치나혀기술가 있으면 1/4, 없으면 3/4으로 유두 고문이 된다
;·죠교대상이남자의 경우는 유두 꾸짖어 밖에 되지 않는다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 3
	;=================================================
	;죠교대상이남자
	;=================================================
	IF 구상기입체크() && TALENT:MASTER:남자
		;전부터애무
		IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 「変な顔しちゃって、男の癖にそんなに乳首が良いのかしら？」
			PRINTFORMW 「이상한 얼굴 해 버려서, 남자의 버릇에 그렇게 유두가 좋은 것일까?」
		;뒤로부터애무
		ELSE
;OriginalString : 「ふふふ、堪えきれないなら好きなように喘いでもいいのよ？」
			PRINTFORMW 「후후후, 견디지 못할이라면 좋아하는 대로 허덕여도 괜찮아?」
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		;전부터애무
		IF TCVAR:MASTER:위치전후 == 1
;OriginalString : 「」
			PRINTFORMW 「」
		;뒤로부터애무
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT12, 입으로 애무
;────────────────────────────────────
;·쿤닐링구스하는/펠라티오 한다
;·하반신의의상가 남아 있어도 발생하는 일이 있다. 죠교자에 TALENT:벽통과가 없는 경우는 비켜 놓아 실행한다
;·턴방침가"이상"라면 발생하지 않는다
;·TEQUIP(바이브/오나홀/페니스밴드/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교대상에페니스가 있으면, 반드시펠라가 된다
;-------------------------------------------------
@KOJO_ACT_K29_12
;-------------------------------------------------
;·TFLAG:ACT파생(0=쿤닐링구스/1=펠라)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_0, 쿤닐링구스한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;안면승마, 아라이멘도가 부의 경우, TALENT:새드등으로 지문이 분기 하는 경우 있어
	;필요에 응해 확장하면 좋을 것입니다
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:가버려
		;애태움도파생 처리
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1 ;애태움중
;OriginalString : 
				PRINTFORMW 
			;애태움종료
			CASE 1
;OriginalString : 
				PRINTFORMW 
			;애태움없음
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마중
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;연속실행의 네스트를 개방한다면 여기는 반드시 써 둡시다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;LOWERJACKET(1) == 1 「」LOWERUNDER(1) == 1 「비켜 놓아」등으로 확장하는 것도 좋을 것입니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_1, 펠라티오 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;안면승마, 아라이멘도가 부의 경우, TALENT:새드등으로 지문이 분기 하는 경우 있어
	;필요에 응해 확장하면 좋을 것입니다
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:가버려
		;애태움도파생 처리
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
;OriginalString : 
				PRINTFORMW 
			;애태움종료
			CASE 1
;OriginalString : 
				PRINTFORMW 
			;애태움없음
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마중
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;연속실행의 네스트를 개방한다면 여기는 반드시 써 둡시다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;LOWERJACKET(1) == 1 「」LOWERUNDER(1) == 1 「비켜 놓아」등으로 확장하는 것도 좋을 것입니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT13, 항문애무
;────────────────────────────────────
;·항문애무/전립선자극/항문바이브출납
;·항문바이브장비중은, 항문바이브출납에 파생
;·연속실행의 경우, 전립선자극에 파생
;·가버려or쾌Ｃ가 5000이상의 경우, 죠교자의애무스킬에 응해전립선자극에 파생
;·발기도가 낮은 경우, 죠교자의애무스킬에 응해전립선자극에 파생
;·요강부디기의 경우, 전립선자극에 파생
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드/항문비즈)장착중에는 발생하지 않는다
;·성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K29_13
;-------------------------------------------------
;·TFLAG:ACT파생（0=항문애무/1=전립선자극/2=항문바이브출납）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_0, 항문애무
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && !TFLAG:ACT파생
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입체크() && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
		IF TALENT:MASTER:남자
;OriginalString : 「ほらっ、真面目にやらないと前立腺ぶち壊しちゃうわよ？」
			PRINTFORMW 「이봐요, 성실하게 하지 않으면 전립선 박살 내버리는 원이야?」
		ELSEIF TALENT:MASTER:후타나리
;OriginalString : 「お尻の穴触るたびにおちんぽぴくぴくさせちゃって……面白い体ね、당신」
			PRINTFORMW 「엉덩이의 구멍 손댈 때마다 실룩실룩 시켜 버려서……재미있는 몸이군요, 당신」
		ELSE
;OriginalString : 「他人に跨がられてお尻の穴ひくつかせてるの？ 変な딸ね」
			PRINTFORMW 「타인에 걸쳐져 엉덩이의 구멍 비굴한가 키 비쳐? 이상한 아가씨」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;하반신복장이 요점 밀치고
	;=================================================
	IF 구상기입체크(0) && PANTSCHECK(1, 1)
		;하반신겉옷를 걷고
		IF LOWERJACKET(1) == 1
;OriginalString : 
			PRINTFORMW 
		;속옷 너머로
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
		;장면이 한정되지만 일단
		IF EXP:MASTER:Ａ경험 == 0 && TALENT:MASTER:남자
;OriginalString : 「男の人って、お尻を弄られても사정できちゃうのよ。知ってた？」
			PRINTFORMW 「남자는, 엉덩이를 만지작거려져도 사정되어져버린다 것. 알았다?」
			RETURN 1
		ENDIF
		;죠교대상Ａ감각파생		변수의 파생을 본다면 SELECTCASE가 바람직하다고 생각합니다. 개별적으로 조건을 바꾸는 경우라면 IF ELSEIF로 OK@/L
		SELECTCASE ABL:MASTER:Ａ감각
			CASE IS <= 2
				SELECTCASE RANDACT(2, 0)
					CASE 0
;OriginalString : 「ここはまだ慣れてないようね」
						PRINTFORMW 「여기는 아직 익숙해지지 않게」
					CASE 1
;OriginalString : 「締りがきつくて、ウブな感じね」
						PRINTFORMW 「야무진 곳이 힘들어서, 우브인 느낌이군요」
				ENDSELECT
			CASE IS <= 3
				SELECTCASE RANDACT(2, 1)
					CASE 0
;OriginalString : 「ようやっと小慣れてきたようね、ここの穴も」
						PRINTFORMW 「겨우 소익숙해져 온 듯, 여기의 구멍도」
					CASE 1
;OriginalString : 「前よりはすんなり受け入れられるよう되었다んじゃない？」
						PRINTFORMW 「전보다는 순조롭게 받아들여지게 되었지 않는다?」
				ENDSELECT
			CASEELSE
				SELECTCASE RANDACT(2, 2)
					CASE 0
;OriginalString : 「あらあら、すっかりゆるゆるじゃない……ふふふ」
						PRINTFORMW 「대강, 가 아니다……후후후」
					CASE 1
;OriginalString : 「深いところまでずっぽりと入っちゃうのねぇ、안쓰러운항문ね」
						PRINTFORMW 「깊은 곳까지않고 라고 들어가버린다의 , 야무지지 못한 어널이군요」
				ENDSELECT
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_1, 전립선자극(항문애무)
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상이남자의 경우에만 파생
;후타나리의 전립선은 요도, 즉 질 위에 있다고 해, Ｇ스팟 자극에
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 1
	SELECTCASE RANDACT(2, 3)
		CASE 0
;OriginalString : 「この内側から圧迫される감각がたまらないでしょう」
			PRINTFORMW 「이 안쪽으로부터 압박받는 감각이 견딜 수 없을 것입니다」
		CASE 1
;OriginalString : 「おちんぽがびくびくしてるわよ、ふふ」
			PRINTFORMW 「가 벌벌 하고 있는 원이야, 후후」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_2, 항문바이브출납
;━━━━━━━━━━━━━━━━━━━━━━━━
;·항문바이브장비중은, 항문바이브출납이 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;죠교대상이남자
	IF TALENT:MASTER:남자
;OriginalString : 
		PRINTFORMW 
	;그 외
	ELSE
;OriginalString : 
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT14, 항문펠라
;────────────────────────────────────
;·항문펠라
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·TEQUIP(항문바이브/항문비즈/관장그릇＋플러그/페니스밴드/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·TCVAR:MASTER:애태움도가 실로 되어 있는 경우는 발생하지 않는다
;·혀에 의한전립선자극에의 파생은 「그런 곳까지 혀가 닿을 이유 없지 않은가www」라고 하는 일로 숙청되고 있습니다
;·현시점에서 ACT가 파생하는 일은 없지만, ACT 개변의 가능성에 대비해, TFLAG:ACT파생 == 0으로 지정
;-------------------------------------------------
@KOJO_ACT_K29_14
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT14_0, 항문펠라
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;죠교자가페니스밴드 몸에 걸치고 있다
		IF TEQUIP:죠교자페니스밴드
;OriginalString : 
			PRINTFORMW
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;하반신복장이 요점 밀치고
	;=================================================
	IF 구상기입체크(0) && PANTSCHECK(1, 1)
		;하반신겉옷를 걷고
		IF LOWERJACKET(1) == 1
;OriginalString : 
			PRINTFORMW 
		;속옷 너머로
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT15, 키스
;────────────────────────────────────
;·키스/미약키스
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·죠교자에 TALENT:벽통과가 없는 경우, TEQUIP:볼개그장착중은 튕겨진다
;-------------------------------------------------
@KOJO_ACT_K29_15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15, 키스 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && FIRSTACT(0)
	;=================================================
	;서로 처음
	;=================================================
	IF 구상기입체크(0) && EXP:MASTER:키스경험 == 0 && EXP:TARGET:키스경험 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가 처음
	;=================================================
	IF 구상기입체크(0) && EXP:MASTER:키스경험 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가 처음
	;=================================================
	IF 구상기입체크(0) && EXP:TARGET:키스경험 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;"첫회"의 네스트를 개방해, 각 조건의 분기를방치 한다면 여기서 보완합시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_0, 키스
;━━━━━━━━━━━━━━━━━━━━━━━━
;! GETBIT(TFLAG:미약등사용, 1) 등의 조건을 삭제해! TFLAG:ACT파생로 정리해 버려도 좋다고 생각합니다
;무리하게 모든 개소를 묻는 일은 없기 때문에, 필요에 응해 구분하여 사용해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생 && !GETBIT(TFLAG:미약등사용, 1)
	;=================================================
	;성교중（죠교자삽입）
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위를 파생 처리
		SELECTCASE TEQUIP:성교중
			;정상위					두를 잡아 정열적인키스를했다…
			CASE 1
;OriginalString : 
				PRINTFORMW 
			;후배위					뒤돌아 보게 한입술을 빼았았다…
			CASE 2
;OriginalString : 
				PRINTFORMW 
			;기승위					하로부터 밀어올리면서 정열적으로입 짓고했다…
			CASE 3
;OriginalString : 
				PRINTFORMW 
			;대면좌위				꽉 껴안으면 그대로입술을 빼았았다…
			CASE 4
;OriginalString : 
				PRINTFORMW 
			;배면좌위				뒤돌아 보게 한 그대로입술을 빼았았다…
			CASE 5
;OriginalString : 
				PRINTFORMW 
			;항문섹스			엉덩이를 범하면서키스 해 왔다…
			CASE 6
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중（죠교대상삽입）
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;애태움도에 의한 파생			TCVAR:MASTER:가버려로 둘러싸지는 않기 때문에, CASEELSE는통상의상태를 의미한다
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
;OriginalString : 
				PRINTFORMW 
			;애태움종료
			CASE 1
;OriginalString : 
				PRINTFORMW 
			;애태움없음
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_1, 미약키스
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상에입옮기기로미약를 먹인다
;-------------------------------------------------
;RESULT치를 2로서@KOJO_USE_KX의미약등사용구상를 호출하지 않는 설정을 디폴트로 해둡니다
;필요에 응해 반환값을 설정해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생 && GETBIT(TFLAG:미약등사용, 1)
	;미약사용구상처리필 체크(미약사용의 지문 컷 할 때는 TFLAG:미약등사용지문스킵의 코멘트를 해제)
	CLEARBIT TFLAG:미약등사용구상, 1
	;CLEARBIT TFLAG:미약등사용지문스킵, 1
	;=================================================
	;성교중（죠교자삽입）
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위를 파생 처리
		SELECTCASE TEQUIP:성교중
			;정상위					두를 잡아 정열적인키스를했다…
			CASE 1
;OriginalString : 
				PRINTFORMW 
			;후배위					뒤돌아 보게 한입술을 빼았았다…
			CASE 2
;OriginalString : 
				PRINTFORMW 
			;기승위					하로부터 밀어올리면서 정열적으로입 짓고했다…
			CASE 3
;OriginalString : 
				PRINTFORMW 
			;대면좌위				꽉 껴안으면 그대로입술을 빼았았다…
			CASE 4
;OriginalString : 
				PRINTFORMW 
			;배면좌위				뒤돌아 보게 한 그대로입술을 빼았았다…
			CASE 5
;OriginalString : 
				PRINTFORMW 
			;항문섹스			엉덩이를 범하면서키스 해 왔다…
			CASE 6
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 2
	ENDIF
	
	;=================================================
	;성교봉사중（죠교대상삽입）
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;애태움도에 의한 파생			TCVAR:MASTER:가버려로 둘러싸지는 않기 때문에, CASEELSE는통상의상태를 의미한다
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
;OriginalString : 
				PRINTFORMW 
			;애태움종료
			CASE 1
;OriginalString : 
				PRINTFORMW 
			;애태움없음
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 2
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 2
	ENDIF
ENDIF

;────────────────────────────────────
;ACT16, 성기마찰한다
;────────────────────────────────────
;·짝 맞추기 놀이/스마타 한다
;·죠교대상에페니스가 있는 경우는 ACT파생 == 1 「스마타 한다」에 파생한다
;·죠교대상가페니스밴드장착중은, ACT파생 == 2 「페니스밴드스마타」가 된다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·죠교대상가여자로바이브장착중이라면 발생하지 않는다
;·TEQUIP(오나홀/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교자에페니스가 있으면 발생하지 않는다（봉사계의스마타강제가 된다）
;-------------------------------------------------
@KOJO_ACT_K29_16
;-------------------------------------------------
;·TFLAG:ACT파생（=0, 조개 우려라/=1, 스마타/=2, 페니스밴드스마타）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_0, 짝 맞추기 놀이
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:가버려
		;애태움도파생
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
;OriginalString : 
				PRINTFORMW 
			;애태움종료
			CASE 1
;OriginalString : 
				PRINTFORMW 
			;애태움없음
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;누움
		IF TFLAG:누움
;OriginalString : 
			PRINTFORMW
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;지문은죠교대상의하반신복장 마다의 분기 있어. 필요에 응해 확장해 주세요
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		;누움
		IF TFLAG:누움
;OriginalString : 
			PRINTFORMW
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_1, 스마타 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·여자성기와 넓적다리로남성기를 사이에 두어애무 한다
;·죠교대상에페니스가 있으면, 반드시스마타 하는게 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:가버려
		;애태움도파생
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
;OriginalString : 
				PRINTFORMW 
			;애태움종료
			CASE 1
;OriginalString : 
				PRINTFORMW 
			;애태움없음
			CASEELSE
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;누움
		IF TFLAG:누움
;OriginalString : 
			PRINTFORMW
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;지문은죠교대상의하반신복장 마다의 분기 있어. 필요에 응해 확장해 주세요
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		;누움
		IF TFLAG:누움
;OriginalString : 
			PRINTFORMW
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_2, 페니스밴드스마타
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상가페니스밴드장착중은, 페니스밴드스마타가 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;지문은죠교대상의하반신복장 마다의 분기 있어. 필요에 응해 확장해 주세요
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		;누움
		IF TFLAG:누움
;OriginalString : 
			PRINTFORMW
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT17, 파이즈리한다
;────────────────────────────────────
;·TFLAG:징계·플래그가 끊고 있으면 발생하지 않는다
;·턴방침가"하드", "이상"의 경우는 발생하지 않는다
;·TEQUIP(오나홀/삼각목마)장착중에는 발생하지 않는다
;·죠교대상에페니스가 없는 경우는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·현재 ACT파생는 존재하지 않지만 장래의 개변에 대비해, ACT파생 == 0으로 한다
;-------------------------------------------------
@KOJO_ACT_K29_17
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT17_0, 파이즈리한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && !TFLAG:ACT파생
	;지문은죠교자의가슴의 사이즈로 분기 합니다.
	;소질을 변경하는 것이 가능한 것으로, 필요에 응해 확장하는 것도 좋을 것입니다
	;=================================================
	;죠교대상가 갈 것 같다
	;=================================================
	IF 구상기입체크() && TCVAR:MASTER:가버려
		;애태움도에 의해 분기
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
;OriginalString : 「まだ出しちゃだめよ、ふふふ」
				PRINTFORMW 「 아직 내면 안 되요, 후후후」
			;애태움종료
			CASE 1
;OriginalString : 「限界？ ふふ、イッちゃえ」
				PRINTFORMW 「한계? 후후, 익네」
			;애태움없음
			CASEELSE
;OriginalString : 「何？ おっぱいの中がそんなに良いの？」
				PRINTFORMW 「무엇? 젖가슴안이 그렇게 좋은거야?」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크() && TFLAG:ACT == TFLAG:전ACT
		IF GET_STAIN("가슴", "정액", MASTER) 
;OriginalString : 「おちんぽに정액擦り込まれる気分はどう？」
			PRINTFORMW 「에 정액 문질러 발라지는 기분은 어때?」
		ELSE
;OriginalString : 「ほらほら、たまらないでしょ。人様の가슴で扱かれるのは」
			PRINTFORMW 「이봐요 이봐요, 견딜 수 없지요. 남의 가슴으로 잡아당겨지는 것은」
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
		[SKIPSTART]
		;---------------------------------------------------
		;지문에는 착의의상태에 의한 분기가 존재합니다만, 나누어 쓰고도 귀찮은 것로필요에 응해 comment out를 제외하든지
		;바라는 형태에 개변하든지 해 사용해 주세요
		;---------------------------------------------------
		;간섭하는 의복이 존재할 때
		IF TRUECHECK(TEQUIP:상반신속옷, TEQUIP:상반신겉옷, TEQUIP:전신겉옷) > 0 || (TEQUIP:MASTER:하반신속옷 && ! TALENT:벽통과) || CFLAG:의상 == 3
			;노출상황에 의해 분기
			SELECTCASE TEQUIP:착의노출 & 6
				;간섭 개소전노출시
				CASE 6
;OriginalString : 
					PRINTFORMW 
				;죠교대상성기노출시
				CASE 4
;OriginalString : 
					PRINTFORMW 
				;죠교자가슴노출시
				CASE 2
;OriginalString : 
					PRINTFORMW 
				;간섭 개소비노출시
				CASEELSE
;OriginalString : 
					PRINTFORMW 
			ENDSELECT
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		[SKIPEND]
		;그 밖에 분기를 마련하지 않는다면, 여기는 써 둡시다
		;분기를 마련한다면 여기의 PRINT는 공행을 낳으므로 삭제합시다
;OriginalString : 「私の가슴の中で耐えられるかしら？」
		PRINTFORMW 「나의 가슴 중(안)에서 견딜 수 있는 것일까?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT18, 풋잡하기
;────────────────────────────────────
;·풋잡하기
;·죠교대상에페니스가 없는 경우는 발생하지 않는다
;·이상계로부터 파생하는 경우도 있다
;·TFLAG:징계·플래그가 끊고 있으면 발생하지 않는다
;·금일의방침가"노닥노닥"라면 발생하지 않는다
;·TEQUIP(오나홀/삼각목마)장착중은 발생하지 않는다
;·성교중/성교봉사중는 발생하지 않는다
;·죠교자에발가 없으면 발생하지 않는다
;·죠교대상가여자라면 발생하지 않는다
;·현재 ACT파생는 존재하지 않지만 장래의 개변에 대비해, ACT파생 == 0으로 한다
;·에이린라면 굉장히 빈번하게 선택된다
;-------------------------------------------------
@KOJO_ACT_K29_18
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT18, 풋잡하기
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && !TFLAG:ACT파생
	;지문에 TCVAR:MASTER:애태움도등의 분기는 존재하지 않는 개소
	;=================================================
	;죠교대상가 갈 것 같다				양발로 %CALLNAME:MASTER%의 페니스를 몹시 거칠게 훑어 왔다…
	;=================================================
	IF 구상기입체크() && TCVAR:MASTER:가버려
		SELECTCASE RANDACT(2, 0)
			CASE 0
;OriginalString : 「ほらっ、汚い정액で私の발を汚してみなさいっ！」
				PRINTFORMW 「이봐요, 더러운 정액으로 나의 다리를 더럽혀 보세요!」
			CASE 1
;OriginalString : 「その、イッちゃいそうな顔、すごく下品でゾクゾクするわ」
				PRINTFORMW 「그, 익있을 것 같은 얼굴, 몹시 천해 두근두근 해요」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;발코키는 작자의 취미는 아닌 것 같아, 지문은 비교적손빼기였거나
	;반대로 말하면, 다양하게 해도 지문과의 정합성이 무너질 걱정은 그다지 없기 때문에, 좋을대로 하면 좋을 것입니다
	;=================================================
	;연속실행						유쾌한 것 같게 %CALLNAME:MASTER%를 미시타하면서페니스를 동글동글 와짓밟아라고 있다…
	;=================================================
	IF 구상기입체크() && TFLAG:ACT == TFLAG:전ACT
		IF TCVAR:MASTER:발기도 >= 1000
;OriginalString : 「ほーらほら、발に敷かれて先走り垂らしてるなんてとんだ変態ね」
			PRINTFORMW 「오-들 이봐요, 다리에 깔려 앞질러 해 늘어뜨리고 있는 터무니 없는 변태군요」
		ELSEIF TALENT:MASTER:후타나리
;OriginalString : 「下のほうもずいぶん過剰にひくつかせちゃってるのね、この음란め」
			PRINTFORMW 「아래 쪽도 대단히 과잉에 끄는 개궔궧비치군요, 이 음란한」
		ELSE
;OriginalString : 「今の私ならいつでも당신のこれを潰せるってこと、気づいてた？」
			PRINTFORMW 「지금의 나라면 언제라도 당신의 이것을 잡을 수 있다는 것, 눈치챘다?」
		ENDIF
		RETURN 1
	ENDIF
	
	;지문에 파생은 마련해 있지 않습니다만, 이것은 비교적필요라고 생각되므로
	;=================================================
	;신발안리 있고하며 발코키(디폴트로신발안 신지 않은 캐릭터는 표시되지 않는다)
	;=================================================
	IF 구상기입체크() && TEQUIP:신발안
;OriginalString : 「私の양말が汚れちゃうけど……양말に욕정する당신を見れるならどうってことないわ」
		PRINTFORMW 「 나의 삭스가 더러워져버린다지만……삭스에 욕정하는 당신을 볼 수 있다면 어떻게는 일 없어요」
		RETURN 1
	ENDIF
	
	;죠교자에 TALENT:벽통과가 없는 경우, 천 너머로 된다
	;=================================================
	;하반신복장이 방해
	;=================================================
	IF 구상기입체크() && PANTSCHECK(1)
;OriginalString : 「もぞもぞしちゃって、芋虫みたい」
		PRINTFORMW 「스멀스멀 해 버려서, 고구마벌레 보고 싶다」
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크()
		SELECTCASE RANDACT(2, 1)
			CASE 0
;OriginalString : 「당신なんか발で十分よ」
				PRINTFORMW 「당신 같은 것 다리로 충분해요」
			CASE 1
;OriginalString : 「私の발で扱いてもらえるのよ。光栄に思いなさい」
				PRINTFORMW 「 나의 다리로 취급해 받을 수 있어. 영광으로 생각하세요」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT19, 손가락삽입한다(ACT19_0, 손가락삽입한다/ACT19_1, Ｇ스팟 자극/ACT19_2, 바이브출납)
;────────────────────────────────────
;·죠교대상가후타나리 혹은 여자의 경우에 발생
;·죠교대상가처녀의 경우Ｇ스팟 자극등에 파생하지 않는＆쾌Ｖ에 마이나스보정
;·바이브장비중은바이브출납에 파생
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교대상이남자라면 발생하지 않는다
;·죠교대상가후타나리의 경우, 애태움중에는 발생하지 않는다
;-------------------------------------------------
;※죠교대상가처녀는 아니다
;※연속실행의 경우
;※가버려or쾌Ｃ가 5000이상의 경우, 죠교자의애무스킬에 응해
;※발기도가 낮은 경우, 죠교자의애무스킬에 응해
;※요강부디기의 경우
;·상기 조건의 경우Ｇ스팟 자극에 파생한다
;-------------------------------------------------
@KOJO_ACT_K29_19
;-------------------------------------------------
;·TFLAG:ACT파생(0=손가락삽입한다/1=Ｇ스팟 자극/2=바이브출납)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_0, 손가락삽입한다					질입에 손가락끝을 주어, 살그머니 안쪽에 밀어넣었다…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;위치전후분기는필요에 응해
	;어디선가 통합해 버리는 것도 좋다고 생각합니다
	;=================================================
	;뒤로부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 「」
		PRINTFORMW 「」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_1, Ｇ스팟 자극					손가락을 진입시키고Ｇ스팟을 직접 자극했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;가버려or쾌Ｃ가 5000이상의 경우, 죠교자의애무스킬에 응해Ｇ스팟 자극
;Ｇ스팟 고문에 파생하면전립선자극취급이 된다
;-------------------------------------------------
;죠교대상가후타나리의 경우에서도 발생하므로, 필요에 응해 PENIS(MASTER) 되어! PENIS(MASTER)로 분기를 마련하면 좋다고 생각합니다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;뒤로부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_2, 바이브출납					Ｇ스팟을 후벼파도록(듯이) 격렬하게 동이나했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;지문 자체에 파생은 없습니다만, @TARGET_DESCRIPTION에서의 상황 묘사가 들어갈 가능성이 있습니다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT27, 밀어 넘어트린다
;────────────────────────────────────
;·밀어 넘어트린다/밀어넘어짐종료
;·이미밀어넘어짐라고 있는 경우, 해방가 된다
;·TFLAG:징계·플래그가 끊고 있으면 발생하지 않는다
;·금일의방침가"노닥노닥"라면 발생하지 않는다
;·TEQUIP:페니스밴드장착중에는 발생하지 않는다
;·애태움중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K29_27
;-------------------------------------------------
;TFLAG:ACT파생（=0, 밀어 넘어트린다/=1, 밀어넘어짐종료）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_0, 밀어 넘어트린다			%CALLNAME:MASTER%를 밀기도했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;지문에 파생은 없습니다만, @TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성이 있습니다
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	SELECTCASE RANDACT(3, 0)
		CASE 0
;OriginalString : 「変に暴れないほうが身のためよ」
			PRINTFORMW 「이상하게 날뛰지 않는 편이 몸이기 때문에」
		CASE 1
;OriginalString : 「大人しくしててね」
			PRINTFORMW 「얌전하게 하고 있어」
		CASE 2
;OriginalString : 「これで당신も私の支配下……」
			PRINTFORMW 「이것으로 당신도 나의 지배하……」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_1, 밀어넘어짐종료		%CALLNAME:MASTER%를자유에했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:ACT파생 == 1
;OriginalString : 「ふぅ」
	PRINTFORMW 「」
	RETURN 1
ENDIF
