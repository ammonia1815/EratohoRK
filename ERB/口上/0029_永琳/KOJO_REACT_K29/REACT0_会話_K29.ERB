;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	에이린구상 회화계 개별 리액션구상파일
;
;	회화계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어선택했다COM가 자연스러운행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운행동/1=무리가 없는행동/2=무리를했다행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 지금 기분을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K29_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일했다 있고)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다로, 리액션이빈정거린다 이외의 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 0
	;자연스러운행동
	;=================================================
	;REACT부하 0, 질문에 분명히 답하는 것이 할 수 없었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 말씨 나쁘게 질문에 답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 나가려 한 말을 삼켜 대답을 탁했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「もっとちゃんと答えなさい」
		PRINTFORMW 「 좀 더 분명하게 대답하세요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 1
	;자연스러운행동
	;=================================================
	;REACT부하 0, 어려운 말을 퍼부었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 용기를 내 강하게 맞았다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 눈을 일등 해입에서는 강하게 거절하는마이
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「いい度가슴してるじゃない」
		PRINTFORMW 「좋은 담력 하고 있잖아」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 2
	;자연스러운행동
	;=================================================
	;REACT부하 0, 여유의 미소로 어깨를 움츠리고 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 조금 생각하고 나서 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 간신히 야유 섞임에 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「質問の内容が悪かったかしら？」
		PRINTFORMW 「질문의 내용이 나빴는지 해들?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 3
	;자연스러운행동
	;=================================================
	;REACT부하 0, 함께 있어지는 것이 기쁘면솔직에입에했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 분명히 지금의 기분을 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 기분을 해치지 않게, 손질해 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「気の利いたこと言うじゃない」
		PRINTFORMW 「멋이 있는 일 말하잖아」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 4
	;자연스러운행동
	;=================================================
	;REACT부하 0, 그렇게 나쁘다고 생각하지는 않다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「그저 나쁘지는 않다」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은적극			시선을 피해, 나쁘지는 않아라고 중얼거렸다
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;실은 혐				힘껏의 허풍으로, 그저 나쁘지 않다고 대답했다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「優柔不断ね」
		PRINTFORMW 「우유부단이군요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일했다 있고
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 5
	;자연스러운행동
	;=================================================
	;REACT부하 0, 좀 더 기분 좋아지고 싶다고 했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 기분 좋은 일했다 있고 기분이라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 아첨하도록(듯이) 감히 추잡한 행위를 졸랐다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「自分に正直なのね。悪くはないけど、ここでは短所ね」
		PRINTFORMW 「자신에게 정직한. 나쁘지는 않지만, 여기에서는 단점이군요」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------
[SKIPSTART]
통상의 경우 리액션의 지문은@TRAIN_MESSAGE2와@REACTION_MESSAGE로 처리됩니다만
이하의 부분은@TRAIN_MESSAGE2의 조건으로부터 빠지기 때문에(위해), @REACTION_MESSAGE에서만 처리됩니다
COM 분기에 있어서도 당연히 TFLAG:REACT파생는 기능하고, @REACTION_MESSAGE내에서도 REACT부하 마다 나누어 쓸 수 있습니다
SELECTCASE GET_COM_RANK()
	;자연스러운행동
	CASE "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
	;무리가 없는행동
	CASE "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
	;무리를했다행동
	CASE "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
ENDSELECT
상기와 같은 분기를 마련하는 일에 의해, 본체측의변화에 대응하는 일은 가능합니다만
텐프레에 있어서 디폴트로 마련하려면 손간이 너무 걸린다고 생각하는 까닭에, 기본적으로 REACT내의 COM 분기에는 테두리를 마련하지 않는방침입니다
범용적인 내용으로 해도 좋아, 또 부분적으로 범용 리액션(COM)에 흘려도 좋다고 생각합니다
뭐 REACT부하 이외에도 확장하는 방법은 얼마든지 있거나 하므로, 그 근처의 판단은 각구상작자에게 맡기고 싶다고 생각합니다
3 BM/4 MB에서도 쓰기 때문에 어쨌든 생각되는 요소를 전부 넣어 줘! 적인 요망이 만약 있으면, 그 때는 또 생각하고 싶다와
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;지금 기분을 묻다에서의 COM침묵한다는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어징계·포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「黙ってても状況は変わらないわよ」
	PRINTFORMW 「입다물어도 상황은 변함없어요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계·포인트가산 파생:「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「ダメじゃない。そんな簡単に弱音を吐いちゃ」
		PRINTFORMW 「안되지 않다. 그런 간단하게 약한소리를 합니다」
;OriginalString : 「根性を鍛え直す필요がありそうね」
		PRINTFORMW 「근성을 다시 단련할 필요가 있을 듯 하구나」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「仕方ないわねぇ」
		PRINTFORMW 「어쩔 수 없네요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「そんなに私が怖いのかしら？」
		PRINTFORMW 「그렇게 내가 무서운 것일까?」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「怯えちゃって、かわいい」
		PRINTFORMW 「무서워해 버려서, 귀엽다」
		RETURN 1
	ENDIF
ENDIF
;REACT0, 지금 기분을 묻다

;────────────────────────────────────
;REACT1, 자신에게의 마음을 묻다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은죠교대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K29_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다!/2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 0
	;자연스러운행동
	;=================================================
	;REACT부하 0, 아무래도 싫다고는 말할 수 없었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 본심을 숨기도록(듯이) 눈을 숙여, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「あら、意外なこと言うのね」
		PRINTFORMW 「어머나, 뜻밖의 일 말하군요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 1
	;자연스러운행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「それはそうよね、ふふ」
		PRINTFORMW 「그것은 그래요, 후후」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 2
	;자연스러운행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「余裕そうじゃない。その余裕もいつまで続くかしらね」
		PRINTFORMW 「여유 그렇지 않다. 그 나머지히로시도 언제까지 계속되는 것일까」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 적은지, REACT분류가태연하게 한다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 3
	;자연스러운행동
	;=================================================
	;REACT부하 0, 「좀 더 좋아하게 되고 싶기 때문에, 심한 것은 하지 말아줘」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 「이제(벌써) 심한 일 하지 않으면, 좋아하게 될 수 있을지도 모른다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 기분을 묻도록(듯이) 시선을 향한 후, 「심한 일만 하는 사람은 싫다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「そんなこと言われると少し考えちゃう……なんてね」
		PRINTFORMW 「그런 일 말해지면(자) 조금 생각해버린다……(이)라니」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 죠교대상호의가 1000이상
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 4
	;부하 2는 연주한 (분)편이 뭔가 형편이 좋은 것 같아 우선
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(4) && TFLAG:REACT부하 < 2
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;자연스러운행동
	;=================================================
	;REACT부하 0, 붙임성 좋게 웃어 정말 좋아한다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 분명히 좋아라고 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 억지웃음을 띄워, 좋아해라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「拉致監禁당했다上で好きだなんて、당신も物好きね」
		PRINTFORMW 「납치 감금된 다음 좋아한다 라고, 당신도 호기심이군요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 5
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(5)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;자연스러운행동
	;=================================================
	;REACT부하 0, 붙임성 좋게 웃어 정말 좋아한다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 분명히 좋아라고 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 억지웃음을 띄워, 좋아해라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「あら、面白いこと言うじゃない。自分の言葉には責任を持ってね？」
		PRINTFORMW 「어머나, 재미있는 일 말하잖아. 자신의 말에는 책임을 가져?」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자신에게의 마음을 묻다의 경우는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어징계·포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「答えられない理由でもあるのかしら？」
	PRINTFORMW 「대답할 수 없는 이유라도 있을까?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계·포인트가산 파생:「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「へぇ、許して欲しいんだ」
		PRINTFORMW 「에 , 허락했으면 좋다」
;OriginalString : 「私は懺悔されるような覚えは無いけど、당신には後ろめたいことがあるのね」
		PRINTFORMW 「 나는 참회 되는 것 같은 기억은 없지만, 당신에게는 꺼림칙한 것이 있군요」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「空気の読めない人は好きじゃないわ」
		PRINTFORMW 「공기를 읽을 수 없는 사람은 좋아하지 않아요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「残念ね。怯えられちゃうと私もがっかりだわ」
		PRINTFORMW 「유감이구나. 겁 얻을 수 있어버리자(면) 나라도 빌리고예요」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「相当怖がられてるみたいね、ふふふ」
		PRINTFORMW 「상당히 무서워해지고 있는 것 같구나, 후후후」
		RETURN 1
	ENDIF
ENDIF
;REACT1, 자신에게의 마음을 묻다

;────────────────────────────────────
;REACT2, 성에 관한 이야기를 하다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K29_2
;-------------------------------------------------
;  파생할 가능성이 있는 COM는 우선 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「私は結構真剣よ？」
	PRINTFORMW 「나는 상당히 진지해요?」
;OriginalString : 「それともコミュニケーションは苦손なの？」
	PRINTFORMW 「그렇지 않으면 커뮤니케이션은 서툴러?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「つれないわねぇ、もう」
		PRINTFORMW 「무정하네요 , 이제」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
;OriginalString : 「話題としては不발だったかしら」
		PRINTFORMW 「화제로서는 부족했던 까」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;징계·포인트가산 파생:「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「何よ、せっかく穏やかな気持ちで話題を持ち出してあげたのに」
		PRINTFORMW 「뭐야, 모처럼 온화한 기분으로 화제를 꺼내 주었는데」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「あら、こういうのは苦손？」
		PRINTFORMW 「어머나, 이런 것은 골칫거리?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;징계·포인트가산 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「こういう話で참다できなくなっちゃうのは可愛いけど、すぐに求めちゃうのは減点ね」
		PRINTFORMW 「이런 이야기로 참을 수 없게 되어버린다 것은 사랑스럽지만, 곧바로 요구해버린다 것은 감점이군요」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「あら、興奮しちゃった？」
		PRINTFORMW 「어머나, 흥분해 버렸다?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("부끄러워한다")
	;TEQUIP에 의해 쾌SOURCE가 들어가 있는 경우에 파생
	;=================================================
	;REACT파생 1, 쾌락에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 상황에부끄러워한다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
;OriginalString : 「당신の体のことでもあるのよ。부끄러워한다ことはないわ」
		PRINTFORMW 「당신의 몸으로도 있어. 부끄러워할 것은 없어요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「何を想像했다の？」
		PRINTFORMW 「무엇을 상상했어?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("자위를 시작한다")
	;=================================================
	;REACT파생 1, 「승손에 자위 하지마」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「こらっ、だめじゃない勝손にそんなこと始めちゃ」
		PRINTFORMW 「이거 참, 안되지 않는 마음대로 그런 일 시작합니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「승손에 자위 하고 있지만 좋은가」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
;OriginalString : 「まったく、百聞は一見にしかずなのね、당신は」
		PRINTFORMW 「완전히, 백문은 일견 밖에않고인 것, 당신은」
		RETURN 1
	ENDIF
ENDIF
;REACT2, 성에 관한 이야기를 함

;────────────────────────────────────
;REACT3, 상냥하게 위로한다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K29_3
;-------------------------------------------------
;  파생할 가능성이 있는 COM에는 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
;  우선회화계 COM를 모두 망라되어 있습니다만
;  10 접수대의애무계 COM, 50 접수대 이후의 그 외 COM등도 상황에 따라서는선택 가능하게 됩니다
;  필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「あら、お気に召さなかったかしら？」
	PRINTFORMW 「어머나, 마음에 드시지 않았는지 해들?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「他人の호의はちゃんと受け止めなさい」
		PRINTFORMW 「타인의 호의는 제대로 받아 들이세요」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
;OriginalString : 「弱気なのは簡単には治らないようね」
		PRINTFORMW 「무기력인 것은 간단하게는 낫지 않게」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
;OriginalString : 「先は長くなりそうね」
	PRINTFORMW 「앞은 길어질 것 같구나」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성 있게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("상냥하게 응답한다")
;OriginalString : 「솔직なところは好感が持てるわ」
	PRINTFORMW 「솔직한 곳은 호감을 가질 수 있어요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("강하게 응답한다")
;OriginalString : 「おっと……少し予想外ね」
	PRINTFORMW 「남편……조금 예상외군요」
;OriginalString : 「慰めてあげてるんだから솔직に受け止めたら？」
	PRINTFORMW 「위로해 주고 있기 때문에 솔직하게 받아 들이면?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 「솔직じゃないのね」
	PRINTFORMW 「솔직하지 않아」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「どうしようもない子ね、당신は」
		PRINTFORMW 「어쩔 수 없는 아이, 당신은」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
;OriginalString : 「考えておくわ」
		PRINTFORMW 「생각해 두어요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「何を許して欲しいのかしら？」
		PRINTFORMW 「무엇을 허락했으면 좋은 것일까?」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「よく分からないわね」
		PRINTFORMW 「자주(잘) 모르네요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「もっと違う意味で慰めてもらいたいのね」
		PRINTFORMW 「 좀 더 다른 의미로 위로해 주었으면 하구나」
;OriginalString : 「嫌よ。気持ち悪い」
		PRINTFORMW 「싫어요. 기분 나쁘다」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「입で言われるより행동で示당했다いのね」
		PRINTFORMW 「입으로 말해지는 것보다 행동으로 나타나고 싶구나」
		RETURN 1
	ENDIF
ENDIF
;REACT3, 상냥하게 위로한다

;────────────────────────────────────
;REACT4, 엄하게 위협한다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_REACT_K29_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「ふふふ、何も言えなくなっちゃったかしら」
	PRINTFORMW 「후후후, 굳이 말할 수 없게 되어 버렸다 까」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「もうやめて？ どの입がそれを言ってるのかしら？」
		PRINTFORMW 「이제 그만두어? 어느 입이 그것을 말하고 있는 거나?」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
;OriginalString : 「脅당했다ならそのくらいの反応が妥当ね」
		PRINTFORMW 「위협해졌다면 그 정도의 반응이 타당하구나」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 「あら、まだ당신にそんな余裕が残ってたの？」
	PRINTFORMW 「어머나, 아직 당신에게 그런 여유가 남았어?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(3)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「許してほしいのね。仕方ないわ……」
		PRINTFORMW 「허락하기를 원하구나. 어쩔 수 없어요……」
;OriginalString : 「とでも言うと思った？ そう簡単に許してあげるわけないじゃない」
		PRINTFORMW 「이라고도 말한다고 생각했다? 그렇게 간단하게 허락해 주는 것 없지 않다」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「許してほしいのね。仕方ないわ」
		PRINTFORMW 「허락하기를 원하구나. 어쩔 수 없어요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「あらあら、壊れちゃったかしら？ ふふふ」
		PRINTFORMW 「대강, 망가져 버렸다 까? 후후후」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「いいわ、もっと怯えなさい」
		PRINTFORMW 「좋아요, 좀 더 무서워하세요」
		RETURN 1
	ENDIF
ENDIF
;REACT4, 엄하게 위협한다

;────────────────────────────────────
;REACT5, 의상 변경을 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가"소극적으로 한다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가"적극적으로 한다", "태연하게 한다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_REACT_K29_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:ACT파생 == 0
	;=================================================
	;한 장 벗었다(소극적 반응)
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("소극적으로 한다/받아들인다")
		;-------------------------------------------------
		;마지막 한 장을 벗어전라되었다
		;-------------------------------------------------
		IF 구상기입체크() && (TEQUIP:MASTER:악세서리 + TEQUIP:MASTER:신발안 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
			;REACT분류받아들인다			전라가 되어, 기대와 흥분으로 피부가 달아오르는 것을 느꼈다
			IF IS_COMGRONAME("받아들인다")
;OriginalString : 「あらあら、赤くなっちゃって、可愛いわ」
				PRINTFORMW 「대강, 붉어져 버려서, 사랑스러워요」
			;REACT분류소극적으로 한다			어쩔 수 없이 마지막 한 장을 벗어, 전라되었다
			ELSE
;OriginalString : 「いい体ね」
				PRINTFORMW 「좋은 몸이군요」
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;완만한 움직임으로 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크() && IS_COMGRONAME("받아들인다")
;OriginalString : 「さっさと脱ぎなさい」
			PRINTFORMW 「빨리 벗으세요」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;어쩔 수 없이 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크() && IS_COMGRONAME("소극적으로 한다")
;OriginalString : 「まぁいいわ」
			PRINTFORMW 「아무튼 좋아요」
			RETURN 1
		ENDIF
		
		;상층의 분기를 모두 묻었을 경우는 불필요한 개소
		;반대로 묻지 않는 경우는 이 개소가 보완이 책임을 다합니다
		;-------------------------------------------------
		;기타·조건없음
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;2매 벗었다(적극적 반응)
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;-------------------------------------------------
		;전라되었다
		;-------------------------------------------------
		IF 구상기입체크() && (TEQUIP:MASTER:악세서리 + TEQUIP:MASTER:신발안 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신겉옷 + TEQUIP:MASTER:상반신겉옷 + TEQUIP:MASTER:전신겉옷) == 0
;OriginalString : 「人前でちゃんと脱げる子は손間がかからなくて良いわ」
			PRINTFORMW 「남의 앞에서 분명하게 벗겨지는 아이는 시간이 들지 않아 좋아요」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;적극적으로 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크() && IS_COMGRONAME("적극적으로 한다")
;OriginalString : 「なかなかいいじゃない」
			PRINTFORMW 「좀처럼 좋지 않아」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;손때 좋게 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크() && IS_COMGRONAME("태연하게 한다")
;OriginalString : 「ずいぶんと慣れた感じね」
			PRINTFORMW 「대단히 익숙해진 느낌이군요」
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;벗지 않았다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("거부")
		;부끄러워서 벗겨지지 않았다
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
;OriginalString : 「あら、恥ずかしがり屋さんね」
			PRINTFORMW 「어머나, 부끄럽게 여기기가게군요」
;OriginalString : 「私が脱がしてあげようかしら？」
			PRINTFORMW 「내가 탈이 해 줄까?」
		;싫어해 벗지 않았다
		ELSE
;OriginalString : 「着衣플레이が好きなの？」
			PRINTFORMW 「착의 플레이를 좋아해?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("소극적으로 한다/받아들인다")
;OriginalString : 「愛想悪いわね」
		PRINTFORMW 「붙임성 나쁘네요」
		RETURN 1
	ENDIF
	
	;=================================================
	;2매 입었다(적극적 반응)
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
;OriginalString : 「ほらほら、着た着た」
		PRINTFORMW 「이봐요 이봐요, 입은 입었다」
		RETURN 1
	ENDIF
	
	;지문에서는 "싫어해"라고 하는 취급
	;=================================================
	;입지 않았다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("거부")
;OriginalString : 「着るのが嫌なの？」
		PRINTFORMW 「입는 것이 싫어?」
		RETURN 1
	ENDIF
ENDIF
;REACT5, 의상 변경을 요구한다

;────────────────────────────────────
;REACT6, 합의를 요구한다·개별 리액션
;────────────────────────────────────
;·죠교대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_REACT_K29_6
;-------------------------------------------------
;손간을 싫어하지 않는 것이면, TALENT:MASTER:남자등으로 내부에서 나누는 것도 있을지도 모릅니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("소극적으로 한다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 각오를 결정한 것처럼, 작은 소리로 승낙했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 0, 작은 소리로 승낙했다	
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은적극			부끄러움에 스러질 것 같은 소리로 승낙했다
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;실은 혐				분함에 얼굴을 비뚤어지게 하면서도 승낙했다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("태연하게 한다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 별로 싫지 않다고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 마음대로 하면 좋다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은적극			했다 좋은들 좋다고 말했다
		IF COMCOR_POSI()
;OriginalString : 
			PRINTFORMW 
		;실은 혐				승손로 하면 좋다고 대답했다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 처음을 줄 수 있어 기쁘다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 뜻을 정해 바쳐도 괜찮다고 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 이제 지킬 수 없다면 적어도 상냥하게 여겨지도록(듯이)와 동의했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게 해 주는 것이라면 처음을 주어도 괜찮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
	;자연스러운행동
	;=================================================
	;REACT부하 0, 처음을 바치기 때문에기분좋게 해 갖고 싶다고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는행동
	;=================================================
	;REACT부하 1, 기분좋게 해 주는 것이라면 처음을 주어도 괜찮다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를했다행동
	;=================================================
	;REACT부하 2, 처음을 바치라고 한다면, 괴로움을 잊을 수 있을 정도로기분좋게 해라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의 하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서"거부"에 세트 된다)
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("거부")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(4)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT6, 합의를 요구한다

;────────────────────────────────────
;REACT7, 징계한다고 위협한다·개별 리액션
;────────────────────────────────────
;·선언해도징계·이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_REACT_K29_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「あら、臆했다の？」
	PRINTFORMW 「어머나, 겁먹었어?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM"마음 약하게 응답한다"는 일정 조건으로 REACT분류"용서를 빌다"에 파생한다
;-------------------------------------------------
IF 구상기입체크() && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「そんなこと言われたら、やめたくなくなっちゃう」
		PRINTFORMW 「그런 일 말해지면(자), 그만두고 싶지 않게 되어버린다」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
;OriginalString : 「聞こえないわねぇ、ふふふ」
		PRINTFORMW 「들리지 않네요 , 후후후」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 「その減らず입もいつまで続くかしら」
	PRINTFORMW 「그 억지도 언제까지 계속되는 것일까」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「いいじゃない……もう後には引けないわよ」
		PRINTFORMW 「좋지 않아……이제 후에는 파하지 않아요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
;OriginalString : 「まだ物발りないのかしら？」
		PRINTFORMW 「아직 어딘지 부족한 것일까?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를 빌다") 
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(7)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「そんなこと言われたら……もっと虐めたくなっちゃう」
		PRINTFORMW 「그런 일 말해지면(자)……좀 더 학째 싶어져버린다」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「だーめ、許してあげないんだから」
		PRINTFORMW 「이다-째, 허락해 주지 않기 때문에」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
;OriginalString : 「いい声ね……もっと鳴きなさい」
		PRINTFORMW 「좋은 소리군요……좀 더 우세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
;OriginalString : 「たまらないわ……」
		PRINTFORMW 「견딜 수 없어요……」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;무서워한다
;기입 체크
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
;OriginalString : 「そんなに怯えてると、私の가학心が掻き立てられるわ」
		PRINTFORMW 「그렇게 무서워하고 있으면, 나의 가학심을 일으킬 수 있어요」
;OriginalString : 「당신も不幸な存在ね」
		PRINTFORMW 「당신도 불행한 존재군요」
		RETURN 1
	ENDIF
ENDIF
;REACT7, 징계한다고 위협한다

;────────────────────────────────────
;REACT8, 고함치다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K29_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT8, 고함치다

;────────────────────────────────────
;REACT9, 기분나쁘게 웃다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K29_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT9, 기분나쁘게 웃다
