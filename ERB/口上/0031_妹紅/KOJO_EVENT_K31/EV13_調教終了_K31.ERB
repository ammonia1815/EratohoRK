;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모코우구상 죠교終了이벤트구상ファイル
;
;	죠교終了이벤트구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 죠교終了구상
;========================================================================
;・@EVENTCOMENDから呼び出される
;・구상主が죠교終了する直前に表示
;・ARG:1, ARG:2の場合は地の文表示後にPRINTLで空行。その後に구상を表示
;・ARG:0の場合は固有の地の文なし。구상表示の前にPRINTLで空行が入る
;・RESULT 0以上を返すと구상表示後にPRINTLで空行が入る
;-------------------------------------------------
;関連変数…
;FLAG:일상제어	죠교終了時状況(1=통상/2=精力0/3=일회휴게/4=실신했다)
;-------------------------------------------------
@KOJO_EVENT_K31_13(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 普通に終了、1, 죠교대상が疲れている、2, 죠교대상が실신했다)
;-------------------------------------------------

;────────────────────────────────────
;ARG:1, ARG:2, 疲労・실신による죠교終了 FIRSTEVENT(0)～
;────────────────────────────────────
;・죠교대상소모도に応じて疲労/실신に分かれる
;・呼び出しタイミングは地の文の表示→PRINTL→구상の表示
;・EVENT201, 介抱(일상이벤트)の箇所を書く場合は、上손く連携できるようにすると良いでしょう
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が疲れている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が실신했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 2
	IF KFLAG:601 == 1
		IF KFLAG:610 == 0
			PRINTFORML 「깨어났다？」
			PRINTFORMDL 여기는···. 그런가 정신을 잃고 있었는가···.
			PRINTFORMDL 위로부터 들여다 봐 붐비는 얼굴과 후두부에 느끼는 감촉으로 무릎 베개받고 있다고 알아, 일어나려고 한다.
			PRINTFORML 「아, 좋아요, 좀 더 자도」
			PRINTFORMDL 그런%CALLNAME:MASTER%(을)를 억제해, 매홍은 머리카락을 사랑스러운 듯이  어루만졌다.
			PRINTFORML 「미안해요, 그만 본궤도에 올라%CNAME(MASTER, TARGET)%의 일 생각하지 않았다」
			PRINTFORMW 「다음으로부터는 좀 더 잘 하기 때문에···」
			KFLAG:610 = 1
		ELSE
			PRINTFORML 「깨어난 것 같구나」
			PRINTFORML 「사양하지 않아도 좋아요, 내가 무릎 베개 해 주고 싶은 것뿐이니까」
			PRINTFORMW 「그러니까 좀 더만 이대로 있게 해···」
		ENDIF
	ELSE
		;아라이멘도正
		IF ALI(0)
			PRINTFORML 「저것, 조금 너무 격렬하게 했는지」
			PRINTFORML 「···기분이야 유혹한데 얼굴 해 버려서」
			PRINTFORMW 「···이 뒤처리 어떻게 해···」
		;아라이멘도負
		ELSE
			PRINTFORML 「저것, 이제 한계？」
			PRINTFORML 「뭐, 그렇다면 그것으로 좋은데」
			PRINTFORMW 「일어나면(자) 스스로 정리하세요」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ARG:0 普通に終了 FIRSTEVENT(10)～
;────────────────────────────────────
;・통상の죠교終了時の구상
;・地の文なし。PRINTLによる空行の後に구상表示
;・枠毎に이벤트번호を振ってありますが、任意に設定する事は可能です
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;대만족보너스を得ている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:금회대만족
	IF CFLAG:601 == 1
		IF CFLAG:611 == 0
			PRINTFORML 「고마워요」
			PRINTFORMDL 조교가 끝나, 방을 나오려고 했더니 되돌아 보면(자), 매홍은 그렇게 말했다.
			PRINTFORML 「 나, 지금 매우 충실해」
			PRINTFORML 「이런 생활%CNAME(MASTER, TARGET)%에는 폐가 되는 만일 것이다 하지만」
			PRINTFORML 「 그렇지만 미안. 이제%CNAME(MASTER, TARGET)%없이는 생각할 수 없으니까」
			PRINTFORML 「생활의 걱정은 하지 않아도 괜찮은거야. 그러니까」
			PRINTFORMW 「그러니까%CNAME(MASTER, TARGET)%(은)는 나를 기분 좋게 시키는 일만 생각해, 군요？」
			CFLAG:611 = 1
		ELSE
			PRINTFORML 「오늘도 굉장히 좋았어요. ···그렇다」
			PRINTFORML 「 포상에 안녕하세요%CNAME(MASTER, TARGET)%(이)가 좋아하는 것 만들어 준다. 기대하고 있어」
			PRINTFORMDW 매홍은 그러한과 만면의 미소로 방을 나와 갔다···.
		ENDIF
	ELSE
		;아라이멘도正
		IF ALI(0)
			PRINTFORML 「응, 굉장히 매력적으로 좋았다」
			PRINTFORMW 「또 즐겁게 해」
		;아라이멘도負
		ELSE
			PRINTFORML 「이번은 여기까지」
			PRINTFORML 「뭐, 좀 더 괴롭혔으면 좋은거야？」
			PRINTFORML 「안돼. 이제 소리 시들어 오고 있잖아」
			PRINTFORMW 「그러면 모처럼의 좋은 소리를 들을 수 없어요」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;*************************************************
;以下、호감도による派生
;*************************************************

;━━━━━━━━━━━━━━━━━━━━━━━━
;（条件なし）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	IF KFLAG:601 ==1
		IF KFLAG:612 == 0
			PRINTFORML 「···어째서？어째서 나를 만족시켜 주지 않는거야？」
			PRINTFORMW 「내가 싫다？이 생활이 불만？」
			PRINTFORML 「자기···어떤 분의 작정(생각)이야」
			PRINTFORMW 「자기는 나의 노예야！나를 기분 좋게 시키는 것만 생각하고 있으면 좋은거야！！」
;OriginalString : 
			PRINTW
			PRINTFORML 「 나는 필요없어, 그 밖에 아무것도！나는 아무것도 필요하지 않은거야！」
			PRINTFORML 「그러니까···그러니까···.」
			PRINTFORMW 「적어도%CNAME(MASTER, TARGET)%만일 수 있는은, 나만의 것으로 있어···」
			KFLAG:612 = 1
		ELSE
			PRINTFORML 「···. 미안 오늘은 여기까지」
			PRINTFORML 「밥은 적당하게 내 두기 때문에 혼자서 먹으며」
			PRINTFORMDW 매홍은 그러한과 어두운 얼굴로 방을 나와 갔다···.
		ENDIF
	ELSE
		PRINTFORML 「무슨 식어 와 버렸다···」
		PRINTFORMW 「이번은 여기까지」
	ENDIF
	RETURN 1
ENDIF
