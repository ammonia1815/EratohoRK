;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레밀리아구상 추가 액션구상파일
;
;		죠교자가죠교지시 이외에 실시하는행동의 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;죠교액션·커맨드구상
;========================================================================

;────────────────────────────────────
;탈의액션구상
;────────────────────────────────────
;·죠교자액션의 앞에강제탈의나자주탈의가 발생했다때에 표시
;·이구상의 시점에서는 아직 TEQUIP는 갱신되어 있지 않다
;·첫회 분기는 「처음 보았다/보였다」란 할 수 없다.
;  숍 메뉴로 벗게 했던 적이 있을지도 모르고, 죠교의 흐름으로 완전하게 벗게 하지 않고서노출만 하고 있는 경우도 있다.
;  어쩌면조수였던 때에 본 적이 있을지도 모른다. 그러한 의미에서는 간이적인 것이다.
;  엄밀한 의미에서의 「초견」플래그를 준비할지 어떨지는 요망하는 대로라고 하는 일로.
;-------------------------------------------------
@KOJO_MALE_DATUI_K10
;-------------------------------------------------
;관련 변수…
;강제탈의:TFLAG:102  자주탈의:TFLAG:103
;=(-1, 강제탈의에 저항, 1, 겉옷, 2, 하반신겉옷, 3, 웃도리, 4, 상반신전부, 5, 하반신전부,
;  6, 악세서리─, 7, 악세서리─이외 모두, 8, 전라까지, 9, 속옷만)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의에 저항당했다 FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 < 0
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(0)
;OriginalString : 「あら、甘くしてあげてるのにわざわざ抵抗するの？」
		PRINTFORMW 「어머나, 달게 해 주어 정원자리원 자리 저항해?」
;OriginalString : 「それともこういう流れの方が好きなのかしら？」
		PRINTFORMW 「그렇지 않으면 이런 흐름을 좋아하는가 해들?」
		RETURN 1
	ENDIF
	
	;=================================================
	;자신은 벗었다
	;=================================================
	IF 구상기입체크(100) && TFLAG:자주탈의
;OriginalString : 「私だけが脱ぐのは不公平じゃなくって？」
		PRINTFORMW 「나만이 벗는 것은 불공평하지 않아서?」
;OriginalString : 「レディにはもう少し気を遣うものよ？」
		PRINTFORMW 「레이디에게는 좀 더 배려를 하는 것이야?」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「…面白いじゃない」
		PRINTFORMW 「…재미있지 않다」
;OriginalString : 「それでこそ、躾け甲斐があるわ…」
		PRINTFORMW 「그래서야말로, 예의범절을 가르치고 보람이 있어요…」
	;아라이멘도부
	ELSE
;OriginalString : 「…いいわ、抗う事も想定の範囲内だったから」
		PRINTFORMW 「…좋아요, 항일도 상정한 범위내였기 때문에」
;OriginalString : 「でも、少々하드になる事は覚悟できてるかしら？」
		PRINTFORMW 「그렇지만, 조금 하드하게 되는 일은 각오 되어지고 있어 해들?」
;OriginalString : 분노を露にするかと思ったが、むしろ레밀리아は嬉しそうだ
		PRINTFORMDW 분노를 드러낼까하고 생각했지만, 오히려 레미리아는 기쁜 듯하다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;두 사람 모두 뭔가 벗은 FIRSTDATUI(1)
;------------------------------------------------
;·주로섹스계행동의 예비 동작으로 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 > 0 && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(1) && ZENRACHECK(0)
;OriginalString : 「さあ、何をグズグズしてるの？早く服を脱ぐのよ」
		PRINTFORMW 「자, 무엇을 우물쭈물해? 빨리 옷을 벗어」
;OriginalString : 生まれ하며 の姿となった레밀리아は、당신も裸になるよう強要する
		PRINTFORMDW 태어난 채로의 모습이 된 레미리아는, 당신도 알몸이 되도록(듯이) 강요한다
;OriginalString : 「さて…お楽しみはここからよ…」
		PRINTFORMW 「그런데…즐거움은 여기로부터…」
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가[정욕]상태
	;=================================================
	IF 구상기입체크(100) && T_COND("정욕")
;OriginalString : 「少し、私の方が抑えられなくなってきたみたい…」
		PRINTFORMW 「조금, 제 쪽이 억제할 수 없어진 것 같다…」
;OriginalString : 「だから遠慮はしないわよ？」
		PRINTFORMW 「그러니까 사양은 하지 않아요?」
;OriginalString : 「さあ、さっさと服を脱ぎなさい…」
		PRINTFORMW 「자, 빨리 옷을 벗으세요…」
;OriginalString : 指をぺロリと舐める레밀리아の裸体からは、溢れんばかりの광기が感じられた
		PRINTFORMDW 손가락로리와 빠는 레미리아의 나체로부터는, 흘러넘치 듯이 광기가 느껴졌다
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
;OriginalString : 「さあ、早く脱ぎなさい？」
	PRINTFORMW 「자, 빨리 벗으세요?」
;OriginalString : 「それとも私に無理矢理剥いで欲しいのかしら？」
	PRINTFORMW 「그렇지 않으면 나에게 무리하게 벗겼으면 좋은 것일까?」
;OriginalString : 「それでも構わないわよ？より一層楽しめるでしょうし…」
	PRINTFORMW 「그런데도 상관없어요? 보다 한층 즐길 수 있겠지요 해…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·악세서리─마셔 FIRSTDATUI(2)
;------------------------------------------------
;·주로신발안. 그 밖에장갑나소매 등
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 == 6 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;전라되었다（벗기 전은신발안 이외 알몸이라든지 그러한 느낌）
	;=================================================
	IF 구상기입체크(0) && TFLAG:자주탈의 && ZENRACHECK(0)
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·만p FIRSTDATUI(3)
;------------------------------------------------
;·이번탈의로성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(3)
	ENDIF
	
	;=================================================
	;벌써가슴가 보이고 있다
	;=================================================
	IF 구상기입체크(0) && B_DATUICHECK(0) == 1
	ENDIF
	
	;=================================================
	;응 이렇게 p
	;=================================================
	IF 구상기입체크(0) && PENIS(TARGET)
	ENDIF
	
	;그 외
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·말하는 p FIRSTDATUI(4)
;------------------------------------------------
;·이번탈의로 젖가슴이 보이도록(듯이)되었다경우에 표시
;·브라 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:주인님 && !TALENT:거유 && FIRSTDATUI(4)
	ENDIF
	
	;=================================================
	;벌써성기가 보이고 있다
	;=================================================
	IF 구상기입체크(0) && P_DATUICHECK(0) == 1
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(0) && ONCEDATUI(4)
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·팬티 보이고 FIRSTDATUI(5)
;------------------------------------------------
;·이번탈의로팬티（캐릭터에 의해드로와즈·트렁크）가 보이도록(듯이)되었다경우에 표시
;·하반신속옷가 없는 캐릭터는 쓰는필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && P_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(5)
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(0) && ONCEDATUI(5)
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·브라 보이고 FIRSTDATUI(6)
;------------------------------------------------
;·이번탈의로 브라（캐릭터에 의해스포츠브라·사라시）가 보이도록(듯이)되었다경우에 표시
;·상반신속옷가 없는 캐릭터는 쓰는필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(6)
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(0) && ONCEDATUI(6)
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·전라에 FIRSTDATUI(7)·RANDDATUI(8) ～ (11)
;------------------------------------------------
;·「전라까지 벗었다」만이 아니고, 「지금 벗었으므로전라되었다」라고 하는 의미
;·「겉옷 밖에 남지 않은상태로겉옷를 벗었다」등도 포함된다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && ZENRACHECK(0) && !IS_NOWASSIACTNAME(, "탈의")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·악세서리─마셔 FIRSTDATUI(8)
;------------------------------------------------
;·주로신발안. 그 밖에장갑나소매 등
;·탈이했다중에 접어들지 않는다! 되기 십상인 것으로 이것만은 부위별 텐프레를 준비
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 == 6
	;=================================================
	;전라되었다
	;=================================================
	IF 구상기입체크(0) && ZENRACHECK(1)
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·고추 보여라 FIRSTDATUI(9)
;------------------------------------------------
;·이번탈의로남성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·그대로응 보여라 FIRSTDATUI(10)
;------------------------------------------------
;·이번탈의로 여자성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·젖가슴 보여라 FIRSTDATUI(11)
;------------------------------------------------
;·이번탈의로 젖가슴이 보이도록(듯이)되었다경우에 표시
;·브라 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;·죠교대상이남자라고 표시하지 않기 때문에, 남자한정구상에서는 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·팬티 보여라 FIRSTDATUI(12)
;------------------------------------------------
;·이번탈의로하반신속옷가 보이도록(듯이)되었다경우에 표시
;·죠교대상가하반신속옷가 없는 캐릭터 한정의구상이면 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·브라 보여라 FIRSTDATUI(13)
;------------------------------------------------
;·이번탈의로 브라상반신속옷가 보이도록(듯이)되었다경우에 표시
;·죠교대상이남자라고 표시하지 않기 때문에, 남자한정구상에서는 쓰는필요가 없다
;·죠교대상가상반신속옷가 없는 캐릭터 한정의구상이면 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 2 && !TALENT:MASTER:남자
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(13)
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·전라에 FIRSTDATUI(14)
;------------------------------------------------
;·「전라까지 벗겼다」만이 아니고, 「지금 벗겼으므로전라되었다」라고 하는 의미
;·「겉옷 밖에 남지 않은상태로겉옷를 벗겼다」등도 포함된다
;·「젖가슴 보여라」～「팬티 보여라」를 썼을 경우는 기입하는필요는 없다（상층의 분기에 먹혀져 여기까지 떨어져 오지 않기 때문에）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && ZENRACHECK(1)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·뭔가 벗은 FIRSTDATUI(15)
;------------------------------------------------
;·자위·봉사계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·뭔가 벗긴 FIRSTDATUI(16)
;------------------------------------------------
;·애무계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의
	;처음
	IF FIRSTDATUI(16)
		;아라이멘도정
		IF ALI(0)
;OriginalString : 「こんな物、邪魔になるだけよ」
			PRINTFORMW 「이런 물건, 방해가 될 뿐(만큼)」
;OriginalString : 「さっさと脱いじゃいなさい」
			PRINTFORMW 「빨리 벗어버릴 없음 있고」
;OriginalString : 레밀리아の慣れた손つきによって、どんどん服を脱がされていった
			PRINTFORMDW 레미리아가 익숙해진 손놀림에 의해, 자꾸자꾸 옷을 탈의해 갔다
		;아라이멘도부
		ELSE
;OriginalString : 「より楽しむためには、服なんて不필요なのよ」
			PRINTFORMW 「보다 즐기기 위해서(때문에)는, 옷은 불필요해」
;OriginalString : 「わかったなら、大人しくしなさい」
			PRINTFORMW 「알았다면, 얌전하게 해 주세요」
;OriginalString : 一方的に喋りながら、레밀리아は당신の服をどんどん脱がせて行く
			PRINTFORMDW 일방적으로 말하면서, 레미리아는 당신의 옷을 자꾸자꾸 벗게 해 간다
		ENDIF
	;그 이외
	ELSE
		;아라이멘도정
		IF ALI(0)
;OriginalString : 「いいわね、その弱々しい抵抗」
			PRINTFORMW 「좋네요, 그 허약한 저항」
;OriginalString : 「당신が壊れちゃうぐらいまで虐めたくなっちゃう…！」
			PRINTFORMW 「당신이 망가져버리는 정도까지 학째 싶어져버린다…!」
;OriginalString : 中途半端な抵抗が、레밀리아の새드ッ気に火を点けてしまったようだ…
			PRINTFORMDW 어중간한 저항이, 레미리아의 사족기분에 화를 켜 버린 것 같다…
;OriginalString : 레밀리아は興奮気味なのか、あっという間に服を剥ぎ取った
			PRINTFORMDW 레미리아는 흥분 기미의 의 것인지, 눈 깜짝할  순간에 옷을 벗겨냈다
		;아라이멘도부
		ELSE
;OriginalString : 「いいわね、その悲壮を帯びた顔」
			PRINTFORMW 「좋네요, 그 비장을 띤 얼굴」
;OriginalString : 「その強がりが粉々に砕けるまで虐めたくなっちゃう…！」
			PRINTFORMW 「그 허풍이 가루들에 부서질 때까지 학째 싶어져버린다…!」
;OriginalString : 中途半端な반항が、레밀리아の새드ッ気に火を点けてしまったようだ…
			PRINTFORMDW 어중간한 반항이, 레미리아의 사족기분에 화를 켜 버린 것 같다…
;OriginalString : 레밀리아は興奮気味なのか、あっという間に服を剥ぎ取った
			PRINTFORMDW 레미리아는 흥분 기미의 의 것인지, 눈 깜짝할  순간에 옷을 벗겨냈다
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;========================================================================
;추가 액션구상
;========================================================================
;·죠교자가추가행동를 취했을 때에 범용적으로 표시
;·쓰는 경우, 액션구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
;────────────────────────────────────
;EXTRA1, Ｃ애무
;────────────────────────────────────
;·추가로애무 또는수음를 실시한다
;·죠교대상에페니스가 없는 경우는애무, 어느 경우는수음
;-------------------------------------------------
@KOJO_MALE_ACT_EXTRA_K10_1
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_0, 애무 한다
;-------------------------------------------------
;·죠교대상에페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !PENIS(MASTER)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_1, 수음 한다
;-------------------------------------------------
;·죠교대상에페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && PENIS(MASTER)
ENDIF

;────────────────────────────────────
;EXTRA2, Ｖ애무
;────────────────────────────────────
;·추가로 바지애무를 실시한다
;·죠교대상이남자의 경우 발생하지 않는다
;·죠교대상가처녀의 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_MALE_ACT_EXTRA_K10_2
;-------------------------------------------------
IF 구상기입체크(0)
ENDIF

;────────────────────────────────────
;EXTRA3, Ａ애무
;────────────────────────────────────
;·추가로항문애무를 실시한다
;-------------------------------------------------
@KOJO_MALE_ACT_EXTRA_K10_3
;-------------------------------------------------
IF 구상기입체크(0)
ENDIF

;────────────────────────────────────
;EXTRA4, Ｂ애무
;────────────────────────────────────
;·추가로 유두 고문을 실시한다
;-------------------------------------------------
@KOJO_MALE_ACT_EXTRA_K10_4
;-------------------------------------------------
IF 구상기입체크(0)
ENDIF

;────────────────────────────────────
;미약등사용구상
;────────────────────────────────────
;·죠교자가미약나로션를 사용했다때에 범용적으로 표시
;·「액션구상」로 기입 체크를=2로했다경우는 표시되지 않는다
;·쓰는 경우, 액션구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
@KOJO_MALE_USE_K10
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션/미약동시 사용
;------------------------------------------------
;·주로성교계 커맨드로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && GETBIT(TFLAG:미약등사용구상, 0) && GETBIT(TFLAG:미약등사용구상, 1)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션사용
;------------------------------------------------
;·완구·성교계 커맨드등으로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && GETBIT(TFLAG:미약등사용구상, 0)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;미약사용
;------------------------------------------------
;·애무계 커맨드등으로부터 발생한다
;·키스로 발생했다경우는입옮기기가 되기 (위해)때문에, 그 케이스는키스의 액션구상내에서 처리하는 편이 좋다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && GETBIT(TFLAG:미약등사용구상, 1)
ENDIF
