;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모미지구상 성교봉사계 개별 리액션구상파일
;
;	성교봉사계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;성교봉사계
;────────────────────────────────────
;· 각 REACT내의 나누는 방법으로서 TFLAG:REACT분류(IS_COMGRONAME)나 SELECTCOM(IS_NOWCOMNAME)가 생각됩니다
;·다만, 범위의 넓은 조건으로 주워 버리는 것은 기본적으로 추천하지 않습니다
;·ACT분류:성교봉사계의 경우, 봉사계 COM(COM40~COM44)를 중심의선택지가 됩니다만
;·상황에 따라서는 그 외의 것이선택지에 포함될 가능성도 있습니다
;·리액션(REACT)으로 처리되지 않았던 것은 범용 리액션(COM)으로 보완되므로
;·무리하게 모두 묻는필요는 없습니다
;·필요라고 생각하는 것을 지켜봐, 좁혀, COM와의 제휴를 생각해 효율 좋게 구축해야 합니다
;-------------------------------------------------
;· 각 REACT내는,&& IS_NOWCOMNAME든지,&& IS_COMGRONAME등의 조건으로 결정하고 쳐 합시다
;·여기에 조건 없음 분기가 있으면, 모두 주워 버려 범용 리액션에 흐르지 않습니다
;·죠교대상의 반응에 관련되지 않고, 같은 리액션 밖에 하지 않게 되어 버립니다
;·요컨데 이것은, 상정당했다시스템의 파괴에 연결됩니다
;-------------------------------------------------
;  REACT의 지문은 ACT_MESSAGE.ERB의 TRAIN_MESSAGE2_{TFLAG:ACT}로 처리됩니다
;  지문은 수용의 경우는 TFLAG:REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)
;  거절했다경우는 REACT분류(날뛴다/도망친다)와 REACT분류:거부로 나누어집니다
;  각 REACT분류내에서 TFLAG:REACT부하(자연스러운행동/무리가 없는행동/무리를했다행동)로 파생
;  REACT분류가(받아들인다/적극적으로 한다/태연하게 한다)의 경우는, TCVAR:MASTER:가버려, TCVAR:MASTER:애태움도등으로 세세하게 묘사됩니다
;  ACT_MESSAGE.ERB REACTION_MESSAGE.ERB를 참조하면서의 작업을 추천합니다
;-------------------------------------------------
;각 REACT에서는 상정되는 반응을 망라되어 있습니다. 필요에 응해 분기를 증감해 사용해 주세요
;COM에 따라서는 REACT파생로 REACT분류가변화 하는 일에 주의
;각 COM의 파생은 기본적으로 REACT파생 1를 조건 결정 우리 소분기로 해, REACT파생 0는 그 외 취급으로 조건 없다고 보고 있습니다
;손간을 걸고 싶지 않은 경우는 그 외만을 써, 파생에 관련되지 않고 해당 개소를 보완할 수가 있습니다
;또 IF 구상기입 체크(0) && TFLAG:REACT파생 < 1으로 하는 일로 파생했다경우는 범용 리액션에 흘린다 따위라고 하는 일도 가능합니다
;파생은 장래 증가할 가능성도 있으므로 != 1등으로 하는 것보다는< 1으로했다 (분)편이 무난하다라고 생각합니다
;-------------------------------------------------
;·COM20. 허리를 흔든다에 대해…
;·COM20, 허리를 흔든다는 ACT에 의해 REACT분류가변화
;·역강간의 경우는 REACT분류:받아들인다. 그 외성교봉사계 T에서는 REACT분류:적극적으로 한다취급이 된다
;·이변화는 함수@SEX_INITIATIVE에서 행해진다
;-------------------------------------------------

;────────────────────────────────────
;REACT95, 정상위시킨다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교계 반응
;	COM:허리를 흔든다
;고통계 반응
;	COM:날뛴다
;봉사계 반응
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K41_95
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·REACT분류가날뛴다/거부의 경우는취소취급
;·ACT가취소가 되었을 경우는 TEQUIP:성교봉사중도 리셋트
;·그 외의 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)의 경우는 수용 취급이 된다
;·죠교대상가동정라면동정상실, 죠교자가처녀라면처녀상실가 일어난다
;·TEQUIP:성교봉사중가 서 있을 때에 REACT분류가날뛴다라면 거절 취급이 된다(즉, 성교봉사계 ACT 이외에도 거절 처리가 되는 경우가 있다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우(REACT분류가날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2로 TEQUIP는 해제되고 있으므로
	;현재의@V_SEX의 사양(TEQUIP:성교봉사중를 참조하고 있다)라고 죽어 분기가 된다고 생각됩니다
	;ACT파생로 묶는 일로 상사형의 결과를 얻을 수 있을 것 같습니다만, 함수가손 넣고 될 가능성을 고려해 우선 이대로라고 하는 일로
	;=================================================
	;이미삽입 하고 있었을 경우
	;=================================================
	;IF 구상기입 체크(0) && V_SEX(MASTER)
	;	PRINTFORMW 
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1은 REACT분류:날뛴다취급인 것으로 고려하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가변화 하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을 담아 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT95, 정상위시킨다

;────────────────────────────────────
;REACT96, 후배위시킨다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교계 반응
;	COM:허리를 흔든다
;고통계 반응
;	COM:날뛴다
;봉사계 반응
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K41_96
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·REACT분류가날뛴다/거부의 경우는취소취급
;·ACT가취소가 되었을 경우는 TEQUIP:성교봉사중도 리셋트
;·그 외의 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)의 경우는 수용 취급이 된다
;·죠교대상가동정라면동정상실, 죠교자가처녀라면처녀상실가 일어난다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우(REACT분류가날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2로 TEQUIP는 해제되고 있으므로
	;현재의@V_SEX의 사양(TEQUIP:성교봉사중를 참조하고 있다)라고 죽어 분기가 된다고 생각됩니다
	;ACT파생로 묶는 일로 상사형의 결과를 얻을 수 있을 것 같습니다만, 함수가손 넣고 될 가능성을 고려해 우선 이대로라고 하는 일로
	;=================================================
	;이미삽입 하고 있었을 경우
	;=================================================
	;IF 구상기입 체크(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;그 외·ACT 개시시의 거절
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1은 REACT분류:날뛴다취급인 것으로 고려하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가변화 하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을 담아 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT96, 후배위시킨다

;────────────────────────────────────
;REACT97, 대면좌위시킨다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교계 반응
;	COM:허리를 흔든다
;고통계 반응
;	COM:날뛴다
;봉사계 반응
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K41_97
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·REACT분류가날뛴다/거부의 경우는취소취급
;·ACT가취소가 되었을 경우는 TEQUIP:성교봉사중도 리셋트
;·그 외의 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)의 경우는 수용 취급이 된다
;·죠교대상가동정라면동정상실, 죠교자가처녀라면처녀상실가 일어난다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우(REACT분류가날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2로 TEQUIP는 해제되고 있으므로
	;현재의@V_SEX의 사양(TEQUIP:성교봉사중를 참조하고 있다)라고 죽어 분기가 된다고 생각됩니다
	;ACT파생로 묶는 일로 상사형의 결과를 얻을 수 있을 것 같습니다만, 함수가손 넣고 될 가능성을 고려해 우선 이대로라고 하는 일로
	;=================================================
	;이미삽입 하고 있었을 경우
	;=================================================
	;IF 구상기입 체크(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1은 REACT분류:날뛴다취급인 것으로 고려하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가변화 하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을 담아 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT97, 대면좌위시킨다

;────────────────────────────────────
;REACT98, 배면좌위시킨다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교계 반응
;	COM:허리를 흔든다
;고통계 반응
;	COM:날뛴다
;봉사계 반응
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K41_98
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·REACT분류가날뛴다/거부의 경우는취소취급
;·ACT가취소가 되었을 경우는 TEQUIP:성교봉사중도 리셋트
;·그 외의 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)의 경우는 수용 취급이 된다
;·죠교대상가동정라면동정상실, 죠교자가처녀라면처녀상실가 일어난다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우(REACT분류가날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2로 TEQUIP는 해제되고 있으므로
	;현재의@V_SEX의 사양(TEQUIP:성교봉사중를 참조하고 있다)라고 죽어 분기가 된다고 생각됩니다
	;ACT파생로 묶는 일로 상사형의 결과를 얻을 수 있을 것 같습니다만, 함수가손 넣고 될 가능성을 고려해 우선 이대로라고 하는 일로
	;=================================================
	;이미삽입 하고 있었을 경우
	;=================================================
	;IF 구상기입 체크(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가변화 하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을 담아 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT98, 배면좌위시킨다

;────────────────────────────────────
;REACT99, 항문섹스시킨다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ａ성교계로부터/3=Ｖ성교봉사계로부터/4=Ｖ성교계로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교계 반응
;	COM:허리를 흔든다
;고통계 반응
;	COM:날뛴다
;봉사계 반응
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K41_99
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·REACT분류가날뛴다/거부의 경우는취소취급
;·ACT가취소가 되었을 경우는 TEQUIP:성교봉사중도 리셋트
;·그 외의 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)의 경우는 수용 취급이 된다
;·죠교대상가동정에서도동정상실는 되지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 거절했다경우(REACT분류가날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2로 TEQUIP는 해제되고 있으므로
	;현재의@V_SEX의 사양(TEQUIP:성교봉사중를 참조하고 있다)라고 죽어 분기가 된다고 생각됩니다
	;ACT파생로 묶는 일로 상사형의 결과를 얻을 수 있을 것 같습니다만, 함수가손 넣고 될 가능성을 고려해 우선 이대로라고 하는 일로
	;=================================================
	;이미삽입 하고 있었을 경우
	;=================================================
	;IF 구상기입 체크(0) && A_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/받아들인다")
	;=================================================
	;REACT분류가적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;당장이라도가버려인상태인 채, 허리를 흔들어봉사 당하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%에 쾌감을 컨트롤 되고
		;갈 수가 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;이하, COM 마다의 분기
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1은 REACT분류:날뛴다취급인 것으로 고려하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를 빌다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분 좋다!")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가변화 하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를 흔든다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("적극적으로 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을 담아 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게 한다")
;OriginalString : 
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움중에는선택불능. TCVAR:MASTER:가버려가 서는 일에 의해 COMABLE를 빠져 온다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("가게해줘!")
	;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도, @COM70로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
	;현상으로 이변화를 주울 방법은 없다
	;=================================================
	;REACT파생 1/REACT파생 2
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;파생 1, 가게해줘 준다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생 2, 가게해줘 주지 않는다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT99, 항문섹스시킨다

;────────────────────────────────────
;REACT103, 역강간·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를 빌다
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통계 반응
;	COM:비명지른다/날뛴다/무서워한다
;성교계 반응
;	COM:허리를 흔든다
;봉사계 반응
;	COM:소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;그 외 반응
;	COM:가게해줘!
;-------------------------------------------------
@KOJO_REACT_K41_103
;-------------------------------------------------
;·지문은 REAC 분류로 분기
;·다른성교봉사계와 달리, 죠교대상는 거절 할 수 없다
;·COM20, 허리를 흔든다는 REACT분류:받아들이는 것에 세트 된다
;·죠교대상가동정라면동정상실, 죠교자가처녀라면처녀상실가 일어난다
;·COM:싫어한다/날뛴다는 REACT분류:날뛴다(즉, 날뛰고가 통과하는 상황)는 되지 않는다
;-------------------------------------------------
;·역강간는 거동 및 COM의경향가 조금 다릅니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가동정
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TCVAR:MASTER:동정상실
	;죠교자의 BASE:만족가 50%이상
	IF TENSION(TARGET, 8) == "높다"
;OriginalString : 「%CNAME(MASTER, TARGET)%も……初めて、……んっ……で했다か」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%도……처음으로,……응……(이었)였습니까」
;OriginalString : 「あはは……初めてどうし、これでおあいこ、ですね」
		PRINTFORMW 「아하하……처음으로 끼리, 이것으로 무승부, 군요」
;OriginalString : まだ고통が残っているのか、時折辛そうな表情を하면서、
		PRINTFORMDW 아직 아픔이 남아 있는지, 때때로 괴로운 것 같은 표정을 하면서,
;OriginalString : 모미지は처녀を失ったばかりの膣で동정を失ったばかり의 페니스를 しごいていく
		PRINTFORMDW 모미지는 처녀를 잃은지 얼마 안된 질로 동정을 잃은지 얼마 안된 페니스를 훑어 내 간다
	;그 외
	ELSE
;OriginalString : 「%CNAME(MASTER, TARGET)%も……初めて、……んっ……で했다か」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%도……처음으로,……응……(이었)였습니까」
;OriginalString : まだ고통が残っているのか、모미지は辛そうな表情のままだ
		PRINTFORMDW 아직 아픔이 남아 있는지, 모미지는 괴로운 것 같은 표정인 채다
;OriginalString : 「くぅ……さ、さすがにちょっとまだ痛いので、今は冷やかしてる余裕は、ちょっと、ないです」
		PRINTFORMW 「구……, 과연 조금 아직 아프기 때문에, 지금은 구경만 하고 있는 여유는, 조금, 없습니다」
;OriginalString : 「私のはじめてを奪ったんですから、初めてのおちんちんで頑張って気持よくさせてくださいね」
		PRINTFORMW 「 나의 처음을 빼앗았습니까들 , 처음의 자지로 노력해 기분 잘 시켜 주세요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("더 하드하게 해")
	;죠교자의 BASE:만족가 50%이상
	IF TENSION(TARGET, 8) == "높다"
;OriginalString : 「こ、これ以上하드に、ですか！？」
		PRINTFORMW 「개, 더 이상 하드하게, 입니까!?」
	;그 외
	ELSE
;OriginalString : 「これでも物발りないんですか？　ずいぶんと贅沢되었다ものですね……」
		PRINTFORMW 「이것이라도 어딘지 부족합니까? 대단히 사치가 된 것이군요……」
	ENDIF
	RETURN 1
ENDIF

;역강간의 경우, 파생 1, REACT분류:날뛴다가 되는 일은 없기 때문에 고려하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("싫어한다")
	;죠교자의 BASE:만족가 50%이상
	IF TENSION(TARGET, 8) == "높다"
;OriginalString : 「や、やめないですっ」
		PRINTFORMW 「이나, 그만두지 않습니다」
;OriginalString : 「そのまま、私の中で気持ちよくなってくださいっ」
		PRINTFORMW 「그대로, 나중에서 기분 좋아져 주세요」
	;그 외
	ELSE
;OriginalString : 「そんなに\@ PENIS(MASTER) ? 大きくして、# \@嫌がっても、説得力は皆無ですよっ」
		PRINTFORMW 「그렇게\@ PENIS(MASTER) ? 크게 해,# \@싫어해도, 설득력은 전무예요 」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고 모욕한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("서투르다고 모욕한다") && PENIS(MASTER)
;OriginalString : 「その下손な動きでっ、おちんちんを硬くしているのはっ、どなたですかっ」
	PRINTFORMW 「그 서투른 움직임으로 , 자지를 딱딱하게 하고 있는 것은 , 어떤 분입니까 」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("겁없이 웃는다")
	;죠교자의 BASE:만족가 50%이상
	IF TENSION(TARGET, 8) == "높다"
;OriginalString : 「ま、まだまだ余裕ということですか？」
		PRINTFORMW 「뭐, 아직도 여유라고 하는 것입니까?」
;OriginalString : 「その余裕っ、崩してみせますから、覚悟してくださいっ」
		PRINTFORMW 「그 나머지히로시, 무너뜨려 보이기 때문에, 각오 해 주세요」
	;그 외
	ELSE
;OriginalString : 「まだまだ余裕とでも言いたげですね」
		PRINTFORMW 「아직도 여유라고도 말군요」
;OriginalString : 「最後まで笑っていられたら大했다ものですが……ねっ」
		PRINTFORMW 「끝까지 웃어 있을 수 있으면 굉장한 것입니다만……그런데 」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;파생:애원(REACT분류:용서를 빌다)
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:REACT파생 == 1
	;ENDIF
	
	;=================================================
	;파생:내구
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		;죠교자의 BASE:만족가 50%이상
		IF TENSION(TARGET, 8) == "높다"
;OriginalString : 「참다……しないで……」
			PRINTFORMW 「인내……하지 말아줘……」
;OriginalString : 「私と一緒に、気持ちよくなってくださいっ……！」
			PRINTFORMW 「나와 함께, 기분 좋아져 주세요……!」
		;그 외
		ELSE
;OriginalString : 「참다なんか、しないで……」
			PRINTFORMW 「인내는, 하지 말아줘……」
;OriginalString : 「もっと堕ちてくださいっ……！！」
			PRINTFORMW 「좀 더 저속해져 주세요……!!」
;OriginalString : 「辛いことなんか忘れて、えっちなことしか、考えられなくなるくらいにっ！！」
			PRINTFORMW 「괴로운 일은 잊어, 인 것 밖에, 생각할 수 없게 될 정도로!!」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다/COM16, 기분 좋다! (REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋다!")
	;죠교자의 BASE:만족가 50%이상
	IF TENSION(TARGET, 8) == "높다"
;OriginalString : 「はぁっ、気持ちっ、いいっ、ですかっ」
		PRINTFORMW 「후~, 기분, 좋은 , 입니까 」
;OriginalString : 「%CNAME(MASTER, TARGET)%の顔、とても気持ちよさそうで」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 얼굴, 매우 기분이야 유혹하는 것으로」
;OriginalString : 「そんな顔당했다ら、私も、참다できなくなっちゃいますっ」
		PRINTFORMW 「그런 얼굴 되면(자), 나도, 참을 수 없고있습니다」
	;그 외
	ELSE
;OriginalString : 「はぁっ、気持ちっ、いいっ、ですかっ」
		PRINTFORMW 「후~, 기분, 좋은 , 입니까 」
;OriginalString : 「いいんですよっ、気持ちいいこと、受け入れてっ！」
		PRINTFORMW 「좋아요, 기분이 좋은 일, 받아들여!」
;OriginalString : 「獣は、참다なんてしないんですからっ……！！」
		PRINTFORMW 「짐승은, 인내는 하지 않으니까……!!」
	ENDIF
	RETURN 1
ENDIF

;역강간의 경우는 REACT분류:받아들이는이 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("허리를 흔든다")
	;죠교대상가정욕
	IF IS_CONDITION(MASTER, "정욕")
;OriginalString : 「あっ、ちょっと、待って、くださいっ」
		PRINTFORMW 「아, 조금, 기다려, 주세요」
;OriginalString : 「い、今、動かれたら、奥にっ、当たってっ」
		PRINTFORMW 「있고, 지금, 움직여지면(자), 안쪽에 , 맞아」
;OriginalString : 「わ、私がしてあげるんですから、あまり激しく動かないでくださいっ」
		PRINTFORMW 「원, 내가 해 주기 때문에, 너무 격렬하게 움직이지 말아 주세요」
;OriginalString : 모미지の懇願を無視して、%CALLNAME:MASTER%は下から모미지を激しく突き上げる……
		PRINTFORMDW 모미지의 간절히 원함을 무시해, %CALLNAME:MASTER%(은)는 아래로부터 모미지를 격렬하게 밀어 올린다……
	;그 외
	ELSE
;OriginalString : 「んっ、いいですよ、その調子です」
		PRINTFORMW 「응, 좋아요, 그 상태입니다」
;OriginalString : 「私の動きに、がんばって付いてきてくださいねっ」
		PRINTFORMW 「 나의 움직임에, 노력해 붙어 와 주세요」
	ENDIF
	RETURN 1
ENDIF

;역강간의 경우 날뛰고가 통과하는 일은 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 2, 제지에 의한 날뛰고 실패 취급. 파생 1은 범용 리액션에
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("날뛴다") && TFLAG:REACT파생 == 2
	;죠교자의 BASE:만족가 50%이상
	IF TENSION(TARGET, 8) == "높다"
;OriginalString : 「この状況で、よく날뛴다元気がありますねっ」
		PRINTFORMW 「이 상황으로, 자주(잘) 날뛰는 건강해 보이네요」
;OriginalString : 「あ、あまり暴れられると、奥が突かれて……ふわぁっ」
		PRINTFORMW 「아, 너무 날뛸 수 있으면, 안쪽이 찔려……후와」
	;그 외
	ELSE
;OriginalString : 「どうせ날뛴다なら、もっと激しく動いて私を気持ちよくさせてほしいですね」
		PRINTFORMW 「어차피 날뛴다면, 좀 더 격렬하게 움직여 나를 기분 좋게 시켜주었으면 좋겠어요」
	ENDIF
	RETURN 1
ENDIF
;REACT103, 역강간
