;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모미지구상 그 외죠교이벤트구상파일
;
;	그 외죠교턴중이벤트를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;EV21, 대만족구상
;────────────────────────────────────
;·죠교자의대만족보너스를취득했다경우에 표시
;-------------------------------------------------
@KOJO_EVENT_K41_21(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님대만족
;━━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
;OriginalString : 「きゃうううううううううううううん！！」
	PRINTFORMW 「꺄우우 우우 우우 우우 우우 우우 응!!」
;OriginalString : よほど만족だったのか、모미지は山の麓まで届きそうな大声で
	PRINTFORMDW 상당히 만족했던가, 모미지는 산의 산기슭까지 닿을 것 같은 큰 소리로
;OriginalString : 절정の咆哮を上げた……
	PRINTFORMDW 절정의 포 사납게 울부짖고를 올렸다……
	RETURN 2
ENDIF

;────────────────────────────────────
;EV22, 상태변화구상
;────────────────────────────────────
;·상태이상가 붙었을 때에 표시
;·TCVAR:상태변화가죠교자, TCVAR:MASTER:상태변화가죠교대상
;·상태변화가 일어나면 3 턴지속 한다
;-------------------------------------------------
@KOJO_EVENT_K41_22(ARG)
;-------------------------------------------------

;*************************************************
;EV22_0, 죠교자상태변화
;*************************************************
;T_COND TARGET가 지정했다상태라면 진을반환
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[피폐]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && T_COND("피폐") && ! TFLAG:일회휴게
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[쇠약]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && T_COND("쇠약") && ! TFLAG:일회휴게
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[무기력]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && T_COND("무기력")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[몽롱]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && T_COND("몽롱")
ENDIF
[SKIPEND]
;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[정욕]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && T_COND("정욕")
	;아라이멘도정
	IF ALI(0)
;OriginalString : 모미지は紅葉のように鮮やかな舌を出し、自分の犬歯를 핥아た
		PRINTFORMDW 모미지는 단풍과 같이 선명한 혀를 내밀어, 자신의 송곳니를 빨았다
;OriginalString : 「すみません……少し、狼としての本能が抑えられそうにないです」
		PRINTFORMW 「미안합니다……조금, 이리로서의 본능이 억제 당할 것 같지 않습니다」
	;아라이멘도부
	ELSE
;OriginalString : 모미지は紅葉のように鮮やかな舌を出し、自分の犬歯를 핥아た
		PRINTFORMDW 모미지는 단풍과 같이 선명한 혀를 내밀어, 자신의 송곳니를 빨았다
;OriginalString : 「すみません……少し、狼としての本能が抑えられそうにないです」
		PRINTFORMW 「미안합니다……조금, 이리로서의 본능이 억제 당할 것 같지 않습니다」
	ENDIF
	RETURN 2
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[분노]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && T_COND("분노")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[싫증]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && T_COND("싫증")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[울굴]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && T_COND("울굴")
ENDIF
;TALENT:광기가 있는 경우에게만 1/5의 확률로울굴화

;*************************************************
;EV22_1, 죠교대상상태변화
;*************************************************
;M_COND MASTER가 지정했다상태라면 진을반환
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[피폐]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && M_COND("피폐") && ! TFLAG:일회휴게
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[쇠약]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && M_COND("쇠약") && ! TFLAG:일회휴게
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[무기력]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && M_COND("무기력")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[몽롱]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && M_COND("몽롱")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[정욕]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && M_COND("정욕")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[분노]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && M_COND("분노")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[싫증]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && M_COND("싫증")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[울굴]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && M_COND("울굴")
ENDIF
[SKIPEND]

;────────────────────────────────────
;EV23, 죠교대상능력치 업구상
;────────────────────────────────────
;·조교중에죠교대상의 능력치가 상승했다때에 표시
;-------------------------------------------------
@KOJO_EVENT_K41_23(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(이번 상승했다ABL번호/각 ARG번호는 ABL의 거기에 상당)
;
;ARG:0 순종/1 욕망/2 기교/3 Ｃ감각/4 Ｖ감각/5 Ａ감각/6 Ｂ감각/7 봉사정신/8 노출증/9 레즈끼/10ＢＬ끼/11마조끼
;12자위중독/13정액중독/14레즈중독/15고통중독/16구속중독
;
;ARG:20 회화/21 애무/22 도구/23 성교/24 수치/25 봉사/26 가학/27 이상
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[순종]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[욕망]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[기교]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[Ｃ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[Ｖ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[Ａ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 5
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상[Ｂ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 6
ENDIF
[SKIPEND]

;────────────────────────────────────
;EV24, 죠교자능력치 업구상
;────────────────────────────────────
;·조교중에죠교자의 능력치가 상승했다때에 표시
;-------------------------------------------------
@KOJO_EVENT_K41_24(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(이번 상승했다ABL번호)
;
;ARG:0 순종/1 욕망/2 기교/3 Ｃ감각/4 Ｖ감각/5 Ａ감각/6 Ｂ감각/7 봉사정신/8 노출증/9 레즈끼/10ＢＬ끼/11마조끼
;12자위중독/13정액중독/14레즈중독/15고통중독/16구속중독
;
;ARG:20 회화/21 애무/22 도구/23 성교/24 수치/25 봉사/26 가학/27 이상
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[욕망]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[기교]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[Ｃ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[Ｖ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[Ａ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 5
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;모미지님[Ｂ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG == 6
ENDIF
[SKIPEND]

;────────────────────────────────────
;EV29, 굴복·고통각인에 의한반항각인상한 처리
;────────────────────────────────────
;·반항각인Lv 상한의 처리
;·반항각인는 10-(고통각인+굴복각인)/2를 넘지 않는다
;-------------------------------------------------
@KOJO_EVENT_K41_29(ARG)
;-------------------------------------------------
;%CALLNAME:TARGET%의 주는쾌락\@ MARK:MASTER:고통각인? (와)과고통 # \@가 %CALLNAME:MASTER%로부터반항의기력를 빼앗아 간다…
;반항각인는{MARK:반항각인}되었다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;반항각인상한 처리
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0)
;ENDIF

;========================================================================
;각인취득구상
;========================================================================
;·조교중에각인를취득시켰다경우에 표시. 어느 레벨로구상표시할까는 가감(상태)자유인 것으로 개개로 조정하는 것이 바람직함
;·기입 체크가=1의 경우, 동시에 복수취득했다때는 이 파일로 맨 위의 것만을 표시한다
;·=2의 경우, 취득했다각인의구상 모든 것을 위로부터 순서에 표시한다
;·각인취득에 의한 소질 변동전에 표시하기 위해(때문에), 소질취득/상실할까는 이 시점에서의 TALENT의 유무로 판정할 수 있다
;-------------------------------------------------
@KOJO_MARK_K41(ARG:0, ARG:1, ARG:2, ARG:3, ARG:4)
#DIM 기입체크
#DIM 모두순서에표시
모두순서에표시 = 0	;각인취득구상 「모두 순서에 표시」를 위한 초기화 처리
;-------------------------------------------------
;인수 내역…
;ARG:(각인번호)=(이번취득했다레벨)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;[트라우마]취득
기입체크 = 1	;0. 비표시/1. 동시에취득했다각인를 이것 이하 표시하지 않는다/2. 모두 순서에 표시한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입체크 && ARG:4
	SETBIT 모두순서에표시, 4
	SELECTCASE ARG:4
		;트라우마Lv2
		CASE 2
;OriginalString : 「申し訳ありません！」
			PRINTFORMW 「죄송합니다!」
;OriginalString : 모미지は加減しきれず%CALLNAME:MASTER%に트라우마を与えてしまったことを謝ってきた……
			PRINTFORMDW 모미지는 조절 다 할 수 있지 않고 %CALLNAME:MASTER%에 트라우마를 주어 버린 것을 사과해 왔다……
		;트라우마Lv5
		CASE 5
;OriginalString : 「しまった……！　ここまでするつもりは……！」
			PRINTFORMW 「끝냈다……! 여기까지 할 생각은……!」
;OriginalString : 모미지は%CALLNAME:MASTER%にひどい트라우마を刻みつけてしまったことを悔やんでいる……
			PRINTFORMDW 모미지는 %CALLNAME:MASTER%에 심한 트라우마를 새겨 붙여 버린 것을 후회하고 있다……
		;트라우마Lv10…[겁쟁이]가 없는 경우, 그것을 얻는다
		;              이미[겁쟁이]가 있어[광기]가 없는 경우,[유치]가 있는 경우는 100%, 없는 경우는 1/3의 확률로[광기]를 얻는다
		;              [광기]의취득판정에 새어, 한편[유치]를 가지지 않는 경우,[유치]를 얻는다(유아 퇴행 한다)
		;              현상,[광기]와[유치]는 어느 쪽으로 구를까 먼저 판정할 방법은 없다(요망이 있으면…). 우선 머리가 이상해진다고 해석해 주세요
		CASE 10
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 4
	ENDSELECT
	SIF GETBIT(모두순서에표시, 4) && 기입체크 == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[고통각인]취득
기입체크 = 1	;0. 비표시/1. 동시에취득했다각인를 이것 이하 표시하지 않는다/2. 모두 순서에 표시한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입체크 && ARG:0
	SETBIT 모두순서에표시, 0
	SELECTCASE ARG:0
		;고통각인Lv2
		CASE 2
;OriginalString : 「も、申し訳ありません！」
			PRINTFORMW 「도, 죄송합니다!」
;OriginalString : 「力加減が出来ませんで했다！　本当にすみません！」
			PRINTFORMW 「힘조절를 할 수 없었습니다! 정말로 미안합니다!」
		;고통각인Lv5
		CASE 5
;OriginalString : 「わ、私はなんてことを……」
			PRINTFORMW 「원, 나는은 일을……」
;OriginalString : 「고통で無理やりに従わせるなど、天狗としての誇りに反する行為です……」
			PRINTFORMW 「고통으로 억지로에 따르게 하는 등, 텐구로서의 자랑에 반하는 행위입니다……」
		;고통각인Lv9…[굳셈]이있는경우, 그걸잃는다
		;CASE 9
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 0
	ENDSELECT
	SIF GETBIT(모두순서에표시, 0) && 기입체크 == 1
		RETURN 1
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;[반항각인]취득
기입 체크 = 0	;（0, 비표시, 1, 동시에취득했다각인를 이것 이하 표시하지 않는, 2, 모두 순서에 표시한다）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(100) && ARG:3
	SETBIT 모두 순서에 표시, 3
	SELECTCASE ARG:3
		;반항각인Lv2
		CASE 2
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두 순서에 표시, 3
	ENDSELECT
	SIF GETBIT(모두 순서에 표시, 3) && 기입 체크 == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[굴복각인]취득
기입 체크 = 0	;（0, 비표시, 1, 동시에취득했다각인를 이것 이하 표시하지 않는, 2, 모두 순서에 표시한다）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입 체크 && ARG:2
	SETBIT 모두 순서에 표시, 2
	SELECTCASE ARG:2
		;굴복각인Lv2
		CASE 2
		;굴복각인Lv7…[반항적]이있는경우, 그걸잃는다
		CASE 7
		;굴복각인Lv10…[프라이드높음]이있는경우, 그걸잃는다
		CASE 10
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두 순서에 표시, 2
	ENDSELECT
	SIF GETBIT(모두 순서에 표시, 2) && 기입 체크 == 1
		RETURN 1
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;[쾌락각인]취득
기입체크 = 1	;（0, 비표시, 1, 동시에취득했다각인를 이것 이하 표시하지 않는, 2, 모두 순서에 표시한다）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입체크 && ARG:1
	SETBIT 모두순서에표시, 1
	SELECTCASE ARG:1
		;쾌락각인Lv2
		CASE 2
;OriginalString : （あれ？）
			PRINTFORMW （어? ）
;OriginalString : （なんだか、声の感じが少し変わったような……）
			PRINTFORMW （어쩐지, 소리의 느낌이 조금 바뀐 것 같은……）
		;쾌락각인Lv5…[쾌감을부정]이있는경우, 그걸잃는다
		CASE 5
;OriginalString : （すごい……気持ちよさそう……）
			PRINTFORMW （대단해…… 기분이야 유혹한다……）
;OriginalString : （この人、私の죠교で感じてるのかな……）
			PRINTFORMW （이 사람, 나의 조교로 느끼고 있을까……）
		;쾌락각인Lv7…[억압]이있는경우, 그걸잃는다
		;CASE 7
		;쾌락각인Lv10…[쾌감에솔직]가 없는 경우, 그것을 얻는다
		;CASE 10
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 1
	ENDSELECT
	SIF GETBIT(모두순서에표시, 1) && 기입체크 == 1
		RETURN 1
ENDIF

;각인취득구상 「모두 순서에 표시」를 위한 종단 처리
RETURN 모두순서에표시 != 0
