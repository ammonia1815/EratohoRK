;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	엘리스구상 가학系個別リアクション구상ファイル
;
;	가학系リアクション구상をまとめたファイル
;
;Version		Reason
;1.71			@구상記入チェック仕様に
;1.67			TEQUIPが解除される場合の処理を追加
;1.65			TFLAG:REACT부하 == 2の場合COMに流れるように
;1.62			@IS_NOWCOMNAMEの新設に伴いSELECTCOMをIS_NOWCOMNAMEに変更
;1.60			ACT분류単体ファイルとして独立
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;가학系
;────────────────────────────────────

;*************************************************
;REACT60, 스펭킹・個別リアクション
;*************************************************
;=================================================
@KOJO_REACT_K14_60
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;고통을 참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(55) && IS_NOWCOMNAME("고통을 참는다") && TFLAG:REACT부하 < 2
	IF 0
	ELSEIF AFFECTION("상사상애")
		PRINTFORMW 「이봐요, 엉덩이 얻어맞으면(자) 흥분해버리는 지요…？」
		PRINTFORM 「숨기지 않아도 괜찮아…나, 그런%CNAME(MASTER, TARGET)%(을)를 아주 좋아하기 때문에
		CALL PRINT_SYMBOL_K14
	ELSEIF AFFECTION("익애")
		PRINTFORMW 「꽤 좋은 소리로 울잖아」
		PRINTFORMW 「사실은%CNAME(MASTER, TARGET)%도 즐기고 있는 지요…？ 이봐요, 정직하게 좋다 차이야」
	ELSEIF AFFECTION("권애")
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 깨닫고 있어 해들？」
		PRINTFORMW 「단 소리가 섞여 나무원이야…」
	ELSEIF AFFECTION("嫌悪") || T_COND("분노") || TFLAG:징계・플래그
		PRINTFORMW 「이봐요 이봐요…！ 아직도 이런 것은 미안해요！」
		PRINTFORMW 「철저하게 가르쳐 주어요… 각오 해 주세요！」
		PRINTFORMDW 앨리스의 너무 가혹한 처사에, %CALLNAME:MASTER%(은)는 소리를 내는 일도 할 수 없다…
	ELSE
		PRINTFORMW 「울어도 외쳐도 그만두지 않아요…」
		PRINTFORMW 「이봐요, 우세요！ 돼지같이 울어 보이세요！」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(50) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「언제 들어도 괜찮은 소리로 울어요…」
		PRINTFORM 「%CNAME(MASTER, TARGET)%, 그것은 권하고 있는 거나
		CALL PRINT_SYMBOL_K14
	ELSEIF AFFECTION("嫌悪") || T_COND("분노") || TFLAG:징계・플래그
		PRINTFORMW 「정말로 번거로운 돼지군요…」
		PRINTFORMW 「좋아요, 두 번 다시 귀에 거슬리는 소리인 울음소리를 낼 수 없게 해 준다…」
	ELSE
		PRINTFORMW 「, 울고 싶은 것뿐 울어도 좋아요？」
		PRINTFORMW 「그래서 멈출 생각은 없지만요. 후후…」
	ENDIF
	RETURN 1
ENDIF
;REACT60, 스펭킹

;*************************************************
;REACT61, 채찍・個別リアクション
;*************************************************
;=================================================
@KOJO_REACT_K14_61
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;고통을 참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(55) && IS_NOWCOMNAME("고통을 참는다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「후후… 정말 감미로운 울음 소리인가 해들…감등 없네요…」
		PRINTFORMW 「 나, 굉장한 흥분해 버린다…」
	ELSEIF AFFECTION("익애")
		PRINTFORMW 「… 이것, 감등 없네요…」
		PRINTFORMW 「 나, 굉장한 흥분해 버린다…」
	ELSEIF AFFECTION("권애")
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 깨닫고 있어 해들…？」
		PRINTFORMW 「단 소리가 섞여 나무원이야…」
	ELSEIF AFFECTION("嫌悪") || T_COND("분노") || TFLAG:징계・플래그
		PRINTFORMW 「이봐요 이봐요…！ 아직도 이런 것은 미안해요！」
		PRINTFORMW 「철저하게 가르쳐 주어요… 각오 해 주세요」
		PRINTFORMDW 앨리스의 처사는 가열을 다해 %CALLNAME:MASTER%(은)는 소리를 내는 일도 할 수 없다…
	ELSE
		PRINTFORMW 「울어도 외쳐도 그만두지 않아요…」
		PRINTFORMW 「이봐요, 우세요！　돼지같이 울어 보이세요！」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(55) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「언제 들어도 괜찮은 소리로 울어요…」
		PRINTFORM 「%CNAME(MASTER, TARGET)%, 그것은 권하고 있는 거나
		CALL PRINT_SYMBOL_K14
	ELSEIF AFFECTION("嫌悪") || T_COND("분노") || TFLAG:징계・플래그
		PRINTFORMW 「정말로 번거로운 돼지군요…」
		PRINTFORMW 「좋아요, 두 번 다시 귀에 거슬리는 소리인 울음소리를 낼 수 없게 해 준다…」
	ELSE
		PRINTFORMW 「, 울고 싶은 것뿐 울어도 좋아요？」
		PRINTFORM 「그래서 멈출 생각은 없지만요…
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 1
ENDIF
;REACT61, 채찍・個別リアクション

;*************************************************
;REACT62, 바늘・個別リアクション FIRSTTIME(262)
;*************************************************
;=================================================
@KOJO_REACT_K14_62
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;고통을 참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("고통을 참는다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「후후…정말 감미로운 울음 소리인가 해들…이것, 감등 없네요…」
		PRINTFORMW 「 나, 굉장한 흥분해 버린다…」
	ELSEIF AFFECTION("익애")
		PRINTFORMW 「…이것, 감등 없네요…」
		PRINTFORMW 「 나, 굉장한 흥분해 버린다…」
	ELSEIF AFFECTION("권애")
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 깨닫고 있어 해들…」
		PRINTFORMW 「단 소리가 섞여 나무원이야…」
	ELSEIF AFFECTION("嫌悪") || T_COND("분노") || TFLAG:징계・플래그
		PRINTFORMW 「응, 충고 해 주지만, 코레 움직이면(자) 위험해요？」
		PRINTFORMW 「무사하고 싶으면 얌전하게 하는 일이군요. 우후후…」
		PRINTFORMDW 앨리스의 처사는 가열을 다해 %CALLNAME:MASTER%(은)는 소리를 내는 일도 할 수 없다…
	ELSE
		PRINTFORMW 「이봐요, 움직이면(자) 위험해요…？」
		PRINTFORMW 「불필요한 상처나고 싶지 않으면, 얌전하고 없음 있고…」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(55) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「언제 들어도 괜찮은 소리로 울어요…」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 그것 권하고 있는 거나♪」
	ELSEIF AFFECTION("嫌悪") || T_COND("분노") || TFLAG:징계・플래그
		PRINTFORMW 「정말로 번거로운 돼지군요…」
		PRINTFORMW 「좋아요, 두 번 다시 귀에 거슬리는 소리인 울음소리를 낼 수 없게 해 준다…」
	ELSE
		PRINTFORMW 「, 울고 싶은 것뿐 울어도 좋아요？」
		PRINTFORMW 「그래서 멈출 생각은 없지만요♪」
	ENDIF
	RETURN 1
ENDIF
;REACT62, 바늘

;*************************************************
;REACT63, 밧줄・個別リアクション
;*************************************************
;=================================================
@KOJO_REACT_K14_63
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;拒絶処理
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_COMGRONAME("용서를 빌다")
	CALL 가학系解除処理_K14
	RETURN 1
ENDIF

;他のSELECTCOMは汎用リアクションで
;━━━━━━━━━━━━━━━━━━━━━━━━
;고통을 참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("고통을 참는다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORM 「%CNAME(MASTER, TARGET)%, 매우 줄이 어울리고 있는 원이야…
		CALL PRINT_SYMBOL_K14
		PRINTFORMW 「조크조크 해버린다… 엄청에 학째 싶어져요…」
	ELSE
		PRINTFORMW 「%RAND_SPLIT("さぁ、これでもう動けないわよ/さて、お愉しみはこれから。ね")%…」
	ENDIF
	RETURN 1
ENDIF
;REACT63, 밧줄

;*************************************************
;REACT64, 눈가리개・個別リアクション
;*************************************************
;눈가리개、볼개그等のTEQUIP系は장착直後のREACTよりも
;それ以降のACT等でいかに上손く処理するかが肝な感じだけど
;まぁ考えれば考えるほど面倒なんで、なんともな感じ
;-------------------------------------------------
;=================================================
@KOJO_REACT_K14_64
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;拒絶処理
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_COMGRONAME("용서를 빌다")
	CALL 가학系解除処理_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;고통을 참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("고통을 참는다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「후후……그 모습 매우 좋네요…」
	ELSE
		PRINTFORMW 「시야를 빼앗긴 기분은 어떠세요…？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「이봐요, 무서워하지 마…」
		PRINTFORMW 「 나는 분명하게 여기에 있기 때문에」
	ELSE
		PRINTFORML 「그렇게 무서워하는 일 없어요…？」
		PRINTFORMW 「따로 혼내주자는 (뜻)이유가 아니기 때문에」
		PRINTFORM 「우선 는, 이군요
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 1
ENDIF
;REACT64, 눈가리개

;*************************************************
;REACT65, 볼개그・個別リアクション FIRSTTIME(265)
;*************************************************
;=================================================
@KOJO_REACT_K14_65
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;拒絶処理
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_COMGRONAME("용서를 빌다")
	CALL 가학系解除処理_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;고통을 참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("고통을 참는다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「군침 흘려 보내 하고 싶은 있고 원이군요…」
		PRINTFORM 「좋아요, 내가 전부철은 준다
		CALL PRINT_SYMBOL_K14
		;フェードアウト
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), KOJO_RAND_K14(("키스"), 1))
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), KOJO_RAND_K14(("키스"), 1))
		CLEARLINE 1
	ELSEIF AFFECTION("익애")
		PRINTFORMW 「후후, 뭐라고 하는 보기 흉한가 해들 」
		PRINTFORMW 「군침 흘려 보내 하고 싶은 있고 원이군요…」
	ELSEIF AFFECTION("권애")
		PRINTFORMW 「후후…정말 보기 흉한가 해들 」
		PRINTFORMW 「군침 흘려 보내 하고 싶은 있고 원이군요…」
	ELSEIF AFFECTION("嫌悪") || T_COND("분노") || TFLAG:징계・플래그
		PRINTFORMW 「아는 , 좋은 모습. 마치 가축이군요」
		PRINTFORMW 「어머나, 무엇인가 말하고 싶은거야？ 그렇지만 유감…♪」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%가 무슨 말을 하고 있는지, 나에게는 알지 않는 것 후후」
	ELSE
		PRINTFORMW 「군침 흘려 보내 버려서, 보기 흉한 일 이 이상 없네요」
		PRINTFORMW 「아무튼에서도,%CNAME(MASTER, TARGET)%에는 잘 어울리는 모습일까？ 후후…」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	SELECTCASE RANDREACT(2, 0)
		CASE 0
			PRINTFORM 「어머나, 유감이구나. 신음소리를 내고 있을 뿐은 아무것도 몰라요
			CALL PRINT_SYMBOL_K14
		CASE 1
			PRINTFORMW 「쓸데없는 발버둥질도 그 근처로 해 두면(자)？」
			PRINTFORMW 「그런 상태로 울든지 아우성치든지, 나에게는 뭐가 뭔지 전혀이고. 후후…」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	SELECTCASE RANDREACT(3, 1)
		CASE 0
			PRINTFORMW 「어머나, 무서워하지 않아 좋아？」
			PRINTFORMW 「지금부터 충분히 귀여워해 줄 생각이니까」
			PRINTFORMW 「다만…, 나나름의 방식으로, 군요」
		CASE 1
			PRINTFORMW 「그렇구나, 방법 없는 면도 있는 것은 알고 있는 작정(생각)이지만…」
			PRINTFORMW 「그렇게 명백하게 무서워할 수 있는 것은, 너무 좋은 기분은 하지 않아」
			PRINTFORMW 「적어도 지금은, 이군요……」
		CASE 2
			PRINTFORMW 「%CNAME(MASTER, TARGET)%가 작은 동물같이 무서워하고 있는 모습도, 가끔  나쁘지 않은 것？」
			PRINTFORMW 「뭐, 언제나 그런 느낌으로 있을 수 있으면, 진절머리 난다고는 생각하지만, 이군요」
	ENDSELECT
	RETURN 1
ENDIF
;REACT65, 볼개그

;*************************************************
;REACT66, 매도・個別リアクション
;*************************************************
;매도は가학系に分類されるが、内容的には회화系と同一（회화系から매도に派生する場合もあり）なので
;REACT内で完結시켰다方が無難ではある
;COMで処理するならGET_ACTM(TFLAG:ACT)を使えば派生に対応できる
;-------------------------------------------------

;=================================================
@KOJO_REACT_K14_66
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("침묵한다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「, 조금. 가벼운 농담인데…」
		PRINTFORMW 「응, 기분 고쳐…」
	ELSE
		PRINTFORMW 「입다물지 말고 무엇인가 말하면 어떻게야…？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("마음 약하게 응답한다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「뭐야, 다쳤어…？」
	ELSE
		PRINTFORMW 「무엇인가？ 들리지 않아요…？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;무뚝뚝하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("무뚝뚝하게 응답한다") && TFLAG:REACT부하 < 2
	SELECTCASE RANDREACT(4, 0)
		CASE 0
			PRINTFORMW 「건방지구나…」
		CASE 1
			PRINTFORMW 「사랑스럽지 않네요……」
			PRINTFORMW 「가장, 당연한 반응이라고는 생각하는데」
		CASE 2
			PRINTFORMW 「어머나, 좋은 담력이군요？」
			PRINTFORMW 「뭐, 그 정도가 아니면 경쟁이 없고, 군요. 후후…」
		CASE 3
			PRINTFORMW 「뭐, 당연한 반응이라고는 생각하는데…」
			PRINTFORMW 「너무 좋아하지 않아, 그러한 것」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;愛想良く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("상냥하게 응답한다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORM 「이 변태…
		CALL PRINT_SYMBOL_K14
	ELSE
		PRINTFORMW 「완전히 길러 말해진 느낌이군요…」
	ENDIF
	RETURN 1
ENDIF

;無理を했다행동、もうやめろと強く言ったの場合はCOMに流す
;━━━━━━━━━━━━━━━━━━━━━━━━
;강하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("강하게 응답한다") && TFLAG:REACT부하 < 2 && TFLAG:REACT파생 < 1
	SELECTCASE RANDREACT(5, 1)
		CASE 0
			PRINTFORMW 「상당히 위세가 좋네요？ 뭐, 이 때 1개(살) 말하는데……」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%(을)를 위해 안 돼요, 그러한 것」
		CASE 1
			PRINTFORMW 「힘차네요. 무엇인가 좋은 일이라도 있었는지 해들？」
			PRINTFORMW 「뭐, 좋아요. 지금 만일 수 있는은 말하게 하고 물어 여(어) 준다……」
		CASE 2
			PRINTFORMW 「……에 , 재미있어요」
			PRINTFORMW 「다만, 허세를 쳤더니 아무 의미도 없는 것이 슬픈 곳이지만, 이군요」
		CASE 3
			PRINTFORMW 「…말해요. 조금 재미있었어요」
			PRINTFORMW 「 그렇지만 본궤도에 오르고 있으면, 아픈 눈에 맞추기 때문에…」
		CASE 4
			PRINTFORMW 「거만함…그런데」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("빈정거린다") && TFLAG:REACT부하 < 2
	SELECTCASE RANDREACT(3, 2)
		CASE 0
			PRINTFORMW 「야유는 싫어요…」
		CASE 1
			PRINTFORMW 「어차피라면, 좀 더 히네리가 효과가 있던 야유를 기대하고 싶은 것이지만…」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%에는 무리한 상담일까, 군요？」
		CASE 2
			PRINTFORMW 「야유, 응？ 뭐, 그렇구나, 나에게 말하게 하면……」
			PRINTFORMW 「지금의 %CNAME(MASTER, TARGET)%의 처지가 야유, 라는 느낌이지만」
	ENDSELECT
	RETURN 1
ENDIF

;無理を했다행동時は弾く
;━━━━━━━━━━━━━━━━━━━━━━━━
;기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("기분좋게 해") && TFLAG:REACT부하 < 2
	;=================================================
	;징계・포인트加算
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
		SELECTCASE RANDREACT(3, 5)
			CASE 0
				PRINTFORMW 「어머나, 어딘지 부족했던 것일까 해들…？」
			CASE 1
				PRINTFORMW 「뭐야, 아직 부족하다고 말하는 (뜻)이유？」
			CASE 2
				PRINTFORMW 「그것, 원 돌려에 나에게 구두쇠 붙이고 있는 작정(생각)？」
		ENDSELECT
		PRINTFORMW 「지금 말한 것, 후회%RANDTEXT_K14("するわよ", "させてやるから", "しないようにね")%……」
		RETURN 1
	ENDIF
	
	;상사상애
	IF AFFECTION("상사상애")
		SELECTCASE RANDREACT(2, 3)
			CASE 0
				PRINTFORM 「우대신, 불쾌하네요
				CALL PRINT_SYMBOL_K14
			CASE 1
				PRINTFORMW 「 나에게 저런 일 말해진 대답이 그것이야？」
				PRINTFORM 「뭐,별로 좋지만, 이군요
				CALL PRINT_SYMBOL_K14
		ENDSELECT
	;その他
	ELSE
		SELECTCASE RANDREACT(2, 4)
			CASE 0
				PRINTFORMW 「프라이드라고 해도의가 없는 것일까…？」
				PRINTFORMW 「야비하네요……」
			CASE 1
				PRINTFORMW 「%CNAME(MASTER, TARGET)%의 머릿속은 그것 밖에 없구나？」
				PRINTFORMW 「뭐, 그렇구나. 그 쪽이 나에게 있어 형편이 좋은 것은 확실한 것은 않은데」
				PRINTFORMW 「그 거 조금 어떻게야？ 라고, 생각도 들어요. 후후…」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;겁없이 웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("겁없이 웃는다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		SELECTCASE RANDREACT(2, 5)
			CASE 0
				PRINTFORMW 「뭐야 그것？ 반항기？」
			CASE 1
				PRINTFORMW 「재미있네요……사랑스럽지 않은데」
				PRINTFORMW 「다만, 아무튼…, 그 정도가 아니면 경쟁이 없는 것은 확실한 것은 않아요」
		ENDSELECT
	ELSE
		SELECTCASE RANDREACT(2, 6)
			CASE 0
				PRINTFORMW 「후후…훌륭하지 않다」
				PRINTFORMDW 앨리스는 %CALLNAME:MASTER%에 겁없게 웃어 돌려주어 왔다…
			CASE 1
				PRINTFORMW 「그런 얼굴이 생긴다 라는 일은, 아직 여유가 있을 듯 하구나…」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		SELECTCASE RANDREACT(2, 7)
			CASE 0
				PRINTFORM 「어머나, 좋은 소리군요
				CALL PRINT_SYMBOL_K14
			CASE 1
				PRINTFORMW 「응……이 정도로 비명을 지르는 일 없다고 생각하는데？」
		ENDSELECT
	ELSE
		SELECTCASE RANDREACT(2, 8)
			CASE 0
				PRINTFORMW 「시끄러운 돼지군요…」
			CASE 1
				PRINTFORML 「하나 말하는데…」
				PRINTFORMW 「 나, 너무 소란스러운 것은 좋아하지 않아」
				PRINTFORMW 「얌전하게 해 주세요」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「조금, 무서워하는 일 없을 것입니다？」
	ELSE
		PRINTFORMW 「후후…좀 더 심한 일 하고 싶어져요…」
	ENDIF
	RETURN 1
ENDIF

;勝손にオナニーするな！に派生했다ら弾く
;━━━━━━━━━━━━━━━━━━━━━━━━
;자위를 시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("자위를 시작한다") && TFLAG:REACT파생 < 1
	IF AFFECTION("상사상애")
		PRINTFORM 「대강, 사양이 없는 사람
		CALL PRINT_SYMBOL_K14
	ELSE
		PRINTFORMW 「그런 것 허가한 기억은 없어요…」
	ENDIF
	RETURN 1
ENDIF

;*************************************************
;REACT67, 삼각목마・個別リアクション
;*************************************************
;=================================================
@KOJO_REACT_K14_67
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;拒絶処理
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_COMGRONAME("용서를 빌다")
	CALL 가학系解除処理_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;고통을 참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("고통을 참는다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「후후…%CNAME(MASTER, TARGET)%최고야. 정말 불쾌한 것일까…」
		PRINTFORM 「허리구군요 는, 상당히 마음에 든 것 같구나…
		CALL PRINT_SYMBOL_K14
	ELSE
		PRINTFORMW 「후후…한심한 모습…」
		PRINTFORM 「보기 흉하고 추악해서 최악의 느낌이군요…
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
	PRINTFORMW 「우대신, 좋아요……그 상태야」
	PRINTFORM 「 좀 더 좋은 소리로 울릴 수 있어 주어요
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	PRINTFORMW 「어머나, 무서워하지 않아 좋아？」
	PRINTFORM 「지금부터 충분히 귀여워해 줄 생각이니까
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;*************************************************
;REACT68, 관장그릇＋플러그・個別リアクション RANDREACT(0)
;*************************************************
;・TFLAG:ACT파생（= 0,注入/= 1,排出/= 2,追加）
;-------------------------------------------------
;=================================================
@KOJO_REACT_K14_68
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;관장液注入＆追加
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && GROUPMATCH(TFLAG:ACT파생, 0, 2)
	;=================================================
	;고통을 참는다 RANDREACT(0)
	;━━━━━━━━━━━━━━━━━━━━━━━━
	IF 구상記入チェック(100) && IS_NOWCOMNAME("고통을 참는다") && TFLAG:REACT부하 < 2
		IF AFFECTION("상사상애")
			SELECTCASE RANDREACT(2, 0)
				CASE 0
					PRINTFORMW 「이봐요, 가득 들어갔어요…」
					PRINTFORM 「괴로운 것과 기분이 좋은 것,%CNAME(MASTER, TARGET)%(은)는 어느 쪽인가 해들
					CALL PRINT_SYMBOL_K14
				CASE 1
					PRINTFORMW 「이런 일 되어 흥분하고 있는 지요？」
					PRINTFORM 「싫을 것 같은 헛 스윙도 외관도, 분명하게 알고 있기 때문에…
					CALL PRINT_HEARTCBW_K14(COLOR2_K14("pink"), , , COLOR2_K14("ALICE"))
			ENDSELECT
		ELSE
			SELECTCASE RANDREACT(2, 1)
				CASE 0
					PRINTFORMW 「응, 배 괴로운거야？ 그렇지 않으면 기분이 좋은거야？」
					PRINTFORMW 「후후후…」
				CASE 1
					PRINTFORMW 「완전히 보기 흉한 일 이 이상 없네요？」
					PRINTFORMW 「아무튼…,%CNAME(MASTER, TARGET)%에는 자주(잘) 어울리고 있지만, 이군요」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;비명지른다
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("비명지른다") && TFLAG:REACT부하 < 2
		IF AFFECTION("상사상애")
			PRINTFORMW 「응, 언제나 생각한다지만…」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 그 비명은, 나를 부르기 위해서(때문에) 일부러 하고 있는 겠지？」
			PRINTFORM 「아무튼 결국 실을 수 있는아니야들 , 피차일반이라는 곳인가 해들 , 이군요
			CALL PRINT_SYMBOL_K14
		ELSE
			PRINTFORML 「우대신, 좋아요……그 상태야」
			PRINTFORM 「 좀 더 좋은 소리로 울릴 수 있어 주어요
			CALL PRINT_SYMBOL_K14
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;무서워한다
	;=================================================
	IF 구상記入チェック(100) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
		PRINTFORMW 「아무것도 무서워하는 일 없을 것입니다？」
		PRINTFORMW 「 나는 기분 좋게 시켜 줄 생각이니까. 후후…」
		RETURN 1
	ENDIF
ENDIF

;何もしないでRETURNしてEV20に渡す。※この状況の場合はCOMの구상を表示시켰다くない
;━━━━━━━━━━━━━━━━━━━━━━━━
;관장液排出＆죠교대상がＡ절정（若しくはＡ責め사정）する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && (TFLAG:ACT파생 == 1 && TRUECHECK(GET_EX_K14("MA責め사정"), GET_EX_K14("MA절정")) > 0)
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;관장液排出
;記入チェック
;LOCAL = 0 ;off
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
;ENDIF

;-------------------------------------------------
;가학系REACT拒絶処理（애원を受け入れてのTEQUIP解除）
;-------------------------------------------------
@가학系解除処理_K14
SELECTCASE TENSION(TARGET, 8)
	CASE "高い"
		PRINTFORMW 「뭐야,\@ IS_NOWCOMNAME("무서워한다") ? そんなに무서워한다事ない%RAND_SPLIT("んじゃない/と思うけど")% # 그렇게 멈추었으면 좋겠다%RAND_SPLIT("訳/ワケ/の")% \@？」
		PRINTFORMW 「아무튼에서도…, 방법 없네요. 멈추어 주어요」
	CASE "一般"
		PRINTFORMW 「네네,\@ IS_NOWCOMNAME("무서워한다") ? 判ったわ。そんなに怯えられちゃ仕方無いわね… # 判ったわよ。止めればいいんでしょう？ \@」
	CASE "低い"
		PRINTFORMW 「…알았다.\@ IS_NOWCOMNAME("무서워한다") ? そんなに무서워한다なら、止めてあげる%RAND_SPLIT("わ/")% # 거기까지 말한다면 방법 없네요… \@」
ENDSELECT
