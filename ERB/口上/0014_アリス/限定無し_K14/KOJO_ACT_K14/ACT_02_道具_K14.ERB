;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	엘리스구상 도구계 액션구상파일
;
;	도구계 액션구상를 정리한 파일
;
;Version		Reason
;1. 71			@구상기입 체크 사양에. 그 외 섬세한 수정
;1. 62			일부 개변＆가필
;1. 60			ACT분류단체 파일로서 독립
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;도구계 ACT20～26
;========================================================================
;·TFLAG:ACT(20=로터/21=바이브/22=항문바이브/23=항문비즈/24=Ｃ애무도구/25=유두캡/26=페니스밴드)
;·TFLAG:ACT분류 == 2에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "도구")로 줍는 것이 가능
;·죠교자가죠교대상에도구를 사용한다. 이구상의 시점에서죠교자는 벌써손를 내고 있다
;·미약·로션를 사용하는 케이스가 있다
;·도구류의장착, 해제등은 TFLAG:ACT파생로 정의된다
;-------------------------------------------------

;────────────────────────────────────
;ACT20, 로터
;────────────────────────────────────
;·Ｃ중심으로애무 한다
;·애무계로부터 발생하는 경우도 있다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·TEQUIP(클리캡/오나홀/페니스밴드/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교대상에페니스가 있는 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K14_20
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;여기는 파생으로 유두 꾸짖어, 엉덩이 구멍 고문이라든지 있어도 괜찮다
;지문으로 PENIS(MASTER)라든지 보고 있는 개소가 있지만, ABLE로 규제되고 있는 것이 현상
;쓴 인간의 의도가 너무 불명함
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT20, 로터
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「모처럼 있기 때문에, 우선은 시험해 보지 않으면」
		PRINTFORMW 「그런데…,%CNAME(MASTER, TARGET)%에는 마음에 들어 받을 수 있는 것일까. 응?」
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		SELECTCASE TCVAR:MASTER:애태움도
			CASE IS > 1
				SELECTCASE RANDACT(2, 0)
					CASE 0
						PRINTFORMW 「응─? 뭐야, 허리 꽉 눌러 주어 버려서. 후후후」
						PRINTFORMW 「직접 맞혔으면 좋겠다? 이런 완구로도, 이대로 이나 키라고 갖고 싶다?」
						PRINTFORMW 「그렇지만 이렇게, 주위로부터 서서히 꾸짖을 수 있는 것도…, 그것은 그래서 감등 없을 것입니다?」
					CASE 1
						PRINTFORMW 「어때? 접할까 접하지 않을 정도로 초조하게 해지는 이 느낌…」
						PRINTFORMW 「이나 키라고 갖고 싶어서…, 울고 싶어져버린다지요? 후후」
				ENDSELECT
			CASEELSE
				PRINTFORMW 「좋아요, 이나 키라고 주어요.\@ TALENT:MASTER:작은체형 ? 새끼 고양이 # 암퇘지 \@같이 울릴 수 있어 준다」
				PRINTFORM 「%CNAME(MASTER, TARGET)%가 이런 완구에 굴복 하는 순간…, 내가 지켜봐 주어요
				CALL PRINT_SYMBOL_K14
		ENDSELECT
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE RANDACT(2, 10)
		CASE 0
			PRINTFORMW 「멋없는 완구이지만, 가끔 씩은 이런 것도 좋을까…」
			PRINTFORMW 「…좋은 소리로 울어, 나를 즐기게 해 주세요」
		CASE 1
			PRINTFORMW 「그런데, 이번은 어째서 갖고 싶다?」
			PRINTL
			CALL FADEIN(10, "「마음껏 강하게 꽉 눌려지고 싶은 것인지…」")
			CALL FADEIN(10, "「바보같게 되어 버릴 정도로, 천천히 꾸짖을 수 있는 것이 좋은 것인지…」")
			WAIT
			PRINTL
			PRINTFORMW 「이라든가 말해 보았지만…, 결국은 나의 기분 나름이야. 후후」
	ENDSELECT
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT21, 바이브
;────────────────────────────────────
;·죠교대상이남자의 경우는 발생하지 않는다
;·TFLAG:징계·플래그가 끊고 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드)장착중이라면 발생하지 않는다
;·성교중에는 발생하지 않는다
;·죠교대상가처녀에서도 발생할 가능성이 있다
;-------------------------------------------------
@KOJO_ACT_K14_21
;-------------------------------------------------
;·TFLAG:ACT파생(0=바이브장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT21_0, 바이브장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;죠교대상가처녀
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:처녀
		PRINTFORMW 「,%CNAME(MASTER, TARGET)%의 처녀는 이 멋없는 완구에 바쳐」
		PRINTFORMW 「쓸데없어요, 저항해도. 그렇게 한다 라고 결정해 버렸다 것」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 파과의 순간, 분명하게 보고 있어 주어요……」
		RETURN 1
	ENDIF
	
	;=================================================
	;항문바이브/항문비즈장착중(두 구멍 고문)
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(TEQUIP:항문바이브, TEQUIP:항문비즈) > 0
		PRINTFORMW 「어머나, 앞쪽이 외로운 것 같구나. 여기에도 갖고 싶은 것일까」
		PRINTFORM 「좋아요…돌진해 준다. 다만…코레, 지만…
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE RANDACT(2, 10)
		CASE 0
			PRINTFORMW 「이봐요, 들어가요? 그렇지만 코레, 굉장히 싫은 것 같은 광경이군요…」
			PRINTFORM 「안쪽까지 삽입시,%CNAME(MASTER, TARGET)%가 어떻게 되어버릴까 즐겨, 군요
		CASE 1
			PRINTFORMW 「%CNAME(MASTER, TARGET)%…, 당연한 듯이 삼켜버린다 것?」
			PRINTFORMW 「너무 시원시럽게 받아들여버린다 것도, 조금 제발이라고 생각해요…」
			PRINTFORM 「뭐,내가 말하는 것도이지만. 그런데
	ENDSELECT
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 바이브해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 바이브해제가 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1 && FLAG:지문제어 == 2
	PRINTFORMW 「, 뽑아 주어요」
	PRINTFORM 「천천히, 천천히, 군요…
	CALL PRINT_SYMBOL_K14
	RETURN 2
ENDIF

;────────────────────────────────────
;ACT22, 항문바이브
;────────────────────────────────────
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·TEQUIP(항문비즈/관장그릇＋플러그/삼각목마/페니스밴드)장착중은 발생하지 않는다
;·항문섹스중(TEQUIP:성교중 == 6, 혹은 A_SEX())에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K14_22
;-------------------------------------------------
;TFLAG:ACT파생(0=항문바이브장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT22_0, 항문바이브장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「이런 것, 엉덩이에 삽입라고 괜찮은가 해들?」
		PRINTFORMW 「소재 자체는 부드러워서, 문제 없을 것 같게 생각되는데……」
		PRINTFORMW 「뭐, 무슨 일도 우선은 시험해 보지 않으면. 응? 후후」
		RETURN 1
	ENDIF
	
	;=================================================
	;바이브장착중(두 구멍 고문)
	;=================================================
	IF 구상기입체크(100) && TEQUIP:바이브
		PRINTFORMW 「엉덩이의 구멍 히크히크 시켜 버려서…」
		PRINTFORM 「좋아요…, 여기도 마음껏 휘저어 준다
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF

	;-------------------------
	;그 외
	;-------------------------
	IF ABL:MASTER:Ａ감각 > 4
		SELECTCASE RANDACT(3, 10)
			CASE 0
				PRINTFORMW 「%CNAME(MASTER, TARGET)%의 경우, 이제 풀어 줄 필요도 없을 것 같구나?」
				PRINTFORMW 「소망이라면 단번에 안쪽까지……라고, 그러면 풍치가 없어. 후후」
			CASE 1
				PRINTFORMW 「히크히크 시켜 버려서 싫은 것 같네요…. 이제 갖고 싶어서 기다릴 수  없는 것일까?」
				PRINTFORM 「자 말해.
				CALL PRINTFONTSTYLE(COLOR_K14("white"), @"\@ TALENT:MASTER:남자 ? 나 # 나 \@의 엉덩이에 주세요엘리스님 부탁합니다", 1)
				PRINTFORMW 라고」
				CALL FADEIN(10, "「제대로 말할 수 있었다면――」")
				WAIT
				PRINTFORMW 「%CNAME(MASTER, TARGET)%에 자비를 주어요…」
			CASE 2
				PRINTFORMW 「이 손의 일도 완전히 당연하게 되어 버려서, 조금 신선미가 부족해요」
				PRINTFORMW 「아무튼 그 만큼…,%CNAME(MASTER, TARGET)%가 좋은 소리로 울어 준다면 좋지만」
				PRINTFORM 「이봐요, 넣어요.
				IF TALENT:MASTER:작은체형
					PRINTFORM \@ TALENT:MASTER:남자 ? 여자 # 새끼 고양이 \@
				ELSE
					PRINTFORM \@ TALENT:MASTER:남자 ? 여자 # 암퇘지 \@
				ENDIF
				PRINTFORMW 같이 울릴 수 있어 주어요……」
		ENDSELECT
	ELSE
		SELECTCASE RANDACT(3, 11)
			CASE 0
				PRINTFORMW 「, 힘을 빼 편하게 해 주세요」
				PRINTFORMW 「이 아니면…, 어떻게 되어도 모르니까. 후후…」
			CASE 1
				PRINTFORMW 「이봐요, 좀 더 힘을 빼세요. 이래서야 들어가지 않아요」
				PRINTFORMW 「부드럽기 때문에 저항이 있으면 구부러져버린다 것이군요」
			CASE 2
				PRINTFORMW 「첨단의 (분)편을 가지고 구부러지지 않게 하면 넣기 쉬운 것 같구나…」
				PRINTFORMW 「응, 좋은 느낌이군요. 이것이라면……」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT22_1, 항문바이브해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;이미 장비 하고 있는 경우, 항문바이브해제가 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1 && FLAG:지문제어 == 2
	PRINTFORMW 「이봐요, 뽑을 때 조쿡은 오겠죠?」
	PRINTFORM 「후후…, 천천히, 천천히 뽑아 준다
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT23, 항문비즈
;────────────────────────────────────
;·항문비즈장착/해제/추가
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·TEQUIP(항문바이브/관장그릇＋플러그/삼각목마/페니스밴드)장착중에는 발생하지 않는다
;·Ａ성교중(TEQUIP:성교중 == 6, 혹은 A_SEX()) 시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K14_23
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=해제/2=추가)
;-------------------------------------------------

LOCALS = \@ AFFECTION("상사상애") ? 쿠쿡 # 후후후 \@

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT23_0, 항문비즈장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·장착시에 1～3개의 사이에 랜덤으로 정해진다
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「이런 것, 누가 생각했는지 해들?」
		PRINTFORMW 「이것은 이것대로, 여러가지 즐거운 하는 것은 않은데…」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%는 마음에 들어 주는 것일까. 응? 후후」
		RETURN 1
	ENDIF
	
	;-----------------------------------
	;그 외
	;-----------------------------------
	SELECTCASE RANDACT(2, 0)
		CASE 0
			PRINTFORMW 「자, 편하게 해. (이)가 아니면 괴로워요」
			PRINTFORMW 「이상하게 힘주거나 하면, 반드시 대단한 일이 된다고 생각해요」
			PRINTFORMW 「%LOCALS%…」
		CASE 1
			PRINTFORMW 「그런데,%CNAME(MASTER, TARGET)%에 사랑스러운 꼬리를 길러 줄까요」
			PRINTFORM 「\@ TALENT:MASTER:남자 ? 남자로서는 비참한 일 이 이상 없다 # 여자노악세서리-로서는, 천한 일 이 이상 없다 \@, (이)라고는 생각하는데…, (이)군요
			CALL PRINT_SYMBOL_K14
		CASE 2
			PRINTFORMW 「학수 고대. 꼬리를 붙여 개가 되는 시간이야」
			PRINTFORMW 「 나의, 나만의, 온순한 개로 해 주어요……」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT_23_1, 항문비즈해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·구슬이 5개 들어가 있는 경우, 항문비즈해제가 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	PRINTFORMW 「어떻게든 해들…, 빠지는 순간이 기분 좋을 것입니다…?」 
	PRINTFORMW 「한 개씩 뽑아 주기 때문에, 즐겨 주세요」
	PRINTFORMW 「그렇지 않으면 단번에 뽑아 내지는 편이 좋을까, 응?」
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%……」")
	CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%……」")
	CLEARLINE 1
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT23_2, 항문비즈추가
;━━━━━━━━━━━━━━━━━━━━━━━━
;·비즈의 추가는 1～3의 사이에 랜덤으로 정해진다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	SELECTCASE RANDACT(2, 20)
		CASE 0
			PRINTFORMW 「아직 들어가는 것일까? 가요……, 이봐요, 힘을 빼 삼키며」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%왜냐하면, 사실은 아직 어딘지 부족하겠지요? 후후…」
		CASE 1
			PRINTFORMW 「후후, 아직들이 그렇게. 이번은 어디까지 안되는 것일까」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%왜냐하면 코레, 반드시 싫지만은 않겠지요?」
	ENDSELECT
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT24, Ｃ애무도구
;────────────────────────────────────
;·클리캡/오나홀의장착, 해제
;·오나홀을 소지 또한죠교대상에페니스가 있는 경우는 「오나홀」에 파생한다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"의 경우는 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드)장착중에는 발생하지 않는다
;·오나홀장착는발기도가 1000 이하(TCVAR:MASTER:발기도 < 1000)의 경우에 연주해져 발생하지 않는다
;·성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K14_24
;-------------------------------------------------
;·TFLAG:ACT파생(0=클리캡장착/1=클리캡해제/2=오나홀장착/3=오나홀해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_0, 클리캡장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	PRINTFORMW 「%CNAME(MASTER, TARGET)%의 코코, 외로운 것 같기 때문에 이것을 대어 준다」
	PRINTFORMW 「곧바로 끝나기 때문에 얌전하게 하고 있어…」
	PRINTFORMDL %CALLNAME%(은)는 %CALLNAME:MASTER%의 질구로부터 떠올려 취한 애액으로 클리토리스를 적셔,
	PRINTFORMDW 구리 캡을 끼어 넣으려 하고 있다…
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_1, 클리캡해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 클리캡해제가 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	IF TCVAR:MASTER:가버려
		PRINTFORMW 「제외해요. 좋을까?」
	ELSEIF PALAM:MASTER:쾌Ｃ >= 4000
		PRINTFORMW 「그러면, 제외해요」
	ELSE
		PRINTFORMW 「, 제외해요」
	ENDIF
	PRINTFORMDL %CALLNAME%(은)는 %CALLNAME:MASTER%의 모습을 엿보면서,
	PRINTFORMDW 클리토리스를 흡인하면서 캡을 이끌어, 떼어내려 하고 있다…
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_2, 오나홀장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「그러나 진짜, 다양한 것이 있군요. 이런 건, 도대체 누가 생각할까」
		PRINTFORMW 「그야말로 촉감이 좋은 것 같은 재질이지만…. 그런데, 안은 어떻게든 해들…」
		PRINTFORMDW %CALLNAME%(은)는 오나홀안에 손가락을 넣어 조금 놀란 것 같은 표정을 보인다…
		PRINTFORMW 「이나다, 뭐야 이것. 좀, 생각했던 것보다…, 자주(잘) 할 수 있어……」
		PRINTFORMW 「이라고 말하는지,%CNAME(MASTER, TARGET)%의 경우……, 반드시 마음에 들거라고 생각해요, 코레」
		;페이드 아웃
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「フフ…」")
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후…」")
		SIF ANIME_CONFIG("FADE")
			CLEARLINE 1
		RETURN 1
	ENDIF
	
	;=================================================
	;눈가리개장착중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:눈가리개
		PRINTFORMW 「응… 지금, 무엇 되고 있어 아는 것일까?」
		PRINTFORM 「후후, 이봐요 어때? 평상시와 다른 감각을 즐겨 주세요
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;-----------------------------------
	;그 외
	;-----------------------------------
	SELECTCASE RANDACT(2, 0)
		CASE 0
			PRINTFORMW 「사실, 자주(잘) 되어있어요. 코레…」
			PRINTFORM 「, 준비는 좋을까. 정령들, 넋을 잃을 때까지 즐겨 주세요
			CALL PRINT_SYMBOL_K14
		CASE 1
			PRINTFORMW 「응, 그렇구나.%CNAME(MASTER, TARGET)%에는 코레, 조금 너무 클지도 모르네요」
			PRINTFORMW 「이라고 말해지면(자),\@ TALENT:MASTER:남자 ? 남자는 # 당신은 \@손상될까. 응?」
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%RANDTEXT_K14("フフフ", "クスクス")%…」")
			CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%RANDTEXT_K14("후후후", "킥킥")%…」")
			CLEARLINE 1
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_3, 오나홀해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 오나홀해제가 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;=================================================
	;정액 서로 더러워져
	;=================================================
	IF 구상기입체크(100) && GET_STAIN("페니스", "정액", MASTER)
		SELECTCASE RANDACT(3, 1)
			CASE 0
				PRINTFORMW 「응, 안으로부터 정액이 흘러넘쳐 유곽. 응만 내, 완전히…」
			CASE 1
				PRINTFORMW 「그러나, 상당히 털어 놓은 것…. 안의 정액이 새어 유곽」
			CASE 2
				PRINTFORMW 「이 모습이라고, 제외한 순간에 정액이 흘러넘쳐버릴 것 같구나」
		ENDSELECT
		PRINTFORMW 「이봐요, 제외해요. 조금 심하게 될 것 같지만. 후후」
		RETURN 1
	ENDIF
	
	;-----------------------------------
	;그 외
	;-----------------------------------
	SELECTCASE RANDACT(3, 2)
		CASE 0
			PRINTFORMW 「빠듯이까지 뽑아…… 그것!」
			PRINTFORMW 「후후후…%CNAME(MASTER, TARGET)%, 보기 흉하고 한심해 매우 좋은 느껴라」
		CASE 1
			PRINTFORMW 「유감이지만,%CNAME(MASTER, TARGET)%의 정말 좋아하는 오나호와는 작별의 시간이야」
			PRINTFORMW 「이봐요, 왜? 분명하게 사요나라 해 주세요. 후후…」
		CASE 2
			PRINTFORMW 「즐기고 있다고(면) 개 나쁘지만, 벌써 끝이야」
			PRINTFORM 「다음은 또 다음에, 군요…
			CALL PRINT_SYMBOL_K14
	ENDSELECT
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT25, 유두캡
;────────────────────────────────────
;·유두캡장착/해제
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·유두캡라고 말해도, 지문에서는 클립로터라고 하는 취급
;-------------------------------------------------
@KOJO_ACT_K14_25
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT25_0, 유두캡장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	PRINTFORMDL %CALLNAME%%NIPPLE_DESCRIPTION(MASTER)%%CALLNAME:MASTER%의 유두를 가볍게 혀로 굴리면(자),
	PRINTFORMDW 클립 로터를 설치하려고 했다…
	SELECTCASE RANDACT(3, 0)
		CASE 0
			PRINTFORMW 「그런데, 이것으로%CNAME(MASTER, TARGET)%의 유두를 사이에 두는 것이지만…」
			PRINTFORMW 「조금 아플 정도의 (분)편이, 나중에보다 느껴진다고 생각해」
			PRINTFORMW 「조금의 사이, 얌전하게 하고 있어 주세요…」
		CASE 1
			PRINTFORM 「분명하게 조정했기 때문에 아프지 않을 것? 그 중 지와지와 오기 때문에, 차분히 즐겨 주세요
			CALL PRINT_SYMBOL_K14
		CASE 2
			PRINTFORMW 「그렇게 말하면 코레, 사이에 두는 힘을 조절할 수 있어」
			PRINTFORMW 「아픈 것과 그렇지 않은거야.%CNAME(MASTER, TARGET)%는 어느 쪽이 기호일까?」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT25_1, 유두캡해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 유두캡해제가 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;죠교대상쾌Ｂ파생
	SELECTCASE PALAM:MASTER:쾌Ｂ
		CASE IS >= 8500
			PRINTFORMW 「제외해요. 좋을까?」
		CASE IS >= 5000
			PRINTFORMW 「그러면, 제외해요」
		CASEELSE
			PRINTFORMW 「, 제외해 주어요」
	ENDSELECT
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT26, 페니스밴드
;────────────────────────────────────
;·페니스밴드장착/해제
;·죠교자가죠교대상에 쌍두페니스밴드장착 시키려고 하는 장면
;·죠교대상가처녀라면, 처녀상실가 일어난다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·TEQUIP:(항문비즈/항문바이브/관장그릇＋플러그/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·죠교대상에페니스가 있으면 발생하지 않는다
;·TEQUIP(바이브/클리캡)는 양립하지 않는 것으로서 해제된다
;-------------------------------------------------
@KOJO_ACT_K14_26
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT26_0, 페니스밴드를장착시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「이봐요, 단념해 얌전하게 해 주세요, 붙이기 어려워요」
		PRINTFORMW 「거기에……, 이것은%CNAME(MASTER, TARGET)%에 있어서도 나쁜 이야기가 아니라고 생각하지만?」
		IF TALENT:MASTER:처녀
			PRINTFORMW 「우도…, 이것으로%CNAME(MASTER, TARGET)%(은)는 처녀를 잃는 일이 되는 것이지만」
			PRINTFORMW 「아무튼, 그것은 그 거 일로……」
		ELSE
			PRINTFORMW 「후후후…」
		ENDIF
		RETURN 1
	ENDIF
	
	;-----------------------------------
	;그 외
	;-----------------------------------
	PRINTFORMW 「완전히 손에 익은 것? 이제 내가 도울 필요없지 않을까」
	PRINTFORMW 「후후후……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT26_1, 페니스밴드를 해제시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	PRINTFORMW 「, 제외해요. 얌전하게 하고 있어……」
	RETURN 1
ENDIF
