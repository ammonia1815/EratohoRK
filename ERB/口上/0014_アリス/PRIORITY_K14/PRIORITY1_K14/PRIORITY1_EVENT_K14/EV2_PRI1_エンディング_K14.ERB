;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	엘리스구상 PRIORITY1, エンディング구상ファイル
;
;		エンディング이벤트구상のファイル
;
;		PRIORITY1の条件を満た했다場合にのみ表示
;
;Version		Reason
;1.70			新規作成
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV2, エンディング이벤트구상
;========================================================================
@KOJO_PRI1_MS_EVENT_K14_2(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;PRIORITY1, エンディング
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && KFLAG:(FLAG:주인님):100 == 1 && FIRSTEVENT(0)
	[SKIPSTART]
	;現状では달성するの至難。ギミック仕込んで何とかって感じ
	;=================================================
	;죠교자が依存
	;=================================================
	IF 구상記入チェック(0) && CFLAG:TARGET:의존도 > CFLAG:MASTER:의존도
	ENDIF
	
	;普通に進めるとだいたいコレ
	;=================================================
	;죠교대상が依存
	;=================================================
	IF 구상記入チェック(0) && CFLAG:TARGET:의존도 < CFLAG:MASTER:의존도
		;15000以上になるとちょっと難しい
		;-------------------------------------------------
		;죠교대상の의존도15000以上
		;-------------------------------------------------
		IF 구상記入チェック(0) && CFLAG:MASTER:의존도 > 15000
		ENDIF
		;その他
	ENDIF
	[SKIPEND]
	
	;-------------------------
	;汎用処理
	;-------------------------
	CALL ENDING_K14
	KFLAG:(FLAG:주인님):102 = 1
	RETURN 1
ENDIF
