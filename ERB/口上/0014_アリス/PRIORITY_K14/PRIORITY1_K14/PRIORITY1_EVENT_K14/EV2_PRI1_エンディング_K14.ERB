;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	엘리스구상 PRIORITY1, 엔딩구상파일
;
;		엔딩이벤트구상의 파일
;
;		PRIORITY1의 조건을 만했다경우에게만 표시
;
;Version		Reason
;1. 70			신규 작성
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV2, 엔딩이벤트구상
;========================================================================
@KOJO_PRI1_MS_EVENT_K14_2(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;PRIORITY1, 엔딩
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && KFLAG:(FLAG:주인님):100 == 1 && FIRSTEVENT(0)
	[SKIPSTART]
	;현재 상태로서는달성 하는 것 지난. 특수 효과 가르쳐 어떻게든이라는 느낌
	;=================================================
	;죠교자가 의존
	;=================================================
	IF 구상기입 체크(0) && CFLAG:TARGET:의존도 > CFLAG:MASTER:의존도
	ENDIF
	
	;보통으로 진행하면 대체로 이것
	;=================================================
	;죠교대상가 의존
	;=================================================
	IF 구상기입 체크(0) && CFLAG:TARGET:의존도 < CFLAG:MASTER:의존도
		;15000이상이 되면 조금 어렵다
		;-------------------------------------------------
		;죠교대상의의존도 15000 이상
		;-------------------------------------------------
		IF 구상기입 체크(0) && CFLAG:MASTER:의존도 > 15000
		ENDIF
		;그 외
	ENDIF
	[SKIPEND]
	
	;-------------------------
	;범용 처리
	;-------------------------
	CALL ENDING_K14
	KFLAG:(FLAG:주인님):102 = 1
	RETURN 1
ENDIF
