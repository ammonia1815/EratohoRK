;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	엘리스구상(대상限定) 회화系アクション구상ファイル
;
;		회화系アクション구상をまとめたファイル
;
;		EXTRA모드でNO:9사쿠야をMASTERに선택했다場合のみ表示
;
;Version		Reason
;1.70			@구상記入チェック仕様に。その他細かな修正
;1.62			一部条件の見直し。誤字、脱字修正
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화系 ACT0～9
;========================================================================
;・TFLAG:ACT(0=지금 기분을 묻다/1=자신에게의 마음을 묻다/2=성에 관한 이야기를 하다/3=상냥하게 위로한다/4=엄하게 위협한다/5=의상 변경을 요구한다/6=합의를 요구한다/7=징계한다고 위협한다/8=고함치다/9=기분나쁘게 웃다)
;・TFLAG:ACT분류 == 0に該当
;・ACT9, 기분나쁘게 웃다は이상系ACT분류から派生する場合がある
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "회화")で拾う事が可能
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では구상側のRESULT(終端のRETURN 1など)によって구상が表示당했다のか否か、
;・また箇所毎の特有の挙動(미약等구상をスキップなど)を制御しています
;・@구상記入チェックの場合代入処理を省略できますが、RESULT値は구상側で決めうちする필요があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------
;関連変数…
;FIRSTACT
;처음ACT判定関数
;TFLAG:ACT（TFLAG:90）を自動で参照、現在実行されているACTごとに번호が独立して保存される
;-------------------------------------------------
;────────────────────────────────────
;NO9_ACT0, 지금 기분을 묻다 FIRSTACT(0) RANDACT(0)(1)
;────────────────────────────────────
;・今の気分について聞いてくる
;・죠교대상の反応に応じてREACT以降でACT파생への代入が行われる
;・턴방침が"이상"なら発生しない
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_0
;-------------------------------------------------
;@EQUIPACTION_DESCRIPTIONが絡むところは、とりあえず書き分けないとって感じ？
;まぁ面倒だし、並び順も頭が痛いところ。一部TEQUIP分岐は何処かに統合してもよさそうだが
;というかこれ、REACTも相応にシチュエーション絞ったもの書く필요があるなぁ
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT0, 지금 기분을 묻다
;RESULT値（=0, 汎用ACTに流れる、1, これ以上表示しない、2, 表示して미약等구상をスキップ）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=============================================
	;처음
	;=============================================
	IF 구상記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「응, 기분은 어떻게든 해들？」
		PRINTFORMW 「그렇게 단단해지는 일 없어요. 얌전하게 하고 있으면 귀여워해 주기 때문에」
		RETURN 1
	ENDIF
	
	;=============================================
	;성교系で질내사정의後
	;=============================================
	IF 구상記入チェック(100) && TFLAG:전회질싸 == 1
		PRINTFORMW 「%CNAME(MASTER, TARGET)%에 질내사정…최고군요♪」
		PRINTFORMW 「응, 나에게 정복 된 기분은 어떻게 까？」
		RETURN 1
	ENDIF
	
	;=============================================
	;イキそう
	;=============================================
	IF 구상記入チェック(100) && TCVAR:MASTER:가버려
		;-------------------------------------------------
		;イキそうなのを참다している
		;-------------------------------------------------
		IF 구상記入チェック(100) && TCVAR:MASTER:가버려 > 5
			PRINTFORMW 「어머나, 노력해요？」
			PRINTFORM 「%CNAME(MASTER, TARGET)%가 언제까지 견딜 수 있는지, 시험해 보는 것도 나쁘지 않네요
			CALL PRINT_SYMBOL_K14
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;애태움中
		;-------------------------------------------------
		IF 구상記入チェック(100) && TCVAR:MASTER:애태움도 > 1
			PRINTFORMW 「응, 초조하게 해지고 있는 기분은 어때？」
			PRINTFORMW 「간단하게는 이나 등 있고 원이야, 나쁜데. 후후…」
			RETURN 1
		ENDIF
		
		;-------------------------------
		;その他
		;-------------------------------
		PRINTFORMW 「후후, 이키 그런？」
		PRINTFORMW 「 그렇지만, 간단하게 이나 키라고는 재미있지 않아」
		RETURN 1
	ENDIF
	
	;=============================================
	;볼개그장착中
	;=============================================
	IF 구상記入チェック(100) && TEQUIP:볼개그
		PRINTFORMW 「후후, 좋은 모습이군요. 지금의 기분은 어떻게 까？」
		PRINTFORMW 「이라고 말해도 대답할 수 없겠지만, 이군요♪」
		RETURN 1
	ENDIF
	
	;=============================================
	;눈가리개장착中
	;=============================================
	IF 구상記入チェック(100) && TEQUIP:눈가리개
		PRINTFORMW 「시야를 빼앗긴 기분은 어떻게든 해들？」
		RETURN 1
	ENDIF
	
	;=============================================
	;안면승마中
	;=============================================
	IF 구상記入チェック(100) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
		PRINTFORMW 「응 사쿠야. 나에게 이런 일 되고 있는 기분은 어떻게든 해들？」
		PRINTFORMW 「 그렇지만 이 상태에서는 대답할 수 없을까？ 후후후…」
		RETURN 1
	ENDIF
	
	;=============================================
	;자위중
	;=============================================
	IF 구상記入チェック(100) && TEQUIP:자위중
		;-------------------------------------------------
		;죠교대상が자위중
		;-------------------------------------------------
		IF 구상記入チェック(100) && GETBIT(TEQUIP:자위중, 0)
			SELECTCASE ABL:MASTER:노출증
				;노출증3以上
				CASE IS >= 3
					PRINTFORMW 「 나로 보여져 불필요하게 흥분하고 있는 것 같구나？」
				;その他
				CASEELSE
					PRINTFORMW 「응 사쿠야. 아직 볼 수 있는 것은 부끄럽다？」
			ENDSELECT
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;죠교자が자위중
		;-------------------------------------------------
		IF 구상記入チェック(100) && GETBIT(TEQUIP:자위중, 1)
			;눈가리개장착中
			IF TEQUIP:눈가리개
				SELECTCASE RANDACT(2, 0)
					CASE 0
						PRINTFORMW 「응 사쿠야. 들리는 것일까？ 이 소리…」
					CASE 1
						PRINTFORMW 「안보여도 알겠지요？ 지금, 내가 무엇을 하고 있을까…」
				ENDSELECT
			;その他
			ELSE
				SELECTCASE RANDACT(2, 1)
					CASE 0
						PRINTFORMW 「 나의 이런 모습 보이고 붙일 수 있어, 어떤 기분일까？」
					CASE 1
						PRINTFORMW 「응 사쿠야. 스스로도 시 싶게 안 된다？」
				ENDSELECT
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=============================================
	;성교중
	;=============================================
	IF 구상記入チェック(100) && TEQUIP:성교중
		SELECTCASE TEQUIP:성교중
			;정상위, 기승위
			CASE 1, 3
				;前COM派生処理
				SELECTCASE TRAINNAME:PREVCOM
					;허리를 흔든다
					CASE "허리를 흔든다"
						PRINTFORMW 「그렇게 허리 털어 버려서, 즐겨 줄 수 있고 있는 것 같구나？」
					;적극的反応
					CASE "쾌감을 받아들인다", "적극적으로 한다", "애정을 담아 한다"
						PRINTFORMW 「후후, 즐겨 줄 수 있고 있는 것 같구나」
					;その他
					CASEELSE
						PRINTFORMW 「즐기지 않은 것 같구나. 그다지 시 싶지 않은거야？」
				ENDSELECT
			;후배위
			CASE 2
				;前COM派生処理
				SELECTCASE TRAINNAME:PREVCOM
					;허리를 흔든다
					CASE "허리를 흔든다"
						PRINTFORMW 「뒤로부터 범해지는 것이 흥분할까？ 응 사쿠야…」
					;적극的反応
					CASE "쾌감을 받아들인다", "적극적으로 한다", "애정을 담아 한다"
						PRINTFORMW 「귀부인…이런 식으로 범해지는 것이 좋아하는 것 같구나？」
					;その他
					CASEELSE
						PRINTFORMW 「뒤로부터 되는 것은 혐？」
				ENDSELECT
			;대면좌위, 배면좌위
			CASE 4, 5
				;前COM派生処理
				SELECTCASE TRAINNAME:PREVCOM
					;허리를 흔든다
					CASE "허리를 흔든다"
						PRINTFORM 「완전히 흥분하고 있는 것 같구나？ 포옹되고 있다고(면) 흥분할까, 응 사쿠야
						CALL PRINT_HEARTCBW_K14(COLOR2_K14("pink"), , , COLOR2_K14("ALICE"))
					;적극的反応
					CASE "쾌감을 받아들인다", "적극적으로 한다", "애정을 담아 한다"
						PRINTFORMW 「이 모습으로 되는 것, 좋아하는 것 같구나？」
					;その他
					CASEELSE
						PRINTFORMW 「기분이 타지 않는 것 같구나？ 그다지 시 싶지 않은 것일까」
				ENDSELECT
			;항문섹스
			CASE 6
				;前COM派生処理
				SELECTCASE TRAINNAME:PREVCOM
					;허리를 흔든다
					CASE "허리를 흔든다"
						PRINTFORMW 「후후, 엉덩이가 그렇게 기분 좋은 것일까？」
					;적극的反応
					CASE "쾌감을 받아들인다", "적극적으로 한다", "애정을 담아 한다"
						PRINTFORMW 「귀부인…엉덩이로 하는 것을 좋아하는？ 후후」
					;その他
					CASEELSE
						PRINTFORMW 「엉덩이로 하는 것은 혐？」
				ENDSELECT
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=============================================
	;삼각목마사용중
	;=============================================
	IF 구상記入チェック(100) && TEQUIP:삼각목마
		PRINTFORMW 「 나라도 생각해？ 이런 상황으로 기분은 (들)물어서는 안된다고」
		PRINTFORMW 「후후후…」
		RETURN 1
	ENDIF
	
	;=============================================
	;죠교대상の상태が분노
	;=============================================
	IF 구상記入チェック(100) && M_COND("분노")
		PRINTFORMW 「상당히 기분이 나쁜 것 같구나？」
		PRINTFORMW 「도대체 무엇이 그렇게 재미있지 않은 것일까」
		RETURN 1
	ENDIF
	
	;통상ならもっと上層でいいかもだけれどTEQUIP系を食いたくないとかそんな感じで
	;=============================================
	;その죠교の最初のアクション
	;=============================================
	IF 구상記入チェック(100) && TFLAG:경과시간 == 1
		;상사상애
		IF AFFECTION("상사상애")
			PRINTFORMW 「응, 기분은 어때？ 라고 말할까…」
			PRINTFORMW 「내가 말을 건네고 있기 때문에, 좀 더 기쁜 듯한 얼굴 해 주세요！」
		;それ以外
		ELSE
			PRINTFORMW 「정직,%CNAME(MASTER, TARGET)%의 지금의 기분이라든지에 흥미없는거야…」
			PRINTFORMW 「 나의 하고 싶게 해요… 우후후…」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「응 사쿠야. 기분은 어떠세요？」
	RETURN 1
ENDIF
;회화系は根本的に見直す필요あり

;────────────────────────────────────
;NO9_ACT1, 자신에게의 마음을 묻다
;────────────────────────────────────
;・自分のことをどう思うか聞く
;・죠교대상の反応に応じてREACT以降でACT파생への代入が行われる
;・턴방침が"이상"なら発生しない
;・話題『호의』が既出となり、以後、同一조교중では抑制される
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_1
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT1, 자신에게의 마음을 묻다
;RESULT値（=0, 汎用ACTに流れる、1, これ以上表示しない、2, 表示して미약等구상をスキップ）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;처음
	;=================================================
	;IF 구상記入チェック(0) && FIRSTACT(0)
	;ENDIF
	
	;=================================================
	;その죠교の最初のアクション
	;=================================================
	IF 구상記入チェック(100) && TFLAG:경과시간 == 1 && ONCEACT(0)
		PRINTFORMW 「응…조금 들려주었으면 좋지만」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 나의 일 어떻게 생각하고 있는 거나？」
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마中
	;=================================================
	;IF 구상記入チェック(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
	;ENDIF
	
	;=================================================
	;자위중
	;=================================================
	;IF 구상記入チェック(0) && TEQUIP:자위중
	;ENDIF
	
	;=================================================
	;성교중
	;=================================================
	;IF 구상記入チェック(0) && TEQUIP:성교중
	;ENDIF

	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「%CNAME(MASTER, TARGET)%(은)는 나의 일 어떻게 생각하고 있는 거나？」
	PRINTFORMW 「응, 들려주어 주세요…」
	RETURN 1
ENDIF

;────────────────────────────────────
;NO9_ACT2, 성에 관한 이야기를 하다
;────────────────────────────────────
;・죠교자が性についての지식を語り始める
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"이상"だと発生しない
;・죠교자に성지식が無いと発生しない
;・話題『지식』が既出となり、以後、同一조교중では抑制される
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_2
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT2, 성에 관한 이야기를 하다
;RESULT値（=0, 汎用ACTに流れる、1, これ以上表示しない、2, 表示して미약等구상をスキップ）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0)
[SKIPSTART]
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
	ENDIF
	;=================================================
	;안면승마中
	;=================================================
	IF 구상記入チェック(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
	ENDIF
	;=================================================
	;자위중
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:자위중
	ENDIF
	;=================================================
	;성교중
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:성교중
	ENDIF
	;=================================================
	;その죠교の最初のアクション
	;=================================================
	IF 구상記入チェック(0) && TFLAG:경과시간 == 1
	ENDIF
[SKIPEND]
ENDIF

;────────────────────────────────────
;NO9_ACT3, 상냥하게 위로한다
;────────────────────────────────────
;・죠교자が優しい言葉で慰めてくる
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"하드", "이상"だと発生しない
;・話題『慰め』が既出となり、以後、同一조교중では抑制される
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_3
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT3, 상냥하게 위로한다 
;RESULT値（=0, 汎用ACTに流れる、1, これ以上表示しない、2, 表示して미약等구상をスキップ）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;성교系で질내사정의後
	;=================================================
	IF 구상記入チェック(100) && TFLAG:전회질싸 == 1
		PRINTFORMW 「걱정하지 말아줘…」
		PRINTFORMW 「데 와 버렸다들 , 분명하게 책임져 주기 때문에♪」
		RETURN 1
	ENDIF
	
	[SKIPSTART]
	;==================================================
	;안면승마中
	;==================================================
	IF 구상記入チェック(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
	ENDIF
	
	;==================================================
	;자위중
	;==================================================
	IF 구상記入チェック(0) && TEQUIP:자위중
		PRINTW
	ENDIF
	
	;==================================================
	;성교중
	;==================================================
	IF 구상記入チェック(0) && TEQUIP:성교중
	ENDIF
	
	;==================================================
	;イキそう
	;==================================================
	IF 구상記入チェック(0) && TCVAR:MASTER:가버려
		;-------------------------------------------------
		;イキそうなのを참다している
		;-------------------------------------------------
		IF 구상記入チェック(0) && TCVAR:MASTER:가버려 > 5
			PRINTFORMW 「인내는 하지 않고 이는 끝내세요」
			PRINTFORMW 「 그렇지만, 참고 있는 모습은 조금 사랑스러울지도…후후후」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;애태움中
		;-------------------------------------------------
		IF 구상記入チェック(0) && TCVAR:MASTER:애태움도 > 1
			PRINTFORMW 「어때？ 초조하게 해지는 것은 괴로울까？」
			PRINTFORML 「 내가 기분이 내키면(자), 이나 키라고 주어도 괜찮아요」
			PRINTFORMW 「후후후……」
			RETURN 1
		ENDIF
		
		;-------------------------------
		;その他
		;-------------------------------
		PRINTFORMW 「이키 그런？」
		PRINTFORMW 「기다리고 있어…곧바로 이나 키라고 주기 때문에♪」
		RETURN 1
	ENDIF
	[SKIPEND]
	
	;-------------------------
	;その他
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMDW 앨리스는 사쿠야의 머리를 상냥하게 어루만졌다…
		PRINTFORMW 「후후, 좋아 좋아♪」
	ELSE
		PRINTFORMW 「 나도 너무 심한은 하고 싶지 않아, 사쿠야」
		PRINTFORMW 「얌전하게 하고 있으면, 나쁘게는 하지 않아요」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT4, 엄하게 위협한다
;────────────────────────────────────
;・厳しい言葉で脅す
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"소프트", "이상"だと発生しない
;・話題『脅す』が既出となり、以後、同一조교중では抑制される
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT4, 엄하게 위협한다
;RESULT値（=0, 汎用ACTに流れる、1, これ以上表示しない、2, 表示して미약等구상をスキップ）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「기억해 두어 주세요」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%(을)를 어떻게 하는지, 모두 나나름도 일」
		PRINTFORMW 「이 의미를 모를 수록, 바보같지 않은 것을 기대하고 있는 원…」
		RETURN 1
	ENDIF
	
	;=================================================
	;성교系で질내사정의後
	;=================================================
	IF 구상記入チェック(100) && TFLAG:전회질싸 == 1
		PRINTFORMW 「\@ IS_PREVCOMNAME("안에 내면 안돼!") ? 억지로 # %CNAME(MASTER, TARGET)%에 \@질내사정, 최고군요. 후후」
		PRINTFORM 「%CNAME(MASTER, TARGET)%가 분명하게 잉까지, 매일질내에 흘러 들어가 주어요
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;イキそう
	;=================================================
	IF 구상記入チェック(100) && TCVAR:MASTER:가버려
		;-------------------------------------------------
		;イキそうなのを참다している
		;-------------------------------------------------
		IF 구상記入チェック(100) && TCVAR:MASTER:가버려 > 5
			PRINTFORMW 「쓸데없는 저항은 그만두면 어때…？」
			PRINTFORMW 「어차피 곧 이낳았기 때문에」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;애태움中
		;-------------------------------------------------
		IF 구상記入チェック(100) && TCVAR:MASTER:애태움도 > 1
			SELECTCASE RANDACT(2, 0)
				CASE 0
					PRINTFORMW 「어머나, 이키 그래？」
					PRINTFORMW 「안되어요, 사쿠야. 좀 더 참아 주세요…」
				CASE 1
					PRINTFORMW 「이 오고 싶어서 견딜 수 없는, 은 얼굴이군요？」
					PRINTFORMW 「이대로 쭉 초조하게 하고 있으면%CNAME(MASTER, TARGET)%, 어떻게 되어버릴까 해들. 응？」
			ENDSELECT
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「フフ……」")
			CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후……」")
			CLEARLINE 1
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;눈가리개장착中
	;=================================================
	IF 구상記入チェック(100) && TEQUIP:눈가리개
		PRINTFORMW 「시야를 빼앗은 귀부인을 방치하면 어떻게 되는 것일까？」
		PRINTFORMW 「조금 재미있을 것 같은 것은 않아요. 유석에 하지 않는데」
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「\@ TENSION(TARGET, 8) == "高い" ? クスクス # フフフ \@……」")
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「\@ TENSION(TARGET, 8) == "高い" ? 킥킥 # 후후후 \@……」")
		CLEARLINE 1
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「얌전하게 하고 있으면, 나쁘게는 하지 않아요」
	PRINTFORMW 「쓸데없는 저항은 그만두는 것이 현명해요」
	RETURN 1
ENDIF

;────────────────────────────────────
;NO9_ACT5, 의상 변경을 요구한다
;────────────────────────────────────
;・죠교자が의상の変更を要求してくる
;・죠교대상の反応に応じて、何枚脱ぐか、あるいは脱がないのかにREACT以降で派生する
;・TFLAG:ACT파생(0=服を脱ぐように命令/1=服を着るように命令)
;・TEQUIP:밀어넘어지는중には発生しない
;・TEQUIP:밧줄が真になっていると発生しない
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_5
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT5_0, 服を脱ぐように命令
;RESULT値（=0, 汎用ACTに流れる、1, これ以上表示しない、2, 表示して미약等구상をスキップ）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「, 옷을 벗어 주세요」 
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 모두, 봐요♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자が죠교대상を익애
	;=================================================
	IF 구상記入チェック(100) && AFFECTION("익애")
		PRINTFORMW 「, 벗어… 나에게 전부 보여 줘…」
		PRINTFORMW 「후후… 흥분해버린다♪」
		RETURN 1
	ENDIF

	;-------------------------
	;その他
	;-------------------------
	PRINTFORML 「그러면, 벗어 줄까」
	PRINTFORMW 「부끄러워하는 일은 없어요」
	PRINTFORMW 「천천히…나에게 잘 보이도록, 이군요♪」
	RETURN 1
ENDIF

;このあたり凡庸だな
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT5_1, 服を着るように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
	PRINTFORMW 「기분이 바뀌었어요. 응, 옷을 입어 주세요」
	PRINTFORMW 「어째서 사？ 별로 이유는 없어요」
	RETURN 1
ENDIF

;────────────────────────────────────
;NO9_ACT6, 합의를 요구한다
;────────────────────────────────────
;・처음を貰ってもいいかと聞く
;・처음＝처녀or동정(両方の場合もある)
;・既に합의を得ている場合は発生しない
;・죠교대상が처녀でも동정でもない場合は発生しない
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"이상"だと発生しない
;・話題『합의』が既出となり、以後、同一조교중では抑制される
;-------------------------------------------------
;@KOJO_NO9_MS_ACT_KXX_6
;-------------------------------------------------
;사쿠야さん처녀じゃないんでｗ
;-------------------------------------------------

;────────────────────────────────────
;NO9_ACT7, 징계한다고 위협한다
;────────────────────────────────────
;・これから징계・すると言い出す
;・このACTが発生している時点でTFLAG:징계・플래그は立っている
;・턴방침が"소프트", "이상"だと発生しない
;・TEQUIP:밀어넘어지는중時には発生しない
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT7, 징계한다고 위협한다
;RESULT値（=0, 汎用ACTに流れる、1, これ以上表示しない、2, 表示して미약等구상をスキップ）
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM側の処理と上손く繋がるよう調整すべき
;1.70以降に一度見直すか…
;------------------------------------------------
IF 구상記入チェック(100)
	;宣言했다ら実行してもらわないとね
	TCVAR:행동예약 = 60
	;=================================================
	;前COMがが「무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다」のいずれか
	;=================================================
	IF 구상記入チェック(100) && IS_PREVCOMNAME("무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 「 아직 자신의 입장이 알지 않게？ ……아휴예요」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 경우, 말로 전하는 것보다, 좀 더 직접적인 방법이 좋은 것일까. 응？」
		RETURN 1
	ENDIF
	
	;=================================================
	;前COMが「기분좋게 해」
	;（この恥知らず！）
	;=================================================
	IF 구상記入チェック(100) && IS_PREVCOMNAME("기분좋게 해")
		PRINTFORMW 「귀부인이 그런 일 말하다니 , 사쿠야」
		PRINTFORMW 「 그렇지만, 지금의 입장을 분별해 주지 않으면 곤란해요」
		RETURN 1
	ENDIF
	
	;=================================================
	;前COMが「더 하드하게 해」
	;（この豚め！）
	;=================================================
	IF 구상記入チェック(100) && IS_PREVCOMNAME("더 하드하게 해")
		PRINTFORMW 「기가 막힌 원이군요, 사쿠야. 귀부인이 그런 일 말하다니」
		PRINTFORMW 「정말로 지금의 상황을 알 수 있고 있을까？」
		RETURN 1
	ENDIF
	
	;=================================================
	;前COMが「용서를 빌다」
	;（許してもらえませんで했다）
	;=================================================
	IF 구상記入チェック(100) && IS_PREVCOMNAME("용서를 빌다")
		PRINTFORMW 「안되어요, 허락할 생각은 없는 것」
		PRINTFORMW 「조금 징계나무가 필요같구나」
		RETURN 1
	ENDIF
	
	;=================================================
	;前COMが「자위를 시작한다」
	;（人の話を聞け！）
	;=================================================
	IF 구상記入チェック(100) && IS_PREVCOMNAME("자위를 시작한다")
		PRINTFORMW 「무엇을 마음대로 시작해 버린다 의 것인지 해들 」
		PRINTFORMW 「이것은 징계나무가 필요할까」
		;TCVAR:행동예약 = 60
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「분수를 분별하지 않는 이 살인 실업 수당에는…」
	PRINTFORMW 「아무래도 징계나무가 필요 보고 싶은, 이군요」
	RETURN 1
ENDIF

;────────────────────────────────────
;NO9_ACT8, 고함치다
;────────────────────────────────────
;・大声で고함치다
;・턴방침が"소프트", "노멀"では発生しない
;・TFLAG:징계・플래그に関わらず、発生する可能性がある（징계・中にさらに고함치다可能性等）
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT8, 고함치다
;RESULT値（=0, 汎用ACTに流れる、1, これ以上表示しない、2, 表示して미약等구상をスキップ）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;前COMがが「무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다」のいずれか
	;=================================================
	IF 구상記入チェック(100) && IS_PREVCOMNAME("무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 「진짜, 거역하고만 있어 초조해해요…」
		PRINTFORMW 「적당, 화가 나！ 그러한 것！」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「사실…, 사랑스럽지 않아요.%CNAME(MASTER, TARGET)%…」
	PRINTFORMW 「그 반항적인 얼굴도 초조해해！」 
	RETURN 1
ENDIF

;────────────────────────────────────
;NO9_ACT9, 기분나쁘게 웃다
;────────────────────────────────────
;・不気味な微笑を浮かべる
;・이상系から発生する場合もある
;・금일의방침が"노닥노닥"だと発生しない
;・話題『嗤う』が既出となり、以後、同一조교중では抑制される
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT9, 기분나쁘게 웃다
;RESULT値（=0, 汎用ACTに流れる、1, これ以上表示しない、2, 表示して미약等구상をスキップ）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;눈가리개장착中
	;=================================================
	IF 구상記入チェック(100) && TEQUIP:눈가리개
		PRINTFORMDW 시야를 빼앗겼다%CALLNAME:MASTER%의 귀에, 앨리스의 기분 나쁜 웃음소리가 닿는다…
		RETURN 1
	ENDIF
	
	;MENUMATCH等を通すと派生に対応してしまうのでむしろTFLAGそのままがいいとかそんなところ
	;まぁもう少しやりようがある感じではある
	;笑い系はBASEやらFAVORやらMENUMATCHで判定して関数化してもいいのかもしれないとか思いついたけど後回しにする
	;=================================================
	;이상系から発生
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT분류 == 7
		PRINTFORMDW 앨리스의 기분 나쁜 웃음소리가 방에 울리고 있다…
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMDW 앨리스의 기분 나쁜 웃음소리가 방안에 영향을 주어 건넌다…
	RETURN 1
ENDIF
;회화系はちょっと梃入れする필요があるよね
