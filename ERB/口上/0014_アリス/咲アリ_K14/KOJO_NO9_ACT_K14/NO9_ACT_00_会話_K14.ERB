;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	엘리스구상(대상한정) 회화계 액션구상파일
;
;		회화계 액션구상를 정리한 파일
;
;		EXTRA모드로 NO:9사쿠야를 MASTER에선택했다경우만 표시
;
;Version		Reason
;1. 70			@구상기입 체크 사양에. 그 외 섬세한 수정
;1. 62			일부 조건의 재검토. 오자, 탈자 수정
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금 기분을 묻다/1=자신에게의 마음을 묻다/2=성에 관한 이야기를 하다/3=상냥하게 위로한다/4=엄하게 위협한다/5=의상 변경을 요구한다/6=합의를 요구한다/7=징계한다고 위협한다/8=고함치다/9=기분나쁘게 웃다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분나쁘게 웃다는이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;관련 변수…
;FIRSTACT
;처음ACT 판정 함수
;TFLAG:ACT（TFLAG:90）를 자동으로 참조, 현재 실행되고 있는 ACT 마다번호가 독립해 보존된다
;-------------------------------------------------
;────────────────────────────────────
;NO9_ACT0, 지금 기분을 묻다 FIRSTACT(0) RANDACT(0)(1)
;────────────────────────────────────
;·지금의 기분에 대해 들어 온다
;·죠교대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침가"이상"라면 발생하지 않는다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_0
;-------------------------------------------------
;@EQUIPACTION_DESCRIPTION가 관련되는 곳은, 우선 나누어 쓰지 않으면이라는 느낌?
;아무튼 귀찮고, 줄 순서도 머리가 아픈 곳. 일부 TEQUIP 분기는 어디엔가 통합해도 좋은 것 같지만
;라고 할까 이것, REACT도 상응하게 시추에이션 짠 것 쓰는필요가 있구나
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT0, 지금 기분을 묻다
;RESULT치（=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해미약등구상를 스킵）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=============================================
	;처음
	;=============================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「응, 기분은 어떻게든 해들?」
		PRINTFORMW 「그렇게 단단해지는 일 없어요. 얌전하게 하고 있으면 귀여워해 주기 때문에」
		RETURN 1
	ENDIF
	
	;=============================================
	;성교계로질내사정의 후
	;=============================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
		PRINTFORMW 「%CNAME(MASTER, TARGET)%에 질내사정…최고군요♪」
		PRINTFORMW 「응, 나에게 정복 된 기분은 어떻게 까?」
		RETURN 1
	ENDIF
	
	;=============================================
	;갈 것 같다
	;=============================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		;-------------------------------------------------
		;갈 것 같은 것을참다 하고 있다
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:MASTER:가버려 > 5
			PRINTFORMW 「어머나, 노력해요?」
			PRINTFORM 「%CNAME(MASTER, TARGET)%가 언제까지 견딜 수 있는지, 시험해 보는 것도 나쁘지 않네요
			CALL PRINT_SYMBOL_K14
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;애태움중
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:MASTER:애태움도 > 1
			PRINTFORMW 「응, 초조하게 해지고 있는 기분은 어때?」
			PRINTFORMW 「간단하게는 이나 등 있고 원이야, 나쁜데. 후후…」
			RETURN 1
		ENDIF
		
		;-------------------------------
		;그 외
		;-------------------------------
		PRINTFORMW 「후후, 이키 그런?」
		PRINTFORMW 「 그렇지만, 간단하게 이나 키라고는 재미있지 않아」
		RETURN 1
	ENDIF
	
	;=============================================
	;볼개그장착중
	;=============================================
	IF 구상기입체크(100) && TEQUIP:볼개그
		PRINTFORMW 「후후, 좋은 모습이군요. 지금의 기분은 어떻게 까?」
		PRINTFORMW 「이라고 말해도 대답할 수 없겠지만, 이군요♪」
		RETURN 1
	ENDIF
	
	;=============================================
	;눈가리개장착중
	;=============================================
	IF 구상기입체크(100) && TEQUIP:눈가리개
		PRINTFORMW 「시야를 빼앗긴 기분은 어떻게든 해들?」
		RETURN 1
	ENDIF
	
	;=============================================
	;안면승마중
	;=============================================
	IF 구상기입체크(100) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
		PRINTFORMW 「응 사쿠야. 나에게 이런 일 되고 있는 기분은 어떻게든 해들?」
		PRINTFORMW 「그렇지만 이 상태에서는 대답할 수 없을까? 후후후…」
		RETURN 1
	ENDIF
	
	;=============================================
	;자위중
	;=============================================
	IF 구상기입체크(100) && TEQUIP:자위중
		;-------------------------------------------------
		;죠교대상가자위중
		;-------------------------------------------------
		IF 구상기입체크(100) && GETBIT(TEQUIP:자위중, 0)
			SELECTCASE ABL:MASTER:노출증
				;노출증 3 이상
				CASE IS >= 3
					PRINTFORMW 「나로 보여져 불필요하게 흥분하고 있는 것 같구나?」
				;그 외
				CASEELSE
					PRINTFORMW 「응 사쿠야. 아직 볼 수 있는 것은 부끄럽다?」
			ENDSELECT
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;죠교자가자위중
		;-------------------------------------------------
		IF 구상기입체크(100) && GETBIT(TEQUIP:자위중, 1)
			;눈가리개장착중
			IF TEQUIP:눈가리개
				SELECTCASE RANDACT(2, 0)
					CASE 0
						PRINTFORMW 「응 사쿠야. 들리는 것일까? 이 소리…」
					CASE 1
						PRINTFORMW 「안보여도 알겠지요? 지금, 내가 무엇을 하고 있을까…」
				ENDSELECT
			;그 외
			ELSE
				SELECTCASE RANDACT(2, 1)
					CASE 0
						PRINTFORMW 「나의 이런 모습 보이고 붙일 수 있어, 어떤 기분일까?」
					CASE 1
						PRINTFORMW 「응 사쿠야. 스스로도 시 싶게 안 된다?」
				ENDSELECT
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=============================================
	;성교중
	;=============================================
	IF 구상기입체크(100) && TEQUIP:성교중
		SELECTCASE TEQUIP:성교중
			;정상위, 기승위
			CASE 1, 3
				;전COM 파생 처리
				SELECTCASE TRAINNAME:PREVCOM
					;허리를 흔든다
					CASE "허리를 흔든다"
						PRINTFORMW 「그렇게 허리 털어 버려서, 즐겨 주어지고 있는 것 같구나?」
					;적극적 반응
					CASE "쾌감을 받아들인다", "적극적으로 한다", "애정을 담아 한다"
						PRINTFORMW 「후후, 즐겨 줄 수 있고 있는 것 같구나」
					;그 외
					CASEELSE
						PRINTFORMW 「즐기지 않은 것 같구나. 그다지 시 싶지 않은거야?」
				ENDSELECT
			;후배위
			CASE 2
				;전COM 파생 처리
				SELECTCASE TRAINNAME:PREVCOM
					;허리를 흔든다
					CASE "허리를 흔든다"
						PRINTFORMW 「뒤로부터 범해지는 것이 흥분할까? 응 사쿠야…」
					;적극적 반응
					CASE "쾌감을 받아들인다", "적극적으로 한다", "애정을 담아 한다"
						PRINTFORMW 「귀부인… 이런 식으로 범해지는 것이 좋아하는 것 같구나?」
					;그 외
					CASEELSE
						PRINTFORMW 「뒤로부터 되는 것은 혐?」
				ENDSELECT
			;대면좌위, 배면좌위
			CASE 4, 5
				;전COM 파생 처리
				SELECTCASE TRAINNAME:PREVCOM
					;허리를 흔든다
					CASE "허리를 흔든다"
						PRINTFORM 「완전히 흥분하고 있는 것 같구나? 포옹되고 있다고(면) 흥분할까, 응 사쿠야
						CALL PRINT_HEARTCBW_K14(COLOR2_K14("pink"), , , COLOR2_K14("ALICE"))
					;적극적 반응
					CASE "쾌감을 받아들인다", "적극적으로 한다", "애정을 담아 한다"
						PRINTFORMW 「이 모습으로 되는 것, 좋아하는 것 같구나?」
					;그 외
					CASEELSE
						PRINTFORMW 「기분이 타지 않는 것 같구나? 그다지 시 싶지 않은 것일까」
				ENDSELECT
			;항문섹스
			CASE 6
				;전COM 파생 처리
				SELECTCASE TRAINNAME:PREVCOM
					;허리를 흔든다
					CASE "허리를 흔든다"
						PRINTFORMW 「후후, 엉덩이가 그렇게 기분 좋은 것일까?」
					;적극적 반응
					CASE "쾌감을 받아들인다", "적극적으로 한다", "애정을 담아 한다"
						PRINTFORMW 「귀부인… 엉덩이로 하는 것을 좋아하는? 후후」
					;그 외
					CASEELSE
						PRINTFORMW 「엉덩이로 하는 것은 혐?」
				ENDSELECT
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=============================================
	;삼각목마사용중
	;=============================================
	IF 구상기입체크(100) && TEQUIP:삼각목마
		PRINTFORMW 「나라도 생각해? 이런 상황으로 기분은 (들)물어서는 안된다고」
		PRINTFORMW 「후후후…」
		RETURN 1
	ENDIF
	
	;=============================================
	;죠교대상의상태가분노
	;=============================================
	IF 구상기입체크(100) && M_COND("분노")
		PRINTFORMW 「상당히 기분이 나쁜 것 같구나?」
		PRINTFORMW 「도대체 무엇이 그렇게 재미있지 않은 것일까」
		RETURN 1
	ENDIF
	
	;통상라면 좀 더 상층에서 좋을지도이지만 TEQUIP계를 먹고 싶지 않다든가 그런 느낌으로
	;=============================================
	;그죠교의 최초의 액션
	;=============================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1
		;상사상애
		IF AFFECTION("상사상애")
			PRINTFORMW 「응, 기분은 어때? 라고 말할까…」
			PRINTFORMW 「내가 말을 건네고 있기 때문에, 좀 더 기쁜 듯한 얼굴 해 주세요!」
		;그 이외
		ELSE
			PRINTFORMW 「정직,%CNAME(MASTER, TARGET)%의 지금의 기분이라든지에 흥미없는거야…」
			PRINTFORMW 「 나의 하고 싶게 해요… 우후후…」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「응 사쿠야. 기분은 어떠세요?」
	RETURN 1
ENDIF
;회화계는 근본적으로 다시 보는필요 있어

;────────────────────────────────────
;NO9_ACT1, 자신에게의 마음을 묻다
;────────────────────────────────────
;·자신의 일을 어떻게 생각할까 얘기한다
;·죠교대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침가"이상"라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_1
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT1, 자신에게의 마음을 묻다
;RESULT치（=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해미약등구상를 스킵）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	;IF 구상기입 체크(0) && FIRSTACT(0)
	;ENDIF
	
	;=================================================
	;그죠교의 최초의 액션
	;=================================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1 && ONCEACT(0)
		PRINTFORMW 「응…조금 들려주었으면 좋지만」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 나의 일 어떻게 생각하고 있는 거나?」
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마중
	;=================================================
	;IF 구상기입 체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
	;ENDIF
	
	;=================================================
	;자위중
	;=================================================
	;IF 구상기입 체크(0) && TEQUIP:자위중
	;ENDIF
	
	;=================================================
	;성교중
	;=================================================
	;IF 구상기입 체크(0) && TEQUIP:성교중
	;ENDIF

	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「%CNAME(MASTER, TARGET)%는 나의 일 어떻게 생각하고 있는 거나?」
	PRINTFORMW 「응, 들려주어 주세요…」
	RETURN 1
ENDIF

;────────────────────────────────────
;NO9_ACT2, 성에 관한 이야기를 함
;────────────────────────────────────
;·죠교자가 성에 대한지식를 말하기 시작한다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·죠교자에성지식가 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_2
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT2, 성에 관한 이야기를 함
;RESULT치（=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해미약등구상를 스킵）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
[SKIPSTART]
	;=================================================
	;처음
	;=================================================
	IF 구상기입 체크(0) && FIRSTACT(0)
	ENDIF
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입 체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
	ENDIF
	;=================================================
	;자위중
	;=================================================
	IF 구상기입 체크(0) && TEQUIP:자위중
	ENDIF
	;=================================================
	;성교중
	;=================================================
	IF 구상기입 체크(0) && TEQUIP:성교중
	ENDIF
	;=================================================
	;그죠교의 최초의 액션
	;=================================================
	IF 구상기입 체크(0) && TFLAG:경과시간 == 1
	ENDIF
[SKIPEND]
ENDIF

;────────────────────────────────────
;NO9_ACT3, 상냥하게 위로한다
;────────────────────────────────────
;·죠교자가 상냥한 말로 위로해 온다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_3
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT3, 상냥하게 위로한다 
;RESULT치（=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해미약등구상를 스킵）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;성교계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
		PRINTFORMW 「걱정하지 말아줘…」
		PRINTFORMW 「데 와 버렸다들 , 분명하게 책임져 주기 때문에♪」
		RETURN 1
	ENDIF
	
	[SKIPSTART]
	;==================================================
	;안면승마중
	;==================================================
	IF 구상기입 체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
	ENDIF
	
	;==================================================
	;자위중
	;==================================================
	IF 구상기입 체크(0) && TEQUIP:자위중
		PRINTW
	ENDIF
	
	;==================================================
	;성교중
	;==================================================
	IF 구상기입 체크(0) && TEQUIP:성교중
	ENDIF
	
	;==================================================
	;갈 것 같다
	;==================================================
	IF 구상기입 체크(0) && TCVAR:MASTER:가버려
		;-------------------------------------------------
		;갈 것 같은 것을참다 하고 있다
		;-------------------------------------------------
		IF 구상기입 체크(0) && TCVAR:MASTER:가버려 > 5
			PRINTFORMW 「인내는 하지 않고 이는 끝내세요」
			PRINTFORMW 「그렇지만, 참고 있는 모습은 조금 사랑스러울지도… 후후후」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;애태움중
		;-------------------------------------------------
		IF 구상기입 체크(0) && TCVAR:MASTER:애태움도 > 1
			PRINTFORMW 「어때? 초조하게 해지는 것은 괴로울까?」
			PRINTFORML 「내가 기분이 내키면, 이나 키라고 주어도 괜찮아요」
			PRINTFORMW 「후후후……」
			RETURN 1
		ENDIF
		
		;-------------------------------
		;그 외
		;-------------------------------
		PRINTFORMW 「이키 그런?」
		PRINTFORMW 「기다리고 있어… 곧바로 이나 키라고 주기 때문에♪」
		RETURN 1
	ENDIF
	[SKIPEND]
	
	;-------------------------
	;그 외
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMDW 앨리스는 사쿠야의 머리를 상냥하게 어루만졌다…
		PRINTFORMW 「후후, 좋아 좋아♪」
	ELSE
		PRINTFORMW 「 나도 너무 심한은 하고 싶지 않아, 사쿠야」
		PRINTFORMW 「얌전하게 하고 있으면, 나쁘게는 하지 않아요」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT4, 엄하게 위협한다
;────────────────────────────────────
;·어려운 말로 위협한다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT4, 엄하게 위협한다
;RESULT치（=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해미약등구상를 스킵）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「기억해 두어 주세요」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%(을)를 어떻게 하는지, 모두 나나름도 일」
		PRINTFORMW 「이 의미를 모를 수록, 바보같지 않은 것을 기대하고 있는 원…」
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
		PRINTFORMW 「\@ IS_PREVCOMNAME("안에 내면 안돼!") ? 억지로 # %CNAME(MASTER, TARGET)%에 \@질내사정, 최고군요. 후후」
		PRINTFORM 「%CNAME(MASTER, TARGET)%가 분명하게 잉까지, 매일질내에 흘러 들어가 주어요
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		;-------------------------------------------------
		;갈 것 같은 것을참다 하고 있다
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:MASTER:가버려 > 5
			PRINTFORMW 「쓸데없는 저항은 그만두면 어때…?」
			PRINTFORMW 「어차피 곧 이낳았기 때문에」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;애태움중
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:MASTER:애태움도 > 1
			SELECTCASE RANDACT(2, 0)
				CASE 0
					PRINTFORMW 「어머나, 이키 그래?」
					PRINTFORMW 「안되어요, 사쿠야. 좀 더 참아 주세요…」
				CASE 1
					PRINTFORMW 「이 오고 싶어서 견딜 수 없는, 은 얼굴이군요?」
					PRINTFORMW 「이대로 쭉 초조하게 하고 있으면%CNAME(MASTER, TARGET)%, 어떻게 되어버릴까 해들. 응?」
			ENDSELECT
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「フフ……」")
			CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후……」")
			CLEARLINE 1
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;눈가리개장착중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:눈가리개
		PRINTFORMW 「시야를 빼앗은 귀부인을 방치하면 어떻게 되는 것일까?」
		PRINTFORMW 「조금 재미있을 것 같은 것은 않아요. 유석에 하지 않는데」
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「\@ TENSION(TARGET, 8) == "高い" ? クスクス # フフフ \@……」")
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「\@ TENSION(TARGET, 8) == "높다" ? 킥킥 # 후후후 \@……」")
		CLEARLINE 1
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「얌전하게 하고 있으면, 나쁘게는 하지 않아요」
	PRINTFORMW 「쓸데없는 저항은 그만두는 것이 현명해요」
	RETURN 1
ENDIF

;────────────────────────────────────
;NO9_ACT5, 의상 변경을 요구한다
;────────────────────────────────────
;·죠교자가의상의 변경을 요구해 온다
;·죠교대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:밀어넘어지는중에는 발생하지 않는다
;·TEQUIP:밧줄가 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_5
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT5_0, 옷을 벗도록(듯이) 명령
;RESULT치（=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해미약등구상를 스킵）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「, 옷을 벗어 주세요」 
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 모두, 봐요♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가죠교대상를익애
	;=================================================
	IF 구상기입체크(100) && AFFECTION("익애")
		PRINTFORMW 「, 벗어… 나에게 전부 보여 줘…」
		PRINTFORMW 「후후… 흥분해버린다♪」
		RETURN 1
	ENDIF

	;-------------------------
	;그 외
	;-------------------------
	PRINTFORML 「그러면, 벗어 줄까」
	PRINTFORMW 「부끄러워하는 일은 없어요」
	PRINTFORMW 「천천히…나에게 잘 보이도록, 이군요♪」
	RETURN 1
ENDIF

;이 근처 범용이다
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	PRINTFORMW 「기분이 바뀌었어요. 응, 옷을 입어 주세요」
	PRINTFORMW 「어째서 사? 별로 이유는 없어요」
	RETURN 1
ENDIF

;────────────────────────────────────
;NO9_ACT6, 합의를 요구한다
;────────────────────────────────────
;·처음를 받아도 괜찮을까 얘기한다
;·처음=처녀or동정(양쪽 모두의 경우도 있다)
;·이미합의를 얻고 있는 경우는 발생하지 않는다
;·죠교대상가처녀에서도동정도 아닌 경우는 발생하지 않는다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
;@KOJO_NO9_MS_ACT_KXX_6
;-------------------------------------------------
;사쿠야씨처녀가 아니어서w
;-------------------------------------------------

;────────────────────────────────────
;NO9_ACT7, 징계한다고 위협한다
;────────────────────────────────────
;·지금부터징계·한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:징계·플래그는 서 있다
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·TEQUIP:밀어넘어지는중시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT7, 징계한다고 위협한다
;RESULT치（=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해미약등구상를 스킵）
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM측의 처리와 상손구 연결되도록(듯이) 조정해야 할
;1. 70이후에 한 번 다시 볼까…
;------------------------------------------------
IF 구상기입체크(100)
	;선언했다등 실행해 받지 않으면
	TCVAR:행동예약 = 60
	;=================================================
	;전COM가가 「무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 「아직 자신의 입장이 알지 않게? …… 아휴예요」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 경우, 말로 전하는 것보다, 좀 더 직접적인 방법이 좋은 것일까. 응?」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「기분좋게 해」
	;（이 철면피! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("기분좋게 해")
		PRINTFORMW 「귀부인이 그런 일 말하다니 , 사쿠야」
		PRINTFORMW 「 그렇지만, 지금의 입장을 분별해 주지 않으면 곤란해요」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「더 하드하게 해」
	;（이 돼지째! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("더 하드하게 해")
		PRINTFORMW 「기가 막힌 원이군요, 사쿠야. 귀부인이 그런 일 말하다니」
		PRINTFORMW 「정말로 지금의 상황을 알려지고 있을까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「용서를 빌다」
	;（허락해 받을 수 없어했다）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("용서를 빌다")
		PRINTFORMW 「안되어요, 허락할 생각은 없는 것」
		PRINTFORMW 「조금 징계나무가 필요같구나」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「자위를 시작한다」
	;（인의 이야기를 들어라! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("자위를 시작한다")
		PRINTFORMW 「무엇을 마음대로 시작해 버린다 의 것인지 해들 」
		PRINTFORMW 「이것은 징계나무가 필요할까」
		;TCVAR:행동예약 = 60
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「분수를 분별하지 않는 이 살인 실업 수당에는…」
	PRINTFORMW 「아무래도 징계나무가 필요 보고 싶은, 이군요」
	RETURN 1
ENDIF

;────────────────────────────────────
;NO9_ACT8, 고함치다
;────────────────────────────────────
;·큰 소리로고함치다
;·턴방침가"소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:징계·플래그에 관련되지 않고, 발생할 가능성이 있다（징계·안에 한층 더고함치다가능성등 ）
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT8, 고함치다
;RESULT치（=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해미약등구상를 스킵）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;전COM가가 「무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 「진짜, 거역하고만 있어 초조해해요…」
		PRINTFORMW 「적당, 화가 나! 그러한 것!」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「사실…, 사랑스럽지 않아요.%CNAME(MASTER, TARGET)%…」
	PRINTFORMW 「그 반항적인 얼굴도 초조해해!」 
	RETURN 1
ENDIF

;────────────────────────────────────
;NO9_ACT9, 기분나쁘게 웃다
;────────────────────────────────────
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·금일의방침가"노닥노닥"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT9, 기분나쁘게 웃다
;RESULT치（=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해미약등구상를 스킵）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;눈가리개장착중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:눈가리개
		PRINTFORMDW 시야를 빼앗겼다%CALLNAME:MASTER%의 귀에, 앨리스의 기분 나쁜 웃음소리가 닿는다…
		RETURN 1
	ENDIF
	
	;MENUMATCH등을 통하면 파생에 대응해 버리므로 오히려 TFLAG 그대로가 좋다든가 그런 곳
	;아무튼 좀 더 할 길이 있는 느낌이다
	;웃음계는 BASE든지 FAVOR든지 MENUMATCH로 판정해 함수화해도 괜찮은 것인지도 모른다든가 생각났지만 뒷전으로 한다
	;=================================================
	;이상계로부터 발생
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT분류 == 7
		PRINTFORMDW 앨리스의 기분 나쁜 웃음소리가 방에 울리고 있다…
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMDW 앨리스의 기분 나쁜 웃음소리가 방안에 영향을 주어 건넌다…
	RETURN 1
ENDIF
;회화계는 조금 정 넣고 하는필요가 있네요
