;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	엘리스구상(대상限定) 일상이벤트구상ファイル
;
;	일상이벤트で呼び出される구상をまとめたファイル
;
;	EXTRA모드でNO:9사쿠야をMASTERに선택했다場合のみ表示
;
;Version		Reason
;1.70			@구상記入チェック仕様に
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;일상이벤트구상
;========================================================================
;・굴복각인*5％で実行 굴복각인は죠교자毎に判別される
;・이벤트が発生했다際の内訳はスキンシップ67％、일상33％
;-------------------------------------------------

;────────────────────────────────────
;EV201, 介抱
;────────────────────────────────────
;・조교중気絶했다後
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_201(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;介抱
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	IF AFFECTION("상사상애")
		PRINTFORMW 「 미안해, 나, 너무 많이 해버렸군요…」
	ELSE
		PRINTFORMW 「저기 괜찮아？ 나중의 일은 걱정하지 않아 좋아」
		PRINTFORMW 「천천히 쉬어줘」
	ENDIF
	RETURN 1
ENDIF

;========================================================================
;スキンシップ（各種の性경험が入ります）
;========================================================================
;@DAILY_LIFE_1_0～からランダムで選ぶ　RETURN 0されると再抽選、RETURN 1(0以外)で抽選から抜ける
;-------------------------------------------------

;────────────────────────────────────
;EV202, スキンシップ0
;────────────────────────────────────
;・台所でセクハラ
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_202(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ0_0 股間まさぐり
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ0_1 おっぱいまさぐり
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 1
	PRINTFORMW 「저기 …」
;OriginalString : (COLOR_K14("사쿠야"), "「はいはい、忙しいから後にして」")
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「네네, 바쁘기 때문에 나중에 해」")
	PRINTFORMW 「상관해 주지 않으면, 못된 장난해요？」
;OriginalString : (COLOR_K14("사쿠야"), "「はいはい、どうぞご자유に」")
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「그래그래 알아서 해」")
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ0_2 無視했다ら諦めて出て行った
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「응, 안돼？ 지금은 쉬고 싶지 않아…？」
		PRINTFORMW 「……정말 , 어쩔 수 없는거네, 계속 해 줘 」
	ELSE
		PRINTFORMW 「어머나, 무정하네…」
		PRINTFORMW 「방해였던 것 같구나. 그러면, 그대로 계속해 줘」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ0_3 身をゆだねて股間でイかされ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 3
	PRINTW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ0_4 身をゆだねておっぱいで
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 4
;OriginalString : (COLOR_K14("사쿠야"), "「ちょ、ちょっと엘리스！ どこ触ってるのよ！」")
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「잠, 잠깐 앨리스！ 어디를 손대고 있는거야！」")
	PRINTFORMW 「사쿠야가 좋다고 말했어요？」
	PRINTL
;OriginalString : (COLOR_K14("사쿠야"), "「いや、確かに、言いま했다けど…」")
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「아니, 확실히, 말했지만…」")
	PRINTFORMW 「그렇지？ 그러면, 얌전히 당해 주세요」
	PRINTL
;OriginalString : (COLOR_K14("사쿠야"), "「ま、まだ、するの…？」")
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「계속, 할꺼야…？」")
	PRINTFORM 「물론
	CALL HEARTCB(COLOR2_K14("pink"), 1, "ＭＳ Ｐゴシック")
	PRINTFORMW  사쿠야가 가버릴때 까지 그만두지 않아요？ 후후」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ0_5 ぱんつお着替えさせてくれま했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 5
	PRINTFORMW 「어라, 이렇게 속옷을 더럽혀 버려서…」
;OriginalString : (COLOR_K14("사쿠야"), "「う…そ、それは…」")
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「우…그, 그것은…」")
	PRINTL
	PRINTFORMW 「싫다고 말해도, 결국에는 좋아져 버린거네？ 후후」
;OriginalString : (COLOR_K14("사쿠야"), "「う、ウルサイわねっ！ 仕方無いでしょう！」")
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「우, 시끄러워！ 방법이 없잖아！」")
	PRINTL
	PRINTFORMW 「그렇지? 어쩔 수 없었던거지？ 후후후」
;OriginalString : (COLOR_K14("사쿠야"), "「あぁ…なんか凄く悔しいわ…」")
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「아아…뭔가 굉장히 분해요…」")
	PRINTFORM 「아무튼 아무튼
	CALL HEARTCB(COLOR2_K14("pink"), 1, "ＭＳ Ｐゴシック")
	PRINTFORMW  갈아입을 속옷을 준비할테니, 기분풀어♪」
	RETURN 1
ENDIF

;────────────────────────────────────
;EV203, スキンシップ1
;────────────────────────────────────
;・食堂でお茶하면서
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_203(ARG)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ1_0 お紅茶いかが？
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ1_1 미약で무라무라おっぱい気になる
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ1_2 おっぱい見せつけられま했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 2
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ1_3 あわてて逃げま했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 3
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ1_4 むしゃぶりついてご봉사했다ら
;ご褒美にぱいずりorクンニでイかされちゃいま했다の巻
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 4
;ENDIF

;────────────────────────────────────
;EV204, スキンシップ2
;────────────────────────────────────
;・廊下で無理やり키스
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_204(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ2_0 廊下で주인님に呼び止められま했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ2_1 いきなりちゅぅ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ2_1 数分後만족してばいばい
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 2
;ENDIF

;────────────────────────────────────
;EV205, スキンシップ3
;────────────────────────────────────
;・廊下でおっぱい揉み揉み
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_205(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ3_0 いきなり背中から抱きつかれたなう
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ3_1 おっぱい揉まれてるなう
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ3_1 만족していなくなったお…
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 2
;ENDIF


;EV206,207はMASTERにPENISが無い場合、RETURN 0され呼び出される事はない
;PENIS有りでもTALENT:MASTER:동정の場合、同様の処理になる
;故に当구상では不要

;────────────────────────────────────
;EV208, スキンシップ6
;────────────────────────────────────
;・入浴中に襲われる
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_208(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ6_0 주인님お風呂場強襲
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ6_1 ほら背中を流してあげる
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ6_2 でもちんこは触ってあげない
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 2
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ6_3 당신は참다できず가게해줘！
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 3
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ6_4 結局주인님の方が참다できなくなりま했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 4
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ6_5 そして손でイかされま했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 5
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ6_6 （女당신の場合）泡塗れで弄られて…
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 6
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ6_7 （女당신の場合）何度も절정させられま했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 7
;ENDIF

;────────────────────────────────────
;EV209, スキンシップ7
;────────────────────────────────────
;・주인님が바이브で…
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_209(ARG)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ7_0 펠라の練習をしている
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ7_1 何見てるの！？ (죠교자が청초・수줍음持ちの場合限定)
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ7_2 あら…試してみる？
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 2
;ENDIF

;────────────────────────────────────
;EV210, スキンシップ8
;────────────────────────────────────
;・入浴中주인님と조수に襲われて…
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_210(ARG)
;-------------------------------------------------
;#DIM SUBTARGET,1
;同時出演するキャラの등록번호をSUBTARGETに
;SUBTARGET = ARG/100
;이벤트번호を戻す
;ARG %= 100
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ8_0 二人でお風呂場強襲
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ8_1 二人で洗いっこ→後に선택肢で分岐
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ8_2 당신に見られて興奮してる様子
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 2
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ8_3 じっくり鑑賞してたらのぼせてしまった
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 3
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ8_4 混ぜてあげるわ、いらっしゃいと仰る
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 4
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ8_5 二人でマット洗いをしてくれま했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 5
;ENDIF

;────────────────────────────────────
;EV211, スキンシップ9
;────────────────────────────────────
;・風呂から上ったらパンツが無かった
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_211(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ9_0 すーすーするけど仕方ない
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スキンシップ9_1 주인님ガン見
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 2
;ENDIF

;========================================================================
;일상（호감도上昇、その他소프트な경험など）
;========================================================================
;・
;-------------------------------------------------

;────────────────────────────────────
;EV212, 일상0
;────────────────────────────────────
;・ティータイム
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_212(ARG)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;일상0_0 お茶はいかが？ このあと地の文
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 0
	IF AFFECTION("상사상애")
		SELECTCASE RANDEVENT(2, 0, DAILY_TARGET)
			CASE 0
				PRINTFORMW 「티타임을 합시다」
				PRINTFORMW 「찻잔을 새로 샀는데, 어때？ 꽤 멋지다고 생각하지 않아？」
			CASEELSE
				PRINTFORMW 「저기 봐. 티캅을 갖추어져로 해 보았지만」
				PRINTFORMW 「뭐？ 수줍어？ 우대신, 재미있는 사람♪」
		ENDSELECT
	ELSE
		SELECTCASE RANDEVENT(2, 1, DAILY_TARGET)
			CASE 0
				PRINTFORMW 「정말 좋았어, 같이 차나 한잔 하는거 어때?」
				PRINTFORMW 「가끔 씩은 이런 것도 좋을 거 같은데？」
			CASE 1
				PRINTFORMW 「응, 쿠키를 구었는데 %CNAME(MASTER, TARGET)%도 먹겠지요？」
				PRINTFORMW 「이 홍차에 반드시 맞는다고 생각해♪」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;일상0_1 주인님と楽しいひととき
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 1
;ENDIF

;────────────────────────────────────
;EV213, 일상1
;────────────────────────────────────
;・昼寝
;・구상主が昼寝している場面
;・とうかこれ、죠교대상がフリーダムすぎるのでなんとか했다いｗ
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_213(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;일상1_0 お昼寝発見
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;일상1_1 どうぞごゆっくり
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상記入チェック(0) && ARG == 1
;ENDIF

;========================================================================
;반항除去（반항각인を持った죠교자の아라이멘도が高くなると発生）
;========================================================================
;・以前は일상系をoffにしてると発生しなかったが、現在は切り離されている
;-------------------------------------------------

;────────────────────────────────────
;EV214, 반항除去
;────────────────────────────────────
;・반항除去
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_214(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;반항除去0_0 懐柔されま했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	SELECTCASE RANDEVENT(2, 0, DAILY_TARGET)
		CASE 0
			PRINTFORMW 「회유라고 말하면, 어쩐지 어감이 나쁘네…」
		CASE 1
			PRINTFORMW 「조금은 좋게 된 느낌 일까, 응？」
	ENDSELECT
	RETURN 1
ENDIF
