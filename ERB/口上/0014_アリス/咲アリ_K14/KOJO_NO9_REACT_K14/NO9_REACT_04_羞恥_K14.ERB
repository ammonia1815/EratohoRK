;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	엘리스구상(대상限定) 수치系個別リアクション구상ファイル
;
;		수치系リアクション구상をまとめたファイル
;
;	EXTRA모드でNO:9사쿠야をMASTERに선택했다場合のみ表示
;
;Version		Reason
;1.70			@구상記入チェック仕様に。REACT40, 자위に加筆。その他細かな修正等
;1.67			REACT43に拒絶処理を追加
;1.65			TFLAG:REACT부하 == 2の場合COMに流れるように
;1.62			@IS_NOWCOMNAMEの新設に伴いSELECTCOMをIS_NOWCOMNAMEに変更
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;수치系 REACT40～45
;========================================================================
;・各REACT内の分け方としてTFLAG:REACT분류(IS_COMGRONAME)やSELECTCOM(IS_NOWCOMNAME)が考えられます
;・ただ、範囲の広い条件で拾ってしまうのは基本的にお勧めしません
;・ACT분류:수치系の場合、基本的に회화系COM、애무系COM、봉사系COMが선택肢となりますが
;・状況によってはその他のものが선택肢に含まれる可能性もあります
;・リアクション(REACT)で処理されなかったものは汎用リアクション(COM)で補完されるので
;・無理に全て埋める필요はありません
;・필요と思うものを絞り込み、COMとの連携を考えて効率よく構築すべきです
;-------------------------------------------------
;　REACTの地の文はACT_MESSAGE.ERBのTRAIN_MESSAGE2_{TFLAG:ACT}で処理されます
;　基本的に地の文はTFLAG:REACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐し、
;　各REACT분류内でTFLAG:REACT부하(自然な행동/無理のない행동/無理を했다행동)で派生
;　固有のTRAIN_MESSAGE2がない場合は、REACTION_MESSAGEでCOM毎に汎用処理されます
;　ACT_MESSAGE.ERB REACTION_MESSAGE.ERBを参照하면서の作業をお勧めします
;-------------------------------------------------
;=================================================
;関連変数/関数等…
;
;TFLAG:REACT분류 (0=なし/1=소극적으로 한다/2=적극적으로 한다/3=용서를 빌다/4=날뛴다/5=要求に応じない,거부/6=逃げる,もがく)
;IS_COMGRONAME（"なし/소극적으로 한다/적극적으로 한다/용서를 빌다/날뛴다/거부/逃げる"）
;
;FIRSTREACT
;初回ACT判定関数
;TFLAG:ACT（TFLAG:90）を自動で参照、現在実行されているREACTごとに번호が独立して保存される
;
;ONCEREACT
;조교중未読判定関数　죠교前に初期化される
;その他の仕様はほぼFIRSTREACTと同様
;=================================================

;────────────────────────────────────
;NO9_REACT40, 자위・個別リアクション
;────────────────────────────────────
;・ACT파생なし。PENIS(MASTER)やTEQUIP:페니스밴드で分ける事は可能
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:부끄러워한다/싫어한다/겁없이 웃는다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;봉사系反応
;	COM:거부한다/소극적으로 한다/적극적으로 한다/태연하게 한다
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_40
;-------------------------------------------------
;・地の文はREACT분류で分岐
;・REACT분류が소극적으로 한다/태연하게 한다/적극적으로 한다/受け入れるである場合は受容扱い
;・その他の反応の場合はREACT분류は全て거부にセットされる(용서를 빌다も거부扱いである事に注意)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が拒絶했다場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_COMGRONAME("거부")
	;=================================================
	;恥ずかしくて命令通りにできなかった（(치정先行）
	;=================================================
	IF 구상記入チェック(100) && PALAM:MASTER:치정 > PALAM:MASTER:반항
		;BASE:만족派生		초조함で拾ってもいいかもだが、BASE:7は변화が少ないので
		SELECTCASE TENSION(TARGET, 8)
			CASE "高い"
				PRINTFORMW 「은？ 뭐야. 부끄럽고 할 수 없다？」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%…, 아직도 수치심은 것을 가지고 있었군요」
				PRINTFORMW 「약간의 놀라, 라는 곳일까. 응？ 후후」
			CASE "一般"
				PRINTFORMW 「뭐, 확실히 부끄럽겠지요지만…」
				PRINTFORMW 「그러한 일 되면(자) 퇴색해. 트마라나이원이군요,%CNAME(MASTER, TARGET)%……」
			CASE "低い"
				PRINTFORMW 「그렇게,%CNAME(MASTER, TARGET)%(을)를 부끄러워하게 하는 것도 나쁘지 않은데. 그렇지만, 이군요…」
				PRINTFORMW 「말해진 일을 하지 않으면%CNAME(MASTER, TARGET)%에 있어 바람직하지 않은 결과가 되어요」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;嫌がって要求に応じない（반항先行）
	;=================================================
	IF 구상記入チェック(100) && PALAM:MASTER:반항 > PALAM:MASTER:치정 
		;BASE:만족派生
		SELECTCASE TENSION(TARGET, 8)
			CASE "高い"
				PRINTFORMW 「그다지 칭찬할 수 있던 것이 아니네요, 그러한 태도…」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%…, 자신의 입장을 분별하는 것이 좋아요」
			CASE "一般"
				PRINTFORMW 「말하는 것이 (들)물을 수 없다면, 상응하는 처사를 하는 일이 되는데…」
				PRINTFORMW 「뭐, 방법 없어요？ %CNAME(MASTER, TARGET)%가 바란 결과야 것」
			CASE "低い"
				PRINTFORMW 「 나에 따를 수 없다고 말한다면, 상응하는 보답을 받아 주어요」
				PRINTFORMW 「이유는 어때라고 해 원.%CNAME(MASTER, TARGET)%(은)는, 자신의 행동에 대해서 벌을 받아」
				CALL FADEIN(12, "「각오 하세요……」")
				WAIT
				TCVAR:행동예약 = 60
		ENDSELECT
		RETURN 1
	ENDIF
RETURN 0
ENDIF

;TEQUIP:눈가리개/거울/야외플레이時は弾く
;쾌락に対して부끄러워한다場合も弾く
;━━━━━━━━━━━━━━━━━━━━━━━━
;부끄러워한다（状況に부끄러워한다）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && (IS_NOWCOMNAME("부끄러워한다") && !TFLAG:REACT파생) && !TRUECHECK(TEQUIP:눈가리개, TEQUIP:거울, TEQUIP:야외플레이)
	SELECTCASE RANDREACT(2, 0)
		CASE 0
			PRINTFORMW 「부끄러우면 그만두면 좋다고 생각하는데」
			PRINTFORMW 「 그렇지만, 손은 멈추지 않아？ \@ AFFECTION("상사상애") ? 후후 # 후후 \@」
		CASE 1
			PRINTFORMW 「어머나, 그렇게 부끄러워하는 일 없어요」
			PRINTFORM 「지금의 %CNAME(MASTER, TARGET)%,\@ AFFECTION("상사상애") ? 매우 # 꽤 \@사랑스러워요
			CALL PRINT_SYMBOL_K14
	ENDSELECT
	RETURN 1
ENDIF

;消極的なのが気に入らない（징계・포인트加算）に派生했다ら弾く
;━━━━━━━━━━━━━━━━━━━━━━━━
;소극적으로 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("소극적으로 한다") && TFLAG:REACT파생 < 1 && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「이제 와서 부끄러워하는 일 없지요…？」
		PRINTFORM 「 그렇지만, 그런 곳도 사랑스럽네요
		CALL PRINT_SYMBOL_K14
	ELSEIF AFFECTION("익애")
		PRINTFORMW 「부끄러운 듯이  , 자신을 위로하고 있는 모습이 감등 없네요…」
		PRINTFORMW 「후후후 후후……」
	ELSEIF AFFECTION("권애")
		PRINTFORMW 「싫어해도 결국 하군요…」
		PRINTFORMW 「후후후……」
	ELSE
		SELECTCASE RANDREACT(2, 1)
			CASE 0
				PRINTFORMW 「이봐요, 좀 더 잘 보이도록 하세요」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%가 어떻게 자신을 위로하는지, 확실히 지켜봐 주어요」
			CASE 1
				PRINTFORMW 「남의 앞에서 자위라든지 부끄럽지 않은거야…？」
				PRINTFORM 「아무튼, 내가 시켜이지만…
				CALL PRINT_SYMBOL_K14
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;無理を했다행동ならCOMへ
;━━━━━━━━━━━━━━━━━━━━━━━━
;적극적으로 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("적극적으로 한다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		SELECTCASE RANDREACT(2, 2)
			CASE 0
				PRINTFORMW 「그렇게 격렬하게 해 괜찮아？ 라고, 생각이 드는데…」
				PRINTFORMW 「 나로 보여지는 것이 그렇게 흥분할까, 군요？」
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%RANDTEXT_K14("クスクス", "ふふ")%…」")
				CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%RANDTEXT_K14("クスクス", "ふふ")%…」")
				CLEARLINE 1
			CASE 1
				PRINTFORMW 「후후, 좋아요. 그래…, 좀 더」
				PRINTFORM 「%CNAME(MASTER, TARGET)%의 한심한 모습, 나에게 자주(잘) 보여 주세요
				CALL PRINT_SYMBOL_K14
			CASE 2
				PRINTFORMW 「좋아요？ 좀 더 격렬하게 시라고도. 후후…, 그렇게 자주. 그 상태」
				PRINTFORMW 「다만 착각 하지 않고 갖고 싶지만…, 마음대로 이크의 것은 허락하지 않으니까」
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%RANDTEXT_K14("ふふっ", "フフ")%……」")
				CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%RANDTEXT_K14("ふふっ", "フフ")%……」")
				CLEARLINE 1
		ENDSELECT
	ELSEIF AFFECTION("익애")
		SELECTCASE RANDREACT(2, 3)
			CASE 0
				PRINTFORM 「후후…멋져요
				CALL PRINT_SYMBOL_K14
				PRINTFORMW 「%CNAME(MASTER, TARGET)%가 스스로 시 비치는 곳, 분명하게 보고 있어 주어요」
			CASE 1
				PRINTFORMW 「 나로 보여져 기쁜 듯하구나？ 진짜, 구제할 길 없어요…」
				PRINTFORM 「 그렇지만 방법 없어요.%CNAME(MASTER, TARGET)%…, 볼 수 있어 기쁨헨타이니까
				CALL PRINT_SYMBOL_K14
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후…」")
				CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후…」")
				CLEARLINE 1
		ENDSELECT
	ELSEIF AFFECTION("권애")
		SELECTCASE RANDREACT(2, 4)
			CASE 0
				PRINTFORM 「어머나, 적극적이군요
				CALL PRINT_SYMBOL_K14
				PRINTFORMW 「후후…, 좋아요.%CNAME(MASTER, TARGET)%의 정 없는 모습, 확실히 보고 있어 준다…」
			CASE 1
				PRINTFORMW 「좋은 상태군요？ 후후…」
				PRINTFORMW 「그렇게,\@ !TEQUIP:눈가리개 ? 눈 # 얼굴 \@(을)를 돌리지 마. 확실히 여기\@ !TEQUIP:눈가리개 ? 를 봐주는거야 # 에 향하고 하는거야 \@」
				PRINTFORM 「 나는 차분히 감상시켜 받기 때문에…
				CALL PRINT_SYMBOL_K14
		ENDSELECT
	ELSE
		SELECTCASE RANDREACT(2, 5)
			CASE 0
				PRINTFORMW 「수치않다 빌려주어 기분도 없고, 자주(잘) 해요？」
				PRINTFORMW 「아무튼, 내가 말할 수 있던 의리가 아닌데. 후후…」
			CASE 1
				PRINTFORMW 「에 , 적극적인 것？ 나로 보여져 흥분하고 있는 거나」
				PRINTFORM 「 그렇지만 아무튼…, 후후. 나쁘지 않은 경치군요
				CALL PRINT_SYMBOL_K14
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF
;NO9_REACT40, 자위

;────────────────────────────────────
;NO9_REACT41, 비패개장・個別リアクション
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:부끄러워한다/싫어한다/겁없이 웃는다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_41
;-------------------------------------------------
;・地の文はREACT분류(受け入れる, 적극적으로 한다, 태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;・ACTION_APLLY2に固有の処理なし
;-------------------------------------------------

;REACT파생1,「意見を出すなんていい度가슴ね！」は弾く
;REACT분류:용서를 빌다も拾う
;━━━━━━━━━━━━━━━━━━━━━━━━
;용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && (IS_NOWCOMNAME("용서를 빌다") && !TFLAG:REACT파생) || IS_COMGRONAME("용서를 빌다")
	PRINTFORMW 「후후, 안되어요. 그런 일 말해도 허락해 주지 않아요」
	PRINTFORM 「이봐요, 좀 더 손가락으로 열려 준다
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;부끄러워한다（状況に부끄러워한다）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(50) && (IS_NOWCOMNAME("부끄러워한다") && !TFLAG:REACT파생) && !TRUECHECK(TEQUIP:눈가리개, TEQUIP:거울, TEQUIP:야외플레이)
	;윤활十分
	IF PALAM:MASTER:윤활 >= 500
		PRINTFORMW 「안쪽으로부터 넘쳐 나오고 해 멈추지 않는 것 같구나…」
		PRINTFORMW 「응…, 부끄럽지만, 나로 보여져 흥분하고 있는 지요」
		PRINTFORMW 「정직하게 말할 수 있으면(자), 좀 더 좋은 일 해 주어도 괜찮아？」
		PRINTFORMW 「\@ AFFECTION("상사상애") ? 후후후 # 우후후 \@…」
	;윤활不발
	ELSE
		PRINTFORMW 「후후, 조금씩 젖어 와」
		PRINTFORM 「부끄러워해 보였더니, 몸에 거짓말은 붙이지 않는 것 같구나
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 1
ENDIF

;용서를 빌다扱いの場合は弾く
;TFLAG:REACT부하 == 2の場合は弾く
;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을 참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && (IS_NOWCOMNAME("쾌감을 참는다") && TFLAG:REACT부하 < 2) && !IS_COMGRONAME("용서를 빌다")
	SELECTCASE RANDREACT(2, 0)
		CASE 0
			PRINTFORMW 「에？ 나로 보여져 기분 좋아져 버렸어？」
			PRINTFORM 「후후, 사랑스러워요
			CALL PRINT_SYMBOL_K14
		CASE 1
			PRINTFORMW 「인내는 하는 일 없어요. 좀 더 솔직하게 쾌락을 받아들이세요」
	ENDSELECT
	RETURN 1
ENDIF

;負荷2は弾く
;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을 받아들인다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 받아들인다") && TFLAG:REACT부하 < 2
	PRINTFORMW 「대강, 상당히 적극적이군요. 그렇게 내가 보았으면 좋은 것일까？」
	PRINTFORM 「후후, 좋아요. 소망 대로, 차분히 봐 준다
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF
;NO9_REACT41, 비패개장

;────────────────────────────────────
;NO9_REACT42, 자위과시・個別リアクション
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:기분좋게 해
;애무系反応
;	COM:부끄러워한다
;その他反応
;	COM:자위를 시작한다
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_42
;-------------------------------------------------
;・地の文はREACT분류(受け入れる, 적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)で分岐
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;부끄러워한다（状況に부끄러워한다）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(50) && (IS_NOWCOMNAME("부끄러워한다") && !TFLAG:REACT파생) && ALLSAMES(1, !TEQUIP:눈가리개, !TEQUIP:거울, !TEQUIP:야외플레이)
	IF AFFECTION("상사상애")
		PRINTFORMW 「아…팥고물…응응……(이)나……」
		PRINTFORMW 「이나……이것…%CNAME(MASTER, TARGET)%(으)로 보여져…흥분해버리는…」
	ELSEIF AFFECTION("익애")
		PRINTFORMW 「아…팥고물…응응……응구…」
		PRINTFORMW 「이봐요, 자주(잘) 봐 주세요…나, 여기가 느껴…」
	ELSEIF AFFECTION("권애")
		PRINTFORMW 「응, 잘 봐…」
		PRINTFORMW 「내가 스스로 위로하는 곳(중)…」
	ELSEIF AFFECTION("嫌悪")
		PRINTFORMW 「이봐요, 눈을 일등 살면 않아요！」
		PRINTFORMW 「분명하게 보지 않으면 아픈 눈에 맞추어요……」
	ELSE
		PRINTFORMW 「…응！ 식은 눈으로 보여지면서 하는 것도, 흥분해요…」
	ENDIF
	RETURN 1
ENDIF
;NO9_REACT42, 자위를과시한다

;────────────────────────────────────
;NO9_REACT43, 수치플레이・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=開始/1=終了)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:침묵한다/빈정거린다/더 하드하게 해/용서를 빌다/기분좋게 해
;애무系反応
;	COM:부끄러워한다
;고통系反応
;	COM:무서워한다
;その他反応
;	COM:자위를 시작한다
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_43
;-------------------------------------------------
;・@TRAIN_MESSAGE2_43が存在せず、固有のリアクションの地の文なし
;・죠교대상の反応に応じてREACTTION_MESSAGEでの汎用処理のみ
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT43_0, 수치플레이開始　OFF
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && !TFLAG:ACT파생
	;=================================================
	;拒絶処理　OFF
	;=================================================
	;現在の仕様ではACTTION_APPLYでコメントアウトされている
	;-------------------------------------------------
	IF 구상記入チェック(0) && !TFLAG:ACT파생 && IS_COMGRONAME("용서를 빌다")
		CALL 가학系解除処理_K14
		RETURN 1
	ENDIF
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT43_0, 수치플레이終了
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
	;汎用リアクションで処理しても終了時の場合は噛みあわない感があるので
	;RESULT値1を返してあえて表示させない。REACT분류で何かやってもよさそうではあるけど、今回は見送る
	RETURN 1
ENDIF
;NO9_REACT43, 수치플레이

[SKIPSTART]
以下、発生し得ないものはコメントアウトする
;────────────────────────────────────
;NO9_REACT44, 야외플레이・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=開始/1=終了)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:침묵한다/빈정거린다/더 하드하게 해/용서를 빌다/기분좋게 해
;애무系反応
;	COM:부끄러워한다
;고통系反応
;	COM:무서워한다
;その他反応
;	COM:자위를 시작한다
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_44
;-------------------------------------------------
;・@TRAIN_MESSAGE2_44が存在せず、固有のリアクションの地の文なし
;・죠교대상の反応に応じてREACTTION_MESSAGEでの汎用処理のみ
;-------------------------------------------------
;구상側で봉인済みのコマンド
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT44_0, 야외플레이開始
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && !TFLAG:ACT파생
ENDIF
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT44_1, 야외플레이終了
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && !TFLAG:ACT파생
ENDIF
;NO9_REACT44, 야외플레이

;────────────────────────────────────
;NO9_REACT45, 방뇨・個別リアクション
;────────────────────────────────────
;・ACT파생なし
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:용서를 빌다/기분좋게 해
;애무系反応
;	COM:부끄러워한다
;고통系反応
;	COM:거부한다/소극적으로 한다/적극적으로 한다/태연하게 한다
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_45
;-------------------------------------------------
;・地の文はREACT분류で分岐
;・REACT분류が소극적으로 한다/태연하게 한다/적극적으로 한다/受け入れるである場合は[受容]扱い
;・REACT분류が소극적으로 한다の場合は「仕方なく」、受け入れる/태연하게 한다/적극적으로 한다の場合は「恥ずかしげもなく」という扱い
;・その他の反応の場合はREACT분류は全て[거부]にセットされる
;-------------------------------------------------
;소변5000以上が発生条件だが、素質がない場合변화しないので不要になる
;お漏らし癖がなくても一定条件で소변の변화があってもいいのかも、とか思う
;구상で捏造してもいいんだけど、仕様として定めたいよねっていう
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;거부となる場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_COMGRONAME("거부")
	;치정先行	恥ずかしくて…
	IF PALAM:MASTER:치정 > PALAM:MASTER:반항
	;반항先行	嫌がって…
	ELSE
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;受容となる場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_COMGRONAME("소극적으로 한다/태연하게 한다/적극적으로 한다/受け入れる")
	SELECTCASE COMGRONAME(TFLAG:REACT분류)
		CASE "소극적으로 한다"
		CASE "태연하게 한다"
		CASE "적극적으로 한다"
		CASE "受け入れる"
	ENDSELECT
	RETURN 1
ENDIF
;거부、受容等はACTINT_APPLY2にてREACT분류の括りで行われているが
;システム側でREACT분류を追加、変更等했다際に混乱が起きる可能性が高いので
;関数等で一括관리できる仕組みにするのが望ましい
;ACTION_APPLY/ACT_MESSAGEから炙り出して修正するの面倒だったしな！
[SKIPEND]
