;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	엘리스구상(대상한정) 도구계 개별 리액션구상파일
;
;		도구계 리액션구상를 정리한 파일
;
;	EXTRA모드로 NO:9사쿠야를 MASTER에선택했다경우만 표시
;
;Version		Reason
;1. 70			@구상기입 체크 사양에. REACT20가 없었다(w 의 것으로 추가. 그 외 섬세한 수정＆가필등
;1. 65			GET_COM_RANK() == "무리를했다행동"의 경우 COM에 흐르도록(듯이)
;1. 62			@IS_NOWCOMNAME의 신설에 수반해 SELECTCOM를 IS_NOWCOMNAME로 변경
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;도구계 REACT20～26
;========================================================================
;  REACT의 지문은 ACT_MESSAGE.ERB의 TRAIN_MESSAGE2_{TFLAG:ACT}로 처리됩니다
;  REACT에 따라서는 전용 처리가 존재하지 않고, @REACTION_MESSAGE에서의 범용 처리만되고 있습니다
;  기본적으로 지문은 TFLAG:REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)로 분기 해,
;  각 REACT분류내에서 TFLAG:REACT부하(자연스러운행동/무리가 없는행동/무리를했다행동)로 파생
;-------------------------------------------------
;이렇게 해 되돌아보면, 도구계 REACT는손빼고 모래
;-------------------------------------------------
;────────────────────────────────────
;NO9_REACT20, 로터
;────────────────────────────────────
;·ACT파생없음
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통계 반응
;	COM:날뛴다
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_20
;-------------------------------------------------
;·지문은 REACT분류(받아들인다/적극적으로 한다, 태연하게 한다/용서를 빌다/소극적으로 한다)로 분기 한다
;·REACT분류:받아들이고는 TCVAR:MASTER:가버려인가 아닌가로 분기 한다
;-------------------------------------------------
;REACT20의 테두리가 존재하고 있지 않는 것에 지금쯤 깨닫는다
;그런 (뜻)이유로 우선 날조한다
;-------------------------------------------------

LOCALS = \@ AFFECTION("익애") ? 킥킥 # 후후후 \@

;━━━━━━━━━━━━━━━━━━━━━━━━
;부끄러워한다（REACT분류:소극적으로 한다）
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생로 나누지 않고 정리해 버린다
;TRAIN_MESSAGE2, REACTION_MESSAGE와의 균형으로 그러한 결론에 이른 나름
;ACT_MESSAGE도 정직, 좀 더 어떻게든 하는필요가 있도록(듯이) 생각한다. 아무튼우선도는 낮지만
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("부끄러워한다")
	SELECTCASE GET_COM_RANK()
		CASE "자연스러운행동", "무리가 없는행동"
			;쾌감에부끄러워한다		이 개소만 REACT파생를 본다
			IF TFLAG:REACT파생 == 1
				PRINTFORM 「후후,%CNAME(MASTER, TARGET)%가 그런%RAND_SPLIT("초심/우부/우브")%반응하다니
				CALL PRINT_SYMBOL_K14
				PRINTFORMW 「 그렇지만, 그렇구나. 나로서는 좀 더 사랑스러운 목소리를 들을만했으면 좋은 곳이지만」
				PRINTFORMW 「이봐요, 사실은 키모치 좋겠지요? 좀 더 자주(잘) 해 주어요」
			;상황에부끄러워한다
			ELSEIF !TFLAG:REACT파생
				PRINTFORMW 「어머나, 사랑스럽네요. 나의 시선에 부끄러워하다니」
				PRINTFORM 「 그렇지만 안되어요.%CNAME(MASTER, TARGET)%(은)는 부끄러워하는 곳, 좀 더%RAND_SPLIT("나에게/자주(잘)")%보여 주세요
				CALL PRINT_SYMBOL_K14
			ENDIF
		CASE "무리를했다행동"
			IF COMCOR_POSI()
				PRINTFORMW 「%CNAME(MASTER, TARGET)%, 그런 얼굴 하고 있는데…」
				PRINTFORMW 「사실은 좀 더 키모치 좋아지고 싶은거 아니야?」
				PRINTFORMW 「후후, 거짓말 해도 안되어요. 어딘지 모르게 알아버린다 것이군요, 그러한 것」
			ELSE
				PRINTFORMW 「후후, 싫지 않아요. 그러한 반응. 다만…」
				PRINTFORM 「그런%CNAME(MASTER, TARGET)%(을)를 보고 있다고(면) 나……, 조금 심술쟁이하고 싶어져버리는 이지만, 이군요
				CALL PRINT_SYMBOL_K14
			ENDIF
	ENDSELECT
	RETURN 1
ENDIF

;는 REACT보다 COM 방향인 생각이 드는구나w
;━━━━━━━━━━━━━━━━━━━━━━━━
;싫어한다（싫어했지만 방해하지 않고）REACT분류:소극적으로 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 1, 싫어해 ACT 방해는 COM로
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("싫어한다") && !TFLAG:REACT파생
	SELECTCASE GET_COM_RANK()
		CASE "자연스러운행동"
			PRINTFORMW 「그런 아이같은 일 말해져도 곤란해」
			PRINTFORMW 「모처럼%CNAME(MASTER, TARGET)%(을)를 위해 준비했다 해. 뭐, 그런 (뜻)이유이니까…」
			PRINTFORMW 「얌전하게 하고 있어 주세요」
		CASE "무리가 없는행동"
			PRINTFORMW 「그런 식으로 싫어할 수 있으면, 불필요하게 하고 싶어져」
			PRINTFORMW 「혹시%CNAME(MASTER, TARGET)%, 그것을 기대하고 있다든가?」
			PRINTFORM 「뭐, 어쨌든……, 멈출 생각은 없어요. 미안한데\@ AFFECTION("익애") ? , 저기 # \@
			CALL PRINT_SYMBOL_K14
		CASE "무리를했다행동"
			;실은 그렇지만도 않다
			IF COMCOR_POSI()
				PRINTFORMW 「정말로 싫으면, 좀 더 저항해 보이면 좋은데?」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%, 실은 상당히 즐기고 있는  것이 아닐까」
				PRINTFORM 「후후, 거짓말해도 쓸데없어요. (이)래%CNAME(MASTER, TARGET)%, 알기 쉽다 것, 이군요…
			;실은 싫어
			ELSE
				PRINTFORMW 「혐이라든가 말하고 있는 비교적은, 상당히 여유가 없는 것 같지만」
				PRINTFORMW 「정말로 싫으면, 좀 더 분명히 말하면 어떻게야?」
				PRINTFORM 「뭐, 어느 쪽으로 해라…, 나는 멈출 생각은 없지만
			ENDIF
			CALL PRINT_SYMBOL_K14
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;서투르다고 모욕한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("서투르다고 모욕한다")
	;=================================================
	;강한척 해（REACT분류:소극적으로 한다）
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		SELECTCASE GET_COM_RANK()
			CASE "자연스러운행동"
				PRINTFORMW 「이런 건은 느끼지 않는, 응? 그러면, 어떻게 하면 느낄까～응이라고, 군요」
				PRINTFORMW 「사실은 느끼고 있는 버릇에, 솔직하지 않아요. 아무튼에서도……」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%의 그러한 곳, 나, 싫지 않아요?」
			CASE "무리가 없는행동"
				PRINTFORMW 「어머나, 서투르고 나빴어요. 그렇지만…」
				PRINTFORMW 「그 모양으로 강한척 했더니, 설득력이 부족해. 유감스럽게, 군요」
			CASE "무리를했다행동"
				PRINTFORMW 「사랑스러운 소리로 울면서, 그런 일 말해져도. 응?」
				PRINTFORMW 「정말로 서투르면, 그런 소리는 나오지 않을 것이라고 생각하는데」
		ENDSELECT
		;페이드 아웃
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%…」")
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%…」")
		CLEARLINE 1
		RETURN 1
	ENDIF
	
	;REACT라고 하는 것보다 COM전용기분이 들어 버렸다. 다만, REACT20에 한정했다 있고 것으로 이대로
	;COM로 IS_NOWACTNAME 할 정도라면, 여기서 좋다
	;=================================================
	;실제로 하손（RAECT 분류:태연하게 한다）
	;=================================================
	;TRAIN_MESSAGE2_20은 죽여 REACTTION_MESSAGE만 호출하는 사양
	;-------------------------------------------------
	IF 구상기입체크(100) && !TFLAG:REACT파생 && FLAG:지문제어 == 1
		SELECTCASE GET_COM_RANK()
			CASE "자연스러운행동"
				PRINTFORMW 「에 , 말하잖아. 그런데……」
				PRINTFORMW 「행동이 수반하지 않는 말은, 아무 의미도 없어. 미안한데」
			CASE "무리가 없는행동"
				PRINTFORMW 「어머나, 그것은 나쁜 일을 한 원이군요. 다만, 그렇구나…」
				PRINTFORMW 「미숙함을 극복하기 위해도,%CNAME(MASTER, TARGET)%의 협력이 필요해」
				PRINTFORMW 「그러한 (뜻)이유이니까, 얌전하게 해 주세요……」
			CASE "무리를했다행동"
				PRINTFORMW 「말과는 정반대로, 상당히 여유가 없게? 게다가, 아무튼……」
				PRINTFORMW 「그러한 허풍도, 가끔  번거롭고, 화가 나지는 일도 있어」
				PRINTFORMW 「본궤도에 오르고 있으면, 아픈 눈에 맞추기 때문에……」
				;상태변화[분노]		REACT파생 0의 경우, COM 인상이악인상대인 것로분노가 될 가능성도 그 나름대로 있지만, 아무튼손 맞히고는 해 두고 싶은 느낌
				TCVAR:상태변화 = 6
		ENDSELECT
		RETURN 1
	ENDIF
;파생이 추가당했다등의 개변이 있었을 경우, RAECT 파생 결정 집이 살아 온다
;정합성이 이상해질 정도라면, 표시시키지 않다고 하는 사상. COM는 아무튼 범용적인 기술이 주된 것으로, 뭐 좋을까적인
;보완의 사상은 본래, 이 바리안트의구상시스템의 근간을 이루는 것이지만, REACT에 있어서는 반드시 들어맞지 않으면 생각한다
;후는, 시스템측 및 재야의 패치 작자에게로의 신뢰감 혹은 불신감이라든지 그러한 이야기가 되어 지는 것으로, 명언은 피하고 싶은 곳w
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을 참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;기본적으로 TRAIN_MESSAGE2_20에 맞춘 기술. 바뀐 보람 없는 곳은 병렬처리로
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;파생, 애원（REACT분류:용서를 빌다）
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		SELECTCASE GET_COM_RANK()
			CASE "자연스러운행동"
				PRINTFORMW 「그런 말을 들어도, 응?  %CNAME(MASTER, TARGET)%, 그렇게 기분 좋음 그런데」
				PRINTFORM 「오히려, 후후……조금 학째 싶어져버리는 원
				CALL PRINT_SYMBOL_K14
			CASE "무리가 없는행동", "무리를했다행동"
				PRINTFORMW 「어머나,\@ GET_COM_RANK() == "무리를했다행동" && !COMCOR_POSI() ? 그런 일 말하지 않고 # 무서워한다일 같은거 없어요.\@솔직하게 느끼면 좋아. 다만, 아무튼…」
				PRINTFORMW 「이런 대용품으로 잘 되어버린다 것은, 뭔가 납득할 수 없는 것이 있을까. 응?」
		ENDSELECT
		;페이드 아웃
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%…」")
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%…」")
		CLEARLINE 1
		RETURN 1
	ENDIF
	
	;=================================================
	;파생, 내구（REACT분류:소극적으로 한다）
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		SELECTCASE GET_COM_RANK()
			CASE "자연스러운행동"
				PRINTFORMW 「후후, 조금 낯간지러운 느낌인가 해들. 그렇지만, 나쁘지 않겠지요?」
				PRINTFORMW 「이봐요, 인내는 하지 말아줘…, 좀 더 솔직하게 느껴……」
			CASE "무리가 없는행동"
				PRINTFORMW 「어머나, 상당히 사랑스러워져 버렸다 원이군요. 뭐, 저항하지 않는다고 것은……」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%도 그 나름대로 즐기고 있다고 일로 좋은 것일까. 응?」
			CASE "무리를했다행동"
				;실은 기분 좋다		COMCOR는 반응의 미묘한 강약을 참조할 수 있으므로 재미있지만, 귀찮기도 하다w
				IF COMCOR_POSI()
					PRINTFORMW 「후후, 그런 얼굴 되어도 곤란해요」
					PRINTFORMW 「원망한다면, 이런 완구로 용이하게 느껴버린다, 자기 자신을 원망하군요」
				;실은 싫어
				ELSE
					PRINTFORMW 「무심코 소리가 나와 버렸다라는 곳일까? 후후…」
					PRINTFORM 「이제 와서 평정을 가장했더니, 이제 뒤늦음이라고 생각하지만, 이군요」
					CALL PRINT_SYMBOL_K14
				ENDIF
		ENDSELECT
		RETURN 1
	ENDIF
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을 받아들인다/기분 좋다! （REACT분류:받아들인다）
;━━━━━━━━━━━━━━━━━━━━━━━━
;현상쾌감을 받아들인다/기분 좋다! 에 REACT파생는 존재하지 않지만 조건 결정 집에서 보험에 든다
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋다!") && !TFLAG:REACT파생
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		;애태움도에 의한 파생
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				PRINTFORMW 「후후, 어때? 초조하게 해지면 감등 돌아가시겠지요?」
				PRINTFORM 「……라고. 지금의 %CNAME(MASTER, TARGET)%에 말했더니, 그것어디 가 아닐지도이지만
				CALL PRINT_SYMBOL_K14
			;애태움종료
			CASE 1
				PRINTFORMW 「이봐요, 좋아요. 초조하게 하는 것은 멈추어 준다」
				PRINTFORMW 「정령들, 좋은 소리로 울어 즐겁게 해 주세요……」
			;애태움없음
			CASEELSE
				PRINTFORMW 「%CNAME(MASTER, TARGET)%…, 이런 완구로 이인가 되는 것 일에는 저항이 없구나?」
				PRINTFORMW 「아무튼 좋아요. 이봐요, 이 오세요……」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE GET_COM_RANK()
		CASE "자연스러운행동"
			PRINTFORMW 「상스럽네요? 그런 소리 내 버려서……」
			PRINTFORMW 「 그렇지만, 후후……, 이것은 그렇게 좋은 것인가 해들 」
		CASE "무리가 없는행동"
			PRINTFORMW 「허리를 띄울 수 있어 산등성이다 는 (뜻)이유? 완전히……」
			PRINTFORMW 「뭐, 즐기고 있는 것 같아 무엇보다, 군요」
		CASE "무리를했다행동"
			PRINTFORMW 「이런 완구탕인가 되어버린다 것은, 조금 분하다든가? 아무튼에서도……」
			PRINTFORMW 「지금의 %CNAME(MASTER, TARGET)%에 있어 그것은, 그다지 중요한 일이 아니라고 생각하는데. 그런데」
	ENDSELECT
	;페이드 아웃
;OriginalString : 20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%…」")
	CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%…」")
	CLEARLINE 1
	RETURN 1
ENDIF

;────────────────────────────────────
;NO9_REACT21, 바이브·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통계 반응
;	COM:고통을 참는다/비명지른다/날뛴다
;그 외 반응
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_21
;-------------------------------------------------
;·장착시, 죠교대상가후타나리의 경우에게만 고유의 지문
;·다른 조건의 경우는 REACTION_MESSAGE로 처리될 뿐
;-------------------------------------------------
;ACT 자체는장착시, 해제시만 어째서 그 외의상태는 TEQUIP로 줍는필요 있어
;-------------------------------------------------

;ACT파생 마다 분리한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT21_0, 바이브장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;죠교대상가처녀상실
	;=================================================
	;IF 구상기입 체크(0) && TCVAR:MASTER:처녀상실
	;	PRINTFORMW 
	;	RETURN 1
	;ENDIF
	
	;REACT분류 == 3에 파생했다등 COM에 흘러 받는다
	;=================================================
	;쾌감을 참는다
	;=================================================
	IF 구상기입체크(65) && (IS_NOWCOMNAME("쾌감을 참는다") && TFLAG:REACT부하 < 2) && IS_COMGRONAME("소극적으로 한다")
		IF AFFECTION("상사상애")
			PRINTFORML 「마음이 내키지 않는 척 했더니…」
			PRINTFORMW 「몸은 거짓말 붙이지 않는 것 같지만? 후후……」
		ELSEIF AFFECTION("익애")
			PRINTFORMW 「마음이 내키지 않는 척 했더니…」
			PRINTFORMW 「몸은 거짓말 붙이지 않는 것 같지만…?」
		ELSEIF AFFECTION("권애")
			PRINTFORMW 「어머나, 조금 싫을 것 같은 헛 스윙이군요…?」
		ELSEIF AFFECTION("혐오")
			PRINTFORMW 「남의 앞에서 수치않다 기울 수 있는도 없고 그런 물삽라고, 그 거인 해 어떻게야…?」
			PRINTFORMW 「굉장히 보기 흉해 터무니없이 같네요. 아하하!」
		ELSE
			PRINTFORMW 「대강, 바이브래이터질천민 모습이 매우 한심하네요」
			PRINTFORMW 「있을 수 없을 정도  보기 흉하고 추악하구나…」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다
	;=================================================
	IF 구상기입체크(65) && (IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋다!") && TFLAG:REACT부하 < 2)
		IF AFFECTION("상사상애")
			PRINTFORMW 「근원까지 질불공평응은은…불쾌해요…♪」
			PRINTFORMW 「매우 기분이야 유혹한다…조금 부러울 정도♪」
		ELSEIF AFFECTION("익애")
			PRINTFORMW 「근원까지 질불공평응은은…불쾌하네요…」
			PRINTFORMW 「이봐요, 뽑을 때 조쿡은 하겠죠? 후후후……」
		ELSEIF AFFECTION("권애")
			PRINTFORMW 「후후…좋은 모습이군요」
			PRINTFORMW 「이제 상당히 친숙해 져 온 느낌 까…?」
		ELSEIF AFFECTION("혐오")
			PRINTFORMW 「남의 앞에서 수치않다 기울 수 있는도 없고 그런 물삽라고… 그 거인 해 어떻게야…?」
			PRINTFORMW 「굉장히 보기 흉해 터무니없이 같네요」
		ELSE
			PRINTFORMW 「대강, 바이브래이터질천민 모습이 매우 한심하네요」
			PRINTFORMW 「있을 수 없을 정도  보기 흉하고 추악하구나…」
		ENDIF
		RETURN 1
	ENDIF
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT20_1, 바이브해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM:도구를 제거한다는 아니고, 어디까지나죠교자에 의한 해제인 일에 주의
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;해제시에Ｖ절정가 되는 경우는 RAECT, COM를 날려 EV20에
	IF NOWEX:MASTER:Ｖ절정
		;RESULT 1을 돌려주어 COM로 보완시키지 않다
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;NO9_REACT21, 바이브

;────────────────────────────────────
;REACT22, 항문바이브·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를 빌다
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통계 반응
;	COM:고통을 참는다/비명지른다/날뛴다/무서워한다
;그 외 반응
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_22
;-------------------------------------------------
;·장착시, 죠교대상이남자의 경우에게만 지문변화
;-------------------------------------------------

;파생마다 분리한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT21_0, 항문바이브장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;REACT분류 == 3에 파생했다등 COM에 흘러 받는다
	;=================================================
	;쾌감을 참는다
	;=================================================
	IF 구상기입체크(65) && (IS_NOWCOMNAME("쾌감을 참는다") && TFLAG:REACT부하 < 2) && IS_COMGRONAME("소극적으로 한다")
		IF AFFECTION("상사상애")
			PRINTFORMW 「마음이 내키지 않는 척 했더니…」
			PRINTFORMW 「몸은 거짓말 붙이지 않는 것 같지만…? 후후……」
		ELSEIF AFFECTION("익애")
			PRINTFORMW 「마음이 내키지 않는 척 했더니…」
			PRINTFORMW 「몸은 거짓말 붙이지 않는 것 같지만…?」
		ELSEIF AFFECTION("권애")
			PRINTFORMW 「어머나, 조금 싫은 것 같구나…?」
		ELSEIF AFFECTION("혐오")
			PRINTFORMW 「수치않다 기울 수 있는도 없게 엉덩이에 그런 물삽라고. 그 거인 해 어떤가 해들…?」
			PRINTFORMW 「정말로 보기 흉하고 한심하네요」
		ELSE
			PRINTFORMW 「대강, 엉덩이의 구멍에 바이브래이터질천민 모습이 매우 한심하네요」
			PRINTFORMW 「있을 수 없을 정도  보기 흉하고 추악하구나…」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다
	;=================================================
	IF 구상기입체크(65) && IS_NOWCOMNAME("쾌감을 받아들인다") && TFLAG:REACT부하 < 2
		IF AFFECTION("상사상애")
			PRINTFORMW 「근원까지 질불공평응은은 불쾌하네요…」
			PRINTFORM 「매우 기분이야 유혹한다… 조금 부러울 정도
			CALL PRINT_SYMBOL_K14
		ELSEIF AFFECTION("익애")
			PRINTFORMW 「근원까지 질불공평응은은…불쾌하다…」
			PRINTFORMW 「후후…, 엉덩이 진동시켜, 그렇게 기분 좋은 것일까…?」
		ELSEIF AFFECTION("권애")
			PRINTFORMW 「후후…, 좋은 모습이군요」
			PRINTFORMW 「이제 엉덩이에서도 키모치 좋아지게 되었지 않는다?」
		ELSEIF AFFECTION("혐오")
			PRINTFORMW 「수치않다 기울 수 있는도 없게 엉덩이에 그런 물삽라고…, 그 거인 해 어떻게야…?」
			PRINTFORMW 「굉장히 보기 흉해 터무니없이 같아요!」
		ELSE
			PRINTFORMW 「대강, 엉덩이의 구멍에 바이브래이터질천민 모습이 매우 한심하네요」
			PRINTFORMW 「있을 수 없을 정도  보기 흉하고 추악하구나…」
		ENDIF
		RETURN 1
	ENDIF
RETURN 0
ENDIF
;NO9_REACT22, 항문바이브

;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT21_1, 항문바이브해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM:도구를 제거한다는 아니고, 어디까지나죠교자에 의한 해제인 일에 주의
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;해제시에Ａ절정가 되는 경우는 RAECT, COM를 날려 EV20에
	IF NOWEX:MASTER:Ａ절정
		RETURN 1
	ENDIF
	
	;------------------------
	;그 외
	;------------------------
	;PRINT
	;RETURN 1
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;NO9_REACT22, 항문바이브

;────────────────────────────────────
;NO9_REACT23, 항문비즈·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=장착/1=해제/2=추가)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를 빌다
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통계 반응
;	COM:고통을 참는다/비명지른다/날뛴다/무서워한다
;그 외 반응
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_23
;-------------------------------------------------
;·지문은 해제시만 REACT분류(받아들인다/적극적으로 한다, 태연하게 한다/용서를 빌다/소극적으로 한다)로 분기 한다
;·장착, 추가시에는 고유의 지문 없음
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT23_1 항문비즈해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;해제시에Ａ절정가 되는 경우는 RAECT, COM를 날려 EV20에
	IF NOWEX:MASTER:Ａ절정
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	;PRINT
	;RETURN 1
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT23_0 & 2, 항문비즈장착, 추가
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(TFLAG:ACT파생, 0, 2) > 0
	;애원에 파생했다등 COM에
	;━━━━━━━━━━━━━━━━━━━━━━━━
	;쾌감을 참는다
	;━━━━━━━━━━━━━━━━━━━━━━━━
	IF 구상기입체크(65) && (IS_NOWCOMNAME("쾌감을 참는다") && TFLAG:REACT부하 < 2) && IS_COMGRONAME("소극적으로 한다")
		IF AFFECTION("상사상애")
			PRINTFORMW 「마음이 내키지 않는 척 했더니…」
			PRINTFORMW 「몸은 거짓말 붙이지 않는 것 같지만. 응? 후후……」
		ELSEIF AFFECTION("익애")
			PRINTFORMW 「마음이 내키지 않는 척 했더니…」
			PRINTFORMW 「몸은 거짓말 붙이지 않는 것 같지만…?」
		ELSEIF AFFECTION("권애")
			PRINTFORMW 「어머나, 내켜하는 마음이 아닌 것일까…?」
		ELSEIF AFFECTION("혐오")
			PRINTFORMW 「잘도 남의 앞에서 엉덩이에 그런 물건, 삽라고 있을 수 있어요…?」
			PRINTFORMW 「굉장히 보기 흉해 터무니없이 같아요」
		ELSE
			PRINTFORMW 「그래서 속이고 있는 작정(생각)일 것이다지만…」
			PRINTFORMW 「단 소리가 나오고 있는 원이야, 사쿠야…」
		ENDIF
		RETURN 1
	ENDIF
	
	;━━━━━━━━━━━━━━━━━━━━━━━━
	;쾌감을 받아들인다/기분 좋다!
	;━━━━━━━━━━━━━━━━━━━━━━━━
	;ACT파생로 확률을 바꾼다
	;------------------------------------------------
	IF 구상기입체크(100) && (IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋다!") && TFLAG:REACT부하 < 2) && (TFLAG:ACT파생 == 1 && PERCENT(65) || TFLAG:ACT파생 == 2 && PERCENT(25))
		IF AFFECTION("상사상애")
			PRINTFORMW 「어머나, 상당히 사랑스러운 꼬리군요…♪」
			PRINTFORM 「매우 기분이야 유혹한다…조금 부러울 정도
			CALL PRINT_SYMBOL_K14
		ELSEIF AFFECTION("익애")
			PRINTFORMW 「후후… 꼬리같이 되어있는」
			PRINTFORM 「엉덩이 진동시켜, 좀 더 삽라고 갖고 싶은 것일까
			CALL PRINT_SYMBOL_K14
		ELSEIF AFFECTION("권애")
			PRINTFORMW 「후후…, 좋은 모습이군요」
			PRINTFORMW 「이제 상당히 친숙해 져 온 느낌 까…?」
		ELSEIF AFFECTION("혐오")
			PRINTFORMW 「잘도 남의 앞에서 엉덩이에 그런 물건, 삽라고 있을 수 있어요…?」
			PRINTFORMW 「굉장히 보기 흉해 터무니없이 같아요」
		ELSE
			PRINTFORMW 「이봐요, 엉덩이 키모치 좋지요? 키모치 좋아?」
			PRINTFORMW 「정직하게 좋다 차이야…」
		ENDIF
		RETURN 1
	ENDIF
RETURN 0
ENDIF
;NO9_REACT23, 항문비즈

;────────────────────────────────────
;NO9_REACT24, Ｃ애무도구·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=클리캡장착/1=클리캡해제/2=오나홀장착/3=오나홀해제)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:더 하드하게 해
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통계 반응
;	COM:날뛴다
;그 외 반응
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_24
;-------------------------------------------------
;·지문은 REACTION_MESSAGE에서 COM마다 범용 처리
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT24_0, 클리캡장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;쾌감을 참는다
	;=================================================
	IF 구상기입체크(45) && (IS_NOWCOMNAME("쾌감을 참는다") && TFLAG:REACT부하 < 2) && IS_COMGRONAME("소극적으로 한다")
		IF AFFECTION("상사상애")
			PRINTFORMW 「마음이 내키지 않는 척 했더니…」
			PRINTFORMW 「몸은 거짓말 붙이지 않는 것 같지만…? 후후……」
		ELSEIF AFFECTION("익애")
			PRINTFORMW 「마음이 내키지 않는 척 했더니…」
			PRINTFORMW 「몸은 거짓말 붙이지 않는 것 같지만…?」
		ELSEIF AFFECTION("권애")
			PRINTFORMW 「어머나, 내켜하는 마음이 아닌 것일까…?」
		ELSEIF AFFECTION("혐오")
			PRINTFORMW 「아는!  %CNAME(MASTER, TARGET)%굉장히 터무니없이 같아요…?」
			PRINTFORMW 「이것은 꽤 걸작이군요」
		ELSE
			PRINTFORMW 「대강, 한심한 모습이군요」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%(와)과 같은 변태에는, 적당하다고 말할 수 있는데」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다
	;=================================================
	IF 구상기입체크(45) && IS_NOWCOMNAME("쾌감을 받아들인다") && TFLAG:REACT부하 < 2
		IF AFFECTION("상사상애")
			PRINTFORMW 「후후……, 매우 한심한 느낌으로 멋져요」
			PRINTFORM 「기분이야 유혹하는군…? 조금 부러울 정도
			CALL PRINT_SYMBOL_K14
		ELSEIF AFFECTION("익애")
			PRINTFORMW 「지금의 %CNAME(MASTER, TARGET)%, 굉장히 보기 흉하구나…」
			PRINTFORMW 「후후…, 그렇지만 기분 좋아?」
		ELSEIF AFFECTION("권애")
			PRINTFORMW 「후후, 좋은 모습…」
			PRINTFORMW 「응, 소레 상당히 마음에 들어있는  것이 아니다…?」
		ELSEIF AFFECTION("혐오")
			PRINTFORMW 「아는!  %CNAME(MASTER, TARGET)%굉장히 터무니없이 같아요…?」
			PRINTFORMW 「이것은 걸작이예요」
		ELSE
			PRINTFORMW 「대강, 한심한 모습이군요」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%(와)과 같은 변태에는, 적당하다고 말할 수 있는데」
		ENDIF
		RETURN 1
	ENDIF
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT24_1, 클리캡해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;날뛰어 방해
	;=================================================
	;싫어한다, 날뛴다로 방해 취급의 경우는 여기서 줍는다
	;-------------------------------------------------
	IF 구상기입체크(100) && IS_COMGRONAME("날뛴다")
		PRINTFORMW 「아그렇게…, 그렇게 떼어지는 것이 싫으면……」
		PRINTFORMW 「쭉 붙인채로 있으면 좋아요!」
		RETURN 1
	ENDIF
	
	;=================================================
	;더 하드하게 해
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("더 하드하게 해")
		PRINTFORMW 「산등성이다 리나응이라고 드무네요? 그렇지만 유감. 이것은 마지막이야」
		PRINTFORMW 「뭐,별의 일로 즐겁게 해 주어요. 다만…」
		PRINTFORM 「그것이%CNAME(MASTER, TARGET)%(이)가 바란 것일까는 알지 않지만, 이군요…
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;부끄러워한다
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("부끄러워한다")
		PRINTFORMW 「이제 와서 부끄러워한다든가, 조금 재미있네요. 후후」
		PRINTFORMW 「이봐요, 얌전하게 해. 지금, 제외해 주어요」
		RETURN 1
	ENDIF
	
	;=================================================
	;싫어한다（방해 없음）
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("싫어한다") && !IS_COMGRONAME("날뛴다")
		PRINTFORMW 「구별하고가 없는 것 말하지 말고 주세요…」
		PRINTFORMW 「마지막이라고 하면 마지막이야, 나쁜데」
		RETURN 1
	ENDIF
	
	;=================================================
	;서투르다고 모욕한다		75%
	;=================================================
	IF 구상기입체크(75) && IS_NOWCOMNAME("서투르다고 모욕한다")
		PRINTFORMW 「제외하는데 서투름이고 뭐고 없다고 생각하지만요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을 참는다
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("쾌감을 참는다")
		IF 0
		ELSEIF IS_COMGRONAME("용서를 빌다")
			PRINTFORMW 「뭐, 이번은 이것으로 마지막이니까 안심해도 좋아요. 그렇지만…」
			PRINTFORMW 「그런 일 말해지면(자), 또 시라고 주고 싶어져버릴까도이지만.\@ AFFECTION("익애") ? 후후 # 후후 \@…」
		ELSEIF IS_COMGRONAME("소극적으로 한다")
			PRINTFORMW 「후후, 이렇게 떼어질 때도 조금 기분이 좋은 것일까」
			PRINTFORMW 「참았더니, 보면 알아버린다 것」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다/기분 좋다!
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋다!")
		PRINTFORMW 「즐기고 있는 것 같고 최상이구나? 지금 떼어져버린다 것은 자취 아까울까」
		PRINTFORMW 「아무튼에서도……, 그 밖에 하고 싶은 것도 있고. 방법 없네요. 후후…」
		RETURN 1
	ENDIF
	
	;TEQUIP:밧줄는 아무튼 보지 않아도 괜찮을까와
	;=================================================
	;날뛴다（날뛰었지만 억제 당한다）
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("날뛴다") && !IS_COMGRONAME("날뛴다")
		PRINTFORMW 「제외해 주기 때문에, 얌전하고 없음 있고……」
		PRINTFORMW 「그렇지 않으면%CNAME(MASTER, TARGET)%…, 떼어지는 것이 싫은 것?」
		RETURN 1
	ENDIF
	
	;이것 성립하는지 잘 모르지만 우선 쓴다
	;=================================================
	;도구를 제거한다
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("도구를 제거한다")
		PRINTFORMW 「어머나, 나의 손을 번거롭게 할 수 있을 것도 없다는 것? 그런데…」
		PRINTFORMW 「제멋대로인 흉내를 허락할 생각은 없어. 어떤 경우여도, 군요」
		RETURN 1
	ENDIF
RETURN 0
ENDIF

;────────────────────────────────────
;NO9_REACT25, 유두캡·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:더 하드하게 해
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통계 반응
;	COM:날뛴다
;그 외 반응
;	COM:도구를 제거한다
;------------------------------------------------
@KOJO_NO9_MS_REACT_K14_25
;------------------------------------------------
;·지문은 REACTION_MESSAGE에서 COM마다 범용 처리
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT25_0, 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:ACT파생 == 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT25_1, 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
ENDIF
[SKIPEND]

;────────────────────────────────────
;NO9_REACT26, 페니스밴드·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를 빌다/기분좋게 해
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고 모욕한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통계 반응
;	COM:고통을 참는다/비명지른다/날뛴다
;그 외 반응
;	COM:도구를 제거한다
;-------------------------------------------------
@KOJO_NO9_MS_REACT_K14_26
;-------------------------------------------------
;·지문은 REACTION_MESSAGE에서 COM마다 범용 처리
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT26_0, 페니스밴드장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ! TFLAG:ACT파생
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_REACT26_1, 페니스밴드해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM:도구를 제거한다는 아니고, 어디까지나죠교자에 의한 해제인 일에 주의
;ACT파생 0를 참고에필요인 분기를 추가해 사용해 주세요
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
ENDIF
[SKIPEND]
