;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 죠교終了이벤트구상ファイル
;
;	죠교終了이벤트구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 죠교終了구상
;========================================================================
;・@EVENTCOMENDから呼び出される
;・구상主が죠교終了する直前に表示
;・ARG:1, ARG:2の場合は地の文表示後にPRINTLで空行。その後に구상を表示
;・ARG:0の場合は固有の地の文なし。구상表示の前にPRINTLで空行が入る
;・RESULT 0以上を返すと구상表示後にPRINTLで空行が入る
;-------------------------------------------------
;関連変数…
;FLAG:일상제어	죠교終了時状況(1=통상/2=精力0/3=일회휴게/4=실신했다)
;-------------------------------------------------
@KOJO_EVENT_K1_13(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 普通に終了、1, 죠교대상が疲れている、2, 죠교대상が실신했다)
;-------------------------------------------------
IF TALENT:Ｖ민감 == 1
	PRINTFORML
	PRINTFORMDW 아이 만들기 섹스에 반응하고 있었다%CALLNAME:TARGET%의 질은, 원의 감도에 돌아왔다
	TALENT:Ｖ민감 = 0
ENDIF
IF TALENT:매혹 == 1
	PRINTFORML
	PRINTFORMDW %CALLNAME:TARGET%의 유노에상태와 함께 매혹은 해제되었습니다
	TALENT:매혹 = 0
ENDIF

;────────────────────────────────────
;ARG:1, ARG:2, 疲労・실신による죠교終了 FIRSTEVENT(0)～
;────────────────────────────────────
;・죠교대상소모도に応じて疲労/실신に分かれる
;・呼び出しタイミングは地の文の表示→PRINTL→구상の表示
;・EVENT201, 介抱(일상이벤트)の箇所を書く場合は、上손く連携できるようにすると良いでしょう
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が疲れている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 1
	PRINTFORMW 「슬슬 끝맺을까요」
	PRINTFORMW 「넘어질 때까지해 주었으면 했다？그렇게 초조해 하지 않아도, 나는」
;OriginalString : 인20, "「逃がさないわよ♪」")
	CALL FADEIN(20, "「놓치지 않아요♪」")
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が실신했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 2
	PRINTFORMDW %CALLNAME:TARGET%는 의식을 잃었다%CALLNAME:MASTER%(을)를 메었다
	RETURN 1
ENDIF

;────────────────────────────────────
;굴복終了　今回で취득했다か？を参照しないと意味がない
;────────────────────────────────────
;【굴복:25】질내사정취득初回
IF 구상記入チェック(100) && GETBIT(CFLAG:각인, 25) && FIRSTEVENT(125)
	PRINTFORML 「이군요. 어땠어요？무엇은 그」
;OriginalString : 아웃6, "  もじもじ")
	CALL FADEOUT(6, "  머뭇머뭇")
	PRINTFORML 「개만들기.」
	PRINTFORML 「개만들기 어, 좋아하게 될 수 있을 것 같다？」
	RETURN 1
ENDIF
;【굴복:11】키스취득初回
IF 구상記入チェック(100) && GETBIT(CFLAG:각인, 11) && FIRSTEVENT(111)
;OriginalString : 아웃4, "  ちら")
	CALL FADEOUT(4, "  등")
	PRINTFORMDL %CALLNAME:TARGET%는 행때문을 끝맺으려고 하면서도,
;OriginalString : 아웃4, "  ちら")
	CALL FADEOUT(4, "  등")
	PRINTFORMDL 끊임없이 %CALLNAME:MASTER%의 입가에 시선을 향하여 왔다…
	PRINTFORML 
	PRINTFORMDL 스스로의 입술에 손가락을 대는 %CALLNAME:TARGET%
	PRINTFORML 「입맞췄으니까, 이것으로 우리들 [애인]이군요」
	PRINTFORMW 「좋구나, 부부 겸 애인이야.」
;OriginalString : 인6, "「んふふ♪」")
	CALL FADEIN(6, "「응후후♪」")
	PRINTFORMW 「이봐, 우리들은 이런 경험이 없었잖아」
	PRINTFORML 
	PRINTFORMDW %CALLNAME:TARGET%는 조교 후 흘린 땀을 씻으러 욕실로 향했다…
	;見えない後ろ姿に聞こえる鼻歌弾む발取りに合わせて소녀기상곡
;OriginalString : 아웃12, "「らんらんらーらーらー♪ららんらーらーら♪」")
	CALL FADEOUT(12, "「들 응등응등-들 -들―♪라라응등-들 -들♪」")
	RETURN 1
ENDIF
;────────────────────────────────────
;ARG:0 普通に終了 FIRSTEVENT(10)～
;────────────────────────────────────
;・통상の죠교終了時の구상
;・地の文なし。PRINTLによる空行の後に구상表示
;・枠毎に이벤트번호を振ってありますが、任意に設定する事は可能です
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;대만족보너스を得ている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:금회대만족
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTEVENT(10)
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상の精力0
	;=================================================
	IF 구상記入チェック(100) && FLAG:일상제어 == 2
		PRINTFORML 「오늘도 즐거웠어요」
		PRINTFORMDL %CALLNAME:TARGET%는 윤기 윤기로 하면서 %CALLNAME:MASTER%에 생긋 한 웃는 얼굴을 향했다
		RETURN 1
	ENDIF

	;=================================================
	;できちゃった　孕ませてから産むまで物발りない為展開強化
	;=================================================
	IF 구상記入チェック(0) && CFLAG:임신 == 10
		PRINTFORMDL 
		RETURN 1
	ENDIF

	IF 구상記入チェック(100) && CFLAG:임신 == 4
		PRINTFORMDW %CALLNAME:TARGET%는 체온계와 같은 것을 전해 왔다
		PRINTFORMDL 체온계와 같은 것에는 환에 종선이 2개 줄표시되고 있다…
		PRINTFORML 
		PRINTFORMDL 아무래도 내일과 모레는 개이는 것 같다…
		PRINTFORMDL %CALLNAME:TARGET%에는 햇빛이 잘 어울린다
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		RETURN 1
	ENDIF
	;何の話だったか忘失
	;ここでリターンさせ下部を大만족していない%CALLNAME:TARGET%さんに仕立て上げる
	RETURN 1
ENDIF

;*************************************************
;以下、호감도による派生
;*************************************************

;EVENT11あたりで任意のKFLAGを立てて条件にしてやると話の辻褄は合わせやすいです
;━━━━━━━━━━━━━━━━━━━━━━━━
;상사상애（호의3000以上・호감도3000以上）
;-------------------------------------------------
;・互いに好き合っている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("상사상애")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTEVENT(20)
		PRINTFORMDW %CALLNAME:MASTER%의 방을 나오려고 해, 한 번 뒤돌아 봐 입가를 피기 시작하게 한다
		PRINTFORML 「행복한 표정 해 버려서.」
		RETURN 1
	ENDIF
	[SKIPSTART]
	;=================================================
	;후타나리%CALLNAME:TARGET%さんが大만족していないかつ성교굴복獲得済み【굴복:22】질내사정당했다
	;=================================================
		;=================================================
		;%CALLNAME:MASTER%의 처녀を貰ってご機嫌
		;=================================================
		
		;=================================================
		;%CALLNAME:MASTER%が남자かつ항문開発済みの場合お尻を使われる
		;=================================================
		
	[SKIPEND]
	;=================================================
	;大만족していないかつ성교봉사済み
	;=================================================
	;%CALLNAME:TARGET%とエッチ했다事ある場合の条件設定が思いつかないので孕ませ後のご褒美に位置付ける
	;大만족は発生しなくとも毎晩만족は시킨다。サティスファクション！
	IF 구상記入チェック(100)
		PRINTFORML 「응. 여기로부터는 너가 마음대로 해도 좋아요」
		PRINTFORMDW %CALLNAME:TARGET%는 아직 만족하지 않는 것 같다…
		PRINTFORML 
		;%CALLNAME:MASTER%が%CALLNAME:TARGET%に惚れている事を暗示
		PRINTFORMDL %CALLNAME:MASTER%(은)는 %CALLNAME:TARGET%는 만족할 때까지 천천히 후희를 즐겼다
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		RETURN 1
	ENDIF
ENDIF

;호감도の伸び的に익애の後になると思われます
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교完了（호의2000以上）
;-------------------------------------------------
;・죠교대상が구상主にもうメロメロ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("죠교完了")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTEVENT(30)
		PRINTFORMDW 지금의 관계를 일방적으로 쌓아 올려지고 나서 상당히 길 때가 지난 것처럼 생각한다
		PRINTFORMDW 시원스러운%CALLNAME:TARGET%의 옆 얼굴만 보고 있던 이전을 돌아본다
		PRINTFORMDW 이 관계가 시작되고 나서, 다양한%CALLNAME:TARGET%의 표정을 볼 수가 있었다
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;익애（호감도2000以上）
;-------------------------------------------------
;・죠교자の方が죠교대상にはまってしまい、深かったり倒錯했다愛を注いでいる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("익애")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTEVENT(40)
		PRINTFORMDW 방을 나오려고 한 %CALLNAME:TARGET%는 되돌아 본다
		;ここは당신表記の方が雰囲気が出る
		PRINTFORMW 「저기. %CALLNAME:MASTER%」
		;一度目は聞き逃しかねない。
;OriginalString : 아웃20, "「好きよ」")
		CALL FADEOUT(20, "「좋아해요」")
		;大事な事なので二回目ははっきりと。
;OriginalString : 인20, "「好き」")
		CALL FADEIN(20, "「좋아」")
		PRINTFORML
		PRINTFORMDW %CALLNAME:TARGET%는 그렇게 말하고 %CALLNAME:MASTER%의 대답도 듣지 않고, 허둥지둥 가 버렸다
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교済み（호의1000以上）
;-------------------------------------------------
;・죠교대상が구상主に죠교されるのを幸せに感じている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("죠교済み")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTEVENT(50)
		PRINTFORMDW %CALLNAME:TARGET%는 왜 이런 일을 저질렀을 것이다
		PRINTFORMDL 본인에게 묻은 곳에서
		;ここは桃色구상色
		PRINTFORMW 「감이야」
		PRINTFORMDL 의 한마디로 정리할 수 있어 버릴까
		PRINTFORMDW 그렇게 쓴 웃음 한다
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;嫌悪（호의-100未満）
;-------------------------------------------------
;・죠교자に対し嫌悪感を抱いている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && AFFECTION("嫌悪")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTEVENT(60)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;・心に傷を作られ、주인님の姿を見るだけで怯えてしまう
;・트라우마の種は「%ACTSTR(CFLAG:MASTER:8)%」で表示できる
;　例）PRINTFORMW 「もう%ACTSTR(CFLAG:MASTER:8)%は嫌ですって？」

;━━━━━━━━━━━━━━━━━━━━━━━━
;공포（호의200未満・트라우마Lv3以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && AFFECTION("공포")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTEVENT(70)
		;コメディ色が強いのでコメントアウト
		;PRINTFORMW 「もう『連日素麺』は嫌？…五箱も送りつけてきた奴に言いなさい」
		;PRINTFORML
		;PRINTFORMDW %CALLNAME:MASTER%は光の消えた瞳でおそうめんをすすりあげた…
		;PRINTFORML
		;PRINTFORMDW ずぞっぞぞっ…
		;PRINTFORMW ちゅるっ…
		;PRINTFORML
		;PRINTFORMDW 山山葵がツンと%CALLNAME:MASTER%의 鼻腔を刺激すると…涙が零れた…
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;반항（호의0以下・반항각인Lv1以上）
;-------------------------------------------------
;・心を開かない노예に苛つく주인님
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && AFFECTION("반항")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTEVENT(80)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;目覚め（호의500以上）
;-------------------------------------------------
;・죠교されるのもむしろ好きになってきたかも知れない…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("目覚め")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTEVENT(90)
		PRINTFORMDW 말도 안되는 시작이였지만, 그 두 명의 관계는 확실한 진전을 보이고 있었다
		PRINTFORMDW 원래 나쁘게 생각하고 있었다지만, %CALLNAME:TARGET%가 머릿속에서 떨어지지 않는다
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;順応（호의100以上・호감도100以上）
;-------------------------------------------------
;・丁寧に죠교を進められて、思ったより嫌じゃないかも…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && AFFECTION("順応")
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTEVENT(100)
		PRINTFORMDW 당신은 조금씩, 지금 상황을 긍정적으로 보게 되었다
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;序盤（条件なし）
;-------------------------------------------------
;・ここまでの条件にヒットしなかった場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTEVENT(110)
		PRINTFORMW 「다른 일은 능숙하게 할 수 있는데…정사는 그렇지 않은 것 같구나…」
		PRINTFORMW 「느긋하게 해 나갑시다」
		PRINTFORMW 「앞으로도, 긴 교제가 되기 때문에…」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「오늘도 즐거웠어」
		PRINTFORMW 「또 올게」
		RETURN 1
	ENDIF
ENDIF
