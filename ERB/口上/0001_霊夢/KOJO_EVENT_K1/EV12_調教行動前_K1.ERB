;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 죠교자행동前이벤트구상ファイル
;
;	죠교자행동前이벤트구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV12, 죠교자행동前구상
;========================================================================
;・TRAIN_PROCCES.ERB内、@TRAIN_CANCELから呼び出される
;・구상主のパラメータ表示の直後に表示。죠교指示は既に決定されている
;・조교중止宣言の差し替えや、죠교ターン開始前に주인님から一言、のように書くことも可能
;・午前、午後の죠교開始ではなく、죠교ターンの開始前には毎回呼び出される箇所
;・その為もあって、조교중止時と경과시간0以外の箇所はIF 구상記入チェック(25)等として確率表示する事を推奨
;・조교중止時にのみ地の文が存在する
;・文章表示後に本体側のDRAWLINE処理あり
;-------------------------------------------------
;FIRSTEVENT(0)～や、FIRSTEVENT(10)～などとして枠毎に割り当てていますが
;FIRSTEVENT/ONCEEVENT/RANDEVENT/ORDEREVENTは事実上無限に이벤트번호を増やせるので
;필요に応じて任意に割り当てて下さい
;-------------------------------------------------
@KOJO_EVENT_K1_12(ARG)
;-------------------------------------------------
;引数内訳…
;ARG(0=죠교可/1=구상主の疲労で中止/2=죠교대상の疲労で中止/3=ご褒美による中止)
;-------------------------------------------------

;────────────────────────────────────
;조교중止 ARG:1 ARG:2 ARG:3 FIRSTEVENT(0)～
;────────────────────────────────────
;・疲労、もしくはご褒美による조교중止の場合の구상
;・記入チェック=1の場合は구상表示後本体側でPRINTLの空行が入る
;・記入チェックを=2にすると、地の文を表示しない
;・というかRESULT > 2で判定されるので2以上なら同じ挙動になりますが一応非推奨ということで
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ご褒美による中止
;RESULT値（1, 구상の後に地の文を表示(非推奨)、2, 구상のみ表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 3
	;記入チェックを2以下に했다場合は以下の地の文が表示される。表示タイミングは구상表示の後
	;%CALLNAME:TARGET%は금일の죠교を中止し、
	;%CALLNAME:MASTER%とまったりと했다시간を過ご했다…
	
	SELECTCASE TIME
		;낮
		CASE 0
			;腰掛ける描写は無い。既に隣に앉아있다。
			PRINTFORMDW 휴게중, %CALLNAME:MASTER%의 근처에 앉는다%CALLNAME:TARGET%는 펑펑무릎을 두드려 유혹해 왔다…
			PRINTFORMW 「이봐요, 여기 오세요」
		;밤
		CASEELSE
			PRINTFORMDW %CALLNAME:TARGET%는 턱을 괴는 행동을 붙어, 식사를 배달시킨다%CALLNAME:MASTER%(을)를 응시하고 있다…
			SELECTCASE RAND:3
				CASE 0
					;なにもなし
				CASE 1
					PRINTFORML
					PRINTFORMDW %CALLNAME:TARGET%는 시선을 창을 향한다
					PRINTFORMW 「오늘 밤은 달을 보면서 술을 마심이군요」
					;言わせない方が良い味出そう
					;PRINTFORMW 「あんたにも付き合って貰うわよ。お晩酌」
				CASEELSE
					PRINTFORML
					PRINTFORMW 「무엇인가 붙고 있는지는？」
					PRINTFORMW 「 는 , 붙어있는 원이야」
			ENDSELECT
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자の疲労による中止
;RESULT値（1, 구상の後に地の文を表示(非推奨)、2, 구상のみ表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 1
	;記入チェックを2以下に했다場合は以下の地の文が表示される。表示タイミングは구상表示の後
	;조교의 시간이다が、入ってきた%CALLNAME:TARGET%はなにやら疲れている様子で、
	;今回は休みだと言って出ていった…
	
	;基本的に「インデント」は「ネスト1階層を意味する」という論理的な意味を持っていることが条件、と考えます@/L
	SELECTCASE TIME
		;낮間
		CASE 0
			PRINTFORMW 「오늘은 중지로 합시다」
			PRINTFORML
			PRINTFORMDW %CALLNAME:TARGET%는 거실에서 데굴데굴 하고 있다…
			PRINTFORMDW %CALLNAME:MASTER%(은)는 생각나는 대로 전떡을 손에 들어, %CALLNAME:TARGET%의 입가에 접근해 보았다…
			PRINTFORML
			;米食動物 あざらしのようにねそべり奈良の鹿のように横暴に食らう
			PRINTFORMW 도！………도…도……
			PRINTFORML
			PRINTFORMDW %CALLNAME:MASTER%(은)는 욕망에 찔러 작동되는 대로, 다음 되는 전떡에 손을 뻗었다…
			;밤が更けるに繋がる
		;밤
		CASEELSE
			PRINTFORMDW %CALLNAME:TARGET%는 방에서 나오자, 곧 바로 베개를 안아 돌아왔다
			PRINTFORML 
			PRINTFORMW 「휴일이라고는 말했지만, 상대는 받아요」
			PRINTFORMW 「너의 팔, 빌려요」
			PRINTFORML 
			PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 팔을 베개로 해 자 버렸다
	ENDSELECT
	;구상のみ表示
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상の疲労による中止
;RESULT値（1, 구상の後に地の文を表示(非推奨)、2, 구상のみ表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 2
	;記入チェックを2以下に했다場合は以下の地の文が表示される。表示タイミングは구상表示の後
	;조교의 시간이다が、%CALLNAME:TARGET%は酷い様子の%CALLNAME:MASTER%を見てしばらく考えると、
	;今回は休んでいいと言って出ていった…
	
	;上の地の分は実際には구상の下に表示される
	SELECTCASE TIME
		;낮間
		CASE 0
	PRINTFORMDW 조교의 시간이 왔지만, %CALLNAME:TARGET%는 심한 모습의 %CALLNAME:MASTER%(을)를 봐 당분간 생각하면, 이번은 쉬어도 좋다고 말해 나와 갔다…
	
			PRINTFORMDW %CALLNAME:TARGET%는 방에서 나오면(자), 추석을 손으로 돌아왔다
			PRINTFORML
			PRINTFORMW …
			PRINTFORML
			PRINTFORMDL …
			PRINTFORMW 즈즛…
			PRINTFORML 
			PRINTFORMDW 그리고, 두 명 줄차를 훌쩍거린다
		;밤
		CASEELSE
			PRINTFORMDW %CALLNAME:TARGET%는 방에서 나오자, 곧 바로 베개를 안아 돌아왔다
			PRINTFORMW 「쉬어도 괜찮다고는 말했지만, 상대는 받아요」
			PRINTFORML 
			PRINTFORMDW %CALLNAME:MASTER%(은)는 안아 베개로 되어 버렸다
			PRINTFORML 
			PRINTFORMW 「다음은, 이번 분도 정리해 전부 짜 취해 주기 때문에」
			PRINTFORMW 「각오 해 두세요」
	ENDSELECT
	;구상のみ表示
	RETURN 2
ENDIF
	
;-------------------------------------------------
;・以下、ARG:0(죠교可)相当
;・TRAIN_PROCCES.ERB内、@BEFORE_ACTから呼び出される
;・この時点でACTはすでに決定しているがACT파생の決定はなされていない
;・NOWEXのリセットはなされていない
;・これらの箇所を抜けた後にACT開始になる
;・分岐の増減は자유にできますが、우선度には注意しましょう
;-------------------------------------------------

;────────────────────────────────────
;죠교開始直後 FIRSTEVENT(10)～
;────────────────────────────────────
;・경과시간が0である場合に表示される
;・その回の죠교開始直後に一度きりで、以後その조교중には表示されない
;・この箇所を抜けた後にACT開始になる
;-------------------------------------------------
;　ここでIS_POLICY("금일", "징계・")等で금일의방침を喋らせる事も可能だったりしますが
;　EVENT17, 本日の방침구상と被る可能性があります
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교開始直後
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:경과시간

	;=================================================
	;できちゃった　孕ませてから産むまで物발りない為展開強化
	;=================================================
	IF 구상記入チェック(0) && CFLAG:임신 == 10
		PRINTFORML 「이제 들리지 않을까」
		PRINTFORML 
		PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%에 배에 귀를 대게 했다
		RETURN 1
	ENDIF

	IF 구상記入チェック(100) && CFLAG:임신 == 4
		PRINTFORMDL %CALLNAME:TARGET%의 표정와 해이해짐에 다 느슨해지고 있다
		PRINTFORML 「네에에에에에～♪」
		PRINTFORMW 「나중에 가르쳐 주어요%UNICODE(0x2661)%」
		RETURN 1
	ENDIF

	;=================================================
	;죠교자が少し疲れている
	;=================================================
	IF 구상記入チェック(0) && CFLAG:소모 > 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ARG:3の分岐を書いた場合は不要な箇所
	;=================================================
	;ご褒美による조교중止の場合
	;=================================================
	IF IS_POLICY("금일", "휴식")
		;消し忘れ
		;PRINTFORMW てすと　휴식
		RETURN 1
	ENDIF
	
	;ARG:1の分岐を書いた場合は不要な箇所
	;=================================================
	;죠교대상の疲労による中止
	;=================================================
	IF 구상記入チェック(0) && CFLAG:MASTER:소모 > 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상が少し疲れている
	;=================================================
	IF 구상記入チェック(0) && CFLAG:MASTER:소모 > 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상에의 호감도1000以上
	;=================================================
	IF 구상記入チェック(0) && AFFECTION("권애")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;징계・ FIRSTEVENT(20)～
;────────────────────────────────────
;・TFLAG:징계・플래그が立っている場合に表示される箇所
;・징계・플래그が立っていても、가학系以外のACTになる場合がある
;-------------------------------------------------
;가학系ACT、派生する事により가학系に含まれる可能性のある이라마치오
;의상 변경을 요구한다/기분나쁘게 웃다/항문바이브/후배위/항문섹스/자위/야외플레이/방뇨/발핥기강제
;이상系ACT/성교봉사系ACT
;上記のACT以外はTFLAG:징계・플래그によってACT_ABLEで規制される(0616版時点)
;-------------------------------------------------
;-------------------------------------------------
;・一回の죠교で一回のみ表示される
;━━━━━━━━━━━━━━━━━━━━━━━━
;징계・開始 
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:징계・플래그 && ONCEEVENT(20)
	;=================================================
	;전회ACTが징계・宣言
	;=================================================
	IF 구상記入チェック(0) && IS_PREVACTNAME("징계한다고 위협한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「조금 뜸을 끝에라고 주어요！」
		PRINTFORMDW %CALLNAME:TARGET%는 불제봉을 짓는다
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;징계・開始以外を処理する箇所
;━━━━━━━━━━━━━━━━━━━━━━━━
;징계・中
;구상記入チェック(確率％, 入力했다数字の確率で구상を表示)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:징계・플래그
	;=================================================
	;전회ACTが징계・宣言かつ今回ACTが가학系
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "가학") && IS_PREVACTNAME("징계한다고 위협한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전회ACTが징계・宣言
	;=================================================
	IF 구상記入チェック(0) && IS_PREVACTNAME("징계한다고 위협한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;질내、장내사정直後 FIRSTEVENT(30)～
;────────────────────────────────────
;NOWEX:사정ないしNOWEX:MASTER:사정で사정種の参照も可能だったりしますが
;EVENT20, 절정구상に同種の分岐がると被る可能性もあります
;-------------------------------------------------
;関連変数…
;
;TFLAG:전회질싸(1=Ｖ성교/2=Ｖ성교봉사/3=Ｖ성교(드라이사정)/4=Ｖ성교봉사(드라이사정)/5=Ａ성교/6=Ａ성교봉사/7=Ａ성교(드라이사정)/8=Ａ성교봉사(드라이사정)
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A責사정)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정直後(죠교대상→죠교자)
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
;-------------------------------------------------
;次のコマンドとの合間。事後。
;この時点では삽입が解除されている場合がある。
;-------------------------------------------------
IF 구상記入チェック(100) && GROUPMATCH(TFLAG:전회질싸, 2, 4)
	;=================================================
	;임신中질내사정
	;=================================================
	;CFLAG:TARGET:임신は조교중も立つので不自然さが否めない TALENTに修正
	IF 구상記入チェック(100) && TALENT:TARGET:임신
		PRINTFORMW 「너의 밑바닥이 없는 성욕은, 나%RAND_SPLIT("하지만 제대로/하지만 전부/의 몸으로 전부")%,%RAND_SPLIT("受け止めて/面倒見て")%준다%RAND_SPLIT("/와")%」
		RETURN 1
	ENDIF
	;=================================================
	;드라이사정
	;=================================================
	IF 구상記入チェック(100) && TFLAG:전회질싸 == 4
		IF V_SEX(MASTER)
		;Ｐアイテムが大きくなりま했다
			PRINTFORMDL %CALLNAME:TARGET%는 웃는 얼굴로, 숨을 어지럽힌다%CALLNAME:MASTER%의 입에[Ｐ](을)를 념 글자 붐비어 왔다
			PRINTFORMW 「 아직, 할 수 있네요？」
			RETURN 1
		ENDIF
	ENDIF
	;=================================================
	;질내대량사정
	;=================================================
	IF 구상記入チェック(100) && NOWEX:사정 == 3
		PRINTFORML 「%RAND_SPLIT("가득/성대/상당히 많이")%사정한 원이군요…」
		IF V_SEX(MASTER)
			PRINTFORM 「%RAND_SPLIT("이만큼 나온다면, 아직사정그래요/맞아 버릴까/그렇게임신해의일까/너의 자식으로 가득 되어 버렸다")%
			CALL HEARTBW
		ELSE
			;中出し後정액見せ付け ちんちんくてってして삽입解除分岐
			IF !RAND:2
				PRINTFORMW 「부드러워져 버렸다 원이군요」
				PRINTFORMW 「하지메일 끝내, 구라고는 하고 있는 자지도, 사랑스러워요♪」
			ELSE
				PRINTFORM 「이봐요？나의 ··응·와. 너의 %RAND_SPLIT("정액/자식")%그리고%RAND_SPLIT("真っ白/どろどろ/たぽたぽ")%
				CALL HEARTBW
			ENDIF
		ENDIF
		;임신中は抑制 上流に別分岐を作り
		SIF !RAND:4 && !CFLAG:TARGET:임신
			PRINTFORMDW 응과%CALLNAME:MASTER%의 정자가 %CALLNAME:TARGET%의 난자에 기어들어 붐비었다
		RETURN 1
	ENDIF
	;=================================================
	;질내조루사정
	;=================================================
	IF 구상記入チェック(100) && NOWEX:사정 == 4
		PRINTFORMW 「조루」
		PRINTFORMDW %CALLNAME:TARGET%는%RAND_SPLIT("심술궂은/우쭐거린 것 같은/-와했다")%미소를 띄운다
		RETURN 1
		;임신中は抑制
		SIF !RAND:10 && !CFLAG:TARGET:임신
			PRINTFORMDW 응과%CALLNAME:MASTER%의 정자가 %CALLNAME:TARGET%의 난자에 기어들어 붐비었다
	ENDIF
	;=================================================
	;드라이사정以外
	;=================================================
	IF 구상記入チェック(100)
		SELECTCASE RAND:5
			CASE 0
				PRINTFORML 「기분 좋았는지 해들？숙원을 이룬 것 같은 얼굴 해 버려서」
				PRINTFORMW 「 그렇지만 생증, 회피에는 자신이 있어…그러니까, 군요？」
			CASE 1
				PRINTFORML 「 나의 배 중(안)에서, 너의 정자와 나의 난자가」
				PRINTFORM 「합의한 간통중
				CALL HEARTBW
			CASE 2
				PRINTFORMW 「응후후. 아기 할 수 있는 것일까？」
				PRINTFORMW 「할 수 있으면 좋네요」
			CASE 3
				PRINTFORMW 「다시 한번, 좋아요？」
			CASEELSE
				PRINTFORM 「너의 자지는, 나를 잉다투기 위해 있어
				CALL HEARTBW
		ENDSELECT
		;임신中は抑制
		SIF !RAND:5 && !CFLAG:TARGET:임신
			PRINTFORMDW 응과%CALLNAME:MASTER%의 정자가 %CALLNAME:TARGET%의 난자에 기어들어 붐비었다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내사정直後(죠교대상→죠교자)
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && GROUPMATCH(TFLAG:전회질싸, 6, 8)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상記入チェック(0) && TFLAG:전회질싸 == 8
		PRINTFORMW 
		RETURN 1
	ENDIF
		
	;=================================================
	;드라이사정以外
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정直後(죠교자→죠교대상)
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && GROUPMATCH(TFLAG:전회질싸, 1, 3)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상記入チェック(0) && TFLAG:전회질싸 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;드라이사정以外
	;=================================================
	IF 구상記入チェック(100)
		SELECTCASE RAND:3
			CASE 0
				PRINTFORML 「갓난 아기, 할 수 있으면 좋네요♪」
				PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 하복부를 살그머니 어루만져 속삭였다
			CASE 1
				PRINTFORML 「너의 보지로 활용되어 버렸다」
				PRINTFORM 「기분 좋았어요
				CALL HEARTBW
				PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 뺨에 입술을 대어 왔다
			CASEELSE
				PRINTFORMDL 유도성노우를 가진다%CALLNAME:TARGET%의 정자가 %CALLNAME:MASTER%의 난자를 수정시키려고, 당 판정을 찾아 자궁을 헤엄쳐 돌고 있다
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내사정直後(죠교자→죠교대상)
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && GROUPMATCH(TFLAG:전회질싸, 5, 7)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상記入チェック(0) && TFLAG:전회질싸 == 7
		IF TALENT:MASTER:남자
			PRINTFORMDW 
			RETURN 1
		ELSE
			RETURN 0
		ENDIF
	ENDIF
	
	;=================================================
	;드라이사정以外
	;=================================================
	IF 구상記入チェック(100)
		IF TALENT:MASTER:남자
			PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 하복부를 살그머니 어루만지면(자), 뺨에 입술을 대어 왔다
			PRINTFORML 「갓난 아기, 할 수 있으면 좋네요♪」
			;%CALLNAME:TARGET%が夫で男%CALLNAME:MASTER%が嫁
			PRINTFORM 「그런 얼굴 해도 안되어요. 너는 나의 신부♪(이)겠지
			CALL HEARTBW
			RETURN 1
		ELSE
			RETURN 0
		ENDIF
	ENDIF
ENDIF

;────────────────────────────────────
;성교중・성교봉사중 FIRSTEVENT(40)～
;────────────────────────────────────
;関連変数…
;TEQUIP:성교중(죠교자が죠교대상に삽입중) (1=정상위/2=후배위/3=기승위/4=대면좌위/5=배면좌위/6=항문섹스)
;TEQUIP:성교봉사중(죠교대상が죠교자に삽입중) 内容は上記のものと同じ
;
;関連関数…
;V_SEX (=TARGET, TEQUIP:성교중 < 6/=MASTER, TEQUIP:성교봉사중 < 6)と同義
;A_SEX (=TARGET, TEQUIP:성교중 == 6/MASTER, TEQUIP:성교봉사중 == 6)と同義
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;성교중(죠교자→죠교대상)
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TEQUIP:성교중
	;=================================================
	;페니스밴드使用
	;=================================================
	IF 구상記入チェック(0) && TRUECHECK(!PENIS(TARGET), TEQUIP:죠교자페니스밴드) > 0
		;항문섹스中
		IF A_SEX(TARGET)
			PRINTFORMW 
		;その他の성교系
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;항문섹스(Ａ성교)
	;=================================================
	IF 구상記入チェック(0) && A_SEX(TARGET)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他の성교系(Ｖ성교)
	;=================================================
	;연속で表示されやすいので確率低下
	IF 구상記入チェック(25) && V_SEX(TARGET)
		PRINTFORMDW %CALLNAME:TARGET%는, %CALLNAME:MASTER%(은)는%RAND_SPLIT("임신한다/임신한다")%까지%RAND_SPLIT("성교を/交尾/こづくり섹스")%(을)를 %RAND_SPLIT("그만둘 생각은 없다/계속할 생각의")%같다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교봉사중(죠교대상→죠교자)
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TEQUIP:성교봉사중
	;=================================================
	;페니스밴드使用
	;=================================================
	IF 구상記入チェック(0) && TRUECHECK(!PENIS(MASTER), TEQUIP:페니스밴드) > 0
		;항문섹스中
		IF A_SEX(MASTER)
			PRINTFORMW 
		;その他성교봉사系
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;항문섹스(Ａ성교봉사)
	;=================================================
	IF 구상記入チェック(0) && A_SEX(MASTER)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;%CALLNAME:MASTER%が[정욕]かつ%CALLNAME:TARGET%が[정욕]でない
	;=================================================
	;構文ミス。연속で出やすいので確率調整。20
	;IF 구상記入チェック(100) && IS_CONDITION(TARGET, "정욕") == 0 && IS_CONDITION(TARGET, "정욕") == 0
	;전회コマンドが消極的でもこうなってしまう為コメントアウト
	;	PRINTFORMW 「もうちょっと優しく…聞いてないわね…」
	;	RETURN 1
	;ENDIF
	;出来に納得できない為コメントアウト
	;IF 구상記入チェック(20) && IS_CONDITION(MASTER, "정욕") && IS_CONDITION(TARGET, "정욕") == 0
	;	;レイプ願望があったとしても、実際にされるのは別。という事で、へたれいむ
	;	PRINTFORMDW 「ちょっと…あは、は…怖いわよ？」
	;	RETURN 1
	;ENDIF
	;=================================================
	;その他성교봉사系(Ｖ성교봉사)
	;=================================================
	;연속실행されやすいので確率低下
	IF 구상記入チェック(50) && V_SEX(MASTER)
		SELECTCASE RAND:5
			CASE 0
				PRINTFORMDL 한숨에 열을 띠게 했다%CALLNAME:TARGET%의 피부를 주의 땀이 탄다…
			CASE 1
				PRINTFORML 「연결되고 있을 뿐으로 기분 좋아져요…」
				PRINTFORML 「어 흩어져, 좋네요」
				;表記しなくても発言側で十分
				;PRINTFORMDL %CALLNAME:TARGET%は%CALLNAME:MASTER%に微笑んだ
			CASE 2
				PRINTFORMDL 두 사람 체온을 교환하면서, %CALLNAME:MASTER%의 체온은 %CALLNAME:TARGET%와 함께 열을 띠어 갔다
			CASE 3
				PRINTFORMDL %CALLNAME:TARGET%의 교성에 %CALLNAME:MASTER%의 귀와 속마음은 간지러워져%CALLNAME:MASTER%(을)를 점차 날카로워지게 해 갔다…
			CASEELSE
				PRINTFORML 「좀 더 격렬하게 해도, 나는 괜찮아요」
				;らめぇは思ったより수치を煽ってくれなかった為コメントアウト
				[SKIPSTART]
				;PRINTFORML 「でも、そ했다らあんたがもう%RAND_SPLIT("駄目/駄目ぇ/らめぇ")%ってなっちゃうかしら？」
				;後に繋げる為無難さが필요。
				IF TCVAR:MASTER:가버려
				ELSEIF TCVAR:가버려
				ELSE
				ENDIF
				[SKIPEND]
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;절정했다直後 FIRSTEVENT(50)～
;────────────────────────────────────
;・죠교자、죠교대상が절정했다場合に表示される구상
;・個別の部位別절정を拾いたいなら、IF 구상記入チェック(0) && NOWEX:Ｃ절정等の分岐を追加してください
;-------------------------------------------------
;関連変数…
;・사정種はNOWEX:사정/NOWEX:MASTER:사정で参照する事が可能です
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A責사정)
;
;関連関数…
;TRUECHECK 真になるものがいくつあるか을반환
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자が절정했다直後
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정) > 0
	;=================================================
	;죠교자사정
	;=================================================
	IF 구상記入チェック(100) && NOWEX:사정
		PRINTFORML 「…」
		PRINTFORML 「어쩐지…인 일이 아무래도 좋아졌어요…」
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 머리를 어루만지면서 멍하니 하고 있다
		RETURN 1
	ENDIF
	
	;各部位の절정が1つ以上の場合に真になる箇所
	;=================================================
	;다중절정
	;=================================================
	IF 구상記入チェック(0) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정) > 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が절정했다直後
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정) > 0
	;=================================================
	;사정
	;=================================================
	IF 구상記入チェック(100) && SHOOTING(MASTER, "드라이사정")
		PRINTFORMDW %CALLNAME:TARGET%는 웃는 얼굴로, 숨을 어지럽힌다%CALLNAME:MASTER%의 입에[Ｐ](을)를 념 글자 붐비어 왔다
		PRINTFORMW 「 아직, 할 수 있네요？」
		RETURN 1
	ENDIF
	;=================================================
	;사정
	;=================================================
	IF 구상記入チェック(100) && NOWEX:MASTER:사정
		IF TEQUIP:MASTER:하반신속옷
			PRINTFORMW 「하얀색 배어 나오고 있다… %RAND_SPLIT("가득/많이")% 사정한 것 같구나♪」
;OriginalString : 아웃8, "「ところで…」")
			CALL FADEOUT(8, "「그런데…」")
			PRINTFORMW 「그 %RAND_SPLIT("질척질척한/끈적해진")% 속옷을 씻는 것은 나인데, 너는 알고 있는 거야？」
		ENDIF
		RETURN 1
	ENDIF
	
	;各部位の절정が1つ以上の場合に真になる箇所
	;=================================================
	;다중절정
	;=================================================
	IF 구상記入チェック(0) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정) > 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;죠교방침変更 FIRSTEVENT(60)
;────────────────────────────────────
;・죠교방침が変わったときに表示する
;・あくまでも방침なので행동を細かく決定するものではない
;-------------------------------------------------
;関連変数…
;TFLAG:턴방침		このターンの죠교방침(1=휴식/2=소프트/3=노멀/4=하드/5=이상)
;TFLAG:전턴방침		전턴の죠교방침(内容は턴방침と同じ)
;
;関連関数…
;IS_POLICY	죠교방침名称と合致すれば真을반환
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교방침変更
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:턴방침 != TFLAG:전턴방침
	;=================================================
	;턴방침1:휴식（確実に체력回復행동になる）
	;=================================================
	IF 구상記入チェック(0) && IS_POLICY("今", "휴식")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침2:소프트
	;=================================================
	IF 구상記入チェック(0) && IS_POLICY("今", "소프트")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침3:노멀
	;=================================================
	IF 구상記入チェック(0) && IS_POLICY("今", "노멀")
		PRINTFORMDW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침4:하드
	;=================================================
	IF 구상記入チェック(0) && IS_POLICY("今", "하드")
		PRINTFORMDW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침5:이상
	;=================================================
	IF 구상記入チェック(0) && IS_POLICY("今", "이상")
		PRINTFORMDW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;条件なし FIRSTEVENT(70)～
;────────────────────────────────────
;・ここまでの条件にヒットしなかった場合に表示される箇所
;・BASEで小分岐を設けてありますが、任意の条件を追加する事もできます
;------------------------------------------------
;関連変数…
;BASE		(0, 체력/1, 기력/2, 사정/3, 모유/4, 소변/5, 이성/6, 흥미/7, 초조함/8, 만족)
;
;関連関数…
;TENSION	引数で指定했다BASE値を表す(50%以上は高い、25%以上は一般、25%未満は低い)
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;条件なし
;구상記入チェック（確率％, 入力했다数字の確率で구상を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0)
	;=================================================
	;만족50％以上
	;=================================================
	IF 구상記入チェック(0) && TENSION(TARGET, 8) == "高い"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;이성25％以下
	;=================================================
	IF 구상記入チェック(0) && TENSION(TARGET, 5) == "低い"
		RETURN 1
	ENDIF
	
	;=================================================
	;흥미25％以下
	;=================================================
	IF 구상記入チェック(0) && TENSION(TARGET, 6) == "低い"
		RETURN 1
	ENDIF
	
	;=================================================
	;초조함50％以上
	;=================================================
	IF 구상記入チェック(0) && TENSION(TARGET, 7) == "高い"
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
