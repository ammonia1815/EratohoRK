;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 죠교자행동 전이벤트구상파일
;
;	죠교자행동 전이벤트구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV12, 죠교자행동 전구상
;========================================================================
;·TRAIN_PROCCES.ERB내, @TRAIN_CANCEL로부터 불려 간다
;·구상주의 파라미터 표시의 직후에 표시. 죠교지시는 이미 결정되고 있다
;·조교중지선언의 교환이나, 죠교턴 개시전에주인님로부터 한 마디, 와 같이 쓰는 일도 가능
;·오전, 오후의죠교개시는 아니고, 죠교턴의 개시전에는 매회 불려 가는 개소
;·그 때문에도 있어, 조교중지시와경과시간 0 이외의 개소는 IF 구상기입 체크(25) 등으로서 확률 표시하는 일을 추천
;·조교중지시에게만 지문이 존재한다
;·문장 표시 후에 본체측의 DRAWLINE 처리 있어
;-------------------------------------------------
;FIRSTEVENT(0)～나, FIRSTEVENT(10)～등으로 해서 테두리마다 할당하고 있습니다만
;FIRSTEVENT/ONCEEVENT/RANDEVENT/ORDEREVENT는 사실상 무한하게이벤트번호를 늘릴 수 있으므로
;필요에 응해 임의에 할당해 주세요
;-------------------------------------------------
@KOJO_EVENT_K1_12(ARG)
;-------------------------------------------------
;인수 내역…
;ARG(0=죠교 가능/1=구상주의 피로로 중지/2=죠교대상의 피로로 중지/3= 포상에 의한 중지)
;-------------------------------------------------

;────────────────────────────────────
;조교중지 ARG:1 ARG:2 ARG:3 FIRSTEVENT(0)～
;────────────────────────────────────
;·피로, 혹은 포상에 의한조교중지의 경우의구상
;·기입 체크=1의 경우는구상표시 후 본체측에서 PRINTL의 공행이 들어간다
;·기입 체크를=2로 하면, 지문을 표시하지 않는다
;·이라고 할까 RESULT > 2로 판정되므로 2이상이라면 같은 거동이 됩니다만 일단 비추천이라고 하는 일로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;포상에 의한 중지
;RESULT치（1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 3
	;기입 체크를 2 이하에했다경우는 이하의 지문이 표시된다. 표시 타이밍은구상표시의 뒤
	;%CALLNAME:TARGET%는금일의죠교를 중지해,
	;%CALLNAME:MASTER%멈추거나와했다시간를 과 했다…
	
	SELECTCASE TIME
		;낮
		CASE 0
			;앉는 묘사는 없다. 이미 근처에앉아있다.
			PRINTFORMDW 휴게중, %CALLNAME:MASTER%의 근처에 앉는다%CALLNAME:TARGET%는 펑펑무릎을 두드려 유혹해 왔다…
			PRINTFORMW 「이봐요, 여기 오세요」
		;밤
		CASEELSE
			PRINTFORMDW %CALLNAME:TARGET%는 턱을 괴는 행동을 붙어, 식사를 먹는 %CALLNAME:MASTER%(을)를 응시하고 있다…
			SELECTCASE RAND:3
				CASE 0
					;아무것도 없음
				CASE 1
					PRINTFORML
					PRINTFORMDW %CALLNAME:TARGET%는 시선을 창을 향한다
					PRINTFORMW 「오늘 밤은 달을 보면서 술을 마심이군요」
					;말하게 하지 않는 것이 좋은 맛 내자
					;PRINTFORMW 「너에게도 교제해 받아요. 저녁 반주」
				CASEELSE
					PRINTFORML
					PRINTFORMW 「뭔가 붙고 있는지는?」
					PRINTFORMW 「 는 , 붙어있는 원이야」
			ENDSELECT
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자의 피로에 의한 중지
;RESULT치（1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	;기입 체크를 2 이하에했다경우는 이하의 지문이 표시된다. 표시 타이밍은구상표시의 뒤
	;조교의 시간이다가, 들어 온 %CALLNAME:TARGET%는 어쩐지 피곤한 모습으로,
	;이번은 휴일이라고 말해 나갔다…
	
	;기본적으로 「인덴트」는 「네스트 1 계층을 의미한다」라고 하는 논리적인 의미를 가지고 있는 것이 조건, 이라고 생각합니다@/L
	SELECTCASE TIME
		;낮간
		CASE 0
			PRINTFORMW 「오늘은 휴식을 하자」
			PRINTFORML
			PRINTFORMDW %CALLNAME:TARGET%는 거실에서 데굴데굴 하고 있다…
			PRINTFORMDW %CALLNAME:MASTER%(은)는 생각나는 대로 전떡을 손에 들어, %CALLNAME:TARGET%의 입가에 접근해 보았다…
			PRINTFORML
			;정식 동물 바다표범과 같게 나라의 사슴과 같이 횡포에 먹는다
			PRINTFORMW 도! ……… 도… 도……
			PRINTFORML
			PRINTFORMDW %CALLNAME:MASTER%(은)는 욕망에 찔러 작동되는 대로, 다음 되는 전떡에 손을 뻗었다…
			;밤가 깊어지는 것에 연결된다
		;밤
		CASEELSE
			PRINTFORMDW %CALLNAME:TARGET%는 방에서 나오자, 곧바로 베개를 안아 돌아왔다
			PRINTFORML 
			PRINTFORMW 「휴일이라고는 말했지만, 나랑 같이.」
			PRINTFORMW 「너의 팔, 빌릴게」
			PRINTFORML 
			PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 팔베개를 받으며 잤다
	ENDSELECT
	;구상만 표시
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상의 피로에 의한 중지
;RESULT치（1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 2
	;기입 체크를 2 이하에했다경우는 이하의 지문이 표시된다. 표시 타이밍은구상표시의 뒤
	;조교의 시간이다가, %CALLNAME:TARGET%는 심한 모습의 %CALLNAME:MASTER%를 봐 당분간 생각하면,
	;이번은 쉬어도 좋다고 말해 나갔다…
	
	;상의땅의 분은 실제로는구상아래에 표시된다
	SELECTCASE TIME
		;낮간
		CASE 0
	PRINTFORMDW 조교의 시간이 왔지만, %CALLNAME:TARGET%는 심한 모습의 %CALLNAME:MASTER%(을)를 봐 당분간 생각하면, 이번은 쉬어도 좋다고 말해 나와 갔다…
	
			PRINTFORMDW %CALLNAME:TARGET%는 방에서 나와, 차를 들고 돌아왔다
			PRINTFORML
			PRINTFORMW …
			PRINTFORML
			PRINTFORMDL …
			PRINTFORMW 후릅…
			PRINTFORML 
			PRINTFORMDW 그리고, 두 명은 한가롭게 차를 훌쩍거린다
		;밤
		CASEELSE
			PRINTFORMDW %CALLNAME:TARGET%는 방에서 나오자, 곧바로 베개를 안고 돌아왔다
			PRINTFORMW 「쉬어도 괜찮다고는 말했지만, 나랑 같이.」
			PRINTFORML 
			PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%에게 베개를 주고, %CALLNAME:MASTER%(을)를 안아 베개로 한다
			PRINTFORML 
			PRINTFORMW 「다음은, 이번 분도 같이 전부 짜 낼 태니깐」
			PRINTFORMW 「각오 해 둬」
	ENDSELECT
	;구상만 표시
	RETURN 2
ENDIF
	
;-------------------------------------------------
;·이하, ARG:0(죠교 가능) 상당
;·TRAIN_PROCCES.ERB내, @BEFORE_ACT로부터 불려 간다
;·이 시점에서 ACT는 벌써 결정하고 있지만 ACT파생의 결정은 이루어지지 않았다
;·NOWEX의 리셋트는 되지 않았다
;·이것들의 개소를 빠진 뒤로 ACT 개시가 된다
;·분기의 증감은자유로 할 수 있습니다만, 우선도에는 주의합시다
;-------------------------------------------------

;────────────────────────────────────
;죠교개시 직후 FIRSTEVENT(10)～
;────────────────────────────────────
;·경과시간가 0인 경우에 표시된다
;·그 회의죠교개시 직후에 한 번만으로, 이후 그조교중에는 표시되지 않는다
;·이 개소를 빠진 뒤로 ACT 개시가 된다
;-------------------------------------------------
;  여기서 IS_POLICY("금일", "징계·") 등으로금일의방침를 말하게 하는 일도 가능했다거나 합니다만
;  EVENT17, 오늘의방침구상와 입을 가능성이 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교개시 직후
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:경과시간

	;=================================================
	;가능하게 되었다  임신하게 하고 나서 낳을 때까지 물건발리나 있고 때문 전개 강화
	;=================================================
	IF 구상기입체크(0) && CFLAG:임신 == 10
		PRINTFORML 「이제 들리지 않을까」
		PRINTFORML 
		PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%에 배에 귀를 대게 했다
		RETURN 1
	ENDIF

	IF 구상기입체크(100) && CFLAG:임신 == 4
		PRINTFORMDL %CALLNAME:TARGET%의 표정와 해이해짐에 다 느슨해지고 있다
		PRINTFORML 「네에에에에에～♪」
		PRINTFORMW 「나중에 가르쳐 주어요%UNICODE(0x2661)%」
		RETURN 1
	ENDIF

	;=================================================
	;죠교자가 조금 피곤함
	;=================================================
	IF 구상기입체크(0) && CFLAG:소모 > 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ARG:3의 분기를 썼을 경우는 불필요한 개소
	;=================================================
	;포상에 의한조교중지의 경우
	;=================================================
	IF IS_POLICY("금일", "휴식")
		;끄는 것을 잊어버림
		;PRINTFORMW 와  휴식
		RETURN 1
	ENDIF
	
	;ARG:1의 분기를 썼을 경우는 불필요한 개소
	;=================================================
	;죠교대상의 피로에 의한 중지
	;=================================================
	IF 구상기입체크(0) && CFLAG:MASTER:소모 > 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가 조금 피곤함
	;=================================================
	IF 구상기입체크(0) && CFLAG:MASTER:소모 > 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상에의 호감도 1000 이상
	;=================================================
	IF 구상기입체크(0) && AFFECTION("권애")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;징계· FIRSTEVENT(20)～
;────────────────────────────────────
;·TFLAG:징계·플래그가 서 있는 경우에 표시되는 개소
;·징계·플래그가 서 있어도, 가학계 이외의 ACT가 되는 경우가 있다
;-------------------------------------------------
;가학계 ACT, 파생하는 일에 의해가학계에 포함될 가능성이 있는이라마치오
;의상 변경을 요구한다/기분나쁘게 웃다/항문바이브/후배위/항문섹스/자위/야외플레이/방뇨/발핥기강제
;이상계 ACT/성교봉사계 ACT
;상기의 ACT 이외는 TFLAG:징계·플래그에 의해 ACT_ABLE로 규제된다(0616판 시점)
;-------------------------------------------------
;-------------------------------------------------
;·1회의죠교로 1회만 표시된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;징계·개시 
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:징계·플래그 && ONCEEVENT(20)
	;=================================================
	;전회ACT가징계·선언
	;=================================================
	IF 구상기입체크(0) && IS_PREVACTNAME("징계한다고 위협한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「조금 뜸을 끝에라고 주어요!」
		PRINTFORMDW %CALLNAME:TARGET%는 불제봉을 짓는다
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;징계·개시 이외를 처리하는 개소
;━━━━━━━━━━━━━━━━━━━━━━━━
;징계·안
;구상기입 체크(확률％, 입력했다숫자의 확률로구상를 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:징계·플래그
	;=================================================
	;전회ACT가징계·선언 한편 이번 ACT가가학계
	;=================================================
	IF 구상기입체크(0) && MENUMATCH(TFLAG:ACT, "가학") && IS_PREVACTNAME("징계한다고 위협한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전회ACT가징계·선언
	;=================================================
	IF 구상기입체크(0) && IS_PREVACTNAME("징계한다고 위협한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;질내, 장내사정직후 FIRSTEVENT(30)～
;────────────────────────────────────
;NOWEX:사정내지 NOWEX:MASTER:사정로사정종의 참조도 가능했다거나 합니다만
;EVENT20, 절정구상에 동종의 분기와 입을 가능성도 있습니다
;-------------------------------------------------
;관련 변수…
;
;TFLAG:전회질싸(1=Ｖ성교/2=Ｖ성교봉사/3=Ｖ성교(드라이사정)/4=Ｖ성교봉사(드라이사정)/5=Ａ성교/6=Ａ성교봉사/7=Ａ성교(드라이사정)/8=Ａ성교봉사(드라이사정)
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정직후(죠교대상→죠교자)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
;-------------------------------------------------
;다음의 커맨드와의 사이. 사후.
;이 시점에서는삽입가 해제되고 있는 경우가 있다.
;-------------------------------------------------
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질싸, 2, 4)
	;=================================================
	;임신중질내사정
	;=================================================
	;CFLAG:TARGET:임신는조교중도 서므로 부자연스러움이 부정할 수 없는 TALENT에 수정
	IF 구상기입체크(100) && TALENT:TARGET:임신
		PRINTFORMW 「너의 밑바닥이 없는 성욕은, %RAND_SPLIT("내가 제대로/내가 전부/내 몸으로")%, %RAND_SPLIT("받아/챙겨")% 줄게
		RETURN 1
	ENDIF
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 4
		IF V_SEX(MASTER)
		;Ｐ 아이템이 크고 했다
			PRINTFORMDL %CALLNAME:TARGET%는 웃는 얼굴로, 숨을 어지럽힌다%CALLNAME:MASTER%의 입에를 념 글자 붐비어 왔다
			PRINTFORMW 「아직, 할 수 있네요?」
			RETURN 1
		ENDIF
	ENDIF
	;=================================================
	;질내대량사정
	;=================================================
	IF 구상기입체크(100) && NOWEX:사정 == 3
		PRINTFORML 「%RAND_SPLIT("가득/성대/상당히 많이")%사정한 원이군요…」
		IF V_SEX(MASTER)
			PRINTFORM 「%RAND_SPLIT("이만큼 나온다면, 아직사정그래요/맞아 버릴까/그렇게임신해의일까/너의 자식으로 가득 되어 버렸다")%
			CALL HEARTBW
		ELSE
			;질내사정 후정액 과시하고 자지구라고는 해삽입해제 분기
			IF !RAND:2
				PRINTFORMW 「부드러워져 버렸다 원이군요」
				PRINTFORMW 「하지메일 끝내, 구라고는 하고 있는 자지도, 사랑스러워요♪」
			ELSE
				PRINTFORM 「이봐요? 나의… 응·와. 너의 %RAND_SPLIT("정액/자식")%그리고%RAND_SPLIT("진흰색/질척질척/")%
				CALL HEARTBW
			ENDIF
		ENDIF
		;임신중은 억제 상류에 별분기를 만들어
		SIF !RAND:4 && !CFLAG:TARGET:임신
			PRINTFORMDW %CALLNAME:MASTER%의 정자가 %CALLNAME:TARGET%의 난자를 쑤시고 있다
		RETURN 1
	ENDIF
	;=================================================
	;질내조루사정
	;=================================================
	IF 구상기입체크(100) && NOWEX:사정 == 4
		PRINTFORMW 「조루」
		PRINTFORMDW %CALLNAME:TARGET%는 %RAND_SPLIT("심술궂은/우쭐거린 것 같은/이긴 듯한")% 미소를 띄운다
		RETURN 1
		;임신중은 억제
		SIF !RAND:10 && !CFLAG:TARGET:임신
			PRINTFORMDW %CALLNAME:MASTER%의 정자가 %CALLNAME:TARGET%의 난자를 쑤시고 있다
	ENDIF
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RAND:5
			CASE 0
				PRINTFORML 「기분 좋았어? 숙원을 이룬 것 같은 표정이네」
				PRINTFORMW 「하지만 역시, 회피에는 자신이 있어… 그러니까, 좀 더?」
			CASE 1
				PRINTFORML 「 나의 배 안에서, 너의 정자와 나의 난자가」
				PRINTFORM 「합의 하 간통중
				CALL HEARTBW
			CASE 2
				PRINTFORMW 「응후후. 아기 할 수 있는 것일까?」
				PRINTFORMW 「할 수 있으면 좋네요」
			CASE 3
				PRINTFORMW 「다시 한번, 좋아요?」
			CASEELSE
				PRINTFORM 「너의 자지는, 나를 임신시키기 위해 있어
				CALL HEARTBW
		ENDSELECT
		;임신중은 억제
		SIF !RAND:5 && !CFLAG:TARGET:임신
			PRINTFORMDW %CALLNAME:MASTER%의 정자가 %CALLNAME:TARGET%의 난자를 쑤시고 있다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내사정직후(죠교대상→죠교자)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && GROUPMATCH(TFLAG:전회질싸, 6, 8)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크(0) && TFLAG:전회질싸 == 8
		PRINTFORMW 
		RETURN 1
	ENDIF
		
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정직후(죠교자→죠교대상)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질싸, 1, 3)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크(0) && TFLAG:전회질싸 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RAND:3
			CASE 0
				PRINTFORML 「갓난 아기, 할 수 있으면 좋네요♪」
				PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 하복부를 살그머니 어루만져 속삭였다
			CASE 1
				PRINTFORML 「너의 보지로 활용되어 버렸다」
				PRINTFORM 「기분 좋았어요
				CALL HEARTBW
				PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 뺨에 입술을 대어 왔다
			CASEELSE
				PRINTFORMDL 유도성노우를 가진다%CALLNAME:TARGET%의 정자가 %CALLNAME:MASTER%의 난자를 수정시키려고, 당 판정을 찾아 자궁을 헤엄쳐 돌고 있다
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내사정직후(죠교자→죠교대상)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질싸, 5, 7)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크(0) && TFLAG:전회질싸 == 7
		IF TALENT:MASTER:남자
			PRINTFORMDW 
			RETURN 1
		ELSE
			RETURN 0
		ENDIF
	ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(100)
		IF TALENT:MASTER:남자
			PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 하복부를 살그머니 어루만지면(자), 뺨에 입술을 대어 왔다
			PRINTFORML 「갓난 아기, 할 수 있으면 좋네요♪」
			;%CALLNAME:TARGET%가 남편으로 남자 %CALLNAME:MASTER%가 신부
			PRINTFORM 「그런 얼굴 해도 안되어요. 너는 나의 신부♪(이)겠지
			CALL HEARTBW
			RETURN 1
		ELSE
			RETURN 0
		ENDIF
	ENDIF
ENDIF

;────────────────────────────────────
;성교중·성교봉사중 FIRSTEVENT(40)～
;────────────────────────────────────
;관련 변수…
;TEQUIP:성교중(죠교자가죠교대상에삽입중) (1=정상위/2=후배위/3=기승위/4=대면좌위/5=배면좌위/6=항문섹스)
;TEQUIP:성교봉사중(죠교대상가죠교자에삽입중) 내용은 상기의 것과 같음
;
;관련 함수…
;V_SEX (=TARGET, TEQUIP:성교중 < 6/=MASTER, TEQUIP:성교봉사중 < 6)와 동의
;A_SEX (=TARGET, TEQUIP:성교중 == 6/MASTER, TEQUIP:성교봉사중 == 6)와 동의
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;성교중(죠교자→죠교대상)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TEQUIP:성교중
	;=================================================
	;페니스밴드사용
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(!PENIS(TARGET), TEQUIP:죠교자페니스밴드) > 0
		;항문섹스중
		IF A_SEX(TARGET)
			PRINTFORMW 
		;그 외의성교계
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;항문섹스(Ａ성교)
	;=================================================
	IF 구상기입체크(0) && A_SEX(TARGET)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외의성교계(Ｖ성교)
	;=================================================
	;연속로 표시되기 쉽기 때문에 확률 저하
	IF 구상기입체크(25) && V_SEX(TARGET)
		PRINTFORMDW %CALLNAME:TARGET%는, %CALLNAME:MASTER%(은)는%RAND_SPLIT("임신한다/임신한다")%까지%RAND_SPLIT("성교를/교미/개만들기섹스")%(을)를 %RAND_SPLIT("그만둘 생각은 없다/계속할 생각의")%같다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교봉사중(죠교대상→죠교자)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TEQUIP:성교봉사중
	;=================================================
	;페니스밴드사용
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(!PENIS(MASTER), TEQUIP:페니스밴드) > 0
		;항문섹스중
		IF A_SEX(MASTER)
			PRINTFORMW 
		;그 외성교봉사계
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;항문섹스(Ａ성교봉사)
	;=================================================
	IF 구상기입체크(0) && A_SEX(MASTER)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;%CALLNAME:MASTER%가[정욕]또한 %CALLNAME:TARGET%가[정욕]가 아니다
	;=================================================
	;구문 미스. 연속로 나오기 쉽기 때문에 확률 조정. 20
	;IF 구상기입 체크(100) && IS_CONDITION(TARGET, "정욕") == 0 && IS_CONDITION(TARGET, "정욕") == 0
	;전회커맨드가 소극적이어도 이렇게 되어 버리기 때문에(위해) comment out
	;	PRINTFORMW 「좀 더 상냥하고…(듣)묻지 않네요…」
	;	RETURN 1
	;ENDIF
	;성과에 납득 할 수 없기 때문에 comment out
	;IF 구상기입 체크(20) && IS_CONDITION(MASTER, "정욕") && IS_CONDITION(TARGET, "정욕") == 0
	;	;강간 욕구가 있었다고 해도, 실제로 되는 것은 구별. 이런 일로, 에 늘어뜨림 있고
	;	PRINTFORMDW 「조금… 아하, 무… 무서워요?」
	;	RETURN 1
	;ENDIF
	;=================================================
	;그 외성교봉사계(Ｖ성교봉사)
	;=================================================
	;연속실행 되기 쉽기 때문에 확률 저하
	IF 구상기입체크(50) && V_SEX(MASTER)
		SELECTCASE RAND:5
			CASE 0
				PRINTFORMDL 한숨에 열을 띠게 해 %CALLNAME:TARGET%의 피부는 땀으로 번질거린다…
			CASE 1
				PRINTFORML 「연결되고 있을 뿐으로 기분 좋아져…」
				PRINTFORML 「앗 흔드는거, 좋아」
				;표기하지 않아도 발언측에서 충분히
				;PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%에 미소지었다
			CASE 2
				PRINTFORMDL 두 사람 체온을 교환하면서, %CALLNAME:MASTER%의 체온은 %CALLNAME:TARGET%와 함께 뜨거워진다
			CASE 3
				PRINTFORMDL %CALLNAME:TARGET%의 교성에 %CALLNAME:MASTER%의 귀와 속마음은 간지러워져 %CALLNAME:MASTER%(을)를 점차 흥분시킨다…
			CASEELSE
				PRINTFORML 「좀 더 격렬하게 해도, 나는 괜찮아」
				;등 째는 생각했던 것보다수치를 부추겨 주지 않았던 때문 comment out
				[SKIPSTART]
				;PRINTFORML 「그렇지만, 했다등 너가 이제(벌써)%RAND_SPLIT("안돼/안돼/앙돼에")%는 되어 버릴까?」
				;뒤로 연결하기 때문에(위해) 무난함이필요.
				IF TCVAR:MASTER:가버려
				ELSEIF TCVAR:가버려
				ELSE
				ENDIF
				[SKIPEND]
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;절정했다직후 FIRSTEVENT(50)～
;────────────────────────────────────
;·죠교자, 죠교대상가절정했다경우에 표시되는구상
;·개별의 부위별절정를 줍고 싶으면, IF 구상기입 체크(0) && NOWEX:Ｃ절정등의 분기를 추가해 주세요
;-------------------------------------------------
;관련 변수…
;·사정종은 NOWEX:사정/NOWEX:MASTER:사정로 참조하는 것이 가능합니다
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;
;관련 함수…
;TRUECHECK 실로 되는 것이 몇개 있을까을반환
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자가절정했다직후
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정) > 0
	;=================================================
	;죠교자사정
	;=================================================
	IF 구상기입체크(100) && NOWEX:사정
		PRINTFORML 「…」
		PRINTFORML 「어쩐지…인 일이 아무래도 좋아졌어요…」
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 머리를 어루만지면서 멍하니 하고 있다
		RETURN 1
	ENDIF
	
	;각 부위의절정가 1개이상의 경우에 실로 되는 개소
	;=================================================
	;다중절정
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정) > 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가절정했다직후
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정) > 0
	;=================================================
	;사정
	;=================================================
	IF 구상기입체크(100) && SHOOTING(MASTER, "드라이사정")
		PRINTFORMDW %CALLNAME:TARGET%는 웃는 얼굴로, 숨을 어지럽힌다%CALLNAME:MASTER%의 입에를 념 글자 붐비어 왔다
		PRINTFORMW 「아직, 할 수 있네요?」
		RETURN 1
	ENDIF
	;=================================================
	;사정
	;=================================================
	IF 구상기입체크(100) && NOWEX:MASTER:사정
		IF TEQUIP:MASTER:하반신속옷
			PRINTFORMW 「하얀색 배어 나오고 있다… %RAND_SPLIT("가득/많이")% 사정한 것 같구나♪」
;OriginalString : 아웃8, "「ところで…」")
			CALL FADEOUT(8, "「그런데…」")
			PRINTFORMW 「그 %RAND_SPLIT("질척질척한/끈적해진")% 속옷을 씻는 것은 나인데, 너는 알고 있는 거야?」
		ENDIF
		RETURN 1
	ENDIF
	
	;각 부위의절정가 1개이상의 경우에 실로 되는 개소
	;=================================================
	;다중절정
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정) > 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;죠교방침변경 FIRSTEVENT(60)
;────────────────────────────────────
;·죠교방침가 바뀌었을 때에 표시한다
;·어디까지나방침인 것로행동를 세세하게 결정하는 것은 아니다
;-------------------------------------------------
;관련 변수…
;TFLAG:턴방침		이 턴의죠교방침(1=휴식/2=소프트/3=노멀/4=하드/5=이상)
;TFLAG:전턴방침		전턴의죠교방침(내용은턴방침와 같음)
;
;관련 함수…
;IS_POLICY	죠교방침명칭과 합치하면 진을반환
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교방침변경
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:턴방침 != TFLAG:전턴방침
	;=================================================
	;턴방침 1:휴식（확실히체력회복행동가 된다）
	;=================================================
	IF 구상기입체크(0) && IS_POLICY("지금", "휴식")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 2:소프트
	;=================================================
	IF 구상기입체크(0) && IS_POLICY("지금", "소프트")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 3:노멀
	;=================================================
	IF 구상기입체크(0) && IS_POLICY("지금", "노멀")
		PRINTFORMDW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 4:하드
	;=================================================
	IF 구상기입체크(0) && IS_POLICY("지금", "하드")
		PRINTFORMDW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 5:이상
	;=================================================
	IF 구상기입체크(0) && IS_POLICY("지금", "이상")
		PRINTFORMDW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;조건 없음 FIRSTEVENT(70)～
;────────────────────────────────────
;·여기까지의 조건에 히트 하지 않았던 경우에 표시되는 개소
;·BASE로 소분기를 마련해 있습니다만, 임의의 조건을 추가하는 일도 할 수 있습니다
;------------------------------------------------
;관련 변수…
;BASE		(0, 체력/1, 기력/2, 사정/3, 모유/4, 소변/5, 이성/6, 흥미/7, 초조함/8, 만족)
;
;관련 함수…
;TENSION	인수로 지정했다BASE치를 나타낸다(50%이상은 높은,25%이상은 일반,25% 미만은 낮다)
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조건 없음
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;만족 50％이상
	;=================================================
	IF 구상기입체크(0) && TENSION(TARGET, 8) == "높다"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;이성 25％이하
	;=================================================
	IF 구상기입체크(0) && TENSION(TARGET, 5) == "낮다"
		RETURN 1
	ENDIF
	
	;=================================================
	;흥미 25％이하
	;=================================================
	IF 구상기입체크(0) && TENSION(TARGET, 6) == "낮다"
		RETURN 1
	ENDIF
	
	;=================================================
	;초조함 50％이상
	;=================================================
	IF 구상기입체크(0) && TENSION(TARGET, 7) == "높다"
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
