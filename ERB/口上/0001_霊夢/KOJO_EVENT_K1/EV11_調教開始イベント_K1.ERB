;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 죠교개시이벤트구상파일
;
;	죠교개시이벤트구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV11, 죠교개시구상
;========================================================================
;·대체로의 경우, 구상작자가 우선 생각하는 것은 "주인"으로서의구상라고 생각되므로
;·@KOJO_MS_EVENT로서 TALENT:주인님인 경우로 한정해 버립니다
;·이벤트관련은구상주의 입장에 의해 정합성이 손상될 가능성이 있으므로 이러한 처치로 섬했다
;·구상내의 모든 개소에서구상함수에 의한 같은 배타적 연산은 가능했다거나 합니다
;-------------------------------------------------
;  소환되어죠교를 맡겨지는 경우의구상를 쓴다면
;  호출을@KOJO_EVENT_KX1_11으로서 배타를 실시하지 않고, TALENT:주인님와 그 외로 분기를 마련한다
;  EVENT1, OP(주인인 경우에 밖에 처리되지 않는다)로 KFLAG에의 대입을 실시해, 분기를 마련한다
;  혹은, @KOJO_AS_EVENT_KX1_11으로서 별파일에 분리해 버리면 좋을 것입니다
;-------------------------------------------------
;우선 분기를 마련해 있습니다만, EVENT11범위내의 분기의 증감은자유로 할 수 있습니다
;COMMON_GETTER_CHARA.ERB내, @AFFECTION에는 여기에 없는 것도 정의되어 있거나 합니다
;다만, 조건의 느슨한 분기를 상층에 가지고 오면 하위의 분기가 모두 죽어구상가 되므로
;우선도에는 충분히 주의해 주세요
;-------------------------------------------------
@KOJO_MS_EVENT_K1_11(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 통상/1, 죠교자의 피로에 의한 중지/2, 죠교대상의 피로에 의한 중지)
;-------------------------------------------------

;────────────────────────────────────
;죠교첫회·10회째
;────────────────────────────────────
;·죠교첫회, 및 10회째에 표시되는구상
;·CFLAG:죠교횟수로횟수는 어떻게라도 참조할 수 있습니다만, 너무 하면 다른 조건을 탕진할 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
[SKIPSTART]
;하부에 통합
;IF 구상기입 체크(100) && CFLAG:죠교횟수 == 1
;	PRINTFORMW 「… 처음은 손대어 사랑옛 노래등, 길들여 갑시다」
;	PRINTFORMW 「멍청이? 시끄럽네요. 너에게 듣고 싶지 않아요!」
;	PRINTFORML
;	PRINTFORMW 「… 이런 일했다일 없기 때문에… 상냥하게 하세요?」
;	RETURN 1
;ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교 10회째
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && CFLAG:죠교횟수 == 10 && !ARG
	PRINTFORMW 
	RETURN 1
ENDIF

;────────────────────────────────────
;조교중지 ARG:1 ARG:2
;────────────────────────────────────
;CFLAG:소모 > 5혹은 CFLAG:MASTER:소모 > 5를 조건으로서 불려 가는 개소
;이 조건으로부터 빠졌을 경우는통상의죠교개시구상가 불린다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;피로로죠교가 중지가 되는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG > 0
	;=================================================
	;피곤한 것은구상주(TARGET)
	;=================================================
	IF 구상기입체크(100) && ARG == 1
		PRINTFORMW 「오늘은 휴일이야. 너도 쉬어 주세요」
		PRINTFORMDW %CALLNAME:TARGET%는 거실에서 데굴데굴 하고 있다
		RETURN 1
	ENDIF
	
	;=================================================
	;피곤한 것은죠교대상(MASTER)
	;=================================================
	IF 구상기입체크(100) && ARG == 2
		PRINTFORMW 「피로가 모여 그렇구나…오늘은 쉬어도 좋아요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;그 외·호감도등에 의한 분기
;────────────────────────────────────
;·현재의 ACTABLE나 COMABLE의 사양적으로, 진전도를 측정하려면 (사실상)호감도를 기준으로 할 수 밖에 없다고 생각된다
;·의존도는@COM로 대입되고 참조할 수 있을 뿐으로, 본체측의 거동에는 아무런영향를 주지 않았다
;·향후의손 넣고 여하에 따라서는 용도도 있을지도? 그렇다고 하는 곳
;-------------------------------------------------
;-------------------------------------------------
;·서로 서로 좋아하고 있다
;-------------------------------------------------
;원래는 ED가 실장되어 있지 않은 시대에, 가짜의 ED 범위로서 사용되는 일이 많았던 개소
;현상에서도 이 단계에 이르려면, 그만한일수와 진전이필요가 된다고 생각됩니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;상사상애（호의 3000 이상·호감도 3000 이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("상사상애")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(0)
		;과거 %CALLNAME:TARGET%묘사  월, 야, 설, 머플러, 요들꿈, 겨울안의 봄, 벚꽃의 꽃잎
		;PRINTFORMDW 

		PRINTFORMDW %CALLNAME:MASTER%(은)는 침상에 앉으면서 %CALLNAME:TARGET%에게 왜 이러한 일을 했는지 물어 보았다
		PRINTFORMW 「과정을 신경써 버리면, 너를 언제까지든 얻을 수 없을 것 같아서, 실력 행사야!」
		PRINTFORMDW %CALLNAME:TARGET%에게 소유되어 키워지고 감시받는다
		PRINTFORMDW 그럴지도 모른다…
		PRINTFORMW 「마지막이 좋다면 그걸로 된거야♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「자,%RAND_SPLIT("즐깁시다♪/기분 좋게 해 주어요!/서로 사랑합시다")%」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·죠교대상가구상 주로 이제(벌써) 헤롱헤롱
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교완료（호의 2000 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("죠교완료")
	;=================================================
	;처음
	;=================================================
	;소녀 퇴고중
	IF 구상기입체크(100) && FIRSTEVENT(10)
		;미래에 대해 생각해 버리는 %CALLNAME:MASTER%. 좋아하는 사람의 성씨와 자신의 이름을 합체 시켜 바타발 하는 저것에 가깝다
		;주인님가 있는 미래 밖에 상상 할 수 없는 정도로죠교가 끝난 상태 
		PRINTFORMDW 이 생활이 시작되고 나서 상당히 긴 시간이 지난 것처럼 생각된다…
		PRINTFORMDW …실제로 지난 시간은 반년에도 못 미친 짧은 것이지만 매일매일이 특별해 날마다 길게 느껴진다
		PRINTFORML 
		PRINTFORMDW 요즈음은 이 생활에 익숙하고 여유로워진 탓인가, 쉬는 시간에는 앞의 일을 생각하게 되었다
		PRINTFORMDW 오늘같은 날처럼, 내일도 미래도 이렇게 살게 되는 것인가
		PRINTFORMDW %CALLNAME:MASTER%(은)는 눈을 닫아 조금 미래를 상상해 보았다
		PRINTFORML 
		;레이무의 거동  구상색  이 타이밍에는 근처에 있지만 방해는 하지 않는레이무씨
		PRINTFORMW …
		PRINTFORML
		PRINTFORMDW 그리운데
		PRINTFORMW 「그립네요」
		PRINTFORML
		PRINTFORMDW 손자의 얼굴을 보고
		PRINTFORMDW 할아버지가 된 자신과 할머니가 된 %CALLNAME:TARGET%
		PRINTFORML
		PRINTFORMDW 날마다 자신의 손을 꼭 쥐고있으면서도
		PRINTFORMDW 애정이 깃들인 손바닥으로 딸을 어루만지는 %CALLNAME:TARGET%
		PRINTFORML
		PRINTFORMDW 조금 성장해 어른스러워지고
		PRINTFORMDW 요염한 %CALLNAME:TARGET%
		PRINTFORML
		PRINTFORMDW 자식이 태어나고
		PRINTFORMDW 우리 아이를 임신한 %CALLNAME:TARGET%
		PRINTFORML
		;%CALLNAME:TARGET%의 거동  구상색
		PRINTFORMW …
		PRINTFORML 
		;흰색자. 당신망상중. 레이무, 당신가 깨닫는 것을 기다린다
		PRINTFORMDW ………
		PRINTFORML 
		;처손, 레이무
		PRINTFORMW 즈즛…
		PRINTFORML
		PRINTFORMDW …언제부터인지 근처에는 따뜻하게 한 차와 조금 전까지 생각하고 있던 여성을 닮은 사랑스러운 소녀
		PRINTFORML 
		PRINTFORMDW …
		PRINTFORMW 즈즛…
		PRINTFORML 
		PRINTFORMDW %CALLNAME:TARGET%
		PRINTFORML 
		PRINTFORMW 「뭐야」
		PRINTFORML 
		PRINTFORMDW 어쩐지 조금 줄어든 것 같은, 사랑스러워졌다
		PRINTFORML 
		PRINTFORMW 「뭐야 그것」
		PRINTFORML 
		PRINTFORMDW %CALLNAME:TARGET%는 부드럽게 미소짓는다
		;여기서 첫손기분을 (듣)묻는 것에 연결되면 기려
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·죠교자가죠교대상에 빠져 버려, 깊기도 하고 도착했다 사랑을 따르고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;익애（호감도 2000 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && AFFECTION("익애")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTEVENT(20)
		RETURN 1
	ENDIF
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·죠교대상가구상 주로죠교 되는 것을 행복하게 느끼고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교가 끝난 상태（호의 1000 이상） 태연하게 한다적극적으로 하지만 다발하는 기회
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("죠교가 끝난 상태")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(30)
		;PRINTFORMDW 그녀의행동에 응하는 것도 나쁘지 않을지도 모른다
		;일행에서는 뭔가 외롭기 때문에 가필하지만, 이것은 이것대로 촌스럽다
		
		;PRINTFORMDW 너무나 불합리한 현상은 『%CALLNAME:TARGET%씨이니까 어쩔 수 없다』라고 납득이 갔다
		;PRINTFORMDW 그녀의행동에 응하는 것도 나쁘지 않을지도 모른다
		
		;PRINTFORML 
		;PRINTFORMDW 염주…
		;무언으로당신의 근처에 있는레이무
		;PRINTFORMW  …
		;PRINTFORML 
		
		;일본식인 풍미가 좀 더 갖고 싶다  정적이발리나 있고
		;PRINTFORMDW 너무나 불합리한 현상도,
		;PRINTFORMDL 가슴의 버팀목도 목의 버팀목도 『%CALLNAME:TARGET%씨이니까, 어쩔 수 없는가』라고
		
		;표현에 조정을 필요로 한다
		;PRINTFORMDW 차가 씻어 납득이 갔다
		;PRINTFORMDW 차가 내장에 스며들면 뱃속으로부터 따뜻해져 갔다…
		
		;PRINTFORMDW 너무나 불합리한 현상도, 목의 버팀목도
		;PRINTFORMDL 가슴의 버팀목도 목의 버팀목도
		;향미가발리나 있고 알맞은 떫은 맛도
		;PRINTFORMDL %CALLNAME:MASTER%의 오장육부에 차의 미지근함이 스며든다
		;PRINTFORMDL 『%CALLNAME:TARGET%씨이니까, 어쩔 수 없는가』
		;PRINTFORMDL 와 %CALLNAME:MASTER%는 이상하게 납득했다
		
		;쓴 웃음인가 시원한 웃음인가  너는 싱글벙글 하고 있을까?
		;＞죠교대상가구상 주로죠교 되는 것을 행복하게 느끼고 있다
		;14. 5세의 아이를 웃어 지켜보는 28.9세의 어른 정도 그런 곳인가
		;
		
		;당신의 긴장이 풀릴 기색  레이무씨 기쁜 것 같다
		;당신의 긴장이 풀리고 조금 안심하는레이무씨
		
		;1일째보다 전의 이야기를 낸다  성적인 관계를 시작해 경화했다풍이 풀린다
		;아니, 레이무씨 쪽이 부담은 있을 것인 상황
		;레이무씨는 씩씩한가?
		
		;여기는당신의레이무씨에게로의호의의 개소이지만 굳이
		;레이무의당신에의 호감도가 올랐는지와 같이 그려
		;당신의 심경의변화가레이무의 웃는 얼굴을 눈치채게 한다.
		;레이무씨는 최초부터 웃는 얼굴이었다. 그렇다고 하는 것은 어때
		;레이무씨는 최초부터 담담하게 무자비하게 즈조조야
		
		RETURN 1
	ENDIF
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·죠교자에 대해 혐오감을 안고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;혐오（호의-100 미만）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("혐오")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(40)
		PRINTFORMDW %CALLNAME:TARGET%는 곤란한 것처럼 머리를 긁고 있다
		RETURN 1
	ENDIF
		
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·마음에 상처를 만들어져주인님의 모습을 보는 것만으로 무서워해 버린다
;·트라우마의 종은 「%ACTSTR(CFLAG:MASTER:8)%」로 표시할 수 있다
;  예）PRINTFORMW 「이제(벌써)%ACTSTR(CFLAG:MASTER:트라우마)%는 싫습니다 라고?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;공포（호의 200 미만·트라우마Lv3 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && AFFECTION("공포")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTEVENT(50)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·마음을 열지 않는노예에 초조해지는주인님
;━━━━━━━━━━━━━━━━━━━━━━━━
;반항（호의 0 이하·반항각인Lv3 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && AFFECTION("반항")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTEVENT(60)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·죠교 되는 것도 오히려 좋아하게 되었는지도 모른다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;눈을 뜨고（호의 500 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
;간신히 접하는데 익숙해지기 시작했을 무렵에 엉덩이 후비어진다
IF 구상기입체크(0) && AFFECTION("눈을 뜸")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTEVENT(70)
		;현재 종료 측에 이전
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·정중하게죠교를 진행되어, 생각했던 것보다 싫지 않을지도…
;━━━━━━━━━━━━━━━━━━━━━━━━
;순응（호의 100 이상·호감도 100 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
;일주일눈으로 간신히 %CALLNAME:TARGET%호감도 100 와 같은 정도
IF 구상기입체크(100) && AFFECTION("순응")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(80)
		;아직 초반. 당분간은 별실. 당신의 아래를레이무가 다닌다
		PRINTFORMW 「… 내가 너의 곳에 다니다니 어쩐지 이상한 느낌이군요?」
;OriginalString : 아웃12, "くすっ")
		CALL FADEOUT(12, "키득")
		PRINTFORMDW %CALLNAME:TARGET%는 입가를 누르고 낡게 미소지었다
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교횟수
;━━━━━━━━━━━━━━━━━━━━━━━━
SELECTCASE CFLAG:죠교횟수
	CASE 1
		PRINTFORMW 「…처음은 서로 스킨쉽부터, 익숙해지도록 합시다」
		PRINTFORMW 「헤타레? 시끄러워요!. 너에게 듣고 싶지 않아요!」
		PRINTFORML
		PRINTFORMW 「… 이런 일 한 일 없기 때문에… 상냥하게 해 주세요?」
	;CASE 3
	;	PRINTFORMW 「응… 석연치 않다는 얼굴이군요? 좋으니까 나에게 맡겨 두세요!」
	;	PRINTFORMW 「과정은 보증하지 않지만, 나쁘게는 안 돼요」
	;대체로 3일째까지는조수소환 코스트는 우선 지불할 수 없다
	CASE 2
		PRINTFORMW 「응… 석연치 않다는 얼굴이군요? 좋으니까 나에게 맡겨 두세요!」
		PRINTFORMW 「과정은 보증하지 않지만, 나쁘게는 안 돼요」
	;처음의 아침
	CASE 3
		PRINTFORMDL %CALLNAME:MASTER%(은)는 맛조 국물의 냄새로 눈을 떴다
		PRINTFORMDW 부엌에 %CALLNAME:TARGET%는 서 있다
		
		PRINTFORML 
		;된장국의 맛보기인 것으로 즈조조는 아니다
		PRINTFORMW 즈즛
		PRINTFORML 
		PRINTFORMW 「어머나, 겨우 일어났군요」
		PRINTFORMW 「안녕」
		;소녀답고 갈팡질팡 한다
		PRINTFORMW 「자주(잘) 잘 수 있었는지 해들? 나는,」
		PRINTFORMW 「그다지 잘 수 없었어요」
		;장면 전환의 개행. 아침의 식탁
		PRINTFORML 
;OriginalString : 아웃12, "「そうそう」")
		CALL FADEOUT(12, "「그래그래」")
		PRINTFORMW 
		PRINTFORM 「밥을 먹고 나면,
		PRINTFORMW
		PRINTFORMW
;OriginalString : 인12, " 어제의 계속이야」")
		CALL FADEIN(12, " 어제의 계속이야」")
		;맛있다（중요）
		PRINTFORMDL 밥을 먹고 있는 %CALLNAME:MASTER%의 숟가락을 든 손은 굳어졌다…
	;CASE 5
	;	PRINTFORMW 
	;4일째아침 7～14에 질리게 하지 않기 위한 궁리가필요
	;CASE 7
	;대체로 2주간째 선택지가 충실해 오는 정도의 때
	;CASE 15
	;슬슬 호의호감도 2000 정도의 때
	;CASE 30
	;	PRINTFORMW 
	;기일 이구상에는 특별한 기일 설정은 없다
	;CASE 60
	;	PRINTFORMW 
	CASEELSE
		PRINTFORMW 「자, 시작해요!」
ENDSELECT
RETURN 1

;-------------------------------------------------
;·요컨데, 다른 조건에 히트 하지 않았던 경우에 떨어지는 개소
;·만일 다른 분기를 모두방치했다경우, 이 개소만 써 두면 뭔가는 표시된다든가, 그런 장소입니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;초반（조건 없음）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTEVENT(90)
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 「」
		RETURN 1
	ENDIF
ENDIF
