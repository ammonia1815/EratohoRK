;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 휴식계 개별 리액션구상파일
;
;		휴식계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;휴식계
;========================================================================
;· 각 REACT내의 나누는 방법으로서 TFLAG:REACT분류(IS_COMGRONAME)나 SELECTCOM(IS_NOWCOMNAME)가 생각됩니다
;·범위의 넓은 조건으로 주워 버리는 것은 기본적으로 추천합니다만, 경우에 따라서는 그것도 개미라고 생각합니다
;·ACT분류:휴식계의 경우, 회화계 COM(COM0~COM8) 중심의선택지가 됩니다만
;·상황에 따라서는 그 외의 것이선택지에 포함될 가능성도 있습니다
;·리액션(REACT)으로 처리되지 않았던 것은 범용 리액션(COM)으로 보완되므로
;·무리하게 모두 묻는필요는 없습니다
;·필요라고 생각하는 것을 좁혀, COM와의 제휴를 생각해 효율 좋게 구축해야 합니다
;-------------------------------------------------
;※지속효과가 있는 TEQUIP로애무계 반응이선택지가 되는 경우가 있습니다
;-------------------------------------------------
;· 각 REACT내는,&& IS_NOWCOMNAME든지,&& IS_COMGRONAME등의 조건으로 결정하고 쳐 합시다
;·여기에 조건 없음 분기가 있으면, 모두 주워 버려 범용 리액션에 흐르지 않습니다
;·죠교대상의 반응에 관련되지 않고, 같은 리액션 밖에 하지 않게 되어 버립니다
;·요컨데 이것은, 상정당했다시스템의 파괴에 연결됩니다
;-------------------------------------------------
;  REACT의 지문은 ACT_MESSAGE.ERB의 TRAIN_MESSAGE2_{TFLAG:ACT}로 처리됩니다
;  기본적으로 지문은 TFLAG:ACT파생에 의해 분기 합니다.
;  휴식계의 경우, ACT파생는 명령에 따른지 아닌지에 할당되어지고 있습니다
;  ACT_MESSAGE.ERB REACTION_MESSAGE.ERB를 참조하면서의 작업을 추천합니다
;-------------------------------------------------
;각 REACT에서는 상정되는 반응을 망라되어 있습니다. 필요에 응해 분기를 증감해 사용해 주세요
;COM에 따라서는 REACT파생로 REACT분류가변화 하는 일에 주의
;각 COM의 파생은 기본적으로 REACT파생 1를 조건 결정 우리 소분기로 해, REACT파생 0는 그 외 취급으로 조건 없다고 보고 있습니다
;손간을 걸고 싶지 않은 경우는 그 외만을 써, 파생에 관련되지 않고 해당 개소를 보완할 수가 있습니다
;또 IF 구상기입 체크(0) && TFLAG:REACT파생 < 1으로 하는 일로 파생했다경우는 범용 리액션에 흘린다 따위라고 하는 일도 가능합니다
;파생은 장래 증가할 가능성도 있으므로 != 1등으로 하는 것보다는< 1으로했다 (분)편이 무난하다라고 생각합니다
;-------------------------------------------------

;────────────────────────────────────
;REACT90, 휴식시킨다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=보통으로 쉰/1=도구의 탓으로 너무 쉬게 하지 않았다/2=승손에자위를하기 시작했다)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:침묵한다/마음 약하게 응답한다/무뚝뚝하게 응답한다/상냥하게 응답한다/강하게 응답한다/빈정거린다/더 하드하게 해/용서를 빌다/기분좋게 해
;애무계 반응
;	COM:쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통계 반응
;	COM:고통을 참는다/비명지른다
;-------------------------------------------------
@KOJO_REACT_K1_90
;-------------------------------------------------
;·지문은 ACT파생로 분기
;·REACT분류가 받아들이는 것 경우만 처리가 다르다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 보통으로 쉬었다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;REACT분류가 받아들이는 것 경우는 지문이변화
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("받아들인다") && IS_NOWCOMNAME("더 하드하게 해/기분좋게 해")
		;%CALLNAME:MASTER%가 쉬는 것보다 기분 좋게 했으면 좋겠다고 말하면,
		;%CALLNAME%는 쓴웃음 지어, 다음에 해 준다고 했다…
;OriginalString : 「本当は共同作業なんだから、あんたも協力しなさいよ？」
		PRINTFORMW 「사실은 공동 작업이니까, 너도 협력해 주세요?」
		RETURN 1
	ENDIF
	
	;REACT분류:받아들이는 이외의 경우는↓
	;%CALLNAME:MASTER%과 %CALLNAME:TARGET%는 조금 쉬었다…
	
	;=================================================
	;COM0, 침묵한다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
;OriginalString : %CALLNAME:MASTER%が無言で横になっていると、%CALLNAME:TARGET%に頬をつつかれた
		PRINTFORMDW %CALLNAME:MASTER%(은)는 무언으로 눕고 있으면, %CALLNAME:TARGET%에 뺨을 쿡쿡 찔러졌다
		RETURN 1
	ENDIF
	
	;=================================================
	;COM1, 마음 약하게 응답한다(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
;OriginalString : 「疲れてる？もうちょっとだけ休みましょうか」
		PRINTFORMW 「지쳐 있다? 좀 더만 쉴까요」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM2, 무뚝뚝하게 응답한다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
;OriginalString : 휴식中、손持無沙汰だったので%CALLNAME:MASTER%は%CALLNAME:TARGET%의 頭を無造作に、ぽん。ぽん。と撫でた
		PRINTFORMDW 휴게중, 소유무소식이었으므로%CALLNAME:MASTER%(은)는 %CALLNAME:TARGET%의 머리를 간단하게, 개. 개. (와)과 어루만졌다
;OriginalString : 
		PRINTFORML
;OriginalString : 「…おしまい？」
		PRINTFORMW 「… 끝?」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM3, 붙임성 있게 응답한다(REACT분류:적극적으로 한다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("상냥하게 응답한다")
;OriginalString : %CALLNAME:MASTER%は、%CALLNAME:TARGET%의 頭を撫でながら一休み했다
		PRINTFORMDW %CALLNAME:MASTER%(은)는, %CALLNAME:TARGET%의 머리를 어루만지면서 좀 쉬었다
		RETURN 1
	ENDIF
	
	;=================================================
	;COM4, 강하게 응답한다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("강하게 응답한다")
;OriginalString : 「休みとなると元気が出るのね」
		PRINTFORMW 「휴일이 되면(자) 활기가 생기군요」
;OriginalString : %CALLNAME:TARGET%は苦笑いしている
		PRINTFORMDW %CALLNAME:TARGET%는 쓴 웃음 하고 있다
		RETURN 1
	ENDIF
	
	;=================================================
	;COM5, 빈정거린다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 「まだまだ元気そうね」
		PRINTFORMW 「아직도 건강하구나」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
		;파생 1의 경우는 REACT분류:없음
		;-------------------------------------------------
		;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(100)
;OriginalString : 「駄目。少し休んだら続きよ」
			PRINTFORMW 「자. 조금만 쉬고 계속하자」
			RETURN 1
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF

;TEQUIP:(바이브/항문바이브/항문비즈/관장그릇＋플러그/유두캡)장착중의 경우는 파생 1이 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT90_1, 도구의 탓으로 너무 쉬게 하지 않았다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
		;%CALLNAME:MASTER%는 몸에 익힌도구로 느껴 버려, 휴식할 경황은 아니었다…
		;%CALLNAME%는 쓴웃음하면서 그 모습을 바라보았다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		;%CALLNAME:TARGET%는 쉬어도 좋다고 말했지만,
		;몸에 익혀지고 있는도구가 계속 꾸짖은 탓으로 %CALLNAME:MASTER%는 너무 쉬게 하지 않았다…
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;리액션이 COM50, 자위를 시작한다의 경우는 파생 2가 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT90_2, 승손에자위를하기 시작했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;지문은 REACT파생에 관련되지 않고 이하와 같다가 된다↓
	;휴일이 주어진 %CALLNAME:MASTER%가 승손에자위를하기 시작해,
	;%CALLNAME:TARGET%는 조금 화내고 있는 것 같다…
	
	;일단 REACT파생소분기를 놓아둡니다만, 그 외만으로 좋은 생각도 듭니다
	;=================================================
	;승손에 자위 하지마(징계·포인트가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「승손에 자위 하고 있지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「何、自分だけ始めてるのよ」
		PRINTFORMW 「무엇, 자신만큼 시작해 의 것」
;OriginalString : 「自分でしてもいいけど、出しちゃ駄目よ？」
		PRINTFORMW 「스스로 해도 되지만, 내면 안되어요?」
		RETURN 1
	ENDIF
ENDIF
;ACT파생 1, 2는 REACT로 완결시킨다의 것이 바람직한 것 처럼 생각하므로 범용 리액션에 흐르지 않는 구조가 되어 있습니다
;범용 리액션으로 처리한다면[그 외]분기는 삭제해 조건 결정 우리 분기만을 남기도록(듯이)해 주세요

;REACT90, 휴식시킨다

;────────────────────────────────────
;REACT91, 영양제를 먹인다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=영양제를 마신/1=영양제를 버렸다)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:침묵한다/마음 약하게 응답한다/무뚝뚝하게 응답한다/상냥하게 응답한다/강하게 응답한다/빈정거린다/더 하드하게 해/용서를 빌다/기분좋게 해
;봉사계 반응
;	COM:거부한다
;-------------------------------------------------
@KOJO_REACT_K1_91
;-------------------------------------------------
;·지문은 TFLAG:ACT파생로 분기
;-------------------------------------------------

;리액션이 COMN40, 거부한다 이외의 경우는 따른 취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 영양제를 마셨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM마다 나누지 않고 REACT분류로 묶는다든가, 마셨을 경우는 1개로 정리해 버린다든가
;아무튼 방식은 여러가지 있습니다. 다만 지문은 최종적으로 REACTION_MESSAGE로 COM 마다의 처리까지 떨어져 내리므로
;세세하게 나누어 쓰는 것은 쓸데없지 않다고 생각합니다
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	
	;%CALLNAME:MASTER%가 %CALLNAME:TARGET%로부터 도당했다 이상한 듯한 약제를 마시면,
	;갑자기 몸중에서 건강이 넘쳐 왔다…
	
	;=================================================
	;COM0, 침묵한다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「입に合わなかったかしら？」
		PRINTFORMW 「입맛에 맞지 않았는지 해들?」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM1, 마음 약하게 응답한다(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
;OriginalString : 「少しきつかったかしら？」
		PRINTFORMW 「조금 힘들었는지 해들?」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM2, 무뚝뚝하게 응답한다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
;OriginalString : 「입に合わなかったかしら？」
		PRINTFORMW 「입맛에 맞지 않았는지 해들?」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM3, 붙임성 있게 응답한다(REACT분류:적극적으로 한다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("상냥하게 응답한다")
		[SKIPSTART]
		;촉촉히 와했다감촉을 갖고 싶기 때문에 comment out
		;공포경험가 들어가는 일에 주의가필요. 개선의 여지 있어.
		;PRINTFORMW 「한 그릇 더? 이것이 끝나면요」
;OriginalString : 「%RAND_SPLIT("한잔 더/한 그릇 더")%? 이것이 끝나면요」
		PRINTFORMW 「%RAND_SPLIT("이제(벌써) 가득/한 그릇 더")%? 이것이 끝나면」
;OriginalString : 「잔 해 주기 때문에, 함께… 말야」
		PRINTFORMW 「따라 주기 때문에, 함께… 그런데」
		[SKIPEND]
		SELECTCASE TIME
			;낮
			CASE 0
;OriginalString : 「%RAND_SPLIT("もう一杯/おかわり")%？まだ%RAND_SPLIT("早いわよ/明るいわよ/昼間よ/お昼よ")%」
				PRINTFORMW 「%RAND_SPLIT("벌써 다 마신거야/한 그릇 더")%? 아직 %RAND_SPLIT("빠른거야 / 밝은거야 / 낮이잖아 / 점심이야")%」
;OriginalString : 「お晩酌の時にね」
				PRINTFORMW 「저녁 반주 때 줄게」
;OriginalString : %CALLNAME:TARGET%は%CALLNAME:MASTER%의 おでこをつん！とつついた
				PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 이마를 콕콕 찔렀다
			;밤
			CASEELSE
;OriginalString : 「%RAND_SPLIT("もう一杯/おかわり")%？」
				PRINTFORMW 「%RAND_SPLIT("벌써 다 마신거야/한 그릇 더")%?」
;OriginalString : 「これが終わったら、お酌してあげるから一緒にね」
				PRINTFORMW 「이것이 끝나고, 마저 줄게」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;COM4, 강하게 응답한다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("강하게 응답한다")
;OriginalString : 「いい飲みっぷりね」
		PRINTFORMW 「좋은 먹는 모습이군요」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM5, 빈정거린다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 「もう少し、솔직な言葉が欲しいわ」
		PRINTFORMW 「좀 더, 솔직한 말을 갖고 싶어요」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
		;파생 1의 경우는 REACT분류:없음
		;-------------------------------------------------
		;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(100)
;OriginalString : 「だーめ。ほら一気にぐいーっと行きなさい」
			PRINTFORMW 「안-돼. 이제 계속하는거야」
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM8, 기분좋게 해(REACT분류:없음)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("기분좋게 해")
		;-------------------------------------------------
		;「이 철면피의 돼지째!」(징계·포인트가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(100)
;OriginalString : 「そうね。続きをしましょ」
			PRINTFORMW 「그렇구나. 계속을 합시다」
			RETURN 1
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF

;리액션이 COM40, 거부한다였을 경우는 파생 1
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 영양제를 버렸다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;%CALLNAME:MASTER%는 %CALLNAME:TARGET%로부터 도당했다 이상한 듯한 약제를 버렸다…
;OriginalString : %CALLNAME:TARGET%의 様子がおかしい
	PRINTFORMDW %CALLNAME:TARGET%의 모습이 이상하다
;OriginalString : %CALLNAME:MASTER%は%CALLNAME:TARGET%의 逆鱗に触れてしまったようだ
	PRINTFORMDW %CALLNAME:MASTER%(은)는 %CALLNAME:TARGET%의 노여움에 접해 버린 것 같다
;OriginalString : 
	PRINTFORML
;OriginalString : 幻想郷においてお酒を粗末にすることは許されない
	PRINTFORMDW 환상시골에 있어 술을 허술하게 하는 것은 용서되지 않는다
	
	TFLAG:징계·플래그 = 1
	RETURN 1
ENDIF
;REACT91, 영양제를 먹인다

;────────────────────────────────────
;REACT92, 치료한다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=몸의 구석구석까지 더듬어치료했다/1=날뛰었기 때문에치료 할 수 없었다)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:침묵한다/마음 약하게 응답한다/무뚝뚝하게 응답한다/상냥하게 응답한다/강하게 응답한다/빈정거린다/더 하드하게 해/용서를 빌다/기분좋게 해
;봉사계 반응
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_K1_92
;-------------------------------------------------
;·지문은 TFLAG:ACT파생로 분기
;-------------------------------------------------

;REACT분류가날뛴다 이외의 경우는 파생 0
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 몸의 구석구석까지 더듬어치료했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM마다 나누지 않고 REACT분류로 묶는다든가, 치료했다경우는 1개로 정리해 버린다든가
;아무튼 방식은 여러가지 있습니다. 다만 지문은 최종적으로 REACTION_MESSAGE로 COM 마다의 처리까지 떨어져 내리므로
;세세하게 나누어 쓰는 것은 쓸데없지 않다고 생각합니다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 체의 구석구석까지 더듬어치료했다…
	
	;=================================================
	;COM0, 침묵한다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM1, 마음 약하게 응답한다(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("마음 약하게 응답한다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM2, 무뚝뚝하게 응답한다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM3, 붙임성 있게 응답한다(REACT분류:적극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("상냥하게 응답한다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM4, 강하게 응답한다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("강하게 응답한다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM5, 빈정거린다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("빈정거린다")
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM7, 용서를 빌다(REACT분류:용서를 빌다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("용서를 빌다")
		;파생 1의 경우는 REACT분류:없음
		;-------------------------------------------------
		;의견을 내다니 좋은 번가슴구나! (징계·포인트가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(죠교대상의 의견을 묵살) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM8, 기분좋게 해(REACT분류:없음)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋게 해")
		;-------------------------------------------------
		;「이 철면피의 돼지째!」(징계·포인트가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「-응과 들은체 만체 한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
;OriginalString : 
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0
ENDIF

;REACT분류가날뛴다였을 경우는 파생 1
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT92_1, 날뛰었기 때문에치료 할 수 없었다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;%CALLNAME:MASTER%가 날뛰었기 때문에치료 할 수 없었다…
;OriginalString : 
	PRINTFORMW
	RETURN 1
ENDIF
;REACT92, 치료한다
