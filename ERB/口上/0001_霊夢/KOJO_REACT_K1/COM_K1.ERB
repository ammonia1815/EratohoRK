;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 범용 리액션구상파일
;
;	개별 리액션(REACT)으로부터 빠져 온 것을 해당 COM로 처리하는 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;%CALLNAME:MASTER%의 행동로부터의 역당겨

;=====================================================================
;범용 리액션구상
;=====================================================================
;·개별 리액션구상를 표시하지 않았던 경우에 표시
;·KOJO_REACT_KX1_Y1의 RESULT가 0이었을 경우, 범용 리액션이 불려 간다
;·REACT로 표시했다인가 아닌가는 RESULT로 판정되므로, REACT로 표시해 RETURN 0으로 한층 더 범용도 표시시킨다일도 가능한 것이긴 함
;·KOJO_COM_KX1_Y1로부터 RESULT 0이 돌아가면 통괄 범용 리액션(KOJO_REACT_KX)이 불려 간다
;---------------------------------------------------------------------
;간이 플로우
;    표시 타이밍·죠교대상의 리액션（커맨드·반응）
;        액션번호별의, 개별 리액션구상
;            표시하지 않았던 경우, 커맨드번호별의 범용 리액션구상←(지금 여기)
;                표시하지 않았던 경우, 통괄 범용 리액션구상
;---------------------------------------------------------------------
;관련 변수/함수류…
;
;ACT분류계
;TFLAG:ACT분류 이 턴의죠교메뉴(0=회화/1=애무/2=도구/3=성교/4=수치/5=봉사/6=가학/7=이상/8=사역/9=휴식/10=성교봉사)
;GET_ACTM ACT번호로부터 ACT분류번호를취득 하는 함수
;MENUMATCH 지정했다ACT분류에 포함되어 있으면 진을반환(회화/애무/도구/성교/수치/봉사/가학/이상/사역/휴식/성봉)
;
;COM계
;TFLAG:REACT파생 각 SELECTCOM의 파생. COM마다 파생 내용은 다르다
;TFLAG:REACT부하 선택했다COM의기력소비를 판별한다(0=자연스러운행동/2=무리가 없는행동/무리를했다행동)
;COMCOR_POSI 주로소극적으로 한다계 COM의기력소비가 큰 때(TFLAG:REACT부하 == 2인 경우)에, COM보정치가호의적인가 어떤가를 판단
;
;그 외
;TFLAG:REACT인상 각 SELECTCOM의 상손에의 심상(0=영향없음/1=좋은인상/2=약간좋은인상/3=나쁜인상/4=악인상대/5=0에서멀어짐/6=0에가까워짐)
;TFLAG:징계·포인트( 100모이면징계·개시)
;FIRSTCOM  첫회 범용 리액션 판정 함수  SELECTCOM를 자동으로 참조하므로, 현재선택했다COM 마다번호가 독립해 보존된다
;ONCECOM  조교중첫회 판정용 함수. FIRSTTIME계와 달라죠교전에 초기화된다. 기본적인 사양은 FIRSTCOM와 같이
;RANDCOM(AUTORAND계)  RAND 처리로이벤트종류마다의번호을반환가, 전종 볼 때까지 같은 숫자는 돌려주지 않는다. 모두 표시했다때에 초기화.
;
;---------------------------------------------------------------------

;=====================================================================
;※ACT분류와 그 파생에 대해
;
;COM에 따라서는 발생하는 ACT분류가 복수에 걸칩니다
;각 COM내를 TFLAG:ACT분류로 나누어 주는 일에 의해, 보다 정확하게 상황을 참조하는 것이 가능합니다
;다만, ACT분류자체도 파생하는 것이 있어, 이 경우, TFLAG:ACT분류에서는 파생 상황을 참조 할 수 있지 못하고, 약간 이상한 일이 됩니다
;
;예를 들면…, ACT55, 발핥기강제는 본래 ACT분류:봉사계입니다만, 이상계로부터 발생하는 경우가 있어,
;TFLAG:ACT분류의 단락에서는, 상정 그대로의구상가 표시되지 않는다고 말하는 것이 생각됩니다
;
;거기서 TFLAG:ACT분류는 아니고, @MENUMATCH든지@GET_ACTM로 ACT분류 마다의 분기를 마련해 주면, 파생처를 참조하는 것이 가능하게 되어
;실제로 행해지고 있는 ACT분류로 처리되게 됩니다
;IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "봉사")와 같은 테두리를 마련해 주면, 위에 올린 것 같은 상황에도 대응이 가능합니다
;MENUMATCH의 분류 마다의 정의에 대해서는 이 파일의 28행째를 참조해 주세요
;
;ACT분류파생 일람…
;（각 수치는 ACT번호）
;회화 (기분 0/호의 1/지식 2/농락해라 3/위협하는 4/의상 5/합의 6/처분 7/고함치다 8/비웃는 9/매도 66)
;애무 (손로 10/가슴를 11/입로 12/Ａ손 13/Ａ입 14/키스 15/Ｃ찰 16/Ｂ찰 17/발로 18/Ｖ손 19/로터 20/압도 27)
;도구 (로터 20/바이 21/A바이 22/A비 23/Ｃ사랑 24/니풀 25/Ｐ대 26/관장 68)
;성교 (정상 30/후배 31/승마 32/대면 33/배면 34/ＡＳ35)
;수치 (자위 40/개장 41/보이고 42/거울 43/야외 44/방뇨 45/코스(미구현)46/방치 73)
;봉사 (손로 50/입로 51/가슴로 52/스마타 53/발지 55/이라 56/승마 32/가슴를 57)
;가학 (평손 60/회초리 61/바늘 62/줄 63/눈가림 64/0입 형벌 도구 65/매도 66/목마 67/관장 68/이라 56)
;이상 (Fist70/ＡFi71/양Fi72/방치 73/공중 74/Ｐ장 75/비웃는 9/발로 18/발지 55/목마 67)
;사역 (미구현)
;휴식 (휴일 90/복약 91/치료 92)
;성교(봉사) (정상 95/후배 96/승마 103/대면 97/배면 98/ＡＳ99)
;
;=====================================================================
[SKIPSTART]
MENUMATCH소분기 견본…

다른 분기와의우선도에 주의해, 필요에 응해 각 COM에 추가해 사용해 주세요
	;=================================================
	;ACT분류, 회화
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "회화")
	ENDIF
	;=================================================
	;ACT분류, 애무
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "애무")
	ENDIF
	;=================================================
	;ACT분류, 도구
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "도구")
	ENDIF
	;=================================================
	;ACT분류, 성교
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "성교")
	ENDIF
	;=================================================
	;ACT분류, 수치
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "수치")
	ENDIF
	;=================================================
	;ACT분류, 봉사
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "봉사")
	ENDIF
	;=================================================
	;ACT분류, 가학
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "가학")
	ENDIF
	;=================================================
	;ACT분류, 이상
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "이상")
	ENDIF
	;=================================================
	;ACT분류, 휴식
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "휴식")
	ENDIF
	;=================================================
	;ACT분류, 성봉
	;=================================================
	IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "성봉")
	ENDIF
[SKIPEND]
;---------------------------------------------------------------------

;※각 분기에는 대응하는 지문을 제시되어 있습니다만, 최종적으로 어떠한 형태로 할까 아직 뭐라고도 말할 수 없는 곳

;=====================================================================
;회화계 반응 COM0 접수대
;=====================================================================
;·주로 ACT분류:회화계/휴식계 시에 발생한다
;·발생은 트리거이벤트제. 특정의 ACT가 트리거가 된다
;·ACT분류:회화에 대해서는 REACT로 완결시킨다의 것이 바람직함
;-------------------------------------------------

;────────────────────────────────────
;COM0, 침묵한다
;────────────────────────────────────
;·따랐던 것에는 안 된다
;·ACT1, 지금 기분을 묻다/자신에게의 마음을 묻다의 경우는 REACT파생 1, "질문에 답하지 않는다"되어징계·포인트가산
;·REACT인상:나쁜인상
;·의존도변화(-1, -1)
;-------------------------------------------------
;·ACT가지금 기분을 묻다/자신에게의 마음을 묻다의 경우는 파생 1, 질문에 답하지 않는 취급
;·그 외의 경우는 파생 0, 단순한 침묵
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류 0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다
;ACT분류 2:도구
;	ACT:
;ACT분류 4:수치
;	ACT:수치플레이/야외플레이
;ACT분류 6:가학
;	ACT:밧줄/눈가리개/볼개그/매도/삼각목마
;ACT분류 7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류 9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;--------------------------------------------------
@KOJO_COM_K1_0
;-------------------------------------------------
;·TFLAG:REACT파생(0=단순한 침묵/1=질문에 답하지 않는다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;ACT, 지금 기분을 묻다/자신에게의 마음을 묻다의 경우에 파생한다
	;=================================================
	;REACT파생 1, 질문에 답하지 않는다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「어쩔 수 없네요. 너의 몸에 들어요」
		PRINTFORMW 「솔직하게 가르침%RAND_SPLIT("라고 갖고 싶어요/라고 줘/없음 있어라")%」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;말을 삼켜, 입다물고 있었다…
			PRINTFORMW 「인 일 생각하고 있다?」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분이 안좋은 것 같게 밀기묵하며 였다…
			PRINTFORMW 「입다물고 있으면, 괴로워져요?」
;OriginalString : 아웃8, "「…ま、いいわ」")
			CALL FADEOUT(8, "「…뭐, 좋아요」")
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 아무것도 말하지 않았다…
		PRINTFORML 「요컨데 곤란하고 있는 겠지」
		PRINTFORMW 「 나에게 맡기세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는%CALLNAME%의 모습을 엿보는 것 같은 기색을 보였지만,
		;아무것도 말하지 못하고 입다물고 있었다…
		PRINTFORMW 「나에게는 솔직해질 수 없다?」
		RETURN 1
	ENDIF
	
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM1, 마음 약하게 응답한다
;────────────────────────────────────
;·REACT분류는 파생에 의해변화
;·ACT("엄하게 위협한다/징계한다고 위협한다/고함치다/밀어 넘어트린다")로 일정 조건을 채우면 REACT파생 1, 애원에 파생
;·REACT인상:약간좋은인상
;·의존도변화(0, +1)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;-------------------------------------------------
@KOJO_COM_K1_1
;-------------------------------------------------
;·TFLAG:REACT파생(0=마음 약하게 응답한다/1=애원 한다)
;-------------------------------------------------

;REACT파생로 내용이 다르므로 파생마다 나누어 버립니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 애원 한다(REACT분류:용서를 빌다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신이 응석부린 기분을 뿌리쳐,
			;허약하면서, 벌써 그만두고라고 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분을 해치지 않게 가능한 한 허약하고,
			;이제(벌써) 이런 일 그만두고라고 말했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 심약구, 이제(벌써) 이런 일 그만두고라고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 심약구 작은 소리로, 기분을 엿보도록(듯이)하면서
		;이제(벌써) 이런 일 그만두고라고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「안되어요, 그만두어 주지 않는다♪」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 마음 약하게 응답한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신의 기분에 희미하게 깨달으면서도,
			;마음이 약하게 거리를 취하는 것 같은 대답을했다…
			PRINTFORMW 「나에게는 솔직해질 수 없다?」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분을 해치지 않게 가능한 한 허약하게 대답했다…
		PRINTFORMDL %CALLNAME:TARGET%는 곤란한 것처럼 머리를 긁고 있다
		PRINTFORMW 「응… 좀 더 솔직해져도 좋아요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 심약구대답했다…
		PRINTFORML 「응…그 말씨의 나쁨…」
		PRINTFORMW 「기분이 결정되지 않은 것 같구나?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 심약구 작은 소리로, 기분을 엿보도록(듯이)하면서대답했다…
		PRINTFORMDL %CALLNAME:TARGET%는 곤란한 것처럼 머리를 긁고 있다
		PRINTFORMW 「응… 곤란하네…」
		RETURN 1
	ENDIF
	
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM2, 무뚝뚝하게 응답한다
;────────────────────────────────────
;·REACT분류:없음
;·따랐던 것에는 안 된다
;·징계·포인트가 가산된다
;·REACT인상:나쁜인상
;·의존도변화(-2, -2)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_2
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;아무것도 아니면 무뚝뚝하게 손질했다…
;OriginalString : 아웃2, "つん")
			CALL FADEOUT(2, "츤")
			PRINTFORMW 개응개응
			PRINTFORMW 에
			PRINTFORMDW %CALLNAME:MASTER%의 고집은 당장 무너뜨려질 것 같다
			PRINTFORMDW 필사적으로 입가를 긴축시키고 견디는 %CALLNAME:MASTER%에 %CALLNAME:TARGET%는 우쭐거렸는지와 같이 입가를 매달아 올렸다
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의 저항을 담아 무뚝뚝하게 응했다…
			PRINTFORMW 「아무것도 아닌 것 않네」
			PRINTFORMW 「응, 나에게는 솔직해질 수 없다?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 무뚝뚝하게\@ IS_NOWACTNAME("밀어 넘어트린다") ? 대답 # 반응 \@했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 심지의 강한 눈동자로%CALLNAME%를 응시해 무뚝뚝하게 응했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;CASE 0의 확률은 짠다　
		SELECTCASE RAND:5
		;SELECTCASE RAND:2
			CASE 0
			;너는 아니고 %CALLNAME:MASTER%호칭으로 기분을 끄는 시도;빈발하는 것도 무엇인 것으로 저확률
				PRINTFORMW 「그런데, %CALLNAME:MASTER%」
				PRINTFORMW 「여길 봐줘?」
			CASEELSE
				PRINTFORMW 「무정하네」
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM3, 상냥하게 응답한다
;────────────────────────────────────
;·REACT분류:적극적으로 한다
;·REACT인상:좋은인상
;·의존도변화(+3, +1)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_3
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 상냥하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;%CALLNAME:MASTER%는 잘 비위를 맞추려고 만들기 웃는 얼굴로 응했다…
		PRINTFORMW 「표정이 딱딱해요?」
		PRINTFORMDW 에!
		PRINTFORMDW %CALLNAME:TARGET%의 손가락이 %CALLNAME:MASTER%의 뺨을 밀어넣는다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 붙임성 좋게\@ IS_NOWACTNAME("밀어 넘어트린다") ? 대답 # 반응 \@했다…
		PRINTFORMW 「너무 애교가 있는 것도, 너답지 않을지도…」
		PRINTFORMW 「조금 사치인가?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 기쁜듯이%CALLNAME%에 미소지어,
		;붙임성 좋게 대답했다…
		PRINTFORMW 「그 얼굴, 다른 사람에게 보이면 %RAND_SPLIT("허락하지 않아요?/안 되는 것이니까/용납하지 않아")%」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM4, 강하게 응답한다
;────────────────────────────────────
;·REACT분류:없음
;·따랐던 것에는 안 된다
;·ACT, 밀어 넘어트린다의 경우는 REACT파생 1, "밀어넘어짐에 굴하지 않는다"에 파생
;·REACT인상:나쁜인상
;·의존도변화(-1, -3)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_4
;-------------------------------------------------
;·TFLAG:REACT파생(0=강하게 응답/1=밀어넘어짐에 굴하지 않는다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;ACT, 밀어 넘어트린다의 경우에게만 파생
	;=================================================
	;REACT파생 1, 밀어넘어짐에 굴하지 않는다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;%CALLNAME:MASTER%는 이제 그만두라고 강하게 말했다…
		;1회째는 (들)물어 놓칠 것 같은 소리로
;OriginalString : 아웃4, "「やめないわ」")
		CALL FADEOUT(4, "「그만두지 않아요」")
;OriginalString : 인20, "「やめないわよ」")
		CALL FADEIN(20, "「그만두지 않아요」")
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;%CALLNAME%에는 관계없으면 강하게 말했다…
			PRINTFORMDW %CALLNAME:TARGET%에게 조금 얻어맞았다
			PRINTFORMW 「%RAND_SPLIT("화낸다?/관계없지 않아/바보같은 소리 하지마.")% 너의 일이고, 나의 일이야」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 강한척 해 응했지만, 소리는 조금 떨고 있다…
			PRINTFORMW 「강한척 하는 너도, 사랑스러워요」
			PRINTFORMDW %CALLNAME:TARGET%는, %CALLNAME:MASTER%의 목덜미에 얼굴을 묻어 목덜미를 빨아 왔다
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 강해 대답했다…
		PRINTFORMW 「어머나, 위세가 좋네요?」
		PRINTFORMDW %CALLNAME:TARGET%는 미소짓는다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 노려봐, 강해 응했다…
		PRINTFORMW %CALLNAME:TARGET%는 곤란한 것처럼 머리를 긁고 있다
		RETURN 1
	ENDIF
	
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM5, 빈정거린다
;────────────────────────────────────
;·ACT, 지금 기분을 묻다/자신에게의 마음을 묻다의 경우는 REACT분류:거부
;·그 이외의 경우는 REACT분류:태연하게 한다가 된다
;·징계·포인트가산
;·REACT인상:나쁜인상
;·의존도변화(-3, -1)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:밧줄/눈가리개/볼개그/매도/삼각목마
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_5
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는솔직인 기분을입로 하려고했다가,
			;왠지 부끄러워져, 야유 섞임에 응했다…
			PRINTFORMW 「 이제 한숨일까」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 작은 악의를 말에 혼합해 따끔 반했다…
			;말 있고는했다의 것의, 기분을 해치지 않은가 끊임없이 신경쓰고 있다…
			PRINTFORML 「너답지 않네요. 뭐 좋아요」
			PRINTFORMW 「그 정도라면 허락해 준다」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 빈정거리면서도, 말하는 일에는솔직에 따라 보였다…
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 여유를 보이는 것 같은 태도로 홀연히 따르면서,
		;야유 섞임에 응답했다…
		PRINTFORMW 「네네」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「아직도 여유가 있을 듯 하구나?」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM6, 더 하드하게 해
;────────────────────────────────────
;·REACT분류:받아들인다
;·죠교자의 소질, BASE:초조함등에 의해 REACT파생 1, 「이 철면피의 암퇘지놈!」(이)가 된다
;·REACT파생 1의 경우는징계·포인트가산
;·REACT인상:0에서멀어짐
;·의존도변화(0, +3)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/상냥하게 위로한다
;		징계한다고 위협한다
;ACT분류:1:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/키스/성기마찰한다/파이즈리한다
;ACT분류:2:도구
;	ACT:Ｃ애무도구/유두캡
;ACT분류:3:성교
;	ACT:정상위
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:스펭킹/밧줄/눈가리개/볼개그/삼각목마
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_6
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=이 철면피의 암퇘지놈!)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 「이 철면피의 암퇘지놈!」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌SOURCE가 일정 이상
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && SOURCE:MASTER:쾌Ｃ + SOURCE:MASTER:쾌Ｖ + SOURCE:MASTER:쾌Ａ + SOURCE:MASTER:쾌Ｂ > 10
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 좀 더 하고라고 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 반 자포자기가 되어, 좀 더하드로 하면 좋다고 말했다…
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 좀 더 굉장한 것당했다실 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 쾌감에 빠져, 좀 더 굉장한 일 하고라고 말했다…
			PRINTFORMW 「 나는, 아기만들기 밖에 흥미없어요」
			PRINTFORMDW 활짝 개이는 쾌청의 하늘과 같이 실로 시원한 웃는 얼굴로, 그렇게 돌려주어졌다
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「아프지 않은 것일까… 그렇지 않으면, 아픈 것이 좋은 것일까?」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;쾌SOURCE가 일정 이상의 네스트를 개방하지 않는 경우는, 상황에 관련되지 않고 이 개소가 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 여러가지 기분을 정리하면서말을 선택해,
			;%CALLNAME%로 된다면 좀 더하드라도 좋다고 말했다…
			PRINTFORMW 「이상한 신경을 쓰지 마. 나는 소프트인 것이 좋아해」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 자포자기가 되어, 좀 더하드로 하면 좋다고 말했다…
			;말 있고는했다의 것의, 입술을 딱 닫아 어깨를 진동시키고 있다…
			PRINTFORMW 「등지지 말아… 그런데, 기분이 나쁜 것을 바꾸는 것이라고?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는흥미반으로, 더 하드하게 해도 좋다고 말해 보았다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 좀 더 기분이 좋은 일을 해 주었으면 해져,
		;무심코더 하드하게 해라고 말해 버렸다…
		PRINTFORMW 「 나는, 아기만들기 밖에 흥미없어요」
		PRINTFORMDW 활짝 개이는 쾌청의 하늘과 같이 실로 시원한 웃는 얼굴로, 그렇게 돌려주어졌다
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「좀 더 격렬해서, 아프지 않은 것일까? 그렇지 않으면 아픈 것이 좋은 것일까?」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM7, 용서를 빌다
;────────────────────────────────────
;·REACT분류:용서를 빌다. 파생했다경우는 REACT분류:없음
;·죠교자의 소질, BASE:이성, 아라이멘도에 의해 REACT파생 1, 「의견을 내다니 좋은 번가슴군요!」(이)가 된다
;·턴방침가이상의 경우는 파생 1이 되기 쉽다
;·파생 1의 경우는징계·포인트가산
;·REACT인상:0에가까워짐
;·의존도변화(0, +2)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다
;ACT분류:1:애무
;	ACT:항문애무/항문펠라/밀어 넘어트린다
;ACT분류:2:도구
;	ACT:항문바이브/항문비즈/페니스밴드
;ACT분류:3:성교
;	ACT:후배위/배면좌위/항문섹스
;ACT분류:4:수치
;	ACT:자위/비패개장/수치플레이/야외플레이/방뇨
;ACT분류:5:봉사
;	ACT:발핥기강제
;ACT분류:6:가학
;	ACT:스펭킹/채찍/바늘/삼각목마/관장세트
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착/피스트퍽/애널피스트/양쪽피스트/공기
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:역강간
;
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_7
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=의견을 내다니 좋은 번가슴군요!)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;ACT, 공기에서는 파생하지 않는다
	;=================================================
	;REACT파생 1, 의견을 내다니 좋은 번가슴군요!
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신이 이상하게 되기 시작하고 있는 것이 무서워져,
			;%CALLNAME%에 이제(벌써) 허락해와 간원했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 아첨하도록(듯이)%CALLNAME%를 올려보면, 이제(벌써) 허락해와 바랐다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 이제(벌써) 허락하고라고 해 출했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 눈물을 머금어%CALLNAME%에 이제(벌써) 허락해 주세요간원했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「허락하면 좋겠다?  %RAND_SPLIT("그렇다면 어떻게 하면 좋은가, 알까?/응후후… 어떻게 하지?")%」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM8, 기분좋게 해
;────────────────────────────────────
;·REACT분류:받아들인다
;·죠교자의 소질, BASE:이성, 아라이멘도에 의해 REACT파생 1, 「이 철면피의 암퇘지놈!」(이)가 된다
;·파생 1의 경우는징계·포인트가산
;·REACT인상:0에서멀어짐
;·의존도변화(0, +2)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:항문애무/항문펠라/밀어 넘어트린다
;ACT분류:2:도구
;	ACT:페니스밴드
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시/수치플레이/야외플레이/방뇨
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;
;TEQUIP:볼개그는 튕겨진다
;-------------------------------------------------
@KOJO_COM_K1_8
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=이 철면피의 암퇘지놈!)
;-------------------------------------------------

;REACT파생 0는 사실상 COM 명칭 그대로 해석 해 줄 수 있으면 좋을까
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 이 철면피의 암퇘지놈!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌SOURCE가 일정 이상
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && SOURCE:MASTER:쾌Ｃ + SOURCE:MASTER:쾌Ｖ + SOURCE:MASTER:쾌Ａ + SOURCE:MASTER:쾌Ｂ > 10
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 좀 더 하고라고 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 몹시 꾸짖을 수 있는 것보다는 좋다고 생각해, 이대로 하고라고 말했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 좀 더기분좋게 해계속 이라고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 쾌감에 빠져, 좀 더 자주(잘) 해와 졸랐다…
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;쾌SOURCE가 일정 이상의 네스트를 개방하지 않는 경우는, 상황에 관련되지 않고 이 개소가 보완합니다
;본체측의 처리를 나누어지고 있으므로, 텐프레측도 일단 나누기라고 했다가
;현시점에서는 내용에 그다지 차이가 없기 때문에, 이 개소만이라도 좋은 생각은 듭니다
;다만 이 바리안트, 지문이 단번에 증가하거나 바뀌거나 하므로 뭐라고도 말할 수 없는 곳
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 욕망에 정직하게
			;좀 더 기분 좋은 일을당했다실 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 몹시 꾸짖을 수 있는 것보다는 좋다고 생각해,
			;어차피라면 기분 좋아지고 싶다고 했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는기분좋게 해라고 해 출했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는정욕의 포로되었다야 게, 좀 더 기분 좋은 일 해라고 호소했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「어떻게 하지? 응후후…」
			CASEELSE
				PRINTFORMW 「솔직하게 말해 주는 것은, 기뻐요」
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;쾌락계 반응
;=====================================================================
;·쾌SOURCE나 특정의 PALAM가 트리거가 되어 발생한다
;·COM에 따라서는 ACT분류가 트리거가 된다
;-------------------------------------------------

;──────────────────────────────────
;COM10, 부끄러워한다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·징계·포인트가산
;·쾌SOURCE가 일정 이상의 경우는 파생 1, 쾌감에부끄러워한다가 된다
;·의존도변화(+1, 0)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:성에 관한 이야기를 하다/의상 변경을 요구한다
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:수치
;	ACT:자위/비패개장/자위과시/수치플레이/야외플레이/방뇨
;ACT분류:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/가슴애무강제
;ACT분류:이상
;	ACT:방치플레이
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;※SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ, PALAM:치정가 트리거가 된다
;-------------------------------------------------
@KOJO_COM_K1_10
;-------------------------------------------------
;·TFLAG:REACT파생(0=상황에부끄러워한다/1=쾌감에부끄러워한다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 쾌감에 대해서부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 좀 더 기분 좋아지고 싶다고 생각하고 있는 자신을 의식해 버려,
			;뺨을 물들여 부끄러워했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감이 주어지는 일에 강한수치를 느껴
			;몸을 움츠러들게 해 부끄러워했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 복받치는 쾌감을 숨기지 못하고, 부끄러워해 얼굴을 돌렸다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 쾌감으로 소리를 흘려, 깜짝했다야 게숨을 삼키면
		;시선을 피해 부끄러워했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;REACT파생 1, 쾌감에부끄러워한다를 썼을 경우는 REACT파생 0, 상황에부끄러워한다취급
;파생 1을 쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;이하에 나타내는 지문은 REACT파생 0의 것
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 모습을 볼 수 있는 것으로 이상한 기분이 되어 버려,
			;더욱 더 뺨을 물들여 부끄러워했다…
			;PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 자신을 욕보이는 시선에 견딜 수 없게 되어,
			;몸을 안도록(듯이)해 부끄러워했다…
			;PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(50) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 욕보여지고 있는 상황에 견딜 수 있지 못하고, 수치의 너무 얼굴을 돌렸다…
		PRINTFORMW 「여길 봐줘?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(50) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는%CALLNAME%가 빠는 것 같은 시선을 느껴
		;무심코 시선을 피해 부끄러워했다…
		PRINTFORMW 「초심인 너도 사랑스러워」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「부끄러워하는 너도 나쁘지 않아」
		SIF IS_CONDITION(TARGET, "싫증")
			PRINTFORMW 「 그렇지만 그렇구나. 좀 더 솔직해져 주면, 나는 기뻐요」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM11, 싫어한다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·REACT파생 1는 ACT 방해가 되어 REACT분류:날뛴다취급
;·의존도변화(-1, -1)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:의상 변경을 요구한다
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/발코키
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;
;※SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ, PALAM:치정가 트리거가 된다
;-------------------------------------------------
@KOJO_COM_K1_11
;-------------------------------------------------
;·TFLAG:REACT파생(0=싫어한다/1=싫어해 ACT를 방해)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 싫어해 방해한다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 순간에 싫어해손를 내 버렸지만,
			;%CALLNAME%에 미안한 것 같은 표정을 보였다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의기력를 쥐어짜, 싫어해%CALLNAME%로부터 피했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 싫어해 몸을 비틀어,%CALLNAME%의 방해를했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 싫어해%CALLNAME%를 밀쳤다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 싫어한다가 ACT는 방해하지 않고 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 내심에서는 그렇게 그렇지만도 않다고 생각하면서도,
			;부끄럽기 때문에 싫어해 보였다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의기력를 쥐어짜, 떨리는 소리로 싫다고 말했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 고개를 저어 싫어했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 그런 것 싫다고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「혐이라고 해도 그만두어 줄 수 없어요」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM12, 서투르다고 모욕한다
;──────────────────────────────────
;·REACT분류:태연하게 한다
;·REACT파생 1(강한척 한다)는 REACT분류:소극적으로 한다취급
;·징계·포인트가산
;·파생 0, 새겨 내린다(REACT인상:악인상대)
;·파생 1, 강한척 한다(REACT인상:나쁜인상)
;·의존도변화(-2, -3)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:성교
;	ACT:정상위/후배위/대면좌위/배면좌위
;
;※TFLAG:ACT분류(1=애무/3=성교)가 트리거
;-------------------------------------------------
@KOJO_COM_K1_12
;-------------------------------------------------
;·TFLAG:REACT파생(0=새겨 내린다/1=강한척 한다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 강한척 해(쾌감을 느끼면서)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;%CALLNAME:MASTER%는 허덕이는 소리 섞임의 뻔히 보임의 허세로,%CALLNAME%에 하손라고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;착실하게 쾌감을 느끼면서도 %CALLNAME:MASTER%는 하손라고 우겼다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 쾌감에 얼굴을 비뚤어지게 하면서도, 이런 것은 느끼지 않는다고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 새겨 내리는(실제로 하손인 경우) 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;%CALLNAME:MASTER%는 힘껏의 허풍으로,%CALLNAME%에 하손라고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:TARGET%의 미숙한 테크닉에, %CALLNAME:MASTER%는 하손라고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 이것이라면 자신이 상손실 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「서투른 일도 그 안에, 그리워지는 날이 오는 것」
				PRINTFORMW 「지금은, 지금의 나를 즐겨 주세요」
				;쓰고 있어 눈치챈 부메랑. 눈치챈 %CALLNAME:TARGET%씨. 스스로 자신을 웃는다
;OriginalString : 아웃4, "くすっ")
				CALL FADEOUT(4, "키득")
				PRINTFORMW 「 나도 그렇게 해요」
			CASEELSE
				PRINTFORMW 「지금부터 능숙해지기 때문에 좋아」
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM13, 겁없이 웃는다
;──────────────────────────────────
;·REACT분류:태연하게 한다
;·징계·포인트가산
;·REACT인상:나쁜인상
;·의존도변화(-2, -3)
;-------------------------------------------------
;·트리거가 되는행동, 소스, 파라미터
;·SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ, SOURCE:고통/구속, ACT분류:가학
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/매도/삼각목마/관장세트
;-------------------------------------------------
@KOJO_COM_K1_13
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;PRINTFORML %CALLNAME:MASTER%는 자신은 아직 제정신이라고 확인하도록(듯이) 웃어 보였다…
			;뉘앙스를 잘못 잡고 있는 예감이 하므로 comment out
			PRINTFORMW 「웃을 수 밖에 없을까?」
			PRINTFORMW 「그런 때는 좀 더, 생긋 웃으세요」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 들러붙은 것 같은 미소를 반했다가, 눈이 헤엄치고 있다…
			PRINTFORMW 「여기 보세요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 일부러 %CALLNAME:TARGET%를 도발하도록(듯이) 웃었다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는%CALLNAME%가죠교에 고심하고 있는 것을 조소했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;아라이멘도와플레이야의 상상에 의지해, 필요이상으로 묘사하지 않는 것이 좋은 것 같다
		PRINTFORMDW %CALLNAME:TARGET%는 미소지어 돌려주었다
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM14, 쾌감을 참는다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·일정 조건으로 REACT파생 1 「애원(REACT분류:용서를 빌다)」취급
;·파생 1의 경우 REACT인상는 0에가까워짐
;·의존도변화(+1, -1)
;-------------------------------------------------
;※트리거가 되는행동, 소스, 파라미터
;·SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/스마타
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트
;-------------------------------------------------
@KOJO_COM_K1_14
;-------------------------------------------------
;·TFLAG:REACT파생(0=내구/1=애원)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 애원 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	;고소비 개소에이루는대로가 되어 버리는 것 같은 언동을 배치하는 것은 어떨까…
	IF 구상기입체크(50) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 쾌감으로 제정신을 잃는 일에 무서워해, 벌써 허락하고라고 말했다…
			PRINTFORMW 「무리해―」
			PRINTFORMW 「이상하게 되어 버려서 좋아요. 내가 받아 들여 주어요」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감을 받아들이려고 하지 않고, 그만두어와 간원했다
			PRINTFORMW 「그만두었으면 좋은거야?」
;OriginalString : 인4, "「私、」")
			CALL FADEIN(4, "「나,」")
;OriginalString : 아웃4, "「私は」")
			CALL FADEOUT(4, "「나는」")
;OriginalString : 인4, "「やめたくないわ」")
			CALL FADEIN(4, "「그만두고 싶지 않아요」")
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(50) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 강한 쾌감을참다하면서, 이제(벌써) 이런 일 그만두어와 간원했다…
		PRINTFORMW 「싫어요, 그만두어는 주지 않아요 원」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(50) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 쾌감을참다하면서허가를 바라고 있다…
		PRINTFORMW 「내가 이렇게 해주는데, 자연스럽게 인내하는것도 어쩐지 화가 나…」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 쾌감을참다취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(50) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는욕망를 뿌리쳐, 쾌감을 견디려고했다…
			PRINTFORMW 「꽉 눈을 감고는%RAND_SPLIT("괴롭혀 신경쓰고 있다/하아하아 하고 있다")%너 꽤 그림이 되어있는 원이야」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감을 받아들이려고 하지 않고참다 하고 있다…
			PRINTFORMW 「이래서야 혐?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 필사적으로 쾌감을참다했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 분명한 쾌감을 느끼면서도, 그것을참다 하려고 하고 있다…
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「참아도 쓸모없어? 네가 %RAND_SPLIT("항복해 줄 때/공략될 때")%까지 계속하는 것」
		PRINTFORMW 「이변은 내가 %RAND_SPLIT("해결할 때/이길 때")%까지 계속되는거야」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM15, 쾌감을 받아들인다
;──────────────────────────────────
;·COM16, 기분 좋다! (와)과 거의 같음. 차이는의존도의변화만
;·죠교대상가 의존하기 쉽다
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도변화(+1, +5)
;-------------------------------------------------
;※트리거가 되는행동, 소스, 파라미터
;·SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/스마타
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K1_15
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;쾌감에방심 해 버리면, %CALLNAME:MASTER%의 심은 순식간에 녹기 시작했다
		PRINTFORMW 「무리하고 있구나. 알아 너의 일인걸」
		PRINTFORMW 「거기까지 하지 않아도 괜찮은 것이지만, 뭐 좋아」
		;전회ACT 추가 묘사
		IF 구상기입체크(100) && IS_NOWACTNAME("정상위시킨다/대면좌위시킨다")
;OriginalString : 아웃4, "よしよし")
			CALL FADEOUT(4, "좋아 좋아")
			PRINTFORMW 「 나를 위해 무리했구나」
;OriginalString : 아웃4, "ぽんぽん")
			CALL FADEOUT(4, "팡팡")
			PRINTFORMW 「노력했구나」
			PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 머리를 쓰다듬어 주었다
		ENDIF
		IF 구상기입체크(100) && IS_NOWACTNAME("키스")
		;바라지 않는 행복감에 마음이 채워지는 것을 느껴 눈에 눈물을 머금었다
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 달콤한 교성을 주면서 쾌감을 받아들였다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;본심은 저확률로 누설한다
		SIF !RAND:3
			PRINTFORMW 「분명하게, 나의 일도 생각하세요?」
		PRINTFORMW 「너만 기분 좋아지는 것은, 안되니까」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「좋을 것 같은 소리 내 버려서♪침 흘리고 있어? 이제… %RAND_SPLIT("어쩔 수 없네/한심하네/안되구나")%」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM16, 기분 좋다!
;──────────────────────────────────
;·COM15, 쾌감을 받아들인다와 거의 같음. 차이는의존도의변화만
;·주인님의 의존을 권하기 쉽다
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도변화(+4, +1)
;-------------------------------------------------
;※트리거가 되는행동, 소스, 파라미터
;·SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/스마타
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K1_16
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;쌍방정욕성교봉사
	;=================================================
	IF 구상기입체크(50) && V_SEX(MASTER) && IS_CONDITION(MASTER, "정욕") && IS_CONDITION(TARGET, "정욕")
		PRINTFORM 「도 취할 수 있고-의 보지로 자지 껴 해, 좀 더 기분 좋아져
		CALL HEARTBW
		PRINTFORMDW %CALLNAME:TARGET%는 군침을 흘리면서 필사적으로 허리를 털고 있다
		RETURN 1
	ENDIF
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;%CALLNAME:MASTER%는 아첨하도록(듯이) 기분 좋은 것 같은 소리로 허덕이고 있다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는쾌락를 얻을 수 있는 행복을 감사하는것 같이 소리에 출했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 달콤한 소리로 쾌감과 기쁨을%CALLNAME%에게 전했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		[SKIPSTART]
		;묘사 가필에 대해 comment out
		PRINTFORMW 「응… 계속해요?」
		[SKIPEND]
		SELECTCASE RAND:3
			CASE 0
;OriginalString : 아웃4, "「ん…」")
				CALL FADEOUT(4, "「응…」")
			CASE 1
;OriginalString : 아웃8, "（気持ちいいんだ…）")
				CALL FADEOUT(8, "（기분이 좋다…）")
			CASEELSE
;OriginalString : 아웃4, "「えへへ…」")
				CALL FADEOUT(4, "「에헤헤…」")
		ENDSELECT
		PRINTFORMW 「계속해요?」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;성교계 반응 COM20 접수대
;=====================================================================
;·ACT분류:성교/성봉, ACT:파이즈리한다/성기마찰한다등이 트리거가 되어 발생
;-------------------------------------------------

;──────────────────────────────────
;COM20, 허리를 흔든다
;──────────────────────────────────
;·주도권(SEX_INITIATIVE)에 의해 내용이 다른 COM
;-------------------------------------------------
;·ACT분류가성교봉사, ACT:기승위의 경우는 주도권은죠교대상(SEX_INITIATIVE(MASTER))
;·REACT분류:적극적으로 한다
;·의존도변화(+5, +3)
;-------------------------------------------------
;·ACT분류가성교, ACT:역강간/성기마찰한다/파이즈리한다의 경우는 주도권은죠교자
;·※성기마찰한다/파이즈리한다는 SEX_INITIATIVE(TARGET)에서는 참조 할 수 없다
;·REACT분류:받아들인다
;·의존도변화(+4, +8)
;-------------------------------------------------
;·REACT인상:좋은인상
;-------------------------------------------------
;·애태움중은 실행 불가
;·ACT:파이즈리한다의 경우, 죠교자가빈유라고 튕겨진다
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:성기마찰한다/파이즈리한다
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K1_20
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;주도권은죠교대상(ACT분류가성교봉사/ACT:기승위의 경우)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && SEX_INITIATIVE(MASTER)
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;%CALLNAME%의 환심을 얻기 위해, %CALLNAME:MASTER%는 힘껏 허리를 사용했다…
		PRINTFORMW 「노력해 봉사해 주는 것은 기뻐요」
		PRINTFORMW 「그런데? 너도 기분 좋아져 주지 않으면…」
;OriginalString : 인16, "「駄目なんだから」")
		CALL FADEIN(16, "「안 되는 것이니까」")
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동/자연스러운행동와 통합
	;=================================================
	;자연스러운행동와 통합
	;IF 구상기입 체크(100) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 기쁘게 하려고 허리를 사용하고 있다…
	IF 구상기입체크(100) && GROUPMATCH(GET_COM_RANK(), "자연스러운행동", "무리가 없는행동")
		;=================================================
		;쌍방정욕 기분이 좋다! 레이무씨
		;=================================================
		IF IS_CONDITION(MASTER, "정욕") && IS_CONDITION(TARGET, "정욕")
			PRINTFORM 「개-기분 좋다…개-기분 있어라…
			CALL HEARTBW
			;상황은삽입중에 대해 「여기」는 부적절. 레이무가 간역의 경우도 여기가 되는 모양.
			;PRINTFORM 「-의 여기… 좀 더%RAND_SPLIT("쑤컹쑤컹/동글동글/쑤컹쑤컹/철퍽철퍽")%해…
			PRINTFORM 「-의 저기. 좀 더,%RAND_SPLIT("쑤컹쑤컹/동글동글/쑤컹쑤컹/철퍽철퍽")%해
			CALL HEARTBW
		;=================================================
		;당신가정욕 강하게 대답하는레이무씨
		;=================================================
		ELSEIF IS_CONDITION(MASTER, "정욕")
			PRINTFORMW 「%RAND_SPLIT("이!/본궤도에 올라아/번창해 버려")%…좀 더 상냥하고…해 주세요…」
			PRINTFORMW 「(듣)묻지 않네요…%RAND_SPLIT("아 벌써/이 바보")%」
			PRINTFORMW 「너로 되는 대로라고 하는 것도, 어쩐지 %RAND_SPLIT("적이군요/마음에 들지 않아요/나답지 않을까요")%…」
		;=================================================
		;%CALLNAME:TARGET%정욕 레이무씨 받고 기분 좋게 해
		;=================================================
		ELSEIF IS_CONDITION(TARGET, "정욕")
					PRINTFORM 「응. 너의 좋아하게 움직여도 좋으니까
					CALL HEARTBW
					PRINTFORMW 「%RAND_SPLIT("/내 보지")% %RAND_SPLIT("/자지로")% 기분 좋게 해줘」
		;=================================================
		;그 이외
		;=================================================
		ELSE
			SELECTCASE RAND:2
				;%CALLNAME:TARGET%씨가 기분 좋게 해
				[SKIPSTART]
				;기입 개소 미스의 예감. comment out
				CASE 0
					PRINTFORM 「응… 너의 좋아하게 움직여도 좋으니까」
					PRINTFORMW 「기분 좋게 해
					CALL HEARTBW
				[SKIPEND]
				CASE 1
					[SKIPSTART]
					;오른쪽 클릭에서는 표시되고 있을 뿐이 되므로 기대가 빗나간다. 역. comment out
					;오른쪽 클릭 해 2행째를 놓친다. 오른쪽 클릭 해 본다. 두 번 즐긴다.
					;템포의 조정
					;CALL FADEIN(20, " 「좀 더, 좀 더!」")
					;CALL FADEOUT(20, @" 「%CALLNAME:TARGET%로! 기분 좋아져!」")
					[SKIPEND]
;OriginalString : 아웃8, "「もっと、もっと動いて！」")
					CALL FADEOUT(8, "「좀 더, 좀 더 움직여!」")
;OriginalString : 인8, @"「%CALLNAME:TARGET%で！気持ち良くなって！」")
					CALL FADEIN(8, @"「%CALLNAME:TARGET%로! 기분 좋아져!」")
				;빈정거린다레이무씨
				CASEELSE
					PRINTFORMW 「완전히 번창해 버려서…♪」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는허리기술로 상손를 느껴시킨다일로 기쁨을 찾아내고 있다…
		PRINTFORMW 「필사적으로 허리 털어 버려서…응후후」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;SEX_INITIATIVE(MASTER)를 쓰지 않았던 경우는 여기서 보완됩니다만 범위가 넓기 때문에
;최저한, TEQUIP:성교중/성교봉사중, IS_COMGRONAME/IS_NOWACTNAME든지로 나누어 쓰지 않으면 정합성은 무너지는 일이 될까하고
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우(ACT분류가성교/ACT:역강간/성기마찰한다/파이즈리한다의 경우)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;%CALLNAME:MASTER%가 허리를 아무리 털어도, 자신의 쾌감이 강해질 뿐이었다…
		PRINTFORM 「응후후…이봐요 힘내라 힘내라
		CALL HEARTBW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME%의 신체에 빠지도록(듯이), %CALLNAME:MASTER%의 허리는 승손에 움직여 쾌감을 탐낸다…
		PRINTFORMW 「 이제 %RAND_SPLIT("조금/살짝")% %RAND_SPLIT("부드럽게/천천히")%」
		PRINTFORMW 「허리, 말을 안들어? 정말… 어쩔 수 없구나」
		PRINTFORMW 「…1번 내버리면 침착해질려나?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 자신이 기분이 좋게 여차저차허리를 흔들고 있다…
		PRINTFORMW 「열중해 버려서」
		PRINTFORM 「%RAND_SPLIT("우후후/에헤헤/…응후후")%
		CALL HEARTBW
		PRINTFORMW 「나의 일도 기분 좋게 해 주세요?」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「함께 기분 좋아집시다
		CALL HEARTBW
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM21, 안에 내!
;──────────────────────────────────
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·죠교대상이남자의 경우는 튕겨진다
;·의존도변화(+10, +10)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;-------------------------------------------------
@KOJO_COM_K1_21
;-------------------------------------------------
;·TFLAG:REACT파생(0=Ｖ에 내/1=Ａ에 내)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에 내!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 범해져쾌락로 마음을 어지럽혀지면서
		;자포자기되었다야 게, 이제(벌써) 안으로 좋은, 안에 내고라고 말했다…
		PRINTFORMW 「좋으면 사양은 하지 않아요」
		PRINTFORMW 「응…욕구를 말하면 좀 더,%RAND_SPLIT("마음속으로부터/기쁜 듯한")%의를 (듣)묻고 싶어요」
		RETURN 1
	ENDIF
	
	;정상위, 대면좌위의 경우는 지문이변화
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는행동"
		;정상위/대면좌위의 경우	%CALLNAME:MASTER%는 %CALLNAME:TARGET%의 허리를 양발로 단단히안아, 안에 내와 속삭였다…
		;그 외의 경우			%CALLNAME:MASTER%는 관철해지는 쾌감에 빠져, 안에 내! 라고 외쳤다…
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%(을)를 꼭 껴안으면(자)%RAND_SPLIT("허리의 움직임을 빨리 했다/%CALLNAME:MASTER%의 자궁에 첨단을 꽉 눌렀다/기쁜 듯한 표정을했다")%」
		;리턴 없음으로기타·조건없음개소에 연결한다
		;RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 범해지는 쾌감과정욕에 지배되어
		;황홀한 기분의 표정으로 어떻게 되어도 괜찮은, 안에 갖고 싶다고 외쳤다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RAND:3
			CASE 0
				PRINTFORM 「나의 아기. 갖고 싶은거야? 응후후
				CALL HEARTBW
			CASE 1
				PRINTFORM 「
				CALL HEARTBW
				;PRINTFORMW」
			CASEELSE
				PRINTFORMW 「아 이제! 너무 좋아!」
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM22, 안에 내면 안돼!
;──────────────────────────────────
;·REACT분류:용서를 빌다
;·REACT파생 1(안에 낸다)의 경우는징계·포인트추가. REACT분류:없음
;·REACT인상:나쁜인상
;·의존도변화(0, -10)
;·참다플래그는 소멸한다
;·죠교대상이남자의 경우는 튕겨진다
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;-------------------------------------------------
@KOJO_COM_K1_22
;-------------------------------------------------
;·TFLAG:REACT파생(0=안에 내지 않는다/1=안에 낸다)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 안에 내면 안돼!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;v1. 010 현재, 파생해도 지문은 공통
	;=================================================
	;REACT파생 1, 안에 낸다
	;=================================================
	IF 구상기입체크(50) && TFLAG:REACT파생 == 1
		PRINTFORMDW %CALLNAME:TARGET%는 최안쪽까지 찔러 붐비면서 허리를 끊었다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 수치도 세상소문도 없고%CALLNAME%를 요구하고 싶은 감정을 열심히 뿌리쳐,
			;중만은 안돼, 밖에 내라고 외쳤다…
			PRINTFORMW 「이상한 곳에서 참아 버려서…안심하며」
			PRINTFORM 「책임을 져 준다
			CALL HEARTBW
		ELSE
			;%CALLNAME:MASTER%는%CALLNAME%의분노를 사는 것을 무서워하면서도,
			;교배되는 것을 무서워해, 안은 그만두어, 내서는 안돼와 비통하게 외쳤다…
			SELECTCASE RAND:3
				CASE 0
					PRINTFORM 「%RAND_SPLIT("당황해 버려/허둥지둥 해 버려")%, 사랑스럽기 때문에
					CALL HEARTBW
				CASE 1
					;남 %CALLNAME:MASTER%의 엉덩이도 가미해, 질내표기를 피한다
					PRINTFORM 「안에 내지 않으면 할 수 없지 않다
					CALL HEARTBW
				CASEELSE
					PRINTFORM 「좋으니까 잉 보세요
					CALL HEARTBW
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 관철해지는 쾌감을 참으면서, 안에 내면 안돼! 라고 외쳤다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 힘 없고%CALLNAME%에 범해져 축하면서도
		;중만은 그만두어, 허락해와애원했다…
		PRINTFORMW 「바보같구나. 여기서 그만둔다면 이런 시무라고 없어요」
		PRINTFORMW 「아무래도 갖고 싶어」
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「바보같구나. 여기서 그만둔다면 이런 시무라고 없어요」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;고통계 반응
;=====================================================================
;·가학계행동나삽입를 수반하는 각종행동로 볼 수 있다
;·v1. 010 현재, 고통계에의존도변화의 처리 없음
;-------------------------------------------------

;──────────────────────────────────
;COM30, 고통을 참는다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·REACT인상:약간좋은인상
;·SOURCE:고통가 없는 경우는 튕겨진다
;-------------------------------------------------
;·트리거가 되는행동, 소스, 파라미터
;·SOURCE:고통/구속
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:가슴애무/입으로 애무/항문애무/항문펠라/키스/파이즈리한다
;ACT분류:도구
;	ACT:바이브/항문바이브/항문비즈
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트
;-------------------------------------------------
@KOJO_COM_K1_30
;-------------------------------------------------
;·TFLAG:REACT파생(0=고통를참다/1=아프지만기분 좋다!)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30_1, 파생:아프지만기분 좋다!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 여러가지 감정을 삼켜, 쾌감과고통를 동시에 참았다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는고통안에 쾌감을 의식해, 무서워하면서 양쪽 모두를참다했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;쾌감과고통가 동시에 덮쳐, %CALLNAME:MASTER%는 흐려진 소리내질렀다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는고통를 감추기 (위해)때문에, 동시에 복받치는 쾌감에 달라붙었다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 1을 개방했다경우는 파생 0, 고통를참다취급
;------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는고통를 견디면서,%CALLNAME%의 행위를 받아들였다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 고민의신음를 루등하면서고통를참다했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는고통를참다했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ACT:이라마치오시에는 지문이변화. 내용은 이하와 같다
	;%CALLNAME:MASTER%는 목 안까지 후벼파지는 괴로움에 오열을 루등하면서도,%CALLNAME%의 행위를 받아들였다
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;그 외 ACT의 경우의 지문
		;%CALLNAME:MASTER%는 이빨을 이를 악물어고통를 참았다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「좋으니까 그대로 굴복해주세요」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM31, 비명지른다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·REACT인상:0에서멀어짐
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다
;ACT분류:애무
;	ACT:가슴애무/입으로 애무/항문애무/항문펠라/키스/파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:바이브/항문바이브/항문비즈
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/매도/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트/공기
;-------------------------------------------------
@KOJO_COM_K1_31
;-------------------------------------------------
;·TFLAG:REACT파생(0=고통에 의한 비명/1=공포에 의한 비명)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 공포에 의한 비명
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 아첨하는 것 같은 비명을 올렸다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 무서워한 것처럼 비명을 올려 보였다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는공포를 느껴 비명을 올렸다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 이상한공포를 느껴 비명을 올렸다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;REACT파생 1를 썼을 경우는 REACT파생 0, 고통에 의한 비명 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는%CALLNAME%가 좋아하는고통의 비명을 자연히(에) 소리에 출했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 비명을 올려고통에 몸부림쳐 보였다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는고통를 견딜 수 있지 못하고 비명을 올렸다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는고통의 너무 비통한 외침을 올렸다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「아 이제, 차이원이군요」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM32, 날뛴다
;────────────────────────────────────
;·파생에 의해 날뛰고 성공(REACT분류:날뛴다), 날뛰고 실패(REACT분류:소극적으로 한다)로 나누어진다
;·날뛰고가 통과하면도구계/성교계/성교봉사계 커맨드를 방해했다취급이 된다
;·징계·포인트추가
;·REACT인상:악인상대
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스
;		성기마찰한다/파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그
;ACT분류:휴식
;	ACT:치료한다
;ACT분류:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_32
;-------------------------------------------------
;·TFLAG:REACT파생(0=날뛰고 성공/1=밧줄구속에 의한 날뛰고 실패/2=제지에 의한 날뛰고 실패)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT파생 2, 제지에 의한 날뛰고 실패(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 2
		;%CALLNAME:MASTER%는 날뛰어 저항하려고했다가, %CALLNAME:TARGET%에 억제 당했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 밧줄구속에 의한 날뛰고 실패(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		;속박된 %CALLNAME:MASTER%는 날뛰어 저항하려고했다가, 밧줄가 한층 더 먹혀들어 다만 아픈 것뿐이다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1으로 2를 썼을 경우는 파생 0, 날뛰고 성공(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
	;※파생 마다의 내용의 차이가 비교적 크기 때문에, 나누어 쓰는 것이 무난하다라고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		;%CALLNAME:MASTER%는 날뛰어 %CALLNAME:TARGET%의 행동를 방해했다. %CALLNAME:TARGET%는 꽤 화나 있는 것 같다…
		PRINTFORMW 「얌전하게 하지 않으면, 얌전하게 시켜요?」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM33, 무서워한다
;──────────────────────────────────
;·REACT분류:용서를 빌다
;·일정 조건으로분노를 사, 징계·포인트추가
;-------------------------------------------------
;·트리거이벤트제
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 함
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다
;ACT분류:1:애무
;	ACT:풋잡하기/밀어 넘어트린다
;ACT분류:2:도구
;	ACT:항문바이브/항문비즈
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:밧줄/눈가리개/볼개그/매도/관장세트
;ACT분류:7:이상
;	ACT:방치플레이/공기플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;-------------------------------------------------
@KOJO_COM_K1_33
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=두려움에 대한초조함)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;ACT, 공기에서는 파생하지 않는다
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는%CALLNAME%보다 오히려 이상하게 될 것 같은 자신에게 무서워하고 있는 것 같다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 분한 생각을 삼켜, 얌전하게 공포의 시선을 향했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는공포를 느끼고 무서워하고 있다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 작은 동물과 같이 무서워하고 있다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「무서워하지 않아 좋아요」
		PRINTFORMW 「무서워해도…하는 것은 함께니까」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;봉사계 반응 COM40 접수대
;=====================================================================
;·죠교자에의애무를 포함한행동로 볼 수 있다
;·ACT분류:봉사+트리거이벤트제
;-------------------------------------------------

;────────────────────────────────────
;COM40, 거부한다
;────────────────────────────────────
;·REACT분류:거부
;·REACT인상:악인상대
;·징계·포인트추가
;·의존도변화(-2, -4)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:영양제를 먹인다
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_40
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM40, 거부한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 미혹을 보이면서도 요구에는 응하지 않았다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 압도되면서도 요구는 삼키지 않았다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%의 요구를거부했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 강한 의지로 요구를 퇴짜놓았다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;이변 해결시의 강경세 김
		PRINTFORMW 「혐이라고 말해지고 있을 것 같습니까와 물러나는 만큼, 벙어리와나인가가 아니어요」
		PRINTFORMW 「무리해서라도 해요!」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM41, 소극적으로 한다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·REACT파생 1는 "소극적인의가 마음에 들지 않는다"되어징계·포인트추가
;·REACT인상:약간좋은인상
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_41
;-------------------------------------------------
;·TFLAG:REACT파생 == 0, 소극적이지만 좋은가
;·의존도변화(-3, +2)
;-------------------------------------------------
;·TFLAG:REACT파생 == 1, 소극적인의가 마음에 들지 않는다
;·의존도변화(-5, 0)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;파생 1, 소극적인의가 마음에 들지 않는다（징계·포인트가산）
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		;%CALLNAME:MASTER%의 소극적인자세에,%CALLNAME%는 불만인 것 같다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신으로부터행동 하는 용기가 없는 것 같다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 분한 듯이 하면서도 거역하려고는 하지 않는다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 어떻게든%CALLNAME%가 말하는 대로 하고 있다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 역네 없다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「손, 떨리고 있어?」
			CASEELSE
				PRINTFORMW 「응…좀 더 빠져들었으면 좋네요」
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM42, 적극적으로 한다
;──────────────────────────────────
;·REACT분류:적극적으로 한다
;·REACT인상:좋은인상
;·참다플래그해소
;·의존도변화(+4, +2)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_42
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자기보다%CALLNAME%를우선 하려고 하고 있다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 의식해 기특하게 거절하는마이구토 하고 있는 것 같다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 진행되어%CALLNAME%에 힘쓰고 있다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%는 어쨌든%CALLNAME%를 기쁘게 하고 싶은 것 같다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그런 기분이 된 것 같구나」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM43, 애정을 담아 한다
;──────────────────────────────────
;·일정 이상의호감도, 죠교레벨가필요
;·REACT분류:적극적으로 한다
;·REACT인상:좋은인상
;·참다플래그해소
;·반발각인가 있으면 튕겨진다
;·의존도변화(+6, +10)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_43
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;%CALLNAME:MASTER%는 평정을 가장하고 있지만, 기쁜 듯한 표정을 숨길 수 없다…
		PRINTFORMW 「지금의 너, 재미있는 얼굴 하고 있는 원이야? 응후후♪」
		;손가락이라도 볼에서도 자지에서도, 통합성이 무너지지 않도록
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 뺨을 때굴때굴밀어넣어 왔다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는 넋을 잃고했다 얼굴로 정중하게 명령을 진한했다…
		;소녀 퇴고중
		;PRINTFORM 「응후…
		;CALL HEARTBW
;OriginalString : 아웃8, "「ね？」")
		CALL FADEOUT(8, "「저기?」")
;OriginalString : 인8, "「손」")
		CALL FADEIN(8, "「손」")
;OriginalString : 인8, "「握って」")
		CALL FADEIN(8, "「잡아」")
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%의 태도는 사랑스러운 사람에 힘쓸 때의 그것 그 자체였다…
		PRINTFORMW 「%RAND_SPLIT("응후후…♪/기분이 좋다/있었는지")%」
		PRINTFORMDW %CALLNAME:TARGET%는 기분을 느슨한, %CALLNAME:MASTER%에 몸을 맡기고 있다
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM44, 태연하게 한다
;────────────────────────────────────
;·REACT분류:태연하게 한다
;·REACT인상:약간좋은인상
;·의존도변화(+2, -2)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_44
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 평상심을 유지하려 하고 있다…
			PRINTFORMW 「너의 열심인 모습도 나쁘지 않지만…」
			PRINTFORMW 「응… 좀 더 솔직해져도 좋아?」
		;실은 싫어
		;ELSE
			;%CALLNAME:MASTER%는 무리를하면서도 평정을 가장하고 있다…
		;	PRINTFORMW 
			RETURN 0
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 기쁘게 해 환심을 권하려고 하고 있다…
		PRINTFORMW 「조금 무리하는 거 아냐?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%에는%CALLNAME%의 모습을 살필 뿐(만큼)의 여유가 있는 것 같다…
		PRINTFORMW 「담담하게 되는 것도, 어쩐지 별로구나…」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	;IF 구상기입 체크(0)
	;	PRINTFORMW 
	;	RETURN 1
	;ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;그 외의 반응 COM50 접수대
;=====================================================================
;·특정의 장면에서 발생하는 이상한행동
;-------------------------------------------------

;────────────────────────────────────
;COM50, 자위를 시작한다
;────────────────────────────────────
;·REACT분류:받아들인다
;·파생 1, 승손에 자위 한데의 경우는징계·포인트추가
;·REACT인상:나쁜인상
;·참다플래그해소
;-------------------------------------------------
;·트리거이벤트제
;-------------------------------------------------
;·밧줄로 속박되고 있으면 안돼
;·벌써자위 하고 있다, 성교중, 성교봉사중는 튕겨진다
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 함
;ACT분류:1:애무
;	ACT:키스
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시/수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/발핥기강제/가슴애무강제
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이
;ACT분류:9:휴식
;	ACT:휴식시킨다
;ACT분류:10:성교봉사
;	ACT:
;-------------------------------------------------
@KOJO_COM_K1_50
;-------------------------------------------------
;TFLAG:REACT파생 == 0, 승손에 자위 하고 있지만 좋은가, 의존도변화(-1, 0)
;TFLAG:REACT파생 == 1, 승손에 자위 하지마, 의존도변화(-3, +2)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;본체 측에 전용의 처리 없음
	
	;=================================================
	;반찬 분기
	;=================================================
	IF 구상기입체크(0) && TEQUIP:자위중
		PRINTFORM 
	ENDIF
	;=================================================
	;REACT파생 1, 승손에 자위 하지마
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「무엇, 마음대로 즉시 시작해」
		;PRINTFORMW 「… 출했다등%RAND_SPLIT("인내/해 알아/허야")%없어요」
		IF TALENT:새드
			PRINTFORMDW %CALLNAME:TARGET%는 비난하는 것 같은 시선을 향하여 왔다
		ELSE
;OriginalString : 아웃8, "「いいけど」")
			CALL FADEOUT(8, "「좋지만」")
			PRINTFORMW 「해도 된다고 말해.」
			PRINTFORMW 「해도 되지만, 내면(자)%RAND_SPLIT("인내해/알아/허야")%없어요」
		ENDIF
		RETURN 1
	ENDIF
	;=================================================
	;이후 승손에 자위 하고 있지만 좋은가
	;=================================================
	;=================================================
	;REACT부하 2, 무리를했다행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를했다행동"
		;%CALLNAME:MASTER%는자위를 하는 것으로 쾌감에 빠져 버리려고 하고 있다…
		PRINTFORMW 「무리하게 그런 일, 하지 않아 좋아요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는행동"
		;%CALLNAME:MASTER%는참다 하지 못하고 승손에 자신을 위로하고 있다…
		PRINTFORMW 「무엇 만지작거려」
		PRINTFORMW 「사람의 일, 마음대로 반찬으로 해…나쁘다\@ TALENT:MASTER:남자 ? 자지 # 아이 \@그런데」
		PRINTFORMW 「부끄럽지만, 멈출 수 없다?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운행동"
		;%CALLNAME:MASTER%의 손는 무의식 중에 자신의성기를 만지작거리고 있는 것 같다…
		PRINTFORMW 「무엇 만지작거려」
		PRINTFORMW 「말해져 깨닫아도 얼굴이군요…」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;=================================================
	;IF 구상기입 체크(0)
	;	RETURN 1
	;ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM51, 도구를 제거한다
;──────────────────────────────────
;·REACT분류:날뛴다
;·TEQUIP:(바이브/항문바이브/항문비즈/관장그릇＋플러그/클리캡/오나홀/유두캡/눈가리개/볼개그/페니스밴드)를장착 하고 있는 것이 조건
;·죠교대상가자위중인 경우는 중단이 된다
;·REACT인상:악인상대
;·의존도변화(-3, -10)
;-------------------------------------------------
;·제외하는도구는 한 번에 1개까지
;※우선도는 이하와 같다
;(눈가리개>관장그릇＋플러그>항문바이브>바이브> 오나홀>클리캡>볼개그>유두캡> 항문비즈)
;-------------------------------------------------
;선택 가능한 조건…
;·TEQUIP:(바이브/항문바이브/항문비즈/관장그릇＋플러그/클리캡/오나홀/유두캡/눈가리개/볼개그/페니스밴드)를장착 하고 있는 것이 조건
;·TEQUIP:밧줄/성교중는 실행 불가
;-------------------------------------------------
@KOJO_COM_K1_51
;-------------------------------------------------
;·밖했다도구의 TEQUIP번호가 TFLAG:REACT파생에 들어간다
;-------------------------------------------------

;지문은 범용 처리가 있을 뿐
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM51, 도구를 제거한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;REACT파생분기를 추가하는 경우는 이 개소의 상층에서
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		;%CALLNAME:MASTER%는장착당했다도구를 승손에 제외해, %CALLNAME:TARGET%는 매우 화나 있는 것 같다…
		;무언이 제일 참는다
		PRINTFORMDW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;COM52, 이루는대로
;────────────────────────────────────
;·효과 없음. 실행할 수 있는 반응이 한 개도 없을 때의 구제
;·죠교자에 좋을대로 희롱해지고 한편, 하라는 대로 되는 목표 상황
;·REACT파생 마다 REACT분류, 의존도변화가 바뀐다
;-------------------------------------------------
@KOJO_COM_K1_52
;-------------------------------------------------
;·TFLAG:REACT파생(0=통상/1=성교계/2=성교봉사계/3=수치계, 강제계/4=봉사계/5=협박계/6=협박계/7=고통계/8=자위중의회화/9=쾌감을 받으면서회화/10=쾌락계/11=회화계/12=일회휴게)
;-------------------------------------------------

;------------------------------------------------
;REACT분류:받아들인다/의존도변화(+4, +10)
;성교계 ACT·역강간
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 「무저항에 범해진다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;PRINTFORML %CALLNAME:MASTER%는 힘이 들어가지 않는 신체를 무저항에 범해져 이송되는쾌락에 허덕이고 있다…
	PRINTFORMW 「어쩐지, 이케나이일 하고 있는 기분이 되어 유곽이군요…」
	RETURN 1
ENDIF

;------------------------------------------------
;REACT분류:받아들인다/의존도변화(+10, +10)
;성교계봉사계 ACT·기승위
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 2, 「빠진 것처럼허리를 흔든다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 2
	;%CALLNAME:MASTER%는%CALLNAME%의 신체에 빠진 것처럼, 숨을 어지럽혀 오로지 허리를 흔들고 있다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;ACT(의상변경/자위/방뇨/개장/이라마치오)
;REACT분류:받아들인다/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 3, 「말해지는 대로 천천히 따른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 3
	;%CALLNAME:MASTER%는 다만%CALLNAME%에 말해지는 대로 따랐다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;봉사계 ACT
;REACT분류:받아들인다/의존도변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 4, 「매료되어진 것처럼봉사를 반복한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 4
	;마치 매료되어진 것처럼, %CALLNAME:MASTER%는 무심하게%CALLNAME%에봉사했다…
	;심 여기에 있지 않고라고 하는 모습으로, 그손 다하고는 이상한 것이다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;ACT(위협하는/징계·과 선언/고함치다/기분나쁘게 웃다/매도)
;반항적인 소질이 있어, 굴복각인 5 이하의 경우
;REACT분류:소극적으로 한다/의존도변화(0, -2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 5, 「분한 듯이 노려본다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 5
	;%CALLNAME:MASTER%는 분한 듯이 입술을 깨물어,%CALLNAME%를 올려보았다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;ACT(위협하는/징계·과 선언/고함치다/기분나쁘게 웃다/매도)
;REACT분류:소극적으로 한다/의존도변화(+2, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 6, 「무서워한 것처럼 응시한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 6
	;%CALLNAME:MASTER%는 무서워한 것처럼%CALLNAME%를 응시했다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;가학계 ACT·고통가 큰 경우
;REACT분류:소극적으로 한다/의존도변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 7, 「소리가 되지 않는 비명을 올려 몸부림쳐 뒹군다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 7
	;%CALLNAME:MASTER%는고통의 그다지 소리가 되지 않는 비명을 올려 몸부림쳐 뒹굴었다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT 또한자위중인 경우
;REACT분류:받아들인다/REACT인상:나쁜인상/의존도변화(-2, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 8, 「오나니─에 열중해 듣지 않았다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 8
	;%CALLNAME:MASTER%는자위에 몰두해 온전히 대답을 할 수 없었다…
	;%CALLNAME%는 불만인 것 같다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT
;REACT분류:받아들인다/의존도변화(+4, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 9, 「기분 좋아서 선대답」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 9
	;%CALLNAME:MASTER%는 쾌감에 몸부림하면서 수긍할 수 밖에 없었다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT 이외의 경우
;REACT분류:받아들인다/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 10, 「머리 진흰색 황홀상태」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 10
	;%CALLNAME:MASTER%는 주어지는 쾌감에 희롱해져 황홀이 되고 있다…
	;녹초가된 머릿속은 새하얗게 물들어, 아무것도 생각하는 것이 할 수 없다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;회화계 ACT
;REACT분류:받아들인다/의존도변화(+8, +4)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 11, 「스스로도 모르는 채 부끄러운 일을입 달린다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 11
	;%CALLNAME:MASTER%는 어딘가 망연히했다대답했다…
	;스스로도 잘 모르는 채로 부끄러운 일을입 달리고 있는 생각이 든다…
	PRINTFORMW 「그러한 일은, 좀 더 맹숭맹숭한 얼굴때에 말하세요」
	RETURN 1
ENDIF

;------------------------------------------------
;죠교자, 혹은죠교대상의상태에 의해일회휴게되었다경우
;혹은대만족보너스로쉬게를선택했다경우
;REACT분류:받아들인다/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;일회휴게의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:일회휴게
	;%CALLNAME:MASTER%는 안심했다야 게체가 힘을 뺐다…
	;=================================================
	;대만족보너스로쉬게를선택했다경우
	;=================================================
	IF 구상기입체크(100) && IS_POLICY("금일", "휴식")
		SELECTCASE TIME
			;낮간  차 후
			CASE 0
				PRINTFORMW 「좋은 날씨군요 」
				;%CALLNAME:MASTER%사망플래그%CALLNAME:MASTER%사망 END에 대응하기 때문에(위해) 가필;아무것도 아닌 것을 모두 즐기는 것이 연인
;OriginalString : 아웃20, "ねぇ見て")
				CALL FADEOUT(20, "저기 봐")
;OriginalString : 인20, "夕日")
				CALL FADEIN(20, "석양")
;OriginalString : 인20, "綺麗ね")
				CALL FADEIN(20, "예쁘구나")
				PRINTFORMDW %CALLNAME:MASTER%(은)는 %CALLNAME:TARGET%와 함께 저녁놀하늘을 바라보았다
				;밤  저녁식사 후
			CASEELSE
				PRINTFORMW 「이런 것, 좋은데…」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;------------------------------------------------
;모든 조건에 해당하지 않는 경우
;REACT분류:받아들인다/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 그 외
;━━━━━━━━━━━━━━━━━━━━━━━━
[SKIPSTART]
;상부 조건에 해당해도 표시되는;죠교대상의 피로에 의한 중지 시에도 표시되는 모양;상정외이기 때문에 comment out
IF 구상기입 체크(25)
	;%CALLNAME:MASTER%는 신체를 내던져, 되는 대로 되어 있다…
	PRINTFORMDW 「응. 반응이 둔하네요… 자기 마음대로해버리는 원이야?」
	RETURN 1
ENDIF
[SKIPEND]

;=====================================================================
;특수한 반응 COM70～
;=====================================================================

;──────────────────────────────────
;COM70, 가게해줘!
;──────────────────────────────────
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도변화(+10, +6)
;-------------------------------------------------
;선택 가능한 조건…
;·애태움중은 아니다
;·TCVAR:MASTER:가버려가 서 있다
;·애무계 ACT(키스/밀어넘어짐 이외) 또한 각 부위의 PALAM가 5000이상이다
;※상기 조건에 들어맞는 경우는 가능
;-------------------------------------------------
;·TFLAG:REACT파생(0=들은체 만체 한다/1=가게해줘 준다/2=가게해줘 주지 않는다）
;-------------------------------------------------
;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도 COM70의선택로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
;현재의 사양에서는 이손의변화를 참조할 방법은 없다. 본체측의 대응이 바람직한다!
;-------------------------------------------------
@KOJO_COM_K1_70
;-------------------------------------------------
;·TFLAG:REACT파생(0=들은체 만체 한다/1=가게해줘 준다/2=가게해줘 주지 않는다）
;-------------------------------------------------

;%CALLNAME:TARGET%는 심술궂은 미소를 띄우며 %CALLNAME:MASTER%를 애태우기 시작했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 2, 가게해줘 주지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;우선 최소한의 분기를 놓아둡니다만
;PENIS(MASTER) 등으로페니스의 유무를 참조했다 (분)편이 무난할지도 모릅니다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:REACT파생 == 2
	;=================================================
	;ACT분류, 애무
	;=================================================
	IF 구상기입체크(0) && MENUMATCH(TFLAG:ACT, "애무")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(MENUMATCH(TFLAG:ACT, "성봉"), TEQUIP:성교봉사중) > 0
		;Ｖ성교봉사중
		;IF V_SEX(MASTER)
		;	PRINTFORMW 
		;;Ａ성교봉사중
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		;Ｖ성교봉사중
		IF V_SEX(MASTER)
			[SKIPSTART]
			PRINTFORMW 「아직 안돼」
			PRINTFORMW 「그렇구나?」
			PRINTFORMW 「네가, 되어 거절해 상관하지 않고 허리를 잡아, 마음대로 허리를 털어 질내 사정해 올 때까지, 보류군요」
			[SKIPEND]
			PRINTFORMW 「 아직 안되어요」
			PRINTFORMW 「그렇구나…네가, 되어 거절해 상관하지 않고 허리를 잡아, 마음대로 허리 털어, 질내 사정해 올 때까지」
			PRINTFORMW 「보류군요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「조금만 더 %RAND_SPLIT("분발해봐/견뎌봐/참아 봐")%」
		RETURN 1
	ENDIF
ENDIF

;%CALLNAME:TARGET%는 상냥하게 미소지으면서애무를 계속했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 1, 가게해줘 준다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;=================================================
	;ACT분류, 애무
	;=================================================
	IF 구상기입체크(0) && MENUMATCH(TFLAG:ACT, "애무")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(MENUMATCH(TFLAG:ACT, "성봉"), TEQUIP:성교봉사중) > 0
		;;Ｖ성교봉사중
		;IF V_SEX(MASTER)
		;	PRINTFORMW 
		;;Ａ성교봉사중
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		;Ｖ성교봉사중
		IF V_SEX(MASTER)
			[SKIPSTART]
			;가버려도 아니어도 발생할 수 있는 위에사정분기와 입기 때문에(위해) 재고가필요
			PRINTFORMW 「%RAND_SPLIT("이제(벌써)사정그래? /좋아요, 가득 내/응, 와")%」
			PRINTFORM 「나는 너%RAND_SPLIT("의 신부/의 것/용")%(이)니까
			CALL HEARTBW
			[SKIPEND]
			IF !RAND:3
				PRINTFORMW 「나의 보지로 가고 싶다? 보지로 자지 기분 좋고--하고 싶다…?」
				PRINTFORMDW %CALLNAME:TARGET%는 기쁜듯이 %CALLNAME:MASTER%의 말을%RAND_SPLIT("악물고/반추 해")%(이)라고 있다
			ELSEIF !RAND:2
				PRINTFORMW 「응…♪」
				PRINTFORM 「너후응응, 의 보지로 살릴 수 있어 주어요
				CALL HEARTBW
			ELSE
				PRINTFORM 「 나는 너%RAND_SPLIT("의 신부/의 것/용")%(이)니까…너는, 물어봐 주지 않으면…
				CALL HEARTBW
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「야무지지 못하기 때문에…뭐, 좋아요」
		RETURN 1
	ENDIF
ENDIF

;%CALLNAME%는 못된 장난 같은 미소를 띄우면서, %CALLNAME:MASTER%의 요구를 (들)물어 류했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 0, 요구를 들은체 만체 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 0
	[SKIPSTART]
	;구문 미스에 대해 comment out
	SELECTCASE RANDCOM(3, 0)
		CASE 0
			;님표기는 살려 두어야 할 것인가가 고민거리
			PRINTFORMW 「%CALLNAME:TARGET%님너무 좋아! 라고 말할 수 있으면, 생각해 주어도 괜찮아요?」
			PRINTFORMW 「%RAND_SPLIT("이봐요! /이봐요 이봐요,/어머 어머♪")%관념해 말해버릴 하세요」
		;CASE 1
		;	PRINTFORMW 
		;CASE 2
		;	PRINTFORMW
		CASEELSE
			PRINTFORMW 「-응… 어떻게 하지?」
	ENDSELECT
	;가게 할 생각의상태로부터가게해줘! 때에 모순
	;들은체 만체 하고 있는 상황으로 상손로 하고 있는 것이 문제
		PRINTFORMW 「-응… 어떻게 하지?」
		PRINTFORM 「응후후…
		PRINTFORMD %CALLNAME:TARGET%님너무 좋아!
		PRINTFORMW 라고 말할 수 있으면, 생각해 주어도 괜찮아요?」
		PRINTFORMW 「%RAND_SPLIT("이봐요! /이봐요 이봐요,/어머 어머♪")%관념해 말해버릴 하세요」
	RETURN 1
	[SKIPEND]
ENDIF
