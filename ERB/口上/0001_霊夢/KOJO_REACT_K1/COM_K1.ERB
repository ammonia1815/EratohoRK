;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 汎用リアクション구상ファイル
;
;	個別リアクション(REACT)から抜けてきたものを当該COMで処理するファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;%CALLNAME:MASTER%의 행동からの逆引き

;=====================================================================
;汎用リアクション구상
;=====================================================================
;・個別リアクション구상を表示しなかった場合に表示
;・KOJO_REACT_KX1_Y1のRESULTが0であった場合、汎用リアクションが呼び出される
;・REACTで表示했다か否かはRESULTで判定されるので、REACTで表示してRETURN 0でさらに汎用も表示시킨다事も可能ではある
;・KOJO_COM_KX1_Y1からRESULT 0が返ると統括汎用リアクション(KOJO_REACT_KX)が呼び出される
;---------------------------------------------------------------------
;簡易フロー
;    表示タイミング・죠교대상のリアクション（コマンド・反応）
;        アクション번호別の、個別リアクション구상
;            表示しなかった場合、コマンド번호別の汎用リアクション구상←(今ここ)
;                表示しなかった場合、統括汎用リアクション구상
;---------------------------------------------------------------------
;関連変数/関数類…
;
;ACT분류系
;TFLAG:ACT분류 このターンの죠교メニュー(0=회화/1=애무/2=도구/3=성교/4=수치/5=봉사/6=가학/7=이상/8=사역/9=휴식/10=성교봉사)
;GET_ACTM ACT번호からACT분류번호を취득する関数
;MENUMATCH 指定했다ACT분류に含まれていれば真を返す(회화/애무/도구/성교/수치/봉사/가학/이상/사역/휴식/성봉)
;
;COM系
;TFLAG:REACT파생 各SELECTCOMの派生。COM毎に派生内容は異なる
;TFLAG:REACT부하 선택했다COMの기력消費を判別する(0=自然な행동/2=無理のない행동/無理を했다행동)
;COMCOR_POSI 主に소극적으로 한다系COMの기력消費が大きい際(TFLAG:REACT부하 == 2である場合)に、COM보정値が호의的かどうかを判断
;
;その他
;TFLAG:REACT인상 各SELECTCOMの相손への心象(0=영향なし/1=好印象大/2=好印象小/3=悪印象小/4=悪印象大/5=0から遠ざかる/6=0に近づく)
;TFLAG:징계・포인트(100溜まると징계・開始)
;FIRSTCOM　初回汎用リアクション判定関数　SELECTCOMを自動で参照するので、現在선택했다COMごとに번호が独立して保存される
;ONCECOM　조교중初回判定用関数。FIRSTTIME系と違い죠교前に初期化される。基本的な仕様はFIRSTCOMと同様
;RANDCOM(AUTORAND系)　RAND処理で이벤트種類ごとの번호を返すが、全種見るまで同じ数字は返さない。全て表示했다ときに初期化。
;
;---------------------------------------------------------------------

;=====================================================================
;※ACT분류とその派生について
;
;COMによっては発生するACT분류が複数にまたがります
;各COM内をTFLAG:ACT분류で分けてやる事により、より正確に状況を参照する事が可能です
;ただ、ACT분류自体も派生する事があり、この場合、TFLAG:ACT분류では派生状況を参照出来ず、若干おかしな事になります
;
;例えば…、ACT55,발핥기강제は本来ACT분류:봉사系ですが、이상系から発生する場合があり、
;TFLAG:ACT분류の区切りでは、想定通りの구상が表示されないという事が考えられます
;
;そこでTFLAG:ACT분류ではなく、@MENUMATCHなり@GET_ACTMでACT분류毎の分岐を設けてやると、派生先を参照する事が可能になり
;実際に行われているACT분류で処理されるようになります
;IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "봉사")のような枠を設けてやれば、上に挙げたような状況にも対応が可能です
;MENUMATCHの分類毎の定義についてはこのファイルの28行目を参照して下さい
;
;ACT분류派生一覧…
;（各数値はACT번호）
;회화 (気分0/호의1/지식2/慰め3/脅す4/의상5/합의6/仕置き7/고함치다8/嗤う9/매도66)
;애무 (손で10/가슴を11/입で12/Ａ손13/Ａ입14/키스15/Ｃ擦16/Ｂ擦17/발で18/Ｖ손19/ﾛｰﾀｰ20/押倒27)
;도구 (ﾛｰﾀｰ20/バイ21/Aﾊﾞｲ22/Aﾋﾞｰ23/Ｃ愛24/ﾆﾌﾟﾙ25/Ｐ帯26/관장68)
;성교 (正常30/後背31/騎乗32/대면33/背面34/ＡＳ35)
;수치 (자위40/開帳41/見せ42/거울43/야외44/방뇨45/コス(미구현)46/방치73)
;봉사 (손で50/입で51/가슴で52/스마타53/발舐55/イラ56/騎乗32/가슴を57)
;가학 (平손60/むち61/はり62/なわ63/目隠64/0입枷65/매도66/木馬67/관장68/イラ56)
;이상 (Fist70/ＡFi71/両Fi72/방치73/公衆74/Ｐ装75/嗤う9/발で18/발舐55/木馬67)
;사역 (미구현)
;휴식 (休み90/服薬91/치료92)
;성교(봉사) (正常95/後背96/騎乗103/대면97/背面98/ＡＳ99)
;
;=====================================================================
[SKIPSTART]
MENUMATCH小分岐見本…

他の分岐との우선度に注意して、필요に応じて各COMに追加して使用して下さい
	;=================================================
	;ACT분류,회화
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "회화")
	ENDIF
	;=================================================
	;ACT분류,애무
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "애무")
	ENDIF
	;=================================================
	;ACT분류,도구
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "도구")
	ENDIF
	;=================================================
	;ACT분류,성교
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "성교")
	ENDIF
	;=================================================
	;ACT분류,수치
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "수치")
	ENDIF
	;=================================================
	;ACT분류,봉사
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "봉사")
	ENDIF
	;=================================================
	;ACT분류,가학
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "가학")
	ENDIF
	;=================================================
	;ACT분류,이상
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "이상")
	ENDIF
	;=================================================
	;ACT분류,휴식
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "휴식")
	ENDIF
	;=================================================
	;ACT분류,성봉
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "성봉")
	ENDIF
[SKIPEND]
;---------------------------------------------------------------------

;※各分岐には対応する地の文を提示してありますが、最終的にどのような形にするかまだ何とも言えないところ

;=====================================================================
;회화系反応 COM0番台
;=====================================================================
;・主にACT분류:회화系/휴식系の際に発生する
;・発生はトリガー이벤트製。特定のACTがトリガーとなる
;・ACT분류:회화についてはREACTで完結시킨다のが望ましい
;-------------------------------------------------

;────────────────────────────────────
;COM0, 침묵한다
;────────────────────────────────────
;・従ったことにはならない
;・ACT1,지금 기분을 묻다/자신에게의 마음을 묻다の場合はREACT파생1,"質問に答えない"となり징계・포인트加算
;・REACT인상:悪印象小
;・의존도변화(-1, -1)
;-------------------------------------------------
;・ACTが지금 기분을 묻다/자신에게의 마음을 묻다の場合は派生1,質問に答えない扱い
;・その他の場合は派生0,ただの沈黙
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다
;ACT분류2:도구
;	ACT:
;ACT분류4:수치
;	ACT:수치플레이/야외플레이
;ACT분류6:가학
;	ACT:밧줄/눈가리개/볼개그/매도/삼각목마
;ACT분류7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;--------------------------------------------------
@KOJO_COM_K1_0
;-------------------------------------------------
;・TFLAG:REACT파생(0=ただの沈黙/1=質問に答えない)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;ACT, 지금 기분을 묻다/자신에게의 마음을 묻다の場合に派生する
	;=================================================
	;REACT파생1, 質問に答えない
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「어쩔 수 없네요. 너의 몸에 들어요」
		PRINTFORMW 「솔직하게 가르침%RAND_SPLIT("라고 갖고 싶어요/라고 줘/없음 있어라")%」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は思わず何か言いかけたが、恥ずかしい内容だったので
			;言葉を飲み込み、黙っていた…
			PRINTFORMW 「인 일 생각하고 있다？」
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は不機嫌そうに押し黙っ하며 だった…
			PRINTFORMW 「입다물고 있으면(자), 괴로워져요？」
;OriginalString : 아웃8, "「…ま、いいわ」")
			CALL FADEOUT(8, "「…뭐, 좋아요」")
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は何も言わなかった…
		PRINTFORML 「요컨데 곤란하고 있는 겠지」
		PRINTFORMW 「 나에게 맡기세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は%CALLNAME%の様子を窺うような素振りを見せたが、
		;何も言えずに黙っていた…
		PRINTFORMW 「 나에게는 솔직해질 수 없다？」
		RETURN 1
	ENDIF
	
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;────────────────────────────────────
;COM1, 마음 약하게 응답한다
;────────────────────────────────────
;・REACT분류は派生によって변화
;・ACT("엄하게 위협한다/징계한다고 위협한다/고함치다/밀어 넘어트린다")で一定条件を満たすとREACT파생1,애원に派生
;・REACT인상:好印象小
;・의존도변화(0, +1)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그は弾かれる
;-------------------------------------------------
@KOJO_COM_K1_1
;-------------------------------------------------
;・TFLAG:REACT파생(0=마음 약하게 응답한다/1=애원する)
;-------------------------------------------------

;REACT파생で内容が異なるので派生毎に分けてしまいます
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생1, 애원する(REACT분류:용서를 빌다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は自分の甘えた気持ちを振り払い、
			;弱々しいながらも、もうやめてと言った…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は機嫌を損ねないようになるべく弱々しく、
			;もうこんなことやめてと言った…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は気弱く、もうこんなことやめてと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は気弱く小さい声で、機嫌を窺うように하면서
		;もうこんなことやめてと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「안되어요, 그만두어 주지 않는다♪」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생0, 마음 약하게 응답한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は自分の気持ちに薄々気が付きながらも、
			;気弱に距離を取るような返事を했다…
			PRINTFORMW 「 나에게는 솔직해질 수 없다？」
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は機嫌を損ねないようになるべく弱々しく返事했다…
		PRINTFORMDL %CALLNAME:TARGET%(은)는 곤란한 것처럼 머리를 긁고 있다
		PRINTFORMW 「응…좀 더 솔직해져도 좋아요？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は気弱く返事했다…
		PRINTFORML 「응…그 말씨의 나쁨…」
		PRINTFORMW 「기분이 결정되지 않은 것 같구나？」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は気弱く小さい声で、機嫌を窺うように하면서返事했다…
		PRINTFORMDL %CALLNAME:TARGET%(은)는 곤란한 것처럼 머리를 긁고 있다
		PRINTFORMW 「응…곤란한 원이군요…」
		RETURN 1
	ENDIF
	
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;────────────────────────────────────
;COM2, 무뚝뚝하게 응답한다
;────────────────────────────────────
;・REACT분류:なし
;・従ったことにはならない
;・징계・포인트が加算される
;・REACT인상:悪印象小
;・의존도변화(-2, -2)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그は弾かれる
;--------------------------------------------------
@KOJO_COM_K1_2
;-------------------------------------------------
;・REACT파생なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は思わず何か言いかけたが、恥ずかしい内容だったので
			;何でもないと無愛想に取り繕った…
;OriginalString : 아웃2, "つん")
			CALL FADEOUT(2, "츤")
			PRINTFORMW 개응개응
			PRINTFORMW 에
			PRINTFORMDW %CALLNAME:MASTER%의 고집은 당장 무너뜨려질 것 같다
			PRINTFORMDW 필사적으로 입가를 긴축시키고 견딘다%CALLNAME:MASTER%에 %CALLNAME:TARGET%(은)는 우쭐거렸는지와 같이 입가를 매달아 올렸다
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は精一杯の抵抗を込めて無愛想に応えた…
			PRINTFORMW 「아무것도 아닌 것 않지요」
			PRINTFORMW 「응, 나에게는 솔직해질 수 없다？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は無愛想に\@ IS_NOWACTNAME("밀어 넘어트린다") ? 대답 # 반응 \@했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は芯の強い瞳で%CALLNAME%を見つめ、無愛想に応えた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		;CASE 0の確率は絞る　
		SELECTCASE RAND:5
		;SELECTCASE RAND:2
			CASE 0
			;あんたではなく%CALLNAME:MASTER%呼称で気を引く試み;頻発するのも何なので低確率
				PRINTFORMW 「그런데,%CALLNAME:MASTER%」
				PRINTFORMW 「여기 향해？」
			CASEELSE
				PRINTFORMW 「무정하네요」
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;────────────────────────────────────
;COM3, 상냥하게 응답한다
;────────────────────────────────────
;・REACT분류:적극적으로 한다
;・REACT인상:好印象大
;・의존도변화(+3, +1)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그は弾かれる
;--------------------------------------------------
@KOJO_COM_K1_3
;-------------------------------------------------
;・REACT파생なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 상냥하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;%CALLNAME:MASTER%はうまく機嫌を取ろうと作り笑顔で応えた…
		PRINTFORMW 「표정이 딱딱해요？」
		PRINTFORMDW 에！
		PRINTFORMDW %CALLNAME:TARGET%의 손가락이%CALLNAME:MASTER%의 뺨을 밀어넣는다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は愛想よく\@ IS_NOWACTNAME("밀어 넘어트린다") ? 대답 # 반응 \@했다…
		PRINTFORMW 「너무 애교가 있는 것도, 너답지 않을지도…」
		PRINTFORMW 「은, 사치일까？」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は嬉しそうに%CALLNAME%に微笑みかけ、
		;愛想よく答えた…
		PRINTFORMW 「그 얼굴, 나 이외에 보이면(자)%RAND_SPLIT("허락하지 않아요？/안 되는 것이니까/용납하지 않아요")%」
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;────────────────────────────────────
;COM4, 강하게 응답한다
;────────────────────────────────────
;・REACT분류:なし
;・従ったことにはならない
;・ACT,밀어 넘어트린다の場合はREACT파생1,"밀어넘어짐に屈しない"に派生
;・REACT인상:悪印象小
;・의존도변화(-1, -3)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그は弾かれる
;--------------------------------------------------
@KOJO_COM_K1_4
;-------------------------------------------------
;・TFLAG:REACT파생(0=強気に応答/1=밀어넘어짐に屈しない)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;ACT, 밀어 넘어트린다の場合にのみ派生
	;=================================================
	;REACT파생1, 밀어넘어짐に屈しない
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
		;%CALLNAME:MASTER%はもうやめろと強く言った…
		;一回目は聞き逃しそうな声で
;OriginalString : 아웃4, "「やめないわ」")
		CALL FADEOUT(4, "「그만두지 않아요」")
;OriginalString : 인20, "「やめないわよ」")
		CALL FADEIN(20, "「그만두지 않아요」")
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は思わず何か言いかけたが、恥ずかしい内容だったので
			;%CALLNAME%には関係ないと強く言った…
			PRINTFORMDW %CALLNAME:TARGET%조금액을 얻어맞았다
			PRINTFORMW 「%RAND_SPLIT("화내요？/관계없지 않아요/바보같은 일 말하지 않는거야.")%너의 일이니까, 나의 일이야」
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は強がって応えたが、声は少し震えている…
			PRINTFORMW 「강한척 하는 너도, 사랑스러워요」
			PRINTFORMDW %CALLNAME:TARGET%(은)는,%CALLNAME:MASTER%의 목덜미에 얼굴을 묻어 목덜미를 빨아 왔다
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は強気で返事했다…
		PRINTFORMW 「어머나, 위세가 좋네요？」
		PRINTFORMDW %CALLNAME:TARGET%(은)는 지만 미소짓는다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は%CALLNAME%を睨みつけ、強気で応えた…
		PRINTFORMW %CALLNAME:TARGET%(은)는 곤란한 것처럼 머리를 긁고 있다
		RETURN 1
	ENDIF
	
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;────────────────────────────────────
;COM5, 빈정거린다
;────────────────────────────────────
;・ACT,지금 기분을 묻다/자신에게의 마음을 묻다の場合はREACT분류:거부
;・それ以外の場合はREACT분류:태연하게 한다になる
;・징계・포인트加算
;・REACT인상:悪印象小
;・의존도변화(-3, -1)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:밧줄/눈가리개/볼개그/매도/삼각목마
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그は弾かれる
;--------------------------------------------------
@KOJO_COM_K1_5
;-------------------------------------------------
;・REACT파생なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は솔직な気持ちを입にしようと했다が、
			;何だか恥ずかしくなり、皮肉交じりに応えた…
			PRINTFORMW 「 이제 한숨일까」
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%はちっぽけな悪意を言葉に混ぜてちくりと返했다…
			;言いは했다ものの、機嫌を損ねていないかしきりに気にしている…
			PRINTFORML 「너답지 않네요. 뭐 좋아요」
			PRINTFORMW 「그 정도라면 허락해 준다」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は皮肉を言いつつも、言うことには솔직に従って見せた…
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は余裕を見せるような態度で飄々と従いつつ、
		;皮肉交じりに応答했다…
		PRINTFORMW 「네네」
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「아직도 여유가 있을 듯 하구나？」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;────────────────────────────────────
;COM6, 더 하드하게 해
;────────────────────────────────────
;・REACT분류:受け入れる
;・죠교자の素質、BASE:초조함等によってREACT파생1,「この恥知らずのメス豚め！」になる
;・REACT파생1の場合は징계・포인트加算
;・REACT인상:0から遠ざかる
;・의존도변화(0, +3)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/상냥하게 위로한다
;		징계한다고 위협한다
;ACT분류:1:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/키스/성기마찰한다/파이즈리한다
;ACT분류:2:도구
;	ACT:Ｃ애무도구/유두캡
;ACT분류:3:성교
;	ACT:정상위
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:스펭킹/밧줄/눈가리개/볼개그/삼각목마
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그は弾かれる
;--------------------------------------------------
@KOJO_COM_K1_6
;-------------------------------------------------
;・TFLAG:REACT파생(0=ふーん/1=この恥知らずのメス豚め！)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생1, 「この恥知らずのメス豚め！」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;快SOURCEが一定以上
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && SOURCE:MASTER:쾌Ｃ + SOURCE:MASTER:쾌Ｖ + SOURCE:MASTER:쾌Ａ + SOURCE:MASTER:쾌Ｂ > 10
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は恥ずかしい気持ちを押さえながら、もっとしてと言った…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は半ば自棄になり、もっと하드にすればいいと言った…
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%はもっとすごいの당했다いと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は快感に溺れ、もっとすごいことしてと言った…
			PRINTFORMW 「 나는, 개만들기 밖에 흥미없어요」
			PRINTFORMDW 활짝 개이는 쾌청의 하늘과 같이 실로 시원한 웃는 얼굴로, 그렇게 돌려주어졌다
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「아프지 않은 것일까…그렇지 않으면, 아픈 것이 좋은 것일까？」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;快SOURCEが一定以上のネストを開放しない場合は、状況に関わらずこの箇所が補完します
;━━━━━━━━━━━━━━━━━━━━━━━━
;その他の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%はいろいろな気持ちを整理하면서言葉を選び、
			;%CALLNAME%にされるならもっと하드でもいいと言った…
			PRINTFORMW 「이상한 신경을 쓰지 마. 나는 소프트인 것이 좋아해」
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は自暴自棄になり、もっと하드にすればいいと言った…
			;言いは했다ものの、唇をぴっちり閉じて肩を震わせている…
			PRINTFORMW 「등지지 말아…그런데, 기분이 나쁜 것을 바꾸는 것이라고？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は흥미半分で、더 하드하게 해もいいと言ってみた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%はもっと気持ちのいいことをして欲しくなり、
		;思わず더 하드하게 해と言ってしまった…
		PRINTFORMW 「 나는, 개만들기 밖에 흥미없어요」
		PRINTFORMDW 활짝 개이는 쾌청의 하늘과 같이 실로 시원한 웃는 얼굴로, 그렇게 돌려주어졌다
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「 좀 더 격렬해서, 아프지 않은 것일까？그렇지 않으면 아픈 것이 좋은 것일까？」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;────────────────────────────────────
;COM7, 용서를 빌다
;────────────────────────────────────
;・REACT분류:용서를 빌다。派生했다場合はREACT분류:なし
;・죠교자の素質、BASE:이성、아라이멘도によってREACT파생1,「意見を出すなんていい度가슴ね！」になる
;・턴방침が이상の場合は派生1になりやすい
;・派生1の場合は징계・포인트加算
;・REACT인상:0に近づく
;・의존도변화(0, +2)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다
;ACT분류:1:애무
;	ACT:항문애무/항문펠라/밀어 넘어트린다
;ACT분류:2:도구
;	ACT:항문바이브/항문비즈/페니스밴드
;ACT분류:3:성교
;	ACT:후배위/배면좌위/항문섹스
;ACT분류:4:수치
;	ACT:자위/비패개장/수치플레이/야외플레이/방뇨
;ACT분류:5:봉사
;	ACT:발핥기강제
;ACT분류:6:가학
;	ACT:스펭킹/채찍/바늘/삼각목마/관장세트
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착/피스트퍽/애널피스트/양쪽피스트/公器
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:역강간
;
;TEQUIP:볼개그は弾かれる
;--------------------------------------------------
@KOJO_COM_K1_7
;-------------------------------------------------
;・TFLAG:REACT파생(0=ふーん/1=意見を出すなんていい度가슴ね！)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;ACT, 公器では派生しない
	;=================================================
	;REACT파생1, 意見を出すなんていい度가슴ね！
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は自分がおかしくなりかけているのが怖くなり、
			;%CALLNAME%にもう許してと懇願했다…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は媚びるように%CALLNAME%を見上げると、もう許してと願った…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%はもう許してと言い出했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は涙を浮かべて%CALLNAME%にもう許してくださいと懇願했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「허락하면 좋겠다？%RAND_SPLIT("그렇다면 어떻게 하면 좋은가, 아네요？/응후후…어떻게 하지？")%」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;────────────────────────────────────
;COM8, 기분좋게 해
;────────────────────────────────────
;・REACT분류:受け入れる
;・죠교자の素質、BASE:이성、아라이멘도によってREACT파생1,「この恥知らずのメス豚め！」になる
;・派生1の場合は징계・포인트加算
;・REACT인상:0から遠ざかる
;・의존도변화(0, +2)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다/상냥하게 위로한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:항문애무/항문펠라/밀어 넘어트린다
;ACT분류:2:도구
;	ACT:페니스밴드
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시/수치플레이/야외플레이/방뇨
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;
;TEQUIP:볼개그は弾かれる
;-------------------------------------------------
@KOJO_COM_K1_8
;-------------------------------------------------
;・TFLAG:REACT파생(0=ふーん/1=この恥知らずのメス豚め！)
;-------------------------------------------------

;REACT파생0は事実上COM名称そのままに解釈してもらえればいいかと
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생1, この恥知らずのメス豚め！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;快SOURCEが一定以上
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && SOURCE:MASTER:쾌Ｃ + SOURCE:MASTER:쾌Ｖ + SOURCE:MASTER:쾌Ａ + SOURCE:MASTER:쾌Ｂ > 10
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は恥ずかしい気持ちを押さえながら、もっとしてと言った…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は痛め付けられるよりは良いと思い、このまましてと言った…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%はもっと기분좋게 해と言い続けた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は快感に溺れ、もっと良くしてとねだった…
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;快SOURCEが一定以上のネストを開放しない場合は、状況に関わらずこの箇所が補完します
;本体側の処理が分けられているので、テンプレ側も一応分けてみま했다が
;現時点では内容にあまり差異がないので、この箇所だけでもいい気はします
;ただしこのバリアント、地の文が一気に増えたり変わったりするのでなんともいえないところ
;━━━━━━━━━━━━━━━━━━━━━━━━
;その他の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は恥ずかしい気持ちを押さえながら、욕망に正直に
			;もっと気持ち良いことを당했다いと言った…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は痛め付けられるよりは良いと思い、
			;どうせなら気持ち良くなりたいと言った…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は기분좋게 해と言い出했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は정욕の虜되었다ように、もっと気持ち良いことしてと訴えた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「어떻게 하지？응후후…」
			CASEELSE
				PRINTFORMW 「솔직하게 말해 주는 것은, 기뻐요」
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;=====================================================================
;쾌락系反応
;=====================================================================
;・快SOURCEや特定のPALAMがトリガーとなって発生する
;・COMによってはACT분류がトリガーとなる
;-------------------------------------------------

;──────────────────────────────────
;COM10, 부끄러워한다
;──────────────────────────────────
;・REACT분류:소극적으로 한다
;・징계・포인트加算
;・快SOURCEが一定以上の場合は派生1,快感に부끄러워한다になる
;・의존도변화(+1, 0)
;-------------------------------------------------
;선택可能な条件…
;ACT분류:회화
;	ACT:성에 관한 이야기를 하다/의상 변경을 요구한다
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:수치
;	ACT:자위/비패개장/자위과시/수치플레이/야외플레이/방뇨
;ACT분류:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/가슴애무강제
;ACT분류:이상
;	ACT:방치플레이
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;※SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ, PALAM:치정がトリガーとなる
;-------------------------------------------------
@KOJO_COM_K1_10
;-------------------------------------------------
;・TFLAG:REACT파생(0=状況に부끄러워한다/1=快感に부끄러워한다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생1, 快感に対して부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%はもっと気持ち良くなりたいと思っている自分を意識してしまい、
			;頬を染めて恥ずかしがった…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は快感を与えられることに強い수치を感じ、
			;身を縮こまらせて恥ずかしがった…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%はこみ上げる快感を隠せず、恥ずかしがって顔を背けた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は快感で声を漏らし、はっと했다ように息を呑むと
		;視線を逸らして恥ずかしがった…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;REACT파생1,快感に부끄러워한다を書いた場合はREACT파생0,状況に부끄러워한다扱い
;派生1を書かなかった場合は派生に関わらずこの箇所を補完します
;━━━━━━━━━━━━━━━━━━━━━━━━
;その他の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;以下に示す地の文はREACT파생0のもの
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は恥ずかしい姿を見られることで変な気持ちになってしまい、
			;余計に頬を染めて恥ずかしがった…
			;PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は自分を辱める視線に堪えられなくなり、
			;身を抱くようにして恥ずかしがった…
			;PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(50) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は辱められている状況に堪えられず、수치のあまり顔を背けた…
		PRINTFORMW 「여기 향해？」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(50) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は%CALLNAME%の舐めるような視線を感じ、
		;思わず視線を逸らして恥ずかしがった…
		PRINTFORMW 「초심인 너도 사랑스러워요」
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「부끄러워하는 너도 나쁘지 않아요」
		SIF IS_CONDITION(TARGET, "싫증")
			PRINTFORMW 「 그렇지만 그렇구나. 좀 더 솔직해져 주면(자), 나는 기뻐요」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM11, 싫어한다
;──────────────────────────────────
;・REACT분류:소극적으로 한다
;・REACT파생1はACT妨害となりREACT분류:날뛴다扱い
;・의존도변화(-1, -1)
;-------------------------------------------------
;선택可能な条件…
;ACT분류:회화
;	ACT:의상 변경을 요구한다
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/발コキ
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;
;※SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ, PALAM:치정がトリガーとなる
;-------------------------------------------------
@KOJO_COM_K1_11
;-------------------------------------------------
;・TFLAG:REACT파생(0=싫어한다/1=嫌がってACTを妨害)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생1, 嫌がって妨害する(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%はとっさに嫌がって손を出してしまったものの、
			;%CALLNAME%に申し訳なさそうな表情を見せた…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は精一杯の기력を振り絞り、嫌がって%CALLNAME%から逃れた…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は嫌がって身をよじり、%CALLNAME%の邪魔を했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は嫌がって%CALLNAME%を押しのけた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;派生1を書いた場合はREACT파생0, 싫어한다がACTは妨害せず扱い
;書かなかった場合は派生に関わらずこの箇所を補完します
;━━━━━━━━━━━━━━━━━━━━━━━━
;その他
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は内心ではそう満更でもないと思いつつも、
			;恥ずかしいので嫌がってみせた…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は精一杯の기력を振り絞って、震える声で嫌だと言った…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は首を振って嫌がった…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は%CALLNAME%にそんなの嫌だと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「혐이라고 해도 그만두어 줄 수 없어요」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM12, 서투르다고 모욕한다
;──────────────────────────────────
;・REACT분류:태연하게 한다
;・REACT파생1(強がる)はREACT분류:소극적으로 한다扱い
;・징계・포인트加算
;・派生0, こきおろす(REACT인상:悪印象大)
;・派生1, 強がる(REACT인상:悪印象小)
;・의존도변화(-2, -3)
;-------------------------------------------------
;선택可能な条件…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:성교
;	ACT:정상위/후배위/대면좌위/배면좌위
;
;※TFLAG:ACT분류(1=애무/3=성교)がトリガー
;-------------------------------------------------
@KOJO_COM_K1_12
;-------------------------------------------------
;・TFLAG:REACT파생(0=こきおろす/1=強がる)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생1, 強がって(快感を感じながら)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;%CALLNAME:MASTER%は喘ぎ声交じりの見え見えの虚勢で、%CALLNAME%に下손だと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;着実に快感を感じながらも%CALLNAME:MASTER%は下손だと言い張った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は快感に顔を歪めつつも、こんなのじゃ感じないと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;派生1を書いた場合はREACT파생0, こきおろす(実際に下손な場合)扱い
;書かなかった場合は派生に関わらずこの箇所を補完します
;━━━━━━━━━━━━━━━━━━━━━━━━
;その他の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;%CALLNAME:MASTER%は精一杯の強がりで、%CALLNAME%に下손だと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:TARGET%의 未熟なテクニックに、%CALLNAME:MASTER%は下손だと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%はこれなら自分の方が上손いと言った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「서투른 일도 그 안에, 그리워지는 날이 오는 것」
				PRINTFORMW 「지금은, 지금의 나를 즐겨 주세요」
				;書いてて気づいたブーメラン。気づいた%CALLNAME:TARGET%さん。自分で自分を笑う
;OriginalString : 아웃4, "くすっ")
				CALL FADEOUT(4, "키득")
				PRINTFORMW 「 나도 그렇게 해요」
			CASEELSE
				PRINTFORMW 「지금부터 능숙해지기 때문에 좋아」
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM13, 겁없이 웃는다
;──────────────────────────────────
;・REACT분류:태연하게 한다
;・징계・포인트加算
;・REACT인상:悪印象小
;・의존도변화(-2, -3)
;-------------------------------------------------
;・トリガーとなる행동、ソース、パラメーター
;・SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ, SOURCE:고통/구속, ACT분류:가학
;-------------------------------------------------
;선택可能な条件…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/매도/삼각목마/관장세트
;-------------------------------------------------
@KOJO_COM_K1_13
;-------------------------------------------------
;・REACT파생なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;PRINTFORML %CALLNAME:MASTER%は自分はまだ正気だと確かめるように笑って見せた…
			;ニュアンスを取り違えている予感がするのでコメントアウト
			PRINTFORMW 「웃을 수 밖에 없을까？」
			PRINTFORMW 「그런 때는 좀 더, 생긋 웃으세요」
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は貼りついたような笑みを返했다が、目が泳いでいる…
			PRINTFORMW 「여기 보세요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%はわざと%CALLNAME:TARGET%を挑発するように笑った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は%CALLNAME%が죠교に苦心しているのを嘲笑った…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		;아라이멘도と플레이ヤーの想像に頼り、필요以上に描写しない方が良さそう
		PRINTFORMDW %CALLNAME:TARGET%(은)는 미소지어 돌려주었다
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM14, 쾌감을 참는다
;──────────────────────────────────
;・REACT분류:소극적으로 한다
;・一定条件でREACT파생1「애원(REACT분류:용서를 빌다)」扱い
;・派生1の場合REACT인상は0に近づく
;・의존도변화(+1, -1)
;-------------------------------------------------
;※トリガーとなる행동、ソース、パラメーター
;・SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ
;-------------------------------------------------
;선택可能な条件…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/스마타
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트
;-------------------------------------------------
@KOJO_COM_K1_14
;-------------------------------------------------
;・TFLAG:REACT파생(0=耐久/1=애원)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생1, 애원する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	;高消費箇所に이루는대로になってしまうような言動を配置するのはどうだろう…
	IF 구상記入チェック(50) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は快感で正気を失うことに怯え、もう許してと言った…
			PRINTFORMW 「무리해―」
			PRINTFORMW 「이상하게 되어 버려서 좋아요. 내가 받아 들여 주어요」
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は快感を受け入れようとせず、やめてと懇願했다
			PRINTFORMW 「그만두었으면 좋은거야？」
;OriginalString : 인4, "「私、」")
			CALL FADEIN(4, "「나,」")
;OriginalString : 아웃4, "「私は」")
			CALL FADEOUT(4, "「나는」")
;OriginalString : 인4, "「やめたくないわ」")
			CALL FADEIN(4, "「그만두고 싶지 않아요」")
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(50) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は強い快感を참다하면서、もうこんなことやめてと懇願했다…
		PRINTFORMW 「싫어요, 그만두어는 주지 않아요 원」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(50) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は快感を참다하면서許しを願っている…
		PRINTFORMW 「자연스럽게 인내 할 수 있어버린다 것도, 어쩐지 화가 나요…」
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;派生1を書いた場合はREACT파생0, 快感を참다扱い
;書かなかった場合は派生に関わらずこの箇所を補完します
;━━━━━━━━━━━━━━━━━━━━━━━━
;その他の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(50) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は욕망を振り払い、快感を堪えようと했다…
			PRINTFORMW 「꽉 눈을 감고는%RAND_SPLIT("괴롭혀 신경쓰고 있다/하아하아 하고 있다")%너 꽤 그림이 되어있는 원이야」
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は快感を受け入れようとせず참다している…
			PRINTFORMW 「이래서야 혐？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は必死になって快感を참다했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は明らかな快感を覚えながらも、それを참다しようとしている…
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「참아도 쓸데없어요？네가 %RAND_SPLIT("항복해 줄 때/공략될 때")%까지 계속하는 것」
		PRINTFORMW 「이변은 내가 %RAND_SPLIT("해결할 때/이길 때")%까지 계속되어」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM15, 쾌감을 받아들인다
;──────────────────────────────────
;・COM16, 기분 좋다!とほぼ同じ。差異は의존도の변화のみ
;・죠교대상が依存しやすい
;・REACT분류:受け入れる
;・REACT인상:好印象大
;・의존도변화(+1, +5)
;-------------------------------------------------
;※トリガーとなる행동、ソース、パラメーター
;・SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ
;-------------------------------------------------
;선택可能な条件…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/스마타
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K1_15
;-------------------------------------------------
;・REACT파생なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;快感に방심してしまうと、%CALLNAME:MASTER%의 心は見る見るうちに蕩け始めた
		PRINTFORMW 「무리하고 있는 원이군요. 알아요 너의 일인걸」
		PRINTFORMW 「거기까지 하지 않아도 괜찮은 것이지만, 뭐 좋아요」
		;전회ACT追加描写
		IF 구상記入チェック(100) && IS_NOWACTNAME("정상위시킨다/대면좌위시킨다")
;OriginalString : 아웃4, "よしよし")
			CALL FADEOUT(4, "좋아 좋아")
			PRINTFORMW 「 나를 위해 무리했어요」
;OriginalString : 아웃4, "ぽんぽん")
			CALL FADEOUT(4, "팡팡")
			PRINTFORMW 「노력한 원이군요」
			PRINTFORMDW %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 키를 쓰다듬고 있고 주었다
		ENDIF
		IF 구상記入チェック(100) && IS_NOWACTNAME("키스")
		;望まない幸福感に心が満たされるのを感じ、目に涙を浮かべた
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は甘い嬌声をあげながら快感を受け入れた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;本音は低確率で洩らす
		SIF !RAND:3
			PRINTFORMW 「분명하게, 나의 일도 생각하세요？」
		PRINTFORMW 「너만 기분 좋아지는 것은, 안되니까」
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「좋을 것 같은 소리 내 버려서♪군침 나오고 있는 원이야？이제…%RAND_SPLIT("어쩔 수 없네요/한심하네요/안되구나")%」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM16, 기분 좋다!
;──────────────────────────────────
;・COM15, 쾌감을 받아들인다とほぼ同じ。差異は의존도の변화のみ
;・주인님の依存を誘いやすい
;・REACT분류:受け入れる
;・REACT인상:好印象大
;・의존도변화(+4, +1)
;-------------------------------------------------
;※トリガーとなる행동、ソース、パラメーター
;・SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ
;-------------------------------------------------
;선택可能な条件…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스/성기마찰한다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/스마타
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K1_16
;-------------------------------------------------
;・REACT파생なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;双方정욕성교봉사
	;=================================================
	IF 구상記入チェック(50) && V_SEX(MASTER) && IS_CONDITION(MASTER, "정욕") && IS_CONDITION(TARGET, "정욕")
		PRINTFORM 「도 취할 수 있고-의 보지로 자지 껴 해, 좀 더 기분 좋아져
		CALL HEARTBW
		PRINTFORMDW %CALLNAME:TARGET%(은)는 군침을 흘리면서 필사적으로 허리를 털고 있다
		RETURN 1
	ENDIF
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;%CALLNAME:MASTER%は媚びるように気持ち良さそうな声で喘いでいる…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は쾌락を得られる幸福を感謝するかのように声に出했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は甘い声で快感と悦びを%CALLNAME%に伝えた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		[SKIPSTART]
		;描写加筆につきコメントアウト
		PRINTFORMW 「응…계속해요？」
		[SKIPEND]
		SELECTCASE RAND:3
			CASE 0
;OriginalString : 아웃4, "「ん…」")
				CALL FADEOUT(4, "「응…」")
			CASE 1
;OriginalString : 아웃8, "（気持ちいいんだ…）")
				CALL FADEOUT(8, "（기분이 좋다…）")
			CASEELSE
;OriginalString : 아웃4, "「えへへ…」")
				CALL FADEOUT(4, "「에헤헤…」")
		ENDSELECT
		PRINTFORMW 「계속해요？」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;=====================================================================
;성교系反応 COM20番台
;=====================================================================
;・ACT분류:성교/성봉, ACT:파이즈리한다/성기마찰한다等がトリガーとなって発生
;-------------------------------------------------

;──────────────────────────────────
;COM20, 허리를 흔든다
;──────────────────────────────────
;・主導権(SEX_INITIATIVE)によって内容が異なるCOM
;-------------------------------------------------
;・ACT분류が성교봉사、ACT:기승위の場合は主導権は죠교대상(SEX_INITIATIVE(MASTER))
;・REACT분류:적극적으로 한다
;・의존도변화(+5, +3)
;-------------------------------------------------
;・ACT분류が성교、ACT:역강간/성기마찰한다/파이즈리한다の場合は主導権は죠교자
;・※성기마찰한다/파이즈리한다はSEX_INITIATIVE(TARGET)では参照できない
;・REACT분류:受け入れる
;・의존도변화(+4, +8)
;-------------------------------------------------
;・REACT인상:好印象大
;-------------------------------------------------
;・애태움中は実行不可
;・ACT:파이즈리한다の場合、죠교자が빈유だと弾かれる
;-------------------------------------------------
;선택可能な条件…
;ACT분류:애무
;	ACT:성기마찰한다/파이즈리한다
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K1_20
;-------------------------------------------------
;・REACT파생なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;主導権は죠교대상(ACT분류が성교봉사/ACT:기승위の場合)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && SEX_INITIATIVE(MASTER)
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;%CALLNAME%の歓心を得るため、%CALLNAME:MASTER%は精一杯腰を使った…
		PRINTFORMW 「노력해 봉사해 주는 것은 기뻐요」
		PRINTFORMW 「그런데？너도 기분 좋아져 주지 않으면…」
;OriginalString : 인16, "「駄目なんだから」")
		CALL FADEIN(16, "「안 되는 것이니까」")
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동/自然な행동と統合
	;=================================================
	;自然な행동と統合
	;IF 구상記入チェック(100) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は%CALLNAME%を悦ばせようと腰を使っている…
	IF 구상記入チェック(100) && GROUPMATCH(GET_COM_RANK(), "自然な행동", "無理のない행동")
		;=================================================
		;双方정욕 気持ちいい！레이무さん
		;=================================================
		IF IS_CONDITION(MASTER, "정욕") && IS_CONDITION(TARGET, "정욕")
			PRINTFORM 「개-기분 좋다…개-기분 있어라…
			CALL HEARTBW
			;状況は삽입중につき「ここ」は不適切。레이무が竿役の場合もここになる模様。
			;PRINTFORM 「れーむのここ…もっと%RAND_SPLIT("ずぽずぽ/ぐりぐり/ずぽずぽ/ぬぷぬぷ")%してぇ…
			PRINTFORM 「-의 저기. 좀 더,%RAND_SPLIT("쑤컹쑤컹/동글동글/쑤컹쑤컹/철퍽철퍽")%해
			CALL HEARTBW
		;=================================================
		;당신が정욕 強気に返答する레이무さん
		;=================================================
		ELSEIF IS_CONDITION(MASTER, "정욕")
			PRINTFORMW 「%RAND_SPLIT("이！/본궤도에 올라아/번창해 버려")%…좀 더 상냥하고…해 주세요…」
			PRINTFORMW 「(듣)묻지 않네요…%RAND_SPLIT("아 이제(벌써)/이 바보")%」
			PRINTFORMW 「너로 되는 대로라고 하는 것도, 어쩐지%RAND_SPLIT("적이군요/마음에 들지 않아요/나답지 않을까요")%…」
		;=================================================
		;%CALLNAME:TARGET%정욕 레이무さん受け気持ちよくして
		;=================================================
		ELSEIF IS_CONDITION(TARGET, "정욕")
					PRINTFORM 「응. 너의 좋아하게 움직여도 좋으니까
					CALL HEARTBW
					PRINTFORMW 「%RAND_SPLIT("/%CALLNAME:TARGET%의 보지")%%RAND_SPLIT("/おちんちんで")%기분 좋게 하며」
		;=================================================
		;それ以外
		;=================================================
		ELSE
			SELECTCASE RAND:2
				;%CALLNAME:TARGET%さんが気持ちよくして
				[SKIPSTART]
				;記入箇所ミスの予感。コメントアウト
				CASE 0
					PRINTFORM 「응…너의 좋아하게 움직여도 좋으니까」
					PRINTFORMW 「기분 좋게 해
					CALL HEARTBW
				[SKIPEND]
				CASE 1
					[SKIPSTART]
					;右クリックでは表示されっぱなしになるので思惑が外れる。逆。コメントアウト
					;右クリックして二行目を見逃す。右クリックして見る。二度楽しむ。
					;テンポの調整
					;CALL FADEIN(20, "「もっと、もっと！」")
					;CALL FADEOUT(20, @"「%CALLNAME:TARGET%で！気持ち良くなって！」")
					[SKIPEND]
;OriginalString : 아웃8, "「もっと、もっと動いて！」")
					CALL FADEOUT(8, "「좀 더, 좀 더 움직여！」")
;OriginalString : 인8, @"「%CALLNAME:TARGET%で！気持ち良くなって！」")
					CALL FADEIN(8, @"「%CALLNAME:TARGET%그래서！기분 좋아져！」")
				;빈정거린다레이무さん
				CASEELSE
					PRINTFORMW 「완전히 번창해 버려서…♪」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は허리기술で相손を感じ시킨다ことに喜びを見出している…
		PRINTFORMW 「필사적으로 허리 털어 버려서…응후후」
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;SEX_INITIATIVE(MASTER)を書かなかった場合はここで補完されますが範囲が広いので
;最低限、TEQUIP:성교중/성교봉사중、IS_COMGRONAME/IS_NOWACTNAMEなりで書き分けないと整合性は崩れる事になるかと
;━━━━━━━━━━━━━━━━━━━━━━━━
;その他の場合(ACT분류が성교/ACT:역강간/성기마찰한다/파이즈리한다の場合)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;%CALLNAME:MASTER%が腰をいくら振っても、自分の快感が強くなるばかりだった…
		PRINTFORM 「응후후…이봐요 힘내라 힘내라
		CALL HEARTBW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME%の身体に溺れるように、%CALLNAME:MASTER%의 腰は勝손に動いて快感を貪る…
		PRINTFORMW 「 이제%RAND_SPLIT("조금/조금")%%RAND_SPLIT("優しく/ゆっくり")%」
		PRINTFORMW 「허리, 그치지 않지 있고？완전히…어쩔 수 없기 때문에」
		PRINTFORMW 「…1회 말해버리면 침착하는 것일까？」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は自分が気持ちいいようにかくかくと腰を振っている…
		PRINTFORMW 「열중해 버려서」
		PRINTFORM 「%RAND_SPLIT("우후후/에헤헤/…응후후")%
		CALL HEARTBW
		PRINTFORMW 「 나의 일도 기분 좋게 해 주세요？」
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「함께 기분 좋아집시다
		CALL HEARTBW
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM21, 안에 내!
;──────────────────────────────────
;・REACT분류:受け入れる
;・REACT인상:好印象大
;・죠교대상が남자の場合は弾かれる
;・의존도변화(+10, +10)
;-------------------------------------------------
;선택可能な条件…
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;-------------------------------------------------
@KOJO_COM_K1_21
;-------------------------------------------------
;・TFLAG:REACT파생(0=Ｖに出して/1=Ａに出して)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에 내!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;%CALLNAME:MASTER%は%CALLNAME%に犯され、쾌락で心を乱されながら
		;捨て鉢되었다ように、もう中でいい、中に出してと言った…
		PRINTFORMW 「좋으면 사양은 하지 않아요」
		PRINTFORMW 「응…욕구를 말하면 좀 더,%RAND_SPLIT("마음속으로부터/기쁜 듯한")%의를 (듣)묻고 싶어요」
		RETURN 1
	ENDIF
	
	;정상위、대면좌위の場合は地の文が변화
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理のない행동"
		;정상위/대면좌위の場合	%CALLNAME:MASTER%は%CALLNAME:TARGET%의 腰を両발でがっちりと抱え、中に出してとささやいた…
		;その他の場合			%CALLNAME:MASTER%は貫かれる快感に溺れて、안에 내!と叫んだ…
		PRINTFORMDW %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%(을)를 꼭 껴안으면(자)%RAND_SPLIT("허리의 움직임을 빨리 했다/%CALLNAME:MASTER%의 자궁에 첨단을 꽉 눌렀다/기쁜 듯한 표정을했다")%」
		;リターン無しでその他・条件なし箇所につなげる
		;RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は%CALLNAME%に犯される快感と정욕に支配され、
		;夢心地の表情でどうなってもいい、中に欲しいと叫んだ…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		SELECTCASE RAND:3
			CASE 0
				PRINTFORM 「 나의 아기. 갖고 싶은거야？응후후
				CALL HEARTBW
			CASE 1
				PRINTFORM 「
				CALL HEARTBW
				;PRINTFORMW 」
			CASEELSE
				PRINTFORMW 「아 이제！너무 좋아！」
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM22, 안에 내면 안돼!
;──────────────────────────────────
;・REACT분류:용서를 빌다
;・REACT파생1(中に出す)の場合は징계・포인트追加。REACT분류:なし
;・REACT인상:悪印象小
;・의존도변화(0, -10)
;・참다플래그は消滅する
;・죠교대상が남자の場合は弾かれる
;-------------------------------------------------
;선택可能な条件…
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;-------------------------------------------------
@KOJO_COM_K1_22
;-------------------------------------------------
;・TFLAG:REACT파생(0=中に出さない/1=中に出す)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 안에 내면 안돼!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;v1.010現在、派生しても地の文は共通
	;=================================================
	;REACT파생1, 中に出す
	;=================================================
	IF 구상記入チェック(50) && TFLAG:REACT파생 == 1
		PRINTFORMDW %CALLNAME:TARGET%(은)는 최안쪽까지 찔러 붐비면서 허리를 끊었다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は恥も外聞も無く%CALLNAME%を求めたい感情を懸命に振り払い、
			;中だけはだめ、外に出してと叫んだ…
			PRINTFORMW 「이상한 곳에서 참아 버려서…안심하며」
			PRINTFORM 「책임을 져 준다
			CALL HEARTBW
		ELSE
			;%CALLNAME:MASTER%は%CALLNAME%の분노を買うことを恐れつつも、
			;種付けされることを恐れ、中はやめて、出しちゃだめと悲痛に叫んだ…
			SELECTCASE RAND:3
				CASE 0
					PRINTFORM 「%RAND_SPLIT("당황해 버려/허둥지둥 해 버려")%, 사랑스럽기 때문에
					CALL HEARTBW
				CASE 1
					;男%CALLNAME:MASTER%의 お尻も加味し、질내表記を避ける
					PRINTFORM 「안에 내지 않으면 할 수 없지 않다
					CALL HEARTBW
				CASEELSE
					PRINTFORM 「좋으니까 잉 보세요
					CALL HEARTBW
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は貫かれる快感を耐えながら、안에 내면 안돼!と叫んだ…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は力なく%CALLNAME%に犯され、ぐったりと하면서も
		;中だけはやめて、許してと애원했다…
		PRINTFORMW 「바보같구나. 여기서 그만둔다면 이런 시무라고 없어요」
		PRINTFORMW 「아무래도 갖고 싶어」
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「바보같구나. 여기서 그만둔다면 이런 시무라고 없어요」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;=====================================================================
;고통系反応
;=====================================================================
;・가학系행동や삽입を伴う各種행동で見られる
;・v1.010現在、고통系に의존도변화の処理なし
;-------------------------------------------------

;──────────────────────────────────
;COM30, 고통을 참는다
;──────────────────────────────────
;・REACT분류:소극적으로 한다
;・REACT인상:好印象小
;・SOURCE:고통が無い場合は弾かれる
;-------------------------------------------------
;・トリガーとなる행동、ソース、パラメーター
;・SOURCE:고통/구속
;-------------------------------------------------
;선택可能な条件…
;ACT분류:애무
;	ACT:가슴애무/입으로 애무/항문애무/항문펠라/키스/파이즈리한다
;ACT분류:도구
;	ACT:바이브/항문바이브/항문비즈
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트
;-------------------------------------------------
@KOJO_COM_K1_30
;-------------------------------------------------
;・TFLAG:REACT파생(0=고통を참다/1=痛いけど기분 좋다!)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30_1, 派生：痛いけど기분 좋다!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は様々な感情を飲み込み、快感と고통を同時にこらえた…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は고통の中に快感を意識し、怯えながら両方を참다했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;快感と고통が同時に襲い、%CALLNAME:MASTER%はくぐもった声내질렀다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は고통を紛らわすため、同時にこみ上げる快感にすがりついた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;その他の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;派生1を開放했다場合は派生0, 고통を참다扱い
;------------------------------------------------
IF 구상記入チェック(100)
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は고통を堪えながら、%CALLNAME%の行為を受け入れた…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は苦悶の신음を漏ら하면서고통を참다했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は고통を참다했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ACT:이라마치오時には地の文が변화。内容は以下の通り
	;%CALLNAME:MASTER%は喉奥まで抉られる苦しみに嗚咽を漏ら하면서も、%CALLNAME%の行為を受け入れた
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;その他ACTの場合の地の文
		;%CALLNAME:MASTER%は歯を食い縛って고통をこらえた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「좋으니까 그대로 굴복해주세요」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM31, 비명지른다
;──────────────────────────────────
;・REACT분류:소극적으로 한다
;・REACT인상:0から遠ざかる
;-------------------------------------------------
;선택可能な条件…
;ACT분류:회화
;	ACT:엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다
;ACT분류:애무
;	ACT:가슴애무/입으로 애무/항문애무/항문펠라/키스/파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:바이브/항문바이브/항문비즈
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/매도/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트/公器
;-------------------------------------------------
@KOJO_COM_K1_31
;-------------------------------------------------
;・TFLAG:REACT파생(0=고통による悲鳴/1=공포による悲鳴)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생1, 공포による悲鳴
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は媚びるような悲鳴を上げた…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は怯えたように悲鳴を上げてみせた…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は공포を感じて悲鳴を上げた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は異様な공포を感じ、悲鳴を上げた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;REACT파생1を書いた場合はREACT파생0, 고통による悲鳴扱い
;書かなかった場合は派生に関わらずこの箇所を補完します
;━━━━━━━━━━━━━━━━━━━━━━━━
;その他の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は%CALLNAME%の好む고통の悲鳴を自然に声に出했다…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は悲鳴を上げて고통に悶えてみせた…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は고통を耐えられずに悲鳴を上げた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は고통のあまり悲痛な叫び声を上げた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「아 이제, 차이원이군요」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;────────────────────────────────────
;COM32, 날뛴다
;────────────────────────────────────
;・派生によって暴れ成功(REACT분류:날뛴다)、暴れ失敗(REACT분류:소극적으로 한다)に分かれる
;・暴れが通ると도구系/성교系/성교봉사系コマンドを妨害했다扱いになる
;・징계・포인트追加
;・REACT인상:悪印象大
;-------------------------------------------------
;선택可能な条件…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스
;		성기마찰한다/파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그
;ACT분류:휴식
;	ACT:치료한다
;ACT분류:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_32
;-------------------------------------------------
;・TFLAG:REACT파생(0=暴れ成功/1=밧줄구속による暴れ失敗/2=制止による暴れ失敗)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;REACT파생2, 制止による暴れ失敗(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 2
		;%CALLNAME:MASTER%は暴れて抵抗しようと했다が、%CALLNAME:TARGET%に抑えられた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생1, 밧줄구속による暴れ失敗(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		;縛られた%CALLNAME:MASTER%は暴れて抵抗しようと했다が、밧줄がさらに食い込んでただ痛いだけだ…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1と2を書いた場合は派生0, 暴れ成功(REACT분류:날뛴다)扱い
	;書かなかった場合は派生に関わらずこの箇所を補完します
	;※派生毎の内容の差異がわりと大きいので、書き分けるのが無難であると考えます
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		;%CALLNAME:MASTER%は暴れて%CALLNAME:TARGET%의 행동を妨害했다。%CALLNAME:TARGET%はかなり怒っているようだ…
		PRINTFORMW 「얌전하게 하지 않으면, 얌전하게 시켜요？」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM33, 무서워한다
;──────────────────────────────────
;・REACT분류:용서를 빌다
;・一定条件で분노を買い、징계・포인트追加
;-------------------------------------------------
;・トリガー이벤트制
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다
;ACT분류:1:애무
;	ACT:풋잡하기/밀어 넘어트린다
;ACT분류:2:도구
;	ACT:항문바이브/항문비즈
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:밧줄/눈가리개/볼개그/매도/관장세트
;ACT분류:7:이상
;	ACT:방치플레이/公器플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;-------------------------------------------------
@KOJO_COM_K1_33
;-------------------------------------------------
;・TFLAG:REACT파생(0=ふーん/1=怯えに対する초조함)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;ACT, 公器では派生しない
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は%CALLNAME%よりむしろ変になりそうな自分に怯えているようだ…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は悔しい思いを飲み込み、大人しく畏怖の視線を向けた…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は공포を感じ怯えている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は小動物のように怯えている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「무서워하지 않아 좋아요」
		PRINTFORMW 「무서워해도…하는 것은 함께니까」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;=====================================================================
;봉사系反応 COM40番台
;=====================================================================
;・죠교자への애무を含む행동で見られる
;・ACT분류:봉사+トリガー이벤트制
;-------------------------------------------------

;────────────────────────────────────
;COM40, 거부한다
;────────────────────────────────────
;・REACT분류:거부
;・REACT인상:悪印象大
;・징계・포인트追加
;・의존도변화(-2, -4)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:영양제를 먹인다
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_40
;-------------------------------------------------
;・REACT파생なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM40, 거부한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は迷いを見せながらも要求には応じなかった…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は気圧されながらも要求は呑まなかった…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%의 要求を거부했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は強い意志で要求を撥ねつけた…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		;異変解決時の強気ノリ
		PRINTFORMW 「혐이라고 말해지고 있을 것 같습니까와 물러나는 만큼, 벙어리와나인가가 아니어요」
		PRINTFORMW 「무리해서라도 해요！」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM41, 소극적으로 한다
;──────────────────────────────────
;・REACT분류:소극적으로 한다
;・REACT파생1は"消極的なのが気に入らない"となり징계・포인트追加
;・REACT인상:好印象小
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_41
;-------------------------------------------------
;・TFLAG:REACT파생 == 0, 消極的だけどいいか
;・의존도변화(-3, +2)
;-------------------------------------------------
;・TFLAG:REACT파생 == 1, 消極的なのが気に入らない
;・의존도변화(-5, 0)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;=================================================
	;派生1, 消極的なのが気に入らない（징계・포인트加算）
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		;%CALLNAME:MASTER%의 消極的な자세に、%CALLNAME%は不満そうだ…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は自分から행동する勇気がないようだ…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は悔しそうに하면서も逆らおうとはしない…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%はどうにか%CALLNAME%の言う通りにしている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%は%CALLNAME%に逆えない…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「손, 떨리고 있는 원이야？」
			CASEELSE
				PRINTFORMW 「응…좀 더 빠져들었으면 좋네요」
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM42, 적극적으로 한다
;──────────────────────────────────
;・REACT분류:적극적으로 한다
;・REACT인상:好印象大
;・참다플래그解消
;・의존도변화(+4, +2)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_42
;-------------------------------------------------
;・REACT파생なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は自分より%CALLNAME%を우선しようとしている…
			PRINTFORMW 
		;実は嫌
		ELSE
			;%CALLNAME:MASTER%は意識して殊勝に振る마이おうとしているようだ…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は進んで%CALLNAME%に尽くしている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%はとにかく%CALLNAME%を喜ばせたいようだ…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「그럴 기분이 들어 준 것 같구나」
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM43, 애정을 담아 한다
;──────────────────────────────────
;・一定以上の호감도、죠교레벨が필요
;・REACT분류:적극적으로 한다
;・REACT인상:好印象大
;・참다플래그解消
;・반발각인があると弾かれる
;・의존도변화(+6, +10)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_43
;-------------------------------------------------
;・REACT파생なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;%CALLNAME:MASTER%は平静を装っているが、嬉しそうな表情を隠せない…
		PRINTFORMW 「지금의 너, 재미있는 얼굴 하고 있는 원이야？응후후♪」
		;指でもほっぺでもおちんぽでも、統合性が崩れないように
		PRINTFORMDW %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 뺨을 때굴때굴밀어넣어 왔다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(0) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%はうっとり했다顔で丁寧に命令をこな했다…
		;少女推敲中
		;PRINTFORM 「んふ…
		;CALL HEARTBW
;OriginalString : 아웃8, "「ね？」")
		CALL FADEOUT(8, "「저기？」")
;OriginalString : 인8, "「손」")
		CALL FADEIN(8, "「손」")
;OriginalString : 인8, "「握って」")
		CALL FADEIN(8, "「잡아」")
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%의 態度は愛しい人に尽くす際のそれそのものだった…
		PRINTFORMW 「%RAND_SPLIT("응후후…♪/기분이 좋다/있었는지")%」
		PRINTFORMDW %CALLNAME:TARGET%(은)는 기분을 느슨한,%CALLNAME:MASTER%에 몸을 맡기고 있다
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;────────────────────────────────────
;COM44, 태연하게 한다
;────────────────────────────────────
;・REACT분류:태연하게 한다
;・REACT인상:好印象小
;・의존도변화(+2, -2)
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_44
;-------------------------------------------------
;・REACT파생なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;実はまんざらでもない
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%は平常心を保とうとしている…
			PRINTFORMW 「너의 열심인 모습도 나쁘지 않지만…」
			PRINTFORMW 「응…좀 더 솔직해져도 좋아요？」
		;実は嫌
		;ELSE
			;%CALLNAME:MASTER%は無理を하면서も平静を装っている…
		;	PRINTFORMW 
			RETURN 0
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は%CALLNAME%を悦ばせて歓心を誘おうとしている…
		PRINTFORMW 「 좀 더가 붙어 있고 원이야？」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%には%CALLNAME%の様子を伺うだけの余裕があるようだ…
		PRINTFORMW 「담담하게 되는 것도, 어쩐지 적이군요…」
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	;IF 구상記入チェック(0)
	;	PRINTFORMW 
	;	RETURN 1
	;ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;=====================================================================
;その他の反応 COM50番台
;=====================================================================
;・特定の場面で発生する変な행동
;-------------------------------------------------

;────────────────────────────────────
;COM50, 자위를 시작한다
;────────────────────────────────────
;・REACT분류:受け入れる
;・派生1,勝손にオナニーするなの場合は징계・포인트追加
;・REACT인상:悪印象小
;・참다플래그解消
;-------------------------------------------------
;・トリガー이벤트制
;-------------------------------------------------
;・밧줄で縛られていると駄目
;・すでに자위してる、성교중、성교봉사중は弾かれる
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다
;ACT분류:1:애무
;	ACT:키스
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시/수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/발핥기강제/가슴애무강제
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이
;ACT분류:9:휴식
;	ACT:휴식시킨다
;ACT분류:10:성교봉사
;	ACT:
;-------------------------------------------------
@KOJO_COM_K1_50
;-------------------------------------------------
;TFLAG:REACT파생 == 0, 勝손にオナニーしてるけどいいか, 의존도변화(-1, 0)
;TFLAG:REACT파생 == 1, 勝손にオナニーするな, 의존도변화(-3, +2)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;本体側に専用の処理なし
	
	;=================================================
	;おかず分岐
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:자위중
		PRINTFORM 
	ENDIF
	;=================================================
	;REACT파생1, 勝손にオナニーするな
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「무엇, 마음대로 즉시 시작해」
		;PRINTFORMW 「…出했다ら%RAND_SPLIT("堪忍/し承知し/許さ")%ないわよ」
		IF TALENT:새드
			PRINTFORMDW %CALLNAME:TARGET%(은)는 비난하는 것 같은 시선을 향하여 왔다
		ELSE
;OriginalString : 아웃8, "「いいけど」")
			CALL FADEOUT(8, "「좋지만」")
			PRINTFORMW 「해도 된다고 말해.」
			PRINTFORMW 「해도 되지만, 내면(자)%RAND_SPLIT("인내해/알아/허야")%없어요」
		ENDIF
		RETURN 1
	ENDIF
	;=================================================
	;以降 勝손にオナニーしてるけどいいか
	;=================================================
	;=================================================
	;REACT부하2, 無理を했다행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
		;%CALLNAME:MASTER%は자위をすることで快感に溺れてしまおうとしている…
		PRINTFORMW 「무리하게 그런 일, 하지 않아 좋아요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하1, 無理のない행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "無理のない행동"
		;%CALLNAME:MASTER%は참다できずに勝손に自分を慰めている…
		PRINTFORMW 「무엇 만지작거려」
		PRINTFORMW 「사람의 일, 마음대로 반찬으로 해…나쁘다\@ TALENT:MASTER:남자 ? 자지 # 아이 \@그런데」
		PRINTFORMW 「부끄럽지만, 멈출 수 없다？」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하0, 自然な행동
	;=================================================
	IF 구상記入チェック(100) && GET_COM_RANK() == "自然な행동"
		;%CALLNAME:MASTER%의 손は無意識に自分の성기をまさぐっているようだ…
		PRINTFORMW 「무엇 만지작거려」
		PRINTFORMW 「말해져 깨닫아도 얼굴이군요…」
		RETURN 1
	ENDIF
	
	;上層の小分岐を全て書いた場合は不要な箇所です。その際は削除してしまうのを推奨
	;派生を書かない、もしくは一部を방치했다場合はこの箇所が補完します
	;=================================================
	;その他・条件なし
	;=================================================
	;IF 구상記入チェック(0)
	;	RETURN 1
	;ENDIF
;条件にヒットしなければ統括汎用リアクションへ
RETURN 0
ENDIF

;──────────────────────────────────
;COM51, 도구를 제거한다
;──────────────────────────────────
;・REACT분류:날뛴다
;・TEQUIP:(바이브/항문바이브/항문비즈/관장그릇＋플러그/클리캡/오나홀/유두캡/눈가리개/볼개그/페니스밴드)を장착している事が条件
;・죠교대상が자위중である場合は中断になる
;・REACT인상:悪印象大
;・의존도변화(-3, -10)
;-------------------------------------------------
;・外す도구は一度に一つまで
;※우선度は以下の通り
;(눈가리개>관장그릇＋플러그>항문바이브>바이브> 오나홀>클리캡>볼개그>유두캡> 항문비즈)
;-------------------------------------------------
;선택可能な条件…
;・TEQUIP:(바이브/항문바이브/항문비즈/관장그릇＋플러그/클리캡/오나홀/유두캡/눈가리개/볼개그/페니스밴드)を장착している事が条件
;・TEQUIP:밧줄/성교중は実行不可
;-------------------------------------------------
@KOJO_COM_K1_51
;-------------------------------------------------
;・外했다도구のTEQUIP번호がTFLAG:REACT파생に入る
;-------------------------------------------------

;地の文は汎用処理があるのみ
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM51, 도구를 제거한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100)
	;REACT파생分岐を追加する場合はこの箇所の上層で
	;=================================================
	;その他・条件なし
	;=================================================
	IF 구상記入チェック(0)
		;%CALLNAME:MASTER%は장착당했다도구を勝손に外し、%CALLNAME:TARGET%はとても怒っているようだ…
		;無言が一番堪える
		PRINTFORMDW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;COM52, 이루는대로
;────────────────────────────────────
;・効果なし。実行できる反応が一つもない時の救済
;・죠교자に好きなように嬲られかつ、言いなりになる的状況
;・REACT파생毎にREACT분류、의존도변화が変わる
;-------------------------------------------------
@KOJO_COM_K1_52
;-------------------------------------------------
;・TFLAG:REACT파생(0=통상/1=성교系/2=성교봉사系/3=수치系、強制系/4=봉사系/5=脅迫系/6=脅迫系/7=고통系/8=자위중の회화/9=快感を受けつつ회화/10=쾌락系/11=회화系/12=일회휴게)
;-------------------------------------------------

;------------------------------------------------
;REACT분류:受け入れる/의존도변화(+4, +10)
;성교系ACT・역강간
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생1, 「無抵抗に犯される」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
	;PRINTFORML %CALLNAME:MASTER%は力の入らない身体を無抵抗に犯され、送り込まれる쾌락に喘いでいる…
	PRINTFORMW 「어쩐지, 이케나이일 하고 있는 기분이 되어 유곽이군요…」
	RETURN 1
ENDIF

;------------------------------------------------
;REACT분류:受け入れる/의존도변화(+10, +10)
;성교系봉사系ACT・기승위
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생2, 「溺れたように허리를 흔든다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 2
	;%CALLNAME:MASTER%は%CALLNAME%の身体に溺れたように、息を乱してひたすら腰を振っている…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;ACT(의상変更/자위/방뇨/開帳/이라마치오)
;REACT분류:受け入れる/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생3, 「言われるままゆっくり従う」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 3
	;%CALLNAME:MASTER%はただ%CALLNAME%に言われるまま従った…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;봉사系ACT
;REACT분류:受け入れる/의존도변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생4, 「魅入られたように봉사を繰り返す」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 4
	;まるで魅入られたように、%CALLNAME:MASTER%は無心に%CALLNAME%に봉사했다…
	;心ここに在らずといった様子で、その손つきは怪しいものだ…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;ACT(脅す/징계・と宣言/고함치다/기분나쁘게 웃다/매도)
;반항적な素質があり、굴복각인5以下の場合
;REACT분류:소극적으로 한다/의존도변화(0, -2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생5, 「悔しそうに睨む」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 5
	;%CALLNAME:MASTER%は悔しそうに唇を噛み、%CALLNAME%を見上げた…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;ACT(脅す/징계・と宣言/고함치다/기분나쁘게 웃다/매도)
;REACT분류:소극적으로 한다/의존도변화(+2, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생6, 「怯えたように見つめる」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 6
	;%CALLNAME:MASTER%は怯えたように%CALLNAME%を見つめた…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;가학系ACT・고통が大きい場合
;REACT분류:소극적으로 한다/의존도변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생7, 「声にならない悲鳴を上げてのたうちまわる」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 7
	;%CALLNAME:MASTER%は고통のあまり声にならない悲鳴を上げてのたうち回った…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;快SOURCEあり
;회화系ACTかつ자위중である場合
;REACT분류:受け入れる/REACT인상:悪印象小/의존도변화(-2, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생8, 「おなにーに夢中で聞いてない」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 8
	;%CALLNAME:MASTER%は자위に没頭してまともに返事ができなかった…
	;%CALLNAME%は不満そうだ…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;快SOURCEあり
;회화系ACT
;REACT분류:受け入れる/의존도변화(+4, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생9, 「気持ちよくって生返事」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 9
	;%CALLNAME:MASTER%は快感に身悶え하면서頷くことしかできなかった…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;快SOURCEあり
;회화系ACT以外の場合
;REACT분류:受け入れる/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생10, 「頭真っ白恍惚상태」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 10
	;%CALLNAME:MASTER%は与えられる快感に翻弄され、恍惚となっている…
	;疲れ切った頭の中は真っ白に染まり、何も考えることができない…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;회화系ACT
;REACT분류:受け入れる/의존도변화(+8, +4)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생11, 「自分でもわからないまま恥ずかしいことを입走る」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:REACT파생 == 11
	;%CALLNAME:MASTER%はどこか茫然と했다まま受け答え했다…
	;自分でもよく解らないままに恥ずかしいことを입走っている気がする…
	PRINTFORMW 「그러한 일은, 좀 더 맹숭맹숭한 얼굴때에 말하세요」
	RETURN 1
ENDIF

;------------------------------------------------
;죠교자、もしくは죠교대상の상태によって일회휴게되었다場合
;もしくは대만족보너스で쉬게を선택했다場合
;REACT분류:受け入れる/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;일회휴게の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:일회휴게
	;%CALLNAME:MASTER%は安心했다ように体の力を抜いた…
	;=================================================
	;대만족보너스で쉬게を선택했다場合
	;=================================================
	IF 구상記入チェック(100) && IS_POLICY("금일", "휴식")
		SELECTCASE TIME
			;昼間　お茶後
			CASE 0
				PRINTFORMW 「좋은 날씨군요 」
				;%CALLNAME:MASTER%死亡플래그%CALLNAME:MASTER%死亡ENDに対応する為加筆;何でもない事を共に楽しむのが恋人
;OriginalString : 아웃20, "ねぇ見て")
				CALL FADEOUT(20, "저기 봐")
;OriginalString : 인20, "夕日")
				CALL FADEIN(20, "석양")
;OriginalString : 인20, "綺麗ね")
				CALL FADEIN(20, "예쁘구나")
				PRINTFORMDW %CALLNAME:MASTER%(은)는 %CALLNAME:TARGET%와 함께 저녁놀하늘을 바라보았다
				;夜　夕食後
			CASEELSE
				PRINTFORMW 「이런 것, 좋은데…」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;------------------------------------------------
;全ての条件に該当しない場合
;REACT분류:受け入れる/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생0, その他
;━━━━━━━━━━━━━━━━━━━━━━━━
[SKIPSTART]
;上部条件に該当しても表示される;죠교대상の疲労による中止の際にも表示される模様;想定外の為コメントアウト
IF 구상記入チェック(25)
	;%CALLNAME:MASTER%は身体を投げ出し、されるがままになっている…
	PRINTFORMDW 「응. 반응이 둔하네요…자기 마음대로해버리는 원이야？」
	RETURN 1
ENDIF
[SKIPEND]

;=====================================================================
;特殊な反応 COM70～
;=====================================================================

;──────────────────────────────────
;COM70, 가게해줘！
;──────────────────────────────────
;・REACT분류:受け入れる
;・REACT인상:好印象大
;・의존도변화(+10, +6)
;-------------------------------------------------
;선택可能な条件…
;・애태움中ではない
;・TCVAR:MASTER:가버려が立っている
;・애무系ACT(키스/밀어넘어짐以外)かつ各部位のPALAMが5000以上である
;※上記条件に適う場合は可
;-------------------------------------------------
;・TFLAG:REACT파생(0=聞き流す/1=가게해줘あげる/2=가게해줘あげない）
;-------------------------------------------------
;ACTに於いてTCVAR:MASTER:애태움도 == 1(イかせる)であってもCOM70の선택でTCVAR:MASTER:애태움도 > 1(焦らす)になる場合あり
;現在の仕様ではこの손の변화を参照する術はない。本体側の対応が望まれる！
;-------------------------------------------------
@KOJO_COM_K1_70
;-------------------------------------------------
;・TFLAG:REACT파생(0=聞き流す/1=가게해줘あげる/2=가게해줘あげない）
;-------------------------------------------------

;%CALLNAME:TARGET%(은)는 심술궂은 미소를 띄우며 %CALLNAME:MASTER%(을)를 애태우기 시작했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생2, 가게해줘あげない
;━━━━━━━━━━━━━━━━━━━━━━━━
;とりあえず最小限の分岐を置いておきますが
;PENIS(MASTER)等で페니스の有無を参照했다方が無難かも知れません
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:REACT파생 == 2
	;=================================================
	;ACT분류,애무
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "애무")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상記入チェック(100) && TRUECHECK(MENUMATCH(TFLAG:ACT, "성봉"), TEQUIP:성교봉사중) > 0
		;Ｖ성교봉사중
		;IF V_SEX(MASTER)
		;	PRINTFORMW 
		;;Ａ성교봉사중
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		;Ｖ성교봉사중
		IF V_SEX(MASTER)
			[SKIPSTART]
			PRINTFORMW 「 아직 안돼」
			PRINTFORMW 「그렇구나？」
			PRINTFORMW 「네가, 되어 거절해 상관하지 않고 허리를 잡아, 마음대로 허리를 털어 질내 사정해 올 때까지, 보류군요」
			[SKIPEND]
			PRINTFORMW 「 아직 안되어요」
			PRINTFORMW 「그렇구나…네가, 되어 거절해 상관하지 않고 허리를 잡아, 마음대로 허리 털어, 질내 사정해 올 때까지」
			PRINTFORMW 「보류군요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「좀 더 만%RAND_SPLIT("분발함/참고 견뎌/참다해")%없음 있고」
		RETURN 1
	ENDIF
ENDIF

;%CALLNAME:TARGET%は優しく微笑みながら애무を続けた
;━━━━━━━━━━━━━━━━━━━━━━━━
;派生1, 가게해줘あげる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
	;=================================================
	;ACT분류,애무
	;=================================================
	IF 구상記入チェック(0) && MENUMATCH(TFLAG:ACT, "애무")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상記入チェック(100) && TRUECHECK(MENUMATCH(TFLAG:ACT, "성봉"), TEQUIP:성교봉사중) > 0
		;;Ｖ성교봉사중
		;IF V_SEX(MASTER)
		;	PRINTFORMW 
		;;Ａ성교봉사중
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		;Ｖ성교봉사중
		IF V_SEX(MASTER)
			[SKIPSTART]
			;가버려でもなくとも発生しうる上に사정分岐と被る為再考が필요
			PRINTFORMW 「%RAND_SPLIT("이제(벌써)사정그래？/좋아요, 가득 내/응, 와")%」
			PRINTFORM 「 나는 너%RAND_SPLIT("의 신부/의 것/용")%(이)니까
			CALL HEARTBW
			[SKIPEND]
			IF !RAND:3
				PRINTFORMW 「 나의 보지로 가고 싶다？보지로 자지 기분 좋고--하고 싶다…？」
				PRINTFORMDW %CALLNAME:TARGET%(은)는 기쁜듯이%CALLNAME:MASTER%의 말을%RAND_SPLIT("악물고/반추 해")%(이)라고 있다
			ELSEIF !RAND:2
				PRINTFORMW 「응…♪」
				PRINTFORM 「너후응응, 의 보지로 살릴 수 있어 주어요
				CALL HEARTBW
			ELSE
				PRINTFORM 「 나는 너%RAND_SPLIT("의 신부/의 것/용")%(이)니까…너는, (들)물어 주지 않으면…
				CALL HEARTBW
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「야무지지 못하기 때문에…뭐, 좋아요」
		RETURN 1
	ENDIF
ENDIF

;%CALLNAME%は悪戯っぽい笑みを浮かべつつ、%CALLNAME:MASTER%의 要求を聞き流했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;派生0, 要求を聞き流す
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:REACT파생 == 0
	[SKIPSTART]
	;構文ミスにつきコメントアウト
	SELECTCASE RANDCOM(3, 0)
		CASE 0
			;さま表記は生かしておくべきかが悩み所
			PRINTFORMW 「%CALLNAME:TARGET%님너무 좋아！라고 말할 수 있으면(자), 생각해 주어도 괜찮아요？」
			PRINTFORMW 「%RAND_SPLIT("이봐요！/이봐요 이봐요,/어머 어머♪")%관념해 말해버릴 없음 있고」
		;CASE 1
		;	PRINTFORMW 
		;CASE 2
		;	PRINTFORMW
		CASEELSE
			PRINTFORMW 「-응…어떻게 하지？」
	ENDSELECT
	;イかせるつもりの상태から가게해줘！時に矛盾
	;聞き流している状況で相손にしているのが問題
		PRINTFORMW 「-응…어떻게 하지？」
		PRINTFORM 「응후후…
		PRINTFORMD %CALLNAME:TARGET%님너무 좋아！
		PRINTFORMW 라고 말할 수 있으면(자), 생각해 주어도 괜찮아요？」
		PRINTFORMW 「%RAND_SPLIT("이봐요！/이봐요 이봐요,/어머 어머♪")%관념해 말해버릴 없음 있고」
	RETURN 1
	[SKIPEND]
ENDIF
