;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 성교봉사系個別リアクション구상ファイル
;
;	성교봉사系リアクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;성교봉사系
;========================================================================
;・各REACT内の分け方としてTFLAG:REACT분류(IS_COMGRONAME)やSELECTCOM(IS_NOWCOMNAME)が考えられます
;・ただ、範囲の広い条件で拾ってしまうのは基本的にお勧めしません
;・ACT분류:성교봉사系の場合、봉사系COM(COM40~COM44)を中心の선택肢となりますが
;・状況によってはその他のものが선택肢に含まれる可能性もあります
;・リアクション(REACT)で処理されなかったものは汎用リアクション(COM)で補完されるので
;・無理に全て埋める필요はありません
;・필요と思うものを見極め、絞り込み、COMとの連携を考えて効率よく構築すべきです
;-------------------------------------------------
;・各REACT内は、&& IS_NOWCOMNAMEなり、&& IS_COMGRONAME等の条件で決めうちしましょう
;・ここに条件無し分岐があると、全て拾ってしまって汎用リアクションに流れません
;・죠교대상の反応に関わらず、同じリアクションしかしなくなってしまいます
;・要するにこれは、想定당했다システムの破壊に繋がります
;-------------------------------------------------
;　REACTの地の文はACT_MESSAGE.ERBのTRAIN_MESSAGE2_{TFLAG:ACT}で処理されます
;　地の文は受容の場合はTFLAG:REACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)
;　拒絶했다場合はREACT분류(날뛴다/逃げる)とREACT분류:거부に分かれます
;　各REACT분류内でTFLAG:REACT부하(自然な행동/無理のない행동/無理を했다행동)で派生
;　REACT분류が(受け入れる/적극적으로 한다/태연하게 한다)の場合は、TCVAR:MASTER:가버려、TCVAR:MASTER:애태움도等で細かく描写されます
;　ACT_MESSAGE.ERB REACTION_MESSAGE.ERBを参照하면서の作業をお勧めします
;-------------------------------------------------
;各REACTでは想定される反応を網羅してあります。필요に応じて分岐を増減して使用して下さい
;COMによってはREACT파생でREACT분류が변화する事に注意
;各COMの派生は基本的にREACT파생1を条件決めうちの小分岐にし、REACT파생0はその他扱いで条件なしとしています
;손間を掛けたくない場合はその他のみを書いて、派生に関わらず当該箇所を補完する事ができます
;またIF 구상記入チェック(0) && TFLAG:REACT파생 < 1とする事で派生했다場合は汎用リアクションへ流すなどという事も可能です
;派生は将来増える可能性もあるので != 1などとするよりは < 1と했다方が無難であると考えます
;-------------------------------------------------
;・COM20.허리를 흔든다について…
;・COM20, 허리를 흔든다はACTによってREACT분류が변화
;・역강간の場合はREACT분류:受け入れる。その他성교봉사系TではREACT분류:적극적으로 한다扱いとなる
;・この변화は関数@SEX_INITIATIVEにて行われる
;-------------------------------------------------

;────────────────────────────────────
;REACT95, 정상위시킨다・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사系から/3=Ａ성교系から/4=Ｖ성교系から/5=Ａ성교봉사系から)
;-------------------------------------------------
;想定されるCOM…
;애무系反応
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교系反応
;	COM:허리를 흔든다
;고통系反応
;	COM:날뛴다
;봉사系反応
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;その他反応
;	COM:가게해줘！
;-------------------------------------------------
@KOJO_REACT_K1_95
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT분류が날뛴다/거부の場合は취소扱い
;・ACTが취소となった場合はTEQUIP:성교봉사중もリセット
;・その他のREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)の場合は受容扱いとなる
;・죠교대상が동정なら동정상실、죠교자が처녀なら처녀상실が起こる
;・TEQUIP:성교봉사중が立っている際にREACT분류が날뛴다だと拒絶扱いになる(即ち、성교봉사系ACT以外でも拒絶処理となる場合がある)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が拒絶했다場合(REACT분류が날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:성교봉사중を参照している)だと死に分岐になると思われます
	;ACT파생で括る事で相似形の結果が得られそうですが、関数が손入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に삽입していた場合
	;=================================================
	;IF 구상記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW 
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「나부터 하지 않으면 안될까？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/受け入れる")
	;=================================================
	;REACT분류が적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상記入チェック(100) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでも가버려な상태のまま、腰を振って봉사させられている…
		SELECTCASE RAND:3
			CASE 0
				PRINTFORMW 「사정하면 타목」
				PRINTFORMW 「…안되지만, 싫지 않으니까」
			CASE 1
				PRINTFORMW 「내면 안돼」
				PRINTFORMW 「…안되지만, 억지로 (안)중이고 하는 것은 상관없어요」
			CASEELSE
;OriginalString : 아웃28, "（駄目って、いいって事なんだけど…こいつ둔감だから、わかってなさそうね）")
				CALL FADEOUT(28, "（안 된다고, 좋다는 일이지만…이 녀석둔감그러니까, 모르고 있는 것 같구나）")
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류が受け入れる
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;快感に부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「今の状況に부끄러워한다」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		IF !RAND:2
			PRINTFORMW 「너가 부끄러워하고 있는 표정, 훤히 들여다보여요」
;OriginalString : 인12, "いい眺めね")
			CALL FADEIN(12, "좋은 경치군요")
;OriginalString : 아웃8, "んふふ")
			CALL FADEOUT(8, "응후후")
		ELSE
			PRINTFORMW 「이봐요, 부끄러워하지 말고 오세요」
;OriginalString : 아웃20, "「私だって、恥ずかしいんだから」")
			CALL FADEOUT(20, "「나도, 부끄럽기 때문에」")
			PRINTFORMW 「 좀 더, 가 뒤따라 주면…만갱이 아니어요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT분류:날뛴다扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("싫어한다")
	PRINTFORMW 「그렇게 혐？」
	PRINTFORMDW %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%에 불안인 관심을 가졌다
	RETURN 1
ENDIF

;派生1の場合はREACT분류:용서를 빌다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「耐久」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「참고 있는 너의 얼굴. 사랑스러워요」
		PRINTFORMDW %CALLNAME:TARGET%나쁜 미소를 띄우면(자), 군요 새와 허리를 움직여%CALLNAME:MASTER%(을)를 추적해 갔다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 받아들인다")
	PRINTFORMW 「녹은 얼굴 해 버려서♪%CALLNAME:TARGET%속, 기분이 좋다？」
	PRINTFORMW 「응후후♪좀 더 기분 좋아져버릴 없음 있고」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("기분 좋다!")
	[SKIPSTART]
	;余裕が有り過ぎて少女らしさを欠く為コメントアウト
	;快晴、春の陽気、少女らしさ、義務教育受けてなさそう。레이무と죠교の食い合わせは、悪い。
	;PRINTFORMW 「うん。続けていいわよ」
	;PRINTFORMW 「もっと気持ちよくなっちゃいなさい」
	[SKIPEND]
	PRINTFORMW 「계속해도 좋으니까, 좀 더 기분 좋아져버릴 없음 있고」
;OriginalString : 아웃8, "（気持ちいいんだ）")
	CALL FADEOUT(8, "（기분이 좋다）")
	PRINTFORM 「…네에에
	CALL HEARTBW
	RETURN 1
ENDIF

;主導権によってREACT분류が변화する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("허리를 흔든다")
	;んきゅって言わせたい この辺りが適当か
	PRINTFORMDW %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 추삽에, 입을 눌러 소리가 되지 않는 소리를 흘리고 있다
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;消極的なのが気に入らない(징계・포인트加算)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		RETURN 1
	ENDIF
	
	IF 구상記入チェック(100) && PALAM:쾌Ｖ > 8500
		PRINTFORMW 「%RAND_SPLIT("초조하게 하지 마/부탁")%, 기분 좋게 하며」
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「받아 들여 주기 때문에, 좀 더가 붙어 있고 원이야」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("적극적으로 한다")
	PRINTFORMW 「응. 좀 더, 좀 더！」
	[SKIPSTART]
	;体位特有の味をもっと出했다い
	;PRINTFORMW 「ずっとこう당했다かったの、あんたに覆い被られて、組み敷かれて、抱きしめられて」
	;정상위を男女逆転기승위と解釈するとどうだろう
	[SKIPEND]
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("애정을 담아 한다")
	IF !RAND:3
		PRINTFORMW 「이것…마음 좋아요…좀 더 해？」
		[SKIPSTART]
		;ここは描写しない方が良さそう
		PRINTFORMDW %CALLNAME:TARGET%는 넑을 잃은 표정으로 %CALLNAME:MASTER%에 몸을 맡기고 있다
		[SKIPEND]
	ELSEIF !RAND:2
		PRINTFORM 「응후후
		CALL HEARTBW
		PRINTFORMDW %CALLNAME:TARGET%는 지워진다%CALLNAME:MASTER%의 뺨을 양손으로 사이에 두어 응시해 왔다
	ELSE
		;ここは%CALLNAME:MASTER%表記
		PRINTFORM 「%CALLNAME:MASTER%
		CALL HEARTBW
		PRINTFORMDW %CALLNAME:TARGET%피부개를 조르는 어린 아이와 같이 양손을 펴 왔다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("태연하게 한다")
	PRINTFORMW 「담담하게 되는 것은, 재미있지 않네요…」
	PRINTFORMW 「너는 하는 것보다도, 되는 편이 좋은 것일까？」
	RETURN 1
ENDIF

;애태움中には선택不能。TCVAR:MASTER:가버려が立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘！(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("가게해줘！")
	;ACTに於いてTCVAR:MASTER:애태움도 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:애태움도 > 1(焦らす)になる場合あり
	;現状でこの변화を拾う術はない
	;=================================================
	;REACT파생1/REACT파생2
	;=================================================
	IF 구상記入チェック(100) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;派生1,가게해줘あげる
		IF TFLAG:REACT파생 == 1
			;%CALLNAME:TARGET%さん겁없이 웃는다
			PRINTFORMW 「이대로, 말해버릴 싶다？정액 내고 싶다？」
			PRINTFORMW 「자식국물\@ TALENT:TARGET:임신 ? 쏟고 싶다 # 쏟아, 나를임신해 \@？」
			PRINTFORMDW %CALLNAME:TARGET%는 겁없게 웃으면(자) 격렬하게 허리를 움직이기 시작했다
		;派生2,가게해줘あげない
		ELSE
			PRINTFORMW 「 아직 안되어요？좀 더 즐기고 싶기 때문에, 참아 주세요」
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		[SKIPSTART]
		;派生1,가게해줘あげるに移転。「ふーんと聞き流す」の「ふーん」を活用
		PRINTFORMW 「이대로, 말해버릴 싶다？정액 내고 싶다？」
		PRINTFORMW 「자식 따라, 나를 잉세대？」
		[SKIPEND]
		PRINTFORMW 「-응？살았다？」
		PRINTFORMDW %CALLNAME:TARGET%는 녹은 시선으로 %CALLNAME:MASTER%(을)를 응시하면서 황라고 있다
		RETURN 1
	ENDIF
ENDIF
;REACT95, 정상위시킨다

;────────────────────────────────────
;REACT96, 후배위시킨다・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사系から/3=Ａ성교系から/4=Ｖ성교系から/5=Ａ성교봉사系から)
;-------------------------------------------------
;想定されるCOM…
;애무系反応
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교系反応
;	COM:허리를 흔든다
;고통系反応
;	COM:날뛴다
;봉사系反応
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;その他反応
;	COM:가게해줘！
;-------------------------------------------------
@KOJO_REACT_K1_96
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT분류が날뛴다/거부の場合は취소扱い
;・ACTが취소となった場合はTEQUIP:성교봉사중もリセット
;・その他のREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)の場合は受容扱いとなる
;・죠교대상が동정なら동정상실、죠교자が처녀なら처녀상실が起こる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が拒絶했다場合(REACT분류が날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:성교봉사중を参照している)だと死に分岐になると思われます
	;ACT파생で括る事で相似形の結果が得られそうですが、関数が손入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に삽입していた場合
	;=================================================
	;IF 구상記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他・ACT開始時の拒絶
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORML 「여자에게 창피를 주지 말아 」
		PRINTFORMDW %CALLNAME:TARGET%는 부풀고 면에서 %CALLNAME:MASTER%(을)를 예응으로 있다
		PRINTFORMW 「나부터 하지 않으면 안될까？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/受け入れる")
	;=================================================
	;REACT분류が적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상記入チェック(100) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでも가버려な상태のまま、腰を振って봉사させられている…
		;솔직になれるの分岐あたりで矛盾しないだろうか
		PRINTFORMW 「%RAND_SPLIT("세키닌 취합니다라고 말한다/솔직(이)가 될 수 있다/좋아한다고 말한다")%까지,%RAND_SPLIT("なかだし/질내사정/出")%하면 안되어요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류が受け入れる
	;=================================================
	IF 구상記入チェック(100) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 「%RAND_SPLIT("세키닌 취합니다라고 말한다/솔직(이)가 될 수 있다/좋아한다고 말한다")%까지, 내면 안되어요」
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;快感に부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「今の状況に부끄러워한다」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「전부터(분)편이 좋아？」
				PRINTFORMW 「전부터 하면, 너에게%RAND_SPLIT("표정/느끼고 있는 곳/군침 나와 있는 곳")%볼 수 있어버리니까…」
				;消え入るような声を表現했다いが、fadeoutはなにか違う気がする。やっと絞り出했다声としてFADEINに変更
;OriginalString : 인12, "「私が恥ずかしいのよ…」")
				CALL FADEIN(12, "「내가 부끄러운거야…」")
			CASEELSE
				PRINTFORMW 「짐승의 교미하고 싶고 부끄러울까？」
				PRINTFORMW 「그렇게 부끄러워하지 않아도, 모처럼 둘이서니까, 군요？\@ IS_CONDITION(MASTER, "정욕") ? 번식합시다 # 즐깁시다 \@」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT분류:날뛴다扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("싫어한다")
	PRINTFORMW 「교미하고 싶고 혐？」
	PRINTFORMW 「케다모노인 너에게는 이것으로 좋아」
	RETURN 1
ENDIF

;派生1の場合はREACT분류:용서를 빌다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「耐久」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 받아들인다")
	[SKIPSTART]
	;こづくりを前面に出し過ぎると赤い座布団運びを連想する為コメントアウト
	;もう取ってる。もう産ませている際に矛盾する為コメントアウト
	;改善の余地が残る
	PRINTFORMDW %CALLNAME:MASTER%(은)는 책임을 진다%RAND_SPLIT("각오를 하면/결의를 굳히면")% %CALLNAME:TARGET%의 허리를 잡아 근원까지 음경을 념 글자 붐비었다
	;%CALLNAME:MASTER%의 ちんぽは果たして奥まで届くのか
	PRINTFORMW 「그렇게…안쪽까지 확실히 삽입없음 있고」
	
	;後ろ손に両손掴みは地の文と矛盾する
	
	;受け당신が攻めを受け入れる플레이
	[SKIPEND]
	;허리를 흔든다より消極的な당신主導 
	IF IS_CONDITION(MASTER, "정욕")
		;레이무さんが気持ちいい！
		PRINTFORML 「너로 되어버린다의, 기분이 좋다」
	ELSE
		;레이무さんが더 하드하게 해
		PRINTFORML 「 좀 더 격렬하고%RAND_SPLIT("라고도 괜찮기 때문에/해도 되니까")%」
	ENDIF
	
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("기분 좋다!")
	PRINTFORMW 「기분 좋다？보지 기분 있고？」
	RETURN 1
ENDIF

;主導権によってREACT분류が변화する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("허리를 흔든다")
	[SKIPSTART]
	IF !RAND:2
		;탈의分岐が필요
		PRINTFORMDW 흰 등에 퍼지는 흑발이 요염하다
		
		;和室に音の反響は合わない気がしてきた
		PRINTFORMDW %CALLNAME:TARGET%의 허리를 단단히잡아, 엉덩이에 허리를 부딪힐 때,
		PRINTFORMDW 리본이 나비와 같이 춤추어 놀아, 짐승과 같이 허덕여 소리가 방에 울렸다
		;조교중の言動とすり合わせ 激しくの移転の為コメントアウト
		;PRINTFORMW 「んんっ！激しくなってきたわね」
		;PRINTFORMW 「一生懸命腰振っちゃって、もっと격렬하게もいいわよ」
		PRINTFORMW 「응응！열심히 허리 털어 버려서」
		;사정時との擦り合わせが필요
		PRINTFORMW 「너의 성욕, 전부 부딪쳐도 좋아요」
		
		;あんたの%CALLNAME:TARGET%（所有）が使いたいが、「の」と相性が悪い条件の調整も필요
		;PRINTFORMW 「もっと%RAND_SPLIT("あんた/れいむ")%の\@ TALENT:TARGET:임신 ? 孕み穴 # おまんこ \@使い込んで」
		;尻に욕정했다ケダモノ風
		PRINTFORMDW %CALLNAME:MASTER%(은)는 %CALLNAME:TARGET%의 엉덩이를 덥석으로 하면서 필사적으로 허리를 부딪혔다
		PRINTFORMDW %CALLNAME:MASTER%(은)는 %CALLNAME:TARGET%의 다리를 움켜 쥐고 필사적으로 허리를 부딪혔다
		RETURN 1
	ENDIF
	[SKIPEND]
	IF IS_CONDITION(MASTER, "정욕")
		PRINTFORMDW %CALLNAME:MASTER%(은)는 %CALLNAME:TARGET%의 허리를 잡아 고정하면서 허리를 부딪혔다
		PRINTFORML 
		PRINTFORMW 「이거 참, 이 너무 다하고」
		RETURN 1
	ENDIF
	
	IF IS_CONDITION(TARGET, "정욕")
		PRINTFORMDW %CALLNAME:TARGET%의 입가를 군침이 탄다
		RETURN 1
	ENDIF
	
	;隠語は입にすれども喘ぎ声は必死に抑える
	IF PALAM:쾌Ｖ > 8500
		IF !RAND:2
			PRINTFORM 「～～
			CALL HEARTBW
			PRINTFORMDW %CALLNAME:TARGET%는 베개에 얼굴을 묻고 있다
		ELSE
			PRINTFORM 「～～
			CALL HEARTBW
			PRINTFORMDW %CALLNAME:TARGET%는 베개에 얼굴을 묻으면서도%CALLNAME:MASTER%의 허리의 움직임에 맞추고 단 소리를 흘리고 있다
		ENDIF
		RETURN 1
	ENDIF
	
	IF 구상記入チェック(100)
		;조교중の発言とのすりあわせのためコメントアウト
		;PRINTFORMW 「んんっ！激しくなってきたわね」
		;PRINTFORMW 「一生懸命腰振っちゃって、もっと격렬하게もいいわよ」
		PRINTFORMW 「응응！열심히 허리 털어 버려서」
		RETURN 1
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;消極的なのが気に入らない(징계・포인트加算)
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
		IF !IS_CONDITION(MASTER, "정욕") && IS_CONDITION(TARGET, "정욕")
			PRINTFORML 「부탁. 초조하게 하지 마」
			PRINTFORMW 「 좀 더, 좀 더 자지, 두 오지 않고 넘으며」
			;초조함が上がる箇所におねだりさせられて悔しい주인님を仕込むのはどうだろうか
			SIF TCVAR:MASTER:가버려
				PRINTFORMW 「안에 내！」
		ELSEIF IS_CONDITION(TARGET, "정욕")
			PRINTFORMW 「무엇 참아」
			PRINTFORMW 「이봐요, 해？」
		ELSE
			PRINTFORMW 「부탁. 초조하게 하지 마」
			PRINTFORMDW %CALLNAME:TARGET%는 아첨하는 것 같은 눈으로 %CALLNAME:MASTER%(을)를 응시해 왔다
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「전부터(분)편이 좋을까？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("적극적으로 한다")
	PRINTFORMW 「응응！격렬해진 원이군요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("애정을 담아 한다")
	PRINTFORMW 「필사적으로 이성을 보토우로서 원이군요」
	PRINTFORMW 「알아요. 허리의 움직여, 상냥한 것」
	
	;安産型のお尻を愛でる플레이はどうだろう
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("태연하게 한다")
	PRINTFORMW 「 좀 더 이성 날려, 열중해. 해 주었으면 하지만」
	PRINTFORMW 「전부터(분)편이 좋을까？」
	RETURN 1
ENDIF

;애태움中には선택不能。TCVAR:MASTER:가버려が立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘！(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("가게해줘！")
	;ACTに於いてTCVAR:MASTER:애태움도 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:애태움도 > 1(焦らす)になる場合あり
	;現状でこの변화を拾う術はない
	;=================================================
	;REACT파생1/REACT파생2
	;=================================================
	IF 구상記入チェック(100) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;派生1,가게해줘あげる
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 「가고 싶으면, 스스로 허리 털어 기분 좋아지세요」
			PRINTFORMW 「%CALLNAME:TARGET%의 보지로, 뭐-해 주세요」
		;派生2,가게해줘あげない
		ELSE
			PRINTFORMW 「사정하면 너, 곧%RAND_SPLIT("녹초가 되어 버린다/구라고는 되어 버린다")%(이)가 아니다」
			PRINTFORMW 「 나라도 기분 좋고해 주었으면 하기 때문에,%RAND_SPLIT("아직 안 돼요/참다해")%」
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「어떻게 하지？…응후후」
		PRINTFORMW 「이 오고 싶으면 스스로 허리 털어, 기분 좋아지세요」
		RETURN 1
	ENDIF
ENDIF
;REACT96, 후배위시킨다

;────────────────────────────────────
;REACT97, 대면좌위시킨다・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사系から/3=Ａ성교系から/4=Ｖ성교系から/5=Ａ성교봉사系から)
;-------------------------------------------------
;想定されるCOM…
;애무系反応
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교系反応
;	COM:허리를 흔든다
;고통系反応
;	COM:날뛴다
;봉사系反応
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;その他反応
;	COM:가게해줘！
;-------------------------------------------------
@KOJO_REACT_K1_97
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT분류が날뛴다/거부の場合は취소扱い
;・ACTが취소となった場合はTEQUIP:성교봉사중もリセット
;・その他のREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)の場合は受容扱いとなる
;・죠교대상が동정なら동정상실、죠교자が처녀なら처녀상실が起こる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が拒絶했다場合(REACT분류が날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:성교봉사중を参照している)だと死に分岐になると思われます
	;ACT파생で括る事で相似形の結果が得られそうですが、関数が손入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に삽입していた場合
	;=================================================
	;IF 구상記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		;대면좌위のいちゃこら感を保ちつつ자세を活かし拒絶する
		PRINTFORMDW %CALLNAME:MASTER%(은)는 방해를 시도%CALLNAME:TARGET%의 겨드랑이에 양손을 쑤셔, □
		PRINTFORMW 「！」
		PRINTFORML 「아는 」
		PRINTFORMW 「그만두고！！아하하는！그만두고！」
		PRINTFORMW 「이나·째···있고！」
		PRINTFORMDL 화가 났다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/受け入れる")
	;=================================================
	;REACT분류が적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでも가버려な상태のまま、腰を振って봉사させられている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류が受け入れる
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;快感に부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「今の状況に부끄러워한다」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT분류:날뛴다扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("싫어한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT분류:용서를 빌다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「耐久」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「필사적으로 참아 버려서, 좋은 경치군요」
		PRINTFORMW 「너의 무릎, 나의 특등석이군요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
	;半기승위として扱うのはどうだろう
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT분류が변화する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("허리를 흔든다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;消極的なのが気に入らない(징계・포인트加算)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("적극적으로 한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("애정을 담아 한다")
	SELECTCASE RAND:2
		CASE 0
			PRINTFORM 「가득 사랑해,%RAND_SPLIT("좀 더/가득")% %CALLNAME:TARGET%의 일, 귀여워해
			CALL HEARTBW
		CASEELSE
			PRINTFORMDW %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%에 안아 도착하면서 만족하게 하고 있다
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("태연하게 한다")
	PRINTFORMW 
	PRINTFORML 
	PRINTFORMW 
	PRINTFORMDW 맹숭맹숭한 얼굴을 유지하려고 하는 %CALLNAME:MASTER%에 %CALLNAME:TARGET%는 입술을 꽉 누를 때에, %CALLNAME:MASTER%의 뺨은 느슨해져 갔다…
	RETURN 1
ENDIF

;애태움中には선택不能。TCVAR:MASTER:가버려が立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘！(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("가게해줘！")
	;ACTに於いてTCVAR:MASTER:애태움도 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:애태움도 > 1(焦らす)になる場合あり
	;現状でこの변화を拾う術はない
	;=================================================
	;REACT파생1/REACT파생2
	;=================================================
	IF 구상記入チェック(100) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;派生1,가게해줘あげる
		;イかせるのやめて焦らす展開に進行する可能性あり
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 「응―？응후후, 어떻게 하지」
			PRINTFORMDL %CALLNAME:TARGET%는 연결된 채로%CALLNAME:MASTER%의 키에 팔을 돌리면(자) 허리의 움직임을 억제해 느긋하게 몸을 문질러 색을 냈다
		;派生2,가게해줘あげない
		ELSE
			;寝起きのまどろみのように「？」 「…」ではなく「、」で長すぎず
			PRINTFORMW 「응―？좀 더,」
			PRINTFORMDL %CALLNAME:TARGET%새와 히붙으면(자) 허리의 움직임을 멈추어 버렸다
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT97, 대면좌위시킨다

;────────────────────────────────────
;REACT98, 배면좌위시킨다・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사系から/3=Ａ성교系から/4=Ｖ성교系から/5=Ａ성교봉사系から)
;-------------------------------------------------
;想定されるCOM…
;애무系反応
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교系反応
;	COM:허리를 흔든다
;고통系反応
;	COM:날뛴다
;봉사系反応
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;その他反応
;	COM:가게해줘！
;-------------------------------------------------
@KOJO_REACT_K1_98
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT분류が날뛴다/거부の場合は취소扱い
;・ACTが취소となった場合はTEQUIP:성교봉사중もリセット
;・その他のREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)の場合は受容扱いとなる
;・죠교대상が동정なら동정상실、죠교자が처녀なら처녀상실が起こる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が拒絶했다場合(REACT분류が날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:성교봉사중を参照している)だと死に分岐になると思われます
	;ACT파생で括る事で相似形の結果が得られそうですが、関数が손入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に삽입していた場合
	;=================================================
	;IF 구상記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/受け入れる")
	;=================================================
	;REACT분류が적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでも가버려な상태のまま、腰を振って봉사させられている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류が受け入れる
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;派生1の場合はREACT분류:용서를 빌다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「耐久」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
;솔직にシンプルに　確率低め
IF 구상記入チェック(25) && IS_NOWCOMNAME("기분 좋다!")
	PRINTFORMW 「 나도」
	RETURN 1
ENDIF

;主導権によってREACT분류が변화する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("허리를 흔든다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;消極的なのが気に入らない(징계・포인트加算)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("적극적으로 한다")
	PRINTFORMW 「！」
	PRINTFORMDW %CALLNAME:MASTER%(은)는 적극적으로 %CALLNAME:TARGET%의 %RAND_SPLIT("귀를 살짝 깨물기/목덜미에입붙어라/어깨를애무스")%와 레이무의 입으로부터 사랑스러운 비명이 올랐다
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("애정을 담아 한다")
	PRINTFORMDL %CALLNAME:MASTER%(은)는 %CALLNAME:TARGET%의 머리카락에 코를 묻으면서 노 깨도록(듯이)%CALLNAME:TARGET%의 전신을 풀도록(듯이) 봉사했다…
	PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%(으)로 하고 눈사태 걸려, 때때로 그 가련한 입술로부터 이완 한 소리를 흘렸다
	;緩みすぎたのでコメントアウト
	;PRINTFORMW 「あ”あ”～生き返るわ～」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("태연하게 한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움中には선택不能。TCVAR:MASTER:가버려が立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘！(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("가게해줘！")
	;ACTに於いてTCVAR:MASTER:애태움도 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:애태움도 > 1(焦らす)になる場合あり
	;現状でこの변화を拾う術はない
	;=================================================
	;REACT파생1/REACT파생2
	;=================================================
	IF 구상記入チェック(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;派生1,가게해줘あげる
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;派生2,가게해줘あげない
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT98, 배면좌위시킨다

;────────────────────────────────────
;REACT99, 항문섹스시킨다・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=통상/1=연속/2=Ａ성교系から/3=Ｖ성교봉사系から/4=Ｖ성교系から)
;-------------------------------------------------
;想定されるCOM…
;애무系反応
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;성교系反応
;	COM:허리를 흔든다
;고통系反応
;	COM:날뛴다
;봉사系反応
;	COM:거부한다/소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;その他反応
;	COM:가게해줘！
;-------------------------------------------------
@KOJO_REACT_K1_99
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT분류が날뛴다/거부の場合は취소扱い
;・ACTが취소となった場合はTEQUIP:성교봉사중もリセット
;・その他のREACT분류(受け入れる/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)の場合は受容扱いとなる
;・죠교대상が동정でも동정상실にはならない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が拒絶했다場合(REACT분류が날뛴다/거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_COMGRONAME("날뛴다/거부")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:성교봉사중を参照している)だと死に分岐になると思われます
	;ACT파생で括る事で相似形の結果が得られそうですが、関数が손入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に삽입していた場合
	;=================================================
	;IF 구상記入チェック(0) && A_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/受け入れる")
	;=================================================
	;REACT분류が적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでも가버려な상태のまま、腰を振って봉사させられている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류が受け入れる
	;=================================================
	IF 구상記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF


;-----------------------------
;以下、COM毎の分岐
;-----------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;快感に부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「今の状況に부끄러워한다」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT분류:날뛴다扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("싫어한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT분류:용서를 빌다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「耐久」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("쾌감을 받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("기분 좋다!")
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT분류が변화する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("허리를 흔든다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("소극적으로 한다")
	;=================================================
	;消極的なのが気に入らない(징계・포인트加算)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("적극적으로 한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다(REACT분류:적극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
;確率の抑制
IF 구상記入チェック(25) && IS_NOWCOMNAME("애정을 담아 한다")
	PRINTFORMW 「너의 팔안, 따뜻하다」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("태연하게 한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움中には선택不能。TCVAR:MASTER:가버려が立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘！(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("가게해줘！")
	;ACTに於いてTCVAR:MASTER:애태움도 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:애태움도 > 1(焦らす)になる場合あり
	;現状でこの변화を拾う術はない
	;=================================================
	;REACT파생1/REACT파생2
	;=================================================
	IF 구상記入チェック(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;派生1,가게해줘あげる
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;派生2,가게해줘あげない
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT99, 항문섹스시킨다

;────────────────────────────────────
;REACT103, 역강간・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사系から/3=Ａ성교系から/4=Ｖ성교系から/5=Ａ성교봉사系から)
;-------------------------------------------------
;想定されるCOM…
;회화系反応
;	COM:용서를 빌다
;애무系反応
;	COM:부끄러워한다/싫어한다/쾌감을 참는다/쾌감을 받아들인다/기분 좋다!
;고통系反応
;	COM:비명지른다/날뛴다/무서워한다
;성교系反応
;	COM:허리를 흔든다
;봉사系反応
;	COM:소극적으로 한다/적극적으로 한다/애정을 담아 한다/태연하게 한다
;その他反応
;	COM:가게해줘！
;-------------------------------------------------
@KOJO_REACT_K1_103
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・他の성교봉사系と異なり、죠교대상は拒絶できない
;・COM20,허리를 흔든다はREACT분류:受け入れるにセットされる
;・죠교대상が동정なら동정상실、죠교자が처녀なら처녀상실が起こる
;・COM:싫어한다/날뛴다はREACT분류:날뛴다(即ち、暴れが通る状況)にはならない
;-------------------------------------------------
;・역강간は挙動及びCOMの경향がちょっと異なります
;-------------------------------------------------

;역강간は拒絶できないので、拒絶系の分岐は設置しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;애태움中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TCVAR:MASTER:가버려 && TCVAR:MASTER:애태움도 > 1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다/受け入れる")
	;=================================================
	;REACT분류が적극적으로 한다/태연하게 한다
	;=================================================
	IF 구상記入チェック(100) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでも가버려な상태のまま、腰を振って봉사させられている…
		PRINTFORMW 「%RAND_SPLIT("참다가능하게 되는 것도,/여유가 나왔을까？조금")%재미있지 않네요」
		PRINTFORMW 「 좀 더 격렬하게 하지 않으면 안될까」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류が受け入れる
	;=================================================
	IF 구상記入チェック(100) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 「응후후…를 (듣)묻고 싶어요」
		PRINTFORM 「녹은 얼굴로
		PRINTFORMD %RAND_SPLIT("누락응 짜내/자지 굽포굽포 해/질내는 안돼")%！
		PRINTFORM 라고 말할 때까지, 사정시켜 주지 않는다
		CALL HEARTBW
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다(REACT분류:용서를 빌다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("용서를 빌다")
	;派生1の場合はREACT분류:なし
	;=================================================
	;意見を出すなんていい度가슴だね！(징계・포인트加算)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「ふーん」(죠교대상の意見を黙殺)扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		;=================================================
		;당신쇠약
		;=================================================
		;당신にも레이무にも制御の자유はない
		IF IS_CONDITION(MASTER, "쇠약")
		;PRINTFORML 「そうね」
		;PRINTFORML 「あんたとは太く長い付き合いにするんだし、許してやるか」
		;레이무さん比較대상を知らない
		;PRINTFORML 「あんたのは細く短いけど」
			IF IS_CONDITION(TARGET, "정욕")
				PRINTFORM 「미안해요. 넘어, 풍부하지 않지 있고
				CALL HEARTBW
			ELSE
				PRINTFORM 「좀 더 만. 그런데
				CALL HEARTBW
				PRINTFORMDL 좀 더가 언제까지 지나도 끝나지 않는다…
			ENDIF
			RETURN 1
		ENDIF
		
		;=================================================
		;당신피폐
		;=================================================
		IF IS_CONDITION(MASTER, "피폐")
			PRINTFORMW 「」
			PRINTFORMW 「%RAND_SPLIT("무엇을 한심한 것 말하고 있는거야/다시 한번. 저기？/너의 여기는, 좀 더했다금기 싶어？/아직, 딱딱하지 않아/아직, 할 수 있겠죠？/………")%」
;OriginalString : 인6, "ゆっさゆっさ")
			CALL FADEIN(6, "백탕야")
;OriginalString : 아웃6, "「んっんっ」")
			CALL FADEOUT(6, "「읏읏」")
			;速める
;OriginalString : 인4, "ゆっさゆっさ")
			CALL FADEIN(4, "백탕야")
;OriginalString : 아웃4, "「んっんっ」")
			CALL FADEOUT(4, "「읏읏」")
			RETURN 1
		ENDIF
		
		;=================================================
		;それ以外
		;=================================================
		PRINTFORMW 
		PRINTFORMW 「무엇을 허락하기를 원하는 것일까？이봐요, 말해 보세요」
		PRINTFORMW 「이봐요 이봐요」
		PRINTFORM 「
		PRINTFORMD %RAND_SPLIT("/-관/(안)중이고")%들 째
		PRINTFORMW 라고」
		PRINTFORMW 「 좀 더 너의 한심한 소리, 들려줘？들려주세요♪」
		PRINTFORML 
		PRINTFORMDW %CALLNAME:TARGET%는 즐거운 듯 하다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;快感に부끄러워한다
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「今の状況に부끄러워한다」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「그렇게 부끄럽다？나라도 부끄러우니까%RAND_SPLIT("괜찮아/괜찮아/무승부")%」
		RETURN 1
	ENDIF
ENDIF

;역강간の場合、派生1、REACT분류:날뛴다になる事はないので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("싫어한다")
	IF IS_CONDITION(MASTER, "울굴")
		PRINTFORMW 「이렇게도 다해 주어…석연치 않아요…」
		PRINTFORMW 「어떤 일이라면 해도 되는거야？그런데. 가르치며」
		IF IS_CONDITION(TARGET, "울굴")
;OriginalString : 아웃12, "「お願いだから」")
			CALL FADEOUT(12, "「부탁이니까」")
			PRINTFORMW 「부탁이니까」
			PRINTFORMDW %CALLNAME:TARGET%의 뺨을 시도케가 타 갔다…
		ENDIF
	ELSE
		PRINTFORMW 「그렇게, 혐？」
		PRINTFORMDW %CALLNAME:TARGET%는 슬픈 듯 하게 표정을 흐리게 한다…
		PRINTFORMW 「…으응」
		PRINTFORMW 「 나답지 않았어요」
		PRINTFORMW 「너는 나아래에서 히응히응 말하고 있으면 좋아」
		PRINTFORMW 「관념해 야없음 있고！」
		PRINTFORMDW %CALLNAME:TARGET%는 눈에 눈물을 배이게 하면서도, 열심히 허리를 털고 있다…
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없이 웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("겁없이 웃는다")
	PRINTFORML 「응？무엇인가 이상할까？」
	PRINTFORMDW %CALLNAME:TARGET%의 두상에？하지만 떠오른다
	PRINTFORMDW 무슨 일에도 얽매이지 않는다%CALLNAME:TARGET%는, 이렇게 해 자신에게 개입해 오는 것이 이상하다고 하면 이상하다
	PRINTFORML
	;PRINTFORMDW %CALLNAME:TARGET%が拗ねたような目を%CALLNAME:MASTER%を向ける
	PRINTFORMDW %CALLNAME:TARGET%에%RAND_SPLIT("이마/뺨/코끝")%(을)를 쿡쿡 찔러졌다
	PRINTFORMW 「이상하고 어쩐지 없어요. 나래%RAND_SPLIT("/적령기인/잉 보고 무렵인/여자/년경인/팥찰밥 오고 있다")%응이니까…」
	RETURN 1
ENDIF

;派生1の場合はREACT분류:용서를 빌다扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 참는다")
	IF 구상記入チェック(100) && PENIS(MASTER) && TCVAR:MASTER:가버려
		PRINTFORMDW %CALLNAME:TARGET%는 사정을 참는다%CALLNAME:MASTER%의 모습에 입가를 매달아 올리고\@ IS_CONDITION(TARGET, "정욕") ? 와 다리를Ｍ의 글자에 열려,정액를 짜기 시작하려고 #  \@허리의 움직임을 빨리 했다
		RETURN 1
	ENDIF
	;=================================================
	;애원(REACT분류:용서를 빌다)
	;=================================================
	IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
		IF !RAND:2
			PRINTFORMW 「…어쩐지, 이상한 버릇이 붙어버릴 것 같고 무서워요」
		ELSE
			PRINTFORMDW %CALLNAME:TARGET%는 군침을 삼키면, 무언으로 허리를 부딪혀 왔다
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「耐久」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「언제까지 참을 수 있는 것일까？응후후…이봐요 힘내라 힘내라」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 받아들인다")
	PRINTFORMW 「소리, 나와 버린다 원이야？」
	PRINTFORMW 「기분이 좋다？\@ TALENT:임신 ? %CALLNAME:TARGET%의 잉 보고 구멍 # 무리하게개만들기 \@기분이 좋다？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋다!(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("기분 좋다!")
	;語尾の？はあった方がいいものか。ACT側で？REACT側では無い方がいいかもしれない
	SELECTCASE RAND:4
		CASE 0
			PRINTFORMW 「…너도, 움직여도 좋아요」
		CASE 1
			PRINTFORMW 「, 정직하고 좋다♪」
			PRINTFORMW 「스스로 단순하다고는 생각하지만, 역시 기뻐요」
		CASE 2
			PRINTFORM 「응후후♪가득, 어 통치합시다
			CALL HEARTBW
		CASE 3
			PRINTFORMW 「응. 계속해요？」
	ENDSELECT
	;레이무さんなりの애정表現
	;えっちが気持ちよくないとも受け取れる為コメントアウト
	;PRINTFORM 「私も、あんたの作るご飯が一番おいしいわ
	;CALL HEARTBW
	RETURN 1
ENDIF

;역강간の場合はREACT분류:受け入れるになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를 흔든다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("허리를 흔든다")
	;%CALLNAME:MASTER%は가버려되었다の後に表示される事に注意
	;애태움時に問題が出るかもしれない
	[SKIPSTART]
	;ハートのみでは物발りない
	PRINTFORM 「
	CALL HEARTBW
	PRINTFORML
	PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%%RAND_SPLIT("노손를 잡아/에 지워져")%, %CALLNAME:MASTER%의 하급자압력을 받아 들이고 있다
	PRINTFORM 「필사적으로 허리 털어 버려서, 나의 질내에서 사정하고 싶구나」
	[SKIPEND]
	;-------------------------------------------------
	;%CALLNAME:MASTER%가버려
	;-------------------------------------------------
	IF 구상記入チェック(100) && TCVAR:MASTER:가버려
		PRINTFORM 「너의 자지, 너의 자지가,
		PRINTFORMD 사정하고 싶은 사정하고 싶다
		PRINTFORM 라고, 나중에서 날뛰고 있다
		CALL HEARTBW
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;%CALLNAME:TARGET%가버려
	;-------------------------------------------------
	IF 구상記入チェック(100) && PALAM:쾌Ｖ > 8500
		PRINTFORMW 「 나, 이제 %RAND_SPLIT("조금/안돼 그렇게")%그러니까」
		PRINTFORM 「살릴 수 있어
		CALL HEARTBW
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;%CALLNAME:TARGET%정욕
	;-------------------------------------------------
	IF 구상記入チェック(0) && IS_CONDITION(TARGET, "정욕")
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;条件無し
	;-------------------------------------------------
	IF 구상記入チェック(100)
		PRINTFORM 「네가 해 주는 것, 솔직하게 기뻐요
		CALL HEARTBW
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%%RAND_SPLIT("노손를 잡아/에 지워져")%, %CALLNAME:MASTER%의 하급자압력을 받아 들이고 있다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 고통을 참는다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && IS_NOWCOMNAME("고통을 참는다")
	;도구責め中の고통が反映されるとの事なので変更が필요
	;PRINTFORMW 「痛いのを참다するのは私の方だと思うのだけど」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("비명지른다")
	;=================================================
	;공포による悲鳴
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「고통による悲鳴」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「너는 이것이라도 질네라고 없음 있고」
				PRINTFORMDW %CALLNAME:TARGET%는 자신의 속옷을%CALLNAME:MASTER%의 입에 밀어넣어 왔다
			CASEELSE
				PRINTFORMW 「차이원이군요. 응 크게 하면 합의한 간통이야！」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;역강간の場合暴れが通る事はない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
;派生2,制止による暴れ失敗扱い。派生1は汎用リアクションへ
;-------------------------------------------------
IF 구상記入チェック(100) && IS_NOWCOMNAME("날뛴다") && TFLAG:REACT파생 == 2
	IF IS_CONDITION(TARGET, "정욕")
		PRINTFORML 「이봐요 움직이지 마」
		PRINTFORML 「곧바로 기분 좋게 하기 때문에」
		PRINTFORMDW %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 양어깨를 누르고 범해 왔다
		RETURN 1
	ENDIF
	PRINTFORMW 「…그렇게, 혐？」
	PRINTFORMDW %CALLNAME:TARGET%는 슬픈 듯 하게 표정을 흐리게 한다
	PRINTFORML
	PRINTFORML 「…으응」
	PRINTFORMW 「 나답지 않았어요」
	PRINTFORMW 「너는 나아래에서 히응히응 말하고 있으면 좋아」
	PRINTFORMW 「관념해 범없음 있고！」
	PRINTFORMDW %CALLNAME:TARGET%는 눈초리에 눈물을 배이게 하면서,%RAND_SPLIT("기특하게/열심히/애정를 구해라")%허리를 털고 있다
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다(REACT분류:용서를 빌다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;派生：怯えに対する초조함
	;=================================================
	IF 구상記入チェック(0) && TFLAG:REACT파생 == 1
	ENDIF
	
	;派生1を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		IF IS_CONDITION(TARGET, "정욕")
			;余裕が無いので無意味に不安を煽る
			;有意義な情緒操作は솔직な레이무には不適切
			PRINTFORMW 「두렵지 않으니까. 두렵지 않으니까.」
		ELSE
			PRINTFORMW 「무서워하지 않아 좋아요. 내가 하기 때문에, 가만히 해 주세요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;애태움中には선택不能。TCVAR:MASTER:가버려が立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, 가게해줘！(REACT분류:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && IS_NOWCOMNAME("가게해줘！")
	;ACTに於いてTCVAR:MASTER:애태움도 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:애태움도 > 1(焦らす)になる場合あり
	;現状でこの변화を拾う術はない
	;=================================================
	;REACT파생1/REACT파생2
	;=================================================
	IF 구상記入チェック(100) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		;派生1,가게해줘あげる
		IF TFLAG:REACT파생 == 1
			;>@COM70でTCVAR:MASTER:애태움도 > 1(焦らす)になる場合あり
			;どちらでも通じる当たり障りの無い方向に軌道修正
			;PRINTFORMW 「いいわよ。一杯사정しちゃいなさい」
			;PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%의 精を絞り出そうと腰の動きを速めていった
			IF !RAND:2
				PRINTFORM 「
				CALL HEARTBW
			ELSE
				;레이무さんが부끄러워한다
				;레이무さんにとっては全編和姦。いいね？
				PRINTFORML 「어쩐지, 너를 무리하게 범하고 있는 「보고 싶다」로 부끄러워졌어요」
				PRINTFORMDL 일여기에 이르러%CALLNAME:TARGET%는 부끄럽게 여기기 움직임을 멈추어 버렸다
				;%CALLNAME:TARGET%의 질내に、%CALLNAME:MASTER%は정액を注ごうと必死に腰を突き上げた
			ENDIF
		;派生2,가게해줘あげない
		ELSE
			PRINTFORMW 「좀 더 연결되어 아프기 때문에, 참으며」
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT파생0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT파생に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「 이제 가고 싶다？무리하게 되어, 사정하고 싶다？」
		PRINTFORMW 「응후후…%RAND_SPLIT("아직 안 돼요/안돼")%」
		RETURN 1
	ENDIF
ENDIF
;REACT103, 역강간
