;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 봉사系アクション구상ファイル
;
;	봉사系アクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;봉사系 ACT50～57
;========================================================================
;・TFLAG:ACT(50=손으로 애무를 강요/51=입으로 애무를 강요/52=파이즈리/53=스마타/55=발핥기강제/56=이라마치오/57=가슴애무강제)
;・TFLAG:ACT분류 == 5に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "봉사")で拾う事が可能
;・죠교대상に、죠교자の身体を애무するように命令する
;・基本的には命令なので、この段階では従うかどうか確定していない(ACT56,이라마치오は거부できない)
;-------------------------------------------------
;・ACT54,발コキは12/04/08版で粛清
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では구상側のRESULT(終端のRETURN 1など)によって구상が表示당했다のか否か、
;・また箇所毎の特有の挙動(미약等구상をスキップなど)を制御しています
;・@구상記入チェックの場合代入処理を省略できますが、RESULT値は구상側で決めうちする필요があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------

;────────────────────────────────────
;ACT50, 손으로 애무를 강요
;────────────────────────────────────
;・애무強制/수음強制/페니스밴드수음強制
;・죠교대상(MASTER)に、손による애무를 강요する
;・죠교자に페니스があると必ず수음強制になる(페니스があると통상の애무強制は起こらない)
;・죠교자が페니스밴드장착中は페니스밴드수음強制になる
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"하드", "이상"だと発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP(밧줄/삼각목마)장착中には発生しない
;・성교중/성교봉사중には発生しない
;・죠교대상が従うか否かはこの時点では未定
;・REACT분류が거부/날뛴다/逃げるの場合は拒絶扱いとなる
;-------------------------------------------------
@KOJO_ACT_K1_50
;-------------------------------------------------
;・TFLAG:ACT파생(0=애무強制/1=수음強制/2=페니스밴드수음強制)
;-------------------------------------------------
;・地の文はACT파생毎に分岐。各ACT파생内では分岐なし
;・ACT파생0,애무の時のみ쾌Ｂ/쾌ＡSOURCE増加
;・その他の場合は쾌Ｃ限定
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50, 初回
;━━━━━━━━━━━━━━━━━━━━━━━━
;この構造で初回分岐に落ちたら派生に関わらず一度きりの表示
;派生毎に初回分岐を設けたいなら各ACT파생内で処理する事をお勧めします
;FIRSTACT(0)/FIRSTACT(1)/FIRSTACT(2)のように分けてやれば派生毎に初回を参照できます
;-------------------------------------------------
IF 구상記入チェック(100) && FIRSTACT(0)
	;=================================================
	;ACT파생1, 수음強制
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT파생2, 페니스밴드수음強制
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;上層の分岐を埋めた場合はTFLAG:ACT파생 == 0と同義となる
	;ここだけで済ませるなら、汎用的に使えるよう留意すべき箇所
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「이봐요, 네가%RAND_SPLIT("하는거야/해？/해 줘")%」
		PRINTFORML 「분명하게 기분 좋게 해 주세요？너이니까 손대게 하기 때문에」
		SIF IS_CONDITION(TARGET, "정욕")
			PRINTFORML 「%RAND_SPLIT("이봐요, 빨리/빨리, 빨리 해/부탁")%」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_0, 애무強制「自分の성기を손으로 애무しろと命令する」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;죠교자がイキそう
	;=================================================
	IF 구상記入チェック(0) && TCVAR:TARGET:가버려
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「이봐요, 너의 차례야」
		PRINTFORML 「상냥하게 해 주세요？확실히 해 주지 않으면 다음에 아프기 때문에」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_1, 수음強制「自分の남성기を손で扱けと命令する」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	;=================================================
	;죠교자がイキそう
	;=================================================
	IF 구상記入チェック(0) && TCVAR:TARGET:가버려
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_2, 페니스밴드수음強制「페니스밴드を손で掴んで動かせと命令する」
;━━━━━━━━━━━━━━━━━━━━━━━━
;페니스밴드への애무は쾌ＣSOURCEが減少し、쾌ＶSOURCEが増加する
;장착している時点で대상が처녀である可能性はない
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
	;=================================================
	;죠교자がイキそう
	;=================================================
	IF 구상記入チェック(0) && TCVAR:TARGET:가버려
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT51, 입으로 애무를 강요
;────────────────────────────────────
;・クンニ/펠라チオ/안면승마/안면승마항문/
;・죠교대상に、입による애무를 강요する
;・죠교자が후타나리の場合、죠교대상が누움なら1/2で안면승마、そうでなければ펠라強制になる
;・죠교자に페니스がなく、죠교대상が누움なら안면승마になる
;・안면승마が選ばれていたとき、안면승마항문に변화することがある
;・全ての条件から漏れた場合は통상のクンニとなる
;・TFLAG:징계・플래그が立っていると発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP(볼개그/삼각목마)장착中は発生しない
;・죠교자が페니스밴드を장착している場合は発生しない
;・성교중/성교봉사중には発生しない
;・REACT분류が거부/날뛴다/逃げるの場合は拒絶扱いになる
;-------------------------------------------------
@KOJO_ACT_K1_51
;-------------------------------------------------
;・TFLAG:ACT파생(0=クンニ/1=펠라/2=안면승마/3=안면승마항문)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51, 初回
;━━━━━━━━━━━━━━━━━━━━━━━━
;とりあえずFIRSTACT(0)で最上層に初回を配置しま했다が、各ACT파생内で処理するのも良いでしょう
;FIRSTACTの이벤트번호を被らないようにすれば、派生毎に初回判定ができます
;-------------------------------------------------
IF 구상記入チェック(100) && FIRSTACT(0)
	;=================================================
	;ACT파생が펠라強制
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT파생が안면승마 69
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT파생 == 2
		PRINTFORMW 「…하」
		;言われるまでやらない消極的なЯ당신
		PRINTFORML 「이군요？너도 하며」
		PRINTFORMW 「짠다」
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT파생が안면승마항문
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;上層の分岐を全て埋めた場合はクンニ強制扱いになる
	;派生毎に書き分けないのであれば、この箇所を汎用的に使える内容に했다上で不要な分岐を削除すると良いでしょう
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
		[SKIPSTART]
		;IF TALENT:TARGET:처녀
		;	PRINTFORMW 
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		[SKIPEND]
;OriginalString : 인4, "くぱぁ")
		CALL FADEIN(4, "")
		PRINTFORMW 「이봐요, 그…알겠죠？」
;OriginalString : 아웃4, "「그…」")
		CALL FADEOUT(4, "「그…」")
;OriginalString : 인8, "「처음이니까…」")
		CALL FADEIN(8, "「처음이니까…」")
		PRINTFORMW 「제대로 풀어줘…」
		;ペニバン처녀상실に対応する内容にしないといけない。双頭ペニバン禁止했다方が楽できそう
		PRINTFORMDW %CALLNAME:TARGET%는 남자를 모르는 비소를 %CALLNAME:MASTER%의 전에 드러냈다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상がＣを責められている
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상のＣを責めつつ안면승마になるシチュエーション
;안면승마になる場合は죠교자의 페니스、페니스밴드等を考慮する필요なし
;필요に応じて
;IF TEQUIP:페니스밴드
;ELSEIF PENIS(MASTER)
;ELSE
;等の分岐を設けて使用して下さい
;TEQUIP:죠교대상Ｃ사용 == 1は손으로 애무中
;TEQUIP:죠교대상Ｃ사용 == 2は입으로 애무中となります
;一応입と손それぞれの分岐を設けま했다が
;죠교자の素質によっては입으로 애무が殆ど出なかったりするので、そのあたりを見極めた上で分岐を増減して下さい
;-------------------------------------------------
IF 구상記入チェック(100) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2) > 0
	;=================================================
	;질내사정後3ターン以内		膣입からは先程たっぷりと搾り取った정액がどろりとあふれでている…
	;=================================================
	IF 구상記入チェック(0) && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
		;입으로 애무中
		IF TEQUIP:죠교대상Ｃ사용 == 2
			PRINTFORMW 
		;손으로 애무中
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마항문派生			目の前にひくつく菊門がさらけ出されている……
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 3
		;입으로 애무中
		IF TEQUIP:죠교대상Ｃ사용 == 2
			PRINTFORMW 
		;손으로 애무中
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;통상の안면승마を想定。@VAGINA_DESCRIPTIONは描写系関数
	;=================================================
	;기타・조건없음			無防備な%VAGINA_DESCRIPTION(TARGET)%がさらけ出されている……
	;=================================================
	IF 구상記入チェック(100)
		;입으로 애무中
		IF TEQUIP:죠교대상Ｃ사용 == 2
			PRINTFORMW （자지 빨아 젖어 와 버린다의, 알아버릴까 해들？）
		;손으로 애무中
		ELSE
			IF !RAND:3
				PRINTFORMW 「여기는 맡아 주기 때문에, 너도 해 주세요」
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%(으)로부터의 \@ AFFECTION("상사상애") ? 애정 # 자극 \@(을)를 요구하는 것처럼 태퇴와 태퇴도 글자도 자득이군요들 키라고 있다…
		PRINTFORML 
			ELSEIF !RAND:2
				;레이무さんの기분좋게 해！
				PRINTFORMW 「이봐요…%CALLNAME:TARGET%의 여기도, 기분 좋게 해…」
				PRINTFORMDW %CALLNAME:TARGET%의 비소는 안쪽부터 젖어와 %CALLNAME:MASTER%의 봉사를 기다려 애태우고 있다…
			ELSE
				PRINTFORMW 「반찬이 있는 것이 좋아요」
				PRINTFORMW 「%CALLNAME:TARGET%의 보지 반찬으로 해, 기분 좋아져버릴 없음 있고」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_0, クンニ強制
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;죠교자がイキそう
	;=================================================
	IF 구상記入チェック(0) && TCVAR:TARGET:가버려
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「…빨리 해 주세요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_1, 펠라チオ 주인님にしゃぶれよされる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	;=================================================
	;죠교자がイキそう
	;=================================================
	IF 구상記入チェック(0) && TCVAR:TARGET:가버려
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_2, 안면승마
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
	;=================================================
	;죠교자がイキそう
	;=================================================
	IF 구상記入チェック(0) && TCVAR:TARGET:가버려
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;질내사정後3ターン以内
	;=================================================
	IF 구상記入チェック(0) && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「\@ ALI(0) ? 이봐요, # 날뛰지 않고, \@확실히 날름날름 해 주세요」
		PRINTFORMW 「너는 나의 엉덩이에 깔리고 있는 것이 어울려요」
		[SKIPSTART]
		;分岐条件が필요
;OriginalString : 아웃2, "「ふっ…」")
		CALL FADEOUT(2, "「훗…」")
;OriginalString : 인4, "「んんっ…」")
		CALL FADEIN(4, "「응읏…」")
		;레이무のちんぽだからだよ（ｷﾘｯ）と言って欲しい후타나리少女の乙女心の補強
		PRINTFORMW 「\@ TALENT:MASTER:남자 ? 남자의 주제에 #  \@응 빨게 해달라고,%RAND_SPLIT("어째서 그렇게 기쁜 듯한 것이야/무엇 기쁜듯이 하고 있는거야")%」
		[SKIPEND]
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_3, 안면승마항문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && TFLAG:ACT파생 == 3
	;쾌Ｃ以外はTCVAR:가버려では拾えないのでPALAMで
	;=================================================
	;죠교자の쾌Ａが一定以上「가버려」
	;=================================================
	IF 구상記入チェック(0) && PALAM:쾌Ａ >= 8500
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW （그쪽이 아니어요. 바보！）
		PRINTFORMDL %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 준비를 갖추면서, 안타까운 듯이  허리를 상하시켰다
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT52, 파이즈리
;────────────────────────────────────
;・죠교대상に파이즈리での봉사를 강요する
;・죠교대상が남자の場合は発生しない
;・죠교자に페니스がない場合は発生しない
;・턴방침が"이상"の場合は発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP(밧줄/삼각목마)장착中には発生しない
;・성교중/성교봉사중には発生しない
;・REACT분류が거부/날뛴다/逃げるの場合は拒絶扱いとなる
;-------------------------------------------------
@KOJO_ACT_K1_52
;-------------------------------------------------
;・ACT파생なし。地の文に分岐なし。@TARGET_DESCRIPTIONによる状況描写が入る可能性あり
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT52, 파이즈리 당신がおっぱい穴役
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자がイキそう
	;=================================================
	IF 구상記入チェック(0) && TCVAR:TARGET:가버려
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;죠교대상のＢ절정が近いというシチュエーション
	;=================================================
	;죠교대상の쾌Ｂが一定以上
	;=================================================
	IF 구상記入チェック(0) && PALAM:MASTER:쾌Ｂ >= 8500
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상が거유
	;=================================================
	IF 구상記入チェック(0) && TALENT:MASTER:거유
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상が빈유
	;=================================================
	IF 구상記入チェック(0) && TALENT:MASTER:빈유
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;기타・조건없음
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「이렇게 크기 때문에 사용하지 않으면 아까워요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT53, 스마타
;────────────────────────────────────
;・죠교대상に페니스밴드、もしくは페니스를股に挟んで擦れと命令する
;・죠교자に棒がない(페니스/페니스밴드)場合は発生しない
;・죠교대상に페니스がある場合は発生しない
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"이상"の場合は発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP:삼각목마장착中には発生しない
;・성교중/성교봉사중には発生しない
;・죠교대상に발がないと発生しない(現状では屠自古が죠교대상である場合のみと考えられる)
;・REACT분류が거부/날뛴다/逃げるである場合は拒絶扱い
;-------------------------------------------------
@KOJO_ACT_K1_53
;-------------------------------------------------
;・TFLAG:ACT파생(0=스마타/1=페니스밴드스마타)
;-------------------------------------------------
;・안면승마系、자위系は解除される
;・地の文はACT파생で分岐。派生内では分岐なし。@TARGET_DESCRIPTIONによる状況描写が入る可能性あり
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;처음
;━━━━━━━━━━━━━━━━━━━━━━━━
;取り敢えず最上層に配置しま했다が、ACT파생内で処理してもいいと思います
;FIRSTACTの引数を揃えるか、FIRSTACT(0), FIRSTACT(1)として個別に初回判定を拾うかは書き손次第
;-------------------------------------------------
IF 구상記入チェック(0) && FIRSTACT(0)
	;=================================================
	;페니스밴드스마타強制
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;TFLAG:ACT파생 == 1の分岐を書いた場合はACT파생 == 0(스마타強制)扱いとなる
	;上層の分岐を방치してここだけにするなら、汎用的に使えるよう留意すべき箇所
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_0, 스마타強制
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자に페니스がある場合
;-------------------------------------------------
IF 구상記入チェック(0) && !TFLAG:ACT파생
	[SKIPSTART]
	;この分岐はバリアントに存在しません
	;처음かつ당신が남자の場合
	PRINTFORMW 「어？미안해요. 조금 기다리며」
	PRINTFORML 때굴때굴
	PRINTFORMW 때굴때굴
	PRINTFORML 「들어가지 않네요…」
	PRINTFORMDL %CALLNAME:TARGET%는 야오이구멍에 삽입하려고 귀두를 %CALLNAME:MASTER%의 좁은 산길에 꽉 누르면서 불가사의 그렇게 고개를 갸웃하고 있다…	
	[SKIPEND]
	;=================================================
	;イキそう
	;=================================================
	IF 구상記入チェック(0) && TRUECHECK(TCVAR:TARGET:가버려, TCVAR:MASTER:가버려) > 0
		;죠교자(TARGET)が가버려
		IF TCVAR:TARGET:가버려
			PRINTFORMW 
		;죠교대상(MASTER)が가버려
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		;윤활十分
		IF PALAM:MASTER:윤활 >= 500
			PRINTFORMW 
		;윤활不발
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_1, 페니스밴드스마타強制
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자が페니스밴드장착中の場合
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	;=================================================
	;イキそう
	;=================================================
	IF 구상記入チェック(0) && TRUECHECK(TCVAR:TARGET:가버려, TCVAR:MASTER:가버려) > 0
		;죠교자(TARGET)が가버려
		IF TCVAR:TARGET:가버려
			PRINTFORMW 
		;죠교대상(MASTER)が가버려
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		;윤활十分
		IF PALAM:MASTER:윤활 >= 500
			PRINTFORMW 
		;윤활不발
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT55, 발핥기강제
;────────────────────────────────────
;・죠교자が발를 핥아ろと命令してくる
;・이상系から発生する場合あり
;・턴방침が"소프트"だと発生しない
;・本日の방침が"イチャイチャ"だと発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP(볼개그/삼각목마)장착中には発生しない
;・성교중/성교봉사중には発生しない
;・죠교자に발がない場合は発生しない
;・REACT분류が거부/날뛴다/逃げるの場合は拒絶扱い
;-------------------------------------------------
@KOJO_ACT_K1_55
;-------------------------------------------------
;・地の文に分岐なし。@TARGET_DESCRIPTIONで状況描写が入る可能性あり
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT55, 발핥기강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「자, 일단 남편으로서 얼마나 복종하는지 볼까?」
		PRINTFORMW 「어차피라면 겨드랑이를 빨고 싶어？ 안돼요♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;전회COMが許しを乞った
	;=================================================
	IF 구상記入チェック(0) && IS_PREVCOMNAME("용서를 빌다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;금일의방침が징계・の場合は징계・플래그が立っ하며 になるので確率表示、もしくはONCEACTを使用することを推奨
	;=================================================
	;징계・모드
	;=================================================
	IF 구상記入チェック(0) && TFLAG:징계・플래그 ;&& ONCEACT(0)
		PRINTFORMW 「진심을 보여준다면...」
		PRINTFORMW 「조금 봐줄수도 있는데?」
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상が누움
	;=================================================
	IF 구상記入チェック(0) && TFLAG:누움
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자が신발안を履いている
	;(デフォルトで신발안履いていないキャラは表示されない)
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:신발안
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 「남편으로서의 성의를 보여줘」
		PRINTFORMW 「그러면 기분 좋은걸 해줄게♪」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT56, 이라마치오
;────────────────────────────────────
;・命令するのではなく、強引に페니스를입に突っ込む
;・죠교대상の反応は未定だが、拒絶する事はできない
;・가학系から発生する場合あり
;・턴방침が"노멀", "소프트"だと発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP(볼개그/삼각목마)장착中には発生しない
;・성교중/성교봉사중には発生しない
;・죠교자に페니스がない場合は発生しない(페니스밴드による이라마치오は無い)
;-------------------------------------------------
@KOJO_ACT_K1_56
;-------------------------------------------------
;・자위중であれば解除される
;・地の文に分岐なし。地の文は↓
;・%CALLNAME:TARGET%は%CALLNAME:MASTER%의 頭をつかんで、乱暴に%CALLNAME:MASTER%의 喉まで突き入れた…
;-------------------------------------------------
;・죠교자の발기도合いはTCVAR:발기で参照できる
;・TCVAR:발기(0=まだ柔らかい/1=徐々に硬度を増하면서ある/2=발기했다/2以上=反り返った)的な感じで
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT56, 이라마치오
;━━━━━━━━━━━━━━━━━━━━━━━━
;필요に応じて分岐を増減して下さい
;-------------------------------------------------
IF 구상記入チェック(0) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자がイキそう
	;=================================================
	IF 구상記入チェック(0) && TCVAR:TARGET:가버려
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상が누움
	;=================================================
	IF 구상記入チェック(0) && TFLAG:누움
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT57, 가슴애무강제
;────────────────────────────────────
;・죠교대상に、죠교자の가슴を애무するよう強制する
;・죠교자が남자の場合は発生しない
;・TFLAG:징계・플래그が立っていると発生しない
;・턴방침が"이상"だと発生しない
;・밀어넘어지는중には発生しない
;・TEQUIP:밧줄장착中は입で가슴애무강제に派生する。ただし볼개그を장착中の場合は弾かれる
;・볼개그がなければ、1/2の確率で입으로 애무になる
;・REACT분류が거부/날뛴다/逃げるの場合は拒絶扱い
;・PALAMが치정先行か반항先行かで"恥ずかしくて봉사できなかった/嫌がって要求に応じなかった"に派生する
;-------------------------------------------------
@KOJO_ACT_K1_57
;-------------------------------------------------
;・TFLAG:ACT파생(0=손で가슴애무강제/2=입で가슴애무강제)
;-------------------------------------------------
;・地の文はACT파생で分岐。派生内では分岐なし
;・@TARGET_DESCRIPTIONによる状況描写が入る可能性あり
;・안면승마は解除される
;・REACTにおいて、누움でなく、ACT파생が입으로 애무でない場合、1/2で後ろから애무に派生する
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;처음
;━━━━━━━━━━━━━━━━━━━━━━━━
;取り敢えず最上層に配置しま했다が、ACT파생内で処理してもいいと思います
;FIRSTACTの引数を揃えるか、FIRSTACT(0), FIRSTACT(1)として個別に初回判定を拾うかは書き손次第
;-------------------------------------------------
IF 구상記入チェック(100) && FIRSTACT(0)
	;=================================================
	;ACT파생2, 입で가슴애무를 강요
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT파생 == 2
		PRINTFORMW 「과연 부끄러울까？」
		PRINTFORMW 「 나는, 몹시 부끄러워」
		RETURN 1
	ENDIF
	
	;TFLAG:ACT파생 == 2分岐を埋めた場合は!TFLAG:ACT파생分岐として機能
	;ここだけで済ませるのであれば、どちらでも使える内容を
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「가슴, 만져줘」
		PRINTFORML 「그렇게 작은 것도 아니지만」
		PRINTFORMW 「지금부터, 네가 크게 해 줘？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_0, 손で가슴애무를 강요
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;以前は体位毎によっては規制されていたが、現在では全ての体位で発生する
	;場合によっては無茶な体制になるが、これは現状では仕様ということで
	;=================================================
	;죠교자삽입중
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:성교중
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상삽입중
	;=================================================
	IF 구상記入チェック(100) && TEQUIP:성교봉사중
		;PRINTFORML 「はい」
		PRINTFORMDW 에
		PRINTFORMW 「………」
		PRINTFORMW 「해？」
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자がイキそう
	;=================================================
	IF 구상記入チェック(0) && PALAM:쾌Ｂ > 8500
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「너에게 손대어진다고 생각하면…두근두근 해, 나쁘지 않아」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_2, 입で가슴애무를 강요
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:ACT파생 == 2
	;以前は体位毎によっては規制されていたが、現在では全ての体位で発生する
	;場合によっては無茶な体制になるが、これは現状では仕様ということで
	;=================================================
	;죠교자삽입중
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:성교중
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상삽입중
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:성교봉사중
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자がイキそう
	;=================================================
	IF 구상記入チェック(0) && PALAM:쾌Ｂ > 8500
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「가슴, 입이라서」
		PRINTFORMW 「잘 할 수 있으면…머리 어루만져 주어요」
		PRINTFORMW 「가끔 씩은？응석부리게 해 주어요」
		RETURN 1
	ENDIF
ENDIF
