;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 가학系アクション구상ファイル
;
;	가학系アクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;가학系 ACT60～68
;========================================================================
;・TFLAG:ACT(60=스펭킹/61=채찍/62=바늘/63=밧줄/64=눈가리개/65=볼개그/66=매도/67=삼각목마/68=관장세트)
;・TFLAG:ACT분류 == 6に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "가학")で参照する事が可能
;・TEQUIP系は基本、ACT開始時点で장착される
;・ACTによっては죠교대상の反応次第で취소される事がある
;・밧줄/눈가리개/볼개그/삼각목마はTEQUIPで관리され当該ACT/REACT以外でもTEQUIP:밧줄等で参照できる
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では구상側のRESULT(終端のRETURN 1など)によって구상が表示당했다のか否か、
;・また箇所毎の特有の挙動(미약等구상をスキップなど)を制御しています
;・@구상記入チェックの場合代入処理を省略できますが、RESULT値は구상側で決めうちする필요があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------

;────────────────────────────────────
;ACT60, 스펭킹
;────────────────────────────────────
;・죠교자が죠교대상の尻を平손で叩く
;・ACT開始時点ですでに叩いている
;・성교봉사중には発生しない
;・안면승마系は解除される
;・죠교자が자위중である場合は解除される
;-------------------------------------------------
@KOJO_ACT_K1_60
;-------------------------------------------------
;・ACTの派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT60, 스펭킹
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文に分岐なし
;・@TARGET_DESCRIPTIONで状況描写が入る可能性あり
;-------------------------------------------------
;序盤に多発する為加筆を要する
IF 구상記入チェック(100) && !TFLAG:ACT파생
	[SKIPSTART]
	各分岐内にABL:MASTER:마조끼やABL:MASTER:고통중독等で派生を設けるのも良いと思います
	デフォルトで用意すると無理に埋めにかかる例が割りと良くみられるので、テンプレとしてはあえて設けません
	필요に応じて拡張して下さい
	
	SELECTCASE ABL:MASTER:마조끼
		;마조끼6以上
		CASE IS > 6
			PRINTFORMW 
		;마조끼以上
		CASE IS > 4
			PRINTFORMW 
		;その他
		CASEELSE
			PRINTFORMW 
	ENDSELECT
	
	とか、こんな感じで。確率も絡めて分けるなら…
	IF ELSEIFでABL:MASTER:마조끼 > 6 && !RAND:2みたいなやり方でもいいかと
	単に派生시킨다だけならIF ELSEIFよりSELECTCASEの使用が望ましいです
	[SKIPEND]
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「우선, 두드리면 무엇인가 나오겠죠…」
		PRINTFORMW 「어머나, 좋은 소리군요♪」
		RETURN 1
	ENDIF
	
	;금일의방침が징계・の場合はここにしか落ちなくなるので
	;구상記入チェック(20)などとして確率を絞るのを推奨
	;=================================================
	;징계・発動中
	;=================================================
	;頻発するとの事なので確立低下
	IF 구상記入チェック(20) && TFLAG:징계・플래그
		PRINTFORMW 「이것은 오시오키니까, 그렇게 기쁜듯이 되어도 곤란해」
		RETURN 1
	ENDIF
	
	;TEQUIP:죠교대상Ｃ사용 == 2（입による애무）は考慮しない
	;=================================================
	;Ｃ애무継続中
	;=================================================
	IF 구상記入チェック(100) && TEQUIP:죠교대상Ｃ사용 == 1
		;수음継続中
		IF PENIS(MASTER)
			PRINTFORMW 「두드리면 무엇인가 나오는 것일까？…아라 아라…！」
			PRINTFORMW 「…이상한 소리 밖에 나오지 않네요」
			;そう。스펭킹は少し連打されやすすぎる。
			SIF !RAND:5
				PRINTFORMDW %RAND_SPLIT("벌꿀병인가/새전함인가/저금통인가")% 다른 것으로 착각 하지 않는가…그런 의문이 뇌리를 지난다
			;貯金箱は上下にゆっさゆっさするものな気がしてきた
		;애무継続中
		ELSE
			RETURN 0
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		SELECTCASE RAND:3
			CASE 0
				PRINTFORML 「너는 이불같구나」
				;ある程度関係が進んでいる事
				SIF AFFECTION("익애", TARGET)
					PRINTFORML 「처음은 차갑고, 감싸줘서, 따뜻하게 되고, 나를, 놓아 주지 않아서…」
				PRINTFORMW 「잡고 두드리면 기쁜거야？」
;OriginalString : 아웃2, "くすっ")
				CALL FADEOUT(2, "키득")
			CASE 1
				PRINTFORM 「엉덩이에 깔리는 것도, 얻어맞는 것도 좋아하다니
				;하쿠레이座布団
				PRINTFORM … 너는,
				PRINTFORMW  방석에 불과해」
			CASEELSE
				PRINTFORML 「뭐야 엉덩이로 기뻐해」
				;棘がありすぎる
				;PRINTFORMW 「%RAND_SPLIT("ひいひい/はひはひ/はひんはひん")%言っちゃって、気味が悪いわね」
				PRINTFORMW 「%RAND_SPLIT("낑낑/앙앙/하앙")% 거리는거, 기분 나빠」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT61, 채찍
;────────────────────────────────────
;・죠교자が채찍を大きく振り上げ思いっきり振り下ろす
;・ACT開始時点ですでに叩いている
;・턴방침が"소프트"だと発生しない
;・성교중/성교봉사중には発生しない
;-------------------------------------------------
@KOJO_ACT_K1_61
;-------------------------------------------------
;・ACTの派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT61, 채찍
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文に分岐なし
;-------------------------------------------------
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;눈가리개장착
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:눈가리개
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;구속中
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:밧줄
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMDW %CALLNAME:TARGET%의 회전하는 불제봉에, %CALLNAME:MASTER%의 신비스러운 힘은 이기주의자와 깎아져 간다
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT62, 바늘
;────────────────────────────────────
;・乳首と성기に바늘を突き刺す
;・ACT開始時点ですでに刺している
;・죠교대상に페니스がある場合は페니스に、無い場合は클리토리스に突き刺す
;・가슴と성기(페니스/바기나)から出血し、血液汚れが追加される
;・턴방침が"소프트", "노멀"だと発生しない
;・성교중/성교봉사중には発生しない
;・안면승마系、자위系は解除される
;-------------------------------------------------
@KOJO_ACT_K1_62
;-------------------------------------------------
;・ACTの派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT62, 바늘
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文は페니스の有無で分岐。分岐内に派生なし
;-------------------------------------------------
;IF 구상記入チェック(0) && PENIS(MASTER)等で分ける손もあります。필요に応じて分岐を増減して下さい
;割りと書きにくい箇所であり、まともにやるのは손間なので\@ PENIS(MASTER) ? 페니스 # 클리토리스 \@のように三項を使うと楽できたりします
;-------------------------------------------------
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;눈가리개장착中
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:눈가리개
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「좀 더 간단하고 확실한 걸 써볼까…」
		;PRINTFORMW 「あんたの雑魚ちんぽなんて통상ショットで十分よ！」
		PRINTFORMW 「이런 조그만 \@ PENIS(MASTER) ? 자지 # 보지 \@따윈 이걸로 충분해」
		PRINTFORMW 「\@ PENIS(MASTER) ? 이 멸치 자지！！ #  \@」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT63, 밧줄
;────────────────────────────────────
;・죠교대상を밧줄で縛ろうとする
;・地の文は"縛ろうと했다…"となっているが処理的にはACT開始時点で縛られている
;・TEQUIP:밧줄 で縛りの強度を参照できる
;・既に装備していた場合は解除になる
;・既に장착済み、かつTFLAG:징계・플래그が立っている場合は発生しない(即ち、解除されない)
;・TEQUIP:밧줄自体はREACT분류が"용서를 빌다"の場合に解除される
;・0616版時点の仕様でこの挙動を確実に参照する손段なし。本体側の改善が求められる
;・(REACT63以外でも취소処理になる場合がある為)
;・성교중/성교봉사중には発生しない
;・구속時に자위や안면승마、죠교대상의 페니스사용중は解除される
;-------------------------------------------------
@KOJO_ACT_K1_63
;-------------------------------------------------
;・TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_0, 밧줄장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;・반항각인、죠교자죠교레벨、기교、긴박능숙素質に応じて구속SOURCE増加
;・구속SOURCEに応じて強度上昇
;-------------------------------------------------
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「박라고 엉덩이 보전라고 해 소리로 운다 라고 했었던 것은…-일이었는지 해들？」
		RETURN 1
	ENDIF
	
	;縛り強度は통상だと1か2程度と思われます
	;それ以上の縛り強度は반항각인や죠교자の긴박능숙素質+一定以上の기교が要求されるかと
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		;スペルカードカットインがないと物발りないかもしれない
		SELECTCASE TEQUIP:밧줄
			;縛り強度3、もしくはそれ以上
			CASE IS >= 3
				PRINTFORML 
				PRINTFORMDL 신기 「팔방룡 난투 장면」
				PRINTFORML 
			;縛り強度3
			CASE 2
				PRINTFORML 
				PRINTFORMDL 몽부 「이중결계」
				PRINTFORML 
			;その他
			CASEELSE
				PRINTFORML 
				PRINTFORMDL 몽부 「봉마진」
				PRINTFORML 
		ENDSELECT
		;ここではまだリターンしない
		
		;条件変更
		SELECTCASE AFFECTION("상사상애")
			CASE 1
				PRINTFORMW 「매너리즘은 부부의 적이야」
				PRINTFORMW 「너를, 움직일 수 없게 해…  %RAND_SPLIT("지금부터/우후후")%…」
				PRINTFORM 「%RAND_SPLIT("무리하게 범/무리하게 엣치/강간/강·간♪")%해 준다
				CALL HEARTBW
			CASEELSE
;OriginalString : 아웃4, "ごくっ…")
				CALL FADEOUT(4, "꿀꺽…")
				PRINTFORMW 「움직일 수 없는 너도…그…나쁘지 않아…」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
				[SKIPSTART]
				;この形では左に流れるように二行目も消えてしまう。調整
;OriginalString : 아웃4, "ごくっ…")
				CALL FADEOUT(4, "꿀꺽…")
				PRINTFORM 「움직일 수 없는 너도…
;OriginalString : 아웃4, "その…")
				CALL FADEOUT(4, "그…")
				PRINTFORMW 나쁘지 않아」
				[SKIPEND]
				[SKIPSTART]
				;%CALLNAME:TARGET%さんぽくない
				PRINTFORMW 「지금부터 어떻게 하셔버릴까」
				PRINTFORMW 「기대해도 좋아」
;OriginalString : 아웃8, "「もっとも…」")
				CALL FADEOUT(8, "「무엇보다…」")
				PRINTFORMW 「그 기대 거리에 갈까는, 모르는데…그런데？」
;OriginalString : 아웃4, "「う후후…」")
				CALL FADEOUT(4, "「우후후…」")
				
				;ドMな%CALLNAME:MASTER%に勝ち目はない。ってキャラ違う
				PRINTFORM 「이것으로 너는…후후…나의 물건이군요
				CALL HEARTBW
				
				;主導権が握られている事が強調できる。高圧的になり過ぎ
				PRINTFORMW 「이것으로 너는, 나로 되는 대로…」
;OriginalString : 아웃4, "「う후후…」")
				CALL FADEOUT(4, "「우후후…」")
				PRINTFORMW 「 나의 좋은 것 같게 시켜 주어요」
				PRINTFORMW 「안심해 주세요. 나쁘게는 하지 않아요…」
				[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_1, 밧줄解除
;━━━━━━━━━━━━━━━━━━━━━━━━
;밧줄解除に派生했다時点で、"TEQUIP:밧줄 = 0"となるので解除時に縛り強度は参照できない
;장착と強度で同じTEQUIPを使いまわしている為、このようになってしまう
;-------------------------------------------------
IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
	;ABL:MASTER:구속중독なりABL:MASTER:마조끼で派生を設けても良いかと思います
	;@RANDACTでランダム処理するのも良いでしょう
	;필요に応じて拡張して下さい
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMDW %CALLNAME:TARGET%는 천천히 부적를 벗겨 갔다
;OriginalString : 아웃4, "ぺりぺり")
		CALL FADEOUT(4, "")
;OriginalString : 아웃4, "ぺりぺり")
		CALL FADEOUT(4, "")
;OriginalString : 아웃4, "ぺりぺり")
		CALL FADEOUT(4, "")
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT64, 눈가리개
;────────────────────────────────────
;・죠교자が눈가리개を着けさせようとする
;・地の文では"着けさせようと했다…"となっているが処理上はACT開始時点で장착されている
;・既に装備していた場合は解除になる
;・既に장착済み、かつTFLAG:징계・플래그が立っている場合は発生しない(即ち、解除されない)
;・TEQUIP:눈가리개はREACT분류が"용서를 빌다"であった場合に解除される
;・0616版時点の仕様でこの挙動を確実に参照する손段なし。本体側の改善が求められる
;・(REACT64以外でも취소処理になる場合がある為)
;・성교중/성교봉사중には発生しない
;・안면승마系は解除される
;・죠교자が자위중であった場合は解除される
;-------------------------------------------------
@KOJO_ACT_K1_64
;-------------------------------------------------
;・TFLAG:ACT派性(0=장착/1=解除)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_0, 눈가리개장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;・죠교대상に페니스がある場合、마조끼に応じて발기도上昇
;-------------------------------------------------
;男性に눈가리개はマイナスな気がしてならない
;독심능력との相性は良い
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독なりABL:MASTER:마조끼で派生を設けても良いかと思います
	;@RANDACTでランダム処理するのも良いでしょう
	;필요に応じて拡張して下さい
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMDW 박려의 부적가 물리적으로 %CALLNAME:MASTER%의 시야를 차지한다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_1, 눈가리개解除
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMDW %CALLNAME:TARGET%는 천천히 부적를 벗겨 갔다
;OriginalString : 아웃4, "ぺりぺり")
		CALL FADEOUT(4, "")
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT65, 볼개그
;────────────────────────────────────
;・죠교자が볼개그を嵌めさせようとする
;・地の文では"嵌めさせようと했다…"となっているが処理上はACT開始時点で장착されている
;・既に装備していた場合は解除になる
;・既に장착済み、かつTFLAG:징계・플래그が立っている場合は発生しない(即ち、解除されない)
;・TEQUIP:볼개그はREACT분류が"용서를 빌다"であった場合に解除される
;・0616版時点の仕様でこの挙動を※確実に参照する손段なし。本体側の改善が求められる
;・※REACT64以外でも취소処理になる場合がある為
;・턴방침が"소프트", "노멀"の場合は発生しない
;・성교중/성교봉사중には発生しない
;・안면승마系は解除される
;・죠교자が자위중であった場合は解除される
;-------------------------------------------------
@KOJO_ACT_K1_65
;-------------------------------------------------
;・TFLAG:ACT派性(0=장착/1=解除)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_0, 볼개그장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;・죠교대상に페니스がある場合、마조끼に応じて발기도上昇
;-------------------------------------------------
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독なりABL:MASTER:마조끼で派生を設けても良いかと思います
	;@RANDACTでランダム処理するのも良いでしょう
	;필요に応じて拡張して下さい
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMDW %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 입에 속옷을 념 글자 붐비어 왔다…
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_1, 볼개그解除
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMW 「구를 차지하면 어딘지 부족하네요」
		PRINTFORMW 「--울면서 고개를 젓는 너도 사랑스럽지만, 역시」
		PRINTFORMW 「너의 말을 (듣)묻고 싶어요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT66, 매도
;────────────────────────────────────
;・挑発するように죠교대상の痴態を声に出す
;・회화系から発生する場合もある
;・턴방침が"소프트"だと発生しない
;・회화系から派生する場合もある
;-------------------------------------------------
@KOJO_ACT_K1_66
;-------------------------------------------------
;・ACTの派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT66, 매도
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文に分岐なし。@TARGET_DESCRIPTIONによる状況描写が入る可能性あり
;-------------------------------------------------
IF 구상記入チェック(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「바보」
		;地の文との擦り合わせの為軌道修正
		PRINTFORMDW %CALLNAME:TARGET%의 매도는 요령을 얻지 않았다
		RETURN 1
	ENDIF
	
	;=================================================
	;성교중/성교봉사중
	;=================================================
	IF 구상記入チェック(0) && TRUECHECK(TEQUIP:성교중, TEQUIP:성교봉사중) > 0
		;성교중
		IF TEQUIP:성교중
			PRINTFORMW 
		;성교봉사중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;자위중
	;=================================================
	IF 구상記入チェック(100) && TRUECHECK(GETBIT(TEQUIP:자위중, 0), GETBIT(TEQUIP:자위중, 1)) > 0
		;죠교대상が자위중
		IF GETBIT(TEQUIP:자위중, 0)
			;シンプルな方が刺さる
			PRINTFORMW 「………」
;OriginalString : 아웃8, "「くすっ」")
			CALL FADEOUT(8, "「키득」")
			PRINTFORMW 「」
		;죠교자が자위중
		ELSE
			RETURN 0
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;Ｃ애무継続中
	;=================================================
	IF 구상記入チェック(0) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2)
		;수음・애무継続中
		IF TEQUIP:죠교대상Ｃ사용 == 1
			PRINTFORMW 
		;펠라・クンニ継続中
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		;長いので分割
		SELECTCASE RAND:7
			CASE 0
				PRINTFORML 「변태」
				PRINTFORML 「케다모노」
				PRINTFORML 「색이상」
			CASE 1
				PRINTFORML 「여자도 참 해」
				PRINTFORML 「여섯개 다람쥐」
				PRINTFORMW 「로리타」
			CASE 2
				PRINTFORML 「겨드랑이 페티시즘」
				PRINTFORML 「제페티시즘」
				PRINTFORMW 「태퇴페티시즘」
			CASE 3
				PRINTFORML 「돌팔이」
				PRINTFORML 「치킨 폭탄」
				PRINTFORMW 「간편 슈터」
			CASE 4
				PRINTFORML 「우유부단」
				PRINTFORML 「고혈이상」
				PRINTFORMW 「다리 페티시즘」
			CASE 5
				PRINTFORML 「바지런한 기질 없음」
				PRINTFORML 「둔감」
				PRINTFORMW 「바보 주인」
			CASEELSE
				PRINTFORML 「조루」
				PRINTFORML 「포경」
				PRINTFORML 「새끼 손가락」
				PRINTFORMW 「조치응」
		ENDSELECT
		PRINTFORML
		PRINTFORMW 「음…그 밖에 무엇이 있었는지 해들…」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT67, 삼각목마
;────────────────────────────────────
;・죠교대상を木馬に乗せようとする
;・地の文は"乗せようと했다…"となっているが処理上はACT開始時点で乗せられている
;・実は쾌Ｃではなく쾌Ｖが入る(生えてる木馬！？)
;・MASTERが처녀なら、처녀상실が起こる(@ACTION_APPLY2で処理。REACT분류が용서를 빌다の場合は回避される)
;・죠교대상が남자の場合は発生しない
;・턴방침が"소프트", "노멀"時には発生しない
;・TEQUIP(바이브/항문바이브/항문비즈/관장그릇＋플러그/페니스밴드)장착時には発生しない
;・성교중/성교봉사중には発生しない
;・야외플레이時には発生しない
;・밀어넘어지는중には発生しない
;・이상系から発生する場合もある
;・既に装備していた場合は解除になる
;・既に장착済み、かつTFLAG:징계・플래그が立っている場合は発生しない(即ち、解除されない)
;・TEQUIP:삼각목마はREACT분류が"용서를 빌다"であった場合に解除される
;・0616版時点の仕様でこの挙動を※確実に参照する손段なし。本体側の改善が求められる
;・※REACT67以外でも취소処理になる場合がある為
;-------------------------------------------------
@KOJO_ACT_K1_67
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=解除)
;-------------------------------------------------
;죠교대상が남자の場合は発生しない쾌Ｖが入る木馬ってそりゃねーよ
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_0, 삼각목마장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT파생で分岐。派生内では分岐なし
;-------------------------------------------------
IF 구상記入チェック(0) && !TFLAG:ACT파생
	;地の文↓
	;%CALLNAME:TARGET%は%CALLNAME:MASTER%を木馬に乗せようと했다…
	
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_1, 삼각목마解除
;━━━━━━━━━━━━━━━━━━━━━━━━
;TEQUIP취소の際にはここで拾える訳でない事に注意
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	
	;地の文↓
	;%CALLNAME:TARGET%は%CALLNAME:MASTER%を木馬から降ろしてやった…
	
	;처음とはいっても、過去にTEQUIP취소や解除されないまま죠교終了であった可能性もある
	;=================================================
	;처음
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT68, 관장세트
;────────────────────────────────────
;・관장液の注入/追加/排出
;・最初の注入時(ACT파생=0)は量1～3。追加時(ACT파생=2)は量1～4。それぞれランダムで処理される
;・;既に装備している場合、관장液追加になる
;・관장液が5本入っている場合、관장プラグ解除になる
;・관장液の分量はTEQUIP:관장그릇＋플러그で参照できる
;・TEQUIP(항문바이브/항문비즈/페니스밴드/삼각목마)장착時には発生しない
;・성교중/성교봉사중には発生しない
;・턴방침が"소프트"だと発生しない
;・도구系から派生する場合もある
;-------------------------------------------------
@KOJO_ACT_K1_68
;-------------------------------------------------
;・TFLAG:ACT파생(0=장착/1=解除/2=관장液追加)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_0, 관장그릇＋플러그장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT파생によって分岐。派生内では分岐なし
;-------------------------------------------------
IF 구상記入チェック(0) && !TFLAG:ACT파생
	
	;地の文↓
	;%CALLNAME:TARGET%は%TARGET_DESCRIPTION("하며、")% %CALLNAME:MASTER%의 菊穴に
	;관장液を流し込み、栓を했다…
	
	;ACT開始時点で液は注入されており、TEQUIP:관장그릇＋플러그で液量を参照する事が可能
	
	;=================================================
	;처음「注入」
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;仕様が変わる可能性を考慮して == ではなく >= としておく
	;=================================================
	;液量3
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:관장그릇＋플러그 >= 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;液量2
	;=================================================
	IF 구상記入チェック(0) && TEQUIP:관장그릇＋플러그 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;液量の分岐を全て埋めた場合はTEQUIP:관장그릇＋플러그 == 1に相当
	;個別に分けずにSELECTCASE TEQUIP:관장그릇＋플러그でまとめてしまう事も可能
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_1, 관장그릇＋플러그解除
;━━━━━━━━━━━━━━━━━━━━━━━━
;ここに派生する時点で液量は5になっている
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
	
	;地の文↓
	;%CALLNAME:TARGET%は%TARGET_DESCRIPTION("하면서、")% %CALLNAME:MASTER%から
	;肛門栓を抜いて、장내の汚物を肛門から撒き散ら시켰다…
	
	;=================================================
	;처음「排出」
	;=================================================
	IF 구상記入チェック(0) && FIRSTACT(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_2, 관장液追加
;━━━━━━━━━━━━━━━━━━━━━━━━
;既に注入済みの場合にこの箇所に派生
;-------------------------------------------------
IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
	
	;地の文↓
	;%CALLNAME:TARGET%は%TARGET_DESCRIPTION("하면서、")% %CALLNAME:MASTER%의 항문に
	;さらに관장液を注入しようと했다…
	
	[SKIPSTART]
	液量で書き分けるならコメントアウトを解除
	下層のPRINTFORMWを削除して下さい
	;관장液分量派生
	SELECTCASE TEQUIP:관장그릇＋플러그
		;液量5
		CASE 5
			PRINTFORMW 
		;液量4
		CASE 4
			PRINTFORMW 
		;液量3
		CASE 3
			PRINTFORMW 
		;その他
		CASEELSE
			PRINTFORMW 
	ENDSELECT
	[SKIPEND]
	;書き分けないのであればここで
	PRINTFORMW 
	RETURN 1
ENDIF
