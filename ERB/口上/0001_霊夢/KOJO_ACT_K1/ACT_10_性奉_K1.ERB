;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 성교봉사계 액션구상파일
;
;	성교봉사계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;성교봉사계 ACT95～103
;========================================================================
;·TFLAG:ACT(95=정상위시킨다/96=후배위시킨다/97=대면좌위시킨다/98=배면좌위시킨다/99=항문섹스시킨다/103=역강간)
;·TFLAG:ACT분류 == 10에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "성봉")로 참조하는 것이 가능
;·원칙으로서죠교대상→죠교자의삽입가 된다
;·체위의 제휴에 의해 TFLAG:ACT파생로 파생한다
;· 각 ACT 에 있어서 주도권은@SEX_INITIATIVE에서 정의된다
;-------------------------------------------------
;※성교중, 성교봉사중로부터의 파생에 대해…
;  일부의 ACT파생는 ACT_VARIATION로 정의되면서도, ACT_ABLE로 튕겨지고 있는 현상입니다
;  것에 따라서는 현재의 사양 자체가 이상한 생각도 듭니다만, 이 건에 대한 본체 측에서의 회답을 얻어지고 있지 않습니다
;  머지않아손가 들어갈 가능성도 있으므로, 주석을 적은 다음 테두리는 마련해 둡니다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT95, 정상위시킨다
;────────────────────────────────────
;·죠교자가죠교대상에정상위에서의삽입를 명한다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상가누움, 한편성교봉사중가 아닌 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·배면좌위에서의성교봉사중로부터는 파생하지 않는다
;·Ａ성교봉사중로부터는 파생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;·REACT분류가"날뛴다", "거부"였던 경우는취소 된다
;·죠교대상가동정라면동정상실가 일어난다
;·죠교자가처녀라면처녀상실가 일어난다
;·이것들의 처리는@ACTION_APPLY2_95에서 행해진다
;-------------------------------------------------
@KOJO_ACT_K1_95
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;※v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT95, 정상위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기
;·각파생내에서애태움도, 페니스밴드, 가버려, 죠교자의속옷의 유무등으로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;처녀·동정
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(TALENT:처녀, TALENT:MASTER:동정) > 0
		;죠교자가처녀＆죠교대상가동정
		IF TALENT:처녀 && TALENT:MASTER:동정
			;무드가발리나 있고 것으로 재고
			;PRINTFORMW %CALLNAME:MASTER%가 방식을 모르면 정직하게 이야기하면 %CALLNAME:TARGET%는 리본을 탁탁 날개를 펼치게 했다
			;PRINTFORML
			;PRINTFORMW 「괜찮아, 나도했다일 없지만… 이렇게, 곧바로」
			;PRINTFORMW 「-와 가면… 어떻게든 되어요」
			;PRINTFORML 
			;PRINTFORMW 대략적이었다
			
			PRINTFORMW 「최초부터 능숙하게 하려고 생각하지 않아 좋아요」
			PRINTFORMW 「생각하는 진에 하면, 우선은 그것으로 좋으니까」
			PRINTFORMW 「오며」
		[SKIPSTART]
		comment out 해 둡니다@/L
		;죠교자가처녀
		ELSEIF TALENT:처녀
			PRINTFORMW 
		;죠교대상가동정
		ELSE
			PRINTFORMW 
		[SKIPEND]
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:안빼고(죠교대상가사정 후페니스를안빼고성교를 속행했다횟수)
	;TFLAG:안빼고 > 1등으로 하는 일로안빼고의횟수참조는 가능
	;죠교자욕망, 기교, Ｖ/Ａ감각, 성교ABL에 의해보정를 받는다
	;금일의방침가애만들기라면+보정
	;요점은질내사정 후, 안빼고에성교봉사계 ACT가 실행당했다라고 하는 시추에이션
	;성교계 ACT의 경우 TFLAG:안빼고는 참조 할 수 없다
	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(100) && TFLAG:안빼고
		PRINTFORMW 「아직, 부족하다? 좋아요」
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상가가버려인 경우는 TCVAR:MASTER:가버려
	;죠교자의 경우는 PALAM:쾌Ｖ > 8500
	;라든지, 그런 느낌으로 참조해 주세요
	;---------------------------------------------
	
	;=================================================
	;연속(전회ACT가정상위시킨다)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		;애태움도에 의한 파생
		;SELECTCASE TCVAR:MASTER:애태움도
		;	;애태움중
		;	CASE IS > 1
		;		PRINTFORMW 
		;	;애태움종료
		;	CASE 1
		;		PRINTFORMW 
		;	;통상의연속실행
		;	CASEELSE
		;		PRINTFORMW 
		;ENDSELECT
		SIF TCVAR:MASTER:가버려
			PRINTFORMW （%RAND_SPLIT("갈 것 같은 자지, 왕복하고 있어/사정할 때의 표정 하고 있어/안쪽에서 동글동글 하고 있다… 이거,질내에 사정할 생각")%）
		PRINTFORMDW %CALLNAME:TARGET%는 눈동자를 물기를 띠게 하고 있다
		;RETURN는 시키지 않고 추가 묘사의 형태로 좋을지도
		;RETURN 1
	ENDIF
	
	;TEQUIP:성교봉사중 == 5(배면좌위시킨다)로부터는 파생하지 않는다
	;전회ACT는 IS_PREVACTNAME("대면좌위시킨다") 등으로 참조할 수 있다
	;다만, 전회의체위는 TEQUIP로 판정되므로전회ACT에서는 주울 수 없을 가능성이 있다
	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		;체위 특유의 풍미가 나와 있지 않기 때문에 재고한다
		;애태움도에 의한 파생
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				PRINTFORM 「 아직, 안돼
				CALL HEARTBW
			;애태움종료
			CASE 1
				SELECTCASE !RAND:2
					CASE 0
						PRINTFORM 「너의 좋아하게,%RAND_SPLIT("/좋아하게")%해도 좋아요
						CALL HEARTBW
					CASEELSE
						;레이무씨가안에 내!
						
						PRINTFORMW 「이대로%RAND_SPLIT("질내에/")%, 질내에 사정하며」
				ENDSELECT
			;통상의연속실행
			CASEELSE
				PRINTFORMW 「전부터(분)편이 안심할 수 있어요」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ａ성교로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ｖ성교계로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ａ성교봉사로부터
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 5
		PRINTFORMW 「엉덩이보다, 여기가 좋네요」
		RETURN 1
	ENDIF
	
	;기본적으로삽입시에죠교대상가가버려이다고 하는 상황을 상정
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		PRINTFORMW 「가버리는 거 참고 있는 너도 사랑스러워」
		PRINTFORM 「그렇지만, 이제 한계? 그렇구나? 응후후
		CALL HEARTBW
		SELECTCASE RAND:3
			CASE 0
				PRINTFORML 
				PRINTFORMDW %CALLNAME:TARGET%는 침상에 앉아, 스스로의 음순을 손가락으로 나누어 연다
				PRINTFORML 
				PRINTFORM 「와
				CALL HEARTBW
				PRINTFORMW 「삽입전에 사정해 버렸다들 화내요?」
			CASE 1
				PRINTFORMW 「그러면 아, 안녕히 주무십시오」
				PRINTFORML 
				PRINTFORMW 「농담이니까 그렇게 울 것 같은 얼굴 하지 않고」
				PRINTFORMW 「%RAND_SPLIT("나도 하고 싶은 거니까/끝까지 해도 좋으니까")%, 계속합시다」
			CASEELSE
				PRINTFORMW 「그러면 아, 안녕히 주무십시오」
				PRINTFORML 
				PRINTFORMDW %CALLNAME:TARGET%는 침상에 누워 버린다
				PRINTFORML 
				PRINTFORMW 「……%RAND_SPLIT("풋내기/둔감/바보")%. 그러니까 만약을 위해 말하지만,」
;OriginalString : 인12, "「襲っていいわよ」")
				CALL FADEIN(12, "「덮쳐도 좋아」")
			ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMDW %CALLNAME:TARGET%는 팔을 %CALLNAME:MASTER%에 늘린다
		PRINTFORMW 「자, 이리 와」
		PRINTFORMW 「 나는 누워 있으니, 자지로 제대로 봉사해 줘」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT96, 후배위시킨다
;────────────────────────────────────
;·죠교자가죠교대상에후배위에서의삽입를 명한다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상가누움, 한편성교봉사중가 아닌 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·대면좌위에서의성교봉사중로부터는 파생하지 않는다
;·Ａ성교봉사중로부터는 파생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;·REACT분류가"날뛴다", "거부"였던 경우는취소 된다
;·죠교대상가동정라면동정상실가 일어난다
;·죠교자가처녀라면처녀상실가 일어난다
;·이것들의 처리는@ACTION_APPLY2_96에서 행해진다
;-------------------------------------------------
@KOJO_ACT_K1_96
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;※v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT96, 후배위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기
;·각파생내에서애태움도, 페니스밴드, 가버려, 죠교자의속옷의 유무등으로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;처녀·동정
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(TALENT:처녀, TALENT:MASTER:동정) > 0
		;죠교자가처녀＆죠교대상가동정
		IF TALENT:처녀 && TALENT:MASTER:동정
		PRINTFORMDW %CALLNAME:MASTER%(은)는 %CALLNAME:TARGET%의 엉덩이를 잡아, 수직근에 귀두를 꽉 눌렀다
		PRINTFORMW 「좀 더 위…개-와 밀어넣으며」
		;말해져 보고 싶지만 무드가발리나 있고. 후배위라면 그것으로 좋은 것인지도 모른다
		;PRINTFORM 「너의동정자지로 %CALLNAME:TARGET%의 일, 비처녀로 해
		CALL HEARTBW
		[SKIPSTART]
		comment out 해 둡니다@/L
		;죠교자가처녀
		ELSEIF TALENT:처녀
			PRINTFORMW 「상냥하게 해 주세요」
		;죠교대상가동정
		ELSE
			PRINTFORMW 
		[SKIPEND]
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:안빼고(죠교대상가사정 후페니스를안빼고성교를 속행했다횟수)
	;TFLAG:안빼고 > 1등으로 하는 일로안빼고의횟수참조는 가능
	;죠교자욕망, 기교, Ｖ/Ａ감각, 성교ABL에 의해보정를 받는다
	;금일의방침가애만들기라면+보정
	;요점은질내사정 후, 안빼고에성교봉사계 ACT가 실행당했다라고 하는 시추에이션
	;성교계 ACT의 경우 TFLAG:안빼고는 참조 할 수 없다
	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(0) && TFLAG:안빼고
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상가가버려인 경우는 TCVAR:MASTER:가버려
	;죠교자의 경우는 PALAM:쾌Ｖ > 8500
	;라든지, 그런 느낌으로 참조해 주세요
	;---------------------------------------------
	
	;=================================================
	;연속(전회ACT가후배위시킨다)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		;애태움도에 의한 파생
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			;CASE IS > 1
			;	PRINTFORMW 
			;애태움종료
			CASE 1
				;중은 안된다고 말하고 있겠죠 분명하게 안에 내세요
				PRINTFORMW 「안돼, (안)중 안돼, 안돼, (안)중이고 안돼」
			;통상의연속실행
			CASEELSE
			;	PRINTFORMW 
				RETURN 0
		ENDSELECT
		RETURN 1
	ENDIF
	
	;TEQUIP:성교봉사중 == 4(대면좌위시킨다)로부터는 파생하지 않는다
	;전회ACT는 IS_PREVACTNAME("정상위시킨다") 등으로 참조할 수 있다
	;다만, 전회의체위는 TEQUIP로 판정되므로전회ACT에서는 주울 수 없을 가능성이 있다
	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		;애태움도에 의한 파생
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				PRINTFORMW 「이번은 뒤로부터, 군요」
				PRINTFORMW 「 내가 기분이 내킬 때까지는, 사정하면 안되기 때문에」
			;애태움종료
			CASE 1
				;야생과 품성. 천함 직구 사노 간이 하나 더
				PRINTFORMW 「최후는 뒤로부터」
				PRINTFORMW 「교배 교미로 잔뜩 사정해버려」
			;통상의연속실행
			CASEELSE
				;겨자가 신경이 쓰인다. 그다지 카타카나는 사용하고 싶지 않다
				;PRINTFORMW 「이번은, 뒤로부터 해」
				PRINTFORMW 「이번은 뒤로부터, 군요」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ａ성교로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ｖ성교계로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ａ성교봉사로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;싫증상태를 살릴 수 있지 않기 때문에 뭔가변화공. 이미지플레이에 달린다. 싫어한다부끄러워한다에 대응할 수 있는 것 같은 것.
	;=================================================
	;%CALLNAME:TARGET%가싫증
	;=================================================
	IF 구상기입체크(25) && IS_CONDITION(TARGET, "싫증")
		PRINTFORMW 「가끔 씩은 취향을 바꾸어 볼까」
		PRINTFORMW 「네가 남편님이나 리나 차이」
		PRINTFORMW 「간단해요. 나의 일자기 마음대로 범해 준다면 좋아요」
		PRINTFORMW 「평상시와 변함없다? 차이원이군요」
		RETURN 1
	ENDIF
	
	;기본적으로삽입시에죠교대상가가버려이다고 하는 상황을 상정
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		PRINTFORML 「이제 안된다는 것은 표정이군요? 응후후♪」
		PRINTFORMW 「내고 싶다? 내고 싶어요? 자지 뛰게 해 정액하고 싶어요?」
		PRINTFORML
		PRINTFORMW 「좋아요」
		PRINTFORMW 「와♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		IF !RAND:2
			PRINTFORMW 「\@ IS_CONDITION(TARGET, "정욕") ? 짐승교미 # 섹스 \@합시다」
			PRINTFORMDW %CALLNAME:TARGET%의 순산형의 엉덩이가 %CALLNAME:MASTER%안의 욕망을 부른다
		ELSE
			;후 키비패개장
			PRINTFORMW 「와」
			PRINTFORMDW %CALLNAME:TARGET%는 스스로 비육을 나누어 넓혀 %CALLNAME:MASTER%에게 권하고 있다
		ENDIF
		
		;수줍음불발, 과제가 남는다
		PRINTFORMW 「%CALLNAME:TARGET%의 안에 넣고 흔들며」
		;12로부터 단축
;OriginalString : 인6, "「た」") たねつけ
		CALL FADEIN(6, "「임」")
;OriginalString : 인6, "「ね」")
		CALL FADEIN(6, "「신」")
;OriginalString : 인6, "「つ」")
		CALL FADEIN(6, "「교」")
;OriginalString : 인6, "「け」")
		CALL FADEIN(6, "「배」")
		PRINTFORMW 「해♪」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT97, 대면좌위시킨다
;────────────────────────────────────
;·죠교자가죠교대상에대면좌위에서의삽입를 명한다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상가누움, 한편성교봉사중가 아닌 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·배면좌위에서의성교봉사중로부터는 파생하지 않는다
;·Ａ성교봉사중로부터는 파생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;·REACT분류가"날뛴다", "거부"였던 경우는취소 된다
;·죠교대상가동정라면동정상실가 일어난다
;·죠교자가처녀라면처녀상실가 일어난다
;·이것들의 처리는@ACTION_APPLY2_97에서 행해진다
;-------------------------------------------------
@KOJO_ACT_K1_97
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;※v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT97, 대면좌위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기
;·각파생내에서애태움도, 페니스밴드, 가버려, 죠교자의속옷의 유무등으로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;처녀·동정
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TALENT:처녀, TALENT:MASTER:동정) > 0
		;죠교자가처녀＆죠교대상가동정
		IF TALENT:처녀 && TALENT:MASTER:동정
			PRINTFORMW 
		;죠교자가처녀
		ELSEIF TALENT:처녀
			PRINTFORMW 
		;죠교대상가동정
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:안빼고(죠교대상가사정 후페니스를안빼고성교를 속행했다횟수)
	;TFLAG:안빼고 > 1등으로 하는 일로안빼고의횟수참조는 가능
	;죠교자욕망, 기교, Ｖ/Ａ감각, 성교ABL에 의해보정를 받는다
	;금일의방침가애만들기라면+보정
	;요점은질내사정 후, 안빼고에성교봉사계 ACT가 실행당했다라고 하는 시추에이션
	;성교계 ACT의 경우 TFLAG:안빼고는 참조 할 수 없다
	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(100) && TFLAG:안빼고
		PRINTFORMW 「 아직, 놓아 주지 않기 때문에」
		[SKIPSTART]
		;지문과 입기 때문에(위해) comment out
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 몸에 팔을 돌려 안아 도착해 왔다
		[SKIPEND]
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%(을)를 물기를 띤 눈동자로 응시하면서 허리를 꿈틀거린다
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상가가버려인 경우는 TCVAR:MASTER:가버려
	;죠교자의 경우는 PALAM:쾌Ｖ > 8500
	;라든지, 그런 느낌으로 참조해 주세요
	;---------------------------------------------
	
	;=================================================
	;연속(대면좌위시킨다로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
		;애태움도에 의한 파생
		;SELECTCASE TCVAR:MASTER:애태움도
		;	;애태움중
		;	CASE IS > 1
		;		PRINTFORMW 
		;	;애태움종료
		;	CASE 1
		;		PRINTFORMW 
		;	;통상의연속실행
		;	CASEELSE
		;		PRINTFORMW 
		;ENDSELECT
		RETURN 1
	ENDIF
	
	;TEQUIP:성교봉사중 == 2(후배위시킨다)/TEQUIP:성교봉사중 == 5(배면좌위시킨다)로부터는 파생하지 않는다
	;전회ACT는 IS_PREVACTNAME("정상위시킨다") 등으로 참조할 수 있다
	;다만, 전회의체위는 TEQUIP로 판정되므로전회ACT에서는 주울 수 없을 가능성이 있다
	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
		;애태움도에 의한 파생
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				PRINTFORMW 
			;애태움종료
			CASE 1
				PRINTFORMW 
			;통상의연속실행
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ａ성교로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ｖ성교계로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ａ성교봉사로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;기본적으로삽입시에죠교대상가가버려이다고 하는 상황을 상정
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		;PRINTFORMW 「이제(벌써)사정그렇게? 너의 일, 제일 근처에서 보고 있어 주어요」
		PRINTFORMW 「이제 사정할 것 같아? 네가 가버리는거, 지켜봐 줄게」
		PRINTFORML 「여기 이렇게, 코앞에서」
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%%RAND_SPLIT("에 제대로 안아 도착해 왔다/의 이마에 이마를 붙여 왔다/에 웃는 얼굴을 보여 주었다")%
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RAND:2
			;%CALLNAME:MASTER%공격
			CASE 0
				PRINTFORMW 「너의 팔안, 따뜻하네요」
;OriginalString : 아웃8, "「んふふ♪」")
				CALL FADEOUT(8, "「응후후♪」")
				;PRINTFORML
				;이손의 묘사는 첫회 한정이 바람직함//이런 느낌으로 어떨까요? @/L//제안 감사합니다@숙녜
				SIF FIRSTACT(80)
					PRINTFORMDW 난폭하게 행동하는 %CALLNAME:TARGET%를 팔안에 안아 보면, 그 몸은 가냘프고, 작았다
			;%CALLNAME:TARGET%공격
			CASEELSE
				;PRINTFORMW 「아쌓입시다」
				PRINTFORM 「편안히 합시다
				CALL HEARTBW
;OriginalString : 아웃8, "「うふふ♪」")
				CALL FADEOUT(8, "「우후후♪」")
				PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%에 안아 도착해 왔다
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT98, 배면좌위시킨다
;────────────────────────────────────
;·죠교자가죠교대상에배면좌위에서의삽입를 명한다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상가누움, 한편성교봉사중가 아닌 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·정상위, 대면좌위에서의성교봉사중로부터는 파생하지 않는다
;·Ａ성교봉사중로부터는 파생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;·REACT분류가"날뛴다", "거부"였던 경우는취소 된다
;·죠교대상가동정라면동정상실가 일어난다
;·죠교자가처녀라면처녀상실가 일어난다
;·이것들의 처리는@ACTION_APPLY2_98에서 행해진다
;-------------------------------------------------
@KOJO_ACT_K1_98
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;※v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT98, 배면좌위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기
;·각파생내에서애태움도, 페니스밴드, 가버려, 죠교자의속옷의 유무등으로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;처녀·동정
	;=================================================
	;IF 구상기입 체크(0) && TRUECHECK(TALENT:처녀, TALENT:MASTER:동정) > 0
	;	;죠교자가처녀＆죠교대상가동정
	;	IF TALENT:처녀 && TALENT:MASTER:동정
	;		PRINTFORMW 
	;	;죠교자가처녀
	;	ELSEIF TALENT:처녀
	;		PRINTFORMW 
	;	;죠교대상가동정
	;	ELSE
	;		PRINTFORMW 
	;	ENDIF
	;	RETURN 1
	;ENDIF
	
	;소스입손수체감 발생율 모두 높은 우대 커맨드
	;주인님에 있어 바람직한 상황을 의식하는 등으로부터 의지해져 한편, 삽입실패의 눈이 있다
	
	;당신에의 호감도[1589]레이무에의 호감도[1666]동정처녀에서 단행한다.
	;테스트시의 상황은기력 잘라라부끄러워한다일택. 실행 후당신가가버려죠교를 종료. 일단 이것을 모델로 한다.
	;꽤 조기. 상사상애의 전에 발생한다고 생각하는 것이 좋다.
	;일선을넘지않음레이무에 일선을 넘고시킨다난제.
	;체위를 살려 어느 쪽이 넘었는지 애매한 경계선상의 회색을 표현해 본다.
	
	;성큼성큼 마요히가에 침입하는 이변 해결시의 문답 무용 좌파, 당신에거부권이 남아 있기 위해서(때문에) 사용할 수 있을 것 같지도 않다
	;처녀성과 강행 마을 저항의 여지와 선택될부끄러워한다
	;「『삽입 해라』라고는 말했지만 『삽입 해라』라고는 말하지 않았다.」는 성립할까?
	IF 구상기입체크(100) && TALENT:처녀
		;Ｃ에의 자극의 확보
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 무릎 위에 걸터앉으면(자) 엉덩이로 음경을 눌러 잡아, %CALLNAME:MASTER%의 표정을 확인하면서 허리를 구 자고들 키
;OriginalString : 아웃8, "  ちゅく…")
		CALL FADEOUT(8, "  츄쿠…")
		;여기서 1궁리하면 어떨까. 당신주도를 암시한다.
		PRINTFORMDW %CALLNAME:MASTER%의 음경은 한계까지 부피를 늘려,
		PRINTFORMDW 한계까지 부풀어 올라 젖은 귀두가 %CALLNAME:TARGET%의 유질구를 확대한다
;OriginalString : 아웃8, "  …ごくっ")
		CALL FADEOUT(8, "  …꿀꺽")
		PRINTFORMW 「여기, 군요…?」
		;조금씩 빨리 해 간다
;OriginalString : 인6, "  ちゅく…")
		CALL FADEIN(6, "  츄쿠…")
;OriginalString : 아웃6, "  「んっ…」")
		CALL FADEOUT(6, "  「읏…」")
		;당신의 소극성을 잔하면서가슴에손를 재촉한다.
		PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 수를 잡아 가슴에 안으면(자), 유질구로 귀두의 앞이 얕고 질네 붐빈 채로에
		PRINTFORMDW 머뭇머뭇허리를 강탈 귀두의 앞을 빚었다
;OriginalString : 인4, "  ちゅく…")
		CALL FADEIN(4, "  츄쿠…")
;OriginalString : 인4, "  ちゅくちゅっ…")
		CALL FADEIN(4, "  츄쿠츗…")
;OriginalString : 아웃6, "  「…んっんっ」")
		CALL FADEOUT(6, "  「…읏읏」")
		PRINTFORMW 「이렇게 하고 있으면, 삽입있을 것 같구나」
		;합의를 요구한다는 별로 커맨드가 있다
		;결국레이무가 넣었는지당신가 넣었는지 애매하게
		;동의를 요구하면서합의는 취하지 않고 명한 것 같아 명하지 않게도 들리는 1문자
		PRINTFORMW 「이군요.」
	ENDIF
	
	;TFLAG:안빼고(죠교대상가사정 후페니스를안빼고성교를 속행했다횟수)
	;TFLAG:안빼고 > 1등으로 하는 일로안빼고의횟수참조는 가능
	;죠교자욕망, 기교, Ｖ/Ａ감각, 성교ABL에 의해보정를 받는다
	;금일의방침가애만들기라면+보정
	;요점은질내사정 후, 안빼고에성교봉사계 ACT가 실행당했다라고 하는 시추에이션
	;성교계 ACT의 경우 TFLAG:안빼고는 참조 할 수 없다
	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(0) && TFLAG:안빼고
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상가가버려인 경우는 TCVAR:MASTER:가버려
	;죠교자의 경우는 PALAM:쾌Ｖ > 8500
	;라든지, 그런 느낌으로 참조해 주세요
	;---------------------------------------------
	
	;=================================================
	;연속(전회ACT가배면좌위시킨다)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		;애태움도에 의한 파생
		;SELECTCASE TCVAR:MASTER:애태움도
		;	;애태움중
		;	CASE IS > 1
		;		PRINTFORMW 
		;	;애태움종료
		;	CASE 1
		;		PRINTFORMW 
		;	;통상의연속실행
		;	CASEELSE
		;		PRINTFORMW 
		;ENDSELECT
		;=================================================
		;싫증
		;=================================================
		IF 구상기입체크(25) && IS_CONDITION(TARGET, "싫증")
			PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%%RAND_SPLIT("노가슴에 푹 수하며 /에 기대고 담당자")%숨소리를 내고 있다…
			RETURN 1
		ENDIF
		
		IF 구상기입체크(100)
			PRINTFORMW 「이 몸의 자세라고, 나부터 할 수 있는 일은 그다지 없어」
			;줘가 딱딱한 생각이 든다
			;PRINTFORMW 「너가 기분 좋게 해 줘」
			PRINTFORMW 「그런데, 기분 좋게 하며」
			RETURN 1
		ENDIF
	ENDIF
	
	;TEQUIP:성교봉사중 == 1(정상위시킨다)/TEQUIP:성교봉사중 == 4(대면좌위시킨다)로부터는 파생하지 않는다
	;전회ACT는 IS_PREVACTNAME("후배위시킨다") 등으로 참조할 수 있다
	;다만, 전회의체위는 TEQUIP로 판정되므로전회ACT에서는 주울 수 없을 가능성이 있다
	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		[SKIPSTART]
		comment out 해 둡니다@/L
		;애태움도에 의한 파생
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				PRINTFORMW 
			;애태움종료
			CASE 1
				PRINTFORMW 
			;통상의연속실행
			CASEELSE
		ENDSELECT
		[SKIPEND]
		PRINTFORMW 「영차와… 팔, 목에 돌려?」
		PRINTFORMW 「네에에♪」
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ａ성교로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ｖ성교계로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ａ성교봉사로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;기본적으로삽입시에죠교대상가가버려이다고 하는 상황을 상정
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		PRINTFORML 「너의 일이니까, 앉아지는 것이 제일 기분이 좋아요?」
		PRINTFORMW 「삽여(어) 주기 때문에, 좀 더 만%RAND_SPLIT("분발함/참고 견뎌/참다해")%없음 있고」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORML 「영차」
		PRINTFORMW 「너의 안정감, 꽤 나쁘지 않아요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT99, 항문섹스시킨다
;────────────────────────────────────
;·죠교자가죠교대상에엉덩이에의삽입를 명한다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상가누움, 한편성교봉사중가 아닌 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·턴방침가"소프트"로Ａ성교봉사중가 아닌 경우는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·Ｖ성교봉사중로부터는 파생하지 않는다
;·죠교자의Ａ감각가 2 이하의 경우는 발생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;·REACT분류가"날뛴다", "거부"였던 경우는취소 된다
;-------------------------------------------------
@KOJO_ACT_K1_99
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ａ성교계로부터/3=Ｖ성교봉사계로부터/4=Ｖ성교계로부터)
;※v1. 010 현재, ACT파생 2로부터 4는 ACT_ABLE로 규제되고 있으므로 기능하고 있지 않다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT99, 항문섹스시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생 1(연속), ACT파생 2 TO 5(Ａ성교/Ｖ성교봉사/Ｖ성교), 그 외로 분기
;·각파생내에서페니스밴드의 유무로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(0)
	;TFLAG:안빼고(죠교대상가사정 후페니스를안빼고성교를 속행했다횟수)
	;TFLAG:안빼고 > 1등으로 하는 일로안빼고의횟수참조는 가능
	;죠교자욕망, 기교, Ｖ/Ａ감각, 성교ABL에 의해보정를 받는다
	;금일의방침가애만들기라면+보정
	;요점은질내사정 후, 안빼고에성교봉사계 ACT가 실행당했다라고 하는 시추에이션
	;성교계 ACT의 경우 TFLAG:안빼고는 참조 할 수 없다
	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(0) && TFLAG:안빼고
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상가가버려인 경우는 TCVAR:MASTER:가버려
	;죠교자의 경우는 PALAM:쾌Ｖ > 8500
	;라든지, 그런 느낌으로 참조해 주세요
	;---------------------------------------------
	
	;=================================================
	;연속(전회ACT가항문섹스시킨다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
		;애태움도에 의한 파생
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				PRINTFORMW 
			;애태움종료
			CASE 1
				PRINTFORMW 
			;통상의연속실행
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ａ성교계로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ｖ성교봉사계로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ｖ성교계로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;기본적으로삽입시에죠교대상가가버려이다고 하는 상황을 상정
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:가버려
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT103, 역강간
;────────────────────────────────────
;·죠교자가기승위로 억지로성교섭에 이른다
;·죠교대상에페니스, 페니스밴드가 없는 경우는 발생하지 않는다
;·죠교자가페니스밴드장착중의 경우는 발생하지 않는다
;·죠교대상의발기도가 1000 이하의 경우는 발생하지 않는다
;·죠교자가남자의 경우는 발생하지 않는다
;·죠교대상가오나홀사용중의 경우에는 발생하지 않는다
;·성교중로부터는 파생하지 않는다
;·Ａ성교봉사중로부터는 파생하지 않는다
;·주도권은죠교대상"SEX_INITIATIVE(TARGET)"
;-------------------------------------------------
;·다른성교봉사계와 달리, 죠교대상는 저항 할 수 없다
;·죠교대상가동정라면동정상실가 일어난다
;·죠교자가처녀라면처녀상실가 일어난다
;·이것들의 처리는@ACTION_APPLY2_103에서 행해진다
;-------------------------------------------------
@KOJO_ACT_K1_103
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;·v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;ACT103, 역강간
;━━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기
;·각파생내에서애태움도, 페니스밴드, 가버려, 죠교자의속옷의 유무등으로 분기 한다
;·필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;처녀·동정
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(TALENT:처녀, TALENT:MASTER:동정) > 0
		;죠교자가처녀＆죠교대상가동정
		IF TALENT:처녀 && TALENT:MASTER:동정
			PRINTFORMW 「너의 처음으로, 받아요」
			PRINTFORML 「대신에 나의 처음으로 주기 때문에, 좋아요?」
		[SKIPSTART]
		comment out 해 둡니다@/L
		;죠교자가처녀
		ELSEIF TALENT:처녀
			;%CALLNAME:MASTER%가 초기상태로 비동정인 경우도 있다
			PRINTFORMW 「나의 처음으로, 네가 받으세요」
			PRINTFORMW 「나는, 네가 좋아」
		;죠교대상가동정
		ELSE
			PRINTFORMW 
		[SKIPEND]
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:안빼고(죠교대상가사정 후페니스를안빼고성교를 속행했다횟수)
	;TFLAG:안빼고 > 1등으로 하는 일로안빼고의횟수참조는 가능
	;죠교자욕망, 기교, Ｖ/Ａ감각, 성교ABL에 의해보정를 받는다
	;금일의방침가애만들기라면+보정
	;요점은질내사정 후, 안빼고에성교봉사계 ACT가 실행당했다라고 하는 시추에이션
	;성교계 ACT의 경우 TFLAG:안빼고는 참조 할 수 없다
	;=================================================
	;안빼고에 속행
	;=================================================
	;사정 하고 나서안빼고에 이었을 경우, 체위 변경시에 여기에 온다
	IF 구상기입체크(100) && TFLAG:안빼고
		PRINTFORMW 「내가 해 줄테니, 너는 편하게 있어」
		PRINTFORML 「내가 만족할 때까지, 범해 줄게」
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상가가버려인 경우는 TCVAR:MASTER:가버려
	;죠교자의 경우는 PALAM:쾌Ｖ > 8500
	;라든지, 그런 느낌으로 참조해 주세요
	;---------------------------------------------
	
	;=================================================
	;연속(전회ACT가역강간)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		;=================================================
		;하드로 하고 응하는 추가 묘사
		;=================================================
		IF 구상기입체크(100) && IS_PREVCOMNAME("더 하드하게 해")
			PRINTFORM 「소망 대로, 격렬하게 해 준다
			CALL HEARTBW
			PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 양어깨를 억누르면서 격렬하게 허리를 부딪혀 왔다
			IF EXP:TARGET:Ｖ경험 < 10
;OriginalString : 아웃4, "「んんっ！！」")
				CALL FADEOUT(4, "「응읏!!」")
				PRINTFORMDL （아직, 상해요…）
			ENDIF
		ENDIF
		;=================================================
		;애태움도에 의한 파생
		;=================================================
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				IF TFLAG:징계·플래그
					PRINTFORMW 「아직 사정하면 안되어요? 이것은 징계 위난이니까」
					PRINTFORMW 「가득 범해 주기 때문에 간단하게 끝내지 마」
				ELSE
					IF IS_CONDITION(TARGET, "정욕")
						PRINTFORML 「 아직 맡기고니까」
						PRINTFORMDW %CALLNAME:TARGET%는 즐거운 듯이 미소지었다
						PRINTFORM 「이봐요, 힘내라 힘내라
						CALL HEARTBW
					ENDIF
						PRINTFORMW 「 아직 사정하면 안되어요」
						PRINTFORML 「 나보다 먼저 말하면…뒤쫓아 주기 때문에…」
				ENDIF
			;애태움종료
			CASE 1
				IF PALAM:쾌Ｖ > 8500
					PRINTFORMW 「 나도 이제…좋아요」
					PRINTFORML 「안에 내며」
				ELSE
					PRINTFORMW 「이제놈들? 억지로놈들? 등째?」
;OriginalString : 아웃4, "「くすっ」")
					CALL FADEOUT(4, "「키득」")
					PRINTFORM 「%RAND_SPLIT("좋으니까/안되지 않으니까")%나의 질내에서, 사정해버릴 없음 있고
					CALL HEARTB
				ENDIF
			;통상의연속실행
			CASEELSE
				IF 구상기입체크(100) && PALAM:쾌Ｖ > 8500 && !TCVAR:MASTER:가버려
					PRINTFORMW 「안돼…나…안돼…」
					PRINTFORMDW %CALLNAME:TARGET%는 절정 직전에 견디면서 천천히 허리를 움직이고 있다
				ENDIF
				IF 구상기입체크(100) && IS_CONDITION(MASTER, "정욕")
					PRINTFORMW 「무리하게기분 있고? 기분이 좋다?」
					PRINTFORML 「-응」
					PRINTFORMDL %CALLNAME:TARGET%는 정욕에 녹는다%CALLNAME:MASTER%(을)를 만족스럽게 업신여기고 있다
				ENDIF
				IF 구상기입체크(100)
					PRINTFORMW 「 나아래에서 허덕이는 너를 보고 있으면, 기뻐져요」
					PRINTFORM 「 좀 더 해 준다
					CALL HEARTBW
				ENDIF
		ENDSELECT
		RETURN 1
	ENDIF
	;어느 체위로부터도 파생할 가능성 있어
	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		;애태움도에 의한 파생
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움중
			CASE IS > 1
				;PRINTFORMW 
				RETURN 0
			;애태움종료
			;CASE 1
				;PRINTFORMW 
				;RETURN 0
			;애태움없음
			CASEELSE
				;슈팅 요소를 포함시키는 것은 무리가 있는지도 모른다
				PRINTFORML 「준비는 좋아. 너의 %RAND_SPLIT("자식/정자/아기")%(은)는, 제대로 받아 줄게」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ａ성교로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ｖ성교계로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	;Ａ성교봉사로부터
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;기본적으로삽입시에죠교대상가가버려이다고 하는 상황을 상정
	;=================================================
	;죠교대상가가버려
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		;=================================================
		;임신중잉키에 대해 언급하지 않는 일단 조건없이 보낸다
		;=================================================
		IF 구상기입체크(0) && TALENT:임신
			[SKIPSTART]
			;PRINTFORMW 「어차피사정모래등, 여기가 좋네요?」
			;모순의 가능성유comment out
			;PRINTFORMW 「소망 대로삽입여(어) 주기 때문에, 조금만 더 노력하세요」
			[SKIPEND]
			RETURN 1
		ENDIF
		;=================================================
		;레이무분노의 나카데 시키고
		;=================================================
		IF 구상기입체크(100) && IS_CONDITION(TARGET, "분노")
			PRINTFORML 「좋으니까 냉큼 사정해 주세요!」
			PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 어깨를 억누르면서 허리를 부딪혀 왔다
			RETURN 1
		ENDIF
		;=================================================
		;죠교대상가가버려임신중이 아니다
		;=================================================
		IF 구상기입체크(100) && !TALENT:임신
			;기입 개소 미스의 예감
			;♪는 없는 것이 냉담함이 낼 수 있다
			PRINTFORMW 「너%RAND_SPLIT("임신할 때까지/임신할 때까지/갓난 아기 생기게 될 때까지/몇번이나")%몇 번이라도, 범해 주어요」
			PRINTFORM 「후후
			CALL HEARTBW
			PRINTFORMW 「%RAND_SPLIT("너의 자지는, 나/서방님의 자지는, 신부")%를 잉다투기 위해 있다 것이군요?」
			;부부취를 강조하기 때문에(위해)당신표기
			PRINTFORMW 「%RAND_SPLIT("각오를 결정하세요? 당·신/♪각오를 결정하세요? 우후후…/너의 일이니까, 기쁘지요? 어머 어머♪")%」
			RETURN 1
		ENDIF
	ENDIF
	;=================================================
	;%CALLNAME:TARGET%분노
	;=================================================
	IF 구상기입체크(100) && IS_CONDITION(TARGET, "분노")
		PRINTFORMW 「좋으니까 냉큼 야등 등차이!」
	ENDIF
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;젖가슴
		IF TALENT:거유 && !RAND:3
			PRINTFORMDW low angle로부터 올려보는 흔들리는 유방으로부터, %CALLNAME:MASTER%(은)는 눈을 일일을 할 수 없다
		;말하고 싶은 일은 통하지만 침착성이 있는 퇴고를 필요로 한다
		;ELSEIF ! TALENT:빈유 && ! RAND:3
		;	PRINTFORMDW %CALLNAME:TARGET%의 아랫가슴이 아랫가슴이 젖가슴으로 젖가슴으로 %CALLNAME:MASTER%힘겹다
		;리본
		ELSEIF !RAND:2
			PRINTFORML 「너의 자식, 전부 짜 내 줄게」
			PRINTFORMW 「진한 것 가득 사정하지 못하면… %RAND_SPLIT("용납하지 않아/인내하지 않아?")%」
			PRINTFORML
			PRINTFORMDW %CALLNAME:TARGET%는 리본을 톡톡홰치게 한다
		;흑발의 강조 성실함 진심진지함을 낸다
		ELSE
			PRINTFORMW 「전부 짜내 줄 테니, 각오해」
			PRINTFORMDW %CALLNAME:TARGET%는 아름다운 흑발을 늘어뜨리면서, %CALLNAME:MASTER%(을)를 진지한 시선으로 업신여기고 있다
		ENDIF
		RETURN 1
	ENDIF
	
	[SKIPSTART]
	;=================================================
	;미정고, 몰호
	;=================================================
		;아라이멘도정
		IF ALI(0)
		;아라이멘도부
		ELSE
			;관계 악화는 소극적으로
			;PRINTFORMW 「곧바로는 끝내지 않지만, %CALLNAME:MASTER%는 천정의 얼룩에서도 세어 주세요」
			
			;IF TALENT:MASTER:청소의 차이를 알 수 있다
			;	PRINTFORMDL 그 말에, 솔직에 천정을 관찰하면
			;	PRINTFORMDL 게을리하기 십상인 천정의 청소도 잘 두루 미치고 있었다…
			;ENDIF
			
			;IF TALENT:MASTER:엉덩이 굉장함
			;	PRINTFORMDL 그 말에 거역해 시선을 옆에 향하면 머리맡의로션와 티슈가 눈에 들어왔다
			;	PRINTFORMDL %CALLNAME:MASTER%의 엉덩이는 꽉 되었다
			;ENDIF
			
			;얇은 책을 꺼내는 것은 없음
			;PRINTFORML 「이것이라도 읽어 주세요」
			;PRINTFORML 
			;PRINTFORMDL 레이무로부터 도당했다 얇은 책에는레이무와의 달콤한 생활이 그려져 있다
			;PRINTFORMDL 레이무씨. 이런 것, 좋아해?
			
			;레이무산지 넓어
			;PRINTFORMDL 다다미 4장 반의 하늘아래, %CALLNAME:TARGET%에 범해지고\@ TIME ? 푸욱푸욱 밤의 꿈에 가라앉는 # 장지의 틈새로부터 가늘게 퍼지는 하늘을 바라보는\@
		ENDIF
	;=================================================
	;삽입시에죠교대상가가버려이다고 하는 상황. 조건을 특정하지 못하고 comment out
	;=================================================
	PRINTFORML 「이제 사정 그렇게? 삽여(어) 주기 때문에 좀 더 만%RAND_SPLIT("분발함/참고 견뎌/참다해")%하세요」
	PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%%RAND_SPLIT("에 제대로 안아 도착해 왔다/의 이마에 이마를 붙여 왔다/에 웃는 얼굴을 보여 주었다")%
	PRINTFORMDW %CALLNAME:TARGET%에 넋을 잃고 보고 있는 동안에 %CALLNAME:MASTER%의 일물은 %CALLNAME:TARGET%의 질에 삼켜져 갔다
	;이쪽의 PRINTFORML는 잔했다 (분)편이삽입중와삽입완료의 타임랙을 나올 수 있을지도
	PRINTFORML 
	PRINTFORMW 「전부 삽입… 제대로 참을 수 있었던 원이군요」
	PRINTFORMW 「훌륭해요」
	RETURN 1
	
	PRINTFORMDW 당장 사정해 버릴 것 같은 음경을 %CALLNAME:TARGET%의 녀음이 천천히 삼켜 간다
	PRINTFORML
	PRINTFORM 「응…%RAND_SPLIT("전부/가장 안쪽까지/너의 자지")%삽입
	CALL HEARTBW
	RETURN 1
	;=================================================
	;삽입즉사정 %CALLNAME:TARGET%가정욕로 에로 에로
	;=================================================
	IF 구상기입 체크(100) && IS_CONDITION(TARGET, "정욕") && TCVAR:MASTER:가버려
		PRINTFORMDW %CALLNAME:TARGET%는 다리를M글자에 열려, %CALLNAME:MASTER%의 음경을 질구에 대어 입맛을 다셨다
		PRINTFORMW 「이제 사정해도 좋아요」
		PRINTFORMW 「삽입라고 구와 개 보면서, 말해버릴 하세요」
		PRINTFORMDW %CALLNAME:TARGET%는 천천히 허리를 내려 갔다
	ENDIF
	;=================================================
	;%CALLNAME:TARGET%가정욕 성교봉사동시절정
	;=================================================
	IF 구상기입 체크(100) && IS_CONDITION(TARGET, "정욕") && PALAM:쾌Ｖ > 8500
		PRINTFORMW 「쏟아! 질내사정으로 %CALLNAME:TARGET%의 보지」
		PRINTFORMW 「살릴 수 있어!」
	ENDIF
	;=================================================
	;REACT라는 조정이필요
	;=================================================
	IF 구상기입 체크(100)
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 음경을 최안쪽까지 질네 붐비면 기쁜듯이 하복부를 어루만졌다
		PRINTFORMW 「나의 질내에 너의 %RAND_SPLIT("너가/하지만/, ")%들어가 있다」
	ENDIF

	;=================================================
	;체위 변경역강간
	;=================================================
	IF 구상기입 체크(100)
		;PRINTFORMW 「조금 전보다 딱딱한 것은 기분탓일까요? 응후후」
		;PRINTFORML 「너는,%RAND_SPLIT("정말,/")%접수지요」
	ENDIF
		
	IF 구상기입 체크(100)
		;PRINTFORMW 「뱃속으로 커져 왔다. 사정 할 것 같은거네」
	ENDIF
	[SKIPEND]
ENDIF
