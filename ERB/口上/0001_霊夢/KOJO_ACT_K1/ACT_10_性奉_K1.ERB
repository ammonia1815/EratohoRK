;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 성교봉사系アクション구상ファイル
;
;	성교봉사系アクション구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;성교봉사系 ACT95～103
;========================================================================
;・TFLAG:ACT(95=정상위시킨다/96=후배위시킨다/97=대면좌위시킨다/98=배면좌위시킨다/99=항문섹스시킨다/103=역강간)
;・TFLAG:ACT분류 == 10に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "성봉")で参照する事が可能
;・原則として죠교대상→죠교자の삽입となる
;・体位の連携によってTFLAG:ACT파생で派生する
;・各ACTに於ける主導権は@SEX_INITIATIVEにて定義される
;-------------------------------------------------
;※성교중、성교봉사중からの派生について…
;　一部のACT파생はACT_VARIATIONで定義されつつも、ACT_ABLEで弾かれている現状です
;　ものによっては現在の仕様自体が怪しい気もしますが、この件についての本体側からの回答が得られていません
;　いずれ손が入る可能性もあるので、注釈をつけた上で枠は設けておきます
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では구상側のRESULT(終端のRETURN 1など)によって구상が表示당했다のか否か、
;・また箇所毎の特有の挙動(미약等구상をスキップなど)を制御しています
;・@구상記入チェックの場合代入処理を省略できますが、RESULT値は구상側で決めうちする필요があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------

;────────────────────────────────────
;ACT95, 정상위시킨다
;────────────────────────────────────
;・죠교자が죠교대상に정상위での삽입を命じる
;・죠교대상に페니스、페니스밴드がない場合は発生しない
;・죠교자が페니스밴드장착中の場合は発生しない
;・죠교대상が누움、かつ성교봉사중でない場合は発生しない
;・죠교대상の발기도が1000以下の場合は発生しない
;・밀어넘어지는중には発生しない
;・죠교자が남자の場合は発生しない
;・죠교대상が오나홀사용중の場合には発生しない
;・성교중からは派生しない
;・배면좌위での성교봉사중からは派生しない
;・Ａ성교봉사중からは派生しない
;・主導権は죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT분류が"날뛴다", "거부"であった場合は취소される
;・죠교대상が동정なら동정상실が起こる
;・죠교자が처녀なら처녀상실が起こる
;・これらの処理は@ACTION_APPLY2_95にて行われる
;-------------------------------------------------
@KOJO_ACT_K1_95
;-------------------------------------------------
;・TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사系から/3=Ａ성교系から/4=Ｖ성교系から/5=Ａ성교봉사系から)
;※v1.010現在、ACT파생3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT95, 정상위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT파생によって分岐
;・各派生内で애태움도、페니스밴드、가버려、죠교자の속옷の有無等で分岐する
;・필요に応じて分岐を増減して下さい
;-------------------------------------------------
IF 구상記入チェック(100)
	;=================================================
	;처녀・동정
	;=================================================
	IF 구상記入チェック(100) && TRUECHECK(TALENT:처녀, TALENT:MASTER:동정) > 0
		;죠교자が처녀＆죠교대상が동정
		IF TALENT:처녀 && TALENT:MASTER:동정
			;ムードが발りないので再考
			;PRINTFORMW %CALLNAME:MASTER%がやり方が分からないと正直に話すと%CALLNAME:TARGET%はリボンをパタパタと羽ばたかせた
			;PRINTFORML
			;PRINTFORMW 「大丈夫、私も했다事ないけれど…こう、真っ直ぐ」
			;PRINTFORMW 「ぐぐーっと行けば…なんとかなるわよ」
			;PRINTFORML 
			;PRINTFORMW 大雑把だった
			
			PRINTFORMW 「최초부터 능숙하게 하려고 생각하지 않아 좋아요」
			PRINTFORMW 「생각하는 진에 하면, 우선은 그것으로 좋으니까」
			PRINTFORMW 「오며」
		[SKIPSTART]
		コメントアウトしておきます@/L
		;죠교자が처녀
		ELSEIF TALENT:처녀
			PRINTFORMW 
		;죠교대상が동정
		ELSE
			PRINTFORMW 
		[SKIPEND]
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:안빼고(죠교대상が사정後페니스를안빼고성교を続行했다횟수)
	;TFLAG:안빼고 > 1などとする事で안빼고の횟수参照は可能
	;죠교자욕망、기교、Ｖ/Ａ감각、성교ABLによって보정を受ける
	;금일의방침が애만들기だと+보정
	;要は질내사정後、안빼고に성교봉사系ACTが実行당했다というシチュエーション
	;성교系ACTの場合TFLAG:안빼고は参照できない
	;=================================================
	;안빼고に続行
	;=================================================
	IF 구상記入チェック(100) && TFLAG:안빼고
		PRINTFORMW 「 아직, 부족하다？좋아요」
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상が가버려な場合はTCVAR:MASTER:가버려
	;죠교자の場合はPALAM:쾌Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;연속(전회ACTが정상위시킨다)
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
		;애태움도による派生
		;SELECTCASE TCVAR:MASTER:애태움도
		;	;애태움中
		;	CASE IS > 1
		;		PRINTFORMW 
		;	;애태움終了
		;	CASE 1
		;		PRINTFORMW 
		;	;통상の연속실행
		;	CASEELSE
		;		PRINTFORMW 
		;ENDSELECT
		SIF TCVAR:MASTER:가버려
			PRINTFORMW （%RAND_SPLIT("갈 것 같은 자지, 출납되고 있다/사정할 때의 표정 하고 있다/안쪽에서 동글동글 하고 있다…이것,질내에사정할 생각")%）
		PRINTFORMDW %CALLNAME:TARGET%는 눈동자를 물기를 띠게 하고 있다
		;RETURNはさせず追加描写の形でいいかも
		;RETURN 1
	ENDIF
	
	;TEQUIP:성교봉사중 == 5(배면좌위시킨다)からは派生しない
	;전회ACTはIS_PREVACTNAME("대면좌위시킨다")等で参照できる
	;ただし、전회의体位はTEQUIPで判定されるので전회ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ성교봉사から)
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT파생 == 2
		;体位特有の風味が出ていないので再考する
		;애태움도による派生
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움中
			CASE IS > 1
				PRINTFORM 「 아직, 안돼
				CALL HEARTBW
			;애태움終了
			CASE 1
				SELECTCASE !RAND:2
					CASE 0
						PRINTFORM 「너의 좋아하게,%RAND_SPLIT("/좋아하게")%해도 좋아요
						CALL HEARTBW
					CASEELSE
						;레이무さんが안에 내!
						
						PRINTFORMW 「이대로%RAND_SPLIT("질내에/")%, 질내에 사정하며」
				ENDSELECT
			;통상の연속실행
			CASEELSE
				PRINTFORMW 「전부터(분)편이 안심할 수 있어요」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ성교から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ성교系から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ성교봉사から
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT파생 == 5
		PRINTFORMW 「엉덩이보다, 여기가 좋네요」
		RETURN 1
	ENDIF
	
	;基本的に삽입時に죠교대상が가버려であるという状況を想定
	;=================================================
	;죠교대상が가버려
	;=================================================
	IF 구상記入チェック(100) && TCVAR:MASTER:가버려
		PRINTFORMW 「말해버릴 것 같은 참고 있는 너도 사랑스러워요」
		PRINTFORM 「 그렇지만, 이제 한계 그렇구나？응후후
		CALL HEARTBW
		SELECTCASE RAND:3
			CASE 0
				PRINTFORML 
				PRINTFORMDW %CALLNAME:TARGET%는 침상에 앉아, 스스로의 음순을 손가락으로 나누어 연다
				PRINTFORML 
				PRINTFORM 「와
				CALL HEARTBW
				PRINTFORMW 「삽입전에 사정해 버렸다들 화내요？」
			CASE 1
				PRINTFORMW 「그러면 아, 안녕히 주무십시오」
				PRINTFORML 
				PRINTFORMW 「농담이니까 그렇게 울 것 같은 얼굴 하지 않고」
				PRINTFORMW 「%RAND_SPLIT("나라고했다있는이니까/끝까지 해도 좋으니까")%, 어 통치합시다」
			CASEELSE
				PRINTFORMW 「그러면 아, 안녕히 주무십시오」
				PRINTFORML 
				PRINTFORMDW %CALLNAME:TARGET%는 침상에 누워 버린다
				PRINTFORML 
				PRINTFORMW 「…너%RAND_SPLIT("풋내기/둔감/안쪽손")%그러니까 만약을 위해 말하지만,」
;OriginalString : 인12, "「襲っていいわよ」")
				CALL FADEIN(12, "「덮치고 좋아요」")
			ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMDW %CALLNAME:TARGET%는 팔을 %CALLNAME:MASTER%에 늘린다
		PRINTFORMW 「이봐요, 오세요」
		PRINTFORMW 「 나는 눕고 있으니, 자지로 제대로 봉사해 주세요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT96,후배위시킨다
;────────────────────────────────────
;・죠교자が죠교대상に후배위での삽입を命じる
;・죠교대상に페니스、페니스밴드がない場合は発生しない
;・죠교자が페니스밴드장착中の場合は発生しない
;・죠교대상が누움、かつ성교봉사중でない場合は発生しない
;・죠교대상の발기도が1000以下の場合は発生しない
;・밀어넘어지는중には発生しない
;・죠교자が남자の場合は発生しない
;・죠교대상が오나홀사용중の場合には発生しない
;・성교중からは派生しない
;・대면좌위での성교봉사중からは派生しない
;・Ａ성교봉사중からは派生しない
;・主導権は죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT분류が"날뛴다", "거부"であった場合は취소される
;・죠교대상が동정なら동정상실が起こる
;・죠교자が처녀なら처녀상실が起こる
;・これらの処理は@ACTION_APPLY2_96にて行われる
;-------------------------------------------------
@KOJO_ACT_K1_96
;-------------------------------------------------
;・TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사系から/3=Ａ성교系から/4=Ｖ성교系から/5=Ａ성교봉사系から)
;※v1.010現在、ACT파생3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT96,후배위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT파생によって分岐
;・各派生内で애태움도、페니스밴드、가버려、죠교자の속옷の有無等で分岐する
;・필요に応じて分岐を増減して下さい
;-------------------------------------------------
IF 구상記入チェック(100)
	;=================================================
	;처녀・동정
	;=================================================
	IF 구상記入チェック(100) && TRUECHECK(TALENT:처녀, TALENT:MASTER:동정) > 0
		;죠교자が처녀＆죠교대상が동정
		IF TALENT:처녀 && TALENT:MASTER:동정
		PRINTFORMDW %CALLNAME:MASTER%(은)는 %CALLNAME:TARGET%의 엉덩이를 잡아, 수직근에 귀두를 꽉 눌렀다
		PRINTFORMW 「좀 더 위…개-와 밀어넣으며」
		;言われてみたいがムードが발りない。후배위ならそれでいいのかもしれない
		;PRINTFORM 「あんたの동정ちんぽで%CALLNAME:TARGET%의 事、非처녀にして
		CALL HEARTBW
		[SKIPSTART]
		コメントアウトしておきます@/L
		;죠교자が처녀
		ELSEIF TALENT:처녀
			PRINTFORMW 「상냥하게 해 주세요」
		;죠교대상が동정
		ELSE
			PRINTFORMW 
		[SKIPEND]
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:안빼고(죠교대상が사정後페니스를안빼고성교を続行했다횟수)
	;TFLAG:안빼고 > 1などとする事で안빼고の횟수参照は可能
	;죠교자욕망、기교、Ｖ/Ａ감각、성교ABLによって보정を受ける
	;금일의방침が애만들기だと+보정
	;要は질내사정後、안빼고に성교봉사系ACTが実行당했다というシチュエーション
	;성교系ACTの場合TFLAG:안빼고は参照できない
	;=================================================
	;안빼고に続行
	;=================================================
	IF 구상記入チェック(0) && TFLAG:안빼고
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상が가버려な場合はTCVAR:MASTER:가버려
	;죠교자の場合はPALAM:쾌Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;연속(전회ACTが후배위시킨다)
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
		;애태움도による派生
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움中
			;CASE IS > 1
			;	PRINTFORMW 
			;애태움終了
			CASE 1
				;中は駄目って言ってるでしょちゃんと中に出しなさい
				PRINTFORMW 「안돼, (안)중 안돼, 안돼, (안)중이고 안돼」
			;통상の연속실행
			CASEELSE
			;	PRINTFORMW 
				RETURN 0
		ENDSELECT
		RETURN 1
	ENDIF
	
	;TEQUIP:성교봉사중 == 4(대면좌위시킨다)からは派生しない
	;전회ACTはIS_PREVACTNAME("정상위시킨다")等で参照できる
	;ただし、전회의体位はTEQUIPで判定されるので전회ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ성교봉사から)
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT파생 == 2
		;애태움도による派生
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움中
			CASE IS > 1
				PRINTFORMW 「이번은 뒤로부터, 군요」
				PRINTFORMW 「 내가 기분이 내킬 때까지는, 사정하면 안되기 때문에」
			;애태움終了
			CASE 1
				;野生と品性。下品さ直球さの塩梅がいまひとつ
				PRINTFORMW 「최후는 뒤로부터」
				PRINTFORMW 「교배 교미로 사정해버릴 없음 있고」
			;통상の연속실행
			CASEELSE
				;からしが気になる。あまりカタカナは使いたくない
				;PRINTFORMW 「今度は、後ろからして」
				PRINTFORMW 「이번은 뒤로부터, 군요」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ성교から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ성교系から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ성교봉사から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;싫증상태が活かせていないので何か변화球。イメージ플레이に走る。싫어한다부끄러워한다に対応できるようなもの。
	;=================================================
	;%CALLNAME:TARGET%が싫증
	;=================================================
	IF 구상記入チェック(25) && IS_CONDITION(TARGET, "싫증")
		PRINTFORMW 「가끔 씩은 취향을 바꾸어 볼까」
		PRINTFORMW 「네가 남편님이나 리나 차이」
		PRINTFORMW 「간단해요. 나의 일자기 마음대로 범해 준다면 좋아요」
		PRINTFORMW 「평상시와 변함없다？차이원이군요」
		RETURN 1
	ENDIF
	
	;基本的に삽입時に죠교대상が가버려であるという状況を想定
	;=================================================
	;죠교대상が가버려
	;=================================================
	IF 구상記入チェック(100) && TCVAR:MASTER:가버려
		PRINTFORML 「 이제 안된다는 것은 표정이군요？응후후♪」
		PRINTFORMW 「내고 싶다？내고 싶어요？자지 뛰게 해 정액하고 싶어요？」
		PRINTFORML
		PRINTFORMW 「좋아요」
		PRINTFORMW 「와♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		IF !RAND:2
			PRINTFORMW 「\@ IS_CONDITION(TARGET, "정욕") ? 개- # 교미 \@합시다」
			PRINTFORMDW %CALLNAME:TARGET%의 순산형의 엉덩이가 %CALLNAME:MASTER%안의유우를 부른다
		ELSE
			;後背비패개장
			PRINTFORMW 「오며」
			PRINTFORMDW %CALLNAME:TARGET%는 스스로 비육을 나누어 넓혀%CALLNAME:MASTER%(을)를 권하고 있다
		ENDIF
		
		;수줍음不발、課題が残る
		PRINTFORMW 「%CALLNAME:TARGET%의 만 와 응 비비 꼬고 글자 붐비며」
		;12から短縮
;OriginalString : 인6, "「た」")
		CALL FADEIN(6, "「」")
;OriginalString : 인6, "「ね」")
		CALL FADEIN(6, "「저기」")
;OriginalString : 인6, "「つ」")
		CALL FADEIN(6, "「개」")
;OriginalString : 인6, "「け」")
		CALL FADEIN(6, "「케」")
		PRINTFORMW 「해♪」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT97, 대면좌위시킨다
;────────────────────────────────────
;・죠교자が죠교대상に대면좌위での삽입を命じる
;・죠교대상に페니스、페니스밴드がない場合は発生しない
;・죠교자が페니스밴드장착中の場合は発生しない
;・죠교대상が누움、かつ성교봉사중でない場合は発生しない
;・죠교대상の발기도が1000以下の場合は発生しない
;・밀어넘어지는중には発生しない
;・죠교자が남자の場合は発生しない
;・죠교대상が오나홀사용중の場合には発生しない
;・성교중からは派生しない
;・배면좌위での성교봉사중からは派生しない
;・Ａ성교봉사중からは派生しない
;・主導権は죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT분류が"날뛴다", "거부"であった場合は취소される
;・죠교대상が동정なら동정상실が起こる
;・죠교자が처녀なら처녀상실が起こる
;・これらの処理は@ACTION_APPLY2_97にて行われる
;-------------------------------------------------
@KOJO_ACT_K1_97
;-------------------------------------------------
;・TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사系から/3=Ａ성교系から/4=Ｖ성교系から/5=Ａ성교봉사系から)
;※v1.010現在、ACT파생3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT97, 대면좌위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT파생によって分岐
;・各派生内で애태움도、페니스밴드、가버려、죠교자の속옷の有無等で分岐する
;・필요に応じて分岐を増減して下さい
;-------------------------------------------------
IF 구상記入チェック(100)
	;=================================================
	;처녀・동정
	;=================================================
	IF 구상記入チェック(0) && TRUECHECK(TALENT:처녀, TALENT:MASTER:동정) > 0
		;죠교자が처녀＆죠교대상が동정
		IF TALENT:처녀 && TALENT:MASTER:동정
			PRINTFORMW 
		;죠교자が처녀
		ELSEIF TALENT:처녀
			PRINTFORMW 
		;죠교대상が동정
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:안빼고(죠교대상が사정後페니스를안빼고성교を続行했다횟수)
	;TFLAG:안빼고 > 1などとする事で안빼고の횟수参照は可能
	;죠교자욕망、기교、Ｖ/Ａ감각、성교ABLによって보정を受ける
	;금일의방침が애만들기だと+보정
	;要は질내사정後、안빼고に성교봉사系ACTが実行당했다というシチュエーション
	;성교系ACTの場合TFLAG:안빼고は参照できない
	;=================================================
	;안빼고に続行
	;=================================================
	IF 구상記入チェック(100) && TFLAG:안빼고
		PRINTFORMW 「 아직, 놓아 주지 않기 때문에」
		[SKIPSTART]
		;地の文と被る為コメントアウト
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 몸에 팔을 돌려 안아 도착해 왔다
		[SKIPEND]
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%(을)를 물기를 띤 눈동자로 응시하면서 허리를 구군요들 키라고 있다
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상が가버려な場合はTCVAR:MASTER:가버려
	;죠교자の場合はPALAM:쾌Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;연속(대면좌위시킨다から)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
		;애태움도による派生
		;SELECTCASE TCVAR:MASTER:애태움도
		;	;애태움中
		;	CASE IS > 1
		;		PRINTFORMW 
		;	;애태움終了
		;	CASE 1
		;		PRINTFORMW 
		;	;통상の연속실행
		;	CASEELSE
		;		PRINTFORMW 
		;ENDSELECT
		RETURN 1
	ENDIF
	
	;TEQUIP:성교봉사중 == 2(후배위시킨다)/TEQUIP:성교봉사중 == 5(배면좌위시킨다)からは派生しない
	;전회ACTはIS_PREVACTNAME("정상위시킨다")等で参照できる
	;ただし、전회의体位はTEQUIPで判定されるので전회ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ성교봉사から)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
		;애태움도による派生
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움中
			CASE IS > 1
				PRINTFORMW 
			;애태움終了
			CASE 1
				PRINTFORMW 
			;통상の연속실행
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ성교から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ성교系から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ성교봉사から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に삽입時に죠교대상が가버려であるという状況を想定
	;=================================================
	;죠교대상が가버려
	;=================================================
	IF 구상記入チェック(100) && TCVAR:MASTER:가버려
		;PRINTFORMW 「もう사정そう？あんたの事、一番近くで見ててあげるわ」
		PRINTFORMW 「 이제 사정 그렇게？네가 말해버리는 곳, 보고 있어 주어요」
		PRINTFORML 「이봐요 이렇게, 제일 근처에서」
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%%RAND_SPLIT("에 제대로 안아 도착해 왔다/의 이마에 이마를 붙여 왔다/에 웃는 얼굴을 보여 주었다")%
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		SELECTCASE RAND:2
			;%CALLNAME:MASTER%攻め
			CASE 0
				PRINTFORMW 「너의 팔안, 따뜻하네요」
;OriginalString : 아웃8, "「んふふ♪」")
				CALL FADEOUT(8, "「응후후♪」")
				;PRINTFORML
				;この손の描写は初回限定が望ましい //こんな感じでどうでしょうか？ @/L //提案ありがとうございます@宿禰
				SIF FIRSTACT(80)
					PRINTFORMDW 난폭하게 행동하는 %CALLNAME:TARGET%를 팔안에 안아 보면, 그 몸은 가냘프고, 작았다
			;%CALLNAME:TARGET%攻め
			CASEELSE
				;PRINTFORMW 「あったまりましょ」
				PRINTFORM 「편안히 합시다
				CALL HEARTBW
;OriginalString : 아웃8, "「うふふ♪」")
				CALL FADEOUT(8, "「우후후♪」")
				PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%에 안아 도착해 왔다
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT98,배면좌위시킨다
;────────────────────────────────────
;・죠교자が죠교대상に배면좌위での삽입を命じる
;・죠교대상に페니스、페니스밴드がない場合は発生しない
;・죠교자が페니스밴드장착中の場合は発生しない
;・죠교대상が누움、かつ성교봉사중でない場合は発生しない
;・죠교대상の발기도が1000以下の場合は発生しない
;・밀어넘어지는중には発生しない
;・죠교자が남자の場合は発生しない
;・죠교대상が오나홀사용중の場合には発生しない
;・성교중からは派生しない
;・정상위、대면좌위での성교봉사중からは派生しない
;・Ａ성교봉사중からは派生しない
;・主導権は죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT분류が"날뛴다", "거부"であった場合は취소される
;・죠교대상が동정なら동정상실が起こる
;・죠교자が처녀なら처녀상실が起こる
;・これらの処理は@ACTION_APPLY2_98にて行われる
;-------------------------------------------------
@KOJO_ACT_K1_98
;-------------------------------------------------
;・TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사系から/3=Ａ성교系から/4=Ｖ성교系から/5=Ａ성교봉사系から)
;※v1.010現在、ACT파생3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT98,배면좌위시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT파생によって分岐
;・各派生内で애태움도、페니스밴드、가버려、죠교자の속옷の有無等で分岐する
;・필요に応じて分岐を増減して下さい
;-------------------------------------------------
IF 구상記入チェック(100)
	;=================================================
	;처녀・동정
	;=================================================
	;IF 구상記入チェック(0) && TRUECHECK(TALENT:처녀, TALENT:MASTER:동정) > 0
	;	;죠교자が처녀＆죠교대상が동정
	;	IF TALENT:처녀 && TALENT:MASTER:동정
	;		PRINTFORMW 
	;	;죠교자が처녀
	;	ELSEIF TALENT:처녀
	;		PRINTFORMW 
	;	;죠교대상が동정
	;	ELSE
	;		PRINTFORMW 
	;	ENDIF
	;	RETURN 1
	;ENDIF
	
	;ソース入손数 体感発生率 共に高い優遇コマンド
	;주인님にとって好ましい状況を意識する 背中から寄りかかられ、かつ、삽입失敗の目がある
	
	;당신에의 호감도[1589]레이무에의 호감도[1666]동정처녀にて踏み切る。
	;テスト時の状況は기력切れ부끄러워한다一択。実行後당신が가버려なまま죠교を終了。ひとまずこれをモデルにする。
	;かなり早期。상사상애の前に発生すると考えた方が良い。
	;일선을넘지않음레이무に一線を越え시킨다難題。
	;体位を活かしどちらが超えたか曖昧な境界線上の灰色を表現してみる。
	
	;ずかずかとマヨヒガに押し入る異変解決時の問答無用さは、당신に거부権が残っている為に使えそうにもない
	;처녀性と強引さと抵抗の余地と選ばれるであろう부끄러워한다
	;「『삽입しろ』とは言ったが『삽입しろ』とは言ってない。」は成立するだろうか？
	IF 구상記入チェック(100) && TALENT:처녀
		;Ｃへの刺激の確保
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 무릎 위에 걸터앉으면(자) 엉덩이로 음경을 눌러 잡아, %CALLNAME:MASTER%의 표정을 확인하면서 허리를 구 자고들 키
;OriginalString : 아웃8, "  ちゅく…")
		CALL FADEOUT(8, "  츄쿠…")
		;ここで一捻りするとどうだろう。당신主導を仄めかす。
		PRINTFORMDW %CALLNAME:MASTER%의 음경은 한계까지 부피를 늘려,
		PRINTFORMDW 한계까지 부풀어 올라 젖은 귀두가 %CALLNAME:TARGET%의 유질구를 확대한다
;OriginalString : 아웃8, "  …ごくっ")
		CALL FADEOUT(8, "  …꿀꺽")
		PRINTFORMW 「여기, 군요…？」
		;小刻みに速めていく
;OriginalString : 인6, "  ちゅく…")
		CALL FADEIN(6, "  츄쿠…")
;OriginalString : 아웃6, "  「んっ…」")
		CALL FADEOUT(6, "  「읏…」")
		;당신の消極性を残하면서가슴に손を促す。
		PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 수를 잡아 가슴에 안으면(자), 유질구로 귀두의 앞이 얕고 질네 붐빈 채로에
		PRINTFORMDW 머뭇머뭇허리를 강탈 귀두의 앞을 빚었다
;OriginalString : 인4, "  ちゅく…")
		CALL FADEIN(4, "  츄쿠…")
;OriginalString : 인4, "  ちゅくちゅっ…")
		CALL FADEIN(4, "  츄쿠츗…")
;OriginalString : 아웃6, "  「…んっんっ」")
		CALL FADEOUT(6, "  「…읏읏」")
		PRINTFORMW 「이렇게 하고 있으면, 삽입있을 것 같구나」
		;합의를 요구한다は別にコマンドがある
		;結局레이무が入れたのか당신が入れたのか曖昧に
		;同意を求めつつ합의は取らず命じたようで命じていないようにも聞こえる一文字
		PRINTFORMW 「이군요.」
	ENDIF
	
	;TFLAG:안빼고(죠교대상が사정後페니스를안빼고성교を続行했다횟수)
	;TFLAG:안빼고 > 1などとする事で안빼고の횟수参照は可能
	;죠교자욕망、기교、Ｖ/Ａ감각、성교ABLによって보정を受ける
	;금일의방침が애만들기だと+보정
	;要は질내사정後、안빼고に성교봉사系ACTが実行당했다というシチュエーション
	;성교系ACTの場合TFLAG:안빼고は参照できない
	;=================================================
	;안빼고に続行
	;=================================================
	IF 구상記入チェック(0) && TFLAG:안빼고
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상が가버려な場合はTCVAR:MASTER:가버려
	;죠교자の場合はPALAM:쾌Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;연속(전회ACTが배면좌위시킨다)
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
		;애태움도による派生
		;SELECTCASE TCVAR:MASTER:애태움도
		;	;애태움中
		;	CASE IS > 1
		;		PRINTFORMW 
		;	;애태움終了
		;	CASE 1
		;		PRINTFORMW 
		;	;통상の연속실행
		;	CASEELSE
		;		PRINTFORMW 
		;ENDSELECT
		;=================================================
		;싫증
		;=================================================
		IF 구상記入チェック(25) && IS_CONDITION(TARGET, "싫증")
			PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%%RAND_SPLIT("노가슴에 푹 수하며 /에 기대고 담당자")%숨소리를 내고 있다…
			RETURN 1
		ENDIF
		
		IF 구상記入チェック(100)
			PRINTFORMW 「이 몸의 자세라고, 나부터 할 수 있는 일은 그다지 없어」
			;頂戴が硬い気がする
			;PRINTFORMW 「あんたが気持ちよくして頂戴」
			PRINTFORMW 「그런데, 기분 좋게 하며」
			RETURN 1
		ENDIF
	ENDIF
	
	;TEQUIP:성교봉사중 == 1(정상위시킨다)/TEQUIP:성교봉사중 == 4(대면좌위시킨다)からは派生しない
	;전회ACTはIS_PREVACTNAME("후배위시킨다")等で参照できる
	;ただし、전회의体位はTEQUIPで判定されるので전회ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ성교봉사から)
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT파생 == 2
		[SKIPSTART]
		コメントアウトしておきます@/L
		;애태움도による派生
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움中
			CASE IS > 1
				PRINTFORMW 
			;애태움終了
			CASE 1
				PRINTFORMW 
			;통상の연속실행
			CASEELSE
		ENDSELECT
		[SKIPEND]
		PRINTFORMW 「영차와…팔, 목에 돌려？」
		PRINTFORMW 「네에에♪」
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ성교から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ성교系から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ성교봉사から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に삽입時に죠교대상が가버려であるという状況を想定
	;=================================================
	;죠교대상が가버려
	;=================================================
	IF 구상記入チェック(100) && TCVAR:MASTER:가버려
		PRINTFORML 「너의 일이니까, 앉아지는 것이 제일 기분이 좋아요？」
		PRINTFORMW 「삽여(어) 주기 때문에, 좀 더 만%RAND_SPLIT("분발함/참고 견뎌/참다해")%없음 있고」
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORML 「영차」
		PRINTFORMW 「너의 안정감, 꽤 나쁘지 않아요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT99, 항문섹스시킨다
;────────────────────────────────────
;・죠교자が죠교대상に엉덩이への삽입を命じる
;・죠교대상に페니스、페니스밴드がない場合は発生しない
;・죠교자が페니스밴드장착中の場合は発生しない
;・죠교대상が누움、かつ성교봉사중でない場合は発生しない
;・죠교대상の발기도が1000以下の場合は発生しない
;・턴방침が"소프트"でＡ성교봉사중でない場合は発生しない
;・밀어넘어지는중には発生しない
;・죠교자が남자の場合は発生しない
;・죠교대상が오나홀사용중の場合には発生しない
;・성교중からは派生しない
;・Ｖ성교봉사중からは派生しない
;・죠교자のＡ감각が2以下の場合は発生しない
;・主導権は죠교대상"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT분류が"날뛴다", "거부"であった場合は취소される
;-------------------------------------------------
@KOJO_ACT_K1_99
;-------------------------------------------------
;・TFLAG:ACT파생(0=통상/1=연속/2=Ａ성교系から/3=Ｖ성교봉사系から/4=Ｖ성교系から)
;※v1.010現在、ACT파생2から4はACT_ABLEで規制されているので機能していない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT99, 항문섹스시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT파생1(연속)、ACT파생2 TO 5(Ａ성교/Ｖ성교봉사/Ｖ성교)、その他で分岐
;・各派生内で페니스밴드の有無で分岐する
;・필요に応じて分岐を増減して下さい
;-------------------------------------------------
IF 구상記入チェック(0)
	;TFLAG:안빼고(죠교대상が사정後페니스를안빼고성교を続行했다횟수)
	;TFLAG:안빼고 > 1などとする事で안빼고の횟수参照は可能
	;죠교자욕망、기교、Ｖ/Ａ감각、성교ABLによって보정を受ける
	;금일의방침が애만들기だと+보정
	;要は질내사정後、안빼고に성교봉사系ACTが実行당했다というシチュエーション
	;성교系ACTの場合TFLAG:안빼고は参照できない
	;=================================================
	;안빼고に続行
	;=================================================
	IF 구상記入チェック(0) && TFLAG:안빼고
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상が가버려な場合はTCVAR:MASTER:가버려
	;죠교자の場合はPALAM:쾌Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;연속(전회ACTが항문섹스시킨다)
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 1
		;애태움도による派生
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움中
			CASE IS > 1
				PRINTFORMW 
			;애태움終了
			CASE 1
				PRINTFORMW 
			;통상の연속실행
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ성교系から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ성교봉사系から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ성교系から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に삽입時に죠교대상が가버려であるという状況を想定
	;=================================================
	;죠교대상が가버려
	;=================================================
	IF 구상記入チェック(0) && TCVAR:MASTER:가버려
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT103, 역강간
;────────────────────────────────────
;・죠교자が기승위で強引に성교渉に及ぶ
;・죠교대상に페니스、페니스밴드がない場合は発生しない
;・죠교자が페니스밴드장착中の場合は発生しない
;・죠교대상の발기도が1000以下の場合は発生しない
;・죠교자が남자の場合は発生しない
;・죠교대상が오나홀사용중の場合には発生しない
;・성교중からは派生しない
;・Ａ성교봉사중からは派生しない
;・主導権は죠교대상"SEX_INITIATIVE(TARGET)"
;-------------------------------------------------
;・他の성교봉사系と異なり、죠교대상は抵抗できない
;・죠교대상が동정なら동정상실が起こる
;・죠교자が처녀なら처녀상실が起こる
;・これらの処理は@ACTION_APPLY2_103にて行われる
;-------------------------------------------------
@KOJO_ACT_K1_103
;-------------------------------------------------
;・TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사系から/3=Ａ성교系から/4=Ｖ성교系から/5=Ａ성교봉사系から)
;・v1.010現在、ACT파생3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;ACT103, 역강간
;━━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT파생によって分岐
;・各派生内で애태움도、페니스밴드、가버려、죠교자の속옷の有無等で分岐する
;・필요に応じて分岐を増減して下さい
;-------------------------------------------------
IF 구상記入チェック(100)
	;=================================================
	;처녀・동정
	;=================================================
	IF 구상記入チェック(100) && TRUECHECK(TALENT:처녀, TALENT:MASTER:동정) > 0
		;죠교자が처녀＆죠교대상が동정
		IF TALENT:처녀 && TALENT:MASTER:동정
			PRINTFORMW 「너의 처음으로, 받아요」
			PRINTFORML 「대신에 나의 처음으로 주기 때문에, 좋아요？」
		[SKIPSTART]
		コメントアウトしておきます@/L
		;죠교자が처녀
		ELSEIF TALENT:처녀
			;%CALLNAME:MASTER%が初期상태で非동정な場合もある
			PRINTFORMW 「 나의 처음으로, 네가 받으세요」
			PRINTFORMW 「 나는, 네가 좋아」
		;죠교대상が동정
		ELSE
			PRINTFORMW 
		[SKIPEND]
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:안빼고(죠교대상が사정後페니스를안빼고성교を続行했다횟수)
	;TFLAG:안빼고 > 1などとする事で안빼고の횟수参照は可能
	;죠교자욕망、기교、Ｖ/Ａ감각、성교ABLによって보정を受ける
	;금일의방침が애만들기だと+보정
	;要は질내사정後、안빼고に성교봉사系ACTが実行당했다というシチュエーション
	;성교系ACTの場合TFLAG:안빼고は参照できない
	;=================================================
	;안빼고に続行
	;=================================================
	;사정してから안빼고に続けた場合、体位変更時にここに来る
	IF 구상記入チェック(100) && TFLAG:안빼고
		PRINTFORMW 「내가 해 주기 때문에, 너는 편하게 해 주세요」
		PRINTFORML 「내가 만족할 때까지는, 교제해 주어요」
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;죠교대상が가버려な場合はTCVAR:MASTER:가버려
	;죠교자の場合はPALAM:쾌Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;연속(전회ACTが역강간)
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT파생 == 1
		;=================================================
		;하드にしてに応じる追加描写
		;=================================================
		IF 구상記入チェック(100) && IS_PREVCOMNAME("더 하드하게 해")
			PRINTFORM 「소망 대로, 격렬하게 해 준다
			CALL HEARTBW
			PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 양어깨를 억누르면서 격렬하게 허리를 부딪혀 왔다
			IF EXP:TARGET:Ｖ경험 < 10
;OriginalString : 아웃4, "「んんっ！！」")
				CALL FADEOUT(4, "「응읏！！」")
				PRINTFORMDL （아직, 상해요…）
			ENDIF
		ENDIF
		;=================================================
		;애태움도による派生
		;=================================================
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움中
			CASE IS > 1
				IF TFLAG:징계・플래그
					PRINTFORMW 「 아직 사정하면 안되어요？이것은 징계 위난이니까」
					PRINTFORMW 「가득 범해 주기 때문에 간단하게 끝내지 마」
				ELSE
					IF IS_CONDITION(TARGET, "정욕")
						PRINTFORML 「 아직 맡기고니까」
						PRINTFORMDW %CALLNAME:TARGET%는 즐거운 듯이 미소지었다
						PRINTFORM 「이봐요, 힘내라 힘내라
						CALL HEARTBW
					ENDIF
						PRINTFORMW 「 아직 사정하면 안되어요」
						PRINTFORML 「 나보다 먼저 말하면…뒤쫓아 주기 때문에…」
				ENDIF
			;애태움終了
			CASE 1
				IF PALAM:쾌Ｖ > 8500
					PRINTFORMW 「 나도 이제…좋아요」
					PRINTFORML 「안에 내며」
				ELSE
					PRINTFORMW 「 이제들 째？억지로들 째？등째？」
;OriginalString : 아웃4, "「くすっ」")
					CALL FADEOUT(4, "「키득」")
					PRINTFORM 「%RAND_SPLIT("좋으니까/안되지 않으니까")%나의 질내에서, 사정해버릴 없음 있고
					CALL HEARTB
				ENDIF
			;통상の연속실행
			CASEELSE
				IF 구상記入チェック(100) && PALAM:쾌Ｖ > 8500 && !TCVAR:MASTER:가버려
					PRINTFORMW 「안돼…나…안돼…」
					PRINTFORMDW %CALLNAME:TARGET%는 절정 직전에 견디면서 천천히 허리를 움직이고 있다
				ENDIF
				IF 구상記入チェック(100) && IS_CONDITION(MASTER, "정욕")
					PRINTFORMW 「무리하게기분 있고？기분이 좋다？」
					PRINTFORML 「-응」
					PRINTFORMDL %CALLNAME:TARGET%는 정욕에 녹는다%CALLNAME:MASTER%(을)를 만족스럽게 업신여기고 있다
				ENDIF
				IF 구상記入チェック(100)
					PRINTFORMW 「 나아래에서 허덕이는 너를 보고 있으면, 기뻐져요」
					PRINTFORM 「 좀 더 해 준다
					CALL HEARTBW
				ENDIF
		ENDSELECT
		RETURN 1
	ENDIF
	;どの体位からも派生する可能性あり
	;=================================================
	;体位変更(Ｖ성교봉사から)
	;=================================================
	IF 구상記入チェック(100) && TFLAG:ACT파생 == 2
		;애태움도による派生
		SELECTCASE TCVAR:MASTER:애태움도
			;애태움中
			CASE IS > 1
				;PRINTFORMW 
				RETURN 0
			;애태움終了
			;CASE 1
				;PRINTFORMW 
				;RETURN 0
			;애태움なし
			CASEELSE
				;シューティング要素を織り込むのは無理があるのかもしれない
				PRINTFORML 「준비는 좋아. 너의 %RAND_SPLIT("자식/정자/아기")%(은)는, 제대로 받아 줄게」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ성교から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ성교系から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ성교봉사から
	;=================================================
	IF 구상記入チェック(0) && TFLAG:ACT파생 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に삽입時に죠교대상が가버려であるという状況を想定
	;=================================================
	;죠교대상が가버려
	;=================================================
	IF 구상記入チェック(100) && TCVAR:MASTER:가버려
		;=================================================
		;임신中 孕ませについて言及しない ひとまず条件なしに送る
		;=================================================
		IF 구상記入チェック(0) && TALENT:임신
			[SKIPSTART]
			;PRINTFORMW 「どうせ사정すなら、ここがいいわよね？」
			;矛盾の可能性有コメントアウト
			;PRINTFORMW 「お望み通り삽입れてあげるから、もう少しだけがんばりなさい」
			[SKIPEND]
			RETURN 1
		ENDIF
		;=================================================
		;레이무분노の中出させ
		;=================================================
		IF 구상記入チェック(100) && IS_CONDITION(TARGET, "분노")
			PRINTFORML 「좋으니까 냉큼 사정해 주세요！」
			PRINTFORMDL %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 어깨를 억누르면서 허리를 부딪혀 왔다
			RETURN 1
		ENDIF
		;=================================================
		;죠교대상が가버려임신中でない
		;=================================================
		IF 구상記入チェック(100) && !TALENT:임신
			;記入箇所ミスの予感
			;♪は無い方が冷淡さが出せる
			PRINTFORMW 「너%RAND_SPLIT("임신할 때까지/임신할 때까지/갓난 아기 생기게 될 때까지/몇번이나")%몇 번이라도, 범해 주어요」
			PRINTFORM 「후후
			CALL HEARTBW
			PRINTFORMW 「%RAND_SPLIT("너의 자지는, 나/서방님의 자지는, 신부")%(을)를 잉다투기 위해 있다 것이군요？」
			;夫婦臭を強調する為당신表記
			PRINTFORMW 「%RAND_SPLIT("각오를 결정하세요？당·신/♪각오를 결정하세요？우후후…/너의 일이니까, 기쁘지요？어머 어머♪")%」
			RETURN 1
		ENDIF
	ENDIF
	;=================================================
	;%CALLNAME:TARGET%분노
	;=================================================
	IF 구상記入チェック(100) && IS_CONDITION(TARGET, "분노")
		PRINTFORMW 「좋으니까 냉큼 야등 등차이！」
	ENDIF
	;=================================================
	;その他・条件無し
	;=================================================
	IF 구상記入チェック(100)
		;おっぱい
		IF TALENT:거유 && !RAND:3
			PRINTFORMDW low angle로부터 올려보는 흔들리는 유방으로부터, %CALLNAME:MASTER%(은)는 눈을 일일을 할 수 없다
		;言いたい事は通じるが落ち着きのある推敲を要する
		;ELSEIF !TALENT:빈유 && !RAND:3
		;	PRINTFORMDW %CALLNAME:TARGET%의 下乳が下乳がおっぱいでおっぱいで%CALLNAME:MASTER%いっぱいいっぱいだ
		;リボン
		ELSEIF !RAND:2
			PRINTFORML 「너의 자식, 전부 짜 취해 주어요」
			PRINTFORMW 「진한 것 가득 사정함 없으면…%RAND_SPLIT("용납하지 않기 때문에/인내하지 않아요？")%」
			PRINTFORML
			PRINTFORMDW %CALLNAME:TARGET%는 리본을 톡톡홰치게 한다
		;黒髪の強調 真面目さ本気さ真剣さを出す
		ELSE
			PRINTFORMW 「짜 취해 주기 때문에, 각오 하며」
			PRINTFORMDW %CALLNAME:TARGET%는 아름다운 흑발을 늘어뜨리면서, %CALLNAME:MASTER%(을)를 진지한 시선으로 업신여기고 있다
		ENDIF
		RETURN 1
	ENDIF
	
	[SKIPSTART]
	;=================================================
	;未定稿、没鎬
	;=================================================
		;아라이멘도正
		IF ALI(0)
		;아라이멘도負
		ELSE
			;関係悪化は控えめに
			;PRINTFORMW 「すぐには終わらせないけど、%CALLNAME:MASTER%は天井のシミでも数えてなさい」
			
			;IF TALENT:MASTER:掃除の違いがわかる
			;	PRINTFORMDL その言葉に、솔직に天井を観察すると
			;	PRINTFORMDL 怠りがちな天井の掃除もよく行き届いていた…
			;ENDIF
			
			;IF TALENT:MASTER:おしりしゅごい
			;	PRINTFORMDL その言葉に逆らい視線を横に向けると枕元の로션とティッシュが目に入った
			;	PRINTFORMDL %CALLNAME:MASTER%의 お尻はキュッとなった
			;ENDIF
			
			;薄い本を持ち出すのは無し
			;PRINTFORML 「これでも読んでなさい」
			;PRINTFORML 
			;PRINTFORMDL 레이무から渡당했다薄い本には레이무との甘い生活が描かれている
			;PRINTFORMDL 레이무さん。こういうの、すきなの？
			
			;레이무さんち広いよ
			;PRINTFORMDL 四畳半の空の下、%CALLNAME:TARGET%に犯され\@ TIME ? ずぶずぶと夜の夢に沈む # 襖の隙間から細く広がる空を望む \@
		ENDIF
	;=================================================
	;삽입時に죠교대상が가버려であるという状況。条件が特定できずコメントアウト
	;=================================================
	PRINTFORML 「 이제 사정 그렇게？삽여(어) 주기 때문에 좀 더 만%RAND_SPLIT("분발함/참고 견뎌/참다해")%없음 있고」
	PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%%RAND_SPLIT("에 제대로 안아 도착해 왔다/의 이마에 이마를 붙여 왔다/에 웃는 얼굴을 보여 주었다")%
	PRINTFORMDW %CALLNAME:TARGET%에 넋을 잃고 보고 있는 동안에 %CALLNAME:MASTER%의 일물은 %CALLNAME:TARGET%의 질에 삼켜져 갔다
	;こちらのPRINTFORMLは残했다方が삽입중と삽입完了のタイムラグが出てくれるかも
	PRINTFORML 
	PRINTFORMW 「전부 삽입…제대로 참을 수 있었던 원이군요」
	PRINTFORMW 「훌륭해요」
	RETURN 1
	
	PRINTFORMDW 당장 사정해 버릴 것 같은 음경을 %CALLNAME:TARGET%의 녀음이 천천히 삼켜 간다
	PRINTFORML
	PRINTFORM 「응…%RAND_SPLIT("전부/가장 안쪽까지/너의 자지")%삽입
	CALL HEARTBW
	RETURN 1
	;=================================================
	;삽입即사정 %CALLNAME:TARGET%が정욕でエロエロ
	;=================================================
	IF 구상記入チェック(100) && IS_CONDITION(TARGET, "정욕") && TCVAR:MASTER:가버려
		PRINTFORMDW %CALLNAME:TARGET%는 다리를M글자에 열려, %CALLNAME:MASTER%의 음경을 질구에 대어 입맛을 다셨다
		PRINTFORMW 「 이제 사정해도 좋아요」
		PRINTFORMW 「삽입라고 구와 개 보면서, 말해버릴 없음 있고」
		PRINTFORMDW %CALLNAME:TARGET%는 천천히 허리를 내려 갔다
	ENDIF
	;=================================================
	;%CALLNAME:TARGET%が정욕 성교봉사동시절정
	;=================================================
	IF 구상記入チェック(100) && IS_CONDITION(TARGET, "정욕") && PALAM:쾌Ｖ > 8500
		PRINTFORMW 「쏟아！질내사정으로 %CALLNAME:TARGET%의 보지」
		PRINTFORMW 「살릴 수 있어！」
	ENDIF
	;=================================================
	;REACTとの調整が필요
	;=================================================
	IF 구상記入チェック(100)
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 음경을 최안쪽까지 질네 붐비면(자) 기쁜듯이 하복부를 어루만졌다
		PRINTFORMW 「 나의 질내에 너의 %RAND_SPLIT("너가/하지만/,")%들어가 있다」
	ENDIF

	;=================================================
	;体位変更역강간
	;=================================================
	IF 구상記入チェック(100)
		;PRINTFORMW 「さっきより硬いのは気の所為かしらね？んふふ」
		;PRINTFORML 「あんたって、%RAND_SPLIT("ほんと、/")%受けよね」
	ENDIF
		
	IF 구상記入チェック(100)
		;PRINTFORMW 「お腹の中で大きくなってきた。사정しそうなのね」
	ENDIF
	[SKIPEND]
ENDIF
