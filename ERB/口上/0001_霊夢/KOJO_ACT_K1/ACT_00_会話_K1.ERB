;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 회화계 액션구상파일
;
;	회화계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금 기분을 묻다/1=자신에게의 마음을 묻다/2=성에 관한 이야기를 하다/3=상냥하게 위로한다/4=엄하게 위협한다/5=의상 변경을 요구한다/6=합의를 요구한다/7=징계한다고 위협한다/8=고함치다/9=기분나쁘게 웃다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분나쁘게 웃다는이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;────────────────────────────────────
;ACT0, 지금 기분을 묻다
;────────────────────────────────────
;·지금의 기분에 대해 들어 온다
;·죠교대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침가"이상"라면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K1_0
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 지금 기분을 묻다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「역시, 저항이 있는 건가?」
		PRINTFORMDW %CALLNAME:TARGET%는 쓴 웃음 하면서 땀을 띄우고 있다
		RETURN 1
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:죠교자페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 실어 %CALLNAME:MASTER%의 질내에 가득
			;CASE 1
			;	PRINTFORMW 
			;후배위				뒤에서 %CALLNAME:MASTER%의 질내에 가득
			;CASE 2
			;	PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 끝까지 밀어넣어
			;CASE 3
			;	PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질을 애무하며
			;CASE 4
			;	PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에게 삽입하여 껴안고
			;CASE 5
			;	PRINTFORMW 
			;항문섹스		%CALLNAME:MASTER%의의 항문에 넣어
			CASE 6
				;후타나리와에 엉덩이 파져 응 어떤 기분? 저기 어떤 기분?
				IF TALENT:MASTER:남자
					PRINTFORMW 「수동적으로, 여자에게 당하는게 익숙한 너라면, 엉덩이로 임신할 수 있지 않을까」
					PRINTFORMW 「만약 진짜로 임신해버리면, 네가 신부? 후후♪」
					RETURN 1
				ENDIF
		ENDSELECT
		RETURN 0
	ENDIF
	
	;죠교대상의Ｃ에의애무를 하고 있는 상황. TEQUIP관리인 것으로 가능성은 비교적 있다
	;=================================================
	;죠교대상Ｃ사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2)
		;수음·애무계속중
		IF TEQUIP:죠교대상Ｃ사용 == 1
			PRINTFORMW 
		;펠라·쿤닐링구스 계속중
		ELSE
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 「여자애처럼 신음흘리면서 필사적으로 허리를 움직이는 너를 볼 수 있다니」
				PRINTFORMW 「너랑 하고있는 나는 %RAND_SPLIT("행운아구나/기뻐")%」
			;후배위시킨다				배후로부터질내에삽입 되고
			;CASE 2
			;	PRINTFORMW
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 「너는, 범해지는 쪽을 좋아해?」
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			;CASE 4
			;	PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			;CASE 5
			;	PRINTFORMW 
			;항문섹스시킨다		장액에 미끌거리는 항문으로 억압
			;CASE 6
			;	PRINTFORMW 
			CASEELSE
				RETURN 0
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;죠교자가페니스밴드 몸에 걸치고 있다
		;IF TEQUIP:죠교자페니스밴드
		;	PRINTFORMW 
		;그 외
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
		PRINTFORMW 「너는 나의 엉덩이에 깔리는 일을 어떻게 생각하고 있는 거야?」
		PRINTFORMW 「그렇게 나쁘진 않아? 너는, 가장은 어울리지 않는 것 같구나」
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가자위중
	;=================================================
	IF 구상기입체크(100) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 「여자 아이의 앞에서 그런 걸 하는게 좋아? 역시 좋은거야? 너는…」
		PRINTFORMW 「부끄러운 것, 정말 좋아하는구나?」
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가임신중
	;=================================================
	IF 구상기입체크(100) && TALENT:임신
		IF CFLAG:임신 >= 15
			PRINTFORMW 「이제…♪」
			PRINTFORMW 「그렇구나? 아빠가 된다는건 어떤 기분이야?」
		ELSE
			PRINTFORMW 「몇번이나 몇번이나, 나의 보지로 범해져,」
			PRINTFORMW 「임신시킨 기분은 어때?」
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「이런 일 해버리고 좀 그렇지만, 기분은 어때?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT1, 자신에게의 마음을 묻다
;────────────────────────────────────
;·자신의 일을 어떻게 생각할까 얘기한다
;·죠교대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침가"이상"라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K1_1
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 자신에게의 마음을 묻다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
;OriginalString : 아웃12, "머뭇머뭇")
		CALL FADEOUT(12, "머뭇머뭇")
;OriginalString : 아웃12, "너는…")
		CALL FADEOUT(12, "너는…")
		PRINTFORMW 「너는 그…」
;OriginalString : 아웃2, "그런？")
		CALL FADEOUT(2, "그런?")
		PRINTFORMW 「그…나…」
;OriginalString : 아웃2, "살짝")
		CALL FADEOUT(2, "살짝")
;OriginalString : 아웃, "머뭇머뭇")
		CALL FADEOUT(, "머뭇머뭇")
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:죠교자페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 실어 %CALLNAME:MASTER%의 질내에 가득
			CASE 1
				PRINTFORMW 
			;후배위				뒤에서 %CALLNAME:MASTER%의 질내에 가득
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 끝까지 밀어넣어
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질을 애무하며
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에게 삽입하여 껴안고
			CASE 5
				PRINTFORMW 
			;항문섹스		%CALLNAME:MASTER%의의 항문에 넣어
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;죠교대상의Ｃ에의애무를 하고 있는 상황. TEQUIP관리인 것으로 가능성은 비교적 있다
	;=================================================
	;죠교대상Ｃ사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2)
		;수음·애무계속중
		IF TEQUIP:죠교대상Ｃ사용 == 1
			PRINTFORMW 
		;펠라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			;CASE 1
			;	PRINTFORMW 「」
			;후배위시킨다				배후로부터질내에삽입 되고
			CASE 2
				PRINTFORMW 「뒤로 하는 건, 너에게 강간당하고 있는 것 같아 흥분되」
				PRINTFORMW 「…실제는 내가 덮쳐버렸지만」
				PRINTFORMW 「너는 나를, 덮치고 싶다고 생각한 적 있어?」
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 「말해봐」
				PRINTFORMW 「 나%RAND_SPLIT("랑 쭉 함께 있고 싶어?/를 얼마나 좋아해?")%」
				PRINTFORMW 「응?」
;OriginalString : 아웃4, "くすっ")
				CALL FADEOUT(4, "키득")
				PRINTFORMW 「바보같구나」
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			;CASE 4
			;	PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			;CASE 5
			;	PRINTFORMW 
			;항문섹스시킨다		장액에 미끌거리는 항문으로 억압
			;CASE 6
			;	PRINTFORMW 
			CASEELSE
				RETURN 0
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;죠교자가페니스밴드 몸에 걸치고 있다
		IF TEQUIP:죠교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가자위중
	;=================================================
	IF 구상기입체크(100) && GETBIT(TEQUIP:자위중, 0)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「너는 나를, 어떻게 생각하고 있는 거야?」
				PRINTFORMW 「딸칠 때의 반찬%RAND_SPLIT("? 그것만으로는 부족해/같은걸 말하면 때려눕일거야")%」
			CASE 1
				PRINTFORMW 「네가 자위하고 있을 때는」
				PRINTFORMW 「너의 상상 속에서, 나는 뭘 하고 있는 거야? 어떻게 되어 버리는 거야?」
			CASEELSE
				PRINTFORMDW %CALLNAME:TARGET%는 걸터앉아 다리를 꼬고 앉은 자세로 도발했다
		ENDSELECT
		RETURN 1
	ENDIF
	;=================================================
	;죠교자가임신중
	;=================================================
	IF 구상기입체크(100) && TALENT:임신
		PRINTFORMW 「너의 기분, 들려줘」
		PRINTFORMW 「너의 아기 %RAND_SPLIT("가져서/내려줘서/임신해서")%, 나는 기뻐」
		RETURN 1
	ENDIF
	
	;=================================================
	;상사상애
	;=================================================
	IF 구상기입체크(100) && AFFECTION("상사상애", TARGET)
		PRINTFORMW 「너의 감정을 말해봐. 솔직하게 말하는 게 좋아?」
		PRINTFORM 「
		;이것→지문 정말 좋아하다→리액션
		;수줍고구상와의 통합성을 위해서(때문에) comment out
		PRINTFORMW 「%CALLNAME:TARGET%, 사랑해」
		PRINTFORMW 「너도?」
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「나를, 어떻게 생각해?」
		PRINTFORMW 「늦됨인 너에게는 말하기 어려울지도 모르는데… 」
		PRINTFORMW 「%RAND_SPLIT("나도 여자니까… 알잖아, 그렇지?/듣고 싶어")%」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT2, 성에 관한 이야기를 함
;────────────────────────────────────
;·죠교자가 성에 대한지식를 말하기 시작한다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·죠교자에성지식가 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K1_2
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT2, 성에 관한 이야기를 함
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:죠교자페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 실어 %CALLNAME:MASTER%의 질내에 가득
			CASE 1
				PRINTFORMW 
			;후배위				뒤에서 %CALLNAME:MASTER%의 질내에 가득
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 끝까지 밀어넣어
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질을 애무하며
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에게 삽입하여 껴안고
			CASE 5
				PRINTFORMW 
			;항문섹스		%CALLNAME:MASTER%의의 항문에 넣어
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;죠교대상의Ｃ에의애무를 하고 있는 상황. TEQUIP관리인 것으로 가능성은 비교적 있다
	;=================================================
	;죠교대상Ｃ사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2)
		;수음·애무계속중
		IF TEQUIP:죠교대상Ｃ사용 == 1
			PRINTFORMW 
		;펠라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터질내에삽입 되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;항문섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;죠교자가페니스밴드 몸에 걸치고 있다
		IF TEQUIP:죠교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT3, 상냥하게 위로한다
;────────────────────────────────────
;·죠교자가 상냥한 말로 위로해 온다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K1_3
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 상냥하게 위로한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:죠교자페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 실어 %CALLNAME:MASTER%의 질내에 가득
			;CASE 1
			;	PRINTFORMW 
			;후배위				뒤에서 %CALLNAME:MASTER%의 질내에 가득
			;CASE 2
			;	PRINTFORMW 
			;;기승위				%CALLNAME:MASTER%의 암컷 구멍에 끝까지 밀어넣어
			;CASE 3
			;	PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질을 애무하며
			;CASE 4
			;	PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에게 삽입하여 껴안고
			;CASE 5
			;	PRINTFORMW 
			;항문섹스		%CALLNAME:MASTER%의의 항문에 넣어
			CASE 6
				IF TALENT:MASTER:남자
					PRINTFORMW 「네가 엉덩이의 구멍 후벼져서 가버리는, 어쩔 수 없는 사람이라도」
					PRINTFORMW 「내가 신부로 받아 줄게」
				ENDIF
			CASEELSE
				RETURN 0
		ENDSELECT
		RETURN 1
	ENDIF
	
	;죠교대상의Ｃ에의애무를 하고 있는 상황. TEQUIP관리인 것으로 가능성은 비교적 있다
	;=================================================
	;죠교대상Ｃ사용
	;=================================================
	IF 구상기입체크(100) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2)
		;수음·애무계속중
		IF TEQUIP:죠교대상Ｃ사용 == 1
				PRINTFORMW 「너무 긴장하지 말아. %RAND_SPLIT("내 손을 분명 좋아하게 될 꺼야/기분좋게 해 줄게")%」
		;펠라·쿤닐링구스 계속중
		ELSE
			PRINTFORM 「맛있어요
			CALL HEARTBW
			RETURN 0
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 「그래도 괜찮아」
				PRINTFORMW 「너의 것은, 나의 것에 맞으면 그것으로 좋아」
			;후배위시킨다				배후로부터질내에삽입 되고
			CASE 2
				PRINTFORMW 「조루라도 나쁘지 않아」
				PRINTFORMW 「하지만, 빠른건 좀 그런가」
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「조루라도 나쁘지 않아」
						PRINTFORMW 「빠를지 늦을지 모르지만, 어쨌든 전부 짜 줄게」
					CASE 1
						PRINTFORMW 「조루라도 나쁘지 않아」
						PRINTFORMW 「너에게 기분 좋은거 받을 수 있어, 나는 기뻐요」
					CASEELSE
						PRINTFORMW 「여자 아이에게 덮쳐져, %RAND_SPLIT("앙앙 우는/하앙하앙 울어대는/히응히응 우는")% 한심한 너도」
						PRINTFORMW 「나에게는 수요가 있으니까」
;OriginalString : 아웃12, "安心しなさい")
						CALL FADEOUT(12, "안심해")
				ENDSELECT
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 「그래도 괜찮아」
				PRINTFORMW 「%RAND_SPLIT("여자 한사람, 만족시킬 수 있으면, 좋은 자지야/너의 일이 느껴지면 그것으로 좋은거야")%」
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 「그래도 괜찮아」
				PRINTFORMW 「 나는 기분이 좋아요」
			;항문섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 「그래도 괜찮아」
				PRINTFORMW 「작은 일에 끙끙거려도, 어쩔 수 없어요」
		ENDSELECT
		RETURN 1
	ENDIF
	;=================================================
	;죠교자가자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;죠교자가페니스밴드 몸에 걸치고 있다
		IF TEQUIP:죠교자페니스밴드
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마항문) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가자위중
	;=================================================
	IF 구상기입체크(100) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 「포경이라도 좋지 않아」
		PRINTFORMW 「가죽에 질내사정하는 것은, 기분이 좋지요?」
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그래도 괜찮아」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT4, 엄하게 위협한다
;────────────────────────────────────
;·어려운 말로 위협한다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K1_4
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은변화 할 가능성이 있다
;기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 엄하게 위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:죠교자페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 실어 %CALLNAME:MASTER%의 질내에 가득
			CASE 1
				PRINTFORMW 
			;후배위				뒤에서 %CALLNAME:MASTER%의 질내에 가득
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 끝까지 밀어넣어
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질을 애무하며
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에게 삽입하여 껴안고
			CASE 5
				PRINTFORMW 
			;항문섹스		%CALLNAME:MASTER%의의 항문에 넣어
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;죠교대상의Ｃ에의애무를 하고 있는 상황. TEQUIP관리인 것으로 가능성은 비교적 있다
	;=================================================
	;죠교대상Ｃ사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2)
		;수음·애무계속중
		IF TEQUIP:죠교대상Ｃ사용 == 1
			PRINTFORMW 
		;펠라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터질내에삽입 되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;항문섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;죠교자가페니스밴드 몸에 걸치고 있다
		IF TEQUIP:죠교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「네가 퇴치되면 무엇이 될까? 후후」
		PRINTFORMW 「%RAND_SPLIT("[점아이템]/아이템/아이템/[1up]아이템")%일려나?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT5, 의상 변경을 요구한다
;────────────────────────────────────
;·죠교자가의상의 변경을 요구해 온다
;·죠교대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:밀어넘어지는중에는 발생하지 않는다
;·TEQUIP:밧줄가 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K1_5
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 옷을 벗어와 명령했다…"되고 있어 이것이변화 하는 일은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「우선은 옷을 벗을까」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상에페니스 있어
	;=================================================
	IF 구상기입체크(0) && PENIS(MASTER)
		;발기 하고 있다
		IF TCVAR:MASTER:발기도 >= 1000
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상에페니스없음
	;=================================================
	IF 구상기입체크(0) && !PENIS(MASTER)
		;젖고 있다
		IF PALAM:MASTER:윤활 > 500
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그대로는 하기 어렵네… 벗어」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT5_1, 옷을 입도록(듯이) 명령
;────────────────────────────────────
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT6, 합의를 요구한다
;────────────────────────────────────
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀or동정(양쪽 모두의 경우도 있다)
;·이미합의를 얻고 있는 경우는 발생하지 않는다
;·죠교대상가처녀에서도동정도 아닌 경우는 발생하지 않는다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K1_6
;-------------------------------------------------
;함수@TARGET_DESCRIPTION로 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은변화 한다
;후는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 합의를 요구한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상이남자의 경우
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:남자
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가후타나리의 경우
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:후타나리
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교대상가여자의 경우
	;=================================================
	IF 구상기입체크(0) && !PENIS(MASTER)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;성별 마다의 나누어 쓰고를 하지 않으면, 여기만 써 둡시다
	;남자, 후타나리, 여자분기를 모두 묻었을 경우는 불필요합니다. 삭제합시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT7, 징계한다고 위협한다
;────────────────────────────────────
;·지금부터징계·한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:징계·플래그는 서 있다
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·TEQUIP:밀어넘어지는중시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K1_7
;-------------------------------------------------
;여기에서는 지문의변화는 없다. 분기의 증감은 기호로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 징계한다고 위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「기분좋게 해」（이 철면피! ）
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("기분좋게 해")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「더 하드하게 해」（이 돼지째! ）
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("더 하드하게 해")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「용서를 빌다」（허락해 받을 수 없어했다）
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("용서를 빌다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「자위를 시작한다」（인의 이야기를 들어라! ）
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("자위를 시작한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT8, 고함치다
;────────────────────────────────────
;·큰 소리로고함치다
;·턴방침가"소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:징계·플래그에 관련되지 않고, 발생할 가능성이 있다（징계·안에 한층 더고함치다가능성등 ）
;-------------------------------------------------
@KOJO_ACT_K1_8
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 큰 소리로 고함쳤다…"만으로 파생은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 고함치다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("무뚝뚝하게 응답한다/강하게 응답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT9, 기분나쁘게 웃다
;────────────────────────────────────
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·금일의방침가"노닥노닥"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K1_9
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/죠교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은변화 한다
;기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 기분나쁘게 웃다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:죠교자페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 실어 %CALLNAME:MASTER%의 질내에 가득
			CASE 1
				PRINTFORMW 
			;후배위				뒤에서 %CALLNAME:MASTER%의 질내에 가득
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 끝까지 밀어넣어
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질을 애무하며
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에게 삽입하여 껴안고
			CASE 5
				PRINTFORMW 
			;항문섹스		%CALLNAME:MASTER%의의 항문에 넣어
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;죠교대상의Ｃ에의애무를 하고 있는 상황. TEQUIP관리인 것으로 가능성은 비교적 있다
	;=================================================
	;죠교대상Ｃ사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:죠교대상Ｃ사용, 1, 2)
		;수음·애무계속중
		IF TEQUIP:죠교대상Ｃ사용 == 1
			PRINTFORMW 
		;펠라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터질내에삽입 되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;항문섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;죠교자가페니스밴드 몸에 걸치고 있다
		IF TEQUIP:죠교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「-!%CALLNAME:TARGET%에 맡겨!」
		RETURN 1
	ENDIF
ENDIF
