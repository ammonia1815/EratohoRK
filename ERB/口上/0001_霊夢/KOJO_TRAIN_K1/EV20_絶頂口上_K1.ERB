;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 죠교이벤트구상ファイル
;
;	죠교ターン中이벤트、절정구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;☆暫定仕様@/L

;========================================================================
;EV20, 절정구상
;========================================================================
;・죠교자か죠교대상が절정に達するときに表示
;・各절정は個別の呼び出しを持たずに、全て派生で処理される
;-------------------------------------------------
;=================================================
;関連変数…
;
;TFLAG:전회질싸,(1=Ｖ성교/2=Ｖ성교봉사/3=Ｖ성교(드라이사정)/4=Ｖ성교봉사(드라이사정)/5=Ａ성교/6=Ａ성교봉사/7=Ａ성교(드라이사정)/8=Ａ성교봉사(드라이사정))
;TFLAG:REACT분류,(0=なし/1=소극적으로 한다/2=적극적으로 한다/3=용서를 빌다/4=날뛴다/5=要求に応じない,거부/6=逃げる,もがく)
;NOWEX:사정,(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A責사정)
;TEQUIP:자위중,(bit0=죠교대상/bit1=죠교자)
;
;=================================================
;関数類…
;
;MASTER_EX
;引　数:ARG:0…애무者→1.죠교대상自身/2.죠교자/4.조수１/8.조수２/16.조수３
;                      /64.도구/128.便所利用者　省略すると애무者を問わない
;              ビット和での指定が可能
;       ARG:1…(省略可)절정の種別→1.Ｃ절정/2.Ｖ절정/3.Ａ절정/4.Ｂ절정
;                                  /11.분유/12.사정/13.방뇨
;ARG:0 (bit0 = 죠교대상自身/bit1 = 죠교자/bit2 = 조수１/bit3 = 조수２/bit4 = 조수３/bit6 = 도구/bit7 = 所利)
;-------------------------------------------------
;TARGET_EX
;引　数:ARG:0…애무者→1.죠교대상/2.죠교자自身/4.조수１/8.조수２/16.조수３
;              ビット和での指定が可能。省略すると애무者を問わない
;       ARG:1…(省略可)절정の種別→1.Ｃ절정/2.Ｖ절정/3.Ａ절정/4.Ｂ절정
;                                  /11.분유/12.사정
;ARG:0 (bit0 = 죠교대상/bit1 = 죠교자自身/bit2 = 조수１/bit3 = 조수２/bit4 = 조수３)
;-------------------------------------------------
@KOJO_EVENT_K1_20(ARG)
;-------------------------------------------------
;TFLAG:전회질싸;(1=Ｖ성교/2=Ｖ성교봉사/3=Ｖ성교(드라이사정)/4=Ｖ성교봉사(드라이사정)/5=Ａ성교/6=Ａ성교봉사/7=Ａ성교(드라이사정)/8=Ａ성교봉사(드라이사정))
;-------------------------------------------------

;────────────────────────────────────
;쾌감을 참는다사정(MASTER→TARGET) 頑張ったけど駄目だったよ
;────────────────────────────────────
IF 구상記入チェック(100) && IS_NOWCOMNAME("쾌감을 참는다") ;COMの参照はIS_NOWCOMNAME推奨@/L
	;레이무さんの절정が遠い상태での질내사정
	IF 구상記入チェック(50) && AFFECTION("상사상애") && PALAM:쾌Ｖ < 8500 && GROUPMATCH(TFLAG:전회질싸, 2, 4) > 0
		;%UNICODE(hoge)%はエンコードがSJISであった時代の名残。ユニコード環境の現在は필요のない文法です@/L
		PRINTFORMW 「응？함께 가고 싶다？응후후%UNICODE(0x2661)%」
		PRINTFORMW 「기다리고 있어. 나도 곧 기분 좋아지기 때문에」
	;以下질내外両用 寧ろ見落としやすいのは男당신のＢ절정
	ELSEIF 구상記入チェック(50)
		PRINTFORMW 「안돼？안돼……그렇지만, 이제 타목 그렇구나？」
	ELSE
		PRINTFORMW 「필사적으로 참다니 %RAND_SPLIT("건방지구나/건방져")%」
		PRINTFORMDW %CALLNAME:MASTER%의 분발함에 응하는것 같이, %CALLNAME:TARGET%의 괴롭힘은 격렬함을 늘려 갔다…
	ENDIF
	RETURN 1
ENDIF
;────────────────────────────────────
;질내사정(MASTER→TARGET)
;────────────────────────────────────
;・Ｖ성교봉사系で질내사정する場合に表示
;・TFLAG:안빼고は抜かない判定ごとに+1される
;・사정量による分岐は필요に応じ追加して使うのが望ましい
;-------------------------------------------------

;사정演出はこの後
IF 구상記入チェック(100) && GROUPMATCH(TFLAG:전회질싸, 2, 4) > 0
	;================================================
	;드라이사정
	;================================================
	IF 구상記入チェック(100) && SHOOTING(MASTER, "드라이사정")
		PRINTFORMW 「 이제 나오지 않는 것일까」
		PRINTFORMW 「%RAND_SPLIT("너에게 쏟아 받을 수 없으면/너가사정해 주지 않으면")%, 어쩐지 어딘지 부족해요」
		PRINTFORML 「…어쩔 수 없네요」
		RETURN 1
	ENDIF
	;=================================================
	;임신中
	;=================================================
	IF 구상記入チェック(100) && CFLAG:TARGET:임신
		SELECTCASE !RAND:3
			CASE 0
				PRINTFORMW 「 이제 사정 그렇게？좋아요」
				;정액を子種に変更
				PRINTFORMW 「너의 자식은 나에게, ·인가·이다·해♪하기 위해 만들고 있어 것이군요？」
			CASE 1
				PRINTFORMW 「그대로 사정해도 좋아요」
				PRINTFORMW 「너후응응은 나의 보지로 뽑지 않으면 들어가지 않는 것」
			CASEELSE
				[SKIPSTART]
				;改善の余地有
				PRINTFORM 「질내에서%RAND_SPLIT("사정/뷰-뷰-/두근두근")%하고 있다…너의 아기, 이제 할 수 있어 버린다 것에…
				CALL HEARTBW
				PRINTFORMW 「뭐 좋아요…너의 자식은, 나의 질내에 사정때문에 만들고 있어 것이군요？」
				IF IS_CONDITION(TARGET, "정욕")
					;改善の余地有
					PRINTFORM 「의 보지로%RAND_SPLIT("전부,/, 보지로")%전부 내버릴 없음 있고
					CALL HEARTBW
				ENDIF
				[SKIPEND]
				IF IS_CONDITION(TARGET, "정욕")
					;改善の余地有
					PRINTFORM 「의 보지로%RAND_SPLIT("전부,/, 보지로")%전부 내버릴 없음 있고
					CALL HEARTBW
				ENDIF
				PRINTFORMW 「질내에서%RAND_SPLIT("사정/뷰-뷰-/두근두근")%하고 있다…」
				PRINTFORM 「따뜻하다
				CALL HEARTBW
		ENDSELECT
		RETURN 1
	ENDIF
	;=================================================
	;初めての질내사정
	;=================================================
	IF 구상記入チェック(100) && FIRSTEVENT(0)
		PRINTFORMW 「소중한 일을 말하지 않았어요」
		PRINTFORM 「 나 이제, 팥찰밥 오고 있으니
		;아라이멘도正
		IF ALI(0)
			CALL HEARTBW
		;아라이멘도負
		ELSE
			PRINTFORML 
		ENDIF
		PRINTFORML 
		;短め 질내사정に反応してのんっ
;OriginalString : 인4, "「んっ！」")
		CALL FADEIN(4, "「읏！」")
		;長め 사정に만족し余韻をかみしめるんっ
;OriginalString : 아웃12, "「んっ…」")
		CALL FADEOUT(12, "「읏…」")
		PRINTFORM 「(안)중으로 나오고 있다
		CALL HEARTBW
		PRINTFORMDL %CALLNAME:TARGET%는 애정과 정욕이 섞인 미소를 띄웠다
		RETURN 1
	ENDIF
	;=================================================
	;임신했다？
	;=================================================
	IF 구상記入チェック(100) && CFLAG:TARGET:질정 > 10000 && !CFLAG:TARGET:임신 && FIRSTEVENT(2)
		IF IS_CONDITION(TARGET, "정욕")
			;改善の余地有
			PRINTFORM 「할 수 있어버린다…할 수 있어버린다…
			CALL HEARTBW
			PRINTFORMDW %CALLNAME:TARGET%의 자궁구는 잉 보고 국물을 빨아내려고 음경을 질네 들러 붙고 있다
		ENDIF
		PRINTFORM 「아―…이것, 피탄 끊어버리는 원
		CALL HEARTBW
		RETURN 1
	ENDIF
	;=================================================
	;%CALLNAME:TARGET%정욕
	;================================================
	IF 구상記入チェック(100) && IS_CONDITION(TARGET, "정욕")
		PRINTFORMW 「%RAND_SPLIT("막혀 탄막으로/막혀 탄막！막혀 탄막/에 해 시켜/임신시켜")%！%RAND_SPLIT("れーむのらんし被弾させてぇっ/れーむのこと孕ませてぇ/あんたの赤ちゃん産ませてぇっ")%！！」
		PRINTFORM 「아…아…배 속에서…%RAND_SPLIT("움찔움찔 말하고 있다/자지 날뛰고 있다")%…
		CALL HEARTBW
		RETURN 1
	ENDIF
	;================================================
	;조루
	;================================================
	IF 구상記入チェック(100) && SHOOTING(MASTER, "조루사정")
		PRINTFORMW 「아…배 중(안)에서, 는 하고 있다」
		PRINTFORMW 「무허가로 질내 사정해 주어 버려서」
		PRINTFORM 「심하기 때문에
		CALL HEARTBW
		RETURN 1
	ENDIF
	;================================================
	;%CALLNAME:MASTER%정욕
	;================================================
	IF 구상記入チェック(100) && IS_CONDITION(MASTER, "정욕")
		IF !RAND:2
			;んふふの前に사정を感知する一呼吸の追加。질내사정시킨다つもりで致してる為、驚きの！はない
;OriginalString : 인4, "「んっ」")
			CALL FADEIN(4, "「읏」")
			PRINTFORM 「응후후♪보지에는 이길 수 없었어요
			CALL HEARTBW
		ELSE
			PRINTFORMW 「아…는 하고 있다…」
			PRINTFORMW 「…응후후♪」
		ENDIF
		RETURN 1
	ENDIF
	;================================================
	;대면좌위 命中判定のある질내사정
	;================================================
	IF TFLAG:ACT == 95
		PRINTFORML 「%RAND_SPLIT("응. 이것, 좋아/응. 이것, 마음 좋아요/행복/너무 좋아")%」
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%에 깊게 안아 도착하면 뺨 켜 해 왔다
		RETURN 1
	ENDIF
	;================================================
	;통상
	;================================================
	IF 구상記入チェック(100)
		IF ALI(0)
			PRINTFORMDL %CALLNAME:MASTER%(은)는 추삽을 멈춤허리를 꽉 누르면(자) 절정을 헤아렸다%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 손을 잡아 왔다
			PRINTFORML
			;お味噌汁は欠かせない
			SELECTCASE RAND:2
				CASE 0
					PRINTFORM 「%RAND_SPLIT("매일 아침 된장국 만들어 주기 때문에/받아 들여 주기 때문에/책임은 이미 취해 있기 때문에/사양은 필요하지 않으니까")%,
					PRINTFORM %RAND_SPLIT(@"%CALLNAME:TARGET%의 보지/가장 안쪽/자궁/질내/너의@%CALLNAME:TARGET%")%에
					PRINTFORM %RAND_SPLIT("정액/자식국물/임신하게 한 국물/성욕 전부")%
					PRINTFORM ,%RAND_SPLIT("/전부/한 방울 남김없이")%사정해버릴 없음 있고
					CALL HEARTBW
					;体位関連で分割するか悩み所
					;これならこうなる。という物はバリアント的によろしくないらしいので分割しない方が良さそう
				CASEELSE
					;こづくりなまえっちとは言わせない。
					;こづくりではないえっちという概念が無い。
					;레이무さんの辞書に避妊という発想は無い。
					PRINTFORM 「이봐요,%RAND_SPLIT("개만들기 엣치해/착정 되어/무리하게정액짜 나와/자지 좁혀져/자지 진동시켜")%
					PRINTFORMW %RAND_SPLIT("잇/방편/질내사정해")%좋은 차이」
			ENDSELECT
			;대량사정時追加描写
			IF 구상記入チェック(100) && SHOOTING(MASTER, "대량사정")
;OriginalString : 인4, "どくっ！")
				CALL FADEIN(4, "쿨렁！")
				;레이무の胎内での감각と입頭での表現をずらす。레이무さんは作家ではないので的確でない方が良い。
;OriginalString : 아웃8, "どぷ……どぷ……")
				CALL FADEOUT(8, "…………")
				PRINTFORMW 「아…배 중(안)에서, 구！구！라고…」
;OriginalString : 아웃4, "ぴゅる……ぴゅ……")
				CALL FADEOUT(4, "퓨루……퓨……")
				PRINTFORM 「%RAND_SPLIT("가능하게 된다…가능하게 된다…/언제까지사정하고 있는거야/자지, 떨리고 있다/가득")%
				CALL HEARTBW
			ENDIF
		ELSE
			PRINTFORML 「싫어해도, 너의 자지는 여자의 딸(아가씨)를 잉세대같구나」
			PRINTFORML 「%RAND_SPLIT("/나, 이제(벌써) 팥찰밥 와 있는 것이지만, 기분 좋았을까/억지로 배게 해 기분 좋았을까")%
			CALL HEARTBW
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;항문内사정(MASTER→TARGET)
;────────────────────────────────────
;・Ａ성교봉사系で장내사정する場合に表示
;・TFLAG:106,안빼고は抜かない判定ごとに+1される
;・사정量による分岐は필요に応じ追加して使うのが望ましい
;-------------------------------------------------
IF 구상記入チェック(0) && GROUPMATCH(TFLAG:전회질싸, 6, 8) > 0
ENDIF

;────────────────────────────────────
;질내사정(TARGET→MASTER)
;────────────────────────────────────
;・Ｖ성교系で질내사정する場合に表示
;・사정量による分岐は필요に応じ追加して使うのが望ましい
;・現在の仕様では죠교자が드라이사정することは非常に稀(ほぼ無い？)
;-------------------------------------------------

	IF TALENT:새드
		
	ELSE
		
	ENDIF
	
IF 구상記入チェック(100) && GROUPMATCH(TFLAG:전회질싸, 1, 3) > 0
	;%CALLNAME:TARGET%に産ませたことがある
	IF TALENT:모성
		PRINTFORM 「너에게도 낳아 세등 없으면 불공평해요
		CALL HEARTBW
		;リターン無しで次につなげる
		;RETURN 1
	ENDIF
	SELECTCASE RAND:4
		CASE 0
			PRINTFORM 「사정원이야…%RAND_SPLIT("임신하세요/임신햇/나의 아기잉 보세요/자궁으로 받아 들여")%
			CALL HEARTBW
		CASE 1
			PRINTFORMDW 사정을 끝낼 때까지%CALLNAME:TARGET%새와 들러붙어 놓아 주지 않았다
		CASE 2
			;レイプはすれども性欲のみに非ず
			PRINTFORMDW 성욕을 채워 상, %CALLNAME:TARGET%는 %CALLNAME:MASTER%(을)를 해방하지 않는다
		CASEELSE
			PRINTFORMW 「…무심코 질내에 사정해버렸지만…책임은 이제 취하고 있기 때문에, 좋아요？」
	ENDSELECT
	;당신が鬱
	;何できちゃうできちゃう言ってるのよもうできてるわよ 당신側はできない
	
	;당신が幼児退行ならぬ老人化
	;そとだしはまだかのう もうできてるでしょ 당신側はできない
	
	;おまんこは締め付けてくるわよ
	;CALL HEARTBW
	RETURN 1
ENDIF

;────────────────────────────────────
;항문内사정(TARGET→MASTER)
;────────────────────────────────────
;・Ａ성교系で장내사정する場合に表示
;・사정量による分岐は필요に応じ追加して使うのが望ましい
;・現在の仕様では죠교자が드라이사정することは非常に稀(ほぼ無い？)
;-------------------------------------------------
IF 구상記入チェック(0) && GROUPMATCH(TFLAG:전회질싸, 5, 7) > 0
	IF TALENT:새드
		
	ELSE
		
	ENDIF
ENDIF

[SKIPSTART]
部位別の절정内で処理しない場合はここで補完@/L
;────────────────────────────────────
;동시절정
;────────────────────────────────────
;・죠교자と죠교대상がともに절정する場合に表示
;-------------------------------------------------
IF 구상記入チェック(0) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정)
ENDIF
[SKIPEND]

;────────────────────────────────────
;죠교대상사정
;────────────────────────────────────
;・죠교대상が사정する場合に表示
;・사정量による分岐は필요に応じ加減して使うのが望ましい
;-------------------------------------------------
;リアクション→これ→사정処理　トドメの一言に適する箇所
IF 구상記入チェック(100) && NOWEX:MASTER:사정
	;────────────────────────────────────
	;처음
	;────────────────────────────────────
	IF 구상記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「벌벌 흔들림, 라고 흰 것이 나오고 있다……자지는 기분 좋아지면(자), 이러한 것……」
		PRINTFORMW 「장미 따돌려 총알은 귀찮지만…맞으러 가면 좋기 때문에, 무려에서도 되네요」
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;こづくりのひ
	;────────────────────────────────────
	IF 구상記入チェック(100) && POLICY("금일") == "애만들기"
		;행동前向きかもしれない
		PRINTFORMW 「응후후. 질외에 내 버렸다 원이군요？」
		SELECTCASE RAND:3
			CASE 0
				;テンポの調整8から2へ。당신の涙に即反応する레이무さん。
;OriginalString : 인2, "「って、泣かないでよ！」")
				CALL FADEIN(2, "「읏, 울지 말아！」")
				PRINTFORMW 「%RAND_SPLIT("아 이제(벌써)…나빴어요/너무 했어요")%…」
				;テンポの調整12ならば二文字程度が適当。空白行が残るので二連FADEOUTは止めておいた方がいい。
				;概ね一文字につき2程度？
;OriginalString : 인8, "「ごめんね」")
				CALL FADEIN(8, "「미안해요」")
;OriginalString : 아웃12, "「よしよし」")
				CALL FADEOUT(12, "「좋아 좋아」")
			CASE 1
				PRINTFORMW 「…그런, 죽을 곳을 잃은 것 같은 얼굴 하지 않고」
;OriginalString : 아웃12, "「大げさなんだから」")
				CALL FADEOUT(12, "「과장되니까」")
			CASEELSE
				PRINTFORMW 「우목이 되어있는 너도 꽤, 사랑스러워요」
		ENDSELECT
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;구강사정
	;────────────────────────────────────
	IF 구상記入チェック(100) && GET_ACTNAME(GET_NORMALACTNUM(TFLAG:ACT)) == "펠라する"
		;────────────────────────────────────
		;구강사정 初回
		;────────────────────────────────────
		IF 구상記入チェック(100) && FIRSTEVENT()
			IF 구상記入チェック(100) && SHOOTING(MASTER, "대량사정")
				;레이무は寺子屋行ってない系女子の為コメントアウト
				;PRINTFORML （사정は小匙一杯程度の量だって케이네が言ってたけれど…随分多いわね）
				PRINTFORML （사정은 작은 숟가락 한 잔 정도의 양이라도 (듣)묻고 있었지만…상당히 많네요）
				PRINTFORML （빠질 것 같다）
				PRINTFORMDW %CALLNAME:TARGET%는 눈감으면서 차분히 입안의 정액을 저작 해 맛보고 있다
			ELSEIF 구상記入チェック(100) && SHOOTING(MASTER, "조루사정")
				PRINTFORML 「！」
				PRINTFORMDW %CALLNAME:TARGET%는 갑자기 기관에 흘러드는 정액에 눈을 좌우 양면 놀라고 있다
			ELSE
				PRINTFORMDW %CALLNAME:TARGET%는 눈감으면서 차분히 입안의 정액을 저작 해 맛보고 있다
				PRINTFORML （우선……）
			ENDIF
			RETURN 1
		ENDIF
		;────────────────────────────────────
		;구강사정 初回以外
		;────────────────────────────────────
		IF 구상記入チェック(100)
			;────────────────────────────────────
			;쾌감을 참는다사정(MASTER→TARGET) 頑張ったけど駄目だったよ
			;────────────────────────────────────
			;참다を決壊시킨다試み
			IF IS_COMGRONAME("소극적으로 한다/용서를 빌다/쾌감을 참는다")
				PRINTFORMDL %CALLNAME:TARGET%는 절정을 참으려고 잡는다%CALLNAME:MASTER%의 손을 풀어 손가락을 걸어 왔다
				PRINTFORMDW %CALLNAME:TARGET%의 부드럽고 난보람 손바닥에 %CALLNAME:MASTER%의 긴장이 느슨해진다
				PRINTFORML 
			;ENDIF
			;追加描写 대량사정시킨다気満々で손を握ってくる레이무さん
			;IF SHOOTING(MASTER, "대량사정")
			ELSEIF SHOOTING(MASTER, "대량사정")
				PRINTFORMDW %CALLNAME:MASTER%(은)는 달할 것 같게 되는이나%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 손을 잡아 꽉 잡아 왔다
				PRINTFORML 
			ENDIF
			;정액補充完了時の一番搾り
			IF BASE:MASTER:사정 == MAXBASE:MASTER:사정
				PRINTFORML 「응”！응구！응…응…응구…」
			ENDIF
			
			PRINTFORMW （입 중(안)에서 날뛰고 있다…）
			PRINTFORMl （맛있지는 않지만, 진한 개가 기분 좋아지고 있다고 생각하면…）
			IF !SHOOTING(MASTER, "드라이사정")
;OriginalString : 아웃12, "　んっ…んっ…こくん…")
				CALL FADEOUT(12, " 읏…읏…꿀꺽…")
				PRINTFORMDW %CALLNAME:TARGET%는 \@ SHOOTING(MASTER, "대량사정") ? 입원을 손가락으로 닦으면서 #  \@기쁜듯이 정액을 삼켰다
			ENDIF
			PRINTFORMW 「또, 해 주고 싶어져…」
			RETURN 1
		ENDIF
	ENDIF
	;────────────────────────────────────
	;자위사정
	;────────────────────────────────────
	IF 구상記入チェック(100) && GET_ACTNAME(GET_NORMALACTNUM(TFLAG:ACT)) == "자위"
		IF 구상記入チェック(100)
			PRINTFORM 「사정시는
			PRINTFORMD %CALLNAME:TARGET%씨를 좋아하는 사람―
			PRINTFORML 라고 말하지 않으면」
			PRINTFORMW 「이봐요」
			PRINTFORM 「
			PRINTFORMD %CALLNAME:TARGET%씨를 좋아하는 사람
			PRINTFORML 」
			PRINTFORM 「
			PRINTFORMD 좋아
			PRINTFORML 」
			PRINTFORM 「
			PRINTFORMD 좋아
			PRINTFORMW 」
			;ここまでからかうような%CALLNAME:MASTER%의 声真似の白。言い聞かせるように刷り込む様に。소프트죠교
			;ここから%CALLNAME:TARGET%의 素の声色の桃。途中からノってきた%CALLNAME:TARGET%さん。自爆セルフ言葉責め
			;PRINTFORMW 「好き」
			;噛みしめるように再認識する。お嫁さんは惚れ直시킨다もの。
;OriginalString : 아웃12, "「好き」")
			CALL FADEOUT(12, "「좋아」")
			PRINTFORMW 「좋아」
			PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 절정이 들어갈 때까지 계속 상냥하게 속삭였다
		ENDIF
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;사정内容分岐
	;────────────────────────────────────
	IF 구상記入チェック(100) && SHOOTING(MASTER, "전립선사정")
		PRINTFORMW 「파져서 나오다니 우물같구나？」
;OriginalString : 아웃4, "「くすっ」")
		CALL FADEOUT(4, "「키득」")
		PRINTFORMW 「 아직도 안나오는 건가… 좀 더 깊게 파 줄게」
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;드라이사정分岐
	;────────────────────────────────────
	IF 구상記入チェック(100) && SHOOTING(MASTER, "드라이사정")
		PRINTFORMW 「네가 사정해 주지 않으면 어쩐지 어딘지 부족해요」
		PRINTFORMW 「…%RAND_SPLIT("/이제(벌써)사정없다？")%어쩔 수 없네요」
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;대량사정分岐
	;────────────────────────────────────
	IF 구상記入チェック(100) && SHOOTING(MASTER, "대량사정")
		;状況によって矛盾する恐れ。ハートが多すぎる。不機嫌やジト目ニヤニヤ顔等、他の仕草も欲しくなった為調整
		PRINTFORM 「이렇게 사정해 주어 버려서…%RAND_SPLIT("/너무 많아요/너무 내고")%
		IF IS_CONDITION(TARGET, "정욕")
			CALL HEARTBW
		ELSE
			PRINTFORMW 」
		ENDIF
		SIF RAND:3
			PRINTFORMW 「%RAND_SPLIT("언제되었다들 멈추는거야…이것/가득 나와 있지만")%, 망가지지 않아요？」
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;조루사정分岐
	;────────────────────────────────────
	IF 구상記入チェック(100) && SHOOTING(MASTER, "조루")
		PRINTFORMW 「아%RAND_SPLIT("/읏/읏앗")%！이제 %RAND_SPLIT("사정しちゃった/ちょっと楽しみたかったのに")%…」
		PRINTFORMW 「%RAND_SPLIT("는 양도 적네요/너의조루만은 짐작 할 수 있을 것 같지 않아요…/좀 더참다해 주세요이, 바보 자지/아직 낼 수 있네요？/안된 자지군요")%」
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;통상사정分岐
	;────────────────────────────────────
	IF 구상記入チェック(100) && SHOOTING(MASTER, "사정")
		;처음사정観察
		SELECTCASE RAND:3
			CASE 0
				PRINTFORM 「사정할 때는,
				PRINTFORMD %CALLNAME:TARGET%씨 너무 좋아요
				PRINTFORMW 라고 말하지 않으면 안되요？」
			CASE 1
				PRINTFORMW 「기분 좋았어？」
				PRINTFORMDW %CALLNAME:TARGET%는 %RAND_SPLIT("실룩실룩 웃으/미소지으/입꼬리를 올리")%면서 %CALLNAME:MASTER%의 눈을 응시하고 있다…
			CASEELSE
				;描写不발改善の余地有り　정액描写が伝わらないと思われる
				PRINTFORM 「가버린 후에 자지가,
				PRINTFORMD 「열심히 사정했어」
				PRINTFORMW 라고 말하고 있는 것 같아, 어쩐지 사랑스럽네요」
				PRINTFORM 「응후후♪ 잘 했어요♪
				CALL HEARTBW
			ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;죠교자사정
;────────────────────────────────────
;・죠교자が사정する場合に表示
;・사정量による分岐は필요に応じ追加して使うのが望ましい
;・現在の仕様では죠교자が드라이사정することは非常に稀(ほぼ無い？)
;-------------------------------------------------
IF 구상記入チェック(0) && NOWEX:사정
	IF TALENT:새드
		
	ELSE
		
	ENDIF
ENDIF

[SKIPSTART]
;────────────────────────────────────
;죠교자절정
;────────────────────────────────────
;・죠교자が절정する場合に表示
;-------------------------------------------------
IF 구상記入チェック(100) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정)
	;성교봉사절정分岐が機能しない為移転コメントアウト
	PRINTFORMW 「응！응%RAND_SPLIT("읏/읏/우웃")%」
	PRINTFORM 「하…하…후~…
	CALL HEARTBW
	SIF !TEQUIP:눈가리개
		PRINTFORMW 「…%RAND_SPLIT("별로/그렇게")%보지 말아…부끄럽기 때문에…」
	RETURN 1
ENDIF
[SKIPEND]

[SKIPSTART]
;部位別で절정を書き分けない場合はここで補完@/L
;────────────────────────────────────
;죠교대상절정
;────────────────────────────────────
;・죠교대상が절정する場合に表示
;-------------------------------------------------
IF 구상記入チェック(0) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정)
ENDIF
[SKIPEND]
;────────────────────────────────────
;죠교대상Ｃ절정
;────────────────────────────────────
;・죠교대상がＣ절정する場合に表示
;・ここでは「여자のＣ절정」を想定している
;-------------------------------------------------
IF 구상記入チェック(0) && NOWEX:MASTER:Ｃ절정
ENDIF

;────────────────────────────────────
;죠교대상Ｖ절정
;────────────────────────────────────
;・죠교대상がＶ절정する場合に表示
;-------------------------------------------------
IF 구상記入チェック(0) && NOWEX:MASTER:Ｖ절정
ENDIF

;────────────────────────────────────
;죠교대상Ａ절정
;────────────────────────────────────
;・죠교대상がＡ절정する場合に表示
;-------------------------------------------------
IF 구상記入チェック(0) && NOWEX:MASTER:Ａ절정
ENDIF

;────────────────────────────────────
;죠교대상Ｂ절정
;────────────────────────────────────
;・죠교대상がＢ절정する場合に表示
;-------------------------------------------------
IF 구상記入チェック(0) && NOWEX:MASTER:Ｂ절정
ENDIF

;────────────────────────────────────
;죠교자Ｃ절정
;────────────────────────────────────
;・죠교자がＣ절정する場合に表示
;・ここでは「여자のＣ절정」を想定している
;・사정は사정구상の分岐で書くのが望ましい
;-------------------------------------------------
IF 구상記入チェック(0) && NOWEX:Ｃ절정
ENDIF

;────────────────────────────────────
;죠교자Ｖ절정
;────────────────────────────────────
;・죠교자がＶ절정する場合に表示
;-------------------------------------------------
IF 구상記入チェック(100) && NOWEX:Ｖ절정
	;────────────────────────────────────
	;죠교자성교봉사Ｖ절정
	;────────────────────────────────────
	IF 구상記入チェック(100) && TEQUIP:성교봉사중
		;죠교자이성派生
		SELECTCASE TENSION(TARGET, 5)
			;25%以下
			CASE "低い"
				PRINTFORM 「아"-…아"-…
				CALL HEARTBW
				;절정で頭真っ白상태
				PRINTFORMW 「………」
				;言語を손放し中　描写が필요と思われる
				;PRINTFORMDL 
			;25%以上
			CASE "一般"
				SELECTCASE RAND:2
					CASE 0
						;
;OriginalString : 아웃8, "「あっ！あ！」")
						CALL FADEOUT(8, "「앗！아！」")
;OriginalString : 인8, "「あ……」")
						CALL FADEIN(8, "「아……」")
						[SKIPSTART]
						;言葉遊びをするにはもう少し深みが欲しい
						PRINTFORMDL 목을 제어 다 할 수 있지 않은 흥분본 교성은 사랑스럽고
						PRINTFORMDW 케모노와 사람의 경계선상의 %CALLNAME:TARGET%도 또한꼬드김정적으로 %CALLNAME:MASTER%(을)를 매료했다
						;言葉責め向きの表現かもしれない
						;PRINTFORMDW %CALLNAME:MASTER%は聴覚からも%CALLNAME:TARGET%に責められている
						[SKIPEND]
					CASEELSE
						;あっけらかんと했다%CALLNAME:TARGET%らしさが不발気味
;OriginalString : 아웃8, "「ふーっ！…ふーっ…")
						CALL FADEOUT(8, "「훗！…훗…")
						;PRINTFORMDW 입元を押さえ嬌声を抑える仕草は彼女の少女性を引き立たせた
				ENDSELECT
			;50%以上
			CASE "高い"
;OriginalString : 인8, "「はーっ…はーっ…」")
				CALL FADEIN(8, "「핫…핫…」")
		ENDSELECT
		;長めの１２。절정で頭真っ白상태から言葉が紡げるまで程度の間隔を空ける
;OriginalString : 인12, "………")
		CALL FADEIN(12, "………")
		;아라이멘도正
		IF ALI(0)
			IF TALENT:새드
				;봉사시킨다コマンドが出な過ぎる레이무さん
				PRINTFORMW 「기분이 좋은데…네가 말해 준 (분)편이, 즐거워요」
			ELSE
				PRINTFORMW 「 나만 기분 좋아지는 것도…어쩐지 분해요」
;OriginalString : 아웃12, "もにょもにょ…")
				CALL FADEOUT(12, "…")
				;삽입継続が参照されていない為コメントアウト
				;PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%が사정していない事を確認すると再び腰を動かし始めた
			ENDIF
		;아라이멘도負
		ELSE
			PRINTFORMW 「무엇 우쭐거린 얼굴 해」
			PRINTFORMDW %CALLNAME:TARGET%는 사랑스럽게 뺨을 부풀린다
		ENDIF
		RETURN 1
	ENDIF
		[SKIPSTART]
			;しっくり来ないコメントアウト
			PRINTFORMW 「너후응응으로, 말해 버렸다」
;OriginalString : 아웃8, "「いかされちゃった」")
			CALL FADEOUT(8, "「활용되어 버렸다」")
			;ここで레이무様大만족につながるときっと플레이ヤーさんが嬉しい
			;만족ゲージに任せ音符やハートは無い方が良い

			;なかだし言及が多すぎる為剪定。
;OriginalString : 인12, "………")
			CALL FADEIN(12, "………")
;OriginalString : 아웃4, "ちら")
			CALL FADEOUT(4, "등")
			;PRINTFORMW 「これで질내に사정してくれたら言うことは無いのだけれど…」
		[SKIPEND]
	
	;────────────────────────────────────
	;죠교자Ｖ절정 それ以外
	;────────────────────────────────────
	[SKIPSTART]
	;ペニバン自爆を想定 いまひとつ
	IF 구상記入チェック(0) && NOWEX:Ｖ절정
		IF IS_CONDITION(TARGET, "정욕")
			PRINTFORMW 「보지 기분 좋다…보지 기분 좋다…」
		ELSE
			PRINTFORMW 「…네가 기분 좋아지지 않는 것은, 어쩐지 어딘지 부족해요」
		ENDIF
	ENDIF
	RETURN 1
	[SKIPEND]
ENDIF
;────────────────────────────────────
;죠교자절정 それ以外
;────────────────────────────────────
;・죠교자が절정する場合に表示
;-------------------------------------------------
IF 구상記入チェック(100) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정)
	;성교봉사절정分岐が機能しない為移転コメントアウト
	PRINTFORMW 「응！응%RAND_SPLIT("읏/읏/우웃")%」
	PRINTFORM 「하…하…후~…
	CALL HEARTBW
	SIF !TEQUIP:눈가리개
		PRINTFORMW 「…%RAND_SPLIT("별로/그렇게")%보지 말아…부끄럽기 때문에…」
	RETURN 1
ENDIF
;────────────────────────────────────
;죠교자Ａ절정
;────────────────────────────────────
;・죠교자がＡ절정する場合に表示
;-------------------------------------------------
IF 구상記入チェック(0) && NOWEX:Ａ절정
ENDIF

;────────────────────────────────────
;죠교자Ｂ절정
;────────────────────────────────────
;・죠교자がＢ절정する場合に表示
;-------------------------------------------------
IF 구상記入チェック(0) && NOWEX:Ｂ절정
ENDIF

;────────────────────────────────────
;죠교대상시오후키
;────────────────────────────────────
;・죠교대상が시오후키の場合に表示
;・現状、ACT손가락삽입한다時にＧスポット責めに派生했다場合にのみ起きる
;・주인님の시오후키は無い
;-------------------------------------------------
IF 구상記入チェック(0) && NOWEX:MASTER:시오후키
ENDIF

;────────────────────────────────────
;죠교대상실금
;────────────────────────────────────
;・죠교대상が실금する場合に表示
;・죠교대상に素質:오줌싸개が無ければ発生しない
;・Ｃ절정なしで실금だけというのは稀にあるケース
;-------------------------------------------------
IF 구상記入チェック(0) && NOWEX:MASTER:방뇨
ENDIF
