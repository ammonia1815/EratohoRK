;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무구상 ショップ이벤트구상ファイル
;
;	ショップから呼び出される구상をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;//暫定版@/L(2015/01/15)

;========================================================================
;ショップ이벤트구상
;========================================================================

;────────────────────────────────────
;EV101, 의상変更
;────────────────────────────────────
;・ショップから의상変更を行った場合に表示
;-------------------------------------------------
@KOJO_EVENT_K1_101(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=
;　一の位…（0, もう着てる、1, 着替えた、2, 恥ずかしくて着られない、3, 嫌がって着ない、9, 持ってない）
;　十の位…（0, 통상、10, 반라、20, 전라、30, 속옷無し）
;　百の位…코스프레のために保留
;　千の位…（0, 죠교자, 1000, 죠교대상）
;　例（죠교대상が恥ずかしがって전라にできなかった＝1022）
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자が着替えた
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG < 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;통상되었다
		CASE 1
			PRINTFORML 「이것이 제일 나다울까？」
			PRINTFORML
			RETURN 1
		;반라되었다
		CASE 11
			;初回
			PRINTFORML 「 좀 더, 어른 스러운 속옷이 좋아해？」
			PRINTFORML
			;PRINTFORMW 「下脱ぎが良い？あんたも遠慮が無くなってきたわね」
			;PRINTFORMW 「いい경향だと思うけど、駄目。속옷が無いって、恥ずかしいんだから」
			RETURN 1
		;전라되었다
		CASE 21
			PRINTFORML 「이 (분)편이 이야기가 빠르네요」
			PRINTFORMDL %CALLNAME:TARGET%(은)는 자연스럽게 양팔로 가슴과 비소를 숨기고 있다
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자が恥ずかしがって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG < 1000 && (ARG % 10) == 2
	SELECTCASE ARG
		;반라だって恥ずかしい
		CASE 12
			PRINTFORML （속옷, 신조 하는 것이 좋을까…）
			PRINTFORML
			RETURN 1
		;普段から전라はちょっと……
		CASE 22
			PRINTFORML 「아 이제, 개가 돕고 는…」
;OriginalString : 아웃12, "もにょもにょ…")
			CALL FADEOUT(12, "…")
			PRINTFORMDL %CALLNAME:MASTER%의 시선을 신경써인가,%CALLNAME:TARGET%(은)는 갈아입음을 멈추어 버렸다
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자が嫌がって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG < 1000 && (ARG % 10) == 3
	SELECTCASE ARG
		;반라は嫌
		CASE 13
			PRINTFORML 
			PRINTFORML 
			RETURN 1
		;普段から전라なんて嫌
		CASE 23
			PRINTFORML 
			PRINTFORML 
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교자が着替えるのをやめた
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%의 의상はすでに[　　]に設定されています
;------------------------------------------------
IF 구상記入チェック(100) && ARG < 1000 && (ARG % 10) == 0
	SELECTCASE ARG
		;통상のまま
		CASE 0
			;少女推敲中
			PRINTFORML 「아 이제, 개가 돕고 는…」
;OriginalString : 인8, "もにょもにょ……")
			CALL FADEIN(8, "……")
			PRINTFORMDL %CALLNAME:MASTER%의 시선을 신경써인가,%CALLNAME:TARGET%(은)는 갈아입음을 멈추어 버렸다
			PRINTFORML
			RETURN 1
		;반라のまま
		CASE 10
			PRINTFORML 「 이제 입어도 좋을까？」
			PRINTFORML
			RETURN 1
		;전라のまま
		CASE 20
			PRINTFORML 「춥기 때문에, 빨리 따뜻하게 하세요」
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상を着替え시켰다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG >= 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;통상되었다
		CASE 1001
			PRINTFORML 「이것이 제일 너답네요」
			;착의플레이선호の場合
			IF TALENT:착의플레이선호
;OriginalString : 인12, "「その、」")
				CALL FADEIN(12, "「그,」")
;OriginalString : 아웃12, "「似合ってるわよ」")
				CALL FADEOUT(12, "「어울리고 있어요」")
				PRINTFORMDL %CALLNAME:TARGET%의 뺨이 붉다…
				PRINTFORMDL %CALLNAME:TARGET%(은)는 평상복 모습의 %CALLNAME:MASTER%에도 욕정하고 있는 것 같다…
				PRINTFORML
			ENDIF
			RETURN 1
		;반라되었다
		CASE 1011
			;전라→반라に対応できない
			;PRINTFORML 「ほら、脱ぎなさいよ」
			[SKIPSTART]
			;着衣から반라へ　条件設定が未だ
			PRINTFORMDL %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 의복을 히응저쪽으로 했지만, 양복에 익숙하지 않는다%CALLNAME:TARGET%(은)는 기세가 지나쳐서 속옷마다 질질 꺼 내려 버렸다…
;OriginalString : 인4, "「あっ…」")
			CALL FADEIN(4, "「앗…」")
;OriginalString : 아웃4, "「ごっ…」")
			CALL FADEOUT(4, "「…」")
			PRINTFORMW 「…미안해요」
			PRINTFORML 
			PRINTFORMDL %CALLNAME:TARGET%(은)는 몹시 놀란 후, 얌전하고%CALLNAME:MASTER%의 속옷을 바탕으로 되돌렸다…
			PRINTFORMDL 강한 입장에 있었다고 해도 솔직하게 사과하는 일을 할 수 있다%CALLNAME:TARGET%에 %CALLNAME:MASTER%(은)는 호인상을 가졌다…
			PRINTFORML
			[SKIPEND]
			RETURN 1
		;전라되었다
		CASE 1021
			PRINTFORMW 「이렇게 해 재차 보면(자)…」
			PRINTFORML （좋은 몸 하고 있는 원이군요）
			PRINTFORML 
			PRINTFORMDL %CALLNAME:TARGET%(은)는 이슬 악어 되었다%CALLNAME:MASTER%의 팔에 두근거리고 있다…
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が恥ずかしがって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG >= 1000 && (ARG & 10) == 2
	SELECTCASE ARG
		;반라だって恥ずかしい
		CASE 1012
			;知らなかったのか？ワープ巫女からは逃げられない。が、無理にはひん剥かない。
			PRINTFORML 벗게 하려고 한다%CALLNAME:TARGET%의 손에 %CALLNAME:MASTER%(은)는 지만 뒤로 물러나 하면(자)%CALLNAME:TARGET%와의 뒤쫓고 고화 시작되었다…
			PRINTFORML 어디까지 도망쳐도 눈앞에 나타난다%CALLNAME:TARGET%에 %CALLNAME:MASTER%의 마음은 당장 접힐 것 같다…
			PRINTFORML
			RETURN 1
		;普段から전라なんて恥ずかしい
		CASE 1022
			PRINTFORML 
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상が嫌がって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG >= 1000 && (ARG & 10) == 3
	SELECTCASE ARG
		;반라は嫌
		CASE 1013
			PRINTFORML 
			PRINTFORML 
			RETURN 1
		;普段から전라なんて嫌
		CASE 1023
			PRINTFORML 
			PRINTFORML 
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상を着替え시킨다のをやめた FIRSTEVENT(70)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:MASTER%의 의상はすでに[　　]に設定されています
;------------------------------------------------
IF 구상記入チェック(0) && ARG >= 1000 && (ARG % 10) == 0
	SELECTCASE ARG
		;통상のまま
		CASE 1000
			IF TALENT:착의플레이선호
				;성교判定に不利なため見送り
				;CALL FADEIN(8, "こくこく")
				;PRINTFORMDW %CALLNAME:TARGET%は何故か首を縦に振った…
				PRINTFORML
				PRINTFORML
			ENDIF
			RETURN 1
		;반라のまま
		CASE 1010
			PRINTFORML 
			PRINTFORML 
			RETURN 1
		;전라のまま
		CASE 1020
			PRINTFORMDL %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 피부를
;OriginalString : 아웃4, "ちらちら")
			CALL FADEOUT(4, "힐끔힐끔")
			PRINTFORMDL (와)과 관찰하고 있다
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;────────────────────────────────────
;EV102, 죠교자変更
;────────────────────────────────────
;・죠교자を交代시켰다ときに表示
;-------------------------------------------------
@KOJO_EVENT_K1_102(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 구상主が他のキャラに任せた、1, 구상主が죠교자되었다)
;旧죠교자は「%CALLNAME:SHOP_OLDTARGET%」、新죠교자は「%CALLNAME:SHOP_NEWTARGET%」で参照できる
;구상主はTARGETで参照
;-------------------------------------------------
;・반항각인は죠교자毎に保存されるので、MARK:MASTER:반항각인などとしてしまうと0にしかならないので注意
;・これは굴복각인も同様です
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;他の죠교자に交代
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 0
	;죠교대상の반항각인Lv5以上
	IF MARK:반항각인 >= 5
		PRINTFORMW 
	;죠교대상の반항각인Lv4以下
	ELSE
		PRINTFORML 「그렇게. 그러면 아…」
		PRINTFORML 「 나는 여기에 있기 때문에」
		;お茶に입をつけるまでの間を空ける為に遅め
;OriginalString : 아웃8, "…ずぞぞっ")
		CALL FADEOUT(8, "…")
		PRINTFORML
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さまが죠교자되었다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 1
	;죠교대상の반항각인Lv5以上
	IF MARK:반항각인 >= 5
		PRINTFORMW 
	;죠교대상の반항각인Lv4以下
	ELSE
		PRINTFORML 「응후후♪」
		PRINTFORMW 「역시%CALLNAME:TARGET%가 제일 사랑스럽다？」
		PRINTFORMDL %CALLNAME:TARGET%(은)는 기분 좋을 것 같다…그 기질은 공중에 떠지고 있다…
		PRINTFORML
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;EV110, 마력使用
;────────────────────────────────────
;・마력を消費して何らかの特典を購入했다ときに表示
;-------------------------------------------------
@KOJO_EVENT_K1_110(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 마력使用前、1, 죠교기교、2, 지식を得る前、3, 배합지식취득、4, 매혹취득、5, 금단의지식취득、6, 도구능숙취득、
;	7, 技術を得る前、8, 혀기술취득、9, 손가락기술취득、10, 긴박능숙、11, 새드消去/새드취득、12, 더러움무시취득、 38, 착의플레이선호、39, 허리기술)
;이벤트구상：アイテム
;引数内訳…
;ARG=(13, アイテム練成前、14, 로터練成、15,바이브練成、16, 항문비즈練成、 17, 페니스밴드練成、 18, 항문바이브練成
; 19, 클리캡練成、 20, 유두캡練成、21, 오나홀練成)
;이벤트구상：責め具錬成
;引数内訳…
;ARG=(22, 責め具練成前、23, 눈가리개練成、24, 채찍練成、 25, 밧줄練成、 26, 볼개그練成、27,  바늘練成、28, 관장세트練成
; 29, 삼각목마練成、 30, 거울練成、 31, 개목걸이練成、 32, 도구をカスタマイズ했다)
;その他이벤트
;引数内訳…
;ARG=(33, 후타나리化/戻す、34, 거유化/戻す、35, 빈유化/戻す、36, 精力の限界値上昇)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま마력使用前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 0
	;=================================================
	;初回
	;=================================================
	IF 구상記入チェック(100) && FIRSTEVENT(0)
		PRINTFORML 「음, 쌀권…쌀권은…」
		PRINTFORMDL 몇분 후, 납죽 엎드림이 되어 떨구는 %CALLNAME:TARGET%의 모습이 있었다
		PRINTFORML
		RETURN 1
	ENDIF
	RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さまの죠교기교がアップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま지식を得る前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 2
	PRINTFORMW 
	RETURN 1
ENDIF
;━━━━━━━━━━━━━━━━━━━━━━━━
;매혹の%CALLNAME:TARGET%さま
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 4
	[SKIPSTART]
	;=================================================
	;당신が새드の場合
	;=================================================
	;巫女さんに日本刀
	;새드당신をお誘いしてみる레이무さん
	;악세서리を見せびらかす女性でもない
	;おもちゃを自慢する子供でもない
	;しかしらしさがいまひとつ出せない
	;あざとさが強く自然体な솔직さとミスマッチ
	IF TALENT:MASTER:새드
		;PRINTFORMDL %CALLNAME:TARGET%は손に日本刀を握りしめている
		;PRINTFORMDL どこから調達했다のかと聞くと廃刀令の영향で日本刀の値崩れは激しいとの説明を受けた
		PRINTFORMDL %CALLNAME:TARGET%(은)는 지만 뒤에 칼을 잡으면서 %CALLNAME:MASTER%의 얼굴을 들여다 봐 넣어 왔다
		PRINTFORMW 「어때？그, 범하고 싶어지는 것일까」
		PRINTFORMDL 
		PRINTFORMDW %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%의 기학심을 자극하려고 시도한 것 같지만%CALLNAME:MASTER%에 그런 것은 없다…
		PRINTFORMDL 
		;企ててはコケる茨레이무さん可愛い
		PRINTFORMDL 납죽 엎드림으로 고개 숙인다%CALLNAME:TARGET%에 매료되었다%CALLNAME:MASTER%(은)는, 마음 여기에 있지 않고라고 하는 표정으로 %CALLNAME:TARGET%(을)를 어루만지고 어루만지고 했다
		PRINTFORMDL 헛손질 한 진심은 호밍이 걸려, 부자연스러운 각도로부터이지만
		PRINTFORMDL %CALLNAME:TARGET%의 매력은 확실히 전해졌다
		PRINTFORML 
		PRINTFORMDL %CALLNAME:TARGET%(은)는 일시적으로[매혹](을)를 얻었다
		PRINTFORML
		RETURN 1
	ENDIF
	[SKIPEND]
	;=================================================
	;당신がマゾの場合
	;=================================================
	;猫巫女레이무さんと虐待されるマゾ당신
	IF TALENT:MASTER:도Ｍ
		PRINTFORML 「-응」
		PRINTFORMDW %CALLNAME:TARGET%(은)는 새빨갛게 되어 프르풀 하면서 고양이의 흉내를 냈다
		PRINTFORML 
		PRINTFORMDL ………
		PRINTFORMW ………
		PRINTFORML 
		PRINTFORMW 「…, -응」
		PRINTFORML 
		PRINTFORMDL ………
		PRINTFORML ………
		PRINTFORMDW ………
		PRINTFORML 
		PRINTFORMDL 침묵에 계속 참을 수 없게 되었다%CALLNAME:TARGET%(은)는 고속 이동으로 도망쳐 갔다
		PRINTFORMDL 고양이에 학대되는 생활을 생각해,%CALLNAME:MASTER%의 피학심은 조용하게 불타고 번창했다…
		RETURN 1
	ENDIF
	
		[SKIPSTART]
		;레이무が着衣中
		;衣服供給源を엘리스にするか유카리にするか悩ましい所 保護者の애만들기奨励が欲しいので유카리に断定
		PRINTFORMDW 홍백의 옷(기모노)에 몸을 쌌다%CALLNAME:TARGET%(은)는 지만 나비와 같이 소매를 펄럭이게 한다…
		PRINTFORML 「보라색에 받았어. 응후후♪가끔 씩은 이런 것도 좋지요？」
		PRINTFORMW 「어때？어울리는 것일까」
		PRINTFORMW %CALLNAME:TARGET%에 매료되고 있는 자각에 %CALLNAME:MASTER%의 뺨은 열을 띠어 갔다…
		
		;ツリ目레이무。ゆかりんの真似
		PRINTFORML 「너무 끈적끈적 하고 있다고(면) 질릴 수 있는아니야 해들 」
		PRINTFORML 「응―. 이런 느낌 까？」
		PRINTFORMDL %CALLNAME:TARGET%(은)는 %CALLNAME:TARGET%(을)를 다시 반하려고 , 거울의 전으로 악전고투 하고 있다…
		[SKIPEND]
	
	;=================================================
	;それ以外の場合
	;=================================================
	IF 구상記入チェック(100)
		;描写不발につき加筆
		PRINTFORMDL 세면소에 들어가면(자) 유노에모습의 %CALLNAME:TARGET%와 직면했다
		PRINTFORML 
		PRINTFORMW 와
		PRINTFORML 
		PRINTFORML 「욕실, 먼저 받았어요」
		PRINTFORMW 「이불%RAND_SPLIT(", 따뜻하게 해 두기 때문에/그리고 기다리고 있기 때문에")%」
		PRINTFORMDL %CALLNAME:TARGET%의 %RAND_SPLIT("말에/젖음머리카락에/하했다머리카락에/상기했다뺨에/나머지탕에/목덜미에/사용이 끝난손닦음에")% %CALLNAME:MASTER%의 가슴은 크게 울렸다…
		;PRINTFORML 
		;PRINTFORMDL %CALLNAME:TARGET%は一時的に[매혹]を得た
	ENDIF
	PRINTFORML
	PRINTFORMD 정말 일이 없는 일상의 한 토막에 %CALLNAME:TARGET%(을)를 의식해 버려%CALLNAME:MASTER%(은)는 목욕통안와 가라앉아 간다
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま素質を취득
;━━━━━━━━━━━━━━━━━━━━━━━━
;・필요に応じて個別の条件に切り分けて下さい
;------------------------------------------------
IF 구상記入チェック(100) && (INRANGE(ARG, 3, 12) || GROUPMATCH(ARG, 38, 39))
	SELECTCASE ARG
		[SKIPSTART]
		허리기술は39です
		ちなみにこの部分の処理はBONUS_GAIN.ERB内にあります@/L
		[SKIPEND]
		;허리기술
		CASE 39
			PRINTFORMDW %CALLNAME:TARGET%(은)는 서서히%CALLNAME:MASTER%(을)를 차 넘어뜨리면(자),%CALLNAME:MASTER%에 걸쳐 허리 사용의 연습을 시작했다
			;地の分で十分
			;PRINTFORMDL %CALLNAME:TARGET%は[허리기술]を취득했다
			;PRINTFORML
			RETURN 1
		;허리기술以外の場合はRETURN 0しておきます@/L
		CASEELSE
			RETURN 0
	ENDSELECT
	;RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さまアイテム錬成前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 13
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さまアイテム練成
;━━━━━━━━━━━━━━━━━━━━━━━━
;・필요に応じて個別の条件に切り分けて下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && INRANGE(ARG, 14, 21)
	SELECTCASE ARG
		;오나홀
		CASE 21
			PRINTFORML 「이것은 생선살 꼬치구이……이것은 생선살 꼬치구이……」
			PRINTFORMDL 무엇인가 이상한 주문이 들려 왔다
			PRINTFORMDL 그렇지만, 슬픈 일에 %CALLNAME:TARGET%의 직감은 야박하게도 「먹을 수 없습니다」라고 고하고 있다…
			PRINTFORML
			RETURN 1
		;오나홀以外の場合はRETURN 0しておきます@/L
		CASEELSE
			RETURN 0
	ENDSELECT
	;RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま責め具練成前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 22
	PRINTFORMW 
	PRINTFORML
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま責め具錬成
;━━━━━━━━━━━━━━━━━━━━━━━━
;・필요に応じて個別の条件に切り分けて下さい
;------------------------------------------------
IF 구상記入チェック(100) && INRANGE(ARG, 23, 31)
	SELECTCASE ARG
		;눈가리개
		;CASE 23
		;채찍
		;CASE 24
		;밧줄　%CALLNAME:TARGET%의 場合鬼縛陣　専用地の文持ち
		;%CALLNAME:TARGET%さんが良い子過ぎてSM系が埋まらない。いっその事SMグッズを自前で用意시킨다のはどうだろう。
		;채찍=紙垂 밧줄目隠し=札 木馬=賽銭箱 바늘=信仰바늘 입枷=ドロワ オナホ=소매、髪飾り
		CASE 25
			PRINTFORMDW %CALLNAME:TARGET%(은)는 빨리 지폐를 만들고 있다…
			PRINTFORMDL …싫은 예감 그러나 없다
			PRINTFORML
			RETURN 1
		;볼개그
		;CASE 26
		;바늘
		;CASE 27
		;거울
		CASE 30
			PRINTFORML 「--！」
			PRINTFORMDW 무엇인가%CALLNAME:TARGET%(은)는 지만 거울의 전으로 자세를 취하고 있다
			PRINTFORMW 「아 이것？옛날이군요. 유행했어요」
			PRINTFORML
			RETURN 1
		;개목걸이
		;犬でも猫でも%CALLNAME:MASTER%でも、개목걸이は初손で着けてしまうのがジャスティス
		;CASE 31
		;条件にヒットしなかった場合はRETURN 0を返しておきます@/L
		CASEELSE
			RETURN 0
	ENDSELECT
	;RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま도구をカスタマイズ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(0) && ARG == 32
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま후타나리化or후타나리消去
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 33
	;=================================================
	;후타나리化
	;=================================================
	IF 구상記入チェック(100) && TALENT:TARGET:후타나리
		PRINTFORMW 「너…이런 것 붙이게 한 이상은, 나 대신에…」
		;男당신でも同一のリアクションむしろ男당신想定
;OriginalString : 인12, "「産んで貰うわよ？」")
		CALL FADEIN(12, "「낳아 받아요？」")
		PRINTFORMDL %CALLNAME:TARGET%의 눈은 진검 그 자체다…
		PRINTFORML
		RETURN 1
	ENDIF
	
	;=================================================
	;후타나리消去
	;=================================================
	IF 구상記入チェック(100) && !TALENT:TARGET:후타나리
		PRINTFORMW 「그것으로 좋아. 역시 너는 짜 취해 주지 않으면 기분이 내키지 않는 것」
		PRINTFORML 「우대신♪…충분히귀여워해 주기 때문에, 각오 해 주세요♪」
		PRINTFORML
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま거유化or통상に戻す
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 34
	;=================================================
	;거유化
	;=================================================
	IF 구상記入チェック(100) && TALENT:TARGET:거유
		[SKIPSTART]
		FIRSTEVENT等の이벤트번호は同じ이벤트内"@KOJO_EVENT_K1_110(ARG)"では分ける필요があります
		例えばこの箇所の場合、%CALLNAME:TARGET%さま마력使用前でFIRSTEVENT(0)が立ってしまうので、(0)では決して真になることはありません
		とりあえずARGの번호を割り振っておきます@/L
		[SKIPEND]
		;初回
		IF FIRSTEVENT(34)
			PRINTFORML 「이 크기라면…」
			PRINTFORMDW %CALLNAME:TARGET%(은)는 지만 훨씬 주먹을 만든다
			PRINTFORML
			PRINTFORMDW …잠시 후, 납죽 엎드림이 되어 떨구는 %CALLNAME:TARGET%의 모습이 있었다
			PRINTFORML 「보라색에는 이길 수 없었어요…」
			PRINTFORML
			;初回時のみRETURNするようにしておきます。初回時以外は0が返ります@/L
			RETURN 1
		ELSE
			PRINTFORML 「가슴 팍이 힘들네요…응？응후후…♪」
			PRINTFORMDW %CALLNAME:MASTER%의 시선을 알아차렸다%CALLNAME:TARGET%(은)는, 겨드랑이를 불과에 지연%CALLNAME:MASTER%(을)를 야유 있기 시작했다
			PRINTFORMW 「여기？여기에 자지 찔러 붐비고 싶다？」
			PRINTFORML 「너는 진짜,%RAND_SPLIT("호색가/호색가/엣치")%(이)니까♪」
			PRINTFORML
			RETURN 1
		ENDIF
		;RETURN 1
	ENDIF
	
	;=================================================
	;元に戻す
	;=================================================
	IF 구상記入チェック(100) && !TALENT:TARGET:거유
		PRINTFORMW 「그렇구나. 나도, 원 인 채의 나를 사랑했으면 좋아요」
		PRINTFORML
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま빈유化or통상に戻す
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && ARG == 35
	;=================================================
	;빈유化
	;=================================================
	IF 구상記入チェック(100) && TALENT:TARGET:빈유
		PRINTFORMDL %CALLNAME:TARGET%(은)는 지만%CALLNAME:MASTER%의 시선을 헤아려 사라시를 푼다
		PRINTFORMDW %RAND_SPLIT("넓어졌다가슴원/헐렁헐렁의 겨드랑이")%(으)로부터 가슴의 첨단이 외관…
		PRINTFORMDW …보일 것 같으면서 안보인다
		PRINTFORML
		PRINTFORML 「♪」
		PRINTFORMW 「이%RAND_SPLIT("호색가/호색가/로리콘")%」
		PRINTFORML 「이런 것, 좋아한다？」
		PRINTFORML 「어머 어머♪」
		PRINTFORML
		PRINTFORMDL %CALLNAME:TARGET%은(는) %CALLNAME:MASTER%(을)를 몹시 나쁘게 야유라고 만족한 것 같다
		PRINTFORML
		RETURN 1
	ENDIF
	
	;=================================================
	;元に戻す
	;=================================================
	IF 구상記入チェック(100) && !TALENT:TARGET:빈유
		PRINTFORML 「그렇구나. 나도, 원 인 채의 나를 사랑했으면 좋아요」
		PRINTFORML
		RETURN 1
	ENDIF
ENDIF

;─────────────────
;EV111, 주인님として新しい죠교자を雇った
;────────────────────────────────────
@KOJO_EVENT_K1_111(ARG)
;-------------------------------------------------
;・新しい죠교자がTARGETになっているため、구상主はTARGETではないので注意
;・구상主を参照했다い場合はFLAG:주인님で
;-------------------------------------------------
;戻り値（1,表示、2,表示して加入했다キャラの加入구상をスキップ）
;-------------------------------------------------
IF 구상記入チェック(0)
	;//NO:TARGETで雇った相손を参照することが可能です
	;CSTR:(FLAG:주인님):호칭목록 = 元の名前_呼ばせたい名前/%CSTR:(FLAG:주인님):호칭목록%
	PRINTFORMW 
	PRINTFORML
	RETURN 1
ENDIF

;────────────────────────────────────
;EV112, %CALLNAME:TARGET%さまが죠교자として加入했다
;────────────────────────────────────
@KOJO_EVENT_K1_112(ARG)
;-------------------------------------------------
;・주인님の名前は「FLAG:주인님」で参照できる
;・주인님が雇用구상で戻り値2を返していると表示されない
;・구상主はTARGET
;-------------------------------------------------
IF 구상記入チェック(100)
	;%CALLNAME:TARGET%が来ても助けてくれない。現実は非情である。
	;죠교대상の반항각인Lv5以上
	IF MARK:(FLAG:주인님):반항각인 >= 5
		PRINTFORMW 「거기까지！」
		PRINTFORML
	;죠교대상の반항각인Lv4以下
	ELSE
		PRINTFORMW 「거기까지！」
		;%CALLNAME:MASTER%に言っているのか주인님に言っているのかはぼかす
		PRINTFORMW 「상당히 즐거운 듯 하는 일. 해」
		PRINTFORML
	ENDIF
	RETURN 1
ENDIF
