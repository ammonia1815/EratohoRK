;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	유카리구상 죠교종료이벤트구상파일
;
;	죠교종료이벤트구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 죠교종료구상
;========================================================================
;·@EVENTCOMEND로부터 불려 간다
;·구상주가죠교종료하기 직전에 표시
;·ARG:1, ARG:2의 경우는 지문 표시 후에 PRINTL로 공행. 그 뒤로구상를 표시
;·ARG:0의 경우는 고유의 지문 없음. 구상표시전에 PRINTL로 공행이 들어간다
;·RESULT 0이상을반환와구상표시 후에 PRINTL로 공행이 들어간다
;-------------------------------------------------
;관련 변수…
;FLAG:일상제어	죠교종료시 상황(1=통상/2=정력 0/3=일회휴게/4=실신했다)
;-------------------------------------------------
@KOJO_FEMALEYOU_EVENT_K22_13(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 보통으로 종료, 1, 죠교대상가 피곤한, 2, 죠교대상가실신했다)
;-------------------------------------------------

;────────────────────────────────────
;ARG:1, ARG:2, 피로·실신에 의한죠교종료 FIRSTEVENT(0)～
;────────────────────────────────────
;·죠교대상소모도에 응해 피로/실신로 나누어진다
;·호출 타이밍은 지문의 표시→PRINTL→구상의 표시
;·EVENT201, 간호(일상이벤트)의 개소를 쓰는 경우는, 상손구제휴할 수 있도록(듯이)하면 좋을 것입니다
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가 피곤함
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교대상가실신했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 2
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「あらあらあら…ちょっと加減に失敗しちゃったかしら」
		PRINTFORMW 「대강 어머나…조금 조절에 실패해 버렸다 까」
;OriginalString : 困ったという風に苦笑いを浮かべ、유카리は%CALLNAME:MASTER%を침대へと運んだ
		PRINTFORMDW 곤란했다고 하는 바람으로 쓴 웃음을 띄워 보라색은 %CALLNAME:MASTER%(을)를 침대로 옮겼다
	;아라이멘도부
	ELSE
;OriginalString : 「この程度で…한심한わね」
		PRINTFORMW 「이 정도로…한심하네요」
;OriginalString : そう吐き捨てながらも、유카리は%CALLNAME:MASTER%を침대へと運んだ
		PRINTFORMDW 그렇게 토해 버리면서도, 보라색은 %CALLNAME:MASTER%(을)를 침대로 옮겼다
		;발해 떠난, 이라면@DAILY_LIFE_NURSING와 이치가 맞지 않게 되므로 개서
		;이구상만을 위해서 시스템측손 넣고 하는 것이 귀찮을 뿐이었다고도 말한다@/L
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ARG:0보통으로 종료 FIRSTEVENT(10)～
;────────────────────────────────────
;·통상의죠교종료시의구상
;·지문 없음. PRINTL에 의한 공행의 뒤로구상표시
;·테두리마다이벤트번호를 털어 있습니다만, 임의로 설정하는 일은 가능합니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;대만족보너스를 얻고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:금회대만족
	;아라이멘도정
	IF ALI(0)
		IF FLAG:구상
;OriginalString : 「よかったわよ、후후후…」
			PRINTFORMW 「좋았어요, 후후후…」
;OriginalString : 만족げに笑みを浮かべると、유카리は%CALLNAME:MASTER%의 体を軽く撫でた
			PRINTFORMDW 만족스럽게 미소를 띄우면(자), 보라색은 %CALLNAME:MASTER%의 몸을 가볍게 어루만졌다
		ENDIF
	;아라이멘도부
	ELSE
		IF FLAG:구상
;OriginalString : 「ええ、そう…その調子よ」
			PRINTFORMW 「예, 그렇게…그 상태야」
;OriginalString : そう言うと유카리は床に伏せる%CALLNAME:MASTER%의 顎を持ち上げ笑ってみせた
			PRINTFORMDW 그렇게 말하면 보라색은 마루에 엎드린다%CALLNAME:MASTER%의 턱을 들어 올리고 웃어 보였다
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;*************************************************
;이하, 호감도에 의한 파생
;*************************************************

[SKIPSTART]
미기입 개소를 comment out
;EVENT11 근처에서 임의의 KFLAG를 세워 조건으로 해 준다고 이야기의 이치는 맞추기 쉽습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;상사상애（호의 3000 이상·호감도 3000 이상）
;-------------------------------------------------
;·서로 서로 좋아하고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("상사상애")
ENDIF

;호감도의 성장적에익애의 뒤가 된다고 생각됩니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교완료（호의 2000 이상）
;-------------------------------------------------
;·죠교대상가구상 주로 이제(벌써) 헤롱헤롱
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("죠교완료")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;익애（호감도 2000 이상）
;-------------------------------------------------
;·죠교자가죠교대상에 빠져 버려, 깊기도 하고 도착했다 사랑을 따르고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("익애")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교가 끝난 상태（호의 1000 이상）
;-------------------------------------------------
;·죠교대상가구상 주로죠교 되는 것을 행복하게 느끼고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("죠교가 끝난 상태")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;혐오（호의-100 미만）
;-------------------------------------------------
;·죠교자에 대해 혐오감을 안고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("혐오")
ENDIF

;-------------------------------------------------
;·마음에 상처를 만들어져주인님의 모습을 보는 것만으로 무서워해 버린다
;·트라우마의 종은 「%ACTSTR(CFLAG:MASTER:8)%」로 표시할 수 있다
;  예）PRINTFORMW 「이제(벌써)%ACTSTR(CFLAG:MASTER:8)%는 싫습니다 라고?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;공포（호의 200 미만·트라우마Lv3 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("공포")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;반항（호의 0 이하·반항각인Lv1 이상）
;-------------------------------------------------
;·마음을 열지 않는노예에 초조해지는주인님
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("반항")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;눈을 뜨고（호의 500 이상）
;-------------------------------------------------
;·죠교 되는 것도 오히려 좋아하게 되었는지도 모른다…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("눈을 뜸")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;순응（호의 100 이상·호감도 100 이상）
;-------------------------------------------------
;·정중하게죠교를 진행되어, 생각했던 것보다 싫지 않을지도…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("순응")
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;초반（조건 없음）
;-------------------------------------------------
;·여기까지의 조건에 히트 하지 않았던 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「うーん…ま、こんなところかしらね」
		PRINTFORMW 「응…뭐, 이런 곳일까」
;OriginalString : そう言って유카리はバイバイと손を振りながらスキマの中へ消えていった
		PRINTFORMDW 그렇게 말해 보라색은 바이바이와 손을 흔들면서 스키마안에 사라져 갔다
	;아라이멘도부
	ELSE
;OriginalString : 「…暇つぶしにはなったけど」
		PRINTFORMW 「…시간때우기로는 되었지만」
;OriginalString : どうやら不満らしく、%CALLNAME:MASTER%を見るその目は冷たい
		PRINTFORMDW 아무래도 불만인것 같고, %CALLNAME:MASTER%(을)를 보는 그 눈은 차갑다
;OriginalString : 「はあ、次はもっと楽しませなさい」
		PRINTFORMW 「은 , 다음은 좀 더 즐겁게 하세요」
;OriginalString : そう言って유카리はさっさとスキマの中へ消えていった
		PRINTFORMDW 그렇게 말해 보라색은 빨리 스키마안에 사라져 갔다
	ENDIF
	RETURN 1
ENDIF
