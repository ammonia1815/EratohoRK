;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	무라사구상 추가 액션구상파일
;
;	죠교자가죠교지시 이외에 실시하는행동의 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;죠교액션·커맨드구상
;========================================================================

;────────────────────────────────────
;탈의액션구상
;────────────────────────────────────
;·죠교자액션의 앞에강제탈의나자주탈의가 발생했다때에 표시
;·이구상의 시점에서는 아직 TEQUIP는 갱신되어 있지 않다
;·첫회 분기는 「처음 보았다/보였다」란 할 수 없다.
;  숍 메뉴로 벗게 했던 적이 있을지도 모르고, 죠교의 흐름으로 완전하게 벗게 하지 않고서노출만 하고 있는 경우도 있다.
;  어쩌면조수였던 때에 본 적이 있을지도 모른다. 그러한 의미에서는 간이적인 것이다.
;  엄밀한 의미에서의 「초견」플래그를 준비할지 어떨지는 요망하는 대로라고 하는 일로.
;-------------------------------------------------
@KOJO_DATUI_K68
;-------------------------------------------------
;관련 변수…
;강제탈의:TFLAG:102  자주탈의:TFLAG:103
;=(-1, 강제탈의에 저항, 1, 겉옷, 2, 하반신겉옷, 3, 웃도리, 4, 상반신전부, 5, 하반신전부,
;  6, 악세서리─, 7, 악세서리─이외 모두, 8, 전라까지, 9, 속옷만)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의에 저항당했다 FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 < 0
;OriginalString : 
	PRINTL
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(0)
;OriginalString : 「あら、そんなに嫌がらなくても」
		PRINTFORMW 「어머나, 그렇게 싫어하지 않아도」
;OriginalString : 「そのままだと、服が汚れてしまいますよ？」
		PRINTFORMW 「그대로라고, 옷이 더러워져 버려요?」
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;자신은 벗었다
	;=================================================
	IF 구상기입체크(100) && TFLAG:자주탈의
		;퍼펙트 서로 사모함
		IF KFLAG:700
;OriginalString : 「私は脱いでるのにそんなに嫌がらなくても～」
			PRINTFORMW 「나는 벗어 그렇게 싫어하지 않아도～」
;OriginalString : 「わざと애태움てるのね？このいけずぅ」
			PRINTFORMW 「일부러 초조하게 해? 이 가지 못하고」
;OriginalString : 
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이멘도정
		IF ALI(0)
;OriginalString : 「うーん、そんなに嫌がらなくても」
			PRINTFORMW 「응, 그렇게 싫어하지 않아도」
;OriginalString : 「服を脱いで誘っている女性を前にして」
			PRINTFORMW 「옷을 벗어 권하고 있는 여성을 앞에 두고」
;OriginalString : 「美味しい状況だ、と言う風には思ってくれないのですかね？」
			PRINTFORMW 「맛있는 상황이다, 라고 하는 식으로는 생각해 주지 않습니까?」
		;아라이멘도부
		ELSE
;OriginalString : 「ふーん…まぁ、やる事は変わらないし」
			PRINTFORMW 「-응…아무튼, 하는 일은 변함없고」
;OriginalString : 「汚れ塗れの臭い服を着て生活했다いのなら止めないですよ」
			PRINTFORMW 「더러워지고 바를 수 있는의 수상한 옷을 입어 생활하고 싶다면 멈추지 않아요」
		ENDIF
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
;OriginalString : 「えー？どうして싫어한다んですかー？」
		PRINTFORMW 「네─? 어째서 싫어합니까─?」
;OriginalString : 「服、脱ぎたくないの？もしかして着하며 했다い…とか？」
		PRINTFORMW 「옷, 벗고 싶지 않은거야? 혹시 입은 채로 하고 싶다… 라든지?」
;OriginalString : 「%CALLNAME:MASTER%が했다いのなら、私はそれでもいいですけどね」
		PRINTFORMW 「%CALLNAME:MASTER%(을)를 하고 싶다면, 나는 그런데도 좋지만」
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「だから、服が汚れてしまいますよ？」
		PRINTFORMW 「그러니까, 옷이 더러워져 버려요?」
;OriginalString : 「%CALLNAME:MASTER%にもこの環境を早く受け入れて欲しいものですね」
		PRINTFORMW 「%CALLNAME:MASTER%에도 이 환경을 빨리 받아들였으면 좋아요」
	;아라이멘도부
	ELSE
;OriginalString : 「ふーん…まぁ、やる事は変わらないし」
		PRINTFORMW 「-응…아무튼, 하는 일은 변함없고」
;OriginalString : 「汚れ塗れの臭い服を着て生活했다いのなら止めないですよ」
		PRINTFORMW 「더러워지고 바를 수 있는의 수상한 옷을 입어 생활하고 싶다면 멈추지 않아요」
	ENDIF
;OriginalString : 
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;두 사람 모두 뭔가 벗은 FIRSTDATUI(1)
;------------------------------------------------
;·주로섹스계행동의 예비 동작으로 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 > 0 && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
;OriginalString : 
	PRINTL
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(1)
		;아라이멘도정
		IF ALI(0)
;OriginalString : 「はい、早く%CALLNAME:MASTER%も脱いで」
			PRINTFORMW 「네, 빨리%CALLNAME:MASTER%도 벗으며」
;OriginalString : ムラサは自分の衣服を脱ぎながら、%CALLNAME:MASTER%にそう告げる
			PRINTFORMDW 무라사는 자신의 의복을 벗으면서, %CALLNAME:MASTER%에 그렇게 고한다
;OriginalString : 「さぁ、一緒に気持ち良くなりましょう？」
			PRINTFORMW 「, 함께 기분 좋아집시다?」
		;아라이멘도부
		ELSE
;OriginalString : 「ほら、早く%CALLNAME:MASTER%も脱ぎなさい」
			PRINTFORMW 「이봐요, 빨리%CALLNAME:MASTER%도 벗으세요」
;OriginalString : ムラサは自分の衣服を脱ぎながら、%CALLNAME:MASTER%にそう告げる
			PRINTFORMDW 무라사는 자신의 의복을 벗으면서, %CALLNAME:MASTER%에 그렇게 고한다
;OriginalString : 「あまり待たせると苛めてしまうわよ？」
			PRINTFORMW 「너무 기다리게 하면 괴롭혀 버려요?」
		ENDIF
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;죠교자가[정욕]상태
	;=================================================
	IF 구상기입체크(100) && T_COND("정욕")
		;퍼펙트 서로 사모함
		IF KFLAG:700
;OriginalString : 「ねぇ、%CALLNAME:MASTER%も早く脱いで、早く早く♪」
			PRINTFORMW 「응, %CALLNAME:MASTER%도 빨리 벗어, 빨리 빨리♪」
;OriginalString : 미나미츠は自分の衣服を勢い良く脱ぎ捨てながら、%CALLNAME:MASTER%を急かす
			PRINTFORMDW 수밀도는 자신의 의복을 기세 좋게 벗어 던지면서, %CALLNAME:MASTER%(을)를 급 빌려준다
;OriginalString : 「早くいっぱいしてね、待ちきれないわ♪」
			PRINTFORMW 「빨리 가득 해, 기다릴 수  없어요♪」
;OriginalString : 
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이멘도정
		IF ALI(0)
;OriginalString : 「早く%CALLNAME:MASTER%も脱いで、なるべく急いでね？」
			PRINTFORMW 「빨리%CALLNAME:MASTER%도 벗어, 가능한 한 서둘러?」
;OriginalString : ムラサは손早く自分の衣服を脱ぎながら、%CALLNAME:MASTER%にそう告げる
			PRINTFORMDW 무라사는 재빠르게 자신의 의복을 벗으면서, %CALLNAME:MASTER%에 그렇게 고한다
;OriginalString : 「私、もう참다できないわ…早く기분좋게 해ね？」
			PRINTFORMW 「나, 이제 참을 수 없어요… 빨리 기분 좋게 해?」
		;아라이멘도부
		ELSE
;OriginalString : 「ふふ、욕정してしまったわ…ほら、早く%CALLNAME:MASTER%も脱ぎなさい」
			PRINTFORMW 「후후, 욕정해 버렸어요…이봐요, 빨리%CALLNAME:MASTER%도 벗으세요」
;OriginalString : ムラサは自分の衣服を脱ぎながら、%CALLNAME:MASTER%にそう告げる
			PRINTFORMDW 무라사는 자신의 의복을 벗으면서, %CALLNAME:MASTER%에 그렇게 고한다
;OriginalString : 「만족させてくれないと苛めてしまうわよ？」
			PRINTFORMW 「만족시켜 주지 않으면 괴롭혀 버려요?」
		ENDIF
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
;OriginalString : 「ふふっ、%CALLNAME:MASTER%も早く脱いで♪」
		PRINTFORMW 「후후, %CALLNAME:MASTER%도 빨리 벗어♪」
;OriginalString : 미나미츠は自分の衣服を脱ぎ捨て、%CALLNAME:MASTER%が服を脱ぐのを손伝った
		PRINTFORMDW 수밀도는 자신의 의복을 벗어 던져%CALLNAME:MASTER%(은)는 옷을 벗는 것을 도왔다
;OriginalString : 「気持ち良い事いっぱいしようね♪」
		PRINTFORMW 「기분 좋은 일 가득 하자♪」
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「はい、早く%CALLNAME:MASTER%も脱いで」
		PRINTFORMW 「네, 빨리%CALLNAME:MASTER%도 벗으며」
;OriginalString : ムラサは自分の衣服を脱ぎながら、%CALLNAME:MASTER%にそう告げる
		PRINTFORMDW 무라사는 자신의 의복을 벗으면서, %CALLNAME:MASTER%에 그렇게 고한다
;OriginalString : 「さぁ、一緒に気持ち良くなりましょう？」
		PRINTFORMW 「, 함께 기분 좋아집시다?」
	;아라이멘도부
	ELSE
;OriginalString : 「ほら、早く%CALLNAME:MASTER%も脱ぎなさい」
		PRINTFORMW 「이봐요, 빨리%CALLNAME:MASTER%도 벗으세요」
;OriginalString : ムラサは自分の衣服を脱ぎながら、%CALLNAME:MASTER%にそう告げる
		PRINTFORMDW 무라사는 자신의 의복을 벗으면서, %CALLNAME:MASTER%에 그렇게 고한다
;OriginalString : 「あまり待たせると苛めてしまうわよ？」
		PRINTFORMW 「너무 기다리게 하면 괴롭혀 버려요?」
	ENDIF
;OriginalString : 
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·악세서리─마셔 FIRSTDATUI(2)
;------------------------------------------------
;·주로신발안. 그 밖에장갑나소매 등
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 == 6 && !IS_NOWASSIACTNAME(, "탈의")
;OriginalString : 
	PRINTL
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(2)
;OriginalString : ムラサはその場に座り込むと、ゆっくりと신발안を脱ぎ捨てた…
		PRINTFORMDW 무라사는 그 자리에 들어앉으면(자), 천천히 양말을 벗어 던졌다…
;OriginalString : 「動いてると結構蒸れて来るんですよね」
		PRINTFORMW 「움직이고 있다고(면) 상당히 뜸들어 옵니다」
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF

	;=================================================
	;전라되었다（벗기 전은신발안 이외 알몸이라든지 그러한 느낌）
	;=================================================
	IF 구상기입체크(100) && TFLAG:자주탈의 && ZENRACHECK(0)
		;퍼펙트 서로 사모함
		IF KFLAG:700
;OriginalString : %CALLNAME:MASTER%は一糸纏わぬ姿되었다미나미츠を、穴の開くほど見つめた。
			PRINTFORMDW %CALLNAME:MASTER%(은)는 일사 감기지 않는 모습이 된 수밀도를, 구멍이 여는 만큼 응시했다.
;OriginalString : 「う…そ、そんなに見られたら恥ずかしいですわ～」
			PRINTFORMW 「…, 그렇게 볼 수 있으면 부끄럽어요～」
;OriginalString : …散々부끄러워한다なって言っていませんで했다か미나미츠さん？
			PRINTFORMDW … 산들 부끄러워하지 말라고 말하지 않았습니까 수밀도씨?
			;너무 길므로 생략			확률을 짜 회수해 버립니다@/L
			IF PERCENT(15)
;OriginalString : 
				PRINTL
;OriginalString : 「あ、ううっ…もう馬鹿っ、심술궂음なんだから」
				PRINTFORMW 「아, 우우…이제 바보, 심술궂은 응이니까」
;OriginalString : 照れ隠しにこちらをポカポカと殴りつけてきた…まぁ、力は全く入ってないですがね
				PRINTFORMDW 수줍고 은폐에 이쪽을 따끈따끈 후려갈겨 왔다…아무튼, 힘은 완전히 들어 있지 않습니다만
			ENDIF
;OriginalString : 
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이멘도정
		IF ALI(0)
;OriginalString : ムラサは신발안を脱ぎ捨て、一糸纏わぬ姿되었다。
			PRINTFORMDW 무라사는 양말을 벗어 던져 일사 감기지 않는 모습이 되었다.
;OriginalString : 「さて、これでこちらは隠すものが何一つありませんよ」
			PRINTFORMW 「그런데, 이것으로 이쪽은 숨기는 것이 무엇하나 없어요」
;OriginalString : 「こんな無防備な私を、襲ってみたくはなりませんか？」
			PRINTFORMW 「이런 무방비인 나를, 덮쳐 보고 싶지는 되지 않겠습니까?」
		;아라이멘도부
		ELSE
;OriginalString : 「何も身に着けていない少女に襲われる気分というのはどうなのでしょうね」
			PRINTFORMW 「아무것도 몸에 대지 않은 소녀에게 습격당하는 기분이라고 하는 것은 어떨까요」
;OriginalString : 「ねぇ、%CALLNAME:MASTER%？　今どんな気持ちですか？」
			PRINTFORMW 「응, %CALLNAME:MASTER%? 지금 어떤 기분입니까?」
;OriginalString : ムラサは%CALLNAME:MASTER%に見せ付けるように신발안を脱ぎ捨て전라되었다…
			PRINTFORMDW 무라사는 %CALLNAME:MASTER%에 과시하듯이 양말을 벗어 던지고 전라가 되었다…
		ENDIF
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
;OriginalString : 미나미츠は%CALLNAME:MASTER%に見せ付けるように、ゆっくりと신발안を脱ぎ捨てた…
		PRINTFORMDW 수밀도는 %CALLNAME:MASTER%에 과시하듯이, 천천히 양말을 벗어 던졌다…
;OriginalString : 「ふふ、服の方が良かった？顔がガッカリしてるわよ」
		PRINTFORMW 「후후, 옷이 좋았다? 얼굴이 실망 하고 있는 원이야」
;OriginalString : 「これが애태움플레이って言うのでしょ？　…え、違うの？」
		PRINTFORMW 「이것이 초조하게 해 플레이라고 말하므로 실마리? … 네, 달라?」
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이멘도정
	IF ALI(0)
;OriginalString : ムラサは%CALLNAME:MASTER%に見せ付けるように、ゆっくりと신발안を脱ぎ捨てた…
		PRINTFORMDW 무라사는 %CALLNAME:MASTER%에 과시하듯이, 천천히 양말을 벗어 던졌다…
;OriginalString : 「あら、もしかして服の方が良かったかしら？」
		PRINTFORMW 「어머나, 혹시 옷이 좋았는지 해들?」
	;아라이멘도부
	ELSE
;OriginalString : ムラサは%CALLNAME:MASTER%に見せ付けるように、ゆっくりと신발안を脱ぎ捨てた…
		PRINTFORMDW 무라사는 %CALLNAME:MASTER%에 과시하듯이, 천천히 양말을 벗어 던졌다…
;OriginalString : 「ふふっ、私の蒸れた발の匂い、嗅がせてあげましょうか？」
		PRINTFORMW 「후후, 내가 뜸든 다리의 냄새나, 냄새 맡게 해 줄까요?」
;OriginalString : 「それとも舐めてみますか？どちらでも%CALLNAME:MASTER%にはご褒美でしょう？」
		PRINTFORMW 「그렇지 않으면 빨아 봅니까? 어디라도%CALLNAME:MASTER%에는 포상이지요?」
	ENDIF
;OriginalString : 
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·만p FIRSTDATUI(3)
;------------------------------------------------
;·이번탈의로성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
;OriginalString : 
	PRINTL
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:주인님 && FIRSTDATUI(3)
;OriginalString : ムラサは自らの속옷を引き下ろすと、%CALLNAME:MASTER%に秘所を見せ付けた。
		PRINTFORMDW 무라사는 스스로의 속옷을 끌어 내리면(자), %CALLNAME:MASTER%에 비소를 과시했다.
;OriginalString : 「どうです？そろそろ했다くなってきませんか？」
		PRINTFORMW 「어떻습니까? 이제 하고 싶어지지 않습니까?」
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써가슴가 보이고 있다
	;=================================================
	IF 구상기입체크(100) && B_DATUICHECK(0) == 1
		;퍼펙트 서로 사모함
		IF KFLAG:700
;OriginalString : 「さて、のこりは신발안だけですが…　え？それは脱がなくていい？」
			PRINTFORMW 「그런데, 나머지는 양말 뿐입니다만… 네? 그것은 벗지 않아 좋다?」
;OriginalString : 「전라に신발안は正義？…うーん、良くわかりませんがそういうものですか？」
			PRINTFORMW 「전라에 양말은 정의? … 응, 잘 모릅니다만 그러한 것입니까?」
;OriginalString : 
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이멘도정
		IF ALI(0)
;OriginalString : 「ふふ、そろそろ下も見たいですか？今脱ぎますよ…と」
			PRINTFORMW 「후후, 이제 아래도 보고 싶습니까? 지금 벗어요…(와)과」
;OriginalString : 「%CALLNAME:MASTER%が솔직にしてくれるのならば、弄ったりしてもいいのですよ？」
			PRINTFORMW 「%CALLNAME:MASTER%는 솔직하게 해 준다면, 만지작거리거나 해도 되는 것이에요?」
		;아라이멘도부
		ELSE
;OriginalString : 「ふふ、脱ぎたての染み付き속옷、%CALLNAME:MASTER%に被せてあげようかしら」
			PRINTFORMW 「후후, 벗어 세우고의 배어들어 속옷, %CALLNAME:MASTER%에 씌워 줄까」
;OriginalString : 「そういうの好きでしょう？遠慮しなくてもいいのですよ？」
			PRINTFORMW 「그러한 것 좋아하겠지요? 사양하지 않아도 괜찮은 것이에요?」
		ENDIF
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;조건을 짜 두지 않으면죠교자의 페니스의 유무에 관련되지 않고 처리되어 버리므로페니스없음의 경우로 한정합니다@/L
	;=================================================
	;응 이렇게 p… 가 아닌 경우(죠교자에페니스없음)
	;=================================================
	IF 구상기입체크(100) && !PENIS(TARGET)
		IF KFLAG:700
;OriginalString : 미나미츠は손早く自らの속옷を脱ぎ捨てると、両손でぱっくりと秘所を広げた。
			PRINTFORMDW 수밀도는 재빠르고 스스로의 속옷을 벗어 던지면(자), 양손으로 빠끔히비소를 넓혔다.
;OriginalString : 「ねぇ%CALLNAME:MASTER%？　私の질내、よく見えますか？」
			PRINTFORMW 「응%CALLNAME:MASTER%? 나의 질내, 자주(잘) 보입니까?」
;OriginalString : 「たまには上だけ着하며 、というのもいいと思わない？」
			PRINTFORMW 「가끔 씩은 위만 입은 채로, 라고 하는 것도 좋다고 생각하지 않는다?」
;OriginalString : 
			PRINTL
;OriginalString : 
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이멘도정
		IF ALI(0)
;OriginalString : 「ふふ、下だけ裸と言うのも興奮しませんか？」
			PRINTFORMW 「후후, 아래만 알몸이라고 하는 것도 흥분하지 않겠습니까?」
;OriginalString : ムラサは%CALLNAME:MASTER%に見せ付けるように、ゆっくりと속옷を下ろしていった…
			PRINTFORMDW 무라사는 %CALLNAME:MASTER%에 과시하듯이, 천천히 속옷을 내려 갔다…
		;아라이멘도부
		ELSE
;OriginalString : 「ふふ、脱ぎたての染み付き속옷、%CALLNAME:MASTER%に被せてあげようかしら」
			PRINTFORMW 「후후, 벗어 세우고의 배어들어 속옷, %CALLNAME:MASTER%에 씌워 줄까」
;OriginalString : 「そういうの好きでしょう？遠慮しなくてもいいのですよ？」
			PRINTFORMW 「그러한 것 좋아하겠지요? 사양하지 않아도 괜찮은 것이에요?」
		ENDIF
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·말하는 p FIRSTDATUI(4)
;------------------------------------------------
;·이번탈의로 젖가슴이 보이도록(듯이)되었다경우에 표시
;·브라 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
;OriginalString : 
	PRINTL
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:주인님 && FIRSTDATUI(4)
;OriginalString : 「よいしょ…と。　ふふ、どうですか私の가슴？」
		PRINTFORMW 「영차…(와)과. 후후, 어떻습니까 나의 가슴?」
;OriginalString : 「%CALLNAME:MASTER%がよければ触ってもいいですよ？」
		PRINTFORMW 「%CALLNAME:MASTER%는 좋다면 손대어도 괜찮아요?」
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써성기가 보이고 있다
	;=================================================
	IF 구상기입체크(100) && P_DATUICHECK(0) == 1
		;퍼펙트 서로 사모함
		IF KFLAG:700
;OriginalString : 「え？やっぱりおっぱいも見たい？全く仕方ない人ですね…」
			PRINTFORMW 「네? 역시 젖가슴도 보고 싶다? 완전히 어쩔 수 없는 사람이군요…」
;OriginalString : 「なら、가슴もたっぷりとして貰いませんとね？%CALLNAME:MASTER%♪」
			PRINTFORMW 「이라면, 가슴도 충분히로서 받지 않습니다와?%CALLNAME:MASTER%♪」
;OriginalString : 
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이멘도정
		IF ALI(0)
;OriginalString : 「ふふ、下だけでは物발りないですか？」
			PRINTFORMW 「후후, 아래만으로는 어딘지 부족합니까?」
;OriginalString : 「あら、目が釘付けですよ？そんなに가슴見たかったのですか？」
			PRINTFORMW 「어머나, 눈이 못부예요? 그렇게 가슴 보고 싶었습니까?」
		;아라이멘도부
		ELSE
;OriginalString : 「下だけではどうも物발りなさそうね」
			PRINTFORMW 「아래만으로는 아무래도 물건 충분하고 없을 것 같구나」
;OriginalString : 「あらあら、目が釘付けね？そんなに가슴を見たかったのかしら？」
			PRINTFORMW 「대강, 눈이 못부군요? 그렇게 가슴을 보고 싶었던 것일까 해들?」
		ENDIF
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
;OriginalString : 「おっぱいが見たい？いいですよ、はい♪」
		PRINTFORMW 「젖가슴을 보고 싶다? 좋아요, 네♪」
;OriginalString : 미나미츠が손早く衣服を脱ぎ捨てると、大きくは無いが形の良い乳房が姿を現했다。
		PRINTFORMDW 수밀도가 재빠르게 의복을 벗어 던지면(자), 크지는 않지만 형태의 좋은 유방이 모습을 나타냈다.
;OriginalString : 「%CALLNAME:MASTER%にだけですよ？こんな風に見せるのは」
		PRINTFORMW 「%CALLNAME:MASTER%에만이에요? 이런 식으로 보이는 것은」
;OriginalString : 
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「ふふ、そろそろ脱ぎましょうか？」
		PRINTFORMW 「후후, 슬슬 벗을까요?」
;OriginalString : 「あら、目が釘付けですよ？そんなに私の가슴を見たかった？」
		PRINTFORMW 「어머나, 눈이 못부예요? 그렇게 나의 가슴을 보고 싶었다?」
	;아라이멘도부
	ELSE
;OriginalString : 「ふふ、そろそろ脱いであげましょうか？仕方ないわね」
		PRINTFORMW 「후후, 이제 벗어 줄까요? 어쩔 수 없네요」
;OriginalString : 「あらあら、そんなに가슴を見たかったのかしら？」
		PRINTFORMW 「대강, 그렇게 가슴을 보고 싶었던 것일까 해들?」
	ENDIF
;OriginalString : 
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·팬티 보이고 FIRSTDATUI(5)
;------------------------------------------------
;·이번탈의로팬티（캐릭터에 의해드로와즈·트렁크）가 보이도록(듯이)되었다경우에 표시
;·하반신속옷가 없는 캐릭터는 쓰는필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && P_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(5)
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(0) && ONCEDATUI(5)
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·브라 보이고 FIRSTDATUI(6)
;------------------------------------------------
;·이번탈의로 브라（캐릭터에 의해스포츠브라·사라시）가 보이도록(듯이)되었다경우에 표시
;·상반신속옷가 없는 캐릭터는 쓰는필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(6)
	ENDIF
	
	;=================================================
	;조교중첫회
	;=================================================
	IF 구상기입체크(0) && ONCEDATUI(6)
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·전라에 FIRSTDATUI(7)·RANDDATUI(8) ～ (11)
;------------------------------------------------
;·「전라까지 벗었다」만이 아니고, 「지금 벗었으므로전라되었다」라고 하는 의미
;·「겉옷 밖에 남지 않은상태로겉옷를 벗었다」등도 포함된다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && ZENRACHECK(0) && !IS_NOWASSIACTNAME(, "탈의")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·악세서리─마셔 FIRSTDATUI(8)
;------------------------------------------------
;·주로신발안. 그 밖에장갑나소매 등
;·탈이했다중에 접어들지 않는다! 되기 십상인 것으로 이것만은 부위별 텐프레를 준비
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 == 6
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(8)
;OriginalString : 「はい、これも脱ぎましょうね」
		PRINTFORMW 「네, 이것도 벗어요」
;OriginalString : ムラサは%CALLNAME:MASTER%의 신발안に손をかけると一気に脱がしていった。
		PRINTFORMDW 무라사는 %CALLNAME:MASTER%의 양말에 손을 대면(자) 단번에 탈이 해 갔다.
		RETURN 1
	ENDIF
	
	;=================================================
	;전라되었다
	;=================================================
	IF 구상기입체크(100) && ZENRACHECK(1)
		;퍼펙트 서로 사모함
		IF KFLAG:700
;OriginalString : 「はい、これも脱いで脱いで♪」
			PRINTFORMW 「네, 이것도 벗고 벗어♪」
;OriginalString : そう言うと미나미츠は%CALLNAME:MASTER%의 신발안を一気に剥ぎ取った。
			PRINTFORMDW 그렇게 말하면 수밀도는 %CALLNAME:MASTER%의 양말을 단번에 벗겨냈다.
;OriginalString : 「さーて…何処から頂いちゃおうかしら？ね、%CALLNAME:MASTER%？」
			PRINTFORMW 「-라고… 어딘가에서 받아 버리자 까? 그런데,  %CALLNAME:MASTER%?」
			RETURN 1
		ENDIF
		
		;아라이멘도정
		IF ALI(0)
;OriginalString : 「さあ、これも脱ぎましょうね～」
			PRINTFORMW 「자, 이것도 벗어요～」
;OriginalString : 身に付ける物が無くなった%CALLNAME:MASTER%を見てムラサは頷いた。
			PRINTFORMDW 몸에 걸치는 것이 없어졌다%CALLNAME:MASTER%(을)를 봐 무라사는 수긍했다.
;OriginalString : 「これで%CALLNAME:MASTER%も観念してくれますよね？」
			PRINTFORMW 「이것으로 %CALLNAME:MASTER%도 관념해 주는군요?」
		;아라이멘도부
		ELSE
;OriginalString : 「さて、これで%CALLNAME:MASTER%의 着けていた衣服は最後ですよ」
			PRINTFORMW 「그런데, 이것으로 %CALLNAME:MASTER%(이)가 대고 있던 의복은 최후예요」
;OriginalString : 完全に전라となった%CALLNAME:MASTER%を見てムラサは目を細めた。
			PRINTFORMDW 완전하게 전라가 된 %CALLNAME:MASTER%(을)를 봐 무라사는 웃음을 띄웠다.
;OriginalString : 「ふふ、何処から責めてあげましょうか？　楽しみですわ」
			PRINTFORMW 「후후, 어딘가에서 꾸짖어 줄까요? 기다려지네요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
;OriginalString : 「あら、結構汚れていますね%CALLNAME:MASTER%의 신발안」
		PRINTFORMW 「어머나, 상당히 더러워져 있네요%CALLNAME:MASTER%의 양말」
;OriginalString : そう言うと、미나미츠は%CALLNAME:MASTER%의 신발안を一気に剥ぎ取り遠くへ放り投げた。
		PRINTFORMDW 그렇게 말하면, 수밀도는 %CALLNAME:MASTER%의 양말을 단번에 벗겨내 멀리 던져 던졌다.
;OriginalString : 「後で洗っておきますから、今は、ね？」
		PRINTFORMW 「다음에 씻어 두기 때문에, 지금은, 이군요?」
		RETURN 1
	ENDIF
	
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「はい、これも脱ぎましょうね」
		PRINTFORMW 「네, 이것도 벗어요」
;OriginalString : ムラサは%CALLNAME:MASTER%의 신발안に손をかけると一気に剥いだ。
		PRINTFORMDW 무라사는 %CALLNAME:MASTER%의 양말에 손을 대면(자) 단번에 벗겼다.
;OriginalString : 「あら、もしかして服を脱がされる方が良かったかしら？」
		PRINTFORMW 「어머나, 혹시 옷을 탈의하는 편이 좋았는지 해들?」
	;아라이멘도부
	ELSE
;OriginalString : 「はい、これも脱ぎましょうね」
		PRINTFORMW 「네, 이것도 벗어요」
;OriginalString : ムラサは%CALLNAME:MASTER%의 신발안に손をかけると一気に剥いだ。
		PRINTFORMDW 무라사는 %CALLNAME:MASTER%의 양말에 손을 대면(자) 단번에 벗겼다.
;OriginalString : 「服では無くて安心しま했다？　ふふ、メインは後に取っておくものですよ」
		PRINTFORMW 「옷은 아니어서 안심했습니다? 후후, 메인은 뒤에 잡아 두는 것이에요」
	ENDIF
	RETURN 1
ENDIF

;0. 60시대의 「저기 보여라」에 상당. 페니스의 유무로 테두리를 단락짓고 있습니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·고추 보여라 FIRSTDATUI(9)
;------------------------------------------------
;·이번탈의로남성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(9)
;OriginalString : 「さて、そろそろ%CALLNAME:MASTER%의 モノを見せてもらいましょうか」
		PRINTFORMW 「그런데, 이제%CALLNAME:MASTER%의 물건을 볼까요」
;OriginalString : 「おや、少し硬くなっていますね？　…いいのですよ？　もっと솔직になっても」
		PRINTFORMW 「이런, 조금 딱딱해지고 있네요? … 좋은 것이에요? 좀 더 솔직해져도」
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
;OriginalString : 「さてと、%CALLNAME:MASTER%의 おちんちん見せてくださいな」
		PRINTFORMW 「그런데와%CALLNAME:MASTER%의 자지 보여 주세요」
;OriginalString : 「もう準備万端ね…입でする？가슴でする？　それとも…もう入れたい？」
		PRINTFORMW 「이제 준비만단이군요… 입으로 한다? 가슴으로 한다? 그렇지 않으면… 이제 들어가지고 싶다?」
		RETURN 1
	ENDIF
	
	;=================================================
	;호감도 1000 이상으로아라이멘도정
	;=================================================
	IF 구상기입체크(100) && AFFECTION("권애") && ALI(0)
;OriginalString : 「さて、%CALLNAME:MASTER%의 おちんちんの様子は…と」
		PRINTFORMW 「그런데, %CALLNAME:MASTER%의 자지의 모습은…(와)과」
;OriginalString : 「ふふっ、大きくなってますね？　そろそろ弄られたくないですか？」
		PRINTFORMW 「후후, 커지네요? 이제 만지작거려진 구 없습니까?」
		RETURN 1
	ENDIF
	
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「さて、また%CALLNAME:MASTER%의 モノを見せてもらいますか」
		PRINTFORMW 「그런데, 또%CALLNAME:MASTER%의 물건을 봅니까」
;OriginalString : 「身体はこんなに正直なのに…まだ受け入れてもらえないのですか？」
		PRINTFORMW 「신체는 이렇게 정직한데… 아직 받아들여 주지 않습니까?」
	;아라이멘도부
	ELSE
;OriginalString : 「はい、おちんちん見せてね？…あらあら、こんなに大きくしてしまって」
		PRINTFORMW 「네, 자지 보여? … 대강, 이렇게 크게 해 버리며」
;OriginalString : 「참다했다ければしていていいですよ？　私は勝손に弄るだけですから」
		PRINTFORMW 「참고 싶다면 하고 있어 좋아요? 나는 마음대로 만지작거릴 뿐이니까」
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]

오리지날로 기술되어 있지 않은 개소는 comment out 해 둡니다@/L

;0. 60시대의 「저기 보여라」에 상당. 페니스의 유무로 테두리를 단락짓고 있습니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·그대로응 보여라 FIRSTDATUI(10)
;------------------------------------------------
;·이번탈의로 여자성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && ! TALENT:MASTER:남자
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·젖가슴 보여라 FIRSTDATUI(11)
;------------------------------------------------
;·이번탈의로 젖가슴이 보이도록(듯이)되었다경우에 표시
;·브라 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;·죠교대상이남자라고 표시하지 않기 때문에, 남자한정구상에서는 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 3 && ! TALENT:MASTER:남자
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·팬티 보여라 FIRSTDATUI(12)
;------------------------------------------------
;·이번탈의로하반신속옷가 보이도록(듯이)되었다경우에 표시
;·죠교대상가하반신속옷가 없는 캐릭터 한정의구상이면 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·브라 보여라 FIRSTDATUI(13)
;------------------------------------------------
;·이번탈의로 브라상반신속옷가 보이도록(듯이)되었다경우에 표시
;·죠교대상이남자라고 표시하지 않기 때문에, 남자한정구상에서는 쓰는필요가 없다
;·죠교대상가상반신속옷가 없는 캐릭터 한정의구상이면 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 2 && ! TALENT:MASTER:남자
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·전라에 FIRSTDATUI(14)
;------------------------------------------------
;·「전라까지 벗겼다」만이 아니고, 「지금 벗겼으므로전라되었다」라고 하는 의미
;·「겉옷 밖에 남지 않은상태로겉옷를 벗겼다」등도 포함된다
;·「젖가슴 보여라」～「팬티 보여라」를 썼을 경우는 기입하는필요는 없다（상층의 분기에 먹혀져 여기까지 떨어져 오지 않기 때문에）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의 && ZENRACHECK(1)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·뭔가 벗은 FIRSTDATUI(15)
;------------------------------------------------
;·자위·봉사계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:자주탈의 && ! IS_NOWASSIACTNAME(, "탈의")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·뭔가 벗긴 FIRSTDATUI(16)
;------------------------------------------------
;·애무계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의
ENDIF
[SKIPEND]

;========================================================================
;추가 액션구상
;========================================================================
;·죠교자가추가행동를 취했을 때에 범용적으로 표시
;·쓰는 경우, 액션구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
;────────────────────────────────────
;EXTRA1, Ｃ애무
;────────────────────────────────────
;·추가로애무 또는수음를 실시한다
;·죠교대상에페니스가 없는 경우는애무, 어느 경우는수음
;-------------------------------------------------
@KOJO_ACT_EXTRA_K68_1
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_0, 애무 한다
;-------------------------------------------------
;·죠교대상에페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && ! PENIS(MASTER)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_1, 수음 한다
;-------------------------------------------------
;·죠교대상에페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && PENIS(MASTER)
;ENDIF

;────────────────────────────────────
;EXTRA2, Ｖ애무
;────────────────────────────────────
;·추가로 바지애무를 실시한다
;·죠교대상이남자의 경우 발생하지 않는다
;·죠교대상가처녀의 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K68_2
;-------------------------------------------------
;IF 구상기입 체크(0)
;ENDIF

;────────────────────────────────────
;EXTRA3, Ａ애무
;────────────────────────────────────
;·추가로항문애무를 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K68_3
;-------------------------------------------------
;IF 구상기입 체크(0)
;ENDIF

;────────────────────────────────────
;EXTRA4, Ｂ애무
;────────────────────────────────────
;·추가로 유두 고문을 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K68_4
;-------------------------------------------------
;IF 구상기입 체크(0)
;ENDIF

;────────────────────────────────────
;미약등사용구상
;────────────────────────────────────
;·죠교자가미약나로션를 사용했다때에 범용적으로 표시
;·「액션구상」로 기입 체크를=2로했다경우는 표시되지 않는다
;·쓰는 경우, 액션구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
@KOJO_USE_K68
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션/미약동시 사용
;------------------------------------------------
;·주로성교계 커맨드로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && GETBIT(TFLAG:미약등사용구상, 0) && GETBIT(TFLAG:미약등사용구상, 1)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션사용
;------------------------------------------------
;·완구·성교계 커맨드등으로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && GETBIT(TFLAG:미약등사용구상, 0)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;미약사용
;------------------------------------------------
;·애무계 커맨드등으로부터 발생한다
;·키스로 발생했다경우는입옮기기가 되기 (위해)때문에, 그 케이스는키스의 액션구상내에서 처리하는 편이 좋다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GETBIT(TFLAG:미약등사용구상, 1)
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
;OriginalString : 「はい、いつものお薬　…え？こんなの필요無い？」
		PRINTFORMW 「네, 평소의 약… 네? 이런 건 필요 없다?」
;OriginalString : 「そんな事言わないで、私も使うからこれで一緒に溺れましょう？」
		PRINTFORMW 「그런 일 말하지 말고, 나도 사용하기 때문에 이것으로 함께 빠집시다?」
		RETURN 1
	ENDIF
	
	;아라이멘도정
	IF ALI(0)
;OriginalString : 「はいこれ、気持ち良くなるお薬使ってあげる」
		PRINTFORMW 「네이것, 기분 좋아지는 약 사용해 준다」
;OriginalString : 「ヤゴコロとか言う薬師が作ったすぐ効くお薬よ…あ、もう効いてきたようね」
		PRINTFORMW 「수채 코로라든가 말하는 약사가 만든 곧 효과가 있는 약이야…아, 벌써 효과가 있어 온 듯」
	;아라이멘도부
	ELSE
;OriginalString : 「はいこれ、何されても良くなるお薬使ってあげる…いらない？」
		PRINTFORMW 「네이것, 무엇 되어도 좋아지는 약 사용해 준다… 필요없다?」
;OriginalString : 「だーめ、これで이성飛んでいる%CALLNAME:MASTER%を見たいの」
		PRINTFORMW 「이다-째, 이것으로 이성 날고 있는 %CALLNAME:MASTER%(을)를 보고 싶은거야」
	ENDIF
	RETURN 1
ENDIF
