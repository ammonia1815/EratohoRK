;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	무라사구상 죠교개시이벤트구상파일
;
;	죠교개시이벤트구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV11, 죠교개시구상
;========================================================================
;·대체로의 경우, 구상작자가 우선 생각하는 것은 "주인"으로서의구상라고 생각되므로
;·@KOJO_MS_EVENT로서 TALENT:주인님인 경우로 한정해 버립니다
;·이벤트관련은구상주의 입장에 의해 정합성이 손상될 가능성이 있으므로 이러한 처치로 섬했다
;·구상내의 모든 개소에서구상함수에 의한 같은 배타적 연산은 가능했다거나 합니다
;-------------------------------------------------
;  소환되어죠교를 맡겨지는 경우의구상를 쓴다면
;  호출을@KOJO_EVENT_KX1_11으로서 배타를 실시하지 않고, TALENT:주인님와 그 외로 분기를 마련한다
;  EVENT1, OP(주인인 경우에 밖에 처리되지 않는다)로 KFLAG에의 대입을 실시해, 분기를 마련한다
;  혹은, @KOJO_AS_EVENT_KX1_11으로서 별파일에 분리해 버리면 좋을 것입니다
;-------------------------------------------------
;우선 분기를 마련해 있습니다만, EVENT11범위내의 분기의 증감은자유로 할 수 있습니다
;COMMON_GETTER_CHARA.ERB내, @AFFECTION에는 여기에 없는 것도 정의되어 있거나 합니다
;다만, 조건의 느슨한 분기를 상층에 가지고 오면 하위의 분기가 모두 죽어구상가 되므로
;우선도에는 충분히 주의해 주세요
;-------------------------------------------------
@KOJO_MS_EVENT_K68_11(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 통상/1, 죠교자의 피로에 의한 중지/2, 죠교대상의 피로에 의한 중지)
;-------------------------------------------------

;구상주가주인님전제의 내용인 것으로, MS로 한정을 걸어 버립니다
;구상호출의 시점에서구상주가주인님가 아니면 배타가 됩니다@/L

;────────────────────────────────────
;죠교첫회·10회째
;────────────────────────────────────
;·죠교첫회, 및 10회째에 표시되는구상
;·CFLAG:죠교횟수로횟수는 어떻게라도 참조할 수 있습니다만, 너무 하면 다른 조건을 탕진할 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
;처음·구상주가주인님
;근성 상냥하기 때문에 기본적으로는죄책감 느끼면서죠교 하는 느낌. 그렇지만 아라부되었다등 조금새드 들어 올지도
;표시 순번이 이상해지므로 이식해 여기는봉인. ←라고 써 있던 것입니다만 문제 없는 것이라고 판단했다의 것으로 표시해 버립니다@/L
;-------------------------------------------------
IF 구상기입체크(100) && CFLAG:죠교횟수 == 1
	PRINTL 
	PRINTFORMW 「기분은 어떻게든 해들? 배멀미 하지 않는다?」
	PRINTFORMDW 그런 일 아무래도 좋으니까 해방해
	PRINTFORMW 「유감이다 하지만 그것은 할 수 없어요」
	PRINTFORMW 「뭐, 기분이 좋은 일 할 뿐(만큼)이니까 편하게 해?」
	PRINTFORMW 「다만, %CALLNAME:MASTER%의 반응 하기에 따라  조금 난폭한 일도 해 버릴지도, 군요?」
	PRINTFORMW 「이런 외관에서도 요괴이고」
	PRINTL
	;유령 여객선의 시공을 넘긴 여행
	CALL BGM_CUTIN(CSTR:곡2)
	PRINTFORMW 「1월도 하면 내 주기 때문에 …그 때까지 나에게 빠져 주세요군요」
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교 10회째
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && CFLAG:죠교횟수 == 10 && !ARG
	;아라이멘도정
	IF ALI(0)
		PRINTL 
		PRINTFORMW 「기분은 어떻게든 해들?」
		PRINTFORMDW 문을 열어 무라사가 방에 들어 왔다.
		PRINTFORMDL 기분 좋은 것, 없는 것 알지요?
		PRINTFORMDW 그렇게 말하면, 무라사는 조금 숙였다.
		PRINTL 
		PRINTFORMW 「…%CALLNAME:MASTER%에는 나쁘다고 생각하고 있어요」
		PRINTFORMDL 그렇지만 해방은 해 주지 않지요
		PRINTFORMW 「…면없음 벼, 이렇게 하지 않으면 나는 살아갈 수 없는거야」
		PRINTFORML 「유령이 산다 라고 하는 것도 이상한 이야기이지만」
		PRINTFORML 「 나의 일은 싫어 해 주어 상관없어요, 그렇지만」
		PRINTL 
		PRINTFORMW 「부탁이니까 내가 주는 쾌감에는 빠져, 군요…」
		PRINTL 
	;아라이멘도부
	ELSE
		PRINTL 
		PRINTFORMW 「기분은 어떻게든 해들?」
		PRINTFORMDW 문을 열어 무라사가 방에 들어 왔다.
		PRINTFORMDL 기분 좋은 것, 없는 것 알지요?
		PRINTFORMDW 그렇게 말하면, 무라사는 조금 숙였다.
		PRINTL 
		PRINTFORMW 「…뭐, 그래요」
		PRINTFORMDL 얼굴을 일으킨 무라사의 얼굴에는 미소가 흘러넘치고 있었다.
		PRINTFORMW 「왠지 해들, 괴롭히고 싶어져.%CALLNAME:MASTER%의 일」
		PRINTFORMW 「이런 나라도 요괴였다고 말하는 일인가 해들 」
		PRINTFORMDW 심한 일은 하지 않는다고 말하지 않았다?
		PRINTL 
		PRINTFORMW 「응, 그 후에 말했어요? 나」
		PRINTFORMW 「%CALLNAME:MASTER%나름으로 조금 난폭한 일도 해 버릴지도 몰라? 라고」
		PRINTFORMDW 무라사의 웃는 얼굴의 뒤에 있는 광기가 보일듯 말듯, %CALLNAME:MASTER%(은)는 꿀꺽 군침이 돌았다.
		PRINTL 
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]
;────────────────────────────────────
;조교중지 ARG:1 ARG:2
;────────────────────────────────────
;CFLAG:소모 > 5혹은 CFLAG:MASTER:소모 > 5를 조건으로서 불려 가는 개소
;이 조건으로부터 빠졌을 경우는통상의죠교개시구상가 불린다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;피로로죠교가 중지가 되는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ARG > 0
	;=================================================
	;피곤한 것은구상주(TARGET)
	;=================================================
	IF 구상기입 체크(0) && ARG == 1
	ENDIF
	
	;=================================================
	;피곤한 것은죠교대상(MASTER)
	;=================================================
	IF 구상기입 체크(0) && ARG == 2
	ENDIF
ENDIF
[SKIPEND]

;────────────────────────────────────
;그 외·호감도등에 의한 분기
;────────────────────────────────────
;·현재의 ACTABLE나 COMABLE의 사양적으로, 진전도를 측정하려면 (사실상)호감도를 기준으로 할 수 밖에 없다고 생각된다
;·의존도는@COM로 대입되고 참조할 수 있을 뿐으로, 본체측의 거동에는 아무런영향를 주지 않았다
;·향후의손 넣고 여하에 따라서는 용도도 있을지도? 그렇다고 하는 곳
;-------------------------------------------------

;-------------------------------------------------
;퍼펙트 서로 사모함（러브 러브모드개시）（호의 3000 이상·호감도 5000 이상  반항각인없음） FIRSTEVENT(0) KFLAG:700
;-------------------------------------------------
;· 실은 벌써 서로를 서로 좋아하고 있는＆SM계등으로반항 붙지 않았다=오로지 러브 러브죠교와 판단
;하고 있는 한계라면 보통, 호의가 3 k가 되는 무렵에는호감도 10k 근처는 가고 있다고 생각하기 때문에 무라사로부터의 고백에.
;응, 미나미츠와 이름으로 부르게 하고 싶었던 것 뿐이다. 이것과 OP의 마지막 일문을 생각해 냈을 때로부터이 구상은 시작되었다.
;Rev로 설마의 고백이벤트. 좋은 것인지? 뭐 좋아. 자중은 내던지는 것.
;2회째 이후  정말로⑨풀입니다 아무래도（ry
;러브 러브모드작성에 해당되어, 도 고담구상의플래그를 참고에 시켜포궖했다（뭔가 있으면 안 되기 때문에 아무것도 없는 것 같은 CFLAG700번대 사용）
;-------------------------------------------------
;서로 사모함（비러브 러브모드（호의 3000 이상·호감도 5000 이상  반항각인 있어） FIRSTEVENT(0)
;-------------------------------------------------
;· 실은 벌써 서로를 서로 좋아하고 있는, 하지만 뭔가의 실수로 심한 일도 해 버리고 있는판.
;현상반항 지울 수 없기 때문에 만들어 보았다. 반항를 지울 수 있게 되면 삭제 혹은 다른 문장에 갈아끼우기,.
;지금까지의 일은 없었던 것으로 하지만, 이제(벌써) SM계는 용서, 라고 하는 느낌.
;반항 10라든지 붙은 다음에도호의 3k 갈까? 그렇다면 아마 굉장한 위화감 있을 것이다이것. 뭐, 문제 있는 것 같으면 그 때에 대처.
;문장적으로 퍼펙트와 큰 차이는 없지만미나미츠라고 부르게 해 주지 않는 것이 최대의 차이. 후, 커맨드문 따위의 변경이 지금 단계 일절 없다.
;에서도 2회째 이후는⑨풀.
;-------------------------------------------------
IF 구상기입체크(100) && FAVOR(3000, 5000)
	;오리지날의 조건 설정을 반영시킨다 때문에@FAVOR를 사용해 둡니다
	;이 단계까지 진전하고 있는 경우, AFFECTION("상사상애")에서도 내부의 수치적으로는 같은 거동을 재현 가능한 것이긴 하다고 생각합니다만, 확실하다고는 단언할 수 없기 때문에@/L
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(0)
		;반항각인없음			첫회 분기는 통합해반항각인로 나누어 버립니다@/L
		IF !MARK:반항각인
			PRINTL
			PRINTL
			PRINTFORMW 「………」
			PRINTFORMDW 문을 열어 무라사가 방에 들어 왔다. 하지만, 어딘가 모습이 이상하다.
			PRINTL 
			PRINTFORMW 「저, %CALLNAME:MASTER%…」
			PRINTFORMW 「지금까지 미안해요」
			PRINTFORMDW 갑자기 사과해져 버렸다.
			PRINTL 
			PRINTFORMW 「화내고 있지요, 나의 제멋대로인 사정으로 휘두르며」
			PRINTFORMW 「갑자기 감금되어, 그, 음란한 일 가득 되며」
			PRINTFORMW 「무슨 말하고 있어 진한 개, 라고 생각하겠지요지만」
;OriginalString : 
			PRINTW 
			PRINTFORMW 「정말 좋아합니다, %CALLNAME:MASTER%」
			PRINTL 
			PRINTL 
;OriginalString : 
			PRINTW 
;OriginalString : 
			PRINTW 
			PRINTFORMW 「……응, 말할 수 있었다」
;OriginalString : 
			PRINTW 
			PRINTFORMW 「미안해요, 제멋대로인 일만 말하며」
			PRINTFORMDW 그렇게 말하면서, 무라사는 방의 문을 열었다.
;OriginalString : 
			PRINTW 
			PRINTFORMW 「 이제%CALLNAME:MASTER%(은)는 자유입니다. 할 수 있을 수 있는 한계의 금품도 밖에 놓여져 있기 때문에 가지고 돌아가 주세요」
			PRINTFORMW 「정말로 미안해요, 지금까지 고마워요 어좌 있었습니다」
			PRINTFORMDW 무라사는 조용히 인사를 했다. 정말로%CALLNAME:MASTER%(을)를 해방할 생각의 같다…
			PRINTL 
;OriginalString : 
			PRINTW 
;OriginalString : 
			PRINTW 
;OriginalString : 
			PRINTW 
			PRINTFORMW 「…… 네?」
			PRINTFORMW 「끼리… 라고?」
			PRINTFORMDL 갑자기 꼭 껴안을 수 있어 곤혹하고 있는 무라사에, 자신도 무라사의 일을 좋아한다고 전했다
			PRINTFORMW 「… 정말로? 지금까지 심한 일을 해 온, 이런 나인데?」
			PRINTFORMDW 어쩔 수 없는 것이었던 것일까? 이제 되었어, 끝난 일이다.
;OriginalString : 
			PRINTW 
			PRINTFORMW 「…」
			PRINTFORMW 「고마워요… 미안, 없음… 아…응」
			PRINTFORMDW 양손으로 얼굴을 눌러 쓰러져 울고 주저앉아 붐빈 무라사를 %CALLNAME:MASTER%(은)는 위로부터 꼭 껴안아
			PRINTFORMDL 무라사가 울어 그칠 때까지 위로했다…
			PRINTL 
			PRINTL 
;OriginalString : 
			PRINTW 
;OriginalString :  
			PRINTW  
			PRINTFORML 「응, 이제 괜찮아」
			PRINTFORMDL 울어 붓게 한 눈을 켜면서, 무라사는 %CALLNAME:MASTER%의 분을 향해 미소지었다
			PRINTFORMW 「네에에… 지금부터 어떻게 하지?」
			PRINTFORMDL 지금까지 대로, 무라사의 하고 싶게 하면 좋아.
			PRINTFORMW 「그렇게?」 「… 응, 정말로 고마워요」
			PRINTL 
			PRINTFORMW 「그래서, 그, 1개(살)만 부탁이 있어」
			PRINTFORMW 「네와…」
			PRINTFORMW 「지금부터는 수밀도는, 이름으로 불렀으면 좋겠다…」
			PRINTFORMDW 알았어. …수밀도.
			PRINTFORMDL 그렇게 불리면(자), 수밀도는 기쁜듯이 %CALLNAME:MASTER%에 안아 붙었다
			PRINTL 
			PRINTFORMW 「앞으로도 쭉, 함께 빠지자…」
;OriginalString : 
			PRINTW 
			KFLAG:700 = 1
		;반항각인 있어
		ELSE
			PRINTL 
			PRINTFORMW 
			PRINTFORMW 「………」
			PRINTFORMDW 문을 열어 무라사가 방에 들어 왔다. 하지만, 어딘가 모습이 이상하다.
			PRINTL 
			PRINTFORMW 「저, %CALLNAME:MASTER%…」
			PRINTFORMW 「지금까지 미안해요」
			PRINTFORMDW 갑자기 사과해져 버렸다.
			PRINTL 
			PRINTFORMW 「화내고 있지요, 나의 제멋대로인 사정으로 휘두르며」
			PRINTFORMW 「갑자기 감금되어, 그, 음란한 일 가득 되며」
			PRINTFORMW 「무슨 말하고 있어 진한 개, 라고 생각하겠지요지만」
;OriginalString : 
			PRINTW 
			PRINTFORMW 「정말 좋아합니다, %CALLNAME:MASTER%」
			PRINTL 
			PRINTL 
;OriginalString : 
			PRINTW 
;OriginalString : 
			PRINTW 
			PRINTFORMW 「……응, 말할 수 있었다」
;OriginalString : 
			PRINTW 
			PRINTFORMW 「미안해요, 제멋대로인 일만 말하며」
			PRINTFORMDW 그렇게 말하면서, 무라사는 방의 문을 열었다.
;OriginalString : 
			PRINTW 
			PRINTFORMW 「 이제%CALLNAME:MASTER%(은)는 자유입니다. 할 수 있을 수 있는 한계의 금품도 밖에 놓여져 있기 때문에 가지고 돌아가 주세요」
			PRINTFORMW 「정말로 미안해요, 지금까지 고마워요 어좌 있었습니다」
			PRINTFORMDW 무라사는 조용히 인사를 했다. 정말로%CALLNAME:MASTER%(을)를 해방할 생각의 같다…
			PRINTL 
;OriginalString : 
			PRINTW 
;OriginalString : 
			PRINTW 
;OriginalString : 
			PRINTW 
			PRINTFORMW 「…… 네?」
			PRINTFORMW 「끼리… 라고?」
			PRINTFORMDL 갑자기 꼭 껴안을 수 있어 곤혹하고 있는 무라사에, 자신도 무라사의 일을 좋아한다고 전했다
			PRINTFORMW 「… 정말로? 지금까지 심한 일을 해 온, 이런 나인데?」
			PRINTFORMDW 어쩔 수 없는 것이었던 것일까? 이제 되었어, 끝난 일이다.
;OriginalString : 
			PRINTW 
			PRINTFORMW 「…」
			PRINTFORMW 「고마워요… 미안, 없음… 아…응」
			PRINTFORMDW 양손으로 얼굴을 눌러 쓰러져 울고 주저앉아 붐빈 무라사를 %CALLNAME:MASTER%(은)는 위로부터 꼭 껴안아
			PRINTFORMDL 무라사가 울어 그칠 때까지 위로했다…
			PRINTL 
			PRINTL 
;OriginalString : 
			PRINTW 
;OriginalString :  
			PRINTW  
			PRINTFORML 「응, 이제 괜찮아」
			PRINTFORMDW 울어 붓게 한 눈을 켜면서, 무라사는 %CALLNAME:MASTER%의 분을 향해 미소지었다
			PRINTFORMW 「네에에… 지금부터 어떻게 할까요?」
			PRINTFORMDL 지금까지 대로, 무라사의 하고 싶게 하면 좋아.
			PRINTFORMDW 다만, 아픈 일이라든지는 그다지해 주었으면 하지 않는데.
			PRINTFORMW 「, 역시 싫었지요. 미안해요」
			PRINTFORMDL 뭐, 견딜 수 있는 범위라면 괜찮은가.
			PRINTFORMW 「응…조심하네요」
			PRINTL 
			PRINTFORML 「앞으로도 쭉, 함께 빠지자…」
			PRINTL 
			KFLAG:701 = 1
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(100)
		;반항각인없음
		IF !MARK:반항각인
			PRINTL 
			PRINTFORMW 「후후, %CALLNAME:MASTER%♪」
			PRINTFORMDL 문을 열어 수밀도가 방에 들어 왔다. 그와 동시에 %CALLNAME:MASTER%에 안아 따라 왔다
;OriginalString : 
			PRINTW 
			PRINTFORMW 「가득%CALLNAME:MASTER%의 일, 빠지게 해 주기 때문에」
			PRINTFORML 「%CALLNAME:MASTER%도 나의 일, 빠지게 해…♪」
;OriginalString : 
			PRINTW 
		;반항각인 있어
		ELSE
			PRINTL 
			PRINTFORML 「후후, %CALLNAME:MASTER%♪」
			PRINTFORMDW 문을 열어 무라사가 방에 들어 왔다. 그와 동시에 %CALLNAME:MASTER%에 안아 따라 왔다
			PRINTL 
			PRINTFORMW 「가득%CALLNAME:MASTER%의 일, 빠지게 해 주기 때문에」
			PRINTFORMW 「%CALLNAME:MASTER%도 나의 일, 빠지게 해…♪」
			PRINTL 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;오리지날에서는（익애）가（죠교완료）보다 상층에 배치되고 있습니다만
;조건을 채우는 것은（익애）가 먼저 된다고 생각되어 그렇게 되면（죠교완료）는 사실상 죽어 분기가 되어 버리므로, 바꿔 넣어 버립니다@/L

;-------------------------------------------------
;·죠교대상가구상 주로 이제(벌써) 헤롱헤롱
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교완료（호의 2000 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상가구상 주로 이제(벌써) 헤롱헤롱
;아마 이 시점에서미나미츠의호감도는 그 이상일테니까
;정은 「당신」가 자신을 좋아하게 되어 있는지 어떤지 알지 않아 안타까운, 그런 느낌으로.
;부는, 역시새드틱, 그렇지만 좋아하게 되어 있기 때문에 조금 반성.
;-------------------------------------------------
IF 구상기입체크(100) && AFFECTION("죠교완료")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(10)
		;아라이멘도정
		IF ALI(0)
			PRINTL 
			PRINTFORMW 「그런데, 시작할까요?」
			PRINTFORMDW 그렇게 말한 무라사에 %CALLNAME:MASTER%(은)는 미소지어 걸쳤다…
			PRINTFORMW  （어? 어쩐지 대단히 기쁠 것 같다）
			PRINTFORML  （설마, 나의 일은 좋아하게 되는 것 없지요）
			PRINTFORML  （그렇다고 해도 기분이 좋은 일을 하고 싶은 것뿐, 이지요）
			PRINTFORML  （기대는, 할 뿐(만큼) 쓸데없지…）
			PRINTL 
			PRINTFORMW 「그러면, 함께 빠집시다…」
			PRINTL 
		;아라이멘도부
		ELSE
			PRINTL 
			PRINTFORMW 「그런데, 시작할까요?」
			PRINTFORMDW 그렇게 말한 무라사에 %CALLNAME:MASTER%(은)는 미소지어 걸쳤다…
			PRINTFORMW 「어머나, 그렇게 기쁜 것일까? 심한 일 될 뿐(만큼)인데?」
			PRINTFORML  （… 그렇지만, 그러한의는 나군요）
			PRINTFORML  （어디선가 길을, 잘못했던가）
			PRINTL 
			PRINTFORMW 「그러면, 빠지게 해 줍시다」
			PRINTL 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(100)
		;아라이멘도정
		IF ALI(0)
			PRINTL 
			PRINTFORMW  （응, 어떻게일까）
			PRINTFORMW  （나의 일, 어떻게 생각하고 있는 것일까. 미움받지는 않다고 생각하지만）
			PRINTL 
			PRINTFORMW  （신경이 쓰여 어쩔 수 없는데…）
			PRINTL 
		;아라이멘도부
		ELSE
			PRINTL 
			PRINTFORML 「자, 어떤 일을 해 주었으면 한 것일까?」
			PRINTFORML 「대강, 그렇게 기뻐해 어쩔 수 없네요」
			PRINTFORMW  （뭐, 이제 와서 어쩔 수 없어요, 나도）
			PRINTL 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·죠교자가죠교대상에 빠져 버려, 깊기도 하고 도착했다 사랑을 따르고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;익애（호감도 2000 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교자가죠교대상에 빠져 버려, 깊기도 하고 도착했다 사랑을 따르고 있다
;정은 좋아하는 것을 자각했다지만죄책감 충분히로 「당신」는 자신의 일 따위 보지 않는다고 생각하고 있다.
;부는Ｓ에 눈을 떠 버린, 적인.
;-------------------------------------------------
IF 구상기입체크(100) && AFFECTION("익애")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(20)
		;아라이멘도정
		IF ALI(0)
			PRINTL 
			PRINTFORMW  （… 어떻게 하지）
			PRINTFORMW  （역시 나,  %CALLNAME:MASTER%의 일을 좋아해 보고 싶다…）
			PRINTL 
			PRINTFORMW  （그렇지만, 이제 와서…(이)군요）
			PRINTFORMW  （이런 일 하고 있는 나를, 쾌락 빼고를 좋아하게 되어 주는 것 없지요）
			PRINTFORML  （적어도,  %CALLNAME:MASTER%는 없게 된다）
			PRINTFORMW  （그 때까지, 가능한 한%CALLNAME:MASTER%를 느끼고 있자…）
			PRINTL 
		;아라이멘도부
		ELSE
			PRINTL 
			PRINTFORMW  （… 어떻게 하지）
			PRINTFORMW  （%CALLNAME:MASTER%의 일이 머리로부터 멀어지지 않아요）
			PRINTL 
			PRINTFORMW  （저, 아픔을 견디는 모습, 쾌감을 견디는 모습, 그리고 견디지 못하고 허가를 청하는 모습…）
			PRINTFORMW  （언제까지나 해방하지 않고 놓아둘까… 라니）
			PRINTL 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(100)
		;아라이멘도정
		IF ALI(0)
			PRINTL 
			PRINTFORMW  （는… 어떻게 하지）
			PRINTFORMW  （%CALLNAME:MASTER%(와)과 떨어지고 싶지 않다…）
			PRINTL 
			PRINTFORML  （그렇지만, 안되네요）
			PRINTFORMW  （약속인거야, 군요…）
			PRINTL 
		;아라이멘도부
		ELSE
			PRINTL 
			PRINTFORMW  （… 아, 역시 좋아요）
			PRINTFORML  （%CALLNAME:MASTER%의 모습을 보는 것만으로 오싹오싹 해요…）
			PRINTL 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;-------------------------------------------------
;·죠교대상가구상 주로죠교 되는 것을 행복하게 느끼고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;죠교가 끝난 상태（호의 1000 이상
;━━━━━━━━━━━━━━━━━━━━━━━━
;·죠교대상가구상 주로죠교 되는 것을 행복하게 느끼고 있다
;좋은 것이 생각해내지 못하기 때문에 보류（500→2000으로 단번에 가도 그렇게 문제 없을까라고도 생각하고 있다. 오히려 1500위로 통일해야할 것인가?
;-------------------------------------------------
IF 구상기입 체크(0) && AFFECTION("죠교가 끝난 상태")
	;=================================================
	;처음
	;=================================================
	IF 구상기입 체크(0) && FIRSTEVENT(30)
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입 체크(0)
	ENDIF
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;불구대천（호의-500 이하）
;━━━━━━━━━━━━━━━━━━━━━━━━
;·거듭되는 심한 처사에, 마음속으로부터죠교자를 미워하고 있다
;·확실히아라이멘도는 부지요
;있을 수 없는 생각은 들지만 일단 묻어, 과연 여기까지 오면Ｓ일직선일 것이다
;에서도 이 상황으로부터 일전, 호의+가 되는 일은 있을까
;-------------------------------------------------
IF 구상기입체크(100) && AFFECTION("불구대천")
	;처음
	IF FIRSTEVENT(100)
		PRINTL 
		PRINTFORMW 「어머나, 그렇게 예없어도」
		PRINTFORMDL 그렇게 말한 무라사를 눈으로 죽이는것 같이, %CALLNAME:MASTER%(은)는 무라사의 얼굴을 예 찾아냈다…
		PRINTFORML 「무섭네요, 이것은 어려운 징계나무가 필요할까」
		PRINTFORMDW 언제의 무렵부터 이렇게 된 것일까?
		PRINTFORMDL %CALLNAME:MASTER%(은)는 만났을 무렵의 무라사를
		PRINTFORMDW 이미 생각해 낼 수가 없게 되고 있었다…
	;2회째 이후
	ELSE
		PRINTL 
		PRINTFORMW 「자, 즐거운 조교의 시간이에요」
		PRINTFORMDL 이미, 무슨 말을 해도 헛됨일 것이다…
		PRINTFORMDL 상대는 요괴, 넘어뜨리는 일 등 무리한 이야기. 하물며 여기는 하늘 위. 도망치는 것조차 들어맞지 않는다.
		PRINTFORMDL 어차피 도망가지 않으면 차라리 망가져 버릴까, 그 쪽이 편해질 것 같다
		PRINTFORMDW %CALLNAME:MASTER%(은)는 뇌리에 그런 일을 마음에 그리는 중, 지옥의 고통은 계속되어 간다…
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]
;-------------------------------------------------
;·죠교자에 대해 혐오감을 안고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;혐오（호의-100 미만）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("혐오")
	;=================================================
	;처음
	;=================================================
	IF 구상기입 체크(0) && FIRSTEVENT(40)
	ENDIF
		
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입 체크(0)
	ENDIF
ENDIF

;-------------------------------------------------
;·마음에 상처를 만들어져주인님의 모습을 보는 것만으로 무서워해 버린다
;·트라우마의 종은 「%ACTSTR(CFLAG:MASTER:8)%」로 표시할 수 있다
;  예）PRINTFORMW 「이제(벌써)%ACTSTR(CFLAG:MASTER:트라우마)%는 싫습니다 라고?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;공포（호의 200 미만·트라우마Lv3 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("공포")
	;=================================================
	;처음
	;=================================================
	IF 구상기입 체크(0) && FIRSTEVENT(50)
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입 체크(0)
	ENDIF
ENDIF

;-------------------------------------------------
;·마음을 열지 않는노예에 초조해지는주인님
;━━━━━━━━━━━━━━━━━━━━━━━━
;반항（호의 0 이하·반항각인Lv3 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && AFFECTION("반항")
	;=================================================
	;처음
	;=================================================
	IF 구상기입 체크(0) && FIRSTEVENT(60)
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입 체크(0)
	ENDIF
ENDIF
[SKIPEND]

;-------------------------------------------------
;·죠교 되는 것도 오히려 좋아하게 되었는지도 모른다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;눈을 뜨고（호의 500 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
;이 근처라면 아직 방향이 미확정인 생각도 들므로 우선 슬쩍.
;이 근처에서 장장 나오면 장황한 것 같은 것으로, 2회째 이후는 없음으로.
;하드르 낮기 때문에 500인 채라면 호감이 꽤 올라 버리는 것은 아닌지? 라고 생각 300에. 부적당 나오면 수정.
;-------------------------------------------------
IF 구상기입체크(100) && AFFECTION("눈을 뜸")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(70)
		;아라이멘도정
		IF ALI(0)
			PRINTL 
			PRINTFORMW 「응, 어떻습니까?」
			PRINTFORML 「나에게 빠져 주는 것처럼 되어 왔습니다?」
			PRINTFORML 「빨리 그렇게 되어 주면 기쁜 것이지만」
			PRINTFORMDW %CALLNAME:MASTER%(은)는 무라사의 말을 반드시 부정할 수 없게 되어 와 있었다…
			PRINTL 
		;아라이멘도부
		ELSE
			PRINTL 
			PRINTFORMW 「이번은, 아픈 것과 기분이 좋은 것 어느 쪽이 좋을 것입니다?」
			PRINTFORMDL 아픈 것이 앞인가!  %CALLNAME:MASTER%(은)는 그렇게 특코미를 넣으려고 했지만
			PRINTFORMDL 뭐 그런데도 좋을까 생각해, 말하는 것을 그만두었다
			PRINTFORMW 「어떻게든 했습니다?」
			PRINTL 
		ENDIF
		RETURN 1
	ENDIF
	
	;처음 이외는 하층의 분기까지 떨어져 처리되어도 좋은 것인지, 그렇지 않으면 RESULT 돌려주어 아무것도 표시되지 않는 것이 바람직한 것인지 고민한 것입니다만
	;오리지날 그대로라고 하는 일에 섬했다@/L
	
	;=================================================
	;처음 이외
	;=================================================
	;IF 구상기입 체크(0)
	;ENDIF
ENDIF

;-------------------------------------------------
;·정중하게죠교를 진행되어, 생각했던 것보다 싫지 않을지도…
;━━━━━━━━━━━━━━━━━━━━━━━━
;순응（호의 100 이상·호감도 100 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
;이것도횟수 많은 것 같고 장황해질 것 같은 것으로 2회째 이후는 패스.
;-------------------------------------------------
IF 구상기입체크(100) && AFFECTION("순응")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(80)
		;아라이멘도정
		IF ALI(0)
			PRINTL 
			PRINTFORMW 「그런데, 이번도 빨리 빠져 주도록(듯이) 노력하네요」
			PRINTFORMDW %CALLNAME:MASTER%(은)는 지금의 환경에 위화감을 기억하지 않게 되어 지고 있었다…
;OriginalString : 
			PRINTW
		;아라이멘도부
		ELSE
			PRINTL 
			PRINTFORMW 「그런데, 이번은 어떤 일을 할까요」
			PRINTFORML 「기분이 좋은 일이 좋은 것인지? 그렇지 않으면 아픈 것이 좋은 것인지?」
			PRINTFORMDW %CALLNAME:MASTER%(은)는 지금의 환경에 위화감을 기억하지 않게 되어 지고 있었다…
;OriginalString : 
			PRINTW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	;IF 구상기입 체크(0)
	;ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;흥미（호감도 500 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
;·사랑스러운노예가손에 들어가주인님 만열
;·호의는 마이나스일 가능성도 높다
;-------------------------------------------------
;장황한 것 같은 것으로 2회째는（ry
;정은죄책감로 흔들어지는 느낌. 그렇지만 아직 자신의 일이 강하다.
;부는 상당히 자기 중풍미.
;-------------------------------------------------
IF 구상기입체크(100) && AFFECTION("흥미")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(110)
		;아라이멘도정
		IF ALI(0)
			PRINTL 
			PRINTFORML  （%CALLNAME:MASTER%도 불쌍하지, 나같은 것에(이) 잡혀）
			PRINTFORML  （그렇지만 미안해요. 나는 아직, 사라지고 싶지 않은거야）
			PRINTFORML  （그 만큼 기분 좋게 해 올리기 때문에, 허락해）
			PRINTFORMDW 무라사는 마음 속에서 %CALLNAME:MASTER%에 사과했다
;OriginalString : 
			PRINTW 
		;아라이멘도부
		ELSE
			PRINTL 
			PRINTFORMW  （%CALLNAME:MASTER%도 불쌍하지, 나같은 것에(이) 잡혀）
			PRINTFORML  （좋아해 마음대로 되어, 싫은 일도 되어）
			PRINTFORML  （그렇지만 미안해요. 나는 아직, 사라지고 싶지 않은거야）
			PRINTFORMW  （비록%CALLNAME:MASTER%는 아무리 싫어도…）
			PRINTL 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	;IF 구상기입 체크(0)
	;ENDIF
ENDIF

;-------------------------------------------------
;·요컨데, 다른 조건에 히트 하지 않았던 경우에 떨어지는 개소
;·만일 다른 분기를 모두방치했다경우, 이 개소만 써 두면 뭔가는 표시된다든가, 그런 장소입니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;초반（조건 없음）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(90)
		;아라이멘도정
		IF ALI(0)
			PRINTFORMW 「기분은 어떻게든 해들? 배멀미 하지 않는다?」
			PRINTFORMDL 그런 일 아무래도 좋으니까 해방해 주세요
			PRINTFORMW 「유감이다 그러나 그것은 할 수 없어요」
			PRINTFORML 「뭐, 기분이 좋은 일 할 뿐(만큼)이니까 편하게 해?」
			PRINTFORML 「다만, %CALLNAME:MASTER%의 반응 하기에 따라  조금 난폭한 일도 해 버릴지도 몰라?」
			PRINTFORMW 「이런 외관에서도 요괴이기 때문에」
			PRINTL 
			PRINTFORMW 「1월도 하면 개방 해 주기 때문에,…그 때까지 나에게 빠져 주세요군요」
		;아라이멘도부
		ELSE
			PRINTFORMW 「그러면, 잘 부탁드려요」
			PRINTFORMDL 드디어, 이 요괴의 조교등이 시작되는 것 같다
			PRINTFORMDL 도대체 어떤 심한 일을 하시는 것이든지…
			PRINTFORMW 「가능한 한, 기분 좋게 할 수 있도록(듯이)는 할테니까」
			PRINTFORMDW %CALLNAME:MASTER%(은)는 무라사의 말을 들으면서, 자신의 몸의 불행을 한탄했다…
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;처음 이외
	;=================================================
	IF 구상기입체크(100)
		PRINTL
		PRINTFORML 「자, 시작할까요」
		PRINTFORMDW %CALLNAME:MASTER%(은)는 \@ !ALI(0) ? 이런 아픈 일 # \@그만두어 주지 않겠는가? 이렇게 말했지만 무시되었다…
		RETURN 1
	ENDIF
ENDIF
