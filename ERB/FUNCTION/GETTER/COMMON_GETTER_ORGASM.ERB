;==================================================
;
;	절정系参照関数置き場
;	
;==================================================
;-------------------------------------------------
;関数名:_SHOOTING
;概　要:사정種から文字列を취득する関数
;引　数:ARG…(TCVAR:사정)
;戻り値:사정種を表す文字列
;備　考:式中関数。内部関数としての使用を想定
;-------------------------------------------------
@_SHOOTING(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "なし/드라이사정/사정/대량사정/조루/전립선사정", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_SHOOTING
;概　要:ARGの사정種を취득する
;;引　数:ARG…キャラ등록번호。省略するとTARGET
;戻り値:사정種を表す文字列
;備　考:式中関数
;-------------------------------------------------
@GET_SHOOTING(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SHOOTING(TCVAR:ARG:사정)

;-------------------------------------------------
;関数名:SHOOTING
;概　要:ARGSが指定했다사정種なら真을반환
;引　数:ARG…キャラ번호
;引　数:ARGS…사정種
;戻り値:その상태なら1、違えば0
;備　考:式中関数
;-------------------------------------------------
@SHOOTING(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SHOOTING(TCVAR:ARG:사정)

;単純にMILKで良い気が했다けどどこかで被りそうな気も했다のでSPLAYING!
;-------------------------------------------------
;関数名:_SPLAYING_MILK
;概　要:분유種から文字列を취득する関数
;引　数:ARG…(TCVAR:분유)
;戻り値:사정種を表す文字列
;備　考:式中関数。内部関数としての使用を想定
;-------------------------------------------------
@_SPLAYING_MILK(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "なし/空분유/분유/大量분유", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_SPLAYING_MILK
;概　要:ARGの분유種を취득する
;;引　数:ARG…キャラ등록번호。省略するとTARGET
;戻り値:분유種を表す文字列
;備　考:式中関数
;-------------------------------------------------
@GET_SPLAYING_MILK(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SHOOTING(TCVAR:ARG:분유)

;-------------------------------------------------
;関数名:SPLAYING_MILK
;概　要:ARGSが指定했다분유種なら真을반환
;引　数:ARG…キャラ번호
;引　数:ARGS…분유種
;戻り値:その상태なら1、違えば0
;備　考:式中関数
;-------------------------------------------------
@SPLAYING_MILK(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SPLAYING_MILK(TCVAR:ARG:분유)

;FEMALE_EJACULATIONとかSHIOFUKI等は論外だと思われるのでSQUIRTING!
;-------------------------------------------------
;関数名:_SQUIRTING
;概　要:시오후키から文字列を취득する関数
;引　数:ARG…(TCVAR:시오후키)
;戻り値:시오후키種を表す文字列
;備　考:式中関数。内部関数としての使用を想定
;-------------------------------------------------
@_SQUIRTING(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "なし/시오후키/大量시오후키", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_SQUIRTING
;概　要:ARGの시오후키種を취득する
;;引　数:ARG…キャラ등록번호。省略するとTARGET
;戻り値:시오후키種を表す文字列
;備　考:式中関数
;-------------------------------------------------
@GET_SQUIRTING(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SQUIRTING(NOWEX:ARG:시오후키)

;-------------------------------------------------
;関数名:SQUIRTING
;概　要:ARGSが指定했다시오후키種なら真을반환
;引　数:ARG…キャラ번호
;引　数:ARGS…시오후키種
;戻り値:その상태なら1、違えば0
;備　考:式中関数
;-------------------------------------------------
@SQUIRTING(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SQUIRTING(TCVAR:ARG:시오후키)

;-------------------------------------------------
;関数名:_DRY_CUM
;概　要:드라이사정種から文字列を취득する関数
;引　数:ARG…(TCVAR:드라이사정)
;戻り値:드라이사정種を表す文字列
;備　考:式中関数。内部関数としての使用を想定
;-------------------------------------------------
@_DRY_CUM(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "なし/드라이사정/前立腺", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_DRY_CUM
;概　要:ARGの드라이사정種を취득する
;;引　数:ARG…キャラ등록번호。省略するとTARGET
;戻り値:드라이사정種を表す文字列
;備　考:式中関数
;-------------------------------------------------
@GET_DRY_CUM(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _DRY_CUM(TCVAR:ARG:드라이사정)

;-------------------------------------------------
;関数名:DRY_CUM
;概　要:ARGSが指定했다드라이사정種なら真을반환
;引　数:ARG…キャラ번호
;引　数:ARGS…드라이사정種
;戻り値:その상태なら1、違えば0
;備　考:式中関数
;-------------------------------------------------
@DRY_CUM(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _DRY_CUM(TCVAR:ARG:드라이사정)
