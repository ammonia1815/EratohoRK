;==================================================
;
;	절정계 참조 함수 두는 곳
;	
;==================================================
;-------------------------------------------------
;함수명:_SHOOTING
;개  요점:사정종으로부터 캐릭터 라인을취득 하는 함수
;인  수:ARG…(TCVAR:사정)
;반환값:사정종을 나타내는 캐릭터 라인
;비  고:식 안의 함수. 내부 함수로서의 사용을 상정
;-------------------------------------------------
@_SHOOTING(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "없음/드라이사정/사정/대량사정/조루/전립선사정", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;함수명:GET_SHOOTING
;개  요점:ARG의사정종을취득 한다
;;인  수:ARG… 캐릭터등록번호. 생략 하면 TARGET
;반환값:사정종을 나타내는 캐릭터 라인
;비  고:식 안의 함수
;-------------------------------------------------
@GET_SHOOTING(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SHOOTING(TCVAR:ARG:사정)

;-------------------------------------------------
;함수명:SHOOTING
;개  요점:ARGS가 지정했다사정종이라면 진을반환
;인  수:ARG… 캐릭터번호
;인  수:ARGS… 사정종
;반환값:그상태라면 1, 다르면 0
;비  고:식 안의 함수
;-------------------------------------------------
@SHOOTING(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SHOOTING(TCVAR:ARG:사정)

;단순하게 MILK로 좋은 기분이했다지만 어디선가 입을 것 같은 기분도했다의 것으로 SPLAYING!
;-------------------------------------------------
;함수명:_SPLAYING_MILK
;개  요점:분유종으로부터 캐릭터 라인을취득 하는 함수
;인  수:ARG…(TCVAR:분유)
;반환값:사정종을 나타내는 캐릭터 라인
;비  고:식 안의 함수. 내부 함수로서의 사용을 상정
;-------------------------------------------------
@_SPLAYING_MILK(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "없음/하늘분유/분유/대량분유", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;함수명:GET_SPLAYING_MILK
;개  요점:ARG의분유종을취득 한다
;;인  수:ARG… 캐릭터등록번호. 생략 하면 TARGET
;반환값:분유종을 나타내는 캐릭터 라인
;비  고:식 안의 함수
;-------------------------------------------------
@GET_SPLAYING_MILK(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SHOOTING(TCVAR:ARG:분유)

;-------------------------------------------------
;함수명:SPLAYING_MILK
;개  요점:ARGS가 지정했다분유종이라면 진을반환
;인  수:ARG… 캐릭터번호
;인  수:ARGS… 분유종
;반환값:그상태라면 1, 다르면 0
;비  고:식 안의 함수
;-------------------------------------------------
@SPLAYING_MILK(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SPLAYING_MILK(TCVAR:ARG:분유)

;FEMALE_EJACULATION라든지 SHIOFUKI등은 논외라고 생각되므로 SQUIRTING!
;-------------------------------------------------
;함수명:_SQUIRTING
;개  요점:시오후키로부터 캐릭터 라인을취득 하는 함수
;인  수:ARG…(TCVAR:시오후키)
;반환값:시오후키종을 나타내는 캐릭터 라인
;비  고:식 안의 함수. 내부 함수로서의 사용을 상정
;-------------------------------------------------
@_SQUIRTING(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "없음/시오후키/대량시오후키", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;함수명:GET_SQUIRTING
;개  요점:ARG의시오후키종을취득 한다
;;인  수:ARG… 캐릭터등록번호. 생략 하면 TARGET
;반환값:시오후키종을 나타내는 캐릭터 라인
;비  고:식 안의 함수
;-------------------------------------------------
@GET_SQUIRTING(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SQUIRTING(NOWEX:ARG:시오후키)

;-------------------------------------------------
;함수명:SQUIRTING
;개  요점:ARGS가 지정했다시오후키종이라면 진을반환
;인  수:ARG… 캐릭터번호
;인  수:ARGS… 시오후키종
;반환값:그상태라면 1, 다르면 0
;비  고:식 안의 함수
;-------------------------------------------------
@SQUIRTING(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SQUIRTING(TCVAR:ARG:시오후키)

;-------------------------------------------------
;함수명:_DRY_CUM
;개  요점:드라이사정종으로부터 캐릭터 라인을취득 하는 함수
;인  수:ARG…(TCVAR:드라이사정)
;반환값:드라이사정종을 나타내는 캐릭터 라인
;비  고:식 안의 함수. 내부 함수로서의 사용을 상정
;-------------------------------------------------
@_DRY_CUM(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "없음/드라이사정/전립선", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;함수명:GET_DRY_CUM
;개  요점:ARG의드라이사정종을취득 한다
;;인  수:ARG… 캐릭터등록번호. 생략 하면 TARGET
;반환값:드라이사정종을 나타내는 캐릭터 라인
;비  고:식 안의 함수
;-------------------------------------------------
@GET_DRY_CUM(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _DRY_CUM(TCVAR:ARG:드라이사정)

;-------------------------------------------------
;함수명:DRY_CUM
;개  요점:ARGS가 지정했다드라이사정종이라면 진을반환
;인  수:ARG… 캐릭터번호
;인  수:ARGS… 드라이사정종
;반환값:그상태라면 1, 다르면 0
;비  고:식 안의 함수
;-------------------------------------------------
@DRY_CUM(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _DRY_CUM(TCVAR:ARG:드라이사정)
