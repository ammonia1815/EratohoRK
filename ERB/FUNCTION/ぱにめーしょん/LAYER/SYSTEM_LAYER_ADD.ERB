;-------------------------------------------------------------------------
;LAYER 관계 함수군의 개인적인 오리지날 요소 추가 분의 통계
;-------------------------------------------------------------------------
;초기설정에 버튼 정보의 초기화를 추가했다함수
;-------------------------------------------------------------------------
@INIT_LAYER, ARGS = "`"
LAYER_BUTTON = %ARGS%
CALL LAYER_INIT, ARGS
RETURN RESULT
;-------------------------------------------------------------------------
;INPUT 가능한 캐릭터 라인인가 어떤가(버튼으로서 표시당했다인가 어떤가)를취득 한다
;다만, HTML_ADDBUTTON를 이용하고 있지 않는 경우는 무효
;표시되어 있지 않은 것을 입력하는 가능하게 하는 경우는 ADD_LAYER_BUTTON로 직접 기술하는 것
;-------------------------------------------------------------------------
@IS_ABLE_LAYER_INPUT, ARGS
#FUNCTION
RETURNF STRFIND(LAYER_BUTTON, NEST_SEPARATOR + ARGS + NEST_SEPARATOR) >= 0

@ADD_LAYER_BUTTON, ARGS
CALLF ADD_LAYER_BUTTONF, ARGS

@ADD_LAYER_BUTTONF, ARGS
#FUNCTION
LAYER_BUTTON += ARGS
LAYER_BUTTON += NEST_SEPARATOR