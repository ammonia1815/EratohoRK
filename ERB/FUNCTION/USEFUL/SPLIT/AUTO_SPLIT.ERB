;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	Module		:AUTO_SPLIT.ERB
;	Facility	:引数として渡당했다ARGSから、特定の文字列ARGS:1で区切った場合のARG番目の文字列을반환関数@AUTO_SPLIT
;
;	Licence		:ライセンスフリー。
;
;	Modification Data:
;
;	Edit	Date			Author					Reason
;	001		2011/09/25		Ｎ鳥					新規作成
;	002		2013/01/09		ぱ。					RESULTの保護
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;@AUTO_SPLIT
;ARGS   操作する文字列
;ARGS:1 ARGSを区切る文字列
;ARG 　 ARGSを区切った後で、操作する箇所。ARGS:2が指定されている場合、発見했다ARGS:2の場所+ARG番目の文字列을반환。
;ARGS:2 検索する文字列
;================================================
@AUTO_SPLIT(ARGS , ARGS:1 , ARG = 0 , ARGS:2 = "")
#FUNCTIONS
#LOCALSSIZE 200
#LOCALSIZE 2
VARSET LOCALS
SPLIT ARGS , ARGS:1 , LOCALS , LOCAL:1
IF ARGS:2 != ""
	LOCAL = FINDELEMENT(LOCALS , ARGS:2 , , , 1)
	IF LOCAL != -1
		SIF ARG + LOCAL < 0
;OriginalString : エラー：関数@AUTO_SPLITにて{LOCAL}番目の文字列に%ARGS:2%を発見しま했다が、第三引数{ARG}の指定が不正です。
			PRINTFORML 에러：함수@AUTO_SPLIT에서{LOCAL}번째의 캐릭터 라인에%ARGS:2%(을)를 발견했습니다만, 제3 인수{ARG}의 지정이 부정합니다.
		RETURNF LOCALS:(LOCAL + ARG)
	ELSE
		RETURNF
	ENDIF
ELSE
	SIF ARG < 0
;OriginalString : エラー：関数@AUTO_SPLITの第三引数として{ARG}が渡されま했다
		PRINTFORML 에러：함수@AUTO_SPLIT의 제3 인수로서{ARG}하지만 건네받았습니다
	RETURNF LOCALS:ARG
ENDIF
