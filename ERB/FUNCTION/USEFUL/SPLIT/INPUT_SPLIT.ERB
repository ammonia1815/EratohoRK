;-------------------------------------------------
;스프릿트인풋트 함수@INPUT_SPLIT
;수치의 입력 대기를 합니다. 인수에 포함되지 않는 숫자를 선택하면 자동적으로 입력 대기를 다시 합니다
;유효한 숫자가 입력당했다등 그것을 RESULT에 돌려주어 돌아옵니다
;
;ARGS		통하는 버튼의 정의
;ARGS:1		단락 기호
;ARGS;2		STR형이나 INT형인가의 지정. 생략했다경우는 INT형이 가능하면 INT형, 그렇지 않으면 STR형이 작성된다.
;-------------------------------------------------
@INPUT_SPLIT, ARGS, ARGS:1 = "/", ARGS:2
#LOCALSIZE 1
#LOCALSSIZE 200
#DIM 요소수
#DIM LINE
#DIM IS_ONEINPUT
#DIM IS_INT
#DIM 원RESULT
#DIMS 원RESULTS
원RESULTS = %RESULTS%
원RESULT = RESULT
LINE = LINECOUNT
SPLIT ARGS, ARGS:1, LOCALS
요소수 = RESULT
IS_ONEINPUT = 1
FOR LOCAL, 0, 요소수
	SIF STRLENS(LOCALS:LOCAL) < 2
		CONTINUE
	IS_ONEINPUT = 0
	BREAK
NEXT
IF ARGS:2 == ""
	ARGS:2 = INT
	FOR LOCAL, 0, 요소수
		SIF ISNUMERIC(LOCALS:LOCAL)
			CONTINUE
		ARGS:2 = STR
		BREAK
	NEXT
ENDIF
$START
IF ARGS:2 == "STR"
	IF IS_ONEINPUT
		ONEINPUTS
	ELSE
		INPUTS
	ENDIF
ELSE
	IF IS_ONEINPUT
		ONEINPUT
	ELSE
		INPUT
	ENDIF
ENDIF

IF ARGS:2 == "STR"
	SIF FINDELEMENT(LOCALS , ESCAPE(RESULTS), 0, 요소수, 1) >= 0
		RETURN 원RESULT
ELSE
	IF FINDELEMENT(LOCALS , TOSTR(RESULT), 0, 요소수, 1) >= 0
		RESULTS = %원RESULTS%
		RETURN RESULT
	ENDIF
ENDIF
CLEARLINE LINECOUNT - LINE
GOTO START
