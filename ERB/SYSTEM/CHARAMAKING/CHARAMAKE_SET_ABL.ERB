;ABL 생성

;순종
@CM_SET_ABL_0
;기본 판정치 10
LOCAL = 10
;소질 판정치를 이용
LOCAL += CM_TALENT_MAKE("프라이드역", "피학", "솔직야", "솔직야", "모랄", "수치역", "공격역", "지성역", "신경질역")
;솔직라면 판정치+50
SIF TALENT:MASTER:솔직
	LOCAL += 50
;반항적라면 판정치-50
SIF TALENT:MASTER:반항적
	LOCAL -= 50
;겁쟁이라면 판정치+30
SIF TALENT:MASTER:겁쟁이
	LOCAL += 30
;굳셈라면 판정치-30
SIF TALENT:MASTER:굳셈
	LOCAL -= 30
;작은체형라면 판정치+30
SIF TALENT:MASTER:작은체형
	LOCAL += 30
;얌전한와 판정치+20
SIF TALENT:MASTER:얌전한
	LOCAL += 20
;건방짐라면 판정치-40
SIF TALENT:MASTER:건방짐
	LOCAL -= 40
;프라이드높음와 판정치-50
SIF TALENT:MASTER:프라이드높음
	LOCAL -= 50
;프라이드낮음라면 판정치+25
SIF TALENT:MASTER:프라이드낮음
	LOCAL += 25
;유치라면 판정치+25
SIF TALENT:MASTER:유치
	LOCAL += 25
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:순종++
	LOCAL /= 2
NEXT

;욕망
@CM_SET_ABL_1
;기본 판정치 10
LOCAL = 10
;소질 판정치를 이용
LOCAL += CM_TALENT_MAKE("피학", "충동", "호기심", "수치", "낙관", "도착", "동성애", "모랄역")
;처녀·동정라면 판정치+30
SIF TALENT:MASTER:처녀 || TALENT:MASTER:동정
	LOCAL += 30
;겁쟁이라면 판정치-20
SIF TALENT:MASTER:겁쟁이
	LOCAL -= 20
;굳셈라면 판정치-20
SIF TALENT:MASTER:굳셈
	LOCAL -= 20
;작은체형라면 판정치-20
SIF TALENT:MASTER:작은체형
	LOCAL -= 20
;자제심라면 판정치-50
SIF TALENT:MASTER:자제심
	LOCAL -= 50
;충동적라면 판정치+30
SIF TALENT:MASTER:충동적
	LOCAL += 30
;호기심라면 판정치+50
SIF TALENT:MASTER:호기심
	LOCAL += 50
;감정결여라면 판정치-50
SIF TALENT:MASTER:감정결여
	LOCAL -= 50
;정서적라면 판정치+20
SIF TALENT:MASTER:정서적
	LOCAL += 20
;일선을넘지않음라면 판정치-40
SIF TALENT:MASTER:일선을넘지않음
	LOCAL -= 40
;정조관념라면 판정치-30
SIF TALENT:MASTER:정조관념
	LOCAL -= 30
;정조무관심라면 판정치+30
SIF TALENT:MASTER:정조무관심
	LOCAL += 30
;쾌감을부정라면 판정치-50
SIF TALENT:MASTER:쾌감을부정
	LOCAL -= 50
;쾌감에솔직라면 판정치+50
SIF TALENT:MASTER:쾌감에솔직
	LOCAL += 50
;변태적라면 판정치+30
SIF TALENT:MASTER:변태적
	LOCAL += 30
;자위하기쉬움라면 판정치+50
SIF TALENT:MASTER:자위하기쉬움
	LOCAL += 50
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:욕망++
	LOCAL /= 2
NEXT

;기교
@CM_SET_ABL_2
;기본 판정치는성교경험*3+수음경험*5+구음경험*5+자위경험*2/5
LOCAL = ((EXP:MASTER:성교경험*3)+(EXP:MASTER:수음경험*5)+(EXP:MASTER:구음경험*5)+(EXP:MASTER:자위경험*2))/5

;처녀·동정라면 판정치-30
SIF TALENT:MASTER:처녀 || TALENT:MASTER:동정
	LOCAL -= 30
;습득빠름라면 판정치+30
SIF TALENT:MASTER:습득빠름
	LOCAL += 30
;습득느림라면 판정치-30
SIF TALENT:MASTER:습득느림
	LOCAL -= 30
;수줍음라면 판정치-20
SIF TALENT:MASTER:수줍음
	LOCAL -= 20
;혀기술라면 판정치+30
SIF TALENT:MASTER:혀기술
	LOCAL += 30
;손가락기술라면 판정치+30
SIF TALENT:MASTER:손가락기술
	LOCAL += 30
;헌신적라면 판정치+20
SIF TALENT:MASTER:헌신적
	LOCAL += 20
;수동적라면 판정치-20
SIF TALENT:MASTER:수동적
	LOCAL -= 20
;새드라면 판정치+50
SIF TALENT:MASTER:새드
	LOCAL += 50
;일선을넘지않음라면 판정치+20
SIF TALENT:MASTER:일선을넘지않음
	LOCAL += 20
;심술궂음라면 판정치+20
SIF TALENT:MASTER:심술궂음
	LOCAL += 20
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:기교++
	LOCAL /= 2
NEXT

;Ｃ감각
@CM_SET_ABL_3
;기본 판정치는Ｃ경험를 3으로 나눈다
LOCAL = EXP:MASTER:Ｃ경험/3
;이 시점에서 0이라면 종료
SIF !LOCAL
	RETURN 0

;Ｃ민감라면 판정치+30
SIF TALENT:MASTER:Ｃ민감
	LOCAL += 30
;Ｃ둔감라면 판정치-30
SIF TALENT:MASTER:Ｃ둔감
	LOCAL -= 30
;페니스가 있다고 판정치-30
SIF PENIS(MASTER)
	LOCAL -= 30
	
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:Ｃ감각++
	LOCAL /= 2
NEXT

;Ｖ감각
@CM_SET_ABL_4
;기본 판정치는Ｖ경험
LOCAL = EXP:MASTER:Ｖ경험
;이 시점에서 0이라면 종료
SIF !LOCAL
	RETURN 0

;Ｖ민감라면 판정치+30
SIF TALENT:MASTER:Ｖ민감
	LOCAL += 30
;Ｖ둔감라면 판정치-30
SIF TALENT:MASTER:Ｖ둔감
	LOCAL -= 30
	
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:Ｖ감각++
	LOCAL /= 2
NEXT

;Ａ감각
@CM_SET_ABL_5
;기본 판정치는Ａ경험
LOCAL = EXP:MASTER:Ａ경험
;이 시점에서 0이라면 종료
SIF !LOCAL
	RETURN 0

;Ａ민감라면 판정치+30
SIF TALENT:MASTER:Ａ민감
	LOCAL += 30
;Ａ둔감라면 판정치-30
SIF TALENT:MASTER:Ａ둔감
	LOCAL -= 30
	
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:Ａ감각++
	LOCAL /= 2
NEXT

;Ｂ감각
@CM_SET_ABL_6
;기본 판정치는Ｂ경험
LOCAL = EXP:MASTER:Ｂ경험
;이 시점에서 0이라면 종료
SIF !LOCAL
	RETURN 0

;Ｂ민감라면 판정치+30
SIF TALENT:MASTER:Ｂ민감
	LOCAL += 30
;Ｂ둔감라면 판정치-30
SIF TALENT:MASTER:Ｂ둔감
	LOCAL -= 30
	
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:Ｂ감각++
	LOCAL /= 2
NEXT

;봉사정신
@CM_SET_ABL_7
;기본 판정치는수음경험+구음경험
LOCAL = EXP:MASTER:수음경험+EXP:MASTER:구음경험

;습득빠름라면 판정치+10
SIF TALENT:MASTER:습득빠름
	LOCAL += 10
;습득느림라면 판정치-10
SIF TALENT:MASTER:습득느림
	LOCAL -= 10
;수줍음라면 판정치-20
SIF TALENT:MASTER:수줍음
	LOCAL -= 20
;혀기술라면 판정치+10
SIF TALENT:MASTER:혀기술
	LOCAL += 10
;손가락기술라면 판정치+10
SIF TALENT:MASTER:손가락기술
	LOCAL += 10
;헌신적라면 판정치+50
SIF TALENT:MASTER:헌신적
	LOCAL += 50
;수동적라면 판정치-20
SIF TALENT:MASTER:수동적
	LOCAL -= 20
;새드라면 판정치-50
SIF TALENT:MASTER:새드
	LOCAL -= 50
;일선을넘지않음라면 판정치+20
SIF TALENT:MASTER:일선을넘지않음
	LOCAL += 20
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:봉사정신++
	LOCAL /= 2
NEXT

;노출증
@CM_SET_ABL_8
;기본 판정치는노출경험의 3배
LOCAL = EXP:MASTER:노출경험*3
;이 시점에서 0이라면 종료
SIF !LOCAL
	RETURN 0

;변태적라면 판정치+30
SIF TALENT:MASTER:변태적
	LOCAL += 30
;자제심라면 판정치-30
SIF TALENT:MASTER:자제심
	LOCAL -= 30
	
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:노출증++
	LOCAL /= 2
NEXT

;레즈끼
@CM_SET_ABL_9
;기본 판정치는레즈경험의 2배
LOCAL = EXP:MASTER:레즈경험*2
;이 시점에서 0이라면 종료
SIF !LOCAL
	RETURN 0

;바이라면 판정치+30
SIF TALENT:MASTER:바이
	LOCAL += 30
;남성혐오라면 판정치+30
SIF TALENT:MASTER:남성혐오
	LOCAL += 30
	
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:레즈끼++
	LOCAL /= 2
NEXT

;ＢＬ끼
@CM_SET_ABL_10
;우선 생성하지 않는다

;마조끼
@CM_SET_ABL_11
;기본 판정치는노출경험-50
LOCAL = EXP:MASTER:노출경험-50
;소질 판정치를 이용
LOCAL += CM_TALENT_MAKE("피학", "피학", "공격역", "도착", "낙관역")
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:마조끼++
	LOCAL /= 2
NEXT

;중독계는 어느 것도죠교요소로서 생성하지 않는다

;회화의 스킬
@CM_SET_ABL_20
;소질 판정치를 반으로 해 이용
LOCAL = CM_TALENT_MAKE("공격역", "프라이드역", "충동역", "수치역", "지성", "지성", "결벽", "모랄", "신경질", "신경질")/2
;솔직라면 판정치+10
SIF TALENT:MASTER:습득빠름
	LOCAL += 10
;얌전한라면 판정치+10
SIF TALENT:MASTER:습득느림
	LOCAL += 10
;자제심라면 판정치+10
SIF TALENT:MASTER:자제심
	LOCAL += 10
;수동적라면 판정치-20
SIF TALENT:MASTER:수동적
	LOCAL -= 20
;충동적라면 판정치-20
SIF TALENT:MASTER:충동적
	LOCAL -= 20
;감정결여라면 판정치-50
SIF TALENT:MASTER:감정결여
	LOCAL -= 50
;정서적라면 판정치+20
SIF TALENT:MASTER:정서적
	LOCAL += 20
;친절함라면 판정치+20
SIF TALENT:MASTER:친절함
	LOCAL += 20
;까다로움라면 판정치-40
SIF TALENT:MASTER:까다로움
	LOCAL -= 40
;일선을넘지않음라면 판정치+20
SIF TALENT:MASTER:일선을넘지않음
	LOCAL += 20

;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:회화++
	LOCAL /= 2
NEXT

;애무의 스킬
@CM_SET_ABL_21
;기본 판정치는성교경험*3+수음경험*5+구음경험*5+자위경험*2/5
LOCAL = ((EXP:MASTER:성교경험*3)+(EXP:MASTER:수음경험*5)+(EXP:MASTER:구음경험*5)+(EXP:MASTER:자위경험*2))/5

;처녀·동정라면 판정치-30
SIF TALENT:MASTER:처녀 || TALENT:MASTER:동정
	LOCAL -= 30
;습득빠름라면 판정치+30
SIF TALENT:MASTER:습득빠름
	LOCAL += 30
;습득느림라면 판정치-30
SIF TALENT:MASTER:습득느림
	LOCAL -= 30
;수줍음라면 판정치-20
SIF TALENT:MASTER:수줍음
	LOCAL -= 20
;혀기술라면 판정치+30
SIF TALENT:MASTER:혀기술
	LOCAL += 30
;손가락기술라면 판정치+30
SIF TALENT:MASTER:손가락기술
	LOCAL += 30
;헌신적라면 판정치+20
SIF TALENT:MASTER:헌신적
	LOCAL += 20
;수동적라면 판정치-20
SIF TALENT:MASTER:수동적
	LOCAL -= 20
;새드라면 판정치+50
SIF TALENT:MASTER:새드
	LOCAL += 50
;일선을넘지않음라면 판정치+20
SIF TALENT:MASTER:일선을넘지않음
	LOCAL += 20
;심술궂음라면 판정치+20
SIF TALENT:MASTER:심술궂음
	LOCAL += 20
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:애무++
	LOCAL /= 2
NEXT

;도구의 스킬
@CM_SET_ABL_22
;기본 판정치는도구사용경험의 5배
LOCAL = EXP:MASTER:도구사용경험*5
;이 시점에서 0이라면 종료
SIF !LOCAL
	RETURN 0

;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:도구++
	LOCAL /= 2
NEXT

;성교의 스킬
@CM_SET_ABL_23
;기본 판정치는성교경험
LOCAL = EXP:MASTER:성교경험
;이 시점에서 0이라면 종료
SIF !LOCAL
	RETURN 0

;허리기술라면 판정치+30
SIF TALENT:MASTER:허리기술
	LOCAL += 30
;작은체형라면 판정치-30
SIF TALENT:MASTER:작은체형
	LOCAL -= 30
	
;3회 돌린다
FOR LOCAL:1, 0, 3
	;가산 실패했다등 종료
	SIF !PERCENT(LOCAL)
		BREAK
	ABL:MASTER:성교++
	LOCAL /= 2
NEXT

;수치·봉사·가학·이상·사역의 스킬은죠교요소로서 생성하지 않는다
;일반의 엣치해봉사라는 말투 하지 않기도 하고…