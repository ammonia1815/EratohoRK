;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	히나구상 고통系反応、汎用リアクション구상ファイル
;
;Version		Reason
;1.11			COMファイルを系統別に分割
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;=====================================================================
;고통系反応
;=====================================================================
;・가학系행동や삽입を伴う各種행동で見られる
;・v1.010現在、고통系に의존도변화の処理なし
;-------------------------------------------------

;──────────────────────────────────
;COM30, 고통을 참는다
;──────────────────────────────────
;・REACT분류:소극적으로 한다
;・REACT인상:好印象小
;・SOURCE:고통が無い場合は弾かれる
;-------------------------------------------------
;・トリガーとなる행동、ソース、パラメーター
;・SOURCE:고통/구속
;-------------------------------------------------
;선택可能な条件…
;ACT분류:애무
;	ACT:가슴애무/입으로 애무/항문애무/항문펠라/키스/파이즈리한다
;ACT분류:도구
;	ACT:바이브/항문바이브/항문비즈
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트
;-------------------------------------------------
;・TFLAG:REACT파생(0=고통を참다/1=痛いけど기분 좋다!)
;-------------------------------------------------
@KOJO_MALE_COM_K39_30
;-------------------------------------------------
VARSET LOCAL
LOCAL:0 = GET_KFLAG_K39("상사상애")
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생１：痛いけど기분 좋다!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
	SELECTCASE GET_COM_RANK()
		CASE "自然な행동"
			PRINTFORMW 「다만 아프다고 만\@ LOCAL:0 ? (이)가 아닌 것 같구나 # 는 아닌 것 같네요 \@…」
		CASE "無理のない행동"
			PRINTFORMW 「후후, 그 소리……다만 아파에 참고 있다\@ LOCAL:0 ? 라는 것이 아닌 것 같구나 # 그렇다고 할 것은 아닌 것 같네요 \@」
		CASE "無理を했다행동"
			PRINTFORMW 「혹시,%CNAME(MASTER, TARGET)%……조금 자주(잘)\@ LOCAL:0 ? 되어 버렸다든가 # 되어 버린 것입니까 \@？」
	ENDSELECT
	PRINTFORM 「\@ LOCAL:0 ? 이봐요…어때？ 속이지 말고, 정직하게 말해 봐 # 후후, 괜찮아요. 이런 일로 경멸했다는 하지 않기 때문에 \@…
	CALL PRINT_SYMBOL_K39
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생０：고통を참다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;=================================================
	;無理を했다행동
	;=================================================
	IF GET_COM_RANK() == "無理を했다행동"
		PRINTFORMW 「아프다면 참지 않고 , 그렇게 말하면 좋다\@ LOCAL:0 ? 의에 # (와)과 사%RAND_SPLIT("있습니다만/우노입니다만")% \@…」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 조금 무리해\@ LOCAL:0 ? 없다 # 전과 \@？」
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류：애무(수음/貝あわせ/스마타발/コキ以外)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "애무")
		;상사상애
		IF LOCAL:0
			PRINTFORMW 「지금의가 아팠어？」
			PRINTFORMW 「이제 그렇게 서투르지 않을 것이지만…」
		;その他
		ELSE
			SELECTCASE RANDCOM(2, 100)
				CASE 0
					PRINTFORMW 「 면없음 있고, 조금 아팠는지요？」
				CASE 1
					PRINTFORMW 「조심하고 한 생각이었던 것입니다만」
					PRINTFORMW 「아파하게 해 버린 것 같네요…」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류：도구(바이브/Ａ바이브/Ａ비즈のみ)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "도구")
		;상사상애
		IF LOCAL:0
			SELECTCASE RANDCOM(2, 200)
				CASE 0
					PRINTFORMW 「좀 더 친숙해 지게 할 필요가 있는 것 같구나…」
				CASE 1
					PRINTFORMW 「코레 사용하는 것은 아직 빨랐다 같구나…？」
					PRINTFORMW 「알았다. 좀 더 길들이고 나서 시라고 준다♪」
			ENDSELECT
		;それ以外
		ELSE
			SELECTCASE RANDCOM(2, 201)
				CASE 0
					PRINTFORMW 「 아직 아픔이 있을까요？」
					PRINTFORMW 「친숙해 질 때까지 좀 더 걸릴 것 같네요…」
				CASE 1
					PRINTFORMW 「이것을 사용하는 것은 조금 너무 빨랐는지요…」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류：성교(성교系全て)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "성교")
		;상사상애
		IF LOCAL:0
			PRINTFORML 「 아직 아픔이 있어？」
			PRINTFORMW 「좀 더 친숙해 지게 할 필요가 있는 것 같구나…」
		;それ以外
		ELSE
			PRINTFORML 「 아직 아픔이 있을까요？」
			PRINTFORMW 「친숙해 질 때까지 좀 더 걸릴 것 같네요…」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류：봉사(이라마치오のみ？)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "봉사")
		IF 0
		ELSEIF IS_NOWACTNAME("이라마치오")
			;상사상애
			IF LOCAL:0
				PRINTFORMW 「응, 괴로운거야？ 호흡을 할 수 없는거야？」
				PRINTFORMW 「나쁘다고 알고 있어도, 그러한 얼굴 되면(자)……무섭게 흥분해버린다…」
			;それ以外
			ELSE
				PRINTFORMW 「역시 후오까지 찔리는 것은 괴로울까요」
				PRINTFORMW 「 그렇지만 흥분하네요, 이것……」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류：가학(매도以外)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "가학")
		;상사상애
		IF LOCAL:0
			PRINTFORMW 「……후후후, 뭐라고 하는 사랑스러운거야…」
			PRINTFORMW 「아, 나다…, 조금 젖어 와 버렸다…」
		;それ以外
		ELSE
			SELECTCASE RANDCOM(3, 600)
				CASE 0
					PRINTFORMW 「꽤 노력하네요」
					PRINTFORM 「좋아요, 매우
					CALL PRINT_SYMBOL_K39
				CASE 1
					PRINTFORM 「아픔에 참는 표정이, 매우 좋아요
					CALL PRINT_SYMBOL_K39
				CASE 2
					PRINTFORMW 「어째서인가 모르는데…」
					PRINTFORMW 「%CNAME(MASTER, TARGET)%의 고민의 표정을 보면(자), 조크조크 해 버립니다…」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류：이상(フィスト系のみ)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "이상")
		PRINTFORMW 「\@ LOCAL:0 ? 내가 말하는 것도 이상한 이야기이지만 # 내가 말하는 것도 이상한 이야기입니다만 \@…」
		PRINTFORMW 「자주(잘)\@ LOCAL:0 ? 견딜 수 있어요 # 자주(잘)참다할 수 있네요 \@？」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「\@ LOCAL:0 ? 그렇게 아프면 빨리 말하면 좋은데, 라고 생각하지만 # 미안해요. 조금 아팠던 것입니까 \@…」
	RETURN 1
ENDIF
;COM30, 고통을 참는다

;──────────────────────────────────
;COM31, 비명지른다
;──────────────────────────────────
;・REACT분류:소극적으로 한다
;・REACT인상:0から遠ざかる
;-------------------------------------------------
;선택可能な条件…
;ACT분류:회화
;	ACT:엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다
;ACT분류:애무
;	ACT:가슴애무/입으로 애무/항문애무/항문펠라/키스/파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:바이브/항문바이브/항문비즈
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/매도/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트/공중육변기플레이
;-------------------------------------------------
;・TFLAG:REACT파생(0=고통による悲鳴/1=공포による悲鳴)
;-------------------------------------------------
@KOJO_MALE_COM_K39_31
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;負荷2：無理を했다행동
;━━━━━━━━━━━━━━━━━━━━━━━━
;・負荷2はまとめてしまう
;-------------------------------------------------
IF 구상記入チェック(100) && GET_COM_RANK() == "無理を했다행동"
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「일부러인것 같은 비명은%RAND_SPLIT("정말/")%귀에 거슬리는 소리인 만일 수 있는」
		PRINTFORMW 「나쁜데……, (들)물으면 알아버린다 것이군요. 그러한 것…」
	ELSE
		PRINTFORMW 「무리해서까지 비명을 올릴 필요가 있습니까？」
		PRINTFORMW 「%RAND_SPLIT("후후…,/")%알아 버립니다. 그러한 것」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생１：공포による悲鳴
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:REACT파생 == 1 && TFLAG:REACT부하 < 2
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「아무튼…, 나에게도 책임이 있겠지요지만……」
		PRINTFORMW 「정직,%RANDTEXT_K39("今になって", "そんな風に")%무서워해지면(자), 조금 낙담하는데%RANDTEXT_K39("ね", "＿")%？」
	ELSE
		;대상限定:남자なんでここでTALENTは参照しない
		PRINTFORMW 「%RANDTEXT_K39("男の癖に", "＿")%비명을 올리는만큼 무섭습니까？ 다만, 그것이…」
		PRINTFORMW 「이 나에 대해서인가, 지금의 상황에 대해서인가」
		PRINTFORMW 「어쩌면, 그 양쪽 모두라든지？ 조금 기분으로는 됩니다%RANDTEXT_K39("ね", "＿")%\@ TENSION(TARGET, 8) != "비싸다" ? … # \@」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생０：고통による悲鳴
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생 && TFLAG:REACT부하 < 2
	IF 0
	ELSEIF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「아, 이제！ 조크조크 해버린다…」
		PRINTFORMW 「응, 좀 더 듣게 해…」
	ELSEIF AFFECTION("익애")
		PRINTFORMW 「상당히 단 소리로 웁니다」
		PRINTFORMW 「조금 흥분해 왔습니다…」
	ELSEIF AFFECTION("眷愛")
		PRINTFORMW 「좋은 느낌이군요, 좀 더 들려주세요…」
	ELSEIF AFFECTION("嫌悪")
		PRINTFORMW 「괴롭습니까？ 괴롭습니까？」
		PRINTFORMW 「 그렇지만,%CNAME(MASTER, TARGET)%가 안 돼요…」
	ELSE
		PRINTFORML 「그런 비명을 거론되면(자)…」
		PRINTFORMW 「조금 흥분해 버리네요……」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;COM32, 날뛴다
;────────────────────────────────────
;・派生によって暴れ成功(REACT분류:날뛴다)、暴れ失敗(REACT분류:소극적으로 한다)に分かれる
;・暴れが通ると도구系/성교系/성교봉사系コマンドを妨害했다扱いになる
;・징계・포인트追加
;・REACT인상:悪印象大
;-------------------------------------------------
;선택可能な条件…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/항문애무/항문펠라/키스
;		성기마찰한다/파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/항문바이브/항문비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그
;ACT분류:휴식
;	ACT:치료한다
;ACT분류:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다
;-------------------------------------------------
;・TFLAG:REACT파생(0=暴れ成功
;                   1=밧줄구속による暴れ失敗
;                    2=制止による暴れ失敗)
;-------------------------------------------------
@KOJO_MALE_COM_K39_32
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;暴れ失敗！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && INRANGE(TFLAG:REACT파생, 1, 2)
	;=================================================
	;負荷2：無理を했다행동
	;=================================================
	IF GET_COM_RANK() == "無理を했다행동"
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「무리하게 날뛰어 보이는 의미가 자주(잘) 알지 않는데…아무튼」
			{
			PRINTFORM 「상관했으면 좋아서 하고 있다고 생각하면,\@ TENSION(TARGET, 8) == "비싸다" 
;OriginalString : 可愛い # 微笑ましい \@って%RAND_SPLIT("思えなくも無い/気がしないではない")%けど、ね
			? 사랑스럽다 # 흐뭇하다 \@라고%RAND_SPLIT("생각되지 않지도 없다/생각이 들지 않지 않아")%けど、ね
			}
			CALL PRINT_SYMBOL_K39
		ELSE
			;//RAND_SPLITは("hoge/")でhogeと空文字列を返す
			PRINTFORMW 「그렇게 날뛰어 보이는 것이,%CNAME(MASTER, TARGET)%의 %RAND_SPLIT("본심/본의")%(와)과도 생각되지 않지만%RAND_SPLIT("ね/")%」
			PRINTFORMW 「다만…, 무엇이 목적으로 그런 일을 하는지, 조금 기분으로는 됩니다」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생１：밧줄구속による暴れ失敗
	;=================================================
	IF TFLAG:REACT파생 == 1
		;상사상애
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「꽤 재미있는데…」
			PRINTFORMW 「그 거 어떤 의미도 없다고 생각해요？」
		;それ以外
		ELSE
			CALL FADEIN(10, "「저, 저……」")
			PRINTFORMW 「그 상태로 날뛰어도 쓸데없다고 생각합니다만…」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생２：制止による暴れ失敗
	;=================================================
	IF TFLAG:REACT파생 == 2
		VARSET LOCALS
		LOCALS:0 '= RAND_SPLIT("暴れて/抵抗して/そんな事して")
		;=================================================
		;ACT분류：휴식
		;=================================================
		IF MENUMATCH(TFLAG:ACT, "휴식")
			;상사상애
			IF GET_KFLAG_K39("상사상애")
				PRINTFORMW 「응, 어째서 날뛰어？」
				PRINTFORMW 「의미를 모르지만…」
			;それ以外
			ELSE
				PRINTFORMW 「저, 지금 날뛰는데 어떤 의미가%RAND_SPLIT("？/있겠지요")%」
				PRINTFORMW 「\@ IS_NOWACTNAME("치료한다") ? 나에게치료되는 것이가 싫습니까？ 그렇지 않으면 # 의미 따위 없고 \@, 다만 날뛰고 싶은 것뿐 무엇이지요……」
			ENDIF
			RETURN 1
		ENDIF
		
		;---------------------
		;その他
		;---------------------
		;상사상애
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「 이제,\@ IS_CONDITION(TARGET, "분노") ? 적당히 해 # 조금 침착해 \@없음 있어라！」
			PRINTFORMW 「%LOCALS:0%도 쓸데없다고 알지 않는거야？」
		;それ以外
		ELSE
			PRINTFORMW 「안되어요, 얌전하게 해 주세요」
			PRINTFORMW 「어쨌든,%LOCALS:0%도 쓸데없어요…」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;=================================================
;REACT파생０：暴れが通った
;=================================================
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;상사상애
	IF GET_KFLAG_K39("상사상애") 
		PRINTFORML 「 이제 , 어째서 그렇게 날뛰어？」
		PRINTFORMW 「적당히 해…」
	;その他
	ELSE
		PRINTFORMW 「저, 그렇게 날뛰지 말아 주세요…」
	ENDIF
	RETURN 1
ENDIF
;COM32, 날뛴다

;──────────────────────────────────
;COM33, 무서워한다
;──────────────────────────────────
;・REACT분류:용서를 빌다
;・一定条件で분노を買い、징계・포인트追加
;-------------------------------------------------
;・トリガー이벤트制
;-------------------------------------------------
;トリガーする행동や状況…
;ACT분류:0:회화
;	ACT:지금 기분을 묻다/자신에게의 마음을 묻다/성에 관한 이야기를 하다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분나쁘게 웃다
;ACT분류:1:애무
;	ACT:풋잡하기/밀어 넘어트린다
;ACT분류:2:도구
;	ACT:항문바이브/항문비즈
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:밧줄/눈가리개/볼개그/매도/관장세트
;ACT분류:7:이상
;	ACT:방치플레이/공중육변기플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;-------------------------------------------------
;・TFLAG:REACT파생(0=ふーん/1=怯えに対する초조함)
;-------------------------------------------------
@KOJO_MALE_COM_K39_33
;-------------------------------------------------
VARSET LOCAL
LOCAL:0 = GET_KFLAG_K39("상사상애")
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생１：怯えに対する초조함
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && TFLAG:REACT파생 == 1
	;=================================================
	;負荷2かつ実は嫌
	;=================================================
	IF GET_COM_RANK() == "無理を했다행동" && !COMCOR_POSI()
		PRINTFORMW 「그러한\@ LOCAL:0 ? 눈으로 볼 수 있으면, 조금 기분이 나빠요 # 시선을 향할 수 있는 것은, 별로 좋아하지는 않습니다 \@……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「%CNAME(MASTER, TARGET)%의 그러한 모습, 너무 보고 싶고\@ LOCAL:0 ? 없네요 # 없네요 \@……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생０：무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상記入チェック(100) && !TFLAG:REACT파생
	;━━━━━━━━━━━━━━━━━━━━━━━━
	;負荷2：無理を했다행동
	;━━━━━━━━━━━━━━━━━━━━━━━━
	IF GET_COM_RANK() == "無理を했다행동"
		IF LOCAL:0
			PRINTFORMW 「응, 농담에서도%RAND_SPLIT("그러한 것/두려워해 보인다든가")%멈추었으면 좋지만？」
			PRINTFORMW 「이렇게 외관%RAND_SPLIT("끊어/라고도")%, 꽤 손상되기 때문에…」
		ELSE
			PRINTFORMW 「그…,%RAND_SPLIT("일부러/무리하게")%무서워해 보이는 일에 어떤 의미가\@ TENSION(TARGET, 8) == "高い" ? ？ # あるのでしょう \@」
			CALL FADEIN(10, "「뭐, 그렇네요……」")
			{
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의\@ COMCOR_POSI() 
;OriginalString : 怯えの대상はこの私ではなく、ちょっと違うみたいですけど。ね # 本心は解りませんが、そういう視線を向けられると傷付きます…… \@」
			? 두려움의대상는 이 나는 아니고, 조금 다른 것 같지만. 저기 # 본심은 알지 않습니다만, 그러한 시선을 향할 수 있으면 손상됩니다…… \@」
			}
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	IF 0
	ELSEIF LOCAL:0
		PRINTFORMW 「, 어째서…」
		PRINTFORMW 「응, 무서워하는 것은 그만두어…」
	ELSEIF AFFECTION("익애")
		PRINTFORMW 「무서워하다니 너무 합니다…」
	ELSEIF AFFECTION("眷愛")
		PRINTFORMW 「그렇게 무서워하지 말아 주세요…」
	ELSEIF AFFECTION("嫌悪")
		PRINTFORMW 「그렇게 무섭습니까…？」
		PRINTFORMW 「확실히 나는 액신이지만……그렇게 명백하게 무서워할 수 있으면(자) 다칩니다…」
	ELSE
		PRINTFORMW 「아무리 액신이니까는, 그렇게 무서워하는 일 없다고 생각합니다만…」
	ENDIF
	RETURN 1
ENDIF
